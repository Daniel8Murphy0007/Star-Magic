"""
QCalc_cpp_extracted.py - PhysicsTerm classes extracted from MAIN_1_CoAnQi.cpp

Auto-generated by extract_cpp_physics_terms.py
Contains ~1,239 physics term classes converted from C++ to Python.

Usage:
    from QCalc_cpp_extracted import *
    
    term = DynamicVacuumTerm()
    result = term.compute({'r': 1e6, 't': 1000, 'M': 1.989e30})
"""

import math
from typing import Dict, Any, Optional

# Import CONSTANTS from QCalc.py
try:
    from QCalc import CONSTANTS
except ImportError:
    CONSTANTS = {"M_sun": 1.989e30, "G": 6.6743e-11, "c": 2.998e8}


# ═══════════════════════════════════════════════════════════════════════════════
# BASE CLASS
# ═══════════════════════════════════════════════════════════════════════════════

class PhysicsTerm:
    """Base class for all physics terms (matching C++ interface)."""
    
    def __init__(self):
        self.name = "PhysicsTerm"
        self.description = "Base physics term"
    
    def compute(self, params: dict) -> float:
        """Override in subclasses."""
        return 0.0
    
    def get_description(self) -> str:
        return self.description
    
    def validate(self, params: dict) -> bool:
        """Validate parameters."""
        return True


# ═══════════════════════════════════════════════════════════════════════════════
# EXTRACTED PhysicsTerm CLASSES (from MAIN_1_CoAnQi.cpp)
# ═══════════════════════════════════════════════════════════════════════════════


class DynamicVacuumTerm(PhysicsTerm):
    """
    PhysicsTerm: DynamicVacuumTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "DynamicVacuumTerm"
        self.description = "PhysicsTerm: DynamicVacuumTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class QuantumCouplingTerm(PhysicsTerm):
    """
    PhysicsTerm: QuantumCouplingTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "QuantumCouplingTerm"
        self.description = "PhysicsTerm: QuantumCouplingTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class DarkMatterHaloTerm(PhysicsTerm):
    """
    PhysicsTerm: DarkMatterHaloTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "DarkMatterHaloTerm"
        self.description = "PhysicsTerm: DarkMatterHaloTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class VacuumEnergyTerm(PhysicsTerm):
    """
    PhysicsTerm: VacuumEnergyTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "VacuumEnergyTerm"
        self.description = "PhysicsTerm: VacuumEnergyTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class QuantumEntanglementTerm(PhysicsTerm):
    """
    PhysicsTerm: QuantumEntanglementTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "QuantumEntanglementTerm"
        self.description = "PhysicsTerm: QuantumEntanglementTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class CosmicNeutrinoTerm(PhysicsTerm):
    """
    PhysicsTerm: CosmicNeutrinoTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "CosmicNeutrinoTerm"
        self.description = "PhysicsTerm: CosmicNeutrinoTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class MultiSystemUQFFTerm(PhysicsTerm):
    """
    PhysicsTerm: MultiSystemUQFFTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "MultiSystemUQFFTerm"
        self.description = "PhysicsTerm: MultiSystemUQFFTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class DPMResonanceTerm(PhysicsTerm):
    """
    PhysicsTerm: DPMResonanceTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "DPMResonanceTerm"
        self.description = "PhysicsTerm: DPMResonanceTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class LENRExtendedTerm(PhysicsTerm):
    """
    PhysicsTerm: LENRExtendedTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "LENRExtendedTerm"
        self.description = "PhysicsTerm: LENRExtendedTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class SMBHAccretionTerm(PhysicsTerm):
    """
    PhysicsTerm: SMBHAccretionTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "SMBHAccretionTerm"
        self.description = "PhysicsTerm: SMBHAccretionTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class TDETerm(PhysicsTerm):
    """
    PhysicsTerm: TDETerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "TDETerm"
        self.description = "PhysicsTerm: TDETerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class NebulaUQFFTerm(PhysicsTerm):
    """
    PhysicsTerm: NebulaUQFFTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "NebulaUQFFTerm"
        self.description = "PhysicsTerm: NebulaUQFFTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class GasIonizationTerm(PhysicsTerm):
    """
    PhysicsTerm: GasIonizationTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "GasIonizationTerm"
        self.description = "PhysicsTerm: GasIonizationTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class NebulaExpansionTerm(PhysicsTerm):
    """
    PhysicsTerm: NebulaExpansionTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "NebulaExpansionTerm"
        self.description = "PhysicsTerm: NebulaExpansionTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class BuoyancyUQFFTerm(PhysicsTerm):
    """
    PhysicsTerm: BuoyancyUQFFTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "BuoyancyUQFFTerm"
        self.description = "PhysicsTerm: BuoyancyUQFFTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class InflationBuoyancyTerm(PhysicsTerm):
    """
    PhysicsTerm: InflationBuoyancyTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "InflationBuoyancyTerm"
        self.description = "PhysicsTerm: InflationBuoyancyTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class SuperconductiveTerm(PhysicsTerm):
    """
    PhysicsTerm: SuperconductiveTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "SuperconductiveTerm"
        self.description = "PhysicsTerm: SuperconductiveTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class NeutronScatteringTerm(PhysicsTerm):
    """
    PhysicsTerm: NeutronScatteringTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "NeutronScatteringTerm"
        self.description = "PhysicsTerm: NeutronScatteringTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class AstroSystemUQFFTerm(PhysicsTerm):
    """
    PhysicsTerm: AstroSystemUQFFTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "AstroSystemUQFFTerm"
        self.description = "PhysicsTerm: AstroSystemUQFFTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class DipoleVortexTerm(PhysicsTerm):
    """
    PhysicsTerm: DipoleVortexTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "DipoleVortexTerm"
        self.description = "PhysicsTerm: DipoleVortexTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class QuantumState26Term(PhysicsTerm):
    """
    PhysicsTerm: QuantumState26Term
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "QuantumState26Term"
        self.description = "PhysicsTerm: QuantumState26Term"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class TriadicScaleTerm(PhysicsTerm):
    """
    PhysicsTerm: TriadicScaleTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "TriadicScaleTerm"
        self.description = "PhysicsTerm: TriadicScaleTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class UQFFMasterTerm(PhysicsTerm):
    """
    PhysicsTerm: UQFFMasterTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "UQFFMasterTerm"
        self.description = "PhysicsTerm: UQFFMasterTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class ElectrostaticBarrierTerm(PhysicsTerm):
    """
    PhysicsTerm: ElectrostaticBarrierTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "ElectrostaticBarrierTerm"
        self.description = "PhysicsTerm: ElectrostaticBarrierTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class ElectricFieldTerm(PhysicsTerm):
    """
    PhysicsTerm: ElectricFieldTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "ElectricFieldTerm"
        self.description = "PhysicsTerm: ElectricFieldTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class NeutronProductionTerm(PhysicsTerm):
    """
    PhysicsTerm: NeutronProductionTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "NeutronProductionTerm"
        self.description = "PhysicsTerm: NeutronProductionTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class CelestialBodyTerm(PhysicsTerm):
    """
    PhysicsTerm: CelestialBodyTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "CelestialBodyTerm"
        self.description = "PhysicsTerm: CelestialBodyTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class MUGETerm(PhysicsTerm):
    """
    PhysicsTerm: MUGETerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "MUGETerm"
        self.description = "PhysicsTerm: MUGETerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class QuasarJetTerm(PhysicsTerm):
    """
    PhysicsTerm: QuasarJetTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "QuasarJetTerm"
        self.description = "PhysicsTerm: QuasarJetTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class ReactorEnergyTerm(PhysicsTerm):
    """
    PhysicsTerm: ReactorEnergyTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "ReactorEnergyTerm"
        self.description = "PhysicsTerm: ReactorEnergyTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class MagneticDipoleTerm(PhysicsTerm):
    """
    PhysicsTerm: MagneticDipoleTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "MagneticDipoleTerm"
        self.description = "PhysicsTerm: MagneticDipoleTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class MagneticJetFieldTerm(PhysicsTerm):
    """
    PhysicsTerm: MagneticJetFieldTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "MagneticJetFieldTerm"
        self.description = "PhysicsTerm: MagneticJetFieldTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class UnifiedBuoyancyTerm(PhysicsTerm):
    """
    PhysicsTerm: UnifiedBuoyancyTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "UnifiedBuoyancyTerm"
        self.description = "PhysicsTerm: UnifiedBuoyancyTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class CompressedMUGEBaseTerm(PhysicsTerm):
    """
    PhysicsTerm: CompressedMUGEBaseTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "CompressedMUGEBaseTerm"
        self.description = "PhysicsTerm: CompressedMUGEBaseTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class CompressedMUGEExpansionTerm(PhysicsTerm):
    """
    PhysicsTerm: CompressedMUGEExpansionTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "CompressedMUGEExpansionTerm"
        self.description = "PhysicsTerm: CompressedMUGEExpansionTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class SuperconductiveAdjustmentTerm(PhysicsTerm):
    """
    PhysicsTerm: SuperconductiveAdjustmentTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "SuperconductiveAdjustmentTerm"
        self.description = "PhysicsTerm: SuperconductiveAdjustmentTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class CosmologicalConstantTerm(PhysicsTerm):
    """
    PhysicsTerm: CosmologicalConstantTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "CosmologicalConstantTerm"
        self.description = "PhysicsTerm: CosmologicalConstantTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class QuantumUncertaintyTerm(PhysicsTerm):
    """
    PhysicsTerm: QuantumUncertaintyTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "QuantumUncertaintyTerm"
        self.description = "PhysicsTerm: QuantumUncertaintyTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class FluidDynamicsTerm(PhysicsTerm):
    """
    PhysicsTerm: FluidDynamicsTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "FluidDynamicsTerm"
        self.description = "PhysicsTerm: FluidDynamicsTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class DensityPerturbationTerm(PhysicsTerm):
    """
    PhysicsTerm: DensityPerturbationTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "DensityPerturbationTerm"
        self.description = "PhysicsTerm: DensityPerturbationTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class UQFFModule5Term(PhysicsTerm):
    """
    PhysicsTerm: UQFFModule5Term
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "UQFFModule5Term"
        self.description = "PhysicsTerm: UQFFModule5Term"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class ResonanceMUGETerm(PhysicsTerm):
    """
    PhysicsTerm: ResonanceMUGETerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "ResonanceMUGETerm"
        self.description = "PhysicsTerm: ResonanceMUGETerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class StateExportTerm(PhysicsTerm):
    """
    PhysicsTerm: StateExportTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "StateExportTerm"
        self.description = "PhysicsTerm: StateExportTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class TimeVaryingRotationTerm(PhysicsTerm):
    """
    PhysicsTerm: TimeVaryingRotationTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "TimeVaryingRotationTerm"
        self.description = "PhysicsTerm: TimeVaryingRotationTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class NavierStokesFluidTerm(PhysicsTerm):
    """
    PhysicsTerm: NavierStokesFluidTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "NavierStokesFluidTerm"
        self.description = "PhysicsTerm: NavierStokesFluidTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class SpacetimeMetricModulationTerm(PhysicsTerm):
    """
    PhysicsTerm: SpacetimeMetricModulationTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "SpacetimeMetricModulationTerm"
        self.description = "PhysicsTerm: SpacetimeMetricModulationTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class FullUnifiedFieldTerm(PhysicsTerm):
    """
    PhysicsTerm: FullUnifiedFieldTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "FullUnifiedFieldTerm"
        self.description = "PhysicsTerm: FullUnifiedFieldTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class ResonanceMUGE_DPMTerm(PhysicsTerm):
    """
    PhysicsTerm: ResonanceMUGE_DPMTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "ResonanceMUGE_DPMTerm"
        self.description = "PhysicsTerm: ResonanceMUGE_DPMTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class ResonanceMUGE_THzTerm(PhysicsTerm):
    """
    PhysicsTerm: ResonanceMUGE_THzTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "ResonanceMUGE_THzTerm"
        self.description = "PhysicsTerm: ResonanceMUGE_THzTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class VacuumEnergyDifferentialTerm(PhysicsTerm):
    """
    PhysicsTerm: VacuumEnergyDifferentialTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "VacuumEnergyDifferentialTerm"
        self.description = "PhysicsTerm: VacuumEnergyDifferentialTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class SuperconductiveFrequencyTerm(PhysicsTerm):
    """
    PhysicsTerm: SuperconductiveFrequencyTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "SuperconductiveFrequencyTerm"
        self.description = "PhysicsTerm: SuperconductiveFrequencyTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class AetherResonanceTerm(PhysicsTerm):
    """
    PhysicsTerm: AetherResonanceTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "AetherResonanceTerm"
        self.description = "PhysicsTerm: AetherResonanceTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class QuantumFrequencyTerm(PhysicsTerm):
    """
    PhysicsTerm: QuantumFrequencyTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "QuantumFrequencyTerm"
        self.description = "PhysicsTerm: QuantumFrequencyTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class AetherFrequencyTerm(PhysicsTerm):
    """
    PhysicsTerm: AetherFrequencyTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "AetherFrequencyTerm"
        self.description = "PhysicsTerm: AetherFrequencyTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class WormholeContributionTerm(PhysicsTerm):
    """
    PhysicsTerm: WormholeContributionTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "WormholeContributionTerm"
        self.description = "PhysicsTerm: WormholeContributionTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class UnifiedFieldUg1Term(PhysicsTerm):
    """
    PhysicsTerm: UnifiedFieldUg1Term
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "UnifiedFieldUg1Term"
        self.description = "PhysicsTerm: UnifiedFieldUg1Term"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class UnifiedFieldUg2Term(PhysicsTerm):
    """
    PhysicsTerm: UnifiedFieldUg2Term
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "UnifiedFieldUg2Term"
        self.description = "PhysicsTerm: UnifiedFieldUg2Term"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class UnifiedFieldUg3Term(PhysicsTerm):
    """
    PhysicsTerm: UnifiedFieldUg3Term
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "UnifiedFieldUg3Term"
        self.description = "PhysicsTerm: UnifiedFieldUg3Term"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class UnifiedFieldUg4Term(PhysicsTerm):
    """
    PhysicsTerm: UnifiedFieldUg4Term
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "UnifiedFieldUg4Term"
        self.description = "PhysicsTerm: UnifiedFieldUg4Term"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class UnifiedFieldUmTerm(PhysicsTerm):
    """
    PhysicsTerm: UnifiedFieldUmTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "UnifiedFieldUmTerm"
        self.description = "PhysicsTerm: UnifiedFieldUmTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class SpacetimeMetricTerm(PhysicsTerm):
    """
    PhysicsTerm: SpacetimeMetricTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "SpacetimeMetricTerm"
        self.description = "PhysicsTerm: SpacetimeMetricTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class CompressedMUGETerm(PhysicsTerm):
    """
    PhysicsTerm: CompressedMUGETerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "CompressedMUGETerm"
        self.description = "PhysicsTerm: CompressedMUGETerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class ResonanceMUGE_VacuumDiffTerm(PhysicsTerm):
    """
    PhysicsTerm: ResonanceMUGE_VacuumDiffTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "ResonanceMUGE_VacuumDiffTerm"
        self.description = "PhysicsTerm: ResonanceMUGE_VacuumDiffTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class ResonanceMUGE_SuperFreqTerm(PhysicsTerm):
    """
    PhysicsTerm: ResonanceMUGE_SuperFreqTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "ResonanceMUGE_SuperFreqTerm"
        self.description = "PhysicsTerm: ResonanceMUGE_SuperFreqTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class ResonanceMUGE_AetherResTerm(PhysicsTerm):
    """
    PhysicsTerm: ResonanceMUGE_AetherResTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "ResonanceMUGE_AetherResTerm"
        self.description = "PhysicsTerm: ResonanceMUGE_AetherResTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class ResonanceMUGE_QuantumFreqTerm(PhysicsTerm):
    """
    PhysicsTerm: ResonanceMUGE_QuantumFreqTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "ResonanceMUGE_QuantumFreqTerm"
        self.description = "PhysicsTerm: ResonanceMUGE_QuantumFreqTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class YAMLConfigTerm(PhysicsTerm):
    """
    PhysicsTerm: YAMLConfigTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "YAMLConfigTerm"
        self.description = "PhysicsTerm: YAMLConfigTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class UQFFCoreBuoyancyTerm(PhysicsTerm):
    """
    PhysicsTerm: UQFFCoreBuoyancyTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "UQFFCoreBuoyancyTerm"
        self.description = "PhysicsTerm: UQFFCoreBuoyancyTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class VacuumRepulsionTerm(PhysicsTerm):
    """
    PhysicsTerm: VacuumRepulsionTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "VacuumRepulsionTerm"
        self.description = "PhysicsTerm: VacuumRepulsionTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class THzShockCommunicationTerm(PhysicsTerm):
    """
    PhysicsTerm: THzShockCommunicationTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "THzShockCommunicationTerm"
        self.description = "PhysicsTerm: THzShockCommunicationTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class ConduitFormationTerm(PhysicsTerm):
    """
    PhysicsTerm: ConduitFormationTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "ConduitFormationTerm"
        self.description = "PhysicsTerm: ConduitFormationTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class SpookyActionTerm(PhysicsTerm):
    """
    PhysicsTerm: SpookyActionTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "SpookyActionTerm"
        self.description = "PhysicsTerm: SpookyActionTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class DPMResonanceEnergyTerm(PhysicsTerm):
    """
    PhysicsTerm: DPMResonanceEnergyTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "DPMResonanceEnergyTerm"
        self.description = "PhysicsTerm: DPMResonanceEnergyTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class Triadic26LayerTerm(PhysicsTerm):
    """
    PhysicsTerm: Triadic26LayerTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "Triadic26LayerTerm"
        self.description = "PhysicsTerm: Triadic26LayerTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class MagnetarCoreTerm(PhysicsTerm):
    """
    PhysicsTerm: MagnetarCoreTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "MagnetarCoreTerm"
        self.description = "PhysicsTerm: MagnetarCoreTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class MagnetarLambdaTerm(PhysicsTerm):
    """
    PhysicsTerm: MagnetarLambdaTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "MagnetarLambdaTerm"
        self.description = "PhysicsTerm: MagnetarLambdaTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class MagnetarEMTerm(PhysicsTerm):
    """
    PhysicsTerm: MagnetarEMTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "MagnetarEMTerm"
        self.description = "PhysicsTerm: MagnetarEMTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class MagnetarGWTerm(PhysicsTerm):
    """
    PhysicsTerm: MagnetarGWTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "MagnetarGWTerm"
        self.description = "PhysicsTerm: MagnetarGWTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class MagnetarQuantumTerm(PhysicsTerm):
    """
    PhysicsTerm: MagnetarQuantumTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "MagnetarQuantumTerm"
        self.description = "PhysicsTerm: MagnetarQuantumTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class MagnetarFluidTerm(PhysicsTerm):
    """
    PhysicsTerm: MagnetarFluidTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "MagnetarFluidTerm"
        self.description = "PhysicsTerm: MagnetarFluidTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class MagnetarOscillatoryTerm(PhysicsTerm):
    """
    PhysicsTerm: MagnetarOscillatoryTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "MagnetarOscillatoryTerm"
        self.description = "PhysicsTerm: MagnetarOscillatoryTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class MagnetarDarkMatterTerm(PhysicsTerm):
    """
    PhysicsTerm: MagnetarDarkMatterTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "MagnetarDarkMatterTerm"
        self.description = "PhysicsTerm: MagnetarDarkMatterTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class MagnetarMagneticEnergyTerm(PhysicsTerm):
    """
    PhysicsTerm: MagnetarMagneticEnergyTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "MagnetarMagneticEnergyTerm"
        self.description = "PhysicsTerm: MagnetarMagneticEnergyTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class MagnetarDecayTerm(PhysicsTerm):
    """
    PhysicsTerm: MagnetarDecayTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "MagnetarDecayTerm"
        self.description = "PhysicsTerm: MagnetarDecayTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class Magnetar0501CoreTerm(PhysicsTerm):
    """
    PhysicsTerm: Magnetar0501CoreTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "Magnetar0501CoreTerm"
        self.description = "PhysicsTerm: Magnetar0501CoreTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class Magnetar0501LambdaTerm(PhysicsTerm):
    """
    PhysicsTerm: Magnetar0501LambdaTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "Magnetar0501LambdaTerm"
        self.description = "PhysicsTerm: Magnetar0501LambdaTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class Magnetar0501EMTerm(PhysicsTerm):
    """
    PhysicsTerm: Magnetar0501EMTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "Magnetar0501EMTerm"
        self.description = "PhysicsTerm: Magnetar0501EMTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class Magnetar0501GWTerm(PhysicsTerm):
    """
    PhysicsTerm: Magnetar0501GWTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "Magnetar0501GWTerm"
        self.description = "PhysicsTerm: Magnetar0501GWTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class Magnetar0501QuantumTerm(PhysicsTerm):
    """
    PhysicsTerm: Magnetar0501QuantumTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "Magnetar0501QuantumTerm"
        self.description = "PhysicsTerm: Magnetar0501QuantumTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class Magnetar0501FluidTerm(PhysicsTerm):
    """
    PhysicsTerm: Magnetar0501FluidTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "Magnetar0501FluidTerm"
        self.description = "PhysicsTerm: Magnetar0501FluidTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class Magnetar0501OscillatoryTerm(PhysicsTerm):
    """
    PhysicsTerm: Magnetar0501OscillatoryTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "Magnetar0501OscillatoryTerm"
        self.description = "PhysicsTerm: Magnetar0501OscillatoryTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class Magnetar0501DarkMatterTerm(PhysicsTerm):
    """
    PhysicsTerm: Magnetar0501DarkMatterTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "Magnetar0501DarkMatterTerm"
        self.description = "PhysicsTerm: Magnetar0501DarkMatterTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class SgrAStarCoreTerm(PhysicsTerm):
    """
    PhysicsTerm: SgrAStarCoreTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "SgrAStarCoreTerm"
        self.description = "PhysicsTerm: SgrAStarCoreTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class SgrAStarLambdaTerm(PhysicsTerm):
    """
    PhysicsTerm: SgrAStarLambdaTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "SgrAStarLambdaTerm"
        self.description = "PhysicsTerm: SgrAStarLambdaTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class SgrAStarEMTerm(PhysicsTerm):
    """
    PhysicsTerm: SgrAStarEMTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "SgrAStarEMTerm"
        self.description = "PhysicsTerm: SgrAStarEMTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class SgrAStarGWTerm(PhysicsTerm):
    """
    PhysicsTerm: SgrAStarGWTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "SgrAStarGWTerm"
        self.description = "PhysicsTerm: SgrAStarGWTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class SgrAStarQuantumTerm(PhysicsTerm):
    """
    PhysicsTerm: SgrAStarQuantumTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "SgrAStarQuantumTerm"
        self.description = "PhysicsTerm: SgrAStarQuantumTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class SgrAStarFluidTerm(PhysicsTerm):
    """
    PhysicsTerm: SgrAStarFluidTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "SgrAStarFluidTerm"
        self.description = "PhysicsTerm: SgrAStarFluidTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class SgrAStarOscillatoryTerm(PhysicsTerm):
    """
    PhysicsTerm: SgrAStarOscillatoryTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "SgrAStarOscillatoryTerm"
        self.description = "PhysicsTerm: SgrAStarOscillatoryTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class SgrAStarDarkMatterTerm(PhysicsTerm):
    """
    PhysicsTerm: SgrAStarDarkMatterTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "SgrAStarDarkMatterTerm"
        self.description = "PhysicsTerm: SgrAStarDarkMatterTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class StarbirthCoreTerm(PhysicsTerm):
    """
    PhysicsTerm: StarbirthCoreTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "StarbirthCoreTerm"
        self.description = "PhysicsTerm: StarbirthCoreTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class StarbirthLambdaTerm(PhysicsTerm):
    """
    PhysicsTerm: StarbirthLambdaTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "StarbirthLambdaTerm"
        self.description = "PhysicsTerm: StarbirthLambdaTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class StarbirthUQFFTerm(PhysicsTerm):
    """
    PhysicsTerm: StarbirthUQFFTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "StarbirthUQFFTerm"
        self.description = "PhysicsTerm: StarbirthUQFFTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class StarbirthEMTerm(PhysicsTerm):
    """
    PhysicsTerm: StarbirthEMTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "StarbirthEMTerm"
        self.description = "PhysicsTerm: StarbirthEMTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class StarbirthQuantumTerm(PhysicsTerm):
    """
    PhysicsTerm: StarbirthQuantumTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "StarbirthQuantumTerm"
        self.description = "PhysicsTerm: StarbirthQuantumTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class StarbirthFluidTerm(PhysicsTerm):
    """
    PhysicsTerm: StarbirthFluidTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "StarbirthFluidTerm"
        self.description = "PhysicsTerm: StarbirthFluidTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class StarbirthOscillatoryTerm(PhysicsTerm):
    """
    PhysicsTerm: StarbirthOscillatoryTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "StarbirthOscillatoryTerm"
        self.description = "PhysicsTerm: StarbirthOscillatoryTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class StarbirthDarkMatterTerm(PhysicsTerm):
    """
    PhysicsTerm: StarbirthDarkMatterTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "StarbirthDarkMatterTerm"
        self.description = "PhysicsTerm: StarbirthDarkMatterTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class StarbirthWindTerm(PhysicsTerm):
    """
    PhysicsTerm: StarbirthWindTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "StarbirthWindTerm"
        self.description = "PhysicsTerm: StarbirthWindTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class Westerlund2CoreTerm(PhysicsTerm):
    """
    PhysicsTerm: Westerlund2CoreTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "Westerlund2CoreTerm"
        self.description = "PhysicsTerm: Westerlund2CoreTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class Westerlund2LambdaTerm(PhysicsTerm):
    """
    PhysicsTerm: Westerlund2LambdaTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "Westerlund2LambdaTerm"
        self.description = "PhysicsTerm: Westerlund2LambdaTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class Westerlund2UQFFTerm(PhysicsTerm):
    """
    PhysicsTerm: Westerlund2UQFFTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "Westerlund2UQFFTerm"
        self.description = "PhysicsTerm: Westerlund2UQFFTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class Westerlund2EMTerm(PhysicsTerm):
    """
    PhysicsTerm: Westerlund2EMTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "Westerlund2EMTerm"
        self.description = "PhysicsTerm: Westerlund2EMTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class Westerlund2QuantumTerm(PhysicsTerm):
    """
    PhysicsTerm: Westerlund2QuantumTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "Westerlund2QuantumTerm"
        self.description = "PhysicsTerm: Westerlund2QuantumTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class Westerlund2FluidTerm(PhysicsTerm):
    """
    PhysicsTerm: Westerlund2FluidTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "Westerlund2FluidTerm"
        self.description = "PhysicsTerm: Westerlund2FluidTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class Westerlund2OscillatoryTerm(PhysicsTerm):
    """
    PhysicsTerm: Westerlund2OscillatoryTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "Westerlund2OscillatoryTerm"
        self.description = "PhysicsTerm: Westerlund2OscillatoryTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class Westerlund2DarkMatterTerm(PhysicsTerm):
    """
    PhysicsTerm: Westerlund2DarkMatterTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "Westerlund2DarkMatterTerm"
        self.description = "PhysicsTerm: Westerlund2DarkMatterTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class Westerlund2WindTerm(PhysicsTerm):
    """
    PhysicsTerm: Westerlund2WindTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "Westerlund2WindTerm"
        self.description = "PhysicsTerm: Westerlund2WindTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class PillarsCoreTerm(PhysicsTerm):
    """
    PhysicsTerm: PillarsCoreTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "PillarsCoreTerm"
        self.description = "PhysicsTerm: PillarsCoreTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class PillarsLambdaTerm(PhysicsTerm):
    """
    PhysicsTerm: PillarsLambdaTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "PillarsLambdaTerm"
        self.description = "PhysicsTerm: PillarsLambdaTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class PillarsUQFFTerm(PhysicsTerm):
    """
    PhysicsTerm: PillarsUQFFTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "PillarsUQFFTerm"
        self.description = "PhysicsTerm: PillarsUQFFTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class PillarsEMTerm(PhysicsTerm):
    """
    PhysicsTerm: PillarsEMTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "PillarsEMTerm"
        self.description = "PhysicsTerm: PillarsEMTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class PillarsQuantumTerm(PhysicsTerm):
    """
    PhysicsTerm: PillarsQuantumTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "PillarsQuantumTerm"
        self.description = "PhysicsTerm: PillarsQuantumTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class PillarsFluidTerm(PhysicsTerm):
    """
    PhysicsTerm: PillarsFluidTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "PillarsFluidTerm"
        self.description = "PhysicsTerm: PillarsFluidTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class PillarsOscillatoryTerm(PhysicsTerm):
    """
    PhysicsTerm: PillarsOscillatoryTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "PillarsOscillatoryTerm"
        self.description = "PhysicsTerm: PillarsOscillatoryTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class PillarsDarkMatterTerm(PhysicsTerm):
    """
    PhysicsTerm: PillarsDarkMatterTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "PillarsDarkMatterTerm"
        self.description = "PhysicsTerm: PillarsDarkMatterTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class PillarsWindTerm(PhysicsTerm):
    """
    PhysicsTerm: PillarsWindTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "PillarsWindTerm"
        self.description = "PhysicsTerm: PillarsWindTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class PillarsErosionTerm(PhysicsTerm):
    """
    PhysicsTerm: PillarsErosionTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "PillarsErosionTerm"
        self.description = "PhysicsTerm: PillarsErosionTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class EinsteinRingCoreTerm(PhysicsTerm):
    """
    PhysicsTerm: EinsteinRingCoreTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "EinsteinRingCoreTerm"
        self.description = "PhysicsTerm: EinsteinRingCoreTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class EinsteinRingLambdaTerm(PhysicsTerm):
    """
    PhysicsTerm: EinsteinRingLambdaTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "EinsteinRingLambdaTerm"
        self.description = "PhysicsTerm: EinsteinRingLambdaTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class EinsteinRingUQFFTerm(PhysicsTerm):
    """
    PhysicsTerm: EinsteinRingUQFFTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "EinsteinRingUQFFTerm"
        self.description = "PhysicsTerm: EinsteinRingUQFFTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class EinsteinRingEMTerm(PhysicsTerm):
    """
    PhysicsTerm: EinsteinRingEMTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "EinsteinRingEMTerm"
        self.description = "PhysicsTerm: EinsteinRingEMTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class EinsteinRingQuantumTerm(PhysicsTerm):
    """
    PhysicsTerm: EinsteinRingQuantumTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "EinsteinRingQuantumTerm"
        self.description = "PhysicsTerm: EinsteinRingQuantumTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class EinsteinRingFluidTerm(PhysicsTerm):
    """
    PhysicsTerm: EinsteinRingFluidTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "EinsteinRingFluidTerm"
        self.description = "PhysicsTerm: EinsteinRingFluidTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class EinsteinRingOscillatoryTerm(PhysicsTerm):
    """
    PhysicsTerm: EinsteinRingOscillatoryTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "EinsteinRingOscillatoryTerm"
        self.description = "PhysicsTerm: EinsteinRingOscillatoryTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class EinsteinRingDarkMatterTerm(PhysicsTerm):
    """
    PhysicsTerm: EinsteinRingDarkMatterTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "EinsteinRingDarkMatterTerm"
        self.description = "PhysicsTerm: EinsteinRingDarkMatterTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class EinsteinRingWindTerm(PhysicsTerm):
    """
    PhysicsTerm: EinsteinRingWindTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "EinsteinRingWindTerm"
        self.description = "PhysicsTerm: EinsteinRingWindTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class NGC2525CoreTerm(PhysicsTerm):
    """
    PhysicsTerm: NGC2525CoreTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "NGC2525CoreTerm"
        self.description = "PhysicsTerm: NGC2525CoreTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class NGC2525BlackHoleTerm(PhysicsTerm):
    """
    PhysicsTerm: NGC2525BlackHoleTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "NGC2525BlackHoleTerm"
        self.description = "PhysicsTerm: NGC2525BlackHoleTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class NGC2525LambdaTerm(PhysicsTerm):
    """
    PhysicsTerm: NGC2525LambdaTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "NGC2525LambdaTerm"
        self.description = "PhysicsTerm: NGC2525LambdaTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class NGC2525UQFFTerm(PhysicsTerm):
    """
    PhysicsTerm: NGC2525UQFFTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "NGC2525UQFFTerm"
        self.description = "PhysicsTerm: NGC2525UQFFTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class NGC2525EMTerm(PhysicsTerm):
    """
    PhysicsTerm: NGC2525EMTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "NGC2525EMTerm"
        self.description = "PhysicsTerm: NGC2525EMTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class NGC2525QuantumTerm(PhysicsTerm):
    """
    PhysicsTerm: NGC2525QuantumTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "NGC2525QuantumTerm"
        self.description = "PhysicsTerm: NGC2525QuantumTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class NGC2525FluidTerm(PhysicsTerm):
    """
    PhysicsTerm: NGC2525FluidTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "NGC2525FluidTerm"
        self.description = "PhysicsTerm: NGC2525FluidTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class NGC2525OscillatoryTerm(PhysicsTerm):
    """
    PhysicsTerm: NGC2525OscillatoryTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "NGC2525OscillatoryTerm"
        self.description = "PhysicsTerm: NGC2525OscillatoryTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class NGC2525DarkMatterTerm(PhysicsTerm):
    """
    PhysicsTerm: NGC2525DarkMatterTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "NGC2525DarkMatterTerm"
        self.description = "PhysicsTerm: NGC2525DarkMatterTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class NGC2525SupernovaTerm(PhysicsTerm):
    """
    PhysicsTerm: NGC2525SupernovaTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "NGC2525SupernovaTerm"
        self.description = "PhysicsTerm: NGC2525SupernovaTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class NGC3603CoreTerm(PhysicsTerm):
    """
    PhysicsTerm: NGC3603CoreTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "NGC3603CoreTerm"
        self.description = "PhysicsTerm: NGC3603CoreTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class NGC3603LambdaTerm(PhysicsTerm):
    """
    PhysicsTerm: NGC3603LambdaTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "NGC3603LambdaTerm"
        self.description = "PhysicsTerm: NGC3603LambdaTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class NGC3603UQFFTerm(PhysicsTerm):
    """
    PhysicsTerm: NGC3603UQFFTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "NGC3603UQFFTerm"
        self.description = "PhysicsTerm: NGC3603UQFFTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class NGC3603EMTerm(PhysicsTerm):
    """
    PhysicsTerm: NGC3603EMTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "NGC3603EMTerm"
        self.description = "PhysicsTerm: NGC3603EMTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class NGC3603QuantumTerm(PhysicsTerm):
    """
    PhysicsTerm: NGC3603QuantumTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "NGC3603QuantumTerm"
        self.description = "PhysicsTerm: NGC3603QuantumTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class NGC3603FluidTerm(PhysicsTerm):
    """
    PhysicsTerm: NGC3603FluidTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "NGC3603FluidTerm"
        self.description = "PhysicsTerm: NGC3603FluidTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class NGC3603OscillatoryTerm(PhysicsTerm):
    """
    PhysicsTerm: NGC3603OscillatoryTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "NGC3603OscillatoryTerm"
        self.description = "PhysicsTerm: NGC3603OscillatoryTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class NGC3603DarkMatterTerm(PhysicsTerm):
    """
    PhysicsTerm: NGC3603DarkMatterTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "NGC3603DarkMatterTerm"
        self.description = "PhysicsTerm: NGC3603DarkMatterTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class NGC3603WindTerm(PhysicsTerm):
    """
    PhysicsTerm: NGC3603WindTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "NGC3603WindTerm"
        self.description = "PhysicsTerm: NGC3603WindTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class NGC3603CavityPressureTerm(PhysicsTerm):
    """
    PhysicsTerm: NGC3603CavityPressureTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "NGC3603CavityPressureTerm"
        self.description = "PhysicsTerm: NGC3603CavityPressureTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class BubbleNebulaCoreTerm(PhysicsTerm):
    """
    PhysicsTerm: BubbleNebulaCoreTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "BubbleNebulaCoreTerm"
        self.description = "PhysicsTerm: BubbleNebulaCoreTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class BubbleNebulaLambdaTerm(PhysicsTerm):
    """
    PhysicsTerm: BubbleNebulaLambdaTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "BubbleNebulaLambdaTerm"
        self.description = "PhysicsTerm: BubbleNebulaLambdaTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class BubbleNebulaUQFFTerm(PhysicsTerm):
    """
    PhysicsTerm: BubbleNebulaUQFFTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "BubbleNebulaUQFFTerm"
        self.description = "PhysicsTerm: BubbleNebulaUQFFTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class BubbleNebulaEMTerm(PhysicsTerm):
    """
    PhysicsTerm: BubbleNebulaEMTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "BubbleNebulaEMTerm"
        self.description = "PhysicsTerm: BubbleNebulaEMTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class BubbleNebulaQuantumTerm(PhysicsTerm):
    """
    PhysicsTerm: BubbleNebulaQuantumTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "BubbleNebulaQuantumTerm"
        self.description = "PhysicsTerm: BubbleNebulaQuantumTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class BubbleNebulaFluidTerm(PhysicsTerm):
    """
    PhysicsTerm: BubbleNebulaFluidTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "BubbleNebulaFluidTerm"
        self.description = "PhysicsTerm: BubbleNebulaFluidTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class BubbleNebulaOscillatoryTerm(PhysicsTerm):
    """
    PhysicsTerm: BubbleNebulaOscillatoryTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "BubbleNebulaOscillatoryTerm"
        self.description = "PhysicsTerm: BubbleNebulaOscillatoryTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class BubbleNebulaDarkMatterTerm(PhysicsTerm):
    """
    PhysicsTerm: BubbleNebulaDarkMatterTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "BubbleNebulaDarkMatterTerm"
        self.description = "PhysicsTerm: BubbleNebulaDarkMatterTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class BubbleNebulaWindTerm(PhysicsTerm):
    """
    PhysicsTerm: BubbleNebulaWindTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "BubbleNebulaWindTerm"
        self.description = "PhysicsTerm: BubbleNebulaWindTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class BubbleNebulaExpansionTerm(PhysicsTerm):
    """
    PhysicsTerm: BubbleNebulaExpansionTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "BubbleNebulaExpansionTerm"
        self.description = "PhysicsTerm: BubbleNebulaExpansionTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class AntennaeGalaxiesCoreTerm(PhysicsTerm):
    """
    PhysicsTerm: AntennaeGalaxiesCoreTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "AntennaeGalaxiesCoreTerm"
        self.description = "PhysicsTerm: AntennaeGalaxiesCoreTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class AntennaeGalaxiesLambdaTerm(PhysicsTerm):
    """
    PhysicsTerm: AntennaeGalaxiesLambdaTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "AntennaeGalaxiesLambdaTerm"
        self.description = "PhysicsTerm: AntennaeGalaxiesLambdaTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class AntennaeGalaxiesUQFFTerm(PhysicsTerm):
    """
    PhysicsTerm: AntennaeGalaxiesUQFFTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "AntennaeGalaxiesUQFFTerm"
        self.description = "PhysicsTerm: AntennaeGalaxiesUQFFTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class AntennaeGalaxiesEMTerm(PhysicsTerm):
    """
    PhysicsTerm: AntennaeGalaxiesEMTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "AntennaeGalaxiesEMTerm"
        self.description = "PhysicsTerm: AntennaeGalaxiesEMTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class AntennaeGalaxiesQuantumTerm(PhysicsTerm):
    """
    PhysicsTerm: AntennaeGalaxiesQuantumTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "AntennaeGalaxiesQuantumTerm"
        self.description = "PhysicsTerm: AntennaeGalaxiesQuantumTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class AntennaeGalaxiesFluidTerm(PhysicsTerm):
    """
    PhysicsTerm: AntennaeGalaxiesFluidTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "AntennaeGalaxiesFluidTerm"
        self.description = "PhysicsTerm: AntennaeGalaxiesFluidTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class AntennaeGalaxiesOscillatoryTerm(PhysicsTerm):
    """
    PhysicsTerm: AntennaeGalaxiesOscillatoryTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "AntennaeGalaxiesOscillatoryTerm"
        self.description = "PhysicsTerm: AntennaeGalaxiesOscillatoryTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class AntennaeGalaxiesDarkMatterTerm(PhysicsTerm):
    """
    PhysicsTerm: AntennaeGalaxiesDarkMatterTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "AntennaeGalaxiesDarkMatterTerm"
        self.description = "PhysicsTerm: AntennaeGalaxiesDarkMatterTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class AntennaeGalaxiesWindTerm(PhysicsTerm):
    """
    PhysicsTerm: AntennaeGalaxiesWindTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "AntennaeGalaxiesWindTerm"
        self.description = "PhysicsTerm: AntennaeGalaxiesWindTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class AntennaeGalaxiesMergerInteractionTerm(PhysicsTerm):
    """
    PhysicsTerm: AntennaeGalaxiesMergerInteractionTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "AntennaeGalaxiesMergerInteractionTerm"
        self.description = "PhysicsTerm: AntennaeGalaxiesMergerInteractionTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class HorseheadNebulaCoreTerm(PhysicsTerm):
    """
    PhysicsTerm: HorseheadNebulaCoreTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "HorseheadNebulaCoreTerm"
        self.description = "PhysicsTerm: HorseheadNebulaCoreTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class HorseheadNebulaLambdaTerm(PhysicsTerm):
    """
    PhysicsTerm: HorseheadNebulaLambdaTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "HorseheadNebulaLambdaTerm"
        self.description = "PhysicsTerm: HorseheadNebulaLambdaTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class HorseheadNebulaUQFFTerm(PhysicsTerm):
    """
    PhysicsTerm: HorseheadNebulaUQFFTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "HorseheadNebulaUQFFTerm"
        self.description = "PhysicsTerm: HorseheadNebulaUQFFTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class HorseheadNebulaEMTerm(PhysicsTerm):
    """
    PhysicsTerm: HorseheadNebulaEMTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "HorseheadNebulaEMTerm"
        self.description = "PhysicsTerm: HorseheadNebulaEMTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class HorseheadNebulaQuantumTerm(PhysicsTerm):
    """
    PhysicsTerm: HorseheadNebulaQuantumTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "HorseheadNebulaQuantumTerm"
        self.description = "PhysicsTerm: HorseheadNebulaQuantumTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class HorseheadNebulaFluidTerm(PhysicsTerm):
    """
    PhysicsTerm: HorseheadNebulaFluidTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "HorseheadNebulaFluidTerm"
        self.description = "PhysicsTerm: HorseheadNebulaFluidTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class HorseheadNebulaOscillatoryTerm(PhysicsTerm):
    """
    PhysicsTerm: HorseheadNebulaOscillatoryTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "HorseheadNebulaOscillatoryTerm"
        self.description = "PhysicsTerm: HorseheadNebulaOscillatoryTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class HorseheadNebulaDarkMatterTerm(PhysicsTerm):
    """
    PhysicsTerm: HorseheadNebulaDarkMatterTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "HorseheadNebulaDarkMatterTerm"
        self.description = "PhysicsTerm: HorseheadNebulaDarkMatterTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class HorseheadNebulaWindTerm(PhysicsTerm):
    """
    PhysicsTerm: HorseheadNebulaWindTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "HorseheadNebulaWindTerm"
        self.description = "PhysicsTerm: HorseheadNebulaWindTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class HorseheadNebulaErosionTerm(PhysicsTerm):
    """
    PhysicsTerm: HorseheadNebulaErosionTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "HorseheadNebulaErosionTerm"
        self.description = "PhysicsTerm: HorseheadNebulaErosionTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class NGC1275CoreTerm(PhysicsTerm):
    """
    PhysicsTerm: NGC1275CoreTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "NGC1275CoreTerm"
        self.description = "PhysicsTerm: NGC1275CoreTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class NGC1275BlackHoleTerm(PhysicsTerm):
    """
    PhysicsTerm: NGC1275BlackHoleTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "NGC1275BlackHoleTerm"
        self.description = "PhysicsTerm: NGC1275BlackHoleTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class NGC1275LambdaTerm(PhysicsTerm):
    """
    PhysicsTerm: NGC1275LambdaTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "NGC1275LambdaTerm"
        self.description = "PhysicsTerm: NGC1275LambdaTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class NGC1275UQFFTerm(PhysicsTerm):
    """
    PhysicsTerm: NGC1275UQFFTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "NGC1275UQFFTerm"
        self.description = "PhysicsTerm: NGC1275UQFFTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class NGC1275EMTerm(PhysicsTerm):
    """
    PhysicsTerm: NGC1275EMTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "NGC1275EMTerm"
        self.description = "PhysicsTerm: NGC1275EMTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class NGC1275QuantumTerm(PhysicsTerm):
    """
    PhysicsTerm: NGC1275QuantumTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "NGC1275QuantumTerm"
        self.description = "PhysicsTerm: NGC1275QuantumTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class NGC1275FluidTerm(PhysicsTerm):
    """
    PhysicsTerm: NGC1275FluidTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "NGC1275FluidTerm"
        self.description = "PhysicsTerm: NGC1275FluidTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class NGC1275OscillatoryTerm(PhysicsTerm):
    """
    PhysicsTerm: NGC1275OscillatoryTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "NGC1275OscillatoryTerm"
        self.description = "PhysicsTerm: NGC1275OscillatoryTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class NGC1275DarkMatterTerm(PhysicsTerm):
    """
    PhysicsTerm: NGC1275DarkMatterTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "NGC1275DarkMatterTerm"
        self.description = "PhysicsTerm: NGC1275DarkMatterTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class NGC1275CoolingFlowTerm(PhysicsTerm):
    """
    PhysicsTerm: NGC1275CoolingFlowTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "NGC1275CoolingFlowTerm"
        self.description = "PhysicsTerm: NGC1275CoolingFlowTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class NGC1275FilamentSupportTerm(PhysicsTerm):
    """
    PhysicsTerm: NGC1275FilamentSupportTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "NGC1275FilamentSupportTerm"
        self.description = "PhysicsTerm: NGC1275FilamentSupportTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class HUDFGalaxiesCoreTerm(PhysicsTerm):
    """
    PhysicsTerm: HUDFGalaxiesCoreTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "HUDFGalaxiesCoreTerm"
        self.description = "PhysicsTerm: HUDFGalaxiesCoreTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class HUDFGalaxiesLambdaTerm(PhysicsTerm):
    """
    PhysicsTerm: HUDFGalaxiesLambdaTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "HUDFGalaxiesLambdaTerm"
        self.description = "PhysicsTerm: HUDFGalaxiesLambdaTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class HUDFGalaxiesUQFFTerm(PhysicsTerm):
    """
    PhysicsTerm: HUDFGalaxiesUQFFTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "HUDFGalaxiesUQFFTerm"
        self.description = "PhysicsTerm: HUDFGalaxiesUQFFTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class HUDFGalaxiesEMTerm(PhysicsTerm):
    """
    PhysicsTerm: HUDFGalaxiesEMTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "HUDFGalaxiesEMTerm"
        self.description = "PhysicsTerm: HUDFGalaxiesEMTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class HUDFGalaxiesQuantumTerm(PhysicsTerm):
    """
    PhysicsTerm: HUDFGalaxiesQuantumTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "HUDFGalaxiesQuantumTerm"
        self.description = "PhysicsTerm: HUDFGalaxiesQuantumTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class HUDFGalaxiesFluidTerm(PhysicsTerm):
    """
    PhysicsTerm: HUDFGalaxiesFluidTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "HUDFGalaxiesFluidTerm"
        self.description = "PhysicsTerm: HUDFGalaxiesFluidTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class HUDFGalaxiesOscillatoryTerm(PhysicsTerm):
    """
    PhysicsTerm: HUDFGalaxiesOscillatoryTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "HUDFGalaxiesOscillatoryTerm"
        self.description = "PhysicsTerm: HUDFGalaxiesOscillatoryTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class HUDFGalaxiesDarkMatterTerm(PhysicsTerm):
    """
    PhysicsTerm: HUDFGalaxiesDarkMatterTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "HUDFGalaxiesDarkMatterTerm"
        self.description = "PhysicsTerm: HUDFGalaxiesDarkMatterTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class HUDFGalaxiesWindTerm(PhysicsTerm):
    """
    PhysicsTerm: HUDFGalaxiesWindTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "HUDFGalaxiesWindTerm"
        self.description = "PhysicsTerm: HUDFGalaxiesWindTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class HUDFGalaxiesInteractionTerm(PhysicsTerm):
    """
    PhysicsTerm: HUDFGalaxiesInteractionTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "HUDFGalaxiesInteractionTerm"
        self.description = "PhysicsTerm: HUDFGalaxiesInteractionTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class NGC1792CoreTerm(PhysicsTerm):
    """
    PhysicsTerm: NGC1792CoreTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "NGC1792CoreTerm"
        self.description = "PhysicsTerm: NGC1792CoreTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class NGC1792LambdaTerm(PhysicsTerm):
    """
    PhysicsTerm: NGC1792LambdaTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "NGC1792LambdaTerm"
        self.description = "PhysicsTerm: NGC1792LambdaTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class NGC1792UQFFTerm(PhysicsTerm):
    """
    PhysicsTerm: NGC1792UQFFTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "NGC1792UQFFTerm"
        self.description = "PhysicsTerm: NGC1792UQFFTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class NGC1792EMTerm(PhysicsTerm):
    """
    PhysicsTerm: NGC1792EMTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "NGC1792EMTerm"
        self.description = "PhysicsTerm: NGC1792EMTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class NGC1792QuantumTerm(PhysicsTerm):
    """
    PhysicsTerm: NGC1792QuantumTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "NGC1792QuantumTerm"
        self.description = "PhysicsTerm: NGC1792QuantumTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class NGC1792FluidTerm(PhysicsTerm):
    """
    PhysicsTerm: NGC1792FluidTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "NGC1792FluidTerm"
        self.description = "PhysicsTerm: NGC1792FluidTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class NGC1792OscillatoryTerm(PhysicsTerm):
    """
    PhysicsTerm: NGC1792OscillatoryTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "NGC1792OscillatoryTerm"
        self.description = "PhysicsTerm: NGC1792OscillatoryTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class NGC1792DarkMatterTerm(PhysicsTerm):
    """
    PhysicsTerm: NGC1792DarkMatterTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "NGC1792DarkMatterTerm"
        self.description = "PhysicsTerm: NGC1792DarkMatterTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class NGC1792SupernovaFeedbackTerm(PhysicsTerm):
    """
    PhysicsTerm: NGC1792SupernovaFeedbackTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "NGC1792SupernovaFeedbackTerm"
        self.description = "PhysicsTerm: NGC1792SupernovaFeedbackTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class AndromedaCoreTerm(PhysicsTerm):
    """
    PhysicsTerm: AndromedaCoreTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "AndromedaCoreTerm"
        self.description = "PhysicsTerm: AndromedaCoreTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class AndromedaBlackHoleTerm(PhysicsTerm):
    """
    PhysicsTerm: AndromedaBlackHoleTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "AndromedaBlackHoleTerm"
        self.description = "PhysicsTerm: AndromedaBlackHoleTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class AndromedaLambdaTerm(PhysicsTerm):
    """
    PhysicsTerm: AndromedaLambdaTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "AndromedaLambdaTerm"
        self.description = "PhysicsTerm: AndromedaLambdaTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class AndromedaUQFFTerm(PhysicsTerm):
    """
    PhysicsTerm: AndromedaUQFFTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "AndromedaUQFFTerm"
        self.description = "PhysicsTerm: AndromedaUQFFTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class AndromedaEMTerm(PhysicsTerm):
    """
    PhysicsTerm: AndromedaEMTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "AndromedaEMTerm"
        self.description = "PhysicsTerm: AndromedaEMTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class AndromedaQuantumTerm(PhysicsTerm):
    """
    PhysicsTerm: AndromedaQuantumTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "AndromedaQuantumTerm"
        self.description = "PhysicsTerm: AndromedaQuantumTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class AndromedaFluidTerm(PhysicsTerm):
    """
    PhysicsTerm: AndromedaFluidTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "AndromedaFluidTerm"
        self.description = "PhysicsTerm: AndromedaFluidTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class AndromedaOscillatoryTerm(PhysicsTerm):
    """
    PhysicsTerm: AndromedaOscillatoryTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "AndromedaOscillatoryTerm"
        self.description = "PhysicsTerm: AndromedaOscillatoryTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class AndromedaDarkMatterTerm(PhysicsTerm):
    """
    PhysicsTerm: AndromedaDarkMatterTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "AndromedaDarkMatterTerm"
        self.description = "PhysicsTerm: AndromedaDarkMatterTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class SombreroCoreTerm(PhysicsTerm):
    """
    PhysicsTerm: SombreroCoreTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "SombreroCoreTerm"
        self.description = "PhysicsTerm: SombreroCoreTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class SombreroBlackHoleTerm(PhysicsTerm):
    """
    PhysicsTerm: SombreroBlackHoleTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "SombreroBlackHoleTerm"
        self.description = "PhysicsTerm: SombreroBlackHoleTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class SombreroLambdaTerm(PhysicsTerm):
    """
    PhysicsTerm: SombreroLambdaTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "SombreroLambdaTerm"
        self.description = "PhysicsTerm: SombreroLambdaTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class SombreroUQFFTerm(PhysicsTerm):
    """
    PhysicsTerm: SombreroUQFFTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "SombreroUQFFTerm"
        self.description = "PhysicsTerm: SombreroUQFFTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class SombreroEMTerm(PhysicsTerm):
    """
    PhysicsTerm: SombreroEMTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "SombreroEMTerm"
        self.description = "PhysicsTerm: SombreroEMTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class SombreroQuantumTerm(PhysicsTerm):
    """
    PhysicsTerm: SombreroQuantumTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "SombreroQuantumTerm"
        self.description = "PhysicsTerm: SombreroQuantumTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class SombreroFluidTerm(PhysicsTerm):
    """
    PhysicsTerm: SombreroFluidTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "SombreroFluidTerm"
        self.description = "PhysicsTerm: SombreroFluidTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class SombreroOscillatoryTerm(PhysicsTerm):
    """
    PhysicsTerm: SombreroOscillatoryTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "SombreroOscillatoryTerm"
        self.description = "PhysicsTerm: SombreroOscillatoryTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class SombreroDarkMatterTerm(PhysicsTerm):
    """
    PhysicsTerm: SombreroDarkMatterTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "SombreroDarkMatterTerm"
        self.description = "PhysicsTerm: SombreroDarkMatterTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class SombreroDustTerm(PhysicsTerm):
    """
    PhysicsTerm: SombreroDustTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "SombreroDustTerm"
        self.description = "PhysicsTerm: SombreroDustTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class SaturnCoreTerm(PhysicsTerm):
    """
    PhysicsTerm: SaturnCoreTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "SaturnCoreTerm"
        self.description = "PhysicsTerm: SaturnCoreTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class SaturnLambdaTerm(PhysicsTerm):
    """
    PhysicsTerm: SaturnLambdaTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "SaturnLambdaTerm"
        self.description = "PhysicsTerm: SaturnLambdaTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class SaturnUQFFTerm(PhysicsTerm):
    """
    PhysicsTerm: SaturnUQFFTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "SaturnUQFFTerm"
        self.description = "PhysicsTerm: SaturnUQFFTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class SaturnEMTerm(PhysicsTerm):
    """
    PhysicsTerm: SaturnEMTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "SaturnEMTerm"
        self.description = "PhysicsTerm: SaturnEMTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class SaturnQuantumTerm(PhysicsTerm):
    """
    PhysicsTerm: SaturnQuantumTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "SaturnQuantumTerm"
        self.description = "PhysicsTerm: SaturnQuantumTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class SaturnFluidTerm(PhysicsTerm):
    """
    PhysicsTerm: SaturnFluidTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "SaturnFluidTerm"
        self.description = "PhysicsTerm: SaturnFluidTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class SaturnOscillatoryTerm(PhysicsTerm):
    """
    PhysicsTerm: SaturnOscillatoryTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "SaturnOscillatoryTerm"
        self.description = "PhysicsTerm: SaturnOscillatoryTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class SaturnDarkMatterTerm(PhysicsTerm):
    """
    PhysicsTerm: SaturnDarkMatterTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "SaturnDarkMatterTerm"
        self.description = "PhysicsTerm: SaturnDarkMatterTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class SaturnRingTidalTerm(PhysicsTerm):
    """
    PhysicsTerm: SaturnRingTidalTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "SaturnRingTidalTerm"
        self.description = "PhysicsTerm: SaturnRingTidalTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class SaturnWindTerm(PhysicsTerm):
    """
    PhysicsTerm: SaturnWindTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "SaturnWindTerm"
        self.description = "PhysicsTerm: SaturnWindTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class M16CoreTerm(PhysicsTerm):
    """
    PhysicsTerm: M16CoreTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "M16CoreTerm"
        self.description = "PhysicsTerm: M16CoreTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class M16LambdaTerm(PhysicsTerm):
    """
    PhysicsTerm: M16LambdaTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "M16LambdaTerm"
        self.description = "PhysicsTerm: M16LambdaTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class M16UQFFTerm(PhysicsTerm):
    """
    PhysicsTerm: M16UQFFTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "M16UQFFTerm"
        self.description = "PhysicsTerm: M16UQFFTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class M16EMTerm(PhysicsTerm):
    """
    PhysicsTerm: M16EMTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "M16EMTerm"
        self.description = "PhysicsTerm: M16EMTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class M16QuantumTerm(PhysicsTerm):
    """
    PhysicsTerm: M16QuantumTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "M16QuantumTerm"
        self.description = "PhysicsTerm: M16QuantumTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class M16FluidTerm(PhysicsTerm):
    """
    PhysicsTerm: M16FluidTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "M16FluidTerm"
        self.description = "PhysicsTerm: M16FluidTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class M16OscillatoryTerm(PhysicsTerm):
    """
    PhysicsTerm: M16OscillatoryTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "M16OscillatoryTerm"
        self.description = "PhysicsTerm: M16OscillatoryTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class M16DarkMatterTerm(PhysicsTerm):
    """
    PhysicsTerm: M16DarkMatterTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "M16DarkMatterTerm"
        self.description = "PhysicsTerm: M16DarkMatterTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class M16StarFormationTerm(PhysicsTerm):
    """
    PhysicsTerm: M16StarFormationTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "M16StarFormationTerm"
        self.description = "PhysicsTerm: M16StarFormationTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class M16RadiationErosionTerm(PhysicsTerm):
    """
    PhysicsTerm: M16RadiationErosionTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "M16RadiationErosionTerm"
        self.description = "PhysicsTerm: M16RadiationErosionTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class CrabCoreTerm(PhysicsTerm):
    """
    PhysicsTerm: CrabCoreTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "CrabCoreTerm"
        self.description = "PhysicsTerm: CrabCoreTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class CrabLambdaTerm(PhysicsTerm):
    """
    PhysicsTerm: CrabLambdaTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "CrabLambdaTerm"
        self.description = "PhysicsTerm: CrabLambdaTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class CrabUQFFTerm(PhysicsTerm):
    """
    PhysicsTerm: CrabUQFFTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "CrabUQFFTerm"
        self.description = "PhysicsTerm: CrabUQFFTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class CrabEMTerm(PhysicsTerm):
    """
    PhysicsTerm: CrabEMTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "CrabEMTerm"
        self.description = "PhysicsTerm: CrabEMTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class CrabQuantumTerm(PhysicsTerm):
    """
    PhysicsTerm: CrabQuantumTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "CrabQuantumTerm"
        self.description = "PhysicsTerm: CrabQuantumTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class CrabFluidTerm(PhysicsTerm):
    """
    PhysicsTerm: CrabFluidTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "CrabFluidTerm"
        self.description = "PhysicsTerm: CrabFluidTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class CrabOscillatoryTerm(PhysicsTerm):
    """
    PhysicsTerm: CrabOscillatoryTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "CrabOscillatoryTerm"
        self.description = "PhysicsTerm: CrabOscillatoryTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class CrabDarkMatterTerm(PhysicsTerm):
    """
    PhysicsTerm: CrabDarkMatterTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "CrabDarkMatterTerm"
        self.description = "PhysicsTerm: CrabDarkMatterTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class CrabPulsarWindTerm(PhysicsTerm):
    """
    PhysicsTerm: CrabPulsarWindTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "CrabPulsarWindTerm"
        self.description = "PhysicsTerm: CrabPulsarWindTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class CrabMagneticTerm(PhysicsTerm):
    """
    PhysicsTerm: CrabMagneticTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "CrabMagneticTerm"
        self.description = "PhysicsTerm: CrabMagneticTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class SGR1745CoreTerm(PhysicsTerm):
    """
    PhysicsTerm: SGR1745CoreTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "SGR1745CoreTerm"
        self.description = "PhysicsTerm: SGR1745CoreTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class SGR1745LambdaTerm(PhysicsTerm):
    """
    PhysicsTerm: SGR1745LambdaTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "SGR1745LambdaTerm"
        self.description = "PhysicsTerm: SGR1745LambdaTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class SGR1745UQFFTerm(PhysicsTerm):
    """
    PhysicsTerm: SGR1745UQFFTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "SGR1745UQFFTerm"
        self.description = "PhysicsTerm: SGR1745UQFFTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class SGR1745EMTerm(PhysicsTerm):
    """
    PhysicsTerm: SGR1745EMTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "SGR1745EMTerm"
        self.description = "PhysicsTerm: SGR1745EMTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class SGR1745QuantumTerm(PhysicsTerm):
    """
    PhysicsTerm: SGR1745QuantumTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "SGR1745QuantumTerm"
        self.description = "PhysicsTerm: SGR1745QuantumTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class SGR1745FluidTerm(PhysicsTerm):
    """
    PhysicsTerm: SGR1745FluidTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "SGR1745FluidTerm"
        self.description = "PhysicsTerm: SGR1745FluidTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class SGR1745OscillatoryTerm(PhysicsTerm):
    """
    PhysicsTerm: SGR1745OscillatoryTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "SGR1745OscillatoryTerm"
        self.description = "PhysicsTerm: SGR1745OscillatoryTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class SGR1745DarkMatterTerm(PhysicsTerm):
    """
    PhysicsTerm: SGR1745DarkMatterTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "SGR1745DarkMatterTerm"
        self.description = "PhysicsTerm: SGR1745DarkMatterTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class SGR1745_DPMTerm(PhysicsTerm):
    """
    PhysicsTerm: SGR1745_DPMTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "SGR1745_DPMTerm"
        self.description = "PhysicsTerm: SGR1745_DPMTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class SGR1745_THzTerm(PhysicsTerm):
    """
    PhysicsTerm: SGR1745_THzTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "SGR1745_THzTerm"
        self.description = "PhysicsTerm: SGR1745_THzTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class SGR1745_VacDiffTerm(PhysicsTerm):
    """
    PhysicsTerm: SGR1745_VacDiffTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "SGR1745_VacDiffTerm"
        self.description = "PhysicsTerm: SGR1745_VacDiffTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class SGR1745_SuperFreqTerm(PhysicsTerm):
    """
    PhysicsTerm: SGR1745_SuperFreqTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "SGR1745_SuperFreqTerm"
        self.description = "PhysicsTerm: SGR1745_SuperFreqTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class SGR1745_AetherResTerm(PhysicsTerm):
    """
    PhysicsTerm: SGR1745_AetherResTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "SGR1745_AetherResTerm"
        self.description = "PhysicsTerm: SGR1745_AetherResTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class SGR1745_Ug4iTerm(PhysicsTerm):
    """
    PhysicsTerm: SGR1745_Ug4iTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "SGR1745_Ug4iTerm"
        self.description = "PhysicsTerm: SGR1745_Ug4iTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class SGR1745_QuantumFreqTerm(PhysicsTerm):
    """
    PhysicsTerm: SGR1745_QuantumFreqTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "SGR1745_QuantumFreqTerm"
        self.description = "PhysicsTerm: SGR1745_QuantumFreqTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class SGR1745_AetherFreqTerm(PhysicsTerm):
    """
    PhysicsTerm: SGR1745_AetherFreqTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "SGR1745_AetherFreqTerm"
        self.description = "PhysicsTerm: SGR1745_AetherFreqTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class SGR1745_FluidFreqTerm(PhysicsTerm):
    """
    PhysicsTerm: SGR1745_FluidFreqTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "SGR1745_FluidFreqTerm"
        self.description = "PhysicsTerm: SGR1745_FluidFreqTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class SGR1745_ExpFreqTerm(PhysicsTerm):
    """
    PhysicsTerm: SGR1745_ExpFreqTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "SGR1745_ExpFreqTerm"
        self.description = "PhysicsTerm: SGR1745_ExpFreqTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class SgrA_DPMTerm(PhysicsTerm):
    """
    PhysicsTerm: SgrA_DPMTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "SgrA_DPMTerm"
        self.description = "PhysicsTerm: SgrA_DPMTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class SgrA_THzTerm(PhysicsTerm):
    """
    PhysicsTerm: SgrA_THzTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "SgrA_THzTerm"
        self.description = "PhysicsTerm: SgrA_THzTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class SgrA_VacDiffTerm(PhysicsTerm):
    """
    PhysicsTerm: SgrA_VacDiffTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "SgrA_VacDiffTerm"
        self.description = "PhysicsTerm: SgrA_VacDiffTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class SgrA_SuperFreqTerm(PhysicsTerm):
    """
    PhysicsTerm: SgrA_SuperFreqTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "SgrA_SuperFreqTerm"
        self.description = "PhysicsTerm: SgrA_SuperFreqTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class SgrA_AetherResTerm(PhysicsTerm):
    """
    PhysicsTerm: SgrA_AetherResTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "SgrA_AetherResTerm"
        self.description = "PhysicsTerm: SgrA_AetherResTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class SgrA_Ug4iTerm(PhysicsTerm):
    """
    PhysicsTerm: SgrA_Ug4iTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "SgrA_Ug4iTerm"
        self.description = "PhysicsTerm: SgrA_Ug4iTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class SgrA_QuantumFreqTerm(PhysicsTerm):
    """
    PhysicsTerm: SgrA_QuantumFreqTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "SgrA_QuantumFreqTerm"
        self.description = "PhysicsTerm: SgrA_QuantumFreqTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class SgrA_AetherFreqTerm(PhysicsTerm):
    """
    PhysicsTerm: SgrA_AetherFreqTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "SgrA_AetherFreqTerm"
        self.description = "PhysicsTerm: SgrA_AetherFreqTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class SgrA_FluidFreqTerm(PhysicsTerm):
    """
    PhysicsTerm: SgrA_FluidFreqTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "SgrA_FluidFreqTerm"
        self.description = "PhysicsTerm: SgrA_FluidFreqTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class SgrA_ExpFreqTerm(PhysicsTerm):
    """
    PhysicsTerm: SgrA_ExpFreqTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "SgrA_ExpFreqTerm"
        self.description = "PhysicsTerm: SgrA_ExpFreqTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class YoungStarsCore(PhysicsTerm):
    """
    PhysicsTerm: YoungStarsCore
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "YoungStarsCore"
        self.description = "PhysicsTerm: YoungStarsCore"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class YoungStarsLambda(PhysicsTerm):
    """
    PhysicsTerm: YoungStarsLambda
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "YoungStarsLambda"
        self.description = "PhysicsTerm: YoungStarsLambda"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class YoungStarsUQFF(PhysicsTerm):
    """
    PhysicsTerm: YoungStarsUQFF
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "YoungStarsUQFF"
        self.description = "PhysicsTerm: YoungStarsUQFF"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class YoungStarsEM(PhysicsTerm):
    """
    PhysicsTerm: YoungStarsEM
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "YoungStarsEM"
        self.description = "PhysicsTerm: YoungStarsEM"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class YoungStarsQuantum(PhysicsTerm):
    """
    PhysicsTerm: YoungStarsQuantum
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "YoungStarsQuantum"
        self.description = "PhysicsTerm: YoungStarsQuantum"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class YoungStarsFluid(PhysicsTerm):
    """
    PhysicsTerm: YoungStarsFluid
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "YoungStarsFluid"
        self.description = "PhysicsTerm: YoungStarsFluid"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class YoungStarsOscillatory(PhysicsTerm):
    """
    PhysicsTerm: YoungStarsOscillatory
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "YoungStarsOscillatory"
        self.description = "PhysicsTerm: YoungStarsOscillatory"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class YoungStarsDarkMatter(PhysicsTerm):
    """
    PhysicsTerm: YoungStarsDarkMatter
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "YoungStarsDarkMatter"
        self.description = "PhysicsTerm: YoungStarsDarkMatter"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class YoungStarsOutflowPressure(PhysicsTerm):
    """
    PhysicsTerm: YoungStarsOutflowPressure
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "YoungStarsOutflowPressure"
        self.description = "PhysicsTerm: YoungStarsOutflowPressure"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class YoungStarsStarFormation(PhysicsTerm):
    """
    PhysicsTerm: YoungStarsStarFormation
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "YoungStarsStarFormation"
        self.description = "PhysicsTerm: YoungStarsStarFormation"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class BigBangCore(PhysicsTerm):
    """
    PhysicsTerm: BigBangCore
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "BigBangCore"
        self.description = "PhysicsTerm: BigBangCore"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class BigBangLambda(PhysicsTerm):
    """
    PhysicsTerm: BigBangLambda
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "BigBangLambda"
        self.description = "PhysicsTerm: BigBangLambda"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class BigBangUg1(PhysicsTerm):
    """
    PhysicsTerm: BigBangUg1
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "BigBangUg1"
        self.description = "PhysicsTerm: BigBangUg1"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class BigBangUg4(PhysicsTerm):
    """
    PhysicsTerm: BigBangUg4
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "BigBangUg4"
        self.description = "PhysicsTerm: BigBangUg4"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class BigBangQuantum(PhysicsTerm):
    """
    PhysicsTerm: BigBangQuantum
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "BigBangQuantum"
        self.description = "PhysicsTerm: BigBangQuantum"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class BigBangFluid(PhysicsTerm):
    """
    PhysicsTerm: BigBangFluid
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "BigBangFluid"
        self.description = "PhysicsTerm: BigBangFluid"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class BigBangOscillatory(PhysicsTerm):
    """
    PhysicsTerm: BigBangOscillatory
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "BigBangOscillatory"
        self.description = "PhysicsTerm: BigBangOscillatory"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class BigBangDarkMatter(PhysicsTerm):
    """
    PhysicsTerm: BigBangDarkMatter
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "BigBangDarkMatter"
        self.description = "PhysicsTerm: BigBangDarkMatter"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class BigBangQuantumGravity(PhysicsTerm):
    """
    PhysicsTerm: BigBangQuantumGravity
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "BigBangQuantumGravity"
        self.description = "PhysicsTerm: BigBangQuantumGravity"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class BigBangGravitationalWave(PhysicsTerm):
    """
    PhysicsTerm: BigBangGravitationalWave
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "BigBangGravitationalWave"
        self.description = "PhysicsTerm: BigBangGravitationalWave"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class BigBangCosmicEvolution(PhysicsTerm):
    """
    PhysicsTerm: BigBangCosmicEvolution
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "BigBangCosmicEvolution"
        self.description = "PhysicsTerm: BigBangCosmicEvolution"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class M51Core(PhysicsTerm):
    """
    PhysicsTerm: M51Core
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "M51Core"
        self.description = "PhysicsTerm: M51Core"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class M51Ug1Dipole(PhysicsTerm):
    """
    PhysicsTerm: M51Ug1Dipole
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "M51Ug1Dipole"
        self.description = "PhysicsTerm: M51Ug1Dipole"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class M51Ug2Superconductor(PhysicsTerm):
    """
    PhysicsTerm: M51Ug2Superconductor
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "M51Ug2Superconductor"
        self.description = "PhysicsTerm: M51Ug2Superconductor"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class M51Ug3Tidal(PhysicsTerm):
    """
    PhysicsTerm: M51Ug3Tidal
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "M51Ug3Tidal"
        self.description = "PhysicsTerm: M51Ug3Tidal"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class M51Ug4Reaction(PhysicsTerm):
    """
    PhysicsTerm: M51Ug4Reaction
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "M51Ug4Reaction"
        self.description = "PhysicsTerm: M51Ug4Reaction"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class M51UiVacuum(PhysicsTerm):
    """
    PhysicsTerm: M51UiVacuum
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "M51UiVacuum"
        self.description = "PhysicsTerm: M51UiVacuum"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class M51Lambda(PhysicsTerm):
    """
    PhysicsTerm: M51Lambda
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "M51Lambda"
        self.description = "PhysicsTerm: M51Lambda"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class M51Quantum(PhysicsTerm):
    """
    PhysicsTerm: M51Quantum
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "M51Quantum"
        self.description = "PhysicsTerm: M51Quantum"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class M51Fluid(PhysicsTerm):
    """
    PhysicsTerm: M51Fluid
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "M51Fluid"
        self.description = "PhysicsTerm: M51Fluid"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class M51DarkMatter(PhysicsTerm):
    """
    PhysicsTerm: M51DarkMatter
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "M51DarkMatter"
        self.description = "PhysicsTerm: M51DarkMatter"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class M51EnvironmentalForces(PhysicsTerm):
    """
    PhysicsTerm: M51EnvironmentalForces
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "M51EnvironmentalForces"
        self.description = "PhysicsTerm: M51EnvironmentalForces"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class NGC1316Core(PhysicsTerm):
    """
    PhysicsTerm: NGC1316Core
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "NGC1316Core"
        self.description = "PhysicsTerm: NGC1316Core"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class NGC1316Ug1Dipole(PhysicsTerm):
    """
    PhysicsTerm: NGC1316Ug1Dipole
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "NGC1316Ug1Dipole"
        self.description = "PhysicsTerm: NGC1316Ug1Dipole"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class NGC1316Ug2Superconductor(PhysicsTerm):
    """
    PhysicsTerm: NGC1316Ug2Superconductor
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "NGC1316Ug2Superconductor"
        self.description = "PhysicsTerm: NGC1316Ug2Superconductor"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class NGC1316Ug3External(PhysicsTerm):
    """
    PhysicsTerm: NGC1316Ug3External
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "NGC1316Ug3External"
        self.description = "PhysicsTerm: NGC1316Ug3External"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class NGC1316Ug4Reaction(PhysicsTerm):
    """
    PhysicsTerm: NGC1316Ug4Reaction
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "NGC1316Ug4Reaction"
        self.description = "PhysicsTerm: NGC1316Ug4Reaction"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class NGC1316UiVacuum(PhysicsTerm):
    """
    PhysicsTerm: NGC1316UiVacuum
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "NGC1316UiVacuum"
        self.description = "PhysicsTerm: NGC1316UiVacuum"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class NGC1316Lambda(PhysicsTerm):
    """
    PhysicsTerm: NGC1316Lambda
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "NGC1316Lambda"
        self.description = "PhysicsTerm: NGC1316Lambda"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class NGC1316Quantum(PhysicsTerm):
    """
    PhysicsTerm: NGC1316Quantum
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "NGC1316Quantum"
        self.description = "PhysicsTerm: NGC1316Quantum"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class NGC1316FluidDust(PhysicsTerm):
    """
    PhysicsTerm: NGC1316FluidDust
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "NGC1316FluidDust"
        self.description = "PhysicsTerm: NGC1316FluidDust"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class NGC1316DarkMatter(PhysicsTerm):
    """
    PhysicsTerm: NGC1316DarkMatter
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "NGC1316DarkMatter"
        self.description = "PhysicsTerm: NGC1316DarkMatter"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class NGC1316MergerForces(PhysicsTerm):
    """
    PhysicsTerm: NGC1316MergerForces
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "NGC1316MergerForces"
        self.description = "PhysicsTerm: NGC1316MergerForces"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class SMBHBinaryDPMResonance(PhysicsTerm):
    """
    PhysicsTerm: SMBHBinaryDPMResonance
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "SMBHBinaryDPMResonance"
        self.description = "PhysicsTerm: SMBHBinaryDPMResonance"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class SMBHBinaryTHzResonance(PhysicsTerm):
    """
    PhysicsTerm: SMBHBinaryTHzResonance
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "SMBHBinaryTHzResonance"
        self.description = "PhysicsTerm: SMBHBinaryTHzResonance"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class SMBHBinaryUg4iResonance(PhysicsTerm):
    """
    PhysicsTerm: SMBHBinaryUg4iResonance
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "SMBHBinaryUg4iResonance"
        self.description = "PhysicsTerm: SMBHBinaryUg4iResonance"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class SMBHBinaryPlasmoticVacuum(PhysicsTerm):
    """
    PhysicsTerm: SMBHBinaryPlasmoticVacuum
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "SMBHBinaryPlasmoticVacuum"
        self.description = "PhysicsTerm: SMBHBinaryPlasmoticVacuum"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class SMBHBinaryQuantumResonance(PhysicsTerm):
    """
    PhysicsTerm: SMBHBinaryQuantumResonance
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "SMBHBinaryQuantumResonance"
        self.description = "PhysicsTerm: SMBHBinaryQuantumResonance"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class SMBHBinaryFluidResonance(PhysicsTerm):
    """
    PhysicsTerm: SMBHBinaryFluidResonance
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "SMBHBinaryFluidResonance"
        self.description = "PhysicsTerm: SMBHBinaryFluidResonance"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class SMBHBinaryOscillatoryResonance(PhysicsTerm):
    """
    PhysicsTerm: SMBHBinaryOscillatoryResonance
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "SMBHBinaryOscillatoryResonance"
        self.description = "PhysicsTerm: SMBHBinaryOscillatoryResonance"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class SMBHBinaryExpansionResonance(PhysicsTerm):
    """
    PhysicsTerm: SMBHBinaryExpansionResonance
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "SMBHBinaryExpansionResonance"
        self.description = "PhysicsTerm: SMBHBinaryExpansionResonance"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class SMBHBinaryCoalescence(PhysicsTerm):
    """
    PhysicsTerm: SMBHBinaryCoalescence
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "SMBHBinaryCoalescence"
        self.description = "PhysicsTerm: SMBHBinaryCoalescence"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class BackgroundAetherMinkowski(PhysicsTerm):
    """
    PhysicsTerm: BackgroundAetherMinkowski
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "BackgroundAetherMinkowski"
        self.description = "PhysicsTerm: BackgroundAetherMinkowski"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class BackgroundAetherPerturbedMetric(PhysicsTerm):
    """
    PhysicsTerm: BackgroundAetherPerturbedMetric
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "BackgroundAetherPerturbedMetric"
        self.description = "PhysicsTerm: BackgroundAetherPerturbedMetric"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class BackgroundAetherStressEnergy(PhysicsTerm):
    """
    PhysicsTerm: BackgroundAetherStressEnergy
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "BackgroundAetherStressEnergy"
        self.description = "PhysicsTerm: BackgroundAetherStressEnergy"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class BackgroundAetherCoupling(PhysicsTerm):
    """
    PhysicsTerm: BackgroundAetherCoupling
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "BackgroundAetherCoupling"
        self.description = "PhysicsTerm: BackgroundAetherCoupling"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class HeavisideFraction(PhysicsTerm):
    """
    PhysicsTerm: HeavisideFraction
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "HeavisideFraction"
        self.description = "PhysicsTerm: HeavisideFraction"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class HeavisideFactor(PhysicsTerm):
    """
    PhysicsTerm: HeavisideFactor
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "HeavisideFactor"
        self.description = "PhysicsTerm: HeavisideFactor"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class HeavisideUmBase(PhysicsTerm):
    """
    PhysicsTerm: HeavisideUmBase
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "HeavisideUmBase"
        self.description = "PhysicsTerm: HeavisideUmBase"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class HeavisideUmWithHeaviside(PhysicsTerm):
    """
    PhysicsTerm: HeavisideUmWithHeaviside
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "HeavisideUmWithHeaviside"
        self.description = "PhysicsTerm: HeavisideUmWithHeaviside"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class HeavisideUmWithoutHeaviside(PhysicsTerm):
    """
    PhysicsTerm: HeavisideUmWithoutHeaviside
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "HeavisideUmWithoutHeaviside"
        self.description = "PhysicsTerm: HeavisideUmWithoutHeaviside"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class HeavisideQuasiFactor(PhysicsTerm):
    """
    PhysicsTerm: HeavisideQuasiFactor
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "HeavisideQuasiFactor"
        self.description = "PhysicsTerm: HeavisideQuasiFactor"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class HeavisideExponentialDecay(PhysicsTerm):
    """
    PhysicsTerm: HeavisideExponentialDecay
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "HeavisideExponentialDecay"
        self.description = "PhysicsTerm: HeavisideExponentialDecay"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class HeavisideMagneticMoment(PhysicsTerm):
    """
    PhysicsTerm: HeavisideMagneticMoment
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "HeavisideMagneticMoment"
        self.description = "PhysicsTerm: HeavisideMagneticMoment"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class HeavisideAmplificationRatio(PhysicsTerm):
    """
    PhysicsTerm: HeavisideAmplificationRatio
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "HeavisideAmplificationRatio"
        self.description = "PhysicsTerm: HeavisideAmplificationRatio"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class HydrogenResonance_Amplitude(PhysicsTerm):
    """
    PhysicsTerm: HydrogenResonance_Amplitude
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "HydrogenResonance_Amplitude"
        self.description = "PhysicsTerm: HydrogenResonance_Amplitude"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class HydrogenResonance_Frequency(PhysicsTerm):
    """
    PhysicsTerm: HydrogenResonance_Frequency
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "HydrogenResonance_Frequency"
        self.description = "PhysicsTerm: HydrogenResonance_Frequency"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class HydrogenResonance_DeepPairing(PhysicsTerm):
    """
    PhysicsTerm: HydrogenResonance_DeepPairing
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "HydrogenResonance_DeepPairing"
        self.description = "PhysicsTerm: HydrogenResonance_DeepPairing"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class HydrogenResonance_NuclearCoupling(PhysicsTerm):
    """
    PhysicsTerm: HydrogenResonance_NuclearCoupling
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "HydrogenResonance_NuclearCoupling"
        self.description = "PhysicsTerm: HydrogenResonance_NuclearCoupling"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class HydrogenResonance_ShellCorrection(PhysicsTerm):
    """
    PhysicsTerm: HydrogenResonance_ShellCorrection
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "HydrogenResonance_ShellCorrection"
        self.description = "PhysicsTerm: HydrogenResonance_ShellCorrection"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class HydrogenResonance_Buoyancy(PhysicsTerm):
    """
    PhysicsTerm: HydrogenResonance_Buoyancy
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "HydrogenResonance_Buoyancy"
        self.description = "PhysicsTerm: HydrogenResonance_Buoyancy"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class HydrogenResonance_Superconductive(PhysicsTerm):
    """
    PhysicsTerm: HydrogenResonance_Superconductive
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "HydrogenResonance_Superconductive"
        self.description = "PhysicsTerm: HydrogenResonance_Superconductive"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class HydrogenResonance_PairingEnergy(PhysicsTerm):
    """
    PhysicsTerm: HydrogenResonance_PairingEnergy
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "HydrogenResonance_PairingEnergy"
        self.description = "PhysicsTerm: HydrogenResonance_PairingEnergy"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class HydrogenResonance_MagicStability(PhysicsTerm):
    """
    PhysicsTerm: HydrogenResonance_MagicStability
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "HydrogenResonance_MagicStability"
        self.description = "PhysicsTerm: HydrogenResonance_MagicStability"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class HydrogenResonance_TunnelingProb(PhysicsTerm):
    """
    PhysicsTerm: HydrogenResonance_TunnelingProb
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "HydrogenResonance_TunnelingProb"
        self.description = "PhysicsTerm: HydrogenResonance_TunnelingProb"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class HydrogenResonance_Integrand(PhysicsTerm):
    """
    PhysicsTerm: HydrogenResonance_Integrand
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "HydrogenResonance_Integrand"
        self.description = "PhysicsTerm: HydrogenResonance_Integrand"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class HydrogenResonance_Full(PhysicsTerm):
    """
    PhysicsTerm: HydrogenResonance_Full
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "HydrogenResonance_Full"
        self.description = "PhysicsTerm: HydrogenResonance_Full"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class SurfaceMagnetic_Oscillation(PhysicsTerm):
    """
    PhysicsTerm: SurfaceMagnetic_Oscillation
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "SurfaceMagnetic_Oscillation"
        self.description = "PhysicsTerm: SurfaceMagnetic_Oscillation"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class SurfaceMagnetic_MinField(PhysicsTerm):
    """
    PhysicsTerm: SurfaceMagnetic_MinField
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "SurfaceMagnetic_MinField"
        self.description = "PhysicsTerm: SurfaceMagnetic_MinField"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class SurfaceMagnetic_MaxField(PhysicsTerm):
    """
    PhysicsTerm: SurfaceMagnetic_MaxField
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "SurfaceMagnetic_MaxField"
        self.description = "PhysicsTerm: SurfaceMagnetic_MaxField"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class SurfaceMagnetic_CycleReference(PhysicsTerm):
    """
    PhysicsTerm: SurfaceMagnetic_CycleReference
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "SurfaceMagnetic_CycleReference"
        self.description = "PhysicsTerm: SurfaceMagnetic_CycleReference"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class Source10_F_U_Bi_iTerm(PhysicsTerm):
    """
    PhysicsTerm: Source10_F_U_Bi_iTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "Source10_F_U_Bi_iTerm"
        self.description = "PhysicsTerm: Source10_F_U_Bi_iTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class Source10_g_UQFFTerm(PhysicsTerm):
    """
    PhysicsTerm: Source10_g_UQFFTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "Source10_g_UQFFTerm"
        self.description = "PhysicsTerm: Source10_g_UQFFTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class Source10_DPM_ResonanceTerm(PhysicsTerm):
    """
    PhysicsTerm: Source10_DPM_ResonanceTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "Source10_DPM_ResonanceTerm"
        self.description = "PhysicsTerm: Source10_DPM_ResonanceTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class Source10_E_cmTerm(PhysicsTerm):
    """
    PhysicsTerm: Source10_E_cmTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "Source10_E_cmTerm"
        self.description = "PhysicsTerm: Source10_E_cmTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class Source11_Ug1Term(PhysicsTerm):
    """
    PhysicsTerm: Source11_Ug1Term
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "Source11_Ug1Term"
        self.description = "PhysicsTerm: Source11_Ug1Term"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class Source11_Ug2Term(PhysicsTerm):
    """
    PhysicsTerm: Source11_Ug2Term
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "Source11_Ug2Term"
        self.description = "PhysicsTerm: Source11_Ug2Term"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class Source11_Ug3Term(PhysicsTerm):
    """
    PhysicsTerm: Source11_Ug3Term
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "Source11_Ug3Term"
        self.description = "PhysicsTerm: Source11_Ug3Term"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class Source11_Ug4Term(PhysicsTerm):
    """
    PhysicsTerm: Source11_Ug4Term
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "Source11_Ug4Term"
        self.description = "PhysicsTerm: Source11_Ug4Term"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class Source11_UmTerm(PhysicsTerm):
    """
    PhysicsTerm: Source11_UmTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "Source11_UmTerm"
        self.description = "PhysicsTerm: Source11_UmTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class UQFFModule5_DarkMatterHaloTerm(PhysicsTerm):
    """
    PhysicsTerm: UQFFModule5_DarkMatterHaloTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "UQFFModule5_DarkMatterHaloTerm"
        self.description = "PhysicsTerm: UQFFModule5_DarkMatterHaloTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class UQFFModule5_VacuumEnergyTerm(PhysicsTerm):
    """
    PhysicsTerm: UQFFModule5_VacuumEnergyTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "UQFFModule5_VacuumEnergyTerm"
        self.description = "PhysicsTerm: UQFFModule5_VacuumEnergyTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class Source5_CompressedBaseTerm(PhysicsTerm):
    """
    PhysicsTerm: Source5_CompressedBaseTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "Source5_CompressedBaseTerm"
        self.description = "PhysicsTerm: Source5_CompressedBaseTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class Source5_CompressedExpansionTerm(PhysicsTerm):
    """
    PhysicsTerm: Source5_CompressedExpansionTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "Source5_CompressedExpansionTerm"
        self.description = "PhysicsTerm: Source5_CompressedExpansionTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class Source5_CompressedQuantumTerm(PhysicsTerm):
    """
    PhysicsTerm: Source5_CompressedQuantumTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "Source5_CompressedQuantumTerm"
        self.description = "PhysicsTerm: Source5_CompressedQuantumTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class Source5_CompressedCosmTerm(PhysicsTerm):
    """
    PhysicsTerm: Source5_CompressedCosmTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "Source5_CompressedCosmTerm"
        self.description = "PhysicsTerm: Source5_CompressedCosmTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class Source5_CompressedFluidTerm(PhysicsTerm):
    """
    PhysicsTerm: Source5_CompressedFluidTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "Source5_CompressedFluidTerm"
        self.description = "PhysicsTerm: Source5_CompressedFluidTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class Source5_CompressedPerturbationTerm(PhysicsTerm):
    """
    PhysicsTerm: Source5_CompressedPerturbationTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "Source5_CompressedPerturbationTerm"
        self.description = "PhysicsTerm: Source5_CompressedPerturbationTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class Source5_ResonanceMUGETerm(PhysicsTerm):
    """
    PhysicsTerm: Source5_ResonanceMUGETerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "Source5_ResonanceMUGETerm"
        self.description = "PhysicsTerm: Source5_ResonanceMUGETerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class Source5_CompressedSuperAdjTerm(PhysicsTerm):
    """
    PhysicsTerm: Source5_CompressedSuperAdjTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "Source5_CompressedSuperAdjTerm"
        self.description = "PhysicsTerm: Source5_CompressedSuperAdjTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class Source5_CompressedEnvTerm(PhysicsTerm):
    """
    PhysicsTerm: Source5_CompressedEnvTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "Source5_CompressedEnvTerm"
        self.description = "PhysicsTerm: Source5_CompressedEnvTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class Source5_CompressedUgSumTerm(PhysicsTerm):
    """
    PhysicsTerm: Source5_CompressedUgSumTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "Source5_CompressedUgSumTerm"
        self.description = "PhysicsTerm: Source5_CompressedUgSumTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class ESO137_BuoyancyTerm(PhysicsTerm):
    """
    PhysicsTerm: ESO137_BuoyancyTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "ESO137_BuoyancyTerm"
        self.description = "PhysicsTerm: ESO137_BuoyancyTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class ESO137_CompressedTerm(PhysicsTerm):
    """
    PhysicsTerm: ESO137_CompressedTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "ESO137_CompressedTerm"
        self.description = "PhysicsTerm: ESO137_CompressedTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class ESO137_ResonantTerm(PhysicsTerm):
    """
    PhysicsTerm: ESO137_ResonantTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "ESO137_ResonantTerm"
        self.description = "PhysicsTerm: ESO137_ResonantTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class ESO137_SuperconductiveTerm(PhysicsTerm):
    """
    PhysicsTerm: ESO137_SuperconductiveTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "ESO137_SuperconductiveTerm"
        self.description = "PhysicsTerm: ESO137_SuperconductiveTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class ESO137_LENRTerm(PhysicsTerm):
    """
    PhysicsTerm: ESO137_LENRTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "ESO137_LENRTerm"
        self.description = "PhysicsTerm: ESO137_LENRTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class ESO137_CompressedGTerm(PhysicsTerm):
    """
    PhysicsTerm: ESO137_CompressedGTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "ESO137_CompressedGTerm"
        self.description = "PhysicsTerm: ESO137_CompressedGTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class ESO137_Q_waveTerm(PhysicsTerm):
    """
    PhysicsTerm: ESO137_Q_waveTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "ESO137_Q_waveTerm"
        self.description = "PhysicsTerm: ESO137_Q_waveTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class ESO137_Ub1Term(PhysicsTerm):
    """
    PhysicsTerm: ESO137_Ub1Term
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "ESO137_Ub1Term"
        self.description = "PhysicsTerm: ESO137_Ub1Term"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class ESO137_UiTerm(PhysicsTerm):
    """
    PhysicsTerm: ESO137_UiTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "ESO137_UiTerm"
        self.description = "PhysicsTerm: ESO137_UiTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class NGC1365_BuoyancyTerm(PhysicsTerm):
    """
    PhysicsTerm: NGC1365_BuoyancyTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "NGC1365_BuoyancyTerm"
        self.description = "PhysicsTerm: NGC1365_BuoyancyTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class NGC1365_CompressedTerm(PhysicsTerm):
    """
    PhysicsTerm: NGC1365_CompressedTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "NGC1365_CompressedTerm"
        self.description = "PhysicsTerm: NGC1365_CompressedTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class NGC1365_LENRTerm(PhysicsTerm):
    """
    PhysicsTerm: NGC1365_LENRTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "NGC1365_LENRTerm"
        self.description = "PhysicsTerm: NGC1365_LENRTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class Vela_BuoyancyTerm(PhysicsTerm):
    """
    PhysicsTerm: Vela_BuoyancyTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "Vela_BuoyancyTerm"
        self.description = "PhysicsTerm: Vela_BuoyancyTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class Vela_CompressedTerm(PhysicsTerm):
    """
    PhysicsTerm: Vela_CompressedTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "Vela_CompressedTerm"
        self.description = "PhysicsTerm: Vela_CompressedTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class Vela_LENRTerm(PhysicsTerm):
    """
    PhysicsTerm: Vela_LENRTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "Vela_LENRTerm"
        self.description = "PhysicsTerm: Vela_LENRTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class ASASSN14li_BuoyancyTerm(PhysicsTerm):
    """
    PhysicsTerm: ASASSN14li_BuoyancyTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "ASASSN14li_BuoyancyTerm"
        self.description = "PhysicsTerm: ASASSN14li_BuoyancyTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class ASASSN14li_CompressedTerm(PhysicsTerm):
    """
    PhysicsTerm: ASASSN14li_CompressedTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "ASASSN14li_CompressedTerm"
        self.description = "PhysicsTerm: ASASSN14li_CompressedTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class ASASSN14li_LENRTerm(PhysicsTerm):
    """
    PhysicsTerm: ASASSN14li_LENRTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "ASASSN14li_LENRTerm"
        self.description = "PhysicsTerm: ASASSN14li_LENRTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class ElGordo_BuoyancyTerm(PhysicsTerm):
    """
    PhysicsTerm: ElGordo_BuoyancyTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "ElGordo_BuoyancyTerm"
        self.description = "PhysicsTerm: ElGordo_BuoyancyTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class ElGordo_CompressedTerm(PhysicsTerm):
    """
    PhysicsTerm: ElGordo_CompressedTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "ElGordo_CompressedTerm"
        self.description = "PhysicsTerm: ElGordo_CompressedTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class ElGordo_LENRTerm(PhysicsTerm):
    """
    PhysicsTerm: ElGordo_LENRTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "ElGordo_LENRTerm"
        self.description = "PhysicsTerm: ElGordo_LENRTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class Heaviside_FactorTerm(PhysicsTerm):
    """
    PhysicsTerm: Heaviside_FactorTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "Heaviside_FactorTerm"
        self.description = "PhysicsTerm: Heaviside_FactorTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class Heaviside_UmBaseTerm(PhysicsTerm):
    """
    PhysicsTerm: Heaviside_UmBaseTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "Heaviside_UmBaseTerm"
        self.description = "PhysicsTerm: Heaviside_UmBaseTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class Heliosphere_H_SCmTerm(PhysicsTerm):
    """
    PhysicsTerm: Heliosphere_H_SCmTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "Heliosphere_H_SCmTerm"
        self.description = "PhysicsTerm: Heliosphere_H_SCmTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class UgIndex_U_giTerm(PhysicsTerm):
    """
    PhysicsTerm: UgIndex_U_giTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "UgIndex_U_giTerm"
        self.description = "PhysicsTerm: UgIndex_U_giTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class UgIndex_K_iTerm(PhysicsTerm):
    """
    PhysicsTerm: UgIndex_K_iTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "UgIndex_K_iTerm"
        self.description = "PhysicsTerm: UgIndex_K_iTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class Inertia_Lambda_iTerm(PhysicsTerm):
    """
    PhysicsTerm: Inertia_Lambda_iTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "Inertia_Lambda_iTerm"
        self.description = "PhysicsTerm: Inertia_Lambda_iTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class Inertia_U_iTerm(PhysicsTerm):
    """
    PhysicsTerm: Inertia_U_iTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "Inertia_U_iTerm"
        self.description = "PhysicsTerm: Inertia_U_iTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class MagMoment_Mu_jTerm(PhysicsTerm):
    """
    PhysicsTerm: MagMoment_Mu_jTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "MagMoment_Mu_jTerm"
        self.description = "PhysicsTerm: MagMoment_Mu_jTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class MagMoment_B_jTerm(PhysicsTerm):
    """
    PhysicsTerm: MagMoment_B_jTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "MagMoment_B_jTerm"
        self.description = "PhysicsTerm: MagMoment_B_jTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class BlackHole_M_bhTerm(PhysicsTerm):
    """
    PhysicsTerm: BlackHole_M_bhTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "BlackHole_M_bhTerm"
        self.description = "PhysicsTerm: BlackHole_M_bhTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class BlackHole_U_b1Term(PhysicsTerm):
    """
    PhysicsTerm: BlackHole_U_b1Term
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "BlackHole_U_b1Term"
        self.description = "PhysicsTerm: BlackHole_U_b1Term"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class NegativeTime_CosPiTnTerm(PhysicsTerm):
    """
    PhysicsTerm: NegativeTime_CosPiTnTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "NegativeTime_CosPiTnTerm"
        self.description = "PhysicsTerm: NegativeTime_CosPiTnTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class NegativeTime_ExpTermTerm(PhysicsTerm):
    """
    PhysicsTerm: NegativeTime_ExpTermTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "NegativeTime_ExpTermTerm"
        self.description = "PhysicsTerm: NegativeTime_ExpTermTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class PiConstant_MuJExampleTerm(PhysicsTerm):
    """
    PhysicsTerm: PiConstant_MuJExampleTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "PiConstant_MuJExampleTerm"
        self.description = "PhysicsTerm: PiConstant_MuJExampleTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class CorePenetration_P_coreTerm(PhysicsTerm):
    """
    PhysicsTerm: CorePenetration_P_coreTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "CorePenetration_P_coreTerm"
        self.description = "PhysicsTerm: CorePenetration_P_coreTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class QuasiLong_FactorTerm(PhysicsTerm):
    """
    PhysicsTerm: QuasiLong_FactorTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "QuasiLong_FactorTerm"
        self.description = "PhysicsTerm: QuasiLong_FactorTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class OuterBubble_S_r_RbTerm(PhysicsTerm):
    """
    PhysicsTerm: OuterBubble_S_r_RbTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "OuterBubble_S_r_RbTerm"
        self.description = "PhysicsTerm: OuterBubble_S_r_RbTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class OuterBubble_U_g2Term(PhysicsTerm):
    """
    PhysicsTerm: OuterBubble_U_g2Term
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "OuterBubble_U_g2Term"
        self.description = "PhysicsTerm: OuterBubble_U_g2Term"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class OuterBubble_R_bTerm(PhysicsTerm):
    """
    PhysicsTerm: OuterBubble_R_bTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "OuterBubble_R_bTerm"
        self.description = "PhysicsTerm: OuterBubble_R_bTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class OuterBubble_R_bInAUTerm(PhysicsTerm):
    """
    PhysicsTerm: OuterBubble_R_bInAUTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "OuterBubble_R_bInAUTerm"
        self.description = "PhysicsTerm: OuterBubble_R_bInAUTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class Reciprocation_Gamma_sTerm(PhysicsTerm):
    """
    PhysicsTerm: Reciprocation_Gamma_sTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "Reciprocation_Gamma_sTerm"
        self.description = "PhysicsTerm: Reciprocation_Gamma_sTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class Reciprocation_CosPiTnTerm(PhysicsTerm):
    """
    PhysicsTerm: Reciprocation_CosPiTnTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "Reciprocation_CosPiTnTerm"
        self.description = "PhysicsTerm: Reciprocation_CosPiTnTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class Reciprocation_ExpTermTerm(PhysicsTerm):
    """
    PhysicsTerm: Reciprocation_ExpTermTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "Reciprocation_ExpTermTerm"
        self.description = "PhysicsTerm: Reciprocation_ExpTermTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class Reciprocation_OneMinusExpTerm(PhysicsTerm):
    """
    PhysicsTerm: Reciprocation_OneMinusExpTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "Reciprocation_OneMinusExpTerm"
        self.description = "PhysicsTerm: Reciprocation_OneMinusExpTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class Reciprocation_Gamma_dayTerm(PhysicsTerm):
    """
    PhysicsTerm: Reciprocation_Gamma_dayTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "Reciprocation_Gamma_dayTerm"
        self.description = "PhysicsTerm: Reciprocation_Gamma_dayTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class Reciprocation_UmExampleTerm(PhysicsTerm):
    """
    PhysicsTerm: Reciprocation_UmExampleTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "Reciprocation_UmExampleTerm"
        self.description = "PhysicsTerm: Reciprocation_UmExampleTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class ScmPenetration_UmBaseTerm(PhysicsTerm):
    """
    PhysicsTerm: ScmPenetration_UmBaseTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "ScmPenetration_UmBaseTerm"
        self.description = "PhysicsTerm: ScmPenetration_UmBaseTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class ScmPenetration_UmContributionTerm(PhysicsTerm):
    """
    PhysicsTerm: ScmPenetration_UmContributionTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "ScmPenetration_UmContributionTerm"
        self.description = "PhysicsTerm: ScmPenetration_UmContributionTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class ScmPenetration_P_SCmTerm(PhysicsTerm):
    """
    PhysicsTerm: ScmPenetration_P_SCmTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "ScmPenetration_P_SCmTerm"
        self.description = "PhysicsTerm: ScmPenetration_P_SCmTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class ScmPenetration_UmPlanetTerm(PhysicsTerm):
    """
    PhysicsTerm: ScmPenetration_UmPlanetTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "ScmPenetration_UmPlanetTerm"
        self.description = "PhysicsTerm: ScmPenetration_UmPlanetTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class ScmReactivity_Kappa_sTerm(PhysicsTerm):
    """
    PhysicsTerm: ScmReactivity_Kappa_sTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "ScmReactivity_Kappa_sTerm"
        self.description = "PhysicsTerm: ScmReactivity_Kappa_sTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class ScmReactivity_E_reactTerm(PhysicsTerm):
    """
    PhysicsTerm: ScmReactivity_E_reactTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "ScmReactivity_E_reactTerm"
        self.description = "PhysicsTerm: ScmReactivity_E_reactTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class ScmReactivity_UmExampleTerm(PhysicsTerm):
    """
    PhysicsTerm: ScmReactivity_UmExampleTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "ScmReactivity_UmExampleTerm"
        self.description = "PhysicsTerm: ScmReactivity_UmExampleTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class ScmReactivity_Kappa_dayTerm(PhysicsTerm):
    """
    PhysicsTerm: ScmReactivity_Kappa_dayTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "ScmReactivity_Kappa_dayTerm"
        self.description = "PhysicsTerm: ScmReactivity_Kappa_dayTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class SolarCycle_Omega_cTerm(PhysicsTerm):
    """
    PhysicsTerm: SolarCycle_Omega_cTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "SolarCycle_Omega_cTerm"
        self.description = "PhysicsTerm: SolarCycle_Omega_cTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class SolarCycle_SinOmegaCTTerm(PhysicsTerm):
    """
    PhysicsTerm: SolarCycle_SinOmegaCTTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "SolarCycle_SinOmegaCTTerm"
        self.description = "PhysicsTerm: SolarCycle_SinOmegaCTTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class SolarCycle_MuJExampleTerm(PhysicsTerm):
    """
    PhysicsTerm: SolarCycle_MuJExampleTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "SolarCycle_MuJExampleTerm"
        self.description = "PhysicsTerm: SolarCycle_MuJExampleTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class SolarCycle_PeriodYearsTerm(PhysicsTerm):
    """
    PhysicsTerm: SolarCycle_PeriodYearsTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "SolarCycle_PeriodYearsTerm"
        self.description = "PhysicsTerm: SolarCycle_PeriodYearsTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class SolarCycle_B_j_ModulatedTerm(PhysicsTerm):
    """
    PhysicsTerm: SolarCycle_B_j_ModulatedTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "SolarCycle_B_j_ModulatedTerm"
        self.description = "PhysicsTerm: SolarCycle_B_j_ModulatedTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class SolarWindMod_ModulationFactorTerm(PhysicsTerm):
    """
    PhysicsTerm: SolarWindMod_ModulationFactorTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "SolarWindMod_ModulationFactorTerm"
        self.description = "PhysicsTerm: SolarWindMod_ModulationFactorTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class SolarWindMod_U_g2Term(PhysicsTerm):
    """
    PhysicsTerm: SolarWindMod_U_g2Term
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "SolarWindMod_U_g2Term"
        self.description = "PhysicsTerm: SolarWindMod_U_g2Term"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class SolarWindMod_Delta_swTerm(PhysicsTerm):
    """
    PhysicsTerm: SolarWindMod_Delta_swTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "SolarWindMod_Delta_swTerm"
        self.description = "PhysicsTerm: SolarWindMod_Delta_swTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class SolarWindMod_U_g2_no_modTerm(PhysicsTerm):
    """
    PhysicsTerm: SolarWindMod_U_g2_no_modTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "SolarWindMod_U_g2_no_modTerm"
        self.description = "PhysicsTerm: SolarWindMod_U_g2_no_modTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class SolarWindVel_ModulationFactorTerm(PhysicsTerm):
    """
    PhysicsTerm: SolarWindVel_ModulationFactorTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "SolarWindVel_ModulationFactorTerm"
        self.description = "PhysicsTerm: SolarWindVel_ModulationFactorTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class SolarWindVel_U_g2Term(PhysicsTerm):
    """
    PhysicsTerm: SolarWindVel_U_g2Term
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "SolarWindVel_U_g2Term"
        self.description = "PhysicsTerm: SolarWindVel_U_g2Term"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class SolarWindVel_V_swTerm(PhysicsTerm):
    """
    PhysicsTerm: SolarWindVel_V_swTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "SolarWindVel_V_swTerm"
        self.description = "PhysicsTerm: SolarWindVel_V_swTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class SolarWindVel_V_swKmSTerm(PhysicsTerm):
    """
    PhysicsTerm: SolarWindVel_V_swKmSTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "SolarWindVel_V_swKmSTerm"
        self.description = "PhysicsTerm: SolarWindVel_V_swKmSTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class SolarWindVel_U_g2_no_swTerm(PhysicsTerm):
    """
    PhysicsTerm: SolarWindVel_U_g2_no_swTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "SolarWindVel_U_g2_no_swTerm"
        self.description = "PhysicsTerm: SolarWindVel_U_g2_no_swTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class StellarMass_M_sOverR2Term(PhysicsTerm):
    """
    PhysicsTerm: StellarMass_M_sOverR2Term
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "StellarMass_M_sOverR2Term"
        self.description = "PhysicsTerm: StellarMass_M_sOverR2Term"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class StellarMass_U_g1Term(PhysicsTerm):
    """
    PhysicsTerm: StellarMass_U_g1Term
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "StellarMass_U_g1Term"
        self.description = "PhysicsTerm: StellarMass_U_g1Term"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class StellarMass_U_g2Term(PhysicsTerm):
    """
    PhysicsTerm: StellarMass_U_g2Term
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "StellarMass_U_g2Term"
        self.description = "PhysicsTerm: StellarMass_U_g2Term"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class StellarMass_M_sTerm(PhysicsTerm):
    """
    PhysicsTerm: StellarMass_M_sTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "StellarMass_M_sTerm"
        self.description = "PhysicsTerm: StellarMass_M_sTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class StellarMass_M_sInMsunTerm(PhysicsTerm):
    """
    PhysicsTerm: StellarMass_M_sInMsunTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "StellarMass_M_sInMsunTerm"
        self.description = "PhysicsTerm: StellarMass_M_sInMsunTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class StellarRot_Omega_s_tTerm(PhysicsTerm):
    """
    PhysicsTerm: StellarRot_Omega_s_tTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "StellarRot_Omega_s_tTerm"
        self.description = "PhysicsTerm: StellarRot_Omega_s_tTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class StellarRot_U_g3Term(PhysicsTerm):
    """
    PhysicsTerm: StellarRot_U_g3Term
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "StellarRot_U_g3Term"
        self.description = "PhysicsTerm: StellarRot_U_g3Term"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class StellarRot_U_iTerm(PhysicsTerm):
    """
    PhysicsTerm: StellarRot_U_iTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "StellarRot_U_iTerm"
        self.description = "PhysicsTerm: StellarRot_U_iTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class StellarRot_Omega_sTerm(PhysicsTerm):
    """
    PhysicsTerm: StellarRot_Omega_sTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "StellarRot_Omega_sTerm"
        self.description = "PhysicsTerm: StellarRot_Omega_sTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class StellarRot_Period_daysTerm(PhysicsTerm):
    """
    PhysicsTerm: StellarRot_Period_daysTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "StellarRot_Period_daysTerm"
        self.description = "PhysicsTerm: StellarRot_Period_daysTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class StepFunc_S_r_RbTerm(PhysicsTerm):
    """
    PhysicsTerm: StepFunc_S_r_RbTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "StepFunc_S_r_RbTerm"
        self.description = "PhysicsTerm: StepFunc_S_r_RbTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class StepFunc_U_g2Term(PhysicsTerm):
    """
    PhysicsTerm: StepFunc_U_g2Term
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "StepFunc_U_g2Term"
        self.description = "PhysicsTerm: StepFunc_U_g2Term"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class StressEnergy_T_sTerm(PhysicsTerm):
    """
    PhysicsTerm: StressEnergy_T_sTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "StressEnergy_T_sTerm"
        self.description = "PhysicsTerm: StressEnergy_T_sTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class StressEnergy_A_mu_nuTerm(PhysicsTerm):
    """
    PhysicsTerm: StressEnergy_A_mu_nuTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "StressEnergy_A_mu_nuTerm"
        self.description = "PhysicsTerm: StressEnergy_A_mu_nuTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class StressEnergy_PerturbationTerm(PhysicsTerm):
    """
    PhysicsTerm: StressEnergy_PerturbationTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "StressEnergy_PerturbationTerm"
        self.description = "PhysicsTerm: StressEnergy_PerturbationTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class SurfaceMagField_B_jTerm(PhysicsTerm):
    """
    PhysicsTerm: SurfaceMagField_B_jTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "SurfaceMagField_B_jTerm"
        self.description = "PhysicsTerm: SurfaceMagField_B_jTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class SurfaceMagField_U_g3_exampleTerm(PhysicsTerm):
    """
    PhysicsTerm: SurfaceMagField_U_g3_exampleTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "SurfaceMagField_U_g3_exampleTerm"
        self.description = "PhysicsTerm: SurfaceMagField_U_g3_exampleTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class SurfaceMagField_B_s_minTerm(PhysicsTerm):
    """
    PhysicsTerm: SurfaceMagField_B_s_minTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "SurfaceMagField_B_s_minTerm"
        self.description = "PhysicsTerm: SurfaceMagField_B_s_minTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class SurfaceMagField_B_s_maxTerm(PhysicsTerm):
    """
    PhysicsTerm: SurfaceMagField_B_s_maxTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "SurfaceMagField_B_s_maxTerm"
        self.description = "PhysicsTerm: SurfaceMagField_B_s_maxTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class SurfaceTemp_B_j_hypotheticalTerm(PhysicsTerm):
    """
    PhysicsTerm: SurfaceTemp_B_j_hypotheticalTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "SurfaceTemp_B_j_hypotheticalTerm"
        self.description = "PhysicsTerm: SurfaceTemp_B_j_hypotheticalTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class SurfaceTemp_U_g3_exampleTerm(PhysicsTerm):
    """
    PhysicsTerm: SurfaceTemp_U_g3_exampleTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "SurfaceTemp_U_g3_exampleTerm"
        self.description = "PhysicsTerm: SurfaceTemp_U_g3_exampleTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class SurfaceTemp_T_sTerm(PhysicsTerm):
    """
    PhysicsTerm: SurfaceTemp_T_sTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "SurfaceTemp_T_sTerm"
        self.description = "PhysicsTerm: SurfaceTemp_T_sTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class TimeReversalZone_TRZFactorTerm(PhysicsTerm):
    """
    PhysicsTerm: TimeReversalZone_TRZFactorTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "TimeReversalZone_TRZFactorTerm"
        self.description = "PhysicsTerm: TimeReversalZone_TRZFactorTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class TimeReversalZone_U_i_baseTerm(PhysicsTerm):
    """
    PhysicsTerm: TimeReversalZone_U_i_baseTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "TimeReversalZone_U_i_baseTerm"
        self.description = "PhysicsTerm: TimeReversalZone_U_i_baseTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class TimeReversalZone_U_iTerm(PhysicsTerm):
    """
    PhysicsTerm: TimeReversalZone_U_iTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "TimeReversalZone_U_iTerm"
        self.description = "PhysicsTerm: TimeReversalZone_U_iTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class TimeReversalZone_F_TRZTerm(PhysicsTerm):
    """
    PhysicsTerm: TimeReversalZone_F_TRZTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "TimeReversalZone_F_TRZTerm"
        self.description = "PhysicsTerm: TimeReversalZone_F_TRZTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class TimeReversalZone_U_i_no_TRZTerm(PhysicsTerm):
    """
    PhysicsTerm: TimeReversalZone_U_i_no_TRZTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "TimeReversalZone_U_i_no_TRZTerm"
        self.description = "PhysicsTerm: TimeReversalZone_U_i_no_TRZTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class Ug1Defect_Delta_defTerm(PhysicsTerm):
    """
    PhysicsTerm: Ug1Defect_Delta_defTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "Ug1Defect_Delta_defTerm"
        self.description = "PhysicsTerm: Ug1Defect_Delta_defTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class Ug1Defect_U_g1Term(PhysicsTerm):
    """
    PhysicsTerm: Ug1Defect_U_g1Term
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "Ug1Defect_U_g1Term"
        self.description = "PhysicsTerm: Ug1Defect_U_g1Term"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class Ug1Defect_Period_yearsTerm(PhysicsTerm):
    """
    PhysicsTerm: Ug1Defect_Period_yearsTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "Ug1Defect_Period_yearsTerm"
        self.description = "PhysicsTerm: Ug1Defect_Period_yearsTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class Ug3DiskVec_PhiHat_jTerm(PhysicsTerm):
    """
    PhysicsTerm: Ug3DiskVec_PhiHat_jTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "Ug3DiskVec_PhiHat_jTerm"
        self.description = "PhysicsTerm: Ug3DiskVec_PhiHat_jTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class Ug3DiskVec_UmBaseTerm(PhysicsTerm):
    """
    PhysicsTerm: Ug3DiskVec_UmBaseTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "Ug3DiskVec_UmBaseTerm"
        self.description = "PhysicsTerm: Ug3DiskVec_UmBaseTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class Ug3DiskVec_UmContributionTerm(PhysicsTerm):
    """
    PhysicsTerm: Ug3DiskVec_UmContributionTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "Ug3DiskVec_UmContributionTerm"
        self.description = "PhysicsTerm: Ug3DiskVec_UmContributionTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class Ug3DiskVec_PhiHatMagnitudeTerm(PhysicsTerm):
    """
    PhysicsTerm: Ug3DiskVec_PhiHatMagnitudeTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "Ug3DiskVec_PhiHatMagnitudeTerm"
        self.description = "PhysicsTerm: Ug3DiskVec_PhiHatMagnitudeTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class AetherVacDensity_Rho_vac_ATerm(PhysicsTerm):
    """
    PhysicsTerm: AetherVacDensity_Rho_vac_ATerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "AetherVacDensity_Rho_vac_ATerm"
        self.description = "PhysicsTerm: AetherVacDensity_Rho_vac_ATerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class AetherVacDensity_T_s_contributionTerm(PhysicsTerm):
    """
    PhysicsTerm: AetherVacDensity_T_s_contributionTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "AetherVacDensity_T_s_contributionTerm"
        self.description = "PhysicsTerm: AetherVacDensity_T_s_contributionTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class AetherVacDensity_A_mu_nuTerm(PhysicsTerm):
    """
    PhysicsTerm: AetherVacDensity_A_mu_nuTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "AetherVacDensity_A_mu_nuTerm"
        self.description = "PhysicsTerm: AetherVacDensity_A_mu_nuTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class AetherVacDensity_Epsilon_factorTerm(PhysicsTerm):
    """
    PhysicsTerm: AetherVacDensity_Epsilon_factorTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "AetherVacDensity_Epsilon_factorTerm"
        self.description = "PhysicsTerm: AetherVacDensity_Epsilon_factorTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class UnivInertiaVac_Rho_vac_UiTerm(PhysicsTerm):
    """
    PhysicsTerm: UnivInertiaVac_Rho_vac_UiTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "UnivInertiaVac_Rho_vac_UiTerm"
        self.description = "PhysicsTerm: UnivInertiaVac_Rho_vac_UiTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class UnivInertiaVac_U_i_baseTerm(PhysicsTerm):
    """
    PhysicsTerm: UnivInertiaVac_U_i_baseTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "UnivInertiaVac_U_i_baseTerm"
        self.description = "PhysicsTerm: UnivInertiaVac_U_i_baseTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class UnivInertiaVac_U_iTerm(PhysicsTerm):
    """
    PhysicsTerm: UnivInertiaVac_U_iTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "UnivInertiaVac_U_iTerm"
        self.description = "PhysicsTerm: UnivInertiaVac_U_iTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class ScmVacDensity_Rho_vac_SCmTerm(PhysicsTerm):
    """
    PhysicsTerm: ScmVacDensity_Rho_vac_SCmTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "ScmVacDensity_Rho_vac_SCmTerm"
        self.description = "PhysicsTerm: ScmVacDensity_Rho_vac_SCmTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class ScmVacDensity_U_g2_exampleTerm(PhysicsTerm):
    """
    PhysicsTerm: ScmVacDensity_U_g2_exampleTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "ScmVacDensity_U_g2_exampleTerm"
        self.description = "PhysicsTerm: ScmVacDensity_U_g2_exampleTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class ScmVacDensity_U_i_exampleTerm(PhysicsTerm):
    """
    PhysicsTerm: ScmVacDensity_U_i_exampleTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "ScmVacDensity_U_i_exampleTerm"
        self.description = "PhysicsTerm: ScmVacDensity_U_i_exampleTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class UaVacDensity_Rho_vac_UATerm(PhysicsTerm):
    """
    PhysicsTerm: UaVacDensity_Rho_vac_UATerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "UaVacDensity_Rho_vac_UATerm"
        self.description = "PhysicsTerm: UaVacDensity_Rho_vac_UATerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class UaVacDensity_U_g2_exampleTerm(PhysicsTerm):
    """
    PhysicsTerm: UaVacDensity_U_g2_exampleTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "UaVacDensity_U_g2_exampleTerm"
        self.description = "PhysicsTerm: UaVacDensity_U_g2_exampleTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class UaVacDensity_U_i_exampleTerm(PhysicsTerm):
    """
    PhysicsTerm: UaVacDensity_U_i_exampleTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "UaVacDensity_U_i_exampleTerm"
        self.description = "PhysicsTerm: UaVacDensity_U_i_exampleTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class UnivInertiaVac2_U_i_baseTerm(PhysicsTerm):
    """
    PhysicsTerm: UnivInertiaVac2_U_i_baseTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "UnivInertiaVac2_U_i_baseTerm"
        self.description = "PhysicsTerm: UnivInertiaVac2_U_i_baseTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class UnivInertiaVac2_U_iTerm(PhysicsTerm):
    """
    PhysicsTerm: UnivInertiaVac2_U_iTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "UnivInertiaVac2_U_iTerm"
        self.description = "PhysicsTerm: UnivInertiaVac2_U_iTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class ScmVelocity_V_scmTerm(PhysicsTerm):
    """
    PhysicsTerm: ScmVelocity_V_scmTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "ScmVelocity_V_scmTerm"
        self.description = "PhysicsTerm: ScmVelocity_V_scmTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class ScmVelocity_E_react_baseTerm(PhysicsTerm):
    """
    PhysicsTerm: ScmVelocity_E_react_baseTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "ScmVelocity_E_react_baseTerm"
        self.description = "PhysicsTerm: ScmVelocity_E_react_baseTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class ScmVelocity_E_reactTerm(PhysicsTerm):
    """
    PhysicsTerm: ScmVelocity_E_reactTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "ScmVelocity_E_reactTerm"
        self.description = "PhysicsTerm: ScmVelocity_E_reactTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class ScmVelocity_UmExampleTerm(PhysicsTerm):
    """
    PhysicsTerm: ScmVelocity_UmExampleTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "ScmVelocity_UmExampleTerm"
        self.description = "PhysicsTerm: ScmVelocity_UmExampleTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class ButterflyNebula_DPM_momentumTerm(PhysicsTerm):
    """
    PhysicsTerm: ButterflyNebula_DPM_momentumTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "ButterflyNebula_DPM_momentumTerm"
        self.description = "PhysicsTerm: ButterflyNebula_DPM_momentumTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class ButterflyNebula_DPM_gravityTerm(PhysicsTerm):
    """
    PhysicsTerm: ButterflyNebula_DPM_gravityTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "ButterflyNebula_DPM_gravityTerm"
        self.description = "PhysicsTerm: ButterflyNebula_DPM_gravityTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class ButterflyNebula_LENR_termTerm(PhysicsTerm):
    """
    PhysicsTerm: ButterflyNebula_LENR_termTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "ButterflyNebula_LENR_termTerm"
        self.description = "PhysicsTerm: ButterflyNebula_LENR_termTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class ButterflyNebula_F_U_BiTerm(PhysicsTerm):
    """
    PhysicsTerm: ButterflyNebula_F_U_BiTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "ButterflyNebula_F_U_BiTerm"
        self.description = "PhysicsTerm: ButterflyNebula_F_U_BiTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class CentaurusA_DPM_momentumTerm(PhysicsTerm):
    """
    PhysicsTerm: CentaurusA_DPM_momentumTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "CentaurusA_DPM_momentumTerm"
        self.description = "PhysicsTerm: CentaurusA_DPM_momentumTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class CentaurusA_F_U_BiTerm(PhysicsTerm):
    """
    PhysicsTerm: CentaurusA_F_U_BiTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "CentaurusA_F_U_BiTerm"
        self.description = "PhysicsTerm: CentaurusA_F_U_BiTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class Abell2256_IntegrandTerm(PhysicsTerm):
    """
    PhysicsTerm: Abell2256_IntegrandTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "Abell2256_IntegrandTerm"
        self.description = "PhysicsTerm: Abell2256_IntegrandTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class Abell2256_DPM_resonanceTerm(PhysicsTerm):
    """
    PhysicsTerm: Abell2256_DPM_resonanceTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "Abell2256_DPM_resonanceTerm"
        self.description = "PhysicsTerm: Abell2256_DPM_resonanceTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class Abell2256_X2Term(PhysicsTerm):
    """
    PhysicsTerm: Abell2256_X2Term
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "Abell2256_X2Term"
        self.description = "PhysicsTerm: Abell2256_X2Term"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class Abell2256_LENR_termTerm(PhysicsTerm):
    """
    PhysicsTerm: Abell2256_LENR_termTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "Abell2256_LENR_termTerm"
        self.description = "PhysicsTerm: Abell2256_LENR_termTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class Abell2256_Q_waveTerm(PhysicsTerm):
    """
    PhysicsTerm: Abell2256_Q_waveTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "Abell2256_Q_waveTerm"
        self.description = "PhysicsTerm: Abell2256_Q_waveTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class Abell2256_Ub1Term(PhysicsTerm):
    """
    PhysicsTerm: Abell2256_Ub1Term
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "Abell2256_Ub1Term"
        self.description = "PhysicsTerm: Abell2256_Ub1Term"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class Abell2256_UiTerm(PhysicsTerm):
    """
    PhysicsTerm: Abell2256_UiTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "Abell2256_UiTerm"
        self.description = "PhysicsTerm: Abell2256_UiTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class Abell2256_FTerm(PhysicsTerm):
    """
    PhysicsTerm: Abell2256_FTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "Abell2256_FTerm"
        self.description = "PhysicsTerm: Abell2256_FTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class Abell2256_CompressedTerm(PhysicsTerm):
    """
    PhysicsTerm: Abell2256_CompressedTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "Abell2256_CompressedTerm"
        self.description = "PhysicsTerm: Abell2256_CompressedTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class Abell2256_ResonantTerm(PhysicsTerm):
    """
    PhysicsTerm: Abell2256_ResonantTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "Abell2256_ResonantTerm"
        self.description = "PhysicsTerm: Abell2256_ResonantTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class Abell2256_BuoyancyTerm(PhysicsTerm):
    """
    PhysicsTerm: Abell2256_BuoyancyTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "Abell2256_BuoyancyTerm"
        self.description = "PhysicsTerm: Abell2256_BuoyancyTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class Abell2256_SuperconductiveTerm(PhysicsTerm):
    """
    PhysicsTerm: Abell2256_SuperconductiveTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "Abell2256_SuperconductiveTerm"
        self.description = "PhysicsTerm: Abell2256_SuperconductiveTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class Abell2256_CompressedGTerm(PhysicsTerm):
    """
    PhysicsTerm: Abell2256_CompressedGTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "Abell2256_CompressedGTerm"
        self.description = "PhysicsTerm: Abell2256_CompressedGTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class ASASSN14li_IntegrandTerm(PhysicsTerm):
    """
    PhysicsTerm: ASASSN14li_IntegrandTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "ASASSN14li_IntegrandTerm"
        self.description = "PhysicsTerm: ASASSN14li_IntegrandTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class ASASSN14li_DPM_resonanceTerm(PhysicsTerm):
    """
    PhysicsTerm: ASASSN14li_DPM_resonanceTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "ASASSN14li_DPM_resonanceTerm"
        self.description = "PhysicsTerm: ASASSN14li_DPM_resonanceTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class ASASSN14li_X2Term(PhysicsTerm):
    """
    PhysicsTerm: ASASSN14li_X2Term
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "ASASSN14li_X2Term"
        self.description = "PhysicsTerm: ASASSN14li_X2Term"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class ASASSN14li_LENR_termTerm(PhysicsTerm):
    """
    PhysicsTerm: ASASSN14li_LENR_termTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "ASASSN14li_LENR_termTerm"
        self.description = "PhysicsTerm: ASASSN14li_LENR_termTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class ASASSN14li_Q_waveTerm(PhysicsTerm):
    """
    PhysicsTerm: ASASSN14li_Q_waveTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "ASASSN14li_Q_waveTerm"
        self.description = "PhysicsTerm: ASASSN14li_Q_waveTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class ASASSN14li_FTerm(PhysicsTerm):
    """
    PhysicsTerm: ASASSN14li_FTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "ASASSN14li_FTerm"
        self.description = "PhysicsTerm: ASASSN14li_FTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class CentaurusA_X2Term(PhysicsTerm):
    """
    PhysicsTerm: CentaurusA_X2Term
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "CentaurusA_X2Term"
        self.description = "PhysicsTerm: CentaurusA_X2Term"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class CentaurusA_LENR_termTerm(PhysicsTerm):
    """
    PhysicsTerm: CentaurusA_LENR_termTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "CentaurusA_LENR_termTerm"
        self.description = "PhysicsTerm: CentaurusA_LENR_termTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class CentaurusA_FTerm(PhysicsTerm):
    """
    PhysicsTerm: CentaurusA_FTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "CentaurusA_FTerm"
        self.description = "PhysicsTerm: CentaurusA_FTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class CentaurusA_CompressedTerm(PhysicsTerm):
    """
    PhysicsTerm: CentaurusA_CompressedTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "CentaurusA_CompressedTerm"
        self.description = "PhysicsTerm: CentaurusA_CompressedTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class CrabNebula_IntegrandTerm(PhysicsTerm):
    """
    PhysicsTerm: CrabNebula_IntegrandTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "CrabNebula_IntegrandTerm"
        self.description = "PhysicsTerm: CrabNebula_IntegrandTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class CrabNebula_LENR_termTerm(PhysicsTerm):
    """
    PhysicsTerm: CrabNebula_LENR_termTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "CrabNebula_LENR_termTerm"
        self.description = "PhysicsTerm: CrabNebula_LENR_termTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class CrabNebula_FTerm(PhysicsTerm):
    """
    PhysicsTerm: CrabNebula_FTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "CrabNebula_FTerm"
        self.description = "PhysicsTerm: CrabNebula_FTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class CrabNebula_CompressedTerm(PhysicsTerm):
    """
    PhysicsTerm: CrabNebula_CompressedTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "CrabNebula_CompressedTerm"
        self.description = "PhysicsTerm: CrabNebula_CompressedTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class ElGordo_IntegrandTerm(PhysicsTerm):
    """
    PhysicsTerm: ElGordo_IntegrandTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "ElGordo_IntegrandTerm"
        self.description = "PhysicsTerm: ElGordo_IntegrandTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class ElGordo_LENR_termTerm(PhysicsTerm):
    """
    PhysicsTerm: ElGordo_LENR_termTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "ElGordo_LENR_termTerm"
        self.description = "PhysicsTerm: ElGordo_LENR_termTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class ElGordo_FTerm(PhysicsTerm):
    """
    PhysicsTerm: ElGordo_FTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "ElGordo_FTerm"
        self.description = "PhysicsTerm: ElGordo_FTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class ESO137_FTerm(PhysicsTerm):
    """
    PhysicsTerm: ESO137_FTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "ESO137_FTerm"
        self.description = "PhysicsTerm: ESO137_FTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class IC2163_IntegrandTerm(PhysicsTerm):
    """
    PhysicsTerm: IC2163_IntegrandTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "IC2163_IntegrandTerm"
        self.description = "PhysicsTerm: IC2163_IntegrandTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class IC2163_LENR_termTerm(PhysicsTerm):
    """
    PhysicsTerm: IC2163_LENR_termTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "IC2163_LENR_termTerm"
        self.description = "PhysicsTerm: IC2163_LENR_termTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class IC2163_FTerm(PhysicsTerm):
    """
    PhysicsTerm: IC2163_FTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "IC2163_FTerm"
        self.description = "PhysicsTerm: IC2163_FTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class IC2163_CompressedTerm(PhysicsTerm):
    """
    PhysicsTerm: IC2163_CompressedTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "IC2163_CompressedTerm"
        self.description = "PhysicsTerm: IC2163_CompressedTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class J1610_IntegrandTerm(PhysicsTerm):
    """
    PhysicsTerm: J1610_IntegrandTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "J1610_IntegrandTerm"
        self.description = "PhysicsTerm: J1610_IntegrandTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class J1610_LENR_termTerm(PhysicsTerm):
    """
    PhysicsTerm: J1610_LENR_termTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "J1610_LENR_termTerm"
        self.description = "PhysicsTerm: J1610_LENR_termTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class J1610_FTerm(PhysicsTerm):
    """
    PhysicsTerm: J1610_FTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "J1610_FTerm"
        self.description = "PhysicsTerm: J1610_FTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class J1610_CompressedTerm(PhysicsTerm):
    """
    PhysicsTerm: J1610_CompressedTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "J1610_CompressedTerm"
        self.description = "PhysicsTerm: J1610_CompressedTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class JupiterAurorae_IntegrandTerm(PhysicsTerm):
    """
    PhysicsTerm: JupiterAurorae_IntegrandTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "JupiterAurorae_IntegrandTerm"
        self.description = "PhysicsTerm: JupiterAurorae_IntegrandTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class JupiterAurorae_LENR_termTerm(PhysicsTerm):
    """
    PhysicsTerm: JupiterAurorae_LENR_termTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "JupiterAurorae_LENR_termTerm"
        self.description = "PhysicsTerm: JupiterAurorae_LENR_termTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class JupiterAurorae_FTerm(PhysicsTerm):
    """
    PhysicsTerm: JupiterAurorae_FTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "JupiterAurorae_FTerm"
        self.description = "PhysicsTerm: JupiterAurorae_FTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class JupiterAurorae_CompressedTerm(PhysicsTerm):
    """
    PhysicsTerm: JupiterAurorae_CompressedTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "JupiterAurorae_CompressedTerm"
        self.description = "PhysicsTerm: JupiterAurorae_CompressedTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class LagoonNebula_IntegrandTerm(PhysicsTerm):
    """
    PhysicsTerm: LagoonNebula_IntegrandTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "LagoonNebula_IntegrandTerm"
        self.description = "PhysicsTerm: LagoonNebula_IntegrandTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class LagoonNebula_LENR_termTerm(PhysicsTerm):
    """
    PhysicsTerm: LagoonNebula_LENR_termTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "LagoonNebula_LENR_termTerm"
        self.description = "PhysicsTerm: LagoonNebula_LENR_termTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class LagoonNebula_FTerm(PhysicsTerm):
    """
    PhysicsTerm: LagoonNebula_FTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "LagoonNebula_FTerm"
        self.description = "PhysicsTerm: LagoonNebula_FTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class LagoonNebula_CompressedTerm(PhysicsTerm):
    """
    PhysicsTerm: LagoonNebula_CompressedTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "LagoonNebula_CompressedTerm"
        self.description = "PhysicsTerm: LagoonNebula_CompressedTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class LagoonNebulaVar_IntegrandTerm(PhysicsTerm):
    """
    PhysicsTerm: LagoonNebulaVar_IntegrandTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "LagoonNebulaVar_IntegrandTerm"
        self.description = "PhysicsTerm: LagoonNebulaVar_IntegrandTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class LagoonNebulaVar_LENR_termTerm(PhysicsTerm):
    """
    PhysicsTerm: LagoonNebulaVar_LENR_termTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "LagoonNebulaVar_LENR_termTerm"
        self.description = "PhysicsTerm: LagoonNebulaVar_LENR_termTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class LagoonNebulaVar_FTerm(PhysicsTerm):
    """
    PhysicsTerm: LagoonNebulaVar_FTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "LagoonNebulaVar_FTerm"
        self.description = "PhysicsTerm: LagoonNebulaVar_FTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class LagoonNebulaVar_CompressedTerm(PhysicsTerm):
    """
    PhysicsTerm: LagoonNebulaVar_CompressedTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "LagoonNebulaVar_CompressedTerm"
        self.description = "PhysicsTerm: LagoonNebulaVar_CompressedTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class M87Jet_IntegrandTerm(PhysicsTerm):
    """
    PhysicsTerm: M87Jet_IntegrandTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "M87Jet_IntegrandTerm"
        self.description = "PhysicsTerm: M87Jet_IntegrandTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class M87Jet_LENR_termTerm(PhysicsTerm):
    """
    PhysicsTerm: M87Jet_LENR_termTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "M87Jet_LENR_termTerm"
        self.description = "PhysicsTerm: M87Jet_LENR_termTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class M87Jet_FTerm(PhysicsTerm):
    """
    PhysicsTerm: M87Jet_FTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "M87Jet_FTerm"
        self.description = "PhysicsTerm: M87Jet_FTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class M87Jet_CompressedTerm(PhysicsTerm):
    """
    PhysicsTerm: M87Jet_CompressedTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "M87Jet_CompressedTerm"
        self.description = "PhysicsTerm: M87Jet_CompressedTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class NGC1365_IntegrandTerm(PhysicsTerm):
    """
    PhysicsTerm: NGC1365_IntegrandTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "NGC1365_IntegrandTerm"
        self.description = "PhysicsTerm: NGC1365_IntegrandTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class NGC1365_LENR_termTerm(PhysicsTerm):
    """
    PhysicsTerm: NGC1365_LENR_termTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "NGC1365_LENR_termTerm"
        self.description = "PhysicsTerm: NGC1365_LENR_termTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class NGC1365_FTerm(PhysicsTerm):
    """
    PhysicsTerm: NGC1365_FTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "NGC1365_FTerm"
        self.description = "PhysicsTerm: NGC1365_FTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class NGC2207_IntegrandTerm(PhysicsTerm):
    """
    PhysicsTerm: NGC2207_IntegrandTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "NGC2207_IntegrandTerm"
        self.description = "PhysicsTerm: NGC2207_IntegrandTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class NGC2207_LENR_termTerm(PhysicsTerm):
    """
    PhysicsTerm: NGC2207_LENR_termTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "NGC2207_LENR_termTerm"
        self.description = "PhysicsTerm: NGC2207_LENR_termTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class NGC2207_FTerm(PhysicsTerm):
    """
    PhysicsTerm: NGC2207_FTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "NGC2207_FTerm"
        self.description = "PhysicsTerm: NGC2207_FTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class NGC2207_CompressedTerm(PhysicsTerm):
    """
    PhysicsTerm: NGC2207_CompressedTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "NGC2207_CompressedTerm"
        self.description = "PhysicsTerm: NGC2207_CompressedTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class RAquarii_IntegrandTerm(PhysicsTerm):
    """
    PhysicsTerm: RAquarii_IntegrandTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "RAquarii_IntegrandTerm"
        self.description = "PhysicsTerm: RAquarii_IntegrandTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class RAquarii_LENR_termTerm(PhysicsTerm):
    """
    PhysicsTerm: RAquarii_LENR_termTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "RAquarii_LENR_termTerm"
        self.description = "PhysicsTerm: RAquarii_LENR_termTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class RAquarii_FTerm(PhysicsTerm):
    """
    PhysicsTerm: RAquarii_FTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "RAquarii_FTerm"
        self.description = "PhysicsTerm: RAquarii_FTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class RAquarii_CompressedTerm(PhysicsTerm):
    """
    PhysicsTerm: RAquarii_CompressedTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "RAquarii_CompressedTerm"
        self.description = "PhysicsTerm: RAquarii_CompressedTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class SgrAStar_IntegrandTerm(PhysicsTerm):
    """
    PhysicsTerm: SgrAStar_IntegrandTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "SgrAStar_IntegrandTerm"
        self.description = "PhysicsTerm: SgrAStar_IntegrandTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class SgrAStar_LENR_termTerm(PhysicsTerm):
    """
    PhysicsTerm: SgrAStar_LENR_termTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "SgrAStar_LENR_termTerm"
        self.description = "PhysicsTerm: SgrAStar_LENR_termTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class SgrAStar_FTerm(PhysicsTerm):
    """
    PhysicsTerm: SgrAStar_FTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "SgrAStar_FTerm"
        self.description = "PhysicsTerm: SgrAStar_FTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class SgrAStar_CompressedTerm(PhysicsTerm):
    """
    PhysicsTerm: SgrAStar_CompressedTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "SgrAStar_CompressedTerm"
        self.description = "PhysicsTerm: SgrAStar_CompressedTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class SPTCLJ2215_IntegrandTerm(PhysicsTerm):
    """
    PhysicsTerm: SPTCLJ2215_IntegrandTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "SPTCLJ2215_IntegrandTerm"
        self.description = "PhysicsTerm: SPTCLJ2215_IntegrandTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class SPTCLJ2215_LENR_termTerm(PhysicsTerm):
    """
    PhysicsTerm: SPTCLJ2215_LENR_termTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "SPTCLJ2215_LENR_termTerm"
        self.description = "PhysicsTerm: SPTCLJ2215_LENR_termTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class SPTCLJ2215_FTerm(PhysicsTerm):
    """
    PhysicsTerm: SPTCLJ2215_FTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "SPTCLJ2215_FTerm"
        self.description = "PhysicsTerm: SPTCLJ2215_FTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class SPTCLJ2215_CompressedTerm(PhysicsTerm):
    """
    PhysicsTerm: SPTCLJ2215_CompressedTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "SPTCLJ2215_CompressedTerm"
        self.description = "PhysicsTerm: SPTCLJ2215_CompressedTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class StephanQuintet_IntegrandTerm(PhysicsTerm):
    """
    PhysicsTerm: StephanQuintet_IntegrandTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "StephanQuintet_IntegrandTerm"
        self.description = "PhysicsTerm: StephanQuintet_IntegrandTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class StephanQuintet_LENR_termTerm(PhysicsTerm):
    """
    PhysicsTerm: StephanQuintet_LENR_termTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "StephanQuintet_LENR_termTerm"
        self.description = "PhysicsTerm: StephanQuintet_LENR_termTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class StephanQuintet_FTerm(PhysicsTerm):
    """
    PhysicsTerm: StephanQuintet_FTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "StephanQuintet_FTerm"
        self.description = "PhysicsTerm: StephanQuintet_FTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class StephanQuintet_CompressedTerm(PhysicsTerm):
    """
    PhysicsTerm: StephanQuintet_CompressedTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "StephanQuintet_CompressedTerm"
        self.description = "PhysicsTerm: StephanQuintet_CompressedTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class VelaPulsar_IntegrandTerm(PhysicsTerm):
    """
    PhysicsTerm: VelaPulsar_IntegrandTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "VelaPulsar_IntegrandTerm"
        self.description = "PhysicsTerm: VelaPulsar_IntegrandTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class VelaPulsar_LENR_termTerm(PhysicsTerm):
    """
    PhysicsTerm: VelaPulsar_LENR_termTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "VelaPulsar_LENR_termTerm"
        self.description = "PhysicsTerm: VelaPulsar_LENR_termTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class VelaPulsar_FTerm(PhysicsTerm):
    """
    PhysicsTerm: VelaPulsar_FTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "VelaPulsar_FTerm"
        self.description = "PhysicsTerm: VelaPulsar_FTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class VelaPulsar_CompressedTerm(PhysicsTerm):
    """
    PhysicsTerm: VelaPulsar_CompressedTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "VelaPulsar_CompressedTerm"
        self.description = "PhysicsTerm: VelaPulsar_CompressedTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class NGC4993_IntegrandTerm(PhysicsTerm):
    """
    PhysicsTerm: NGC4993_IntegrandTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "NGC4993_IntegrandTerm"
        self.description = "PhysicsTerm: NGC4993_IntegrandTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class NGC4993_LENR_termTerm(PhysicsTerm):
    """
    PhysicsTerm: NGC4993_LENR_termTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "NGC4993_LENR_termTerm"
        self.description = "PhysicsTerm: NGC4993_LENR_termTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class NGC4993_FTerm(PhysicsTerm):
    """
    PhysicsTerm: NGC4993_FTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "NGC4993_FTerm"
        self.description = "PhysicsTerm: NGC4993_FTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class NGC4993_CompressedTerm(PhysicsTerm):
    """
    PhysicsTerm: NGC4993_CompressedTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "NGC4993_CompressedTerm"
        self.description = "PhysicsTerm: NGC4993_CompressedTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class NGC6240_IntegrandTerm(PhysicsTerm):
    """
    PhysicsTerm: NGC6240_IntegrandTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "NGC6240_IntegrandTerm"
        self.description = "PhysicsTerm: NGC6240_IntegrandTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class NGC6240_LENR_termTerm(PhysicsTerm):
    """
    PhysicsTerm: NGC6240_LENR_termTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "NGC6240_LENR_termTerm"
        self.description = "PhysicsTerm: NGC6240_LENR_termTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class NGC6240_FTerm(PhysicsTerm):
    """
    PhysicsTerm: NGC6240_FTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "NGC6240_FTerm"
        self.description = "PhysicsTerm: NGC6240_FTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class NGC6240_CompressedTerm(PhysicsTerm):
    """
    PhysicsTerm: NGC6240_CompressedTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "NGC6240_CompressedTerm"
        self.description = "PhysicsTerm: NGC6240_CompressedTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class OmegaCentauri_IntegrandTerm(PhysicsTerm):
    """
    PhysicsTerm: OmegaCentauri_IntegrandTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "OmegaCentauri_IntegrandTerm"
        self.description = "PhysicsTerm: OmegaCentauri_IntegrandTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class OmegaCentauri_LENR_termTerm(PhysicsTerm):
    """
    PhysicsTerm: OmegaCentauri_LENR_termTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "OmegaCentauri_LENR_termTerm"
        self.description = "PhysicsTerm: OmegaCentauri_LENR_termTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class OmegaCentauri_FTerm(PhysicsTerm):
    """
    PhysicsTerm: OmegaCentauri_FTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "OmegaCentauri_FTerm"
        self.description = "PhysicsTerm: OmegaCentauri_FTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class OmegaCentauri_CompressedTerm(PhysicsTerm):
    """
    PhysicsTerm: OmegaCentauri_CompressedTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "OmegaCentauri_CompressedTerm"
        self.description = "PhysicsTerm: OmegaCentauri_CompressedTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class OrionNebula_IntegrandTerm(PhysicsTerm):
    """
    PhysicsTerm: OrionNebula_IntegrandTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "OrionNebula_IntegrandTerm"
        self.description = "PhysicsTerm: OrionNebula_IntegrandTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class OrionNebula_LENR_termTerm(PhysicsTerm):
    """
    PhysicsTerm: OrionNebula_LENR_termTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "OrionNebula_LENR_termTerm"
        self.description = "PhysicsTerm: OrionNebula_LENR_termTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class OrionNebula_FTerm(PhysicsTerm):
    """
    PhysicsTerm: OrionNebula_FTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "OrionNebula_FTerm"
        self.description = "PhysicsTerm: OrionNebula_FTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class OrionNebula_CompressedTerm(PhysicsTerm):
    """
    PhysicsTerm: OrionNebula_CompressedTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "OrionNebula_CompressedTerm"
        self.description = "PhysicsTerm: OrionNebula_CompressedTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class Perseus_IntegrandTerm(PhysicsTerm):
    """
    PhysicsTerm: Perseus_IntegrandTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "Perseus_IntegrandTerm"
        self.description = "PhysicsTerm: Perseus_IntegrandTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class Perseus_LENR_termTerm(PhysicsTerm):
    """
    PhysicsTerm: Perseus_LENR_termTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "Perseus_LENR_termTerm"
        self.description = "PhysicsTerm: Perseus_LENR_termTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class Perseus_FTerm(PhysicsTerm):
    """
    PhysicsTerm: Perseus_FTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "Perseus_FTerm"
        self.description = "PhysicsTerm: Perseus_FTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class Perseus_CompressedTerm(PhysicsTerm):
    """
    PhysicsTerm: Perseus_CompressedTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "Perseus_CompressedTerm"
        self.description = "PhysicsTerm: Perseus_CompressedTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class PolarRing_IntegrandTerm(PhysicsTerm):
    """
    PhysicsTerm: PolarRing_IntegrandTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "PolarRing_IntegrandTerm"
        self.description = "PhysicsTerm: PolarRing_IntegrandTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class PolarRing_LENR_termTerm(PhysicsTerm):
    """
    PhysicsTerm: PolarRing_LENR_termTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "PolarRing_LENR_termTerm"
        self.description = "PhysicsTerm: PolarRing_LENR_termTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class PolarRing_FTerm(PhysicsTerm):
    """
    PhysicsTerm: PolarRing_FTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "PolarRing_FTerm"
        self.description = "PhysicsTerm: PolarRing_FTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class PolarRing_CompressedTerm(PhysicsTerm):
    """
    PhysicsTerm: PolarRing_CompressedTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "PolarRing_CompressedTerm"
        self.description = "PhysicsTerm: PolarRing_CompressedTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class QuasarJ0313_IntegrandTerm(PhysicsTerm):
    """
    PhysicsTerm: QuasarJ0313_IntegrandTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "QuasarJ0313_IntegrandTerm"
        self.description = "PhysicsTerm: QuasarJ0313_IntegrandTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class QuasarJ0313_LENR_termTerm(PhysicsTerm):
    """
    PhysicsTerm: QuasarJ0313_LENR_termTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "QuasarJ0313_LENR_termTerm"
        self.description = "PhysicsTerm: QuasarJ0313_LENR_termTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class QuasarJ0313_FTerm(PhysicsTerm):
    """
    PhysicsTerm: QuasarJ0313_FTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "QuasarJ0313_FTerm"
        self.description = "PhysicsTerm: QuasarJ0313_FTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class QuasarJ0313_CompressedTerm(PhysicsTerm):
    """
    PhysicsTerm: QuasarJ0313_CompressedTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "QuasarJ0313_CompressedTerm"
        self.description = "PhysicsTerm: QuasarJ0313_CompressedTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class RXJ1713_IntegrandTerm(PhysicsTerm):
    """
    PhysicsTerm: RXJ1713_IntegrandTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "RXJ1713_IntegrandTerm"
        self.description = "PhysicsTerm: RXJ1713_IntegrandTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class RXJ1713_LENR_termTerm(PhysicsTerm):
    """
    PhysicsTerm: RXJ1713_LENR_termTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "RXJ1713_LENR_termTerm"
        self.description = "PhysicsTerm: RXJ1713_LENR_termTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class RXJ1713_FTerm(PhysicsTerm):
    """
    PhysicsTerm: RXJ1713_FTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "RXJ1713_FTerm"
        self.description = "PhysicsTerm: RXJ1713_FTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class RXJ1713_CompressedTerm(PhysicsTerm):
    """
    PhysicsTerm: RXJ1713_CompressedTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "RXJ1713_CompressedTerm"
        self.description = "PhysicsTerm: RXJ1713_CompressedTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class BuoyancyAstro_IntegrandTerm(PhysicsTerm):
    """
    PhysicsTerm: BuoyancyAstro_IntegrandTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "BuoyancyAstro_IntegrandTerm"
        self.description = "PhysicsTerm: BuoyancyAstro_IntegrandTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class BuoyancyAstro_LENR_termTerm(PhysicsTerm):
    """
    PhysicsTerm: BuoyancyAstro_LENR_termTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "BuoyancyAstro_LENR_termTerm"
        self.description = "PhysicsTerm: BuoyancyAstro_LENR_termTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class BuoyancyAstro_FTerm(PhysicsTerm):
    """
    PhysicsTerm: BuoyancyAstro_FTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "BuoyancyAstro_FTerm"
        self.description = "PhysicsTerm: BuoyancyAstro_FTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class BuoyancyAstro_CompressedTerm(PhysicsTerm):
    """
    PhysicsTerm: BuoyancyAstro_CompressedTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "BuoyancyAstro_CompressedTerm"
        self.description = "PhysicsTerm: BuoyancyAstro_CompressedTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class BuoyancyCNB_IntegrandTerm(PhysicsTerm):
    """
    PhysicsTerm: BuoyancyCNB_IntegrandTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "BuoyancyCNB_IntegrandTerm"
        self.description = "PhysicsTerm: BuoyancyCNB_IntegrandTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class BuoyancyCNB_LENR_termTerm(PhysicsTerm):
    """
    PhysicsTerm: BuoyancyCNB_LENR_termTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "BuoyancyCNB_LENR_termTerm"
        self.description = "PhysicsTerm: BuoyancyCNB_LENR_termTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class BuoyancyCNB_FTerm(PhysicsTerm):
    """
    PhysicsTerm: BuoyancyCNB_FTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "BuoyancyCNB_FTerm"
        self.description = "PhysicsTerm: BuoyancyCNB_FTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class BuoyancyCNB_CompressedTerm(PhysicsTerm):
    """
    PhysicsTerm: BuoyancyCNB_CompressedTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "BuoyancyCNB_CompressedTerm"
        self.description = "PhysicsTerm: BuoyancyCNB_CompressedTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class AstroSystems163_IntegrandTerm(PhysicsTerm):
    """
    PhysicsTerm: AstroSystems163_IntegrandTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "AstroSystems163_IntegrandTerm"
        self.description = "PhysicsTerm: AstroSystems163_IntegrandTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class AstroSystems163_LENR_termTerm(PhysicsTerm):
    """
    PhysicsTerm: AstroSystems163_LENR_termTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "AstroSystems163_LENR_termTerm"
        self.description = "PhysicsTerm: AstroSystems163_LENR_termTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class AstroSystems163_FTerm(PhysicsTerm):
    """
    PhysicsTerm: AstroSystems163_FTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "AstroSystems163_FTerm"
        self.description = "PhysicsTerm: AstroSystems163_FTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class AstroSystems163_CompressedTerm(PhysicsTerm):
    """
    PhysicsTerm: AstroSystems163_CompressedTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "AstroSystems163_CompressedTerm"
        self.description = "PhysicsTerm: AstroSystems163_CompressedTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class NebulaTriadic_IntegrandTerm(PhysicsTerm):
    """
    PhysicsTerm: NebulaTriadic_IntegrandTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "NebulaTriadic_IntegrandTerm"
        self.description = "PhysicsTerm: NebulaTriadic_IntegrandTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class NebulaTriadic_LENR_termTerm(PhysicsTerm):
    """
    PhysicsTerm: NebulaTriadic_LENR_termTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "NebulaTriadic_LENR_termTerm"
        self.description = "PhysicsTerm: NebulaTriadic_LENR_termTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class NebulaTriadic_FTerm(PhysicsTerm):
    """
    PhysicsTerm: NebulaTriadic_FTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "NebulaTriadic_FTerm"
        self.description = "PhysicsTerm: NebulaTriadic_FTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class NebulaTriadic_CompressedTerm(PhysicsTerm):
    """
    PhysicsTerm: NebulaTriadic_CompressedTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "NebulaTriadic_CompressedTerm"
        self.description = "PhysicsTerm: NebulaTriadic_CompressedTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class BuoyancyModule_IntegrandTerm(PhysicsTerm):
    """
    PhysicsTerm: BuoyancyModule_IntegrandTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "BuoyancyModule_IntegrandTerm"
        self.description = "PhysicsTerm: BuoyancyModule_IntegrandTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class BuoyancyModule_LENR_termTerm(PhysicsTerm):
    """
    PhysicsTerm: BuoyancyModule_LENR_termTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "BuoyancyModule_LENR_termTerm"
        self.description = "PhysicsTerm: BuoyancyModule_LENR_termTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class BuoyancyModule_FTerm(PhysicsTerm):
    """
    PhysicsTerm: BuoyancyModule_FTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "BuoyancyModule_FTerm"
        self.description = "PhysicsTerm: BuoyancyModule_FTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class BuoyancyModule_CompressedTerm(PhysicsTerm):
    """
    PhysicsTerm: BuoyancyModule_CompressedTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "BuoyancyModule_CompressedTerm"
        self.description = "PhysicsTerm: BuoyancyModule_CompressedTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class UQFF8Astro_IntegrandTerm(PhysicsTerm):
    """
    PhysicsTerm: UQFF8Astro_IntegrandTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "UQFF8Astro_IntegrandTerm"
        self.description = "PhysicsTerm: UQFF8Astro_IntegrandTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class UQFF8Astro_LENR_termTerm(PhysicsTerm):
    """
    PhysicsTerm: UQFF8Astro_LENR_termTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "UQFF8Astro_LENR_termTerm"
        self.description = "PhysicsTerm: UQFF8Astro_LENR_termTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class UQFF8Astro_FTerm(PhysicsTerm):
    """
    PhysicsTerm: UQFF8Astro_FTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "UQFF8Astro_FTerm"
        self.description = "PhysicsTerm: UQFF8Astro_FTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class UQFF8Astro_CompressedTerm(PhysicsTerm):
    """
    PhysicsTerm: UQFF8Astro_CompressedTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "UQFF8Astro_CompressedTerm"
        self.description = "PhysicsTerm: UQFF8Astro_CompressedTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class UQFFCore_IntegrandTerm(PhysicsTerm):
    """
    PhysicsTerm: UQFFCore_IntegrandTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "UQFFCore_IntegrandTerm"
        self.description = "PhysicsTerm: UQFFCore_IntegrandTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class UQFFCore_LENR_termTerm(PhysicsTerm):
    """
    PhysicsTerm: UQFFCore_LENR_termTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "UQFFCore_LENR_termTerm"
        self.description = "PhysicsTerm: UQFFCore_LENR_termTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class UQFFCore_FTerm(PhysicsTerm):
    """
    PhysicsTerm: UQFFCore_FTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "UQFFCore_FTerm"
        self.description = "PhysicsTerm: UQFFCore_FTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class UQFFCore_CompressedTerm(PhysicsTerm):
    """
    PhysicsTerm: UQFFCore_CompressedTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "UQFFCore_CompressedTerm"
        self.description = "PhysicsTerm: UQFFCore_CompressedTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class Buoyancy168_IntegrandTerm(PhysicsTerm):
    """
    PhysicsTerm: Buoyancy168_IntegrandTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "Buoyancy168_IntegrandTerm"
        self.description = "PhysicsTerm: Buoyancy168_IntegrandTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class Buoyancy168_LENR_termTerm(PhysicsTerm):
    """
    PhysicsTerm: Buoyancy168_LENR_termTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "Buoyancy168_LENR_termTerm"
        self.description = "PhysicsTerm: Buoyancy168_LENR_termTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class Buoyancy168_FTerm(PhysicsTerm):
    """
    PhysicsTerm: Buoyancy168_FTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "Buoyancy168_FTerm"
        self.description = "PhysicsTerm: Buoyancy168_FTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class Buoyancy168_CompressedTerm(PhysicsTerm):
    """
    PhysicsTerm: Buoyancy168_CompressedTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "Buoyancy168_CompressedTerm"
        self.description = "PhysicsTerm: Buoyancy168_CompressedTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class CassiniBuoyancy_IntegrandTerm(PhysicsTerm):
    """
    PhysicsTerm: CassiniBuoyancy_IntegrandTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "CassiniBuoyancy_IntegrandTerm"
        self.description = "PhysicsTerm: CassiniBuoyancy_IntegrandTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class CassiniBuoyancy_LENR_termTerm(PhysicsTerm):
    """
    PhysicsTerm: CassiniBuoyancy_LENR_termTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "CassiniBuoyancy_LENR_termTerm"
        self.description = "PhysicsTerm: CassiniBuoyancy_LENR_termTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class CassiniBuoyancy_FTerm(PhysicsTerm):
    """
    PhysicsTerm: CassiniBuoyancy_FTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "CassiniBuoyancy_FTerm"
        self.description = "PhysicsTerm: CassiniBuoyancy_FTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class CassiniBuoyancy_CompressedTerm(PhysicsTerm):
    """
    PhysicsTerm: CassiniBuoyancy_CompressedTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "CassiniBuoyancy_CompressedTerm"
        self.description = "PhysicsTerm: CassiniBuoyancy_CompressedTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class MultiAstro_IntegrandTerm(PhysicsTerm):
    """
    PhysicsTerm: MultiAstro_IntegrandTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "MultiAstro_IntegrandTerm"
        self.description = "PhysicsTerm: MultiAstro_IntegrandTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class MultiAstro_LENR_termTerm(PhysicsTerm):
    """
    PhysicsTerm: MultiAstro_LENR_termTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "MultiAstro_LENR_termTerm"
        self.description = "PhysicsTerm: MultiAstro_LENR_termTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class MultiAstro_FTerm(PhysicsTerm):
    """
    PhysicsTerm: MultiAstro_FTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "MultiAstro_FTerm"
        self.description = "PhysicsTerm: MultiAstro_FTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class MultiAstro_CompressedTerm(PhysicsTerm):
    """
    PhysicsTerm: MultiAstro_CompressedTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "MultiAstro_CompressedTerm"
        self.description = "PhysicsTerm: MultiAstro_CompressedTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class EightAstro_IntegrandTerm(PhysicsTerm):
    """
    PhysicsTerm: EightAstro_IntegrandTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "EightAstro_IntegrandTerm"
        self.description = "PhysicsTerm: EightAstro_IntegrandTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class EightAstro_LENR_termTerm(PhysicsTerm):
    """
    PhysicsTerm: EightAstro_LENR_termTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "EightAstro_LENR_termTerm"
        self.description = "PhysicsTerm: EightAstro_LENR_termTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class EightAstro_FTerm(PhysicsTerm):
    """
    PhysicsTerm: EightAstro_FTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "EightAstro_FTerm"
        self.description = "PhysicsTerm: EightAstro_FTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class EightAstro_CompressedTerm(PhysicsTerm):
    """
    PhysicsTerm: EightAstro_CompressedTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "EightAstro_CompressedTerm"
        self.description = "PhysicsTerm: EightAstro_CompressedTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class NineteenAstro_IntegrandTerm(PhysicsTerm):
    """
    PhysicsTerm: NineteenAstro_IntegrandTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "NineteenAstro_IntegrandTerm"
        self.description = "PhysicsTerm: NineteenAstro_IntegrandTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class NineteenAstro_LENR_termTerm(PhysicsTerm):
    """
    PhysicsTerm: NineteenAstro_LENR_termTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "NineteenAstro_LENR_termTerm"
        self.description = "PhysicsTerm: NineteenAstro_LENR_termTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class NineteenAstro_FTerm(PhysicsTerm):
    """
    PhysicsTerm: NineteenAstro_FTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "NineteenAstro_FTerm"
        self.description = "PhysicsTerm: NineteenAstro_FTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class NineteenAstro_CompressedTerm(PhysicsTerm):
    """
    PhysicsTerm: NineteenAstro_CompressedTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "NineteenAstro_CompressedTerm"
        self.description = "PhysicsTerm: NineteenAstro_CompressedTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class WolframFieldUnity_IntegrandTerm(PhysicsTerm):
    """
    PhysicsTerm: WolframFieldUnity_IntegrandTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "WolframFieldUnity_IntegrandTerm"
        self.description = "PhysicsTerm: WolframFieldUnity_IntegrandTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class WolframFieldUnity_LENR_termTerm(PhysicsTerm):
    """
    PhysicsTerm: WolframFieldUnity_LENR_termTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "WolframFieldUnity_LENR_termTerm"
        self.description = "PhysicsTerm: WolframFieldUnity_LENR_termTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class WolframFieldUnity_FTerm(PhysicsTerm):
    """
    PhysicsTerm: WolframFieldUnity_FTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "WolframFieldUnity_FTerm"
        self.description = "PhysicsTerm: WolframFieldUnity_FTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class WolframFieldUnity_CompressedTerm(PhysicsTerm):
    """
    PhysicsTerm: WolframFieldUnity_CompressedTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "WolframFieldUnity_CompressedTerm"
        self.description = "PhysicsTerm: WolframFieldUnity_CompressedTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class J1610_DPM_resonanceTerm(PhysicsTerm):
    """
    PhysicsTerm: J1610_DPM_resonanceTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "J1610_DPM_resonanceTerm"
        self.description = "PhysicsTerm: J1610_DPM_resonanceTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class J1610_Q_waveTerm(PhysicsTerm):
    """
    PhysicsTerm: J1610_Q_waveTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "J1610_Q_waveTerm"
        self.description = "PhysicsTerm: J1610_Q_waveTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class J1610_Ub1Term(PhysicsTerm):
    """
    PhysicsTerm: J1610_Ub1Term
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "J1610_Ub1Term"
        self.description = "PhysicsTerm: J1610_Ub1Term"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class J1610_UiTerm(PhysicsTerm):
    """
    PhysicsTerm: J1610_UiTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "J1610_UiTerm"
        self.description = "PhysicsTerm: J1610_UiTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class J1610_X2Term(PhysicsTerm):
    """
    PhysicsTerm: J1610_X2Term
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "J1610_X2Term"
        self.description = "PhysicsTerm: J1610_X2Term"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class J1610_ActivationTerm(PhysicsTerm):
    """
    PhysicsTerm: J1610_ActivationTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "J1610_ActivationTerm"
        self.description = "PhysicsTerm: J1610_ActivationTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class J1610_DirectedEnergyTerm(PhysicsTerm):
    """
    PhysicsTerm: J1610_DirectedEnergyTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "J1610_DirectedEnergyTerm"
        self.description = "PhysicsTerm: J1610_DirectedEnergyTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class J1610_NeutronTerm(PhysicsTerm):
    """
    PhysicsTerm: J1610_NeutronTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "J1610_NeutronTerm"
        self.description = "PhysicsTerm: J1610_NeutronTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class J1610_RelativisticTerm(PhysicsTerm):
    """
    PhysicsTerm: J1610_RelativisticTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "J1610_RelativisticTerm"
        self.description = "PhysicsTerm: J1610_RelativisticTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class J1610_NeutrinoTerm(PhysicsTerm):
    """
    PhysicsTerm: J1610_NeutrinoTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "J1610_NeutrinoTerm"
        self.description = "PhysicsTerm: J1610_NeutrinoTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class M87Jet_DPM_resonanceTerm(PhysicsTerm):
    """
    PhysicsTerm: M87Jet_DPM_resonanceTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "M87Jet_DPM_resonanceTerm"
        self.description = "PhysicsTerm: M87Jet_DPM_resonanceTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class M87Jet_Q_waveTerm(PhysicsTerm):
    """
    PhysicsTerm: M87Jet_Q_waveTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "M87Jet_Q_waveTerm"
        self.description = "PhysicsTerm: M87Jet_Q_waveTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class M87Jet_Ub1Term(PhysicsTerm):
    """
    PhysicsTerm: M87Jet_Ub1Term
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "M87Jet_Ub1Term"
        self.description = "PhysicsTerm: M87Jet_Ub1Term"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class M87Jet_UiTerm(PhysicsTerm):
    """
    PhysicsTerm: M87Jet_UiTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "M87Jet_UiTerm"
        self.description = "PhysicsTerm: M87Jet_UiTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class M87Jet_X2Term(PhysicsTerm):
    """
    PhysicsTerm: M87Jet_X2Term
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "M87Jet_X2Term"
        self.description = "PhysicsTerm: M87Jet_X2Term"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class M87Jet_ActivationTerm(PhysicsTerm):
    """
    PhysicsTerm: M87Jet_ActivationTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "M87Jet_ActivationTerm"
        self.description = "PhysicsTerm: M87Jet_ActivationTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class M87Jet_DirectedEnergyTerm(PhysicsTerm):
    """
    PhysicsTerm: M87Jet_DirectedEnergyTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "M87Jet_DirectedEnergyTerm"
        self.description = "PhysicsTerm: M87Jet_DirectedEnergyTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class M87Jet_NeutronTerm(PhysicsTerm):
    """
    PhysicsTerm: M87Jet_NeutronTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "M87Jet_NeutronTerm"
        self.description = "PhysicsTerm: M87Jet_NeutronTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class M87Jet_RelativisticTerm(PhysicsTerm):
    """
    PhysicsTerm: M87Jet_RelativisticTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "M87Jet_RelativisticTerm"
        self.description = "PhysicsTerm: M87Jet_RelativisticTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class M87Jet_NeutrinoTerm(PhysicsTerm):
    """
    PhysicsTerm: M87Jet_NeutrinoTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "M87Jet_NeutrinoTerm"
        self.description = "PhysicsTerm: M87Jet_NeutrinoTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class SgrAStar_DPM_resonanceTerm(PhysicsTerm):
    """
    PhysicsTerm: SgrAStar_DPM_resonanceTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "SgrAStar_DPM_resonanceTerm"
        self.description = "PhysicsTerm: SgrAStar_DPM_resonanceTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class SgrAStar_Q_waveTerm(PhysicsTerm):
    """
    PhysicsTerm: SgrAStar_Q_waveTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "SgrAStar_Q_waveTerm"
        self.description = "PhysicsTerm: SgrAStar_Q_waveTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class SgrAStar_Ub1Term(PhysicsTerm):
    """
    PhysicsTerm: SgrAStar_Ub1Term
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "SgrAStar_Ub1Term"
        self.description = "PhysicsTerm: SgrAStar_Ub1Term"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class SgrAStar_UiTerm(PhysicsTerm):
    """
    PhysicsTerm: SgrAStar_UiTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "SgrAStar_UiTerm"
        self.description = "PhysicsTerm: SgrAStar_UiTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class SgrAStar_X2Term(PhysicsTerm):
    """
    PhysicsTerm: SgrAStar_X2Term
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "SgrAStar_X2Term"
        self.description = "PhysicsTerm: SgrAStar_X2Term"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class SgrAStar_ActivationTerm(PhysicsTerm):
    """
    PhysicsTerm: SgrAStar_ActivationTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "SgrAStar_ActivationTerm"
        self.description = "PhysicsTerm: SgrAStar_ActivationTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class SgrAStar_DirectedEnergyTerm(PhysicsTerm):
    """
    PhysicsTerm: SgrAStar_DirectedEnergyTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "SgrAStar_DirectedEnergyTerm"
        self.description = "PhysicsTerm: SgrAStar_DirectedEnergyTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class SgrAStar_NeutronTerm(PhysicsTerm):
    """
    PhysicsTerm: SgrAStar_NeutronTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "SgrAStar_NeutronTerm"
        self.description = "PhysicsTerm: SgrAStar_NeutronTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class SgrAStar_RelativisticTerm(PhysicsTerm):
    """
    PhysicsTerm: SgrAStar_RelativisticTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "SgrAStar_RelativisticTerm"
        self.description = "PhysicsTerm: SgrAStar_RelativisticTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class SgrAStar_NeutrinoTerm(PhysicsTerm):
    """
    PhysicsTerm: SgrAStar_NeutrinoTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "SgrAStar_NeutrinoTerm"
        self.description = "PhysicsTerm: SgrAStar_NeutrinoTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class NGC1365_DPM_resonanceTerm(PhysicsTerm):
    """
    PhysicsTerm: NGC1365_DPM_resonanceTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "NGC1365_DPM_resonanceTerm"
        self.description = "PhysicsTerm: NGC1365_DPM_resonanceTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class NGC1365_Q_waveTerm(PhysicsTerm):
    """
    PhysicsTerm: NGC1365_Q_waveTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "NGC1365_Q_waveTerm"
        self.description = "PhysicsTerm: NGC1365_Q_waveTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class NGC1365_Ub1Term(PhysicsTerm):
    """
    PhysicsTerm: NGC1365_Ub1Term
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "NGC1365_Ub1Term"
        self.description = "PhysicsTerm: NGC1365_Ub1Term"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class NGC1365_UiTerm(PhysicsTerm):
    """
    PhysicsTerm: NGC1365_UiTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "NGC1365_UiTerm"
        self.description = "PhysicsTerm: NGC1365_UiTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class NGC1365_X2Term(PhysicsTerm):
    """
    PhysicsTerm: NGC1365_X2Term
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "NGC1365_X2Term"
        self.description = "PhysicsTerm: NGC1365_X2Term"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class NGC1365_ActivationTerm(PhysicsTerm):
    """
    PhysicsTerm: NGC1365_ActivationTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "NGC1365_ActivationTerm"
        self.description = "PhysicsTerm: NGC1365_ActivationTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class NGC1365_DirectedEnergyTerm(PhysicsTerm):
    """
    PhysicsTerm: NGC1365_DirectedEnergyTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "NGC1365_DirectedEnergyTerm"
        self.description = "PhysicsTerm: NGC1365_DirectedEnergyTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class NGC1365_NeutronTerm(PhysicsTerm):
    """
    PhysicsTerm: NGC1365_NeutronTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "NGC1365_NeutronTerm"
        self.description = "PhysicsTerm: NGC1365_NeutronTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class NGC1365_RelativisticTerm(PhysicsTerm):
    """
    PhysicsTerm: NGC1365_RelativisticTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "NGC1365_RelativisticTerm"
        self.description = "PhysicsTerm: NGC1365_RelativisticTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class NGC1365_NeutrinoTerm(PhysicsTerm):
    """
    PhysicsTerm: NGC1365_NeutrinoTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "NGC1365_NeutrinoTerm"
        self.description = "PhysicsTerm: NGC1365_NeutrinoTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class VelaPulsar_DPM_resonanceTerm(PhysicsTerm):
    """
    PhysicsTerm: VelaPulsar_DPM_resonanceTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "VelaPulsar_DPM_resonanceTerm"
        self.description = "PhysicsTerm: VelaPulsar_DPM_resonanceTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class VelaPulsar_Q_waveTerm(PhysicsTerm):
    """
    PhysicsTerm: VelaPulsar_Q_waveTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "VelaPulsar_Q_waveTerm"
        self.description = "PhysicsTerm: VelaPulsar_Q_waveTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class VelaPulsar_Ub1Term(PhysicsTerm):
    """
    PhysicsTerm: VelaPulsar_Ub1Term
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "VelaPulsar_Ub1Term"
        self.description = "PhysicsTerm: VelaPulsar_Ub1Term"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class VelaPulsar_UiTerm(PhysicsTerm):
    """
    PhysicsTerm: VelaPulsar_UiTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "VelaPulsar_UiTerm"
        self.description = "PhysicsTerm: VelaPulsar_UiTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class VelaPulsar_X2Term(PhysicsTerm):
    """
    PhysicsTerm: VelaPulsar_X2Term
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "VelaPulsar_X2Term"
        self.description = "PhysicsTerm: VelaPulsar_X2Term"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class VelaPulsar_ActivationTerm(PhysicsTerm):
    """
    PhysicsTerm: VelaPulsar_ActivationTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "VelaPulsar_ActivationTerm"
        self.description = "PhysicsTerm: VelaPulsar_ActivationTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class VelaPulsar_DirectedEnergyTerm(PhysicsTerm):
    """
    PhysicsTerm: VelaPulsar_DirectedEnergyTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "VelaPulsar_DirectedEnergyTerm"
        self.description = "PhysicsTerm: VelaPulsar_DirectedEnergyTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class VelaPulsar_NeutronTerm(PhysicsTerm):
    """
    PhysicsTerm: VelaPulsar_NeutronTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "VelaPulsar_NeutronTerm"
        self.description = "PhysicsTerm: VelaPulsar_NeutronTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class VelaPulsar_RelativisticTerm(PhysicsTerm):
    """
    PhysicsTerm: VelaPulsar_RelativisticTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "VelaPulsar_RelativisticTerm"
        self.description = "PhysicsTerm: VelaPulsar_RelativisticTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class VelaPulsar_NeutrinoTerm(PhysicsTerm):
    """
    PhysicsTerm: VelaPulsar_NeutrinoTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "VelaPulsar_NeutrinoTerm"
        self.description = "PhysicsTerm: VelaPulsar_NeutrinoTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class NGC2207_DPM_resonanceTerm(PhysicsTerm):
    """
    PhysicsTerm: NGC2207_DPM_resonanceTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "NGC2207_DPM_resonanceTerm"
        self.description = "PhysicsTerm: NGC2207_DPM_resonanceTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class NGC2207_Q_waveTerm(PhysicsTerm):
    """
    PhysicsTerm: NGC2207_Q_waveTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "NGC2207_Q_waveTerm"
        self.description = "PhysicsTerm: NGC2207_Q_waveTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class NGC2207_Ub1Term(PhysicsTerm):
    """
    PhysicsTerm: NGC2207_Ub1Term
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "NGC2207_Ub1Term"
        self.description = "PhysicsTerm: NGC2207_Ub1Term"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class NGC2207_UiTerm(PhysicsTerm):
    """
    PhysicsTerm: NGC2207_UiTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "NGC2207_UiTerm"
        self.description = "PhysicsTerm: NGC2207_UiTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class NGC2207_X2Term(PhysicsTerm):
    """
    PhysicsTerm: NGC2207_X2Term
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "NGC2207_X2Term"
        self.description = "PhysicsTerm: NGC2207_X2Term"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class NGC2207_ActivationTerm(PhysicsTerm):
    """
    PhysicsTerm: NGC2207_ActivationTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "NGC2207_ActivationTerm"
        self.description = "PhysicsTerm: NGC2207_ActivationTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class NGC2207_DirectedEnergyTerm(PhysicsTerm):
    """
    PhysicsTerm: NGC2207_DirectedEnergyTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "NGC2207_DirectedEnergyTerm"
        self.description = "PhysicsTerm: NGC2207_DirectedEnergyTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class NGC2207_NeutronTerm(PhysicsTerm):
    """
    PhysicsTerm: NGC2207_NeutronTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "NGC2207_NeutronTerm"
        self.description = "PhysicsTerm: NGC2207_NeutronTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class NGC2207_RelativisticTerm(PhysicsTerm):
    """
    PhysicsTerm: NGC2207_RelativisticTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "NGC2207_RelativisticTerm"
        self.description = "PhysicsTerm: NGC2207_RelativisticTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class NGC2207_NeutrinoTerm(PhysicsTerm):
    """
    PhysicsTerm: NGC2207_NeutrinoTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "NGC2207_NeutrinoTerm"
        self.description = "PhysicsTerm: NGC2207_NeutrinoTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class PerseusCluster_DPM_resonanceTerm(PhysicsTerm):
    """
    PhysicsTerm: PerseusCluster_DPM_resonanceTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "PerseusCluster_DPM_resonanceTerm"
        self.description = "PhysicsTerm: PerseusCluster_DPM_resonanceTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class PerseusCluster_Q_waveTerm(PhysicsTerm):
    """
    PhysicsTerm: PerseusCluster_Q_waveTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "PerseusCluster_Q_waveTerm"
        self.description = "PhysicsTerm: PerseusCluster_Q_waveTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class PerseusCluster_Ub1Term(PhysicsTerm):
    """
    PhysicsTerm: PerseusCluster_Ub1Term
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "PerseusCluster_Ub1Term"
        self.description = "PhysicsTerm: PerseusCluster_Ub1Term"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class PerseusCluster_UiTerm(PhysicsTerm):
    """
    PhysicsTerm: PerseusCluster_UiTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "PerseusCluster_UiTerm"
        self.description = "PhysicsTerm: PerseusCluster_UiTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class PerseusCluster_X2Term(PhysicsTerm):
    """
    PhysicsTerm: PerseusCluster_X2Term
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "PerseusCluster_X2Term"
        self.description = "PhysicsTerm: PerseusCluster_X2Term"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class PerseusCluster_ActivationTerm(PhysicsTerm):
    """
    PhysicsTerm: PerseusCluster_ActivationTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "PerseusCluster_ActivationTerm"
        self.description = "PhysicsTerm: PerseusCluster_ActivationTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class PerseusCluster_DirectedEnergyTerm(PhysicsTerm):
    """
    PhysicsTerm: PerseusCluster_DirectedEnergyTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "PerseusCluster_DirectedEnergyTerm"
        self.description = "PhysicsTerm: PerseusCluster_DirectedEnergyTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class PerseusCluster_NeutronTerm(PhysicsTerm):
    """
    PhysicsTerm: PerseusCluster_NeutronTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "PerseusCluster_NeutronTerm"
        self.description = "PhysicsTerm: PerseusCluster_NeutronTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class PerseusCluster_RelativisticTerm(PhysicsTerm):
    """
    PhysicsTerm: PerseusCluster_RelativisticTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "PerseusCluster_RelativisticTerm"
        self.description = "PhysicsTerm: PerseusCluster_RelativisticTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class PerseusCluster_NeutrinoTerm(PhysicsTerm):
    """
    PhysicsTerm: PerseusCluster_NeutrinoTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "PerseusCluster_NeutrinoTerm"
        self.description = "PhysicsTerm: PerseusCluster_NeutrinoTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class OrionM42_DPM_resonanceTerm(PhysicsTerm):
    """
    PhysicsTerm: OrionM42_DPM_resonanceTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "OrionM42_DPM_resonanceTerm"
        self.description = "PhysicsTerm: OrionM42_DPM_resonanceTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class OrionM42_Q_waveTerm(PhysicsTerm):
    """
    PhysicsTerm: OrionM42_Q_waveTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "OrionM42_Q_waveTerm"
        self.description = "PhysicsTerm: OrionM42_Q_waveTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class OrionM42_Ub1Term(PhysicsTerm):
    """
    PhysicsTerm: OrionM42_Ub1Term
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "OrionM42_Ub1Term"
        self.description = "PhysicsTerm: OrionM42_Ub1Term"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class OrionM42_UiTerm(PhysicsTerm):
    """
    PhysicsTerm: OrionM42_UiTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "OrionM42_UiTerm"
        self.description = "PhysicsTerm: OrionM42_UiTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class OrionM42_X2Term(PhysicsTerm):
    """
    PhysicsTerm: OrionM42_X2Term
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "OrionM42_X2Term"
        self.description = "PhysicsTerm: OrionM42_X2Term"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class OrionM42_ActivationTerm(PhysicsTerm):
    """
    PhysicsTerm: OrionM42_ActivationTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "OrionM42_ActivationTerm"
        self.description = "PhysicsTerm: OrionM42_ActivationTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class OrionM42_DirectedEnergyTerm(PhysicsTerm):
    """
    PhysicsTerm: OrionM42_DirectedEnergyTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "OrionM42_DirectedEnergyTerm"
        self.description = "PhysicsTerm: OrionM42_DirectedEnergyTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class OrionM42_NeutronTerm(PhysicsTerm):
    """
    PhysicsTerm: OrionM42_NeutronTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "OrionM42_NeutronTerm"
        self.description = "PhysicsTerm: OrionM42_NeutronTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class OrionM42_RelativisticTerm(PhysicsTerm):
    """
    PhysicsTerm: OrionM42_RelativisticTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "OrionM42_RelativisticTerm"
        self.description = "PhysicsTerm: OrionM42_RelativisticTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class OrionM42_NeutrinoTerm(PhysicsTerm):
    """
    PhysicsTerm: OrionM42_NeutrinoTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "OrionM42_NeutrinoTerm"
        self.description = "PhysicsTerm: OrionM42_NeutrinoTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class LagoonM8_DPM_resonanceTerm(PhysicsTerm):
    """
    PhysicsTerm: LagoonM8_DPM_resonanceTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "LagoonM8_DPM_resonanceTerm"
        self.description = "PhysicsTerm: LagoonM8_DPM_resonanceTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class LagoonM8_Q_waveTerm(PhysicsTerm):
    """
    PhysicsTerm: LagoonM8_Q_waveTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "LagoonM8_Q_waveTerm"
        self.description = "PhysicsTerm: LagoonM8_Q_waveTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class LagoonM8_Ub1Term(PhysicsTerm):
    """
    PhysicsTerm: LagoonM8_Ub1Term
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "LagoonM8_Ub1Term"
        self.description = "PhysicsTerm: LagoonM8_Ub1Term"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class LagoonM8_UiTerm(PhysicsTerm):
    """
    PhysicsTerm: LagoonM8_UiTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "LagoonM8_UiTerm"
        self.description = "PhysicsTerm: LagoonM8_UiTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class LagoonM8_X2Term(PhysicsTerm):
    """
    PhysicsTerm: LagoonM8_X2Term
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "LagoonM8_X2Term"
        self.description = "PhysicsTerm: LagoonM8_X2Term"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class LagoonM8_ActivationTerm(PhysicsTerm):
    """
    PhysicsTerm: LagoonM8_ActivationTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "LagoonM8_ActivationTerm"
        self.description = "PhysicsTerm: LagoonM8_ActivationTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class LagoonM8_DirectedEnergyTerm(PhysicsTerm):
    """
    PhysicsTerm: LagoonM8_DirectedEnergyTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "LagoonM8_DirectedEnergyTerm"
        self.description = "PhysicsTerm: LagoonM8_DirectedEnergyTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class LagoonM8_NeutronTerm(PhysicsTerm):
    """
    PhysicsTerm: LagoonM8_NeutronTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "LagoonM8_NeutronTerm"
        self.description = "PhysicsTerm: LagoonM8_NeutronTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class LagoonM8_RelativisticTerm(PhysicsTerm):
    """
    PhysicsTerm: LagoonM8_RelativisticTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "LagoonM8_RelativisticTerm"
        self.description = "PhysicsTerm: LagoonM8_RelativisticTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class LagoonM8_NeutrinoTerm(PhysicsTerm):
    """
    PhysicsTerm: LagoonM8_NeutrinoTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "LagoonM8_NeutrinoTerm"
        self.description = "PhysicsTerm: LagoonM8_NeutrinoTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class ESO137_G_timeTerm(PhysicsTerm):
    """
    PhysicsTerm: ESO137_G_timeTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "ESO137_G_timeTerm"
        self.description = "PhysicsTerm: ESO137_G_timeTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class NGC1365A_G_timeTerm(PhysicsTerm):
    """
    PhysicsTerm: NGC1365A_G_timeTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "NGC1365A_G_timeTerm"
        self.description = "PhysicsTerm: NGC1365A_G_timeTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class NGC5728_G_timeTerm(PhysicsTerm):
    """
    PhysicsTerm: NGC5728_G_timeTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "NGC5728_G_timeTerm"
        self.description = "PhysicsTerm: NGC5728_G_timeTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class Vela_G_timeTerm(PhysicsTerm):
    """
    PhysicsTerm: Vela_G_timeTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "Vela_G_timeTerm"
        self.description = "PhysicsTerm: Vela_G_timeTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class M82_G_timeTerm(PhysicsTerm):
    """
    PhysicsTerm: M82_G_timeTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "M82_G_timeTerm"
        self.description = "PhysicsTerm: M82_G_timeTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class Carina_G_timeTerm(PhysicsTerm):
    """
    PhysicsTerm: Carina_G_timeTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "Carina_G_timeTerm"
        self.description = "PhysicsTerm: Carina_G_timeTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class TadpoleGalaxy_G_timeTerm(PhysicsTerm):
    """
    PhysicsTerm: TadpoleGalaxy_G_timeTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "TadpoleGalaxy_G_timeTerm"
        self.description = "PhysicsTerm: TadpoleGalaxy_G_timeTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class MiceGalaxies_G_timeTerm(PhysicsTerm):
    """
    PhysicsTerm: MiceGalaxies_G_timeTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "MiceGalaxies_G_timeTerm"
        self.description = "PhysicsTerm: MiceGalaxies_G_timeTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class NGC2264_G_timeTerm(PhysicsTerm):
    """
    PhysicsTerm: NGC2264_G_timeTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "NGC2264_G_timeTerm"
        self.description = "PhysicsTerm: NGC2264_G_timeTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class M42_G_timeTerm(PhysicsTerm):
    """
    PhysicsTerm: M42_G_timeTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "M42_G_timeTerm"
        self.description = "PhysicsTerm: M42_G_timeTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class CrabNebula_G_timeTerm(PhysicsTerm):
    """
    PhysicsTerm: CrabNebula_G_timeTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "CrabNebula_G_timeTerm"
        self.description = "PhysicsTerm: CrabNebula_G_timeTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class BlackWidowPulsar_G_timeTerm(PhysicsTerm):
    """
    PhysicsTerm: BlackWidowPulsar_G_timeTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "BlackWidowPulsar_G_timeTerm"
        self.description = "PhysicsTerm: BlackWidowPulsar_G_timeTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class Redback_G_timeTerm(PhysicsTerm):
    """
    PhysicsTerm: Redback_G_timeTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "Redback_G_timeTerm"
        self.description = "PhysicsTerm: Redback_G_timeTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class Fermi_G_timeTerm(PhysicsTerm):
    """
    PhysicsTerm: Fermi_G_timeTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "Fermi_G_timeTerm"
        self.description = "PhysicsTerm: Fermi_G_timeTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class Holmberg15A_G_timeTerm(PhysicsTerm):
    """
    PhysicsTerm: Holmberg15A_G_timeTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "Holmberg15A_G_timeTerm"
        self.description = "PhysicsTerm: Holmberg15A_G_timeTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class NineteenSystems_G_timeTerm(PhysicsTerm):
    """
    PhysicsTerm: NineteenSystems_G_timeTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "NineteenSystems_G_timeTerm"
        self.description = "PhysicsTerm: NineteenSystems_G_timeTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class WolframHypergraph_G_timeTerm(PhysicsTerm):
    """
    PhysicsTerm: WolframHypergraph_G_timeTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "WolframHypergraph_G_timeTerm"
        self.description = "PhysicsTerm: WolframHypergraph_G_timeTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class NGC253_G_timeTerm(PhysicsTerm):
    """
    PhysicsTerm: NGC253_G_timeTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "NGC253_G_timeTerm"
        self.description = "PhysicsTerm: NGC253_G_timeTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class M87Jet_G_timeTerm(PhysicsTerm):
    """
    PhysicsTerm: M87Jet_G_timeTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "M87Jet_G_timeTerm"
        self.description = "PhysicsTerm: M87Jet_G_timeTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class Centaurus_G_timeTerm(PhysicsTerm):
    """
    PhysicsTerm: Centaurus_G_timeTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "Centaurus_G_timeTerm"
        self.description = "PhysicsTerm: Centaurus_G_timeTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class Antlia_G_timeTerm(PhysicsTerm):
    """
    PhysicsTerm: Antlia_G_timeTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "Antlia_G_timeTerm"
        self.description = "PhysicsTerm: Antlia_G_timeTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class Fornax_G_timeTerm(PhysicsTerm):
    """
    PhysicsTerm: Fornax_G_timeTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "Fornax_G_timeTerm"
        self.description = "PhysicsTerm: Fornax_G_timeTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class Sculptor_G_timeTerm(PhysicsTerm):
    """
    PhysicsTerm: Sculptor_G_timeTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "Sculptor_G_timeTerm"
        self.description = "PhysicsTerm: Sculptor_G_timeTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class LeoI_G_timeTerm(PhysicsTerm):
    """
    PhysicsTerm: LeoI_G_timeTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "LeoI_G_timeTerm"
        self.description = "PhysicsTerm: LeoI_G_timeTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class LeoII_G_timeTerm(PhysicsTerm):
    """
    PhysicsTerm: LeoII_G_timeTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "LeoII_G_timeTerm"
        self.description = "PhysicsTerm: LeoII_G_timeTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class UrsaMinor_G_timeTerm(PhysicsTerm):
    """
    PhysicsTerm: UrsaMinor_G_timeTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "UrsaMinor_G_timeTerm"
        self.description = "PhysicsTerm: UrsaMinor_G_timeTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class Draco_G_timeTerm(PhysicsTerm):
    """
    PhysicsTerm: Draco_G_timeTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "Draco_G_timeTerm"
        self.description = "PhysicsTerm: Draco_G_timeTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class Sextans_G_timeTerm(PhysicsTerm):
    """
    PhysicsTerm: Sextans_G_timeTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "Sextans_G_timeTerm"
        self.description = "PhysicsTerm: Sextans_G_timeTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class Carina2_G_timeTerm(PhysicsTerm):
    """
    PhysicsTerm: Carina2_G_timeTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "Carina2_G_timeTerm"
        self.description = "PhysicsTerm: Carina2_G_timeTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class Reticulum_G_timeTerm(PhysicsTerm):
    """
    PhysicsTerm: Reticulum_G_timeTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "Reticulum_G_timeTerm"
        self.description = "PhysicsTerm: Reticulum_G_timeTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class Tucana_G_timeTerm(PhysicsTerm):
    """
    PhysicsTerm: Tucana_G_timeTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "Tucana_G_timeTerm"
        self.description = "PhysicsTerm: Tucana_G_timeTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class Horologium_G_timeTerm(PhysicsTerm):
    """
    PhysicsTerm: Horologium_G_timeTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "Horologium_G_timeTerm"
        self.description = "PhysicsTerm: Horologium_G_timeTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class Grus_G_timeTerm(PhysicsTerm):
    """
    PhysicsTerm: Grus_G_timeTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "Grus_G_timeTerm"
        self.description = "PhysicsTerm: Grus_G_timeTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class Phoenix_G_timeTerm(PhysicsTerm):
    """
    PhysicsTerm: Phoenix_G_timeTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "Phoenix_G_timeTerm"
        self.description = "PhysicsTerm: Phoenix_G_timeTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class Eridanus_G_timeTerm(PhysicsTerm):
    """
    PhysicsTerm: Eridanus_G_timeTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "Eridanus_G_timeTerm"
        self.description = "PhysicsTerm: Eridanus_G_timeTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class Aquarius_G_timeTerm(PhysicsTerm):
    """
    PhysicsTerm: Aquarius_G_timeTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "Aquarius_G_timeTerm"
        self.description = "PhysicsTerm: Aquarius_G_timeTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class Pisces_G_timeTerm(PhysicsTerm):
    """
    PhysicsTerm: Pisces_G_timeTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "Pisces_G_timeTerm"
        self.description = "PhysicsTerm: Pisces_G_timeTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class Pegasus_G_timeTerm(PhysicsTerm):
    """
    PhysicsTerm: Pegasus_G_timeTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "Pegasus_G_timeTerm"
        self.description = "PhysicsTerm: Pegasus_G_timeTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class Andromeda_G_timeTerm(PhysicsTerm):
    """
    PhysicsTerm: Andromeda_G_timeTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "Andromeda_G_timeTerm"
        self.description = "PhysicsTerm: Andromeda_G_timeTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class Triangulum_G_timeTerm(PhysicsTerm):
    """
    PhysicsTerm: Triangulum_G_timeTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "Triangulum_G_timeTerm"
        self.description = "PhysicsTerm: Triangulum_G_timeTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class LMC_G_timeTerm(PhysicsTerm):
    """
    PhysicsTerm: LMC_G_timeTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "LMC_G_timeTerm"
        self.description = "PhysicsTerm: LMC_G_timeTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class J1610_G_timeTerm(PhysicsTerm):
    """
    PhysicsTerm: J1610_G_timeTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "J1610_G_timeTerm"
        self.description = "PhysicsTerm: J1610_G_timeTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class JupiterAurorae_G_timeTerm(PhysicsTerm):
    """
    PhysicsTerm: JupiterAurorae_G_timeTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "JupiterAurorae_G_timeTerm"
        self.description = "PhysicsTerm: JupiterAurorae_G_timeTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class LagoonM8HII_G_timeTerm(PhysicsTerm):
    """
    PhysicsTerm: LagoonM8HII_G_timeTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "LagoonM8HII_G_timeTerm"
        self.description = "PhysicsTerm: LagoonM8HII_G_timeTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class M87JetEHT_G_timeTerm(PhysicsTerm):
    """
    PhysicsTerm: M87JetEHT_G_timeTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "M87JetEHT_G_timeTerm"
        self.description = "PhysicsTerm: M87JetEHT_G_timeTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class NGC1365Seyfert_G_timeTerm(PhysicsTerm):
    """
    PhysicsTerm: NGC1365Seyfert_G_timeTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "NGC1365Seyfert_G_timeTerm"
        self.description = "PhysicsTerm: NGC1365Seyfert_G_timeTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class NGC2207IC2163_G_timeTerm(PhysicsTerm):
    """
    PhysicsTerm: NGC2207IC2163_G_timeTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "NGC2207IC2163_G_timeTerm"
        self.description = "PhysicsTerm: NGC2207IC2163_G_timeTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class RAquarii_G_timeTerm(PhysicsTerm):
    """
    PhysicsTerm: RAquarii_G_timeTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "RAquarii_G_timeTerm"
        self.description = "PhysicsTerm: RAquarii_G_timeTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class SgrAStarGC_G_timeTerm(PhysicsTerm):
    """
    PhysicsTerm: SgrAStarGC_G_timeTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "SgrAStarGC_G_timeTerm"
        self.description = "PhysicsTerm: SgrAStarGC_G_timeTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class SPTCLJ2215_G_timeTerm(PhysicsTerm):
    """
    PhysicsTerm: SPTCLJ2215_G_timeTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "SPTCLJ2215_G_timeTerm"
        self.description = "PhysicsTerm: SPTCLJ2215_G_timeTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class StephanQuintet_G_timeTerm(PhysicsTerm):
    """
    PhysicsTerm: StephanQuintet_G_timeTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "StephanQuintet_G_timeTerm"
        self.description = "PhysicsTerm: StephanQuintet_G_timeTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class VelaPulsarSNR_G_timeTerm(PhysicsTerm):
    """
    PhysicsTerm: VelaPulsarSNR_G_timeTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "VelaPulsarSNR_G_timeTerm"
        self.description = "PhysicsTerm: VelaPulsarSNR_G_timeTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class NGC4993GW170817_G_timeTerm(PhysicsTerm):
    """
    PhysicsTerm: NGC4993GW170817_G_timeTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "NGC4993GW170817_G_timeTerm"
        self.description = "PhysicsTerm: NGC4993GW170817_G_timeTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class NGC6240DualAGN_G_timeTerm(PhysicsTerm):
    """
    PhysicsTerm: NGC6240DualAGN_G_timeTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "NGC6240DualAGN_G_timeTerm"
        self.description = "PhysicsTerm: NGC6240DualAGN_G_timeTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class OmegaCentauri_G_timeTerm(PhysicsTerm):
    """
    PhysicsTerm: OmegaCentauri_G_timeTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "OmegaCentauri_G_timeTerm"
        self.description = "PhysicsTerm: OmegaCentauri_G_timeTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class OrionM42Trapezium_G_timeTerm(PhysicsTerm):
    """
    PhysicsTerm: OrionM42Trapezium_G_timeTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "OrionM42Trapezium_G_timeTerm"
        self.description = "PhysicsTerm: OrionM42Trapezium_G_timeTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class PerseusAbell426_G_timeTerm(PhysicsTerm):
    """
    PhysicsTerm: PerseusAbell426_G_timeTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "PerseusAbell426_G_timeTerm"
        self.description = "PhysicsTerm: PerseusAbell426_G_timeTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class PolarRingNGC4650A_G_timeTerm(PhysicsTerm):
    """
    PhysicsTerm: PolarRingNGC4650A_G_timeTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "PolarRingNGC4650A_G_timeTerm"
        self.description = "PhysicsTerm: PolarRingNGC4650A_G_timeTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class QuasarJ0313_G_timeTerm(PhysicsTerm):
    """
    PhysicsTerm: QuasarJ0313_G_timeTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "QuasarJ0313_G_timeTerm"
        self.description = "PhysicsTerm: QuasarJ0313_G_timeTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class RXJ1713TeVSNR_G_timeTerm(PhysicsTerm):
    """
    PhysicsTerm: RXJ1713TeVSNR_G_timeTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "RXJ1713TeVSNR_G_timeTerm"
        self.description = "PhysicsTerm: RXJ1713TeVSNR_G_timeTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class SMC_G_timeTerm(PhysicsTerm):
    """
    PhysicsTerm: SMC_G_timeTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "SMC_G_timeTerm"
        self.description = "PhysicsTerm: SMC_G_timeTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class BuoyancyAstro5_G_timeTerm(PhysicsTerm):
    """
    PhysicsTerm: BuoyancyAstro5_G_timeTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "BuoyancyAstro5_G_timeTerm"
        self.description = "PhysicsTerm: BuoyancyAstro5_G_timeTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class BuoyancyCNB_G_timeTerm(PhysicsTerm):
    """
    PhysicsTerm: BuoyancyCNB_G_timeTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "BuoyancyCNB_G_timeTerm"
        self.description = "PhysicsTerm: BuoyancyCNB_G_timeTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class AstroSystems163_G_timeTerm(PhysicsTerm):
    """
    PhysicsTerm: AstroSystems163_G_timeTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "AstroSystems163_G_timeTerm"
        self.description = "PhysicsTerm: AstroSystems163_G_timeTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class NebulaTriadic_G_timeTerm(PhysicsTerm):
    """
    PhysicsTerm: NebulaTriadic_G_timeTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "NebulaTriadic_G_timeTerm"
        self.description = "PhysicsTerm: NebulaTriadic_G_timeTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class BuoyancyModule_G_timeTerm(PhysicsTerm):
    """
    PhysicsTerm: BuoyancyModule_G_timeTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "BuoyancyModule_G_timeTerm"
        self.description = "PhysicsTerm: BuoyancyModule_G_timeTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class UQFF8Astro_G_timeTerm(PhysicsTerm):
    """
    PhysicsTerm: UQFF8Astro_G_timeTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "UQFF8Astro_G_timeTerm"
        self.description = "PhysicsTerm: UQFF8Astro_G_timeTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class UQFFCore_G_timeTerm(PhysicsTerm):
    """
    PhysicsTerm: UQFFCore_G_timeTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "UQFFCore_G_timeTerm"
        self.description = "PhysicsTerm: UQFFCore_G_timeTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class Buoyancy168_G_timeTerm(PhysicsTerm):
    """
    PhysicsTerm: Buoyancy168_G_timeTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "Buoyancy168_G_timeTerm"
        self.description = "PhysicsTerm: Buoyancy168_G_timeTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class CassiniBuoyancy_G_timeTerm(PhysicsTerm):
    """
    PhysicsTerm: CassiniBuoyancy_G_timeTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "CassiniBuoyancy_G_timeTerm"
        self.description = "PhysicsTerm: CassiniBuoyancy_G_timeTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class MultiAstro_G_timeTerm(PhysicsTerm):
    """
    PhysicsTerm: MultiAstro_G_timeTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "MultiAstro_G_timeTerm"
        self.description = "PhysicsTerm: MultiAstro_G_timeTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class EightAstro_G_timeTerm(PhysicsTerm):
    """
    PhysicsTerm: EightAstro_G_timeTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "EightAstro_G_timeTerm"
        self.description = "PhysicsTerm: EightAstro_G_timeTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class NineteenAstro26D_G_timeTerm(PhysicsTerm):
    """
    PhysicsTerm: NineteenAstro26D_G_timeTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "NineteenAstro26D_G_timeTerm"
        self.description = "PhysicsTerm: NineteenAstro26D_G_timeTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class WolframFieldUnity_G_timeTerm(PhysicsTerm):
    """
    PhysicsTerm: WolframFieldUnity_G_timeTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "WolframFieldUnity_G_timeTerm"
        self.description = "PhysicsTerm: WolframFieldUnity_G_timeTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class ESO137_HeavisideFactor(PhysicsTerm):
    """
    PhysicsTerm: ESO137_HeavisideFactor
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "ESO137_HeavisideFactor"
        self.description = "PhysicsTerm: ESO137_HeavisideFactor"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class ESO137_UmBase(PhysicsTerm):
    """
    PhysicsTerm: ESO137_UmBase
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "ESO137_UmBase"
        self.description = "PhysicsTerm: ESO137_UmBase"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class ESO137_UmContribution(PhysicsTerm):
    """
    PhysicsTerm: ESO137_UmContribution
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "ESO137_UmContribution"
        self.description = "PhysicsTerm: ESO137_UmContribution"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class NGC1365A_H_SCm(PhysicsTerm):
    """
    PhysicsTerm: NGC1365A_H_SCm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "NGC1365A_H_SCm"
        self.description = "PhysicsTerm: NGC1365A_H_SCm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class NGC1365A_U_g2(PhysicsTerm):
    """
    PhysicsTerm: NGC1365A_U_g2
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "NGC1365A_U_g2"
        self.description = "PhysicsTerm: NGC1365A_U_g2"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class NGC1365A_SwirlFactor(PhysicsTerm):
    """
    PhysicsTerm: NGC1365A_SwirlFactor
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "NGC1365A_SwirlFactor"
        self.description = "PhysicsTerm: NGC1365A_SwirlFactor"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class NGC5728_U_gi(PhysicsTerm):
    """
    PhysicsTerm: NGC5728_U_gi
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "NGC5728_U_gi"
        self.description = "PhysicsTerm: NGC5728_U_gi"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class NGC5728_K_i(PhysicsTerm):
    """
    PhysicsTerm: NGC5728_K_i
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "NGC5728_K_i"
        self.description = "PhysicsTerm: NGC5728_K_i"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class NGC5728_SumKUgi(PhysicsTerm):
    """
    PhysicsTerm: NGC5728_SumKUgi
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "NGC5728_SumKUgi"
        self.description = "PhysicsTerm: NGC5728_SumKUgi"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class MagneticMoment_Mu_j(PhysicsTerm):
    """
    PhysicsTerm: MagneticMoment_Mu_j
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "MagneticMoment_Mu_j"
        self.description = "PhysicsTerm: MagneticMoment_Mu_j"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class MagneticMoment_B_j(PhysicsTerm):
    """
    PhysicsTerm: MagneticMoment_B_j
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "MagneticMoment_B_j"
        self.description = "PhysicsTerm: MagneticMoment_B_j"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class MagneticMoment_UmContrib(PhysicsTerm):
    """
    PhysicsTerm: MagneticMoment_UmContrib
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "MagneticMoment_UmContrib"
        self.description = "PhysicsTerm: MagneticMoment_UmContrib"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class BlackHole_M_bh(PhysicsTerm):
    """
    PhysicsTerm: BlackHole_M_bh
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "BlackHole_M_bh"
        self.description = "PhysicsTerm: BlackHole_M_bh"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class BlackHole_M_bhInMsun(PhysicsTerm):
    """
    PhysicsTerm: BlackHole_M_bhInMsun
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "BlackHole_M_bhInMsun"
        self.description = "PhysicsTerm: BlackHole_M_bhInMsun"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class BlackHole_U_b1(PhysicsTerm):
    """
    PhysicsTerm: BlackHole_U_b1
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "BlackHole_U_b1"
        self.description = "PhysicsTerm: BlackHole_U_b1"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class NegativeTime_T_n(PhysicsTerm):
    """
    PhysicsTerm: NegativeTime_T_n
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "NegativeTime_T_n"
        self.description = "PhysicsTerm: NegativeTime_T_n"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class NegativeTime_CosPiTn(PhysicsTerm):
    """
    PhysicsTerm: NegativeTime_CosPiTn
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "NegativeTime_CosPiTn"
        self.description = "PhysicsTerm: NegativeTime_CosPiTn"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class NegativeTime_OneMinusExp(PhysicsTerm):
    """
    PhysicsTerm: NegativeTime_OneMinusExp
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "NegativeTime_OneMinusExp"
        self.description = "PhysicsTerm: NegativeTime_OneMinusExp"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class PiConstant_Pi(PhysicsTerm):
    """
    PhysicsTerm: PiConstant_Pi
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "PiConstant_Pi"
        self.description = "PhysicsTerm: PiConstant_Pi"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class PiConstant_CosPiTn(PhysicsTerm):
    """
    PhysicsTerm: PiConstant_CosPiTn
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "PiConstant_CosPiTn"
        self.description = "PhysicsTerm: PiConstant_CosPiTn"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class PiConstant_SinOmegaCT(PhysicsTerm):
    """
    PhysicsTerm: PiConstant_SinOmegaCT
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "PiConstant_SinOmegaCT"
        self.description = "PhysicsTerm: PiConstant_SinOmegaCT"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class Decay_Gamma_s(PhysicsTerm):
    """
    PhysicsTerm: Decay_Gamma_s
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "Decay_Gamma_s"
        self.description = "PhysicsTerm: Decay_Gamma_s"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class Decay_ExpTerm(PhysicsTerm):
    """
    PhysicsTerm: Decay_ExpTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "Decay_ExpTerm"
        self.description = "PhysicsTerm: Decay_ExpTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class Decay_OneMinusExp(PhysicsTerm):
    """
    PhysicsTerm: Decay_OneMinusExp
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "Decay_OneMinusExp"
        self.description = "PhysicsTerm: Decay_OneMinusExp"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class ScmPenetration_P_SCm(PhysicsTerm):
    """
    PhysicsTerm: ScmPenetration_P_SCm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "ScmPenetration_P_SCm"
        self.description = "PhysicsTerm: ScmPenetration_P_SCm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class ScmPenetration_UmPlanet(PhysicsTerm):
    """
    PhysicsTerm: ScmPenetration_UmPlanet
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "ScmPenetration_UmPlanet"
        self.description = "PhysicsTerm: ScmPenetration_UmPlanet"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class ScmReactivity_Kappa_day(PhysicsTerm):
    """
    PhysicsTerm: ScmReactivity_Kappa_day
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "ScmReactivity_Kappa_day"
        self.description = "PhysicsTerm: ScmReactivity_Kappa_day"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class ScmReactivity_E_reactDecay(PhysicsTerm):
    """
    PhysicsTerm: ScmReactivity_E_reactDecay
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "ScmReactivity_E_reactDecay"
        self.description = "PhysicsTerm: ScmReactivity_E_reactDecay"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class SolarCycle_Omega_c(PhysicsTerm):
    """
    PhysicsTerm: SolarCycle_Omega_c
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "SolarCycle_Omega_c"
        self.description = "PhysicsTerm: SolarCycle_Omega_c"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class SolarCycle_PeriodYears(PhysicsTerm):
    """
    PhysicsTerm: SolarCycle_PeriodYears
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "SolarCycle_PeriodYears"
        self.description = "PhysicsTerm: SolarCycle_PeriodYears"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class SolarCycle_MuJ_Modulated(PhysicsTerm):
    """
    PhysicsTerm: SolarCycle_MuJ_Modulated
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "SolarCycle_MuJ_Modulated"
        self.description = "PhysicsTerm: SolarCycle_MuJ_Modulated"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class SolarCycle_B_j_Modulated(PhysicsTerm):
    """
    PhysicsTerm: SolarCycle_B_j_Modulated
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "SolarCycle_B_j_Modulated"
        self.description = "PhysicsTerm: SolarCycle_B_j_Modulated"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class SolarWind_Delta_sw(PhysicsTerm):
    """
    PhysicsTerm: SolarWind_Delta_sw
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "SolarWind_Delta_sw"
        self.description = "PhysicsTerm: SolarWind_Delta_sw"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class SolarWind_ModulationFactor(PhysicsTerm):
    """
    PhysicsTerm: SolarWind_ModulationFactor
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "SolarWind_ModulationFactor"
        self.description = "PhysicsTerm: SolarWind_ModulationFactor"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class SolarWind_U_g2_NoMod(PhysicsTerm):
    """
    PhysicsTerm: SolarWind_U_g2_NoMod
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "SolarWind_U_g2_NoMod"
        self.description = "PhysicsTerm: SolarWind_U_g2_NoMod"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class SolarWindVelocity_V_sw(PhysicsTerm):
    """
    PhysicsTerm: SolarWindVelocity_V_sw
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "SolarWindVelocity_V_sw"
        self.description = "PhysicsTerm: SolarWindVelocity_V_sw"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class SolarWindVelocity_V_swKmS(PhysicsTerm):
    """
    PhysicsTerm: SolarWindVelocity_V_swKmS
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "SolarWindVelocity_V_swKmS"
        self.description = "PhysicsTerm: SolarWindVelocity_V_swKmS"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class SolarWindVelocity_U_g2_NoSW(PhysicsTerm):
    """
    PhysicsTerm: SolarWindVelocity_U_g2_NoSW
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "SolarWindVelocity_U_g2_NoSW"
        self.description = "PhysicsTerm: SolarWindVelocity_U_g2_NoSW"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class StellarMass_M_s(PhysicsTerm):
    """
    PhysicsTerm: StellarMass_M_s
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "StellarMass_M_s"
        self.description = "PhysicsTerm: StellarMass_M_s"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class StellarMass_M_sInMsun(PhysicsTerm):
    """
    PhysicsTerm: StellarMass_M_sInMsun
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "StellarMass_M_sInMsun"
        self.description = "PhysicsTerm: StellarMass_M_sInMsun"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class ReactorEfficiencyTerm(PhysicsTerm):
    """
    PhysicsTerm: ReactorEfficiencyTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "ReactorEfficiencyTerm"
        self.description = "PhysicsTerm: ReactorEfficiencyTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class MuSTerm(PhysicsTerm):
    """
    PhysicsTerm: MuSTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "MuSTerm"
        self.description = "PhysicsTerm: MuSTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class GradMsRTerm(PhysicsTerm):
    """
    PhysicsTerm: GradMsRTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "GradMsRTerm"
        self.description = "PhysicsTerm: GradMsRTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class BjTerm(PhysicsTerm):
    """
    PhysicsTerm: BjTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "BjTerm"
        self.description = "PhysicsTerm: BjTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class OmegaSTTerm(PhysicsTerm):
    """
    PhysicsTerm: OmegaSTTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "OmegaSTTerm"
        self.description = "PhysicsTerm: OmegaSTTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class MuJTerm(PhysicsTerm):
    """
    PhysicsTerm: MuJTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "MuJTerm"
        self.description = "PhysicsTerm: MuJTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class UniversalGravity1Term(PhysicsTerm):
    """
    PhysicsTerm: UniversalGravity1Term
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "UniversalGravity1Term"
        self.description = "PhysicsTerm: UniversalGravity1Term"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class UniversalGravity2Term(PhysicsTerm):
    """
    PhysicsTerm: UniversalGravity2Term
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "UniversalGravity2Term"
        self.description = "PhysicsTerm: UniversalGravity2Term"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class UniversalGravity3Term(PhysicsTerm):
    """
    PhysicsTerm: UniversalGravity3Term
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "UniversalGravity3Term"
        self.description = "PhysicsTerm: UniversalGravity3Term"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class UniversalGravity4Term(PhysicsTerm):
    """
    PhysicsTerm: UniversalGravity4Term
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "UniversalGravity4Term"
        self.description = "PhysicsTerm: UniversalGravity4Term"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class UniversalBuoyancyTerm(PhysicsTerm):
    """
    PhysicsTerm: UniversalBuoyancyTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "UniversalBuoyancyTerm"
        self.description = "PhysicsTerm: UniversalBuoyancyTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class UniversalMagnetismTerm(PhysicsTerm):
    """
    PhysicsTerm: UniversalMagnetismTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "UniversalMagnetismTerm"
        self.description = "PhysicsTerm: UniversalMagnetismTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class UniversalAetherTerm(PhysicsTerm):
    """
    PhysicsTerm: UniversalAetherTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "UniversalAetherTerm"
        self.description = "PhysicsTerm: UniversalAetherTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class UnifiedFieldTerm(PhysicsTerm):
    """
    PhysicsTerm: UnifiedFieldTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "UnifiedFieldTerm"
        self.description = "PhysicsTerm: UnifiedFieldTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class MUGECompressedBaseTerm(PhysicsTerm):
    """
    PhysicsTerm: MUGECompressedBaseTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "MUGECompressedBaseTerm"
        self.description = "PhysicsTerm: MUGECompressedBaseTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class MUGEExpansionTerm(PhysicsTerm):
    """
    PhysicsTerm: MUGEExpansionTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "MUGEExpansionTerm"
        self.description = "PhysicsTerm: MUGEExpansionTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class MUGESuperAdjustmentTerm(PhysicsTerm):
    """
    PhysicsTerm: MUGESuperAdjustmentTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "MUGESuperAdjustmentTerm"
        self.description = "PhysicsTerm: MUGESuperAdjustmentTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class MUGEEnvelopeTerm(PhysicsTerm):
    """
    PhysicsTerm: MUGEEnvelopeTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "MUGEEnvelopeTerm"
        self.description = "PhysicsTerm: MUGEEnvelopeTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class MUGEUgSumTerm(PhysicsTerm):
    """
    PhysicsTerm: MUGEUgSumTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "MUGEUgSumTerm"
        self.description = "PhysicsTerm: MUGEUgSumTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class MUGECosmologicalTerm(PhysicsTerm):
    """
    PhysicsTerm: MUGECosmologicalTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "MUGECosmologicalTerm"
        self.description = "PhysicsTerm: MUGECosmologicalTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class MUGEQuantumTerm(PhysicsTerm):
    """
    PhysicsTerm: MUGEQuantumTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "MUGEQuantumTerm"
        self.description = "PhysicsTerm: MUGEQuantumTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class MUGEFluidTerm(PhysicsTerm):
    """
    PhysicsTerm: MUGEFluidTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "MUGEFluidTerm"
        self.description = "PhysicsTerm: MUGEFluidTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class MUGEPerturbationTerm(PhysicsTerm):
    """
    PhysicsTerm: MUGEPerturbationTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "MUGEPerturbationTerm"
        self.description = "PhysicsTerm: MUGEPerturbationTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class MUGEResonanceADPMTerm(PhysicsTerm):
    """
    PhysicsTerm: MUGEResonanceADPMTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "MUGEResonanceADPMTerm"
        self.description = "PhysicsTerm: MUGEResonanceADPMTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class MUGEResonanceATHzTerm(PhysicsTerm):
    """
    PhysicsTerm: MUGEResonanceATHzTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "MUGEResonanceATHzTerm"
        self.description = "PhysicsTerm: MUGEResonanceATHzTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class MUGEResonanceAvacDiffTerm(PhysicsTerm):
    """
    PhysicsTerm: MUGEResonanceAvacDiffTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "MUGEResonanceAvacDiffTerm"
        self.description = "PhysicsTerm: MUGEResonanceAvacDiffTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class MUGEResonanceASuperFreqTerm(PhysicsTerm):
    """
    PhysicsTerm: MUGEResonanceASuperFreqTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "MUGEResonanceASuperFreqTerm"
        self.description = "PhysicsTerm: MUGEResonanceASuperFreqTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class MUGEResonanceAAetherResTerm(PhysicsTerm):
    """
    PhysicsTerm: MUGEResonanceAAetherResTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "MUGEResonanceAAetherResTerm"
        self.description = "PhysicsTerm: MUGEResonanceAAetherResTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class MUGEResonanceUg4iTerm(PhysicsTerm):
    """
    PhysicsTerm: MUGEResonanceUg4iTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "MUGEResonanceUg4iTerm"
        self.description = "PhysicsTerm: MUGEResonanceUg4iTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class MUGEResonanceAQuantumFreqTerm(PhysicsTerm):
    """
    PhysicsTerm: MUGEResonanceAQuantumFreqTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "MUGEResonanceAQuantumFreqTerm"
        self.description = "PhysicsTerm: MUGEResonanceAQuantumFreqTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class MUGEResonanceAAetherFreqTerm(PhysicsTerm):
    """
    PhysicsTerm: MUGEResonanceAAetherFreqTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "MUGEResonanceAAetherFreqTerm"
        self.description = "PhysicsTerm: MUGEResonanceAAetherFreqTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class MUGEResonanceAFluidFreqTerm(PhysicsTerm):
    """
    PhysicsTerm: MUGEResonanceAFluidFreqTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "MUGEResonanceAFluidFreqTerm"
        self.description = "PhysicsTerm: MUGEResonanceAFluidFreqTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class MUGEResonanceOscTerm(PhysicsTerm):
    """
    PhysicsTerm: MUGEResonanceOscTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "MUGEResonanceOscTerm"
        self.description = "PhysicsTerm: MUGEResonanceOscTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class MUGEResonanceAExpFreqTerm(PhysicsTerm):
    """
    PhysicsTerm: MUGEResonanceAExpFreqTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "MUGEResonanceAExpFreqTerm"
        self.description = "PhysicsTerm: MUGEResonanceAExpFreqTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class MUGEResonanceFTRZTerm(PhysicsTerm):
    """
    PhysicsTerm: MUGEResonanceFTRZTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "MUGEResonanceFTRZTerm"
        self.description = "PhysicsTerm: MUGEResonanceFTRZTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class MUGEResonanceWormholeTerm(PhysicsTerm):
    """
    PhysicsTerm: MUGEResonanceWormholeTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "MUGEResonanceWormholeTerm"
        self.description = "PhysicsTerm: MUGEResonanceWormholeTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class TauLeptonDipoleTerm(PhysicsTerm):
    """
    PhysicsTerm: TauLeptonDipoleTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "TauLeptonDipoleTerm"
        self.description = "PhysicsTerm: TauLeptonDipoleTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class CKMVcbTerm(PhysicsTerm):
    """
    PhysicsTerm: CKMVcbTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "CKMVcbTerm"
        self.description = "PhysicsTerm: CKMVcbTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class LFVBranchingTerm(PhysicsTerm):
    """
    PhysicsTerm: LFVBranchingTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "LFVBranchingTerm"
        self.description = "PhysicsTerm: LFVBranchingTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class VectorLikeQuarkTerm(PhysicsTerm):
    """
    PhysicsTerm: VectorLikeQuarkTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "VectorLikeQuarkTerm"
        self.description = "PhysicsTerm: VectorLikeQuarkTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class UnifiedFieldTerm_SOURCE4(PhysicsTerm):
    """
    PhysicsTerm: UnifiedFieldTerm_SOURCE4
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "UnifiedFieldTerm_SOURCE4"
        self.description = "PhysicsTerm: UnifiedFieldTerm_SOURCE4"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class MUGESuperAdjTerm(PhysicsTerm):
    """
    PhysicsTerm: MUGESuperAdjTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "MUGESuperAdjTerm"
        self.description = "PhysicsTerm: MUGESuperAdjTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class MUGECosmTerm(PhysicsTerm):
    """
    PhysicsTerm: MUGECosmTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "MUGECosmTerm"
        self.description = "PhysicsTerm: MUGECosmTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class SGR1745MagnetarTerm(PhysicsTerm):
    """
    PhysicsTerm: SGR1745MagnetarTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "SGR1745MagnetarTerm"
        self.description = "PhysicsTerm: SGR1745MagnetarTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class SagittariusAStarTerm(PhysicsTerm):
    """
    PhysicsTerm: SagittariusAStarTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "SagittariusAStarTerm"
        self.description = "PhysicsTerm: SagittariusAStarTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class TapestryStarbirthTerm(PhysicsTerm):
    """
    PhysicsTerm: TapestryStarbirthTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "TapestryStarbirthTerm"
        self.description = "PhysicsTerm: TapestryStarbirthTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class Westerlund2ClusterTerm(PhysicsTerm):
    """
    PhysicsTerm: Westerlund2ClusterTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "Westerlund2ClusterTerm"
        self.description = "PhysicsTerm: Westerlund2ClusterTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class PillarsCreationTerm(PhysicsTerm):
    """
    PhysicsTerm: PillarsCreationTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "PillarsCreationTerm"
        self.description = "PhysicsTerm: PillarsCreationTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class RingsRelativityTerm(PhysicsTerm):
    """
    PhysicsTerm: RingsRelativityTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "RingsRelativityTerm"
        self.description = "PhysicsTerm: RingsRelativityTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class StudentGuideUniverseTerm(PhysicsTerm):
    """
    PhysicsTerm: StudentGuideUniverseTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "StudentGuideUniverseTerm"
        self.description = "PhysicsTerm: StudentGuideUniverseTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class CompressedMUGETerm_SOURCE4(PhysicsTerm):
    """
    PhysicsTerm: CompressedMUGETerm_SOURCE4
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "CompressedMUGETerm_SOURCE4"
        self.description = "PhysicsTerm: CompressedMUGETerm_SOURCE4"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class ResonanceMUGETerm_SOURCE4(PhysicsTerm):
    """
    PhysicsTerm: ResonanceMUGETerm_SOURCE4
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "ResonanceMUGETerm_SOURCE4"
        self.description = "PhysicsTerm: ResonanceMUGETerm_SOURCE4"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class DynamicVacuumTerm(PhysicsTerm):
    """
    PhysicsTerm: DynamicVacuumTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "DynamicVacuumTerm"
        self.description = "PhysicsTerm: DynamicVacuumTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class QuantumCouplingTerm(PhysicsTerm):
    """
    PhysicsTerm: QuantumCouplingTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "QuantumCouplingTerm"
        self.description = "PhysicsTerm: QuantumCouplingTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class DynamicVacuumTerm(PhysicsTerm):
    """
    PhysicsTerm: DynamicVacuumTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "DynamicVacuumTerm"
        self.description = "PhysicsTerm: DynamicVacuumTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class QuantumCouplingTerm(PhysicsTerm):
    """
    PhysicsTerm: QuantumCouplingTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "QuantumCouplingTerm"
        self.description = "PhysicsTerm: QuantumCouplingTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class DynamicVacuumTerm(PhysicsTerm):
    """
    PhysicsTerm: DynamicVacuumTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "DynamicVacuumTerm"
        self.description = "PhysicsTerm: DynamicVacuumTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class QuantumCouplingTerm(PhysicsTerm):
    """
    PhysicsTerm: QuantumCouplingTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "QuantumCouplingTerm"
        self.description = "PhysicsTerm: QuantumCouplingTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class DynamicVacuumTerm(PhysicsTerm):
    """
    PhysicsTerm: DynamicVacuumTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "DynamicVacuumTerm"
        self.description = "PhysicsTerm: DynamicVacuumTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class QuantumCouplingTerm(PhysicsTerm):
    """
    PhysicsTerm: QuantumCouplingTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "QuantumCouplingTerm"
        self.description = "PhysicsTerm: QuantumCouplingTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class DynamicVacuumTerm(PhysicsTerm):
    """
    PhysicsTerm: DynamicVacuumTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "DynamicVacuumTerm"
        self.description = "PhysicsTerm: DynamicVacuumTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class QuantumCouplingTerm(PhysicsTerm):
    """
    PhysicsTerm: QuantumCouplingTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "QuantumCouplingTerm"
        self.description = "PhysicsTerm: QuantumCouplingTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class DynamicVacuumTerm(PhysicsTerm):
    """
    PhysicsTerm: DynamicVacuumTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "DynamicVacuumTerm"
        self.description = "PhysicsTerm: DynamicVacuumTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class QuantumCouplingTerm(PhysicsTerm):
    """
    PhysicsTerm: QuantumCouplingTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "QuantumCouplingTerm"
        self.description = "PhysicsTerm: QuantumCouplingTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class DynamicVacuumTerm(PhysicsTerm):
    """
    PhysicsTerm: DynamicVacuumTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "DynamicVacuumTerm"
        self.description = "PhysicsTerm: DynamicVacuumTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class QuantumCouplingTerm(PhysicsTerm):
    """
    PhysicsTerm: QuantumCouplingTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "QuantumCouplingTerm"
        self.description = "PhysicsTerm: QuantumCouplingTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class DynamicVacuumTerm(PhysicsTerm):
    """
    PhysicsTerm: DynamicVacuumTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "DynamicVacuumTerm"
        self.description = "PhysicsTerm: DynamicVacuumTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class QuantumCouplingTerm(PhysicsTerm):
    """
    PhysicsTerm: QuantumCouplingTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "QuantumCouplingTerm"
        self.description = "PhysicsTerm: QuantumCouplingTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class DynamicVacuumTerm(PhysicsTerm):
    """
    PhysicsTerm: DynamicVacuumTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "DynamicVacuumTerm"
        self.description = "PhysicsTerm: DynamicVacuumTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class QuantumCouplingTerm(PhysicsTerm):
    """
    PhysicsTerm: QuantumCouplingTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "QuantumCouplingTerm"
        self.description = "PhysicsTerm: QuantumCouplingTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class DynamicVacuumTerm(PhysicsTerm):
    """
    PhysicsTerm: DynamicVacuumTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "DynamicVacuumTerm"
        self.description = "PhysicsTerm: DynamicVacuumTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class QuantumCouplingTerm(PhysicsTerm):
    """
    PhysicsTerm: QuantumCouplingTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "QuantumCouplingTerm"
        self.description = "PhysicsTerm: QuantumCouplingTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class DynamicVacuumTerm(PhysicsTerm):
    """
    PhysicsTerm: DynamicVacuumTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "DynamicVacuumTerm"
        self.description = "PhysicsTerm: DynamicVacuumTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class QuantumCouplingTerm(PhysicsTerm):
    """
    PhysicsTerm: QuantumCouplingTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "QuantumCouplingTerm"
        self.description = "PhysicsTerm: QuantumCouplingTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class DynamicVacuumTerm(PhysicsTerm):
    """
    PhysicsTerm: DynamicVacuumTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "DynamicVacuumTerm"
        self.description = "PhysicsTerm: DynamicVacuumTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class QuantumCouplingTerm(PhysicsTerm):
    """
    PhysicsTerm: QuantumCouplingTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "QuantumCouplingTerm"
        self.description = "PhysicsTerm: QuantumCouplingTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class DynamicVacuumTerm(PhysicsTerm):
    """
    PhysicsTerm: DynamicVacuumTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "DynamicVacuumTerm"
        self.description = "PhysicsTerm: DynamicVacuumTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class QuantumCouplingTerm(PhysicsTerm):
    """
    PhysicsTerm: QuantumCouplingTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "QuantumCouplingTerm"
        self.description = "PhysicsTerm: QuantumCouplingTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class DynamicVacuumTerm(PhysicsTerm):
    """
    PhysicsTerm: DynamicVacuumTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "DynamicVacuumTerm"
        self.description = "PhysicsTerm: DynamicVacuumTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class QuantumCouplingTerm(PhysicsTerm):
    """
    PhysicsTerm: QuantumCouplingTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "QuantumCouplingTerm"
        self.description = "PhysicsTerm: QuantumCouplingTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class DynamicVacuumTerm(PhysicsTerm):
    """
    PhysicsTerm: DynamicVacuumTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "DynamicVacuumTerm"
        self.description = "PhysicsTerm: DynamicVacuumTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class QuantumCouplingTerm(PhysicsTerm):
    """
    PhysicsTerm: QuantumCouplingTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "QuantumCouplingTerm"
        self.description = "PhysicsTerm: QuantumCouplingTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class DynamicVacuumTerm(PhysicsTerm):
    """
    PhysicsTerm: DynamicVacuumTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "DynamicVacuumTerm"
        self.description = "PhysicsTerm: DynamicVacuumTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class QuantumCouplingTerm(PhysicsTerm):
    """
    PhysicsTerm: QuantumCouplingTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "QuantumCouplingTerm"
        self.description = "PhysicsTerm: QuantumCouplingTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class DynamicVacuumTerm(PhysicsTerm):
    """
    PhysicsTerm: DynamicVacuumTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "DynamicVacuumTerm"
        self.description = "PhysicsTerm: DynamicVacuumTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class QuantumCouplingTerm(PhysicsTerm):
    """
    PhysicsTerm: QuantumCouplingTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "QuantumCouplingTerm"
        self.description = "PhysicsTerm: QuantumCouplingTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class DynamicVacuumTerm(PhysicsTerm):
    """
    PhysicsTerm: DynamicVacuumTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "DynamicVacuumTerm"
        self.description = "PhysicsTerm: DynamicVacuumTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class QuantumCouplingTerm(PhysicsTerm):
    """
    PhysicsTerm: QuantumCouplingTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "QuantumCouplingTerm"
        self.description = "PhysicsTerm: QuantumCouplingTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class DynamicVacuumTerm(PhysicsTerm):
    """
    PhysicsTerm: DynamicVacuumTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "DynamicVacuumTerm"
        self.description = "PhysicsTerm: DynamicVacuumTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class QuantumCouplingTerm(PhysicsTerm):
    """
    PhysicsTerm: QuantumCouplingTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "QuantumCouplingTerm"
        self.description = "PhysicsTerm: QuantumCouplingTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class DynamicVacuumTerm(PhysicsTerm):
    """
    PhysicsTerm: DynamicVacuumTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "DynamicVacuumTerm"
        self.description = "PhysicsTerm: DynamicVacuumTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class QuantumCouplingTerm(PhysicsTerm):
    """
    PhysicsTerm: QuantumCouplingTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "QuantumCouplingTerm"
        self.description = "PhysicsTerm: QuantumCouplingTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class DynamicVacuumTerm(PhysicsTerm):
    """
    PhysicsTerm: DynamicVacuumTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "DynamicVacuumTerm"
        self.description = "PhysicsTerm: DynamicVacuumTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class QuantumCouplingTerm(PhysicsTerm):
    """
    PhysicsTerm: QuantumCouplingTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "QuantumCouplingTerm"
        self.description = "PhysicsTerm: QuantumCouplingTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class DynamicVacuumTerm(PhysicsTerm):
    """
    PhysicsTerm: DynamicVacuumTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "DynamicVacuumTerm"
        self.description = "PhysicsTerm: DynamicVacuumTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class QuantumCouplingTerm(PhysicsTerm):
    """
    PhysicsTerm: QuantumCouplingTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "QuantumCouplingTerm"
        self.description = "PhysicsTerm: QuantumCouplingTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class DynamicVacuumTerm(PhysicsTerm):
    """
    PhysicsTerm: DynamicVacuumTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "DynamicVacuumTerm"
        self.description = "PhysicsTerm: DynamicVacuumTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class QuantumCouplingTerm(PhysicsTerm):
    """
    PhysicsTerm: QuantumCouplingTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "QuantumCouplingTerm"
        self.description = "PhysicsTerm: QuantumCouplingTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class DynamicVacuumTerm(PhysicsTerm):
    """
    PhysicsTerm: DynamicVacuumTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "DynamicVacuumTerm"
        self.description = "PhysicsTerm: DynamicVacuumTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class QuantumCouplingTerm(PhysicsTerm):
    """
    PhysicsTerm: QuantumCouplingTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "QuantumCouplingTerm"
        self.description = "PhysicsTerm: QuantumCouplingTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class DynamicVacuumTerm(PhysicsTerm):
    """
    PhysicsTerm: DynamicVacuumTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "DynamicVacuumTerm"
        self.description = "PhysicsTerm: DynamicVacuumTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class QuantumCouplingTerm(PhysicsTerm):
    """
    PhysicsTerm: QuantumCouplingTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "QuantumCouplingTerm"
        self.description = "PhysicsTerm: QuantumCouplingTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class DynamicVacuumTerm(PhysicsTerm):
    """
    PhysicsTerm: DynamicVacuumTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "DynamicVacuumTerm"
        self.description = "PhysicsTerm: DynamicVacuumTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class QuantumCouplingTerm(PhysicsTerm):
    """
    PhysicsTerm: QuantumCouplingTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "QuantumCouplingTerm"
        self.description = "PhysicsTerm: QuantumCouplingTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class DynamicVacuumTerm(PhysicsTerm):
    """
    PhysicsTerm: DynamicVacuumTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "DynamicVacuumTerm"
        self.description = "PhysicsTerm: DynamicVacuumTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class QuantumCouplingTerm(PhysicsTerm):
    """
    PhysicsTerm: QuantumCouplingTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "QuantumCouplingTerm"
        self.description = "PhysicsTerm: QuantumCouplingTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class DynamicVacuumTerm(PhysicsTerm):
    """
    PhysicsTerm: DynamicVacuumTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "DynamicVacuumTerm"
        self.description = "PhysicsTerm: DynamicVacuumTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class QuantumCouplingTerm(PhysicsTerm):
    """
    PhysicsTerm: QuantumCouplingTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "QuantumCouplingTerm"
        self.description = "PhysicsTerm: QuantumCouplingTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class DynamicVacuumTerm(PhysicsTerm):
    """
    PhysicsTerm: DynamicVacuumTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "DynamicVacuumTerm"
        self.description = "PhysicsTerm: DynamicVacuumTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class QuantumCouplingTerm(PhysicsTerm):
    """
    PhysicsTerm: QuantumCouplingTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "QuantumCouplingTerm"
        self.description = "PhysicsTerm: QuantumCouplingTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class DynamicVacuumTerm(PhysicsTerm):
    """
    PhysicsTerm: DynamicVacuumTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "DynamicVacuumTerm"
        self.description = "PhysicsTerm: DynamicVacuumTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class QuantumCouplingTerm(PhysicsTerm):
    """
    PhysicsTerm: QuantumCouplingTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "QuantumCouplingTerm"
        self.description = "PhysicsTerm: QuantumCouplingTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class DynamicVacuumTerm(PhysicsTerm):
    """
    PhysicsTerm: DynamicVacuumTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "DynamicVacuumTerm"
        self.description = "PhysicsTerm: DynamicVacuumTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class QuantumCouplingTerm(PhysicsTerm):
    """
    PhysicsTerm: QuantumCouplingTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "QuantumCouplingTerm"
        self.description = "PhysicsTerm: QuantumCouplingTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class DynamicVacuumTerm(PhysicsTerm):
    """
    PhysicsTerm: DynamicVacuumTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "DynamicVacuumTerm"
        self.description = "PhysicsTerm: DynamicVacuumTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class QuantumCouplingTerm(PhysicsTerm):
    """
    PhysicsTerm: QuantumCouplingTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "QuantumCouplingTerm"
        self.description = "PhysicsTerm: QuantumCouplingTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class DynamicVacuumTerm(PhysicsTerm):
    """
    PhysicsTerm: DynamicVacuumTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "DynamicVacuumTerm"
        self.description = "PhysicsTerm: DynamicVacuumTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class QuantumCouplingTerm(PhysicsTerm):
    """
    PhysicsTerm: QuantumCouplingTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "QuantumCouplingTerm"
        self.description = "PhysicsTerm: QuantumCouplingTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class DynamicVacuumTerm(PhysicsTerm):
    """
    PhysicsTerm: DynamicVacuumTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "DynamicVacuumTerm"
        self.description = "PhysicsTerm: DynamicVacuumTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class QuantumCouplingTerm(PhysicsTerm):
    """
    PhysicsTerm: QuantumCouplingTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "QuantumCouplingTerm"
        self.description = "PhysicsTerm: QuantumCouplingTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class DynamicVacuumTerm(PhysicsTerm):
    """
    PhysicsTerm: DynamicVacuumTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "DynamicVacuumTerm"
        self.description = "PhysicsTerm: DynamicVacuumTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class QuantumCouplingTerm(PhysicsTerm):
    """
    PhysicsTerm: QuantumCouplingTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "QuantumCouplingTerm"
        self.description = "PhysicsTerm: QuantumCouplingTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class DynamicVacuumTerm(PhysicsTerm):
    """
    PhysicsTerm: DynamicVacuumTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "DynamicVacuumTerm"
        self.description = "PhysicsTerm: DynamicVacuumTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class QuantumCouplingTerm(PhysicsTerm):
    """
    PhysicsTerm: QuantumCouplingTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "QuantumCouplingTerm"
        self.description = "PhysicsTerm: QuantumCouplingTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class DynamicVacuumTerm(PhysicsTerm):
    """
    PhysicsTerm: DynamicVacuumTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "DynamicVacuumTerm"
        self.description = "PhysicsTerm: DynamicVacuumTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class QuantumCouplingTerm(PhysicsTerm):
    """
    PhysicsTerm: QuantumCouplingTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "QuantumCouplingTerm"
        self.description = "PhysicsTerm: QuantumCouplingTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class DynamicVacuumTerm(PhysicsTerm):
    """
    PhysicsTerm: DynamicVacuumTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "DynamicVacuumTerm"
        self.description = "PhysicsTerm: DynamicVacuumTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class QuantumCouplingTerm(PhysicsTerm):
    """
    PhysicsTerm: QuantumCouplingTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "QuantumCouplingTerm"
        self.description = "PhysicsTerm: QuantumCouplingTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class DynamicVacuumTerm(PhysicsTerm):
    """
    PhysicsTerm: DynamicVacuumTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "DynamicVacuumTerm"
        self.description = "PhysicsTerm: DynamicVacuumTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class QuantumCouplingTerm(PhysicsTerm):
    """
    PhysicsTerm: QuantumCouplingTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "QuantumCouplingTerm"
        self.description = "PhysicsTerm: QuantumCouplingTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class DynamicVacuumTerm(PhysicsTerm):
    """
    PhysicsTerm: DynamicVacuumTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "DynamicVacuumTerm"
        self.description = "PhysicsTerm: DynamicVacuumTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class QuantumCouplingTerm(PhysicsTerm):
    """
    PhysicsTerm: QuantumCouplingTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "QuantumCouplingTerm"
        self.description = "PhysicsTerm: QuantumCouplingTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class DynamicVacuumTerm(PhysicsTerm):
    """
    PhysicsTerm: DynamicVacuumTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "DynamicVacuumTerm"
        self.description = "PhysicsTerm: DynamicVacuumTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class QuantumCouplingTerm(PhysicsTerm):
    """
    PhysicsTerm: QuantumCouplingTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "QuantumCouplingTerm"
        self.description = "PhysicsTerm: QuantumCouplingTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class DynamicVacuumTerm(PhysicsTerm):
    """
    PhysicsTerm: DynamicVacuumTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "DynamicVacuumTerm"
        self.description = "PhysicsTerm: DynamicVacuumTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class QuantumCouplingTerm(PhysicsTerm):
    """
    PhysicsTerm: QuantumCouplingTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "QuantumCouplingTerm"
        self.description = "PhysicsTerm: QuantumCouplingTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class DynamicVacuumTerm(PhysicsTerm):
    """
    PhysicsTerm: DynamicVacuumTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "DynamicVacuumTerm"
        self.description = "PhysicsTerm: DynamicVacuumTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class QuantumCouplingTerm(PhysicsTerm):
    """
    PhysicsTerm: QuantumCouplingTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "QuantumCouplingTerm"
        self.description = "PhysicsTerm: QuantumCouplingTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class DynamicVacuumTerm(PhysicsTerm):
    """
    PhysicsTerm: DynamicVacuumTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "DynamicVacuumTerm"
        self.description = "PhysicsTerm: DynamicVacuumTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class QuantumCouplingTerm(PhysicsTerm):
    """
    PhysicsTerm: QuantumCouplingTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "QuantumCouplingTerm"
        self.description = "PhysicsTerm: QuantumCouplingTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class DynamicVacuumTerm(PhysicsTerm):
    """
    PhysicsTerm: DynamicVacuumTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "DynamicVacuumTerm"
        self.description = "PhysicsTerm: DynamicVacuumTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class QuantumCouplingTerm(PhysicsTerm):
    """
    PhysicsTerm: QuantumCouplingTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "QuantumCouplingTerm"
        self.description = "PhysicsTerm: QuantumCouplingTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class DynamicVacuumTerm(PhysicsTerm):
    """
    PhysicsTerm: DynamicVacuumTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "DynamicVacuumTerm"
        self.description = "PhysicsTerm: DynamicVacuumTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class QuantumCouplingTerm(PhysicsTerm):
    """
    PhysicsTerm: QuantumCouplingTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "QuantumCouplingTerm"
        self.description = "PhysicsTerm: QuantumCouplingTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class DynamicVacuumTerm(PhysicsTerm):
    """
    PhysicsTerm: DynamicVacuumTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "DynamicVacuumTerm"
        self.description = "PhysicsTerm: DynamicVacuumTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class QuantumCouplingTerm(PhysicsTerm):
    """
    PhysicsTerm: QuantumCouplingTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "QuantumCouplingTerm"
        self.description = "PhysicsTerm: QuantumCouplingTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class DynamicVacuumTerm(PhysicsTerm):
    """
    PhysicsTerm: DynamicVacuumTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "DynamicVacuumTerm"
        self.description = "PhysicsTerm: DynamicVacuumTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class QuantumCouplingTerm(PhysicsTerm):
    """
    PhysicsTerm: QuantumCouplingTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "QuantumCouplingTerm"
        self.description = "PhysicsTerm: QuantumCouplingTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class DynamicVacuumTerm(PhysicsTerm):
    """
    PhysicsTerm: DynamicVacuumTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "DynamicVacuumTerm"
        self.description = "PhysicsTerm: DynamicVacuumTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class QuantumCouplingTerm(PhysicsTerm):
    """
    PhysicsTerm: QuantumCouplingTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "QuantumCouplingTerm"
        self.description = "PhysicsTerm: QuantumCouplingTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class DynamicVacuumTerm(PhysicsTerm):
    """
    PhysicsTerm: DynamicVacuumTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "DynamicVacuumTerm"
        self.description = "PhysicsTerm: DynamicVacuumTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class QuantumCouplingTerm(PhysicsTerm):
    """
    PhysicsTerm: QuantumCouplingTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "QuantumCouplingTerm"
        self.description = "PhysicsTerm: QuantumCouplingTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class DynamicVacuumTerm(PhysicsTerm):
    """
    PhysicsTerm: DynamicVacuumTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "DynamicVacuumTerm"
        self.description = "PhysicsTerm: DynamicVacuumTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class QuantumCouplingTerm(PhysicsTerm):
    """
    PhysicsTerm: QuantumCouplingTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "QuantumCouplingTerm"
        self.description = "PhysicsTerm: QuantumCouplingTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class DynamicVacuumTerm(PhysicsTerm):
    """
    PhysicsTerm: DynamicVacuumTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "DynamicVacuumTerm"
        self.description = "PhysicsTerm: DynamicVacuumTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class QuantumCouplingTerm(PhysicsTerm):
    """
    PhysicsTerm: QuantumCouplingTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "QuantumCouplingTerm"
        self.description = "PhysicsTerm: QuantumCouplingTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class DynamicVacuumTerm(PhysicsTerm):
    """
    PhysicsTerm: DynamicVacuumTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "DynamicVacuumTerm"
        self.description = "PhysicsTerm: DynamicVacuumTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class QuantumCouplingTerm(PhysicsTerm):
    """
    PhysicsTerm: QuantumCouplingTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "QuantumCouplingTerm"
        self.description = "PhysicsTerm: QuantumCouplingTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class DynamicVacuumTerm(PhysicsTerm):
    """
    PhysicsTerm: DynamicVacuumTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "DynamicVacuumTerm"
        self.description = "PhysicsTerm: DynamicVacuumTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class QuantumCouplingTerm(PhysicsTerm):
    """
    PhysicsTerm: QuantumCouplingTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "QuantumCouplingTerm"
        self.description = "PhysicsTerm: QuantumCouplingTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class DynamicVacuumTerm(PhysicsTerm):
    """
    PhysicsTerm: DynamicVacuumTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "DynamicVacuumTerm"
        self.description = "PhysicsTerm: DynamicVacuumTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class QuantumCouplingTerm(PhysicsTerm):
    """
    PhysicsTerm: QuantumCouplingTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "QuantumCouplingTerm"
        self.description = "PhysicsTerm: QuantumCouplingTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class DynamicVacuumTerm(PhysicsTerm):
    """
    PhysicsTerm: DynamicVacuumTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "DynamicVacuumTerm"
        self.description = "PhysicsTerm: DynamicVacuumTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class QuantumCouplingTerm(PhysicsTerm):
    """
    PhysicsTerm: QuantumCouplingTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "QuantumCouplingTerm"
        self.description = "PhysicsTerm: QuantumCouplingTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class DynamicVacuumTerm(PhysicsTerm):
    """
    PhysicsTerm: DynamicVacuumTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "DynamicVacuumTerm"
        self.description = "PhysicsTerm: DynamicVacuumTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class QuantumCouplingTerm(PhysicsTerm):
    """
    PhysicsTerm: QuantumCouplingTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "QuantumCouplingTerm"
        self.description = "PhysicsTerm: QuantumCouplingTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class DynamicVacuumTerm(PhysicsTerm):
    """
    PhysicsTerm: DynamicVacuumTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "DynamicVacuumTerm"
        self.description = "PhysicsTerm: DynamicVacuumTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class QuantumCouplingTerm(PhysicsTerm):
    """
    PhysicsTerm: QuantumCouplingTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "QuantumCouplingTerm"
        self.description = "PhysicsTerm: QuantumCouplingTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class DynamicVacuumTerm(PhysicsTerm):
    """
    PhysicsTerm: DynamicVacuumTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "DynamicVacuumTerm"
        self.description = "PhysicsTerm: DynamicVacuumTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


class QuantumCouplingTerm(PhysicsTerm):
    """
    PhysicsTerm: QuantumCouplingTerm
    
    Converted from MAIN_1_CoAnQi.cpp PhysicsTerm class.
    """
    
    def __init__(self):
        super().__init__()
        self.name = "QuantumCouplingTerm"
        self.description = "PhysicsTerm: QuantumCouplingTerm"
    
    def compute(self, params: dict) -> float:
        """Compute physics term value."""
        try:
            # Extract parameters with defaults
            r = params.get('r', 1.0)
            t = params.get('t', 0.0)
            M = params.get('M', CONSTANTS.get('M_sun', 1.989e30))
            
            # Computation from C++
            result = 0.0
            return float(result) if result == result else 0.0  # NaN check
        except Exception:
            return 0.0
    
    def get_description(self) -> str:
        return self.description


# ═══════════════════════════════════════════════════════════════════════════════
# PHYSICS TERM REGISTRY
# ═══════════════════════════════════════════════════════════════════════════════

CPP_PHYSICS_TERMS = {
    "DynamicVacuumTerm": DynamicVacuumTerm,
    "QuantumCouplingTerm": QuantumCouplingTerm,
    "DarkMatterHaloTerm": DarkMatterHaloTerm,
    "VacuumEnergyTerm": VacuumEnergyTerm,
    "QuantumEntanglementTerm": QuantumEntanglementTerm,
    "CosmicNeutrinoTerm": CosmicNeutrinoTerm,
    "MultiSystemUQFFTerm": MultiSystemUQFFTerm,
    "DPMResonanceTerm": DPMResonanceTerm,
    "LENRExtendedTerm": LENRExtendedTerm,
    "SMBHAccretionTerm": SMBHAccretionTerm,
    "TDETerm": TDETerm,
    "NebulaUQFFTerm": NebulaUQFFTerm,
    "GasIonizationTerm": GasIonizationTerm,
    "NebulaExpansionTerm": NebulaExpansionTerm,
    "BuoyancyUQFFTerm": BuoyancyUQFFTerm,
    "InflationBuoyancyTerm": InflationBuoyancyTerm,
    "SuperconductiveTerm": SuperconductiveTerm,
    "NeutronScatteringTerm": NeutronScatteringTerm,
    "AstroSystemUQFFTerm": AstroSystemUQFFTerm,
    "DipoleVortexTerm": DipoleVortexTerm,
    "QuantumState26Term": QuantumState26Term,
    "TriadicScaleTerm": TriadicScaleTerm,
    "UQFFMasterTerm": UQFFMasterTerm,
    "ElectrostaticBarrierTerm": ElectrostaticBarrierTerm,
    "ElectricFieldTerm": ElectricFieldTerm,
    "NeutronProductionTerm": NeutronProductionTerm,
    "CelestialBodyTerm": CelestialBodyTerm,
    "MUGETerm": MUGETerm,
    "QuasarJetTerm": QuasarJetTerm,
    "ReactorEnergyTerm": ReactorEnergyTerm,
    "MagneticDipoleTerm": MagneticDipoleTerm,
    "MagneticJetFieldTerm": MagneticJetFieldTerm,
    "UnifiedBuoyancyTerm": UnifiedBuoyancyTerm,
    "CompressedMUGEBaseTerm": CompressedMUGEBaseTerm,
    "CompressedMUGEExpansionTerm": CompressedMUGEExpansionTerm,
    "SuperconductiveAdjustmentTerm": SuperconductiveAdjustmentTerm,
    "CosmologicalConstantTerm": CosmologicalConstantTerm,
    "QuantumUncertaintyTerm": QuantumUncertaintyTerm,
    "FluidDynamicsTerm": FluidDynamicsTerm,
    "DensityPerturbationTerm": DensityPerturbationTerm,
    "UQFFModule5Term": UQFFModule5Term,
    "ResonanceMUGETerm": ResonanceMUGETerm,
    "StateExportTerm": StateExportTerm,
    "TimeVaryingRotationTerm": TimeVaryingRotationTerm,
    "NavierStokesFluidTerm": NavierStokesFluidTerm,
    "SpacetimeMetricModulationTerm": SpacetimeMetricModulationTerm,
    "FullUnifiedFieldTerm": FullUnifiedFieldTerm,
    "ResonanceMUGE_DPMTerm": ResonanceMUGE_DPMTerm,
    "ResonanceMUGE_THzTerm": ResonanceMUGE_THzTerm,
    "VacuumEnergyDifferentialTerm": VacuumEnergyDifferentialTerm,
    "SuperconductiveFrequencyTerm": SuperconductiveFrequencyTerm,
    "AetherResonanceTerm": AetherResonanceTerm,
    "QuantumFrequencyTerm": QuantumFrequencyTerm,
    "AetherFrequencyTerm": AetherFrequencyTerm,
    "WormholeContributionTerm": WormholeContributionTerm,
    "UnifiedFieldUg1Term": UnifiedFieldUg1Term,
    "UnifiedFieldUg2Term": UnifiedFieldUg2Term,
    "UnifiedFieldUg3Term": UnifiedFieldUg3Term,
    "UnifiedFieldUg4Term": UnifiedFieldUg4Term,
    "UnifiedFieldUmTerm": UnifiedFieldUmTerm,
    "SpacetimeMetricTerm": SpacetimeMetricTerm,
    "CompressedMUGETerm": CompressedMUGETerm,
    "ResonanceMUGE_VacuumDiffTerm": ResonanceMUGE_VacuumDiffTerm,
    "ResonanceMUGE_SuperFreqTerm": ResonanceMUGE_SuperFreqTerm,
    "ResonanceMUGE_AetherResTerm": ResonanceMUGE_AetherResTerm,
    "ResonanceMUGE_QuantumFreqTerm": ResonanceMUGE_QuantumFreqTerm,
    "YAMLConfigTerm": YAMLConfigTerm,
    "UQFFCoreBuoyancyTerm": UQFFCoreBuoyancyTerm,
    "VacuumRepulsionTerm": VacuumRepulsionTerm,
    "THzShockCommunicationTerm": THzShockCommunicationTerm,
    "ConduitFormationTerm": ConduitFormationTerm,
    "SpookyActionTerm": SpookyActionTerm,
    "DPMResonanceEnergyTerm": DPMResonanceEnergyTerm,
    "Triadic26LayerTerm": Triadic26LayerTerm,
    "MagnetarCoreTerm": MagnetarCoreTerm,
    "MagnetarLambdaTerm": MagnetarLambdaTerm,
    "MagnetarEMTerm": MagnetarEMTerm,
    "MagnetarGWTerm": MagnetarGWTerm,
    "MagnetarQuantumTerm": MagnetarQuantumTerm,
    "MagnetarFluidTerm": MagnetarFluidTerm,
    "MagnetarOscillatoryTerm": MagnetarOscillatoryTerm,
    "MagnetarDarkMatterTerm": MagnetarDarkMatterTerm,
    "MagnetarMagneticEnergyTerm": MagnetarMagneticEnergyTerm,
    "MagnetarDecayTerm": MagnetarDecayTerm,
    "Magnetar0501CoreTerm": Magnetar0501CoreTerm,
    "Magnetar0501LambdaTerm": Magnetar0501LambdaTerm,
    "Magnetar0501EMTerm": Magnetar0501EMTerm,
    "Magnetar0501GWTerm": Magnetar0501GWTerm,
    "Magnetar0501QuantumTerm": Magnetar0501QuantumTerm,
    "Magnetar0501FluidTerm": Magnetar0501FluidTerm,
    "Magnetar0501OscillatoryTerm": Magnetar0501OscillatoryTerm,
    "Magnetar0501DarkMatterTerm": Magnetar0501DarkMatterTerm,
    "SgrAStarCoreTerm": SgrAStarCoreTerm,
    "SgrAStarLambdaTerm": SgrAStarLambdaTerm,
    "SgrAStarEMTerm": SgrAStarEMTerm,
    "SgrAStarGWTerm": SgrAStarGWTerm,
    "SgrAStarQuantumTerm": SgrAStarQuantumTerm,
    "SgrAStarFluidTerm": SgrAStarFluidTerm,
    "SgrAStarOscillatoryTerm": SgrAStarOscillatoryTerm,
    "SgrAStarDarkMatterTerm": SgrAStarDarkMatterTerm,
    "StarbirthCoreTerm": StarbirthCoreTerm,
    "StarbirthLambdaTerm": StarbirthLambdaTerm,
    "StarbirthUQFFTerm": StarbirthUQFFTerm,
    "StarbirthEMTerm": StarbirthEMTerm,
    "StarbirthQuantumTerm": StarbirthQuantumTerm,
    "StarbirthFluidTerm": StarbirthFluidTerm,
    "StarbirthOscillatoryTerm": StarbirthOscillatoryTerm,
    "StarbirthDarkMatterTerm": StarbirthDarkMatterTerm,
    "StarbirthWindTerm": StarbirthWindTerm,
    "Westerlund2CoreTerm": Westerlund2CoreTerm,
    "Westerlund2LambdaTerm": Westerlund2LambdaTerm,
    "Westerlund2UQFFTerm": Westerlund2UQFFTerm,
    "Westerlund2EMTerm": Westerlund2EMTerm,
    "Westerlund2QuantumTerm": Westerlund2QuantumTerm,
    "Westerlund2FluidTerm": Westerlund2FluidTerm,
    "Westerlund2OscillatoryTerm": Westerlund2OscillatoryTerm,
    "Westerlund2DarkMatterTerm": Westerlund2DarkMatterTerm,
    "Westerlund2WindTerm": Westerlund2WindTerm,
    "PillarsCoreTerm": PillarsCoreTerm,
    "PillarsLambdaTerm": PillarsLambdaTerm,
    "PillarsUQFFTerm": PillarsUQFFTerm,
    "PillarsEMTerm": PillarsEMTerm,
    "PillarsQuantumTerm": PillarsQuantumTerm,
    "PillarsFluidTerm": PillarsFluidTerm,
    "PillarsOscillatoryTerm": PillarsOscillatoryTerm,
    "PillarsDarkMatterTerm": PillarsDarkMatterTerm,
    "PillarsWindTerm": PillarsWindTerm,
    "PillarsErosionTerm": PillarsErosionTerm,
    "EinsteinRingCoreTerm": EinsteinRingCoreTerm,
    "EinsteinRingLambdaTerm": EinsteinRingLambdaTerm,
    "EinsteinRingUQFFTerm": EinsteinRingUQFFTerm,
    "EinsteinRingEMTerm": EinsteinRingEMTerm,
    "EinsteinRingQuantumTerm": EinsteinRingQuantumTerm,
    "EinsteinRingFluidTerm": EinsteinRingFluidTerm,
    "EinsteinRingOscillatoryTerm": EinsteinRingOscillatoryTerm,
    "EinsteinRingDarkMatterTerm": EinsteinRingDarkMatterTerm,
    "EinsteinRingWindTerm": EinsteinRingWindTerm,
    "NGC2525CoreTerm": NGC2525CoreTerm,
    "NGC2525BlackHoleTerm": NGC2525BlackHoleTerm,
    "NGC2525LambdaTerm": NGC2525LambdaTerm,
    "NGC2525UQFFTerm": NGC2525UQFFTerm,
    "NGC2525EMTerm": NGC2525EMTerm,
    "NGC2525QuantumTerm": NGC2525QuantumTerm,
    "NGC2525FluidTerm": NGC2525FluidTerm,
    "NGC2525OscillatoryTerm": NGC2525OscillatoryTerm,
    "NGC2525DarkMatterTerm": NGC2525DarkMatterTerm,
    "NGC2525SupernovaTerm": NGC2525SupernovaTerm,
    "NGC3603CoreTerm": NGC3603CoreTerm,
    "NGC3603LambdaTerm": NGC3603LambdaTerm,
    "NGC3603UQFFTerm": NGC3603UQFFTerm,
    "NGC3603EMTerm": NGC3603EMTerm,
    "NGC3603QuantumTerm": NGC3603QuantumTerm,
    "NGC3603FluidTerm": NGC3603FluidTerm,
    "NGC3603OscillatoryTerm": NGC3603OscillatoryTerm,
    "NGC3603DarkMatterTerm": NGC3603DarkMatterTerm,
    "NGC3603WindTerm": NGC3603WindTerm,
    "NGC3603CavityPressureTerm": NGC3603CavityPressureTerm,
    "BubbleNebulaCoreTerm": BubbleNebulaCoreTerm,
    "BubbleNebulaLambdaTerm": BubbleNebulaLambdaTerm,
    "BubbleNebulaUQFFTerm": BubbleNebulaUQFFTerm,
    "BubbleNebulaEMTerm": BubbleNebulaEMTerm,
    "BubbleNebulaQuantumTerm": BubbleNebulaQuantumTerm,
    "BubbleNebulaFluidTerm": BubbleNebulaFluidTerm,
    "BubbleNebulaOscillatoryTerm": BubbleNebulaOscillatoryTerm,
    "BubbleNebulaDarkMatterTerm": BubbleNebulaDarkMatterTerm,
    "BubbleNebulaWindTerm": BubbleNebulaWindTerm,
    "BubbleNebulaExpansionTerm": BubbleNebulaExpansionTerm,
    "AntennaeGalaxiesCoreTerm": AntennaeGalaxiesCoreTerm,
    "AntennaeGalaxiesLambdaTerm": AntennaeGalaxiesLambdaTerm,
    "AntennaeGalaxiesUQFFTerm": AntennaeGalaxiesUQFFTerm,
    "AntennaeGalaxiesEMTerm": AntennaeGalaxiesEMTerm,
    "AntennaeGalaxiesQuantumTerm": AntennaeGalaxiesQuantumTerm,
    "AntennaeGalaxiesFluidTerm": AntennaeGalaxiesFluidTerm,
    "AntennaeGalaxiesOscillatoryTerm": AntennaeGalaxiesOscillatoryTerm,
    "AntennaeGalaxiesDarkMatterTerm": AntennaeGalaxiesDarkMatterTerm,
    "AntennaeGalaxiesWindTerm": AntennaeGalaxiesWindTerm,
    "AntennaeGalaxiesMergerInteractionTerm": AntennaeGalaxiesMergerInteractionTerm,
    "HorseheadNebulaCoreTerm": HorseheadNebulaCoreTerm,
    "HorseheadNebulaLambdaTerm": HorseheadNebulaLambdaTerm,
    "HorseheadNebulaUQFFTerm": HorseheadNebulaUQFFTerm,
    "HorseheadNebulaEMTerm": HorseheadNebulaEMTerm,
    "HorseheadNebulaQuantumTerm": HorseheadNebulaQuantumTerm,
    "HorseheadNebulaFluidTerm": HorseheadNebulaFluidTerm,
    "HorseheadNebulaOscillatoryTerm": HorseheadNebulaOscillatoryTerm,
    "HorseheadNebulaDarkMatterTerm": HorseheadNebulaDarkMatterTerm,
    "HorseheadNebulaWindTerm": HorseheadNebulaWindTerm,
    "HorseheadNebulaErosionTerm": HorseheadNebulaErosionTerm,
    "NGC1275CoreTerm": NGC1275CoreTerm,
    "NGC1275BlackHoleTerm": NGC1275BlackHoleTerm,
    "NGC1275LambdaTerm": NGC1275LambdaTerm,
    "NGC1275UQFFTerm": NGC1275UQFFTerm,
    "NGC1275EMTerm": NGC1275EMTerm,
    "NGC1275QuantumTerm": NGC1275QuantumTerm,
    "NGC1275FluidTerm": NGC1275FluidTerm,
    "NGC1275OscillatoryTerm": NGC1275OscillatoryTerm,
    "NGC1275DarkMatterTerm": NGC1275DarkMatterTerm,
    "NGC1275CoolingFlowTerm": NGC1275CoolingFlowTerm,
    "NGC1275FilamentSupportTerm": NGC1275FilamentSupportTerm,
    "HUDFGalaxiesCoreTerm": HUDFGalaxiesCoreTerm,
    "HUDFGalaxiesLambdaTerm": HUDFGalaxiesLambdaTerm,
    "HUDFGalaxiesUQFFTerm": HUDFGalaxiesUQFFTerm,
    "HUDFGalaxiesEMTerm": HUDFGalaxiesEMTerm,
    "HUDFGalaxiesQuantumTerm": HUDFGalaxiesQuantumTerm,
    "HUDFGalaxiesFluidTerm": HUDFGalaxiesFluidTerm,
    "HUDFGalaxiesOscillatoryTerm": HUDFGalaxiesOscillatoryTerm,
    "HUDFGalaxiesDarkMatterTerm": HUDFGalaxiesDarkMatterTerm,
    "HUDFGalaxiesWindTerm": HUDFGalaxiesWindTerm,
    "HUDFGalaxiesInteractionTerm": HUDFGalaxiesInteractionTerm,
    "NGC1792CoreTerm": NGC1792CoreTerm,
    "NGC1792LambdaTerm": NGC1792LambdaTerm,
    "NGC1792UQFFTerm": NGC1792UQFFTerm,
    "NGC1792EMTerm": NGC1792EMTerm,
    "NGC1792QuantumTerm": NGC1792QuantumTerm,
    "NGC1792FluidTerm": NGC1792FluidTerm,
    "NGC1792OscillatoryTerm": NGC1792OscillatoryTerm,
    "NGC1792DarkMatterTerm": NGC1792DarkMatterTerm,
    "NGC1792SupernovaFeedbackTerm": NGC1792SupernovaFeedbackTerm,
    "AndromedaCoreTerm": AndromedaCoreTerm,
    "AndromedaBlackHoleTerm": AndromedaBlackHoleTerm,
    "AndromedaLambdaTerm": AndromedaLambdaTerm,
    "AndromedaUQFFTerm": AndromedaUQFFTerm,
    "AndromedaEMTerm": AndromedaEMTerm,
    "AndromedaQuantumTerm": AndromedaQuantumTerm,
    "AndromedaFluidTerm": AndromedaFluidTerm,
    "AndromedaOscillatoryTerm": AndromedaOscillatoryTerm,
    "AndromedaDarkMatterTerm": AndromedaDarkMatterTerm,
    "SombreroCoreTerm": SombreroCoreTerm,
    "SombreroBlackHoleTerm": SombreroBlackHoleTerm,
    "SombreroLambdaTerm": SombreroLambdaTerm,
    "SombreroUQFFTerm": SombreroUQFFTerm,
    "SombreroEMTerm": SombreroEMTerm,
    "SombreroQuantumTerm": SombreroQuantumTerm,
    "SombreroFluidTerm": SombreroFluidTerm,
    "SombreroOscillatoryTerm": SombreroOscillatoryTerm,
    "SombreroDarkMatterTerm": SombreroDarkMatterTerm,
    "SombreroDustTerm": SombreroDustTerm,
    "SaturnCoreTerm": SaturnCoreTerm,
    "SaturnLambdaTerm": SaturnLambdaTerm,
    "SaturnUQFFTerm": SaturnUQFFTerm,
    "SaturnEMTerm": SaturnEMTerm,
    "SaturnQuantumTerm": SaturnQuantumTerm,
    "SaturnFluidTerm": SaturnFluidTerm,
    "SaturnOscillatoryTerm": SaturnOscillatoryTerm,
    "SaturnDarkMatterTerm": SaturnDarkMatterTerm,
    "SaturnRingTidalTerm": SaturnRingTidalTerm,
    "SaturnWindTerm": SaturnWindTerm,
    "M16CoreTerm": M16CoreTerm,
    "M16LambdaTerm": M16LambdaTerm,
    "M16UQFFTerm": M16UQFFTerm,
    "M16EMTerm": M16EMTerm,
    "M16QuantumTerm": M16QuantumTerm,
    "M16FluidTerm": M16FluidTerm,
    "M16OscillatoryTerm": M16OscillatoryTerm,
    "M16DarkMatterTerm": M16DarkMatterTerm,
    "M16StarFormationTerm": M16StarFormationTerm,
    "M16RadiationErosionTerm": M16RadiationErosionTerm,
    "CrabCoreTerm": CrabCoreTerm,
    "CrabLambdaTerm": CrabLambdaTerm,
    "CrabUQFFTerm": CrabUQFFTerm,
    "CrabEMTerm": CrabEMTerm,
    "CrabQuantumTerm": CrabQuantumTerm,
    "CrabFluidTerm": CrabFluidTerm,
    "CrabOscillatoryTerm": CrabOscillatoryTerm,
    "CrabDarkMatterTerm": CrabDarkMatterTerm,
    "CrabPulsarWindTerm": CrabPulsarWindTerm,
    "CrabMagneticTerm": CrabMagneticTerm,
    "SGR1745CoreTerm": SGR1745CoreTerm,
    "SGR1745LambdaTerm": SGR1745LambdaTerm,
    "SGR1745UQFFTerm": SGR1745UQFFTerm,
    "SGR1745EMTerm": SGR1745EMTerm,
    "SGR1745QuantumTerm": SGR1745QuantumTerm,
    "SGR1745FluidTerm": SGR1745FluidTerm,
    "SGR1745OscillatoryTerm": SGR1745OscillatoryTerm,
    "SGR1745DarkMatterTerm": SGR1745DarkMatterTerm,
    "SGR1745_DPMTerm": SGR1745_DPMTerm,
    "SGR1745_THzTerm": SGR1745_THzTerm,
    "SGR1745_VacDiffTerm": SGR1745_VacDiffTerm,
    "SGR1745_SuperFreqTerm": SGR1745_SuperFreqTerm,
    "SGR1745_AetherResTerm": SGR1745_AetherResTerm,
    "SGR1745_Ug4iTerm": SGR1745_Ug4iTerm,
    "SGR1745_QuantumFreqTerm": SGR1745_QuantumFreqTerm,
    "SGR1745_AetherFreqTerm": SGR1745_AetherFreqTerm,
    "SGR1745_FluidFreqTerm": SGR1745_FluidFreqTerm,
    "SGR1745_ExpFreqTerm": SGR1745_ExpFreqTerm,
    "SgrA_DPMTerm": SgrA_DPMTerm,
    "SgrA_THzTerm": SgrA_THzTerm,
    "SgrA_VacDiffTerm": SgrA_VacDiffTerm,
    "SgrA_SuperFreqTerm": SgrA_SuperFreqTerm,
    "SgrA_AetherResTerm": SgrA_AetherResTerm,
    "SgrA_Ug4iTerm": SgrA_Ug4iTerm,
    "SgrA_QuantumFreqTerm": SgrA_QuantumFreqTerm,
    "SgrA_AetherFreqTerm": SgrA_AetherFreqTerm,
    "SgrA_FluidFreqTerm": SgrA_FluidFreqTerm,
    "SgrA_ExpFreqTerm": SgrA_ExpFreqTerm,
    "YoungStarsCore": YoungStarsCore,
    "YoungStarsLambda": YoungStarsLambda,
    "YoungStarsUQFF": YoungStarsUQFF,
    "YoungStarsEM": YoungStarsEM,
    "YoungStarsQuantum": YoungStarsQuantum,
    "YoungStarsFluid": YoungStarsFluid,
    "YoungStarsOscillatory": YoungStarsOscillatory,
    "YoungStarsDarkMatter": YoungStarsDarkMatter,
    "YoungStarsOutflowPressure": YoungStarsOutflowPressure,
    "YoungStarsStarFormation": YoungStarsStarFormation,
    "BigBangCore": BigBangCore,
    "BigBangLambda": BigBangLambda,
    "BigBangUg1": BigBangUg1,
    "BigBangUg4": BigBangUg4,
    "BigBangQuantum": BigBangQuantum,
    "BigBangFluid": BigBangFluid,
    "BigBangOscillatory": BigBangOscillatory,
    "BigBangDarkMatter": BigBangDarkMatter,
    "BigBangQuantumGravity": BigBangQuantumGravity,
    "BigBangGravitationalWave": BigBangGravitationalWave,
    "BigBangCosmicEvolution": BigBangCosmicEvolution,
    "M51Core": M51Core,
    "M51Ug1Dipole": M51Ug1Dipole,
    "M51Ug2Superconductor": M51Ug2Superconductor,
    "M51Ug3Tidal": M51Ug3Tidal,
    "M51Ug4Reaction": M51Ug4Reaction,
    "M51UiVacuum": M51UiVacuum,
    "M51Lambda": M51Lambda,
    "M51Quantum": M51Quantum,
    "M51Fluid": M51Fluid,
    "M51DarkMatter": M51DarkMatter,
    "M51EnvironmentalForces": M51EnvironmentalForces,
    "NGC1316Core": NGC1316Core,
    "NGC1316Ug1Dipole": NGC1316Ug1Dipole,
    "NGC1316Ug2Superconductor": NGC1316Ug2Superconductor,
    "NGC1316Ug3External": NGC1316Ug3External,
    "NGC1316Ug4Reaction": NGC1316Ug4Reaction,
    "NGC1316UiVacuum": NGC1316UiVacuum,
    "NGC1316Lambda": NGC1316Lambda,
    "NGC1316Quantum": NGC1316Quantum,
    "NGC1316FluidDust": NGC1316FluidDust,
    "NGC1316DarkMatter": NGC1316DarkMatter,
    "NGC1316MergerForces": NGC1316MergerForces,
    "SMBHBinaryDPMResonance": SMBHBinaryDPMResonance,
    "SMBHBinaryTHzResonance": SMBHBinaryTHzResonance,
    "SMBHBinaryUg4iResonance": SMBHBinaryUg4iResonance,
    "SMBHBinaryPlasmoticVacuum": SMBHBinaryPlasmoticVacuum,
    "SMBHBinaryQuantumResonance": SMBHBinaryQuantumResonance,
    "SMBHBinaryFluidResonance": SMBHBinaryFluidResonance,
    "SMBHBinaryOscillatoryResonance": SMBHBinaryOscillatoryResonance,
    "SMBHBinaryExpansionResonance": SMBHBinaryExpansionResonance,
    "SMBHBinaryCoalescence": SMBHBinaryCoalescence,
    "BackgroundAetherMinkowski": BackgroundAetherMinkowski,
    "BackgroundAetherPerturbedMetric": BackgroundAetherPerturbedMetric,
    "BackgroundAetherStressEnergy": BackgroundAetherStressEnergy,
    "BackgroundAetherCoupling": BackgroundAetherCoupling,
    "HeavisideFraction": HeavisideFraction,
    "HeavisideFactor": HeavisideFactor,
    "HeavisideUmBase": HeavisideUmBase,
    "HeavisideUmWithHeaviside": HeavisideUmWithHeaviside,
    "HeavisideUmWithoutHeaviside": HeavisideUmWithoutHeaviside,
    "HeavisideQuasiFactor": HeavisideQuasiFactor,
    "HeavisideExponentialDecay": HeavisideExponentialDecay,
    "HeavisideMagneticMoment": HeavisideMagneticMoment,
    "HeavisideAmplificationRatio": HeavisideAmplificationRatio,
    "HydrogenResonance_Amplitude": HydrogenResonance_Amplitude,
    "HydrogenResonance_Frequency": HydrogenResonance_Frequency,
    "HydrogenResonance_DeepPairing": HydrogenResonance_DeepPairing,
    "HydrogenResonance_NuclearCoupling": HydrogenResonance_NuclearCoupling,
    "HydrogenResonance_ShellCorrection": HydrogenResonance_ShellCorrection,
    "HydrogenResonance_Buoyancy": HydrogenResonance_Buoyancy,
    "HydrogenResonance_Superconductive": HydrogenResonance_Superconductive,
    "HydrogenResonance_PairingEnergy": HydrogenResonance_PairingEnergy,
    "HydrogenResonance_MagicStability": HydrogenResonance_MagicStability,
    "HydrogenResonance_TunnelingProb": HydrogenResonance_TunnelingProb,
    "HydrogenResonance_Integrand": HydrogenResonance_Integrand,
    "HydrogenResonance_Full": HydrogenResonance_Full,
    "SurfaceMagnetic_Oscillation": SurfaceMagnetic_Oscillation,
    "SurfaceMagnetic_MinField": SurfaceMagnetic_MinField,
    "SurfaceMagnetic_MaxField": SurfaceMagnetic_MaxField,
    "SurfaceMagnetic_CycleReference": SurfaceMagnetic_CycleReference,
    "Source10_F_U_Bi_iTerm": Source10_F_U_Bi_iTerm,
    "Source10_g_UQFFTerm": Source10_g_UQFFTerm,
    "Source10_DPM_ResonanceTerm": Source10_DPM_ResonanceTerm,
    "Source10_E_cmTerm": Source10_E_cmTerm,
    "Source11_Ug1Term": Source11_Ug1Term,
    "Source11_Ug2Term": Source11_Ug2Term,
    "Source11_Ug3Term": Source11_Ug3Term,
    "Source11_Ug4Term": Source11_Ug4Term,
    "Source11_UmTerm": Source11_UmTerm,
    "UQFFModule5_DarkMatterHaloTerm": UQFFModule5_DarkMatterHaloTerm,
    "UQFFModule5_VacuumEnergyTerm": UQFFModule5_VacuumEnergyTerm,
    "Source5_CompressedBaseTerm": Source5_CompressedBaseTerm,
    "Source5_CompressedExpansionTerm": Source5_CompressedExpansionTerm,
    "Source5_CompressedQuantumTerm": Source5_CompressedQuantumTerm,
    "Source5_CompressedCosmTerm": Source5_CompressedCosmTerm,
    "Source5_CompressedFluidTerm": Source5_CompressedFluidTerm,
    "Source5_CompressedPerturbationTerm": Source5_CompressedPerturbationTerm,
    "Source5_ResonanceMUGETerm": Source5_ResonanceMUGETerm,
    "Source5_CompressedSuperAdjTerm": Source5_CompressedSuperAdjTerm,
    "Source5_CompressedEnvTerm": Source5_CompressedEnvTerm,
    "Source5_CompressedUgSumTerm": Source5_CompressedUgSumTerm,
    "ESO137_BuoyancyTerm": ESO137_BuoyancyTerm,
    "ESO137_CompressedTerm": ESO137_CompressedTerm,
    "ESO137_ResonantTerm": ESO137_ResonantTerm,
    "ESO137_SuperconductiveTerm": ESO137_SuperconductiveTerm,
    "ESO137_LENRTerm": ESO137_LENRTerm,
    "ESO137_CompressedGTerm": ESO137_CompressedGTerm,
    "ESO137_Q_waveTerm": ESO137_Q_waveTerm,
    "ESO137_Ub1Term": ESO137_Ub1Term,
    "ESO137_UiTerm": ESO137_UiTerm,
    "NGC1365_BuoyancyTerm": NGC1365_BuoyancyTerm,
    "NGC1365_CompressedTerm": NGC1365_CompressedTerm,
    "NGC1365_LENRTerm": NGC1365_LENRTerm,
    "Vela_BuoyancyTerm": Vela_BuoyancyTerm,
    "Vela_CompressedTerm": Vela_CompressedTerm,
    "Vela_LENRTerm": Vela_LENRTerm,
    "ASASSN14li_BuoyancyTerm": ASASSN14li_BuoyancyTerm,
    "ASASSN14li_CompressedTerm": ASASSN14li_CompressedTerm,
    "ASASSN14li_LENRTerm": ASASSN14li_LENRTerm,
    "ElGordo_BuoyancyTerm": ElGordo_BuoyancyTerm,
    "ElGordo_CompressedTerm": ElGordo_CompressedTerm,
    "ElGordo_LENRTerm": ElGordo_LENRTerm,
    "Heaviside_FactorTerm": Heaviside_FactorTerm,
    "Heaviside_UmBaseTerm": Heaviside_UmBaseTerm,
    "Heliosphere_H_SCmTerm": Heliosphere_H_SCmTerm,
    "UgIndex_U_giTerm": UgIndex_U_giTerm,
    "UgIndex_K_iTerm": UgIndex_K_iTerm,
    "Inertia_Lambda_iTerm": Inertia_Lambda_iTerm,
    "Inertia_U_iTerm": Inertia_U_iTerm,
    "MagMoment_Mu_jTerm": MagMoment_Mu_jTerm,
    "MagMoment_B_jTerm": MagMoment_B_jTerm,
    "BlackHole_M_bhTerm": BlackHole_M_bhTerm,
    "BlackHole_U_b1Term": BlackHole_U_b1Term,
    "NegativeTime_CosPiTnTerm": NegativeTime_CosPiTnTerm,
    "NegativeTime_ExpTermTerm": NegativeTime_ExpTermTerm,
    "PiConstant_MuJExampleTerm": PiConstant_MuJExampleTerm,
    "CorePenetration_P_coreTerm": CorePenetration_P_coreTerm,
    "QuasiLong_FactorTerm": QuasiLong_FactorTerm,
    "OuterBubble_S_r_RbTerm": OuterBubble_S_r_RbTerm,
    "OuterBubble_U_g2Term": OuterBubble_U_g2Term,
    "OuterBubble_R_bTerm": OuterBubble_R_bTerm,
    "OuterBubble_R_bInAUTerm": OuterBubble_R_bInAUTerm,
    "Reciprocation_Gamma_sTerm": Reciprocation_Gamma_sTerm,
    "Reciprocation_CosPiTnTerm": Reciprocation_CosPiTnTerm,
    "Reciprocation_ExpTermTerm": Reciprocation_ExpTermTerm,
    "Reciprocation_OneMinusExpTerm": Reciprocation_OneMinusExpTerm,
    "Reciprocation_Gamma_dayTerm": Reciprocation_Gamma_dayTerm,
    "Reciprocation_UmExampleTerm": Reciprocation_UmExampleTerm,
    "ScmPenetration_UmBaseTerm": ScmPenetration_UmBaseTerm,
    "ScmPenetration_UmContributionTerm": ScmPenetration_UmContributionTerm,
    "ScmPenetration_P_SCmTerm": ScmPenetration_P_SCmTerm,
    "ScmPenetration_UmPlanetTerm": ScmPenetration_UmPlanetTerm,
    "ScmReactivity_Kappa_sTerm": ScmReactivity_Kappa_sTerm,
    "ScmReactivity_E_reactTerm": ScmReactivity_E_reactTerm,
    "ScmReactivity_UmExampleTerm": ScmReactivity_UmExampleTerm,
    "ScmReactivity_Kappa_dayTerm": ScmReactivity_Kappa_dayTerm,
    "SolarCycle_Omega_cTerm": SolarCycle_Omega_cTerm,
    "SolarCycle_SinOmegaCTTerm": SolarCycle_SinOmegaCTTerm,
    "SolarCycle_MuJExampleTerm": SolarCycle_MuJExampleTerm,
    "SolarCycle_PeriodYearsTerm": SolarCycle_PeriodYearsTerm,
    "SolarCycle_B_j_ModulatedTerm": SolarCycle_B_j_ModulatedTerm,
    "SolarWindMod_ModulationFactorTerm": SolarWindMod_ModulationFactorTerm,
    "SolarWindMod_U_g2Term": SolarWindMod_U_g2Term,
    "SolarWindMod_Delta_swTerm": SolarWindMod_Delta_swTerm,
    "SolarWindMod_U_g2_no_modTerm": SolarWindMod_U_g2_no_modTerm,
    "SolarWindVel_ModulationFactorTerm": SolarWindVel_ModulationFactorTerm,
    "SolarWindVel_U_g2Term": SolarWindVel_U_g2Term,
    "SolarWindVel_V_swTerm": SolarWindVel_V_swTerm,
    "SolarWindVel_V_swKmSTerm": SolarWindVel_V_swKmSTerm,
    "SolarWindVel_U_g2_no_swTerm": SolarWindVel_U_g2_no_swTerm,
    "StellarMass_M_sOverR2Term": StellarMass_M_sOverR2Term,
    "StellarMass_U_g1Term": StellarMass_U_g1Term,
    "StellarMass_U_g2Term": StellarMass_U_g2Term,
    "StellarMass_M_sTerm": StellarMass_M_sTerm,
    "StellarMass_M_sInMsunTerm": StellarMass_M_sInMsunTerm,
    "StellarRot_Omega_s_tTerm": StellarRot_Omega_s_tTerm,
    "StellarRot_U_g3Term": StellarRot_U_g3Term,
    "StellarRot_U_iTerm": StellarRot_U_iTerm,
    "StellarRot_Omega_sTerm": StellarRot_Omega_sTerm,
    "StellarRot_Period_daysTerm": StellarRot_Period_daysTerm,
    "StepFunc_S_r_RbTerm": StepFunc_S_r_RbTerm,
    "StepFunc_U_g2Term": StepFunc_U_g2Term,
    "StressEnergy_T_sTerm": StressEnergy_T_sTerm,
    "StressEnergy_A_mu_nuTerm": StressEnergy_A_mu_nuTerm,
    "StressEnergy_PerturbationTerm": StressEnergy_PerturbationTerm,
    "SurfaceMagField_B_jTerm": SurfaceMagField_B_jTerm,
    "SurfaceMagField_U_g3_exampleTerm": SurfaceMagField_U_g3_exampleTerm,
    "SurfaceMagField_B_s_minTerm": SurfaceMagField_B_s_minTerm,
    "SurfaceMagField_B_s_maxTerm": SurfaceMagField_B_s_maxTerm,
    "SurfaceTemp_B_j_hypotheticalTerm": SurfaceTemp_B_j_hypotheticalTerm,
    "SurfaceTemp_U_g3_exampleTerm": SurfaceTemp_U_g3_exampleTerm,
    "SurfaceTemp_T_sTerm": SurfaceTemp_T_sTerm,
    "TimeReversalZone_TRZFactorTerm": TimeReversalZone_TRZFactorTerm,
    "TimeReversalZone_U_i_baseTerm": TimeReversalZone_U_i_baseTerm,
    "TimeReversalZone_U_iTerm": TimeReversalZone_U_iTerm,
    "TimeReversalZone_F_TRZTerm": TimeReversalZone_F_TRZTerm,
    "TimeReversalZone_U_i_no_TRZTerm": TimeReversalZone_U_i_no_TRZTerm,
    "Ug1Defect_Delta_defTerm": Ug1Defect_Delta_defTerm,
    "Ug1Defect_U_g1Term": Ug1Defect_U_g1Term,
    "Ug1Defect_Period_yearsTerm": Ug1Defect_Period_yearsTerm,
    "Ug3DiskVec_PhiHat_jTerm": Ug3DiskVec_PhiHat_jTerm,
    "Ug3DiskVec_UmBaseTerm": Ug3DiskVec_UmBaseTerm,
    "Ug3DiskVec_UmContributionTerm": Ug3DiskVec_UmContributionTerm,
    "Ug3DiskVec_PhiHatMagnitudeTerm": Ug3DiskVec_PhiHatMagnitudeTerm,
    "AetherVacDensity_Rho_vac_ATerm": AetherVacDensity_Rho_vac_ATerm,
    "AetherVacDensity_T_s_contributionTerm": AetherVacDensity_T_s_contributionTerm,
    "AetherVacDensity_A_mu_nuTerm": AetherVacDensity_A_mu_nuTerm,
    "AetherVacDensity_Epsilon_factorTerm": AetherVacDensity_Epsilon_factorTerm,
    "UnivInertiaVac_Rho_vac_UiTerm": UnivInertiaVac_Rho_vac_UiTerm,
    "UnivInertiaVac_U_i_baseTerm": UnivInertiaVac_U_i_baseTerm,
    "UnivInertiaVac_U_iTerm": UnivInertiaVac_U_iTerm,
    "ScmVacDensity_Rho_vac_SCmTerm": ScmVacDensity_Rho_vac_SCmTerm,
    "ScmVacDensity_U_g2_exampleTerm": ScmVacDensity_U_g2_exampleTerm,
    "ScmVacDensity_U_i_exampleTerm": ScmVacDensity_U_i_exampleTerm,
    "UaVacDensity_Rho_vac_UATerm": UaVacDensity_Rho_vac_UATerm,
    "UaVacDensity_U_g2_exampleTerm": UaVacDensity_U_g2_exampleTerm,
    "UaVacDensity_U_i_exampleTerm": UaVacDensity_U_i_exampleTerm,
    "UnivInertiaVac2_U_i_baseTerm": UnivInertiaVac2_U_i_baseTerm,
    "UnivInertiaVac2_U_iTerm": UnivInertiaVac2_U_iTerm,
    "ScmVelocity_V_scmTerm": ScmVelocity_V_scmTerm,
    "ScmVelocity_E_react_baseTerm": ScmVelocity_E_react_baseTerm,
    "ScmVelocity_E_reactTerm": ScmVelocity_E_reactTerm,
    "ScmVelocity_UmExampleTerm": ScmVelocity_UmExampleTerm,
    "ButterflyNebula_DPM_momentumTerm": ButterflyNebula_DPM_momentumTerm,
    "ButterflyNebula_DPM_gravityTerm": ButterflyNebula_DPM_gravityTerm,
    "ButterflyNebula_LENR_termTerm": ButterflyNebula_LENR_termTerm,
    "ButterflyNebula_F_U_BiTerm": ButterflyNebula_F_U_BiTerm,
    "CentaurusA_DPM_momentumTerm": CentaurusA_DPM_momentumTerm,
    "CentaurusA_F_U_BiTerm": CentaurusA_F_U_BiTerm,
    "Abell2256_IntegrandTerm": Abell2256_IntegrandTerm,
    "Abell2256_DPM_resonanceTerm": Abell2256_DPM_resonanceTerm,
    "Abell2256_X2Term": Abell2256_X2Term,
    "Abell2256_LENR_termTerm": Abell2256_LENR_termTerm,
    "Abell2256_Q_waveTerm": Abell2256_Q_waveTerm,
    "Abell2256_Ub1Term": Abell2256_Ub1Term,
    "Abell2256_UiTerm": Abell2256_UiTerm,
    "Abell2256_FTerm": Abell2256_FTerm,
    "Abell2256_CompressedTerm": Abell2256_CompressedTerm,
    "Abell2256_ResonantTerm": Abell2256_ResonantTerm,
    "Abell2256_BuoyancyTerm": Abell2256_BuoyancyTerm,
    "Abell2256_SuperconductiveTerm": Abell2256_SuperconductiveTerm,
    "Abell2256_CompressedGTerm": Abell2256_CompressedGTerm,
    "ASASSN14li_IntegrandTerm": ASASSN14li_IntegrandTerm,
    "ASASSN14li_DPM_resonanceTerm": ASASSN14li_DPM_resonanceTerm,
    "ASASSN14li_X2Term": ASASSN14li_X2Term,
    "ASASSN14li_LENR_termTerm": ASASSN14li_LENR_termTerm,
    "ASASSN14li_Q_waveTerm": ASASSN14li_Q_waveTerm,
    "ASASSN14li_FTerm": ASASSN14li_FTerm,
    "CentaurusA_X2Term": CentaurusA_X2Term,
    "CentaurusA_LENR_termTerm": CentaurusA_LENR_termTerm,
    "CentaurusA_FTerm": CentaurusA_FTerm,
    "CentaurusA_CompressedTerm": CentaurusA_CompressedTerm,
    "CrabNebula_IntegrandTerm": CrabNebula_IntegrandTerm,
    "CrabNebula_LENR_termTerm": CrabNebula_LENR_termTerm,
    "CrabNebula_FTerm": CrabNebula_FTerm,
    "CrabNebula_CompressedTerm": CrabNebula_CompressedTerm,
    "ElGordo_IntegrandTerm": ElGordo_IntegrandTerm,
    "ElGordo_LENR_termTerm": ElGordo_LENR_termTerm,
    "ElGordo_FTerm": ElGordo_FTerm,
    "ESO137_FTerm": ESO137_FTerm,
    "IC2163_IntegrandTerm": IC2163_IntegrandTerm,
    "IC2163_LENR_termTerm": IC2163_LENR_termTerm,
    "IC2163_FTerm": IC2163_FTerm,
    "IC2163_CompressedTerm": IC2163_CompressedTerm,
    "J1610_IntegrandTerm": J1610_IntegrandTerm,
    "J1610_LENR_termTerm": J1610_LENR_termTerm,
    "J1610_FTerm": J1610_FTerm,
    "J1610_CompressedTerm": J1610_CompressedTerm,
    "JupiterAurorae_IntegrandTerm": JupiterAurorae_IntegrandTerm,
    "JupiterAurorae_LENR_termTerm": JupiterAurorae_LENR_termTerm,
    "JupiterAurorae_FTerm": JupiterAurorae_FTerm,
    "JupiterAurorae_CompressedTerm": JupiterAurorae_CompressedTerm,
    "LagoonNebula_IntegrandTerm": LagoonNebula_IntegrandTerm,
    "LagoonNebula_LENR_termTerm": LagoonNebula_LENR_termTerm,
    "LagoonNebula_FTerm": LagoonNebula_FTerm,
    "LagoonNebula_CompressedTerm": LagoonNebula_CompressedTerm,
    "LagoonNebulaVar_IntegrandTerm": LagoonNebulaVar_IntegrandTerm,
    "LagoonNebulaVar_LENR_termTerm": LagoonNebulaVar_LENR_termTerm,
    "LagoonNebulaVar_FTerm": LagoonNebulaVar_FTerm,
    "LagoonNebulaVar_CompressedTerm": LagoonNebulaVar_CompressedTerm,
    "M87Jet_IntegrandTerm": M87Jet_IntegrandTerm,
    "M87Jet_LENR_termTerm": M87Jet_LENR_termTerm,
    "M87Jet_FTerm": M87Jet_FTerm,
    "M87Jet_CompressedTerm": M87Jet_CompressedTerm,
    "NGC1365_IntegrandTerm": NGC1365_IntegrandTerm,
    "NGC1365_LENR_termTerm": NGC1365_LENR_termTerm,
    "NGC1365_FTerm": NGC1365_FTerm,
    "NGC2207_IntegrandTerm": NGC2207_IntegrandTerm,
    "NGC2207_LENR_termTerm": NGC2207_LENR_termTerm,
    "NGC2207_FTerm": NGC2207_FTerm,
    "NGC2207_CompressedTerm": NGC2207_CompressedTerm,
    "RAquarii_IntegrandTerm": RAquarii_IntegrandTerm,
    "RAquarii_LENR_termTerm": RAquarii_LENR_termTerm,
    "RAquarii_FTerm": RAquarii_FTerm,
    "RAquarii_CompressedTerm": RAquarii_CompressedTerm,
    "SgrAStar_IntegrandTerm": SgrAStar_IntegrandTerm,
    "SgrAStar_LENR_termTerm": SgrAStar_LENR_termTerm,
    "SgrAStar_FTerm": SgrAStar_FTerm,
    "SgrAStar_CompressedTerm": SgrAStar_CompressedTerm,
    "SPTCLJ2215_IntegrandTerm": SPTCLJ2215_IntegrandTerm,
    "SPTCLJ2215_LENR_termTerm": SPTCLJ2215_LENR_termTerm,
    "SPTCLJ2215_FTerm": SPTCLJ2215_FTerm,
    "SPTCLJ2215_CompressedTerm": SPTCLJ2215_CompressedTerm,
    "StephanQuintet_IntegrandTerm": StephanQuintet_IntegrandTerm,
    "StephanQuintet_LENR_termTerm": StephanQuintet_LENR_termTerm,
    "StephanQuintet_FTerm": StephanQuintet_FTerm,
    "StephanQuintet_CompressedTerm": StephanQuintet_CompressedTerm,
    "VelaPulsar_IntegrandTerm": VelaPulsar_IntegrandTerm,
    "VelaPulsar_LENR_termTerm": VelaPulsar_LENR_termTerm,
    "VelaPulsar_FTerm": VelaPulsar_FTerm,
    "VelaPulsar_CompressedTerm": VelaPulsar_CompressedTerm,
    "NGC4993_IntegrandTerm": NGC4993_IntegrandTerm,
    "NGC4993_LENR_termTerm": NGC4993_LENR_termTerm,
    "NGC4993_FTerm": NGC4993_FTerm,
    "NGC4993_CompressedTerm": NGC4993_CompressedTerm,
    "NGC6240_IntegrandTerm": NGC6240_IntegrandTerm,
    "NGC6240_LENR_termTerm": NGC6240_LENR_termTerm,
    "NGC6240_FTerm": NGC6240_FTerm,
    "NGC6240_CompressedTerm": NGC6240_CompressedTerm,
    "OmegaCentauri_IntegrandTerm": OmegaCentauri_IntegrandTerm,
    "OmegaCentauri_LENR_termTerm": OmegaCentauri_LENR_termTerm,
    "OmegaCentauri_FTerm": OmegaCentauri_FTerm,
    "OmegaCentauri_CompressedTerm": OmegaCentauri_CompressedTerm,
    "OrionNebula_IntegrandTerm": OrionNebula_IntegrandTerm,
    "OrionNebula_LENR_termTerm": OrionNebula_LENR_termTerm,
    "OrionNebula_FTerm": OrionNebula_FTerm,
    "OrionNebula_CompressedTerm": OrionNebula_CompressedTerm,
    "Perseus_IntegrandTerm": Perseus_IntegrandTerm,
    "Perseus_LENR_termTerm": Perseus_LENR_termTerm,
    "Perseus_FTerm": Perseus_FTerm,
    "Perseus_CompressedTerm": Perseus_CompressedTerm,
    "PolarRing_IntegrandTerm": PolarRing_IntegrandTerm,
    "PolarRing_LENR_termTerm": PolarRing_LENR_termTerm,
    "PolarRing_FTerm": PolarRing_FTerm,
    "PolarRing_CompressedTerm": PolarRing_CompressedTerm,
    "QuasarJ0313_IntegrandTerm": QuasarJ0313_IntegrandTerm,
    "QuasarJ0313_LENR_termTerm": QuasarJ0313_LENR_termTerm,
    "QuasarJ0313_FTerm": QuasarJ0313_FTerm,
    "QuasarJ0313_CompressedTerm": QuasarJ0313_CompressedTerm,
    "RXJ1713_IntegrandTerm": RXJ1713_IntegrandTerm,
    "RXJ1713_LENR_termTerm": RXJ1713_LENR_termTerm,
    "RXJ1713_FTerm": RXJ1713_FTerm,
    "RXJ1713_CompressedTerm": RXJ1713_CompressedTerm,
    "BuoyancyAstro_IntegrandTerm": BuoyancyAstro_IntegrandTerm,
    "BuoyancyAstro_LENR_termTerm": BuoyancyAstro_LENR_termTerm,
    "BuoyancyAstro_FTerm": BuoyancyAstro_FTerm,
    "BuoyancyAstro_CompressedTerm": BuoyancyAstro_CompressedTerm,
    "BuoyancyCNB_IntegrandTerm": BuoyancyCNB_IntegrandTerm,
    "BuoyancyCNB_LENR_termTerm": BuoyancyCNB_LENR_termTerm,
    "BuoyancyCNB_FTerm": BuoyancyCNB_FTerm,
    "BuoyancyCNB_CompressedTerm": BuoyancyCNB_CompressedTerm,
    "AstroSystems163_IntegrandTerm": AstroSystems163_IntegrandTerm,
    "AstroSystems163_LENR_termTerm": AstroSystems163_LENR_termTerm,
    "AstroSystems163_FTerm": AstroSystems163_FTerm,
    "AstroSystems163_CompressedTerm": AstroSystems163_CompressedTerm,
    "NebulaTriadic_IntegrandTerm": NebulaTriadic_IntegrandTerm,
    "NebulaTriadic_LENR_termTerm": NebulaTriadic_LENR_termTerm,
    "NebulaTriadic_FTerm": NebulaTriadic_FTerm,
    "NebulaTriadic_CompressedTerm": NebulaTriadic_CompressedTerm,
    "BuoyancyModule_IntegrandTerm": BuoyancyModule_IntegrandTerm,
    "BuoyancyModule_LENR_termTerm": BuoyancyModule_LENR_termTerm,
    "BuoyancyModule_FTerm": BuoyancyModule_FTerm,
    "BuoyancyModule_CompressedTerm": BuoyancyModule_CompressedTerm,
    "UQFF8Astro_IntegrandTerm": UQFF8Astro_IntegrandTerm,
    "UQFF8Astro_LENR_termTerm": UQFF8Astro_LENR_termTerm,
    "UQFF8Astro_FTerm": UQFF8Astro_FTerm,
    "UQFF8Astro_CompressedTerm": UQFF8Astro_CompressedTerm,
    "UQFFCore_IntegrandTerm": UQFFCore_IntegrandTerm,
    "UQFFCore_LENR_termTerm": UQFFCore_LENR_termTerm,
    "UQFFCore_FTerm": UQFFCore_FTerm,
    "UQFFCore_CompressedTerm": UQFFCore_CompressedTerm,
    "Buoyancy168_IntegrandTerm": Buoyancy168_IntegrandTerm,
    "Buoyancy168_LENR_termTerm": Buoyancy168_LENR_termTerm,
    "Buoyancy168_FTerm": Buoyancy168_FTerm,
    "Buoyancy168_CompressedTerm": Buoyancy168_CompressedTerm,
    "CassiniBuoyancy_IntegrandTerm": CassiniBuoyancy_IntegrandTerm,
    "CassiniBuoyancy_LENR_termTerm": CassiniBuoyancy_LENR_termTerm,
    "CassiniBuoyancy_FTerm": CassiniBuoyancy_FTerm,
    "CassiniBuoyancy_CompressedTerm": CassiniBuoyancy_CompressedTerm,
    "MultiAstro_IntegrandTerm": MultiAstro_IntegrandTerm,
    "MultiAstro_LENR_termTerm": MultiAstro_LENR_termTerm,
    "MultiAstro_FTerm": MultiAstro_FTerm,
    "MultiAstro_CompressedTerm": MultiAstro_CompressedTerm,
    "EightAstro_IntegrandTerm": EightAstro_IntegrandTerm,
    "EightAstro_LENR_termTerm": EightAstro_LENR_termTerm,
    "EightAstro_FTerm": EightAstro_FTerm,
    "EightAstro_CompressedTerm": EightAstro_CompressedTerm,
    "NineteenAstro_IntegrandTerm": NineteenAstro_IntegrandTerm,
    "NineteenAstro_LENR_termTerm": NineteenAstro_LENR_termTerm,
    "NineteenAstro_FTerm": NineteenAstro_FTerm,
    "NineteenAstro_CompressedTerm": NineteenAstro_CompressedTerm,
    "WolframFieldUnity_IntegrandTerm": WolframFieldUnity_IntegrandTerm,
    "WolframFieldUnity_LENR_termTerm": WolframFieldUnity_LENR_termTerm,
    "WolframFieldUnity_FTerm": WolframFieldUnity_FTerm,
    "WolframFieldUnity_CompressedTerm": WolframFieldUnity_CompressedTerm,
    "J1610_DPM_resonanceTerm": J1610_DPM_resonanceTerm,
    "J1610_Q_waveTerm": J1610_Q_waveTerm,
    "J1610_Ub1Term": J1610_Ub1Term,
    "J1610_UiTerm": J1610_UiTerm,
    "J1610_X2Term": J1610_X2Term,
    "J1610_ActivationTerm": J1610_ActivationTerm,
    "J1610_DirectedEnergyTerm": J1610_DirectedEnergyTerm,
    "J1610_NeutronTerm": J1610_NeutronTerm,
    "J1610_RelativisticTerm": J1610_RelativisticTerm,
    "J1610_NeutrinoTerm": J1610_NeutrinoTerm,
    "M87Jet_DPM_resonanceTerm": M87Jet_DPM_resonanceTerm,
    "M87Jet_Q_waveTerm": M87Jet_Q_waveTerm,
    "M87Jet_Ub1Term": M87Jet_Ub1Term,
    "M87Jet_UiTerm": M87Jet_UiTerm,
    "M87Jet_X2Term": M87Jet_X2Term,
    "M87Jet_ActivationTerm": M87Jet_ActivationTerm,
    "M87Jet_DirectedEnergyTerm": M87Jet_DirectedEnergyTerm,
    "M87Jet_NeutronTerm": M87Jet_NeutronTerm,
    "M87Jet_RelativisticTerm": M87Jet_RelativisticTerm,
    "M87Jet_NeutrinoTerm": M87Jet_NeutrinoTerm,
    "SgrAStar_DPM_resonanceTerm": SgrAStar_DPM_resonanceTerm,
    "SgrAStar_Q_waveTerm": SgrAStar_Q_waveTerm,
    "SgrAStar_Ub1Term": SgrAStar_Ub1Term,
    "SgrAStar_UiTerm": SgrAStar_UiTerm,
    "SgrAStar_X2Term": SgrAStar_X2Term,
    "SgrAStar_ActivationTerm": SgrAStar_ActivationTerm,
    "SgrAStar_DirectedEnergyTerm": SgrAStar_DirectedEnergyTerm,
    "SgrAStar_NeutronTerm": SgrAStar_NeutronTerm,
    "SgrAStar_RelativisticTerm": SgrAStar_RelativisticTerm,
    "SgrAStar_NeutrinoTerm": SgrAStar_NeutrinoTerm,
    "NGC1365_DPM_resonanceTerm": NGC1365_DPM_resonanceTerm,
    "NGC1365_Q_waveTerm": NGC1365_Q_waveTerm,
    "NGC1365_Ub1Term": NGC1365_Ub1Term,
    "NGC1365_UiTerm": NGC1365_UiTerm,
    "NGC1365_X2Term": NGC1365_X2Term,
    "NGC1365_ActivationTerm": NGC1365_ActivationTerm,
    "NGC1365_DirectedEnergyTerm": NGC1365_DirectedEnergyTerm,
    "NGC1365_NeutronTerm": NGC1365_NeutronTerm,
    "NGC1365_RelativisticTerm": NGC1365_RelativisticTerm,
    "NGC1365_NeutrinoTerm": NGC1365_NeutrinoTerm,
    "VelaPulsar_DPM_resonanceTerm": VelaPulsar_DPM_resonanceTerm,
    "VelaPulsar_Q_waveTerm": VelaPulsar_Q_waveTerm,
    "VelaPulsar_Ub1Term": VelaPulsar_Ub1Term,
    "VelaPulsar_UiTerm": VelaPulsar_UiTerm,
    "VelaPulsar_X2Term": VelaPulsar_X2Term,
    "VelaPulsar_ActivationTerm": VelaPulsar_ActivationTerm,
    "VelaPulsar_DirectedEnergyTerm": VelaPulsar_DirectedEnergyTerm,
    "VelaPulsar_NeutronTerm": VelaPulsar_NeutronTerm,
    "VelaPulsar_RelativisticTerm": VelaPulsar_RelativisticTerm,
    "VelaPulsar_NeutrinoTerm": VelaPulsar_NeutrinoTerm,
    "NGC2207_DPM_resonanceTerm": NGC2207_DPM_resonanceTerm,
    "NGC2207_Q_waveTerm": NGC2207_Q_waveTerm,
    "NGC2207_Ub1Term": NGC2207_Ub1Term,
    "NGC2207_UiTerm": NGC2207_UiTerm,
    "NGC2207_X2Term": NGC2207_X2Term,
    "NGC2207_ActivationTerm": NGC2207_ActivationTerm,
    "NGC2207_DirectedEnergyTerm": NGC2207_DirectedEnergyTerm,
    "NGC2207_NeutronTerm": NGC2207_NeutronTerm,
    "NGC2207_RelativisticTerm": NGC2207_RelativisticTerm,
    "NGC2207_NeutrinoTerm": NGC2207_NeutrinoTerm,
    "PerseusCluster_DPM_resonanceTerm": PerseusCluster_DPM_resonanceTerm,
    "PerseusCluster_Q_waveTerm": PerseusCluster_Q_waveTerm,
    "PerseusCluster_Ub1Term": PerseusCluster_Ub1Term,
    "PerseusCluster_UiTerm": PerseusCluster_UiTerm,
    "PerseusCluster_X2Term": PerseusCluster_X2Term,
    "PerseusCluster_ActivationTerm": PerseusCluster_ActivationTerm,
    "PerseusCluster_DirectedEnergyTerm": PerseusCluster_DirectedEnergyTerm,
    "PerseusCluster_NeutronTerm": PerseusCluster_NeutronTerm,
    "PerseusCluster_RelativisticTerm": PerseusCluster_RelativisticTerm,
    "PerseusCluster_NeutrinoTerm": PerseusCluster_NeutrinoTerm,
    "OrionM42_DPM_resonanceTerm": OrionM42_DPM_resonanceTerm,
    "OrionM42_Q_waveTerm": OrionM42_Q_waveTerm,
    "OrionM42_Ub1Term": OrionM42_Ub1Term,
    "OrionM42_UiTerm": OrionM42_UiTerm,
    "OrionM42_X2Term": OrionM42_X2Term,
    "OrionM42_ActivationTerm": OrionM42_ActivationTerm,
    "OrionM42_DirectedEnergyTerm": OrionM42_DirectedEnergyTerm,
    "OrionM42_NeutronTerm": OrionM42_NeutronTerm,
    "OrionM42_RelativisticTerm": OrionM42_RelativisticTerm,
    "OrionM42_NeutrinoTerm": OrionM42_NeutrinoTerm,
    "LagoonM8_DPM_resonanceTerm": LagoonM8_DPM_resonanceTerm,
    "LagoonM8_Q_waveTerm": LagoonM8_Q_waveTerm,
    "LagoonM8_Ub1Term": LagoonM8_Ub1Term,
    "LagoonM8_UiTerm": LagoonM8_UiTerm,
    "LagoonM8_X2Term": LagoonM8_X2Term,
    "LagoonM8_ActivationTerm": LagoonM8_ActivationTerm,
    "LagoonM8_DirectedEnergyTerm": LagoonM8_DirectedEnergyTerm,
    "LagoonM8_NeutronTerm": LagoonM8_NeutronTerm,
    "LagoonM8_RelativisticTerm": LagoonM8_RelativisticTerm,
    "LagoonM8_NeutrinoTerm": LagoonM8_NeutrinoTerm,
    "ESO137_G_timeTerm": ESO137_G_timeTerm,
    "NGC1365A_G_timeTerm": NGC1365A_G_timeTerm,
    "NGC5728_G_timeTerm": NGC5728_G_timeTerm,
    "Vela_G_timeTerm": Vela_G_timeTerm,
    "M82_G_timeTerm": M82_G_timeTerm,
    "Carina_G_timeTerm": Carina_G_timeTerm,
    "TadpoleGalaxy_G_timeTerm": TadpoleGalaxy_G_timeTerm,
    "MiceGalaxies_G_timeTerm": MiceGalaxies_G_timeTerm,
    "NGC2264_G_timeTerm": NGC2264_G_timeTerm,
    "M42_G_timeTerm": M42_G_timeTerm,
    "CrabNebula_G_timeTerm": CrabNebula_G_timeTerm,
    "BlackWidowPulsar_G_timeTerm": BlackWidowPulsar_G_timeTerm,
    "Redback_G_timeTerm": Redback_G_timeTerm,
    "Fermi_G_timeTerm": Fermi_G_timeTerm,
    "Holmberg15A_G_timeTerm": Holmberg15A_G_timeTerm,
    "NineteenSystems_G_timeTerm": NineteenSystems_G_timeTerm,
    "WolframHypergraph_G_timeTerm": WolframHypergraph_G_timeTerm,
    "NGC253_G_timeTerm": NGC253_G_timeTerm,
    "M87Jet_G_timeTerm": M87Jet_G_timeTerm,
    "Centaurus_G_timeTerm": Centaurus_G_timeTerm,
    "Antlia_G_timeTerm": Antlia_G_timeTerm,
    "Fornax_G_timeTerm": Fornax_G_timeTerm,
    "Sculptor_G_timeTerm": Sculptor_G_timeTerm,
    "LeoI_G_timeTerm": LeoI_G_timeTerm,
    "LeoII_G_timeTerm": LeoII_G_timeTerm,
    "UrsaMinor_G_timeTerm": UrsaMinor_G_timeTerm,
    "Draco_G_timeTerm": Draco_G_timeTerm,
    "Sextans_G_timeTerm": Sextans_G_timeTerm,
    "Carina2_G_timeTerm": Carina2_G_timeTerm,
    "Reticulum_G_timeTerm": Reticulum_G_timeTerm,
    "Tucana_G_timeTerm": Tucana_G_timeTerm,
    "Horologium_G_timeTerm": Horologium_G_timeTerm,
    "Grus_G_timeTerm": Grus_G_timeTerm,
    "Phoenix_G_timeTerm": Phoenix_G_timeTerm,
    "Eridanus_G_timeTerm": Eridanus_G_timeTerm,
    "Aquarius_G_timeTerm": Aquarius_G_timeTerm,
    "Pisces_G_timeTerm": Pisces_G_timeTerm,
    "Pegasus_G_timeTerm": Pegasus_G_timeTerm,
    "Andromeda_G_timeTerm": Andromeda_G_timeTerm,
    "Triangulum_G_timeTerm": Triangulum_G_timeTerm,
    "LMC_G_timeTerm": LMC_G_timeTerm,
    "J1610_G_timeTerm": J1610_G_timeTerm,
    "JupiterAurorae_G_timeTerm": JupiterAurorae_G_timeTerm,
    "LagoonM8HII_G_timeTerm": LagoonM8HII_G_timeTerm,
    "M87JetEHT_G_timeTerm": M87JetEHT_G_timeTerm,
    "NGC1365Seyfert_G_timeTerm": NGC1365Seyfert_G_timeTerm,
    "NGC2207IC2163_G_timeTerm": NGC2207IC2163_G_timeTerm,
    "RAquarii_G_timeTerm": RAquarii_G_timeTerm,
    "SgrAStarGC_G_timeTerm": SgrAStarGC_G_timeTerm,
    "SPTCLJ2215_G_timeTerm": SPTCLJ2215_G_timeTerm,
    "StephanQuintet_G_timeTerm": StephanQuintet_G_timeTerm,
    "VelaPulsarSNR_G_timeTerm": VelaPulsarSNR_G_timeTerm,
    "NGC4993GW170817_G_timeTerm": NGC4993GW170817_G_timeTerm,
    "NGC6240DualAGN_G_timeTerm": NGC6240DualAGN_G_timeTerm,
    "OmegaCentauri_G_timeTerm": OmegaCentauri_G_timeTerm,
    "OrionM42Trapezium_G_timeTerm": OrionM42Trapezium_G_timeTerm,
    "PerseusAbell426_G_timeTerm": PerseusAbell426_G_timeTerm,
    "PolarRingNGC4650A_G_timeTerm": PolarRingNGC4650A_G_timeTerm,
    "QuasarJ0313_G_timeTerm": QuasarJ0313_G_timeTerm,
    "RXJ1713TeVSNR_G_timeTerm": RXJ1713TeVSNR_G_timeTerm,
    "SMC_G_timeTerm": SMC_G_timeTerm,
    "BuoyancyAstro5_G_timeTerm": BuoyancyAstro5_G_timeTerm,
    "BuoyancyCNB_G_timeTerm": BuoyancyCNB_G_timeTerm,
    "AstroSystems163_G_timeTerm": AstroSystems163_G_timeTerm,
    "NebulaTriadic_G_timeTerm": NebulaTriadic_G_timeTerm,
    "BuoyancyModule_G_timeTerm": BuoyancyModule_G_timeTerm,
    "UQFF8Astro_G_timeTerm": UQFF8Astro_G_timeTerm,
    "UQFFCore_G_timeTerm": UQFFCore_G_timeTerm,
    "Buoyancy168_G_timeTerm": Buoyancy168_G_timeTerm,
    "CassiniBuoyancy_G_timeTerm": CassiniBuoyancy_G_timeTerm,
    "MultiAstro_G_timeTerm": MultiAstro_G_timeTerm,
    "EightAstro_G_timeTerm": EightAstro_G_timeTerm,
    "NineteenAstro26D_G_timeTerm": NineteenAstro26D_G_timeTerm,
    "WolframFieldUnity_G_timeTerm": WolframFieldUnity_G_timeTerm,
    "ESO137_HeavisideFactor": ESO137_HeavisideFactor,
    "ESO137_UmBase": ESO137_UmBase,
    "ESO137_UmContribution": ESO137_UmContribution,
    "NGC1365A_H_SCm": NGC1365A_H_SCm,
    "NGC1365A_U_g2": NGC1365A_U_g2,
    "NGC1365A_SwirlFactor": NGC1365A_SwirlFactor,
    "NGC5728_U_gi": NGC5728_U_gi,
    "NGC5728_K_i": NGC5728_K_i,
    "NGC5728_SumKUgi": NGC5728_SumKUgi,
    "MagneticMoment_Mu_j": MagneticMoment_Mu_j,
    "MagneticMoment_B_j": MagneticMoment_B_j,
    "MagneticMoment_UmContrib": MagneticMoment_UmContrib,
    "BlackHole_M_bh": BlackHole_M_bh,
    "BlackHole_M_bhInMsun": BlackHole_M_bhInMsun,
    "BlackHole_U_b1": BlackHole_U_b1,
    "NegativeTime_T_n": NegativeTime_T_n,
    "NegativeTime_CosPiTn": NegativeTime_CosPiTn,
    "NegativeTime_OneMinusExp": NegativeTime_OneMinusExp,
    "PiConstant_Pi": PiConstant_Pi,
    "PiConstant_CosPiTn": PiConstant_CosPiTn,
    "PiConstant_SinOmegaCT": PiConstant_SinOmegaCT,
    "Decay_Gamma_s": Decay_Gamma_s,
    "Decay_ExpTerm": Decay_ExpTerm,
    "Decay_OneMinusExp": Decay_OneMinusExp,
    "ScmPenetration_P_SCm": ScmPenetration_P_SCm,
    "ScmPenetration_UmPlanet": ScmPenetration_UmPlanet,
    "ScmReactivity_Kappa_day": ScmReactivity_Kappa_day,
    "ScmReactivity_E_reactDecay": ScmReactivity_E_reactDecay,
    "SolarCycle_Omega_c": SolarCycle_Omega_c,
    "SolarCycle_PeriodYears": SolarCycle_PeriodYears,
    "SolarCycle_MuJ_Modulated": SolarCycle_MuJ_Modulated,
    "SolarCycle_B_j_Modulated": SolarCycle_B_j_Modulated,
    "SolarWind_Delta_sw": SolarWind_Delta_sw,
    "SolarWind_ModulationFactor": SolarWind_ModulationFactor,
    "SolarWind_U_g2_NoMod": SolarWind_U_g2_NoMod,
    "SolarWindVelocity_V_sw": SolarWindVelocity_V_sw,
    "SolarWindVelocity_V_swKmS": SolarWindVelocity_V_swKmS,
    "SolarWindVelocity_U_g2_NoSW": SolarWindVelocity_U_g2_NoSW,
    "StellarMass_M_s": StellarMass_M_s,
    "StellarMass_M_sInMsun": StellarMass_M_sInMsun,
    "ReactorEfficiencyTerm": ReactorEfficiencyTerm,
    "MuSTerm": MuSTerm,
    "GradMsRTerm": GradMsRTerm,
    "BjTerm": BjTerm,
    "OmegaSTTerm": OmegaSTTerm,
    "MuJTerm": MuJTerm,
    "UniversalGravity1Term": UniversalGravity1Term,
    "UniversalGravity2Term": UniversalGravity2Term,
    "UniversalGravity3Term": UniversalGravity3Term,
    "UniversalGravity4Term": UniversalGravity4Term,
    "UniversalBuoyancyTerm": UniversalBuoyancyTerm,
    "UniversalMagnetismTerm": UniversalMagnetismTerm,
    "UniversalAetherTerm": UniversalAetherTerm,
    "UnifiedFieldTerm": UnifiedFieldTerm,
    "MUGECompressedBaseTerm": MUGECompressedBaseTerm,
    "MUGEExpansionTerm": MUGEExpansionTerm,
    "MUGESuperAdjustmentTerm": MUGESuperAdjustmentTerm,
    "MUGEEnvelopeTerm": MUGEEnvelopeTerm,
    "MUGEUgSumTerm": MUGEUgSumTerm,
    "MUGECosmologicalTerm": MUGECosmologicalTerm,
    "MUGEQuantumTerm": MUGEQuantumTerm,
    "MUGEFluidTerm": MUGEFluidTerm,
    "MUGEPerturbationTerm": MUGEPerturbationTerm,
    "MUGEResonanceADPMTerm": MUGEResonanceADPMTerm,
    "MUGEResonanceATHzTerm": MUGEResonanceATHzTerm,
    "MUGEResonanceAvacDiffTerm": MUGEResonanceAvacDiffTerm,
    "MUGEResonanceASuperFreqTerm": MUGEResonanceASuperFreqTerm,
    "MUGEResonanceAAetherResTerm": MUGEResonanceAAetherResTerm,
    "MUGEResonanceUg4iTerm": MUGEResonanceUg4iTerm,
    "MUGEResonanceAQuantumFreqTerm": MUGEResonanceAQuantumFreqTerm,
    "MUGEResonanceAAetherFreqTerm": MUGEResonanceAAetherFreqTerm,
    "MUGEResonanceAFluidFreqTerm": MUGEResonanceAFluidFreqTerm,
    "MUGEResonanceOscTerm": MUGEResonanceOscTerm,
    "MUGEResonanceAExpFreqTerm": MUGEResonanceAExpFreqTerm,
    "MUGEResonanceFTRZTerm": MUGEResonanceFTRZTerm,
    "MUGEResonanceWormholeTerm": MUGEResonanceWormholeTerm,
    "TauLeptonDipoleTerm": TauLeptonDipoleTerm,
    "CKMVcbTerm": CKMVcbTerm,
    "LFVBranchingTerm": LFVBranchingTerm,
    "VectorLikeQuarkTerm": VectorLikeQuarkTerm,
    "UnifiedFieldTerm_SOURCE4": UnifiedFieldTerm_SOURCE4,
    "MUGESuperAdjTerm": MUGESuperAdjTerm,
    "MUGECosmTerm": MUGECosmTerm,
    "SGR1745MagnetarTerm": SGR1745MagnetarTerm,
    "SagittariusAStarTerm": SagittariusAStarTerm,
    "TapestryStarbirthTerm": TapestryStarbirthTerm,
    "Westerlund2ClusterTerm": Westerlund2ClusterTerm,
    "PillarsCreationTerm": PillarsCreationTerm,
    "RingsRelativityTerm": RingsRelativityTerm,
    "StudentGuideUniverseTerm": StudentGuideUniverseTerm,
    "CompressedMUGETerm_SOURCE4": CompressedMUGETerm_SOURCE4,
    "ResonanceMUGETerm_SOURCE4": ResonanceMUGETerm_SOURCE4,
    "DynamicVacuumTerm": DynamicVacuumTerm,
    "QuantumCouplingTerm": QuantumCouplingTerm,
    "DynamicVacuumTerm": DynamicVacuumTerm,
    "QuantumCouplingTerm": QuantumCouplingTerm,
    "DynamicVacuumTerm": DynamicVacuumTerm,
    "QuantumCouplingTerm": QuantumCouplingTerm,
    "DynamicVacuumTerm": DynamicVacuumTerm,
    "QuantumCouplingTerm": QuantumCouplingTerm,
    "DynamicVacuumTerm": DynamicVacuumTerm,
    "QuantumCouplingTerm": QuantumCouplingTerm,
    "DynamicVacuumTerm": DynamicVacuumTerm,
    "QuantumCouplingTerm": QuantumCouplingTerm,
    "DynamicVacuumTerm": DynamicVacuumTerm,
    "QuantumCouplingTerm": QuantumCouplingTerm,
    "DynamicVacuumTerm": DynamicVacuumTerm,
    "QuantumCouplingTerm": QuantumCouplingTerm,
    "DynamicVacuumTerm": DynamicVacuumTerm,
    "QuantumCouplingTerm": QuantumCouplingTerm,
    "DynamicVacuumTerm": DynamicVacuumTerm,
    "QuantumCouplingTerm": QuantumCouplingTerm,
    "DynamicVacuumTerm": DynamicVacuumTerm,
    "QuantumCouplingTerm": QuantumCouplingTerm,
    "DynamicVacuumTerm": DynamicVacuumTerm,
    "QuantumCouplingTerm": QuantumCouplingTerm,
    "DynamicVacuumTerm": DynamicVacuumTerm,
    "QuantumCouplingTerm": QuantumCouplingTerm,
    "DynamicVacuumTerm": DynamicVacuumTerm,
    "QuantumCouplingTerm": QuantumCouplingTerm,
    "DynamicVacuumTerm": DynamicVacuumTerm,
    "QuantumCouplingTerm": QuantumCouplingTerm,
    "DynamicVacuumTerm": DynamicVacuumTerm,
    "QuantumCouplingTerm": QuantumCouplingTerm,
    "DynamicVacuumTerm": DynamicVacuumTerm,
    "QuantumCouplingTerm": QuantumCouplingTerm,
    "DynamicVacuumTerm": DynamicVacuumTerm,
    "QuantumCouplingTerm": QuantumCouplingTerm,
    "DynamicVacuumTerm": DynamicVacuumTerm,
    "QuantumCouplingTerm": QuantumCouplingTerm,
    "DynamicVacuumTerm": DynamicVacuumTerm,
    "QuantumCouplingTerm": QuantumCouplingTerm,
    "DynamicVacuumTerm": DynamicVacuumTerm,
    "QuantumCouplingTerm": QuantumCouplingTerm,
    "DynamicVacuumTerm": DynamicVacuumTerm,
    "QuantumCouplingTerm": QuantumCouplingTerm,
    "DynamicVacuumTerm": DynamicVacuumTerm,
    "QuantumCouplingTerm": QuantumCouplingTerm,
    "DynamicVacuumTerm": DynamicVacuumTerm,
    "QuantumCouplingTerm": QuantumCouplingTerm,
    "DynamicVacuumTerm": DynamicVacuumTerm,
    "QuantumCouplingTerm": QuantumCouplingTerm,
    "DynamicVacuumTerm": DynamicVacuumTerm,
    "QuantumCouplingTerm": QuantumCouplingTerm,
    "DynamicVacuumTerm": DynamicVacuumTerm,
    "QuantumCouplingTerm": QuantumCouplingTerm,
    "DynamicVacuumTerm": DynamicVacuumTerm,
    "QuantumCouplingTerm": QuantumCouplingTerm,
    "DynamicVacuumTerm": DynamicVacuumTerm,
    "QuantumCouplingTerm": QuantumCouplingTerm,
    "DynamicVacuumTerm": DynamicVacuumTerm,
    "QuantumCouplingTerm": QuantumCouplingTerm,
    "DynamicVacuumTerm": DynamicVacuumTerm,
    "QuantumCouplingTerm": QuantumCouplingTerm,
    "DynamicVacuumTerm": DynamicVacuumTerm,
    "QuantumCouplingTerm": QuantumCouplingTerm,
    "DynamicVacuumTerm": DynamicVacuumTerm,
    "QuantumCouplingTerm": QuantumCouplingTerm,
    "DynamicVacuumTerm": DynamicVacuumTerm,
    "QuantumCouplingTerm": QuantumCouplingTerm,
    "DynamicVacuumTerm": DynamicVacuumTerm,
    "QuantumCouplingTerm": QuantumCouplingTerm,
    "DynamicVacuumTerm": DynamicVacuumTerm,
    "QuantumCouplingTerm": QuantumCouplingTerm,
    "DynamicVacuumTerm": DynamicVacuumTerm,
    "QuantumCouplingTerm": QuantumCouplingTerm,
    "DynamicVacuumTerm": DynamicVacuumTerm,
    "QuantumCouplingTerm": QuantumCouplingTerm,
    "DynamicVacuumTerm": DynamicVacuumTerm,
    "QuantumCouplingTerm": QuantumCouplingTerm,
    "DynamicVacuumTerm": DynamicVacuumTerm,
    "QuantumCouplingTerm": QuantumCouplingTerm,
    "DynamicVacuumTerm": DynamicVacuumTerm,
    "QuantumCouplingTerm": QuantumCouplingTerm,
    "DynamicVacuumTerm": DynamicVacuumTerm,
    "QuantumCouplingTerm": QuantumCouplingTerm,
    "DynamicVacuumTerm": DynamicVacuumTerm,
    "QuantumCouplingTerm": QuantumCouplingTerm,
    "DynamicVacuumTerm": DynamicVacuumTerm,
    "QuantumCouplingTerm": QuantumCouplingTerm,
    "DynamicVacuumTerm": DynamicVacuumTerm,
    "QuantumCouplingTerm": QuantumCouplingTerm,
    "DynamicVacuumTerm": DynamicVacuumTerm,
    "QuantumCouplingTerm": QuantumCouplingTerm,
    "DynamicVacuumTerm": DynamicVacuumTerm,
    "QuantumCouplingTerm": QuantumCouplingTerm,
    "DynamicVacuumTerm": DynamicVacuumTerm,
    "QuantumCouplingTerm": QuantumCouplingTerm,
    "DynamicVacuumTerm": DynamicVacuumTerm,
    "QuantumCouplingTerm": QuantumCouplingTerm,
    "DynamicVacuumTerm": DynamicVacuumTerm,
    "QuantumCouplingTerm": QuantumCouplingTerm,
    "DynamicVacuumTerm": DynamicVacuumTerm,
    "QuantumCouplingTerm": QuantumCouplingTerm,
    "DynamicVacuumTerm": DynamicVacuumTerm,
    "QuantumCouplingTerm": QuantumCouplingTerm,
    "DynamicVacuumTerm": DynamicVacuumTerm,
    "QuantumCouplingTerm": QuantumCouplingTerm,
    "DynamicVacuumTerm": DynamicVacuumTerm,
    "QuantumCouplingTerm": QuantumCouplingTerm,
    "DynamicVacuumTerm": DynamicVacuumTerm,
    "QuantumCouplingTerm": QuantumCouplingTerm,
    "DynamicVacuumTerm": DynamicVacuumTerm,
    "QuantumCouplingTerm": QuantumCouplingTerm,
    "DynamicVacuumTerm": DynamicVacuumTerm,
    "QuantumCouplingTerm": QuantumCouplingTerm,
    "DynamicVacuumTerm": DynamicVacuumTerm,
    "QuantumCouplingTerm": QuantumCouplingTerm,
    "DynamicVacuumTerm": DynamicVacuumTerm,
    "QuantumCouplingTerm": QuantumCouplingTerm,
}

CPP_EXTRACTED_AVAILABLE = True
CPP_TERM_COUNT = 1064
