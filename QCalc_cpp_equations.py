"""
QCalc_cpp_equations.py - ALL equations extracted from MAIN_1_CoAnQi.cpp

Auto-generated comprehensive extraction including:
- PhysicsTerm class compute() methods
- Standalone double functions
- Namespace functions (SOURCE4, etc.)
- Inline sub-equations

Total equations: 3565

Usage:
    from QCalc_cpp_equations import ALL_EQUATIONS, compute_equation
    
    result = compute_equation('DynamicVacuumTerm', {'r': 1e6, 't': 1000})
"""

import math
from dpm_helpers import dpm_ug1_seed, dpm_ug2_shell

from typing import Dict, Any, Callable

try:
    from QCalc import CONSTANTS
except ImportError:
    CONSTANTS = {"M_sun": 1.989e30, "G": 6.6743e-11, "c": 2.998e8, "PI": 3.141592653589793}


def safe_compute(expr_func, params: dict, default: float = 0.0) -> float:
    """Safely compute an expression with error handling."""
    try:
        result = expr_func(params)
        if result != result:  # NaN check
            return default
        if abs(result) == float('inf'):
            return default
        return float(result)
    except Exception:
        return default



class PhysicsTerm:
    """Base class for physics equations."""
    
    def __init__(self, name: str, description: str, compute_func: Callable):
        self.name = name
        self.description = description
        self._compute = compute_func
    
    def compute(self, params: dict) -> float:
        return safe_compute(self._compute, params)
    
    def get_description(self) -> str:
        return self.description


# =============================================================================
# EQUATION FUNCTIONS
# =============================================================================

def _compute_DynamicVacuumTerm(p: dict) -> float:
    """Time-varying vacuum energy"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_QuantumCouplingTerm(p: dict) -> float:
    """Non-local quantum effects"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_DarkMatterHaloTerm(p: dict) -> float:
    """NFW dark matter halo contribution (G*M*ln(1+x)/(r*x))"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_VacuumEnergyTerm(p: dict) -> float:
    """Vacuum energy fluctuation (lambda*E_vac*(1 + 0.1*sin(t)))"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_QuantumEntanglementTerm(p: dict) -> float:
    """Non-local quantum entanglement (hbar^2/(M*r^2)*cos(t/1e6))"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = coupling_strength * (hbar * hbar) / (M * r * r) * math.cos(t / 1e6)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_CosmicNeutrinoTerm(p: dict) -> float:
    """CNB energy density contribution (n_nu * k_B * T_cnb / r^2)"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = energy_density / (r * r)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_MultiSystemUQFFTerm(p: dict) -> float:
    """Multi-system UQFF (NGC685/3507/3511/AT2024tvd) with DPM resonance, LENR, gravity compressed"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (term_mom + term_grav + term_vac + LENR_term + term_res) * x2 + gravity_compressed + resonance + buoyancy
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_DPMResonanceTerm(p: dict) -> float:
    """DPM quantum magnetic resonance (Lande factor based)"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (g_Lande * mu_B * B) / (hbar * omega)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_LENRExtendedTerm(p: dict) -> float:
    """Extended LENR with frequency ratio squared scaling"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = k * pow(omega_L / omega_0, 2.0)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_SMBHAccretionTerm(p: dict) -> float:
    """SMBH accretion disk luminosity (radiative efficiency model)"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = eta * M_dot * c * c
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_TDETerm(p: dict) -> float:
    """Tidal Disruption Event with exponential + power-law decay"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = L * math.exp(-abs(dt_norm) / 0.3) * pow(1.0 + abs(dt_norm), -5.0 / 3.0)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_NebulaUQFFTerm(p: dict) -> float:
    """Nebula UQFF with gas expansion, ionization, and radiation pressure"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = gravity + gas_nebula
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_GasIonizationTerm(p: dict) -> float:
    """Gas ionization fraction (Stromgren sphere model)"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = ion_frac * 1e40
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_NebulaExpansionTerm(p: dict) -> float:
    """Nebula expansion with time-dependent radius and density"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = E_expansion / r_t
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_BuoyancyUQFFTerm(p: dict) -> float:
    """Buoyancy UQFF for """
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = integrand * x2
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_InflationBuoyancyTerm(p: dict) -> float:
    """Inflation-driven buoyancy force"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = beta_i * V_infl * rho_vac * a_univ
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_SuperconductiveTerm(p: dict) -> float:
    """Time-dependent superconductivity with oscillating vacuum density"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = lambda_i * (rho_sc / rho_ua) * omega_s * cos_term * (1.0 + f_TRZ)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_NeutronScatteringTerm(p: dict) -> float:
    """Neutron scattering cross-section contribution"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = k_neutron * sigma_n
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_AstroSystemUQFFTerm(p: dict) -> float:
    """Astro System UQFF for """
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = integrand * x2 + gravity + resonance + buoyancy
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_DipoleVortexTerm(p: dict) -> float:
    """Dipole vortex species determination with golden ratio cycle"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = dipole_base * math.sin(phase)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_QuantumState26Term(p: dict) -> float:
    """26-state quantum system with alphabet-like scaling"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = sum
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_TriadicScaleTerm(p: dict) -> float:
    """Triadic UQFF scaling with enhanced beta_i = 1.0"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = beta_i * V_infl * rho_vac * a_univ
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_UQFFMasterTerm(p: dict) -> float:
    """UQFF Master Force for """
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = U_g1 + U_g3 * f_Ub_scale
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_ElectrostaticBarrierTerm(p: dict) -> float:
    """Exponential barrier penetration term for electrostatic force"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = math.math.exp(-R_EB / r)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_ElectricFieldTerm(p: dict) -> float:
    """Electric field derived from Universal Magnetism"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (U_m / rho_vac_UA) * (1.0 / r)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_NeutronProductionTerm(p: dict) -> float:
    """Neutron production rate with 26 quantum states"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = k_eta * exp_ssq * exp_pi_t * field_term
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_CelestialBodyTerm(p: dict) -> float:
    """Source4 celestial body simulation for """
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = gravitational * time_decay * oscillation
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_MUGETerm(p: dict) -> float:
    """Source4 MUGE compressed gravity calculation"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = compressed * time_mod
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_QuasarJetTerm(p: dict) -> float:
    """Source4 quasar jet Navier-Stokes simulation"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = jet_power * gamma * time_evolution
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_ReactorEnergyTerm(p: dict) -> float:
    """Source4: SCm reactor efficiency with exponential time decay"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (scm_d * v * v / rho) * math.exp(-k * t)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_MagneticDipoleTerm(p: dict) -> float:
    """Source4: Time-varying magnetic dipole moment with SCm enhancement"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = Bs_t * pow(R, 3)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_MagneticJetFieldTerm(p: dict) -> float:
    """Source4: Oscillating magnetic field in stellar jets"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = B0 + 0.4 * math.sin(omega * t) + scm
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_UnifiedBuoyancyTerm(p: dict) -> float:
    """Source4: Galactic buoyancy force from Ug components"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = -beta * Ugi * omega * M / d * wind_mod * ua * math.cos(math.pi * tn)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_CompressedMUGEBaseTerm(p: dict) -> float:
    """Source4: Compressed MUGE base gravitational term"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_CompressedMUGEExpansionTerm(p: dict) -> float:
    """Source4: Hubble expansion correction for compressed MUGE"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1.0 + H * t
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_SuperconductiveAdjustmentTerm(p: dict) -> float:
    """Source4: Magnetic superconductivity adjustment factor"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_CosmologicalConstantTerm(p: dict) -> float:
    """Source4: Cosmological constant dark energy term"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = L * c_light * c_light / 3.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_QuantumUncertaintyTerm(p: dict) -> float:
    """Source4: Heisenberg uncertainty principle quantum term"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_FluidDynamicsTerm(p: dict) -> float:
    """Source4: Fluid dynamics contribution to gravity"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = rho * V * g
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_DensityPerturbationTerm(p: dict) -> float:
    """Source4: Visible + dark matter density perturbation"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_UQFFModule5Term(p: dict) -> float:
    """Source5 self-expanding UQFF with dark matter halo and vacuum energy"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = dm_contrib + vac_contrib
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_ResonanceMUGETerm(p: dict) -> float:
    """Source5 resonance-based MUGE with superconductive enhancement"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = base_gravity * resonance_mod * superconductive_factor
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_StateExportTerm(p: dict) -> float:
    """Source5 cross-module state export and communication"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = state_evolution
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_TimeVaryingRotationTerm(p: dict) -> float:
    """Source5 time-varying rotation rate with cyclic modulation"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = omega_s_base - amplitude * math.sin(omega_c * t)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_NavierStokesFluidTerm(p: dict) -> float:
    """Source5 Navier-Stokes fluid dynamics with diffusion, advection, projection"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = velocity
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_SpacetimeMetricModulationTerm(p: dict) -> float:
    """Source5 spacetime metric modulation: A_mu_nu = g_mu_nu + eta * Ts00 * cos(pi*tn)"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 4.0 * modulation
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_FullUnifiedFieldTerm(p: dict) -> float:
    """Source5 complete unified field: FU = sum(Ugi) + sum(Ubi) + Um + A_mu_nu"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = Ug_sum + Ubi_sum + Um + A_scalar
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_ResonanceMUGE_DPMTerm(p: dict) -> float:
    """Source5 resonance MUGE DPM term: fundamental dipolar moment resonance"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = FDPM * fDPM * Evac_neb * c_res * Vsys
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_ResonanceMUGE_THzTerm(p: dict) -> float:
    """Source5 resonance MUGE THz frequency term"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = fTHz * Evac_neb * vexp * aDPM / (Evac_ISM * c_res)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_VacuumEnergyDifferentialTerm(p: dict) -> float:
    """Source5 vacuum energy differential between nebula and ISM"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = Delta_Evac * vexp * vexp * aDPM / (Evac_neb * c_res * c_res)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_SuperconductiveFrequencyTerm(p: dict) -> float:
    """Source5 superconductive frequency enhancement term"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = Fsuper * fTHz * aDPM / (Evac_neb * c_res)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_AetherResonanceTerm(p: dict) -> float:
    """Source5 Universal Aether resonance with TRZ modulation"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = UA_SCM * omega_i * fTHz * aDPM * (1 + fTRZ)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_QuantumFrequencyTerm(p: dict) -> float:
    """Source5 quantum frequency coupling term"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = fquantum * Evac_neb * aDPM / (Evac_ISM * c_res)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_AetherFrequencyTerm(p: dict) -> float:
    """Source5 Aether frequency term"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = fAether * Evac_neb * aDPM / (Evac_ISM * c_res)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_WormholeContributionTerm(p: dict) -> float:
    """Source5 wormhole spacetime contribution term"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = f_worm * Evac_neb / (b * b + r * r)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_UnifiedFieldUg1Term(p: dict) -> float:
    """Source6 unified field Ug1: magnetic dipole and mass gradient with decay"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = k1 * mu_s * grad_Ms_r * math.exp(-alpha * t) * math.cos(math.pi * tn) * defect
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_UnifiedFieldUg2Term(p: dict) -> float:
    """Source6 unified field Ug2: UA charge and SCm interaction with stellar wind"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = k2 * (QA + QUA) * Ms / (r * r) * S * wind_mod * HSCm * Ereact
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_UnifiedFieldUg3Term(p: dict) -> float:
    """Source6 unified field Ug3: magnetic jet and core penetration with rotation"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = k3 * Bj * math.cos(omega_s_t * t * math.pi) * Pcore * Ereact
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_UnifiedFieldUg4Term(p: dict) -> float:
    """Source6 unified field Ug4: galactic vacuum concentration with feedback"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = k4 * rho_v * C_concentration * Mbh / dg * decay * cycle * (1 + f_feedback)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_UnifiedFieldUmTerm(p: dict) -> float:
    """Source6 unified field Um: magnetic strings with multi-layer contribution"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = single * num_strings * PSCm * Ereact
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_SpacetimeMetricTerm(p: dict) -> float:
    """Source6 spacetime metric A_mu_nu perturbation with stress-energy coupling"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = trace
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_CompressedMUGETerm(p: dict) -> float:
    """Source6 compressed MUGE: multi-layer universal gravity with compression"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = g_compressed
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_ResonanceMUGE_VacuumDiffTerm(p: dict) -> float:
    """Source7 resonance MUGE vacuum: nebula-ISM vacuum energy differential"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = avac_diff
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_ResonanceMUGE_SuperFreqTerm(p: dict) -> float:
    """Source7 resonance MUGE superconductivity: frequency-based resonance"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = asuper_freq
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_ResonanceMUGE_AetherResTerm(p: dict) -> float:
    """Source7 resonance MUGE aether: universal aether resonance coupling"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = aaether_res
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_ResonanceMUGE_QuantumFreqTerm(p: dict) -> float:
    """Source7 resonance MUGE quantum: quantum frequency modulation"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = aquantum_freq
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_YAMLConfigTerm(p: dict) -> float:
    """Source7 YAML configuration: enhanced data loading with YAML support"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = config_influence
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_UQFFCoreBuoyancyTerm(p: dict) -> float:
    """Source10 UQFF core: buoyancy force with LENR, activation, DE, resonance"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = F_U_Bi_i
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_VacuumRepulsionTerm(p: dict) -> float:
    """Source10 vacuum repulsion: surface tension spike/drop analogy"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = F_vac_rep
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_THzShockCommunicationTerm(p: dict) -> float:
    """Source10 THz shock: 26-layer tail star formation with terahertz communication"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = F_thz_shock
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_ConduitFormationTerm(p: dict) -> float:
    """Source10 conduit: H + H2O abundance leading to COx formation"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = F_conduit
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_SpookyActionTerm(p: dict) -> float:
    """Source10 spooky action: quantum string/wave entanglement at distance"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = F_spooky
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_DPMResonanceEnergyTerm(p: dict) -> float:
    """Source10 DPM resonance: energy density from hydrogen g-factor and Bohr magneton"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = E_DPM
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_Triadic26LayerTerm(p: dict) -> float:
    """Source10 triadic: compressed UQFF with 26 gravitational layers (Ug1-4)"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = result
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_MagnetarCoreTerm(p: dict) -> float:
    """Source13 magnetar: base gravity + H(z) + B corrections + BH (M_BH="""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = term1 + term_BH + term2
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_MagnetarLambdaTerm(p: dict) -> float:
    """Source13 magnetar: cosmological constant Lambda/3 term"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (Lambda * c_light * c_light) / 3.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_MagnetarEMTerm(p: dict) -> float:
    """Source13 magnetar: electromagnetic v×B coupling (Lorentz force)"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = em_base * scale_EM
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_MagnetarGWTerm(p: dict) -> float:
    """Source13 magnetar: gravitational wave emission (spindown dΩ/dt)^2"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = gw_prefactor * (dOdt * dOdt)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_MagnetarQuantumTerm(p: dict) -> float:
    """Source13 magnetar: quantum uncertainty (ℏ/√(Δx·Δp)) contribution"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (hbar / sqrt_unc) * integral_psi * (2 * math.pi / t_Hubble)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_MagnetarFluidTerm(p: dict) -> float:
    """Source13 magnetar: magnetospheric fluid dynamics (ρ_fluid × V)"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (rho_fluid * V * ug1_base) / M
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_MagnetarOscillatoryTerm(p: dict) -> float:
    """Source13 magnetar: oscillatory perturbations (standing + traveling waves)"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = term_osc1 + term_osc2
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_MagnetarDarkMatterTerm(p: dict) -> float:
    """Source13 magnetar: dark matter halo + density perturbations (M_DM="""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = term_dm_force_like / M
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_MagnetarMagneticEnergyTerm(p: dict) -> float:
    """Source13 magnetar: magnetic field energy (B²/2μ₀ × V)"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = M_mag / (M * r)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_MagnetarDecayTerm(p: dict) -> float:
    """Source13 magnetar: cumulative decay energy (L₀ × τ_decay × [1-e^(-t/τ)])"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = cum_D / (M * r)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_Magnetar0501CoreTerm(p: dict) -> float:
    """Source14 SGR 0501+4516: base gravity + H(z) + B corrections + f_TRZ time-reversal factor"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = term1 + term2
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_Magnetar0501LambdaTerm(p: dict) -> float:
    """Source14 SGR 0501+4516: cosmological constant Lambda/3"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (Lambda * c_light * c_light) / 3.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_Magnetar0501EMTerm(p: dict) -> float:
    """Source14 SGR 0501+4516: scaled EM v×B with UA/SCm vacuum correction"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (em_base * corr_UA) * scale_EM
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_Magnetar0501GWTerm(p: dict) -> float:
    """Source14 SGR 0501+4516: gravitational wave (dΩ/dt)^2, P_init="""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = gw_prefactor * (dOdt * dOdt)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_Magnetar0501QuantumTerm(p: dict) -> float:
    """Source14 SGR 0501+4516: quantum uncertainty (ℏ/√(Δx·Δp))"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (hbar / sqrt_unc) * integral_psi * (2 * math.pi / t_Hubble)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_Magnetar0501FluidTerm(p: dict) -> float:
    """Source14 SGR 0501+4516: magnetospheric fluid (ρ_fluid × V × g / M)"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (rho_fluid * V * ug1_base) / M
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_Magnetar0501OscillatoryTerm(p: dict) -> float:
    """Source14 SGR 0501+4516: oscillatory waves (standing + traveling), A="""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = term_osc1 + term_osc2
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_Magnetar0501DarkMatterTerm(p: dict) -> float:
    """Source14 SGR 0501+4516: dark matter + density perturbations (M_DM="""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = term_dm_force_like / M
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_SgrAStarCoreTerm(p: dict) -> float:
    """Source15 Sgr A*: SMBH with M(t) growth + H(z) + B + f_TRZ (M_dot_0="""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = term1 + term2
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_SgrAStarLambdaTerm(p: dict) -> float:
    """Source15 Sgr A*: cosmological constant Lambda/3"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (Lambda * c_light * c_light) / 3.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_SgrAStarEMTerm(p: dict) -> float:
    """Source15 Sgr A*: EM v×B acceleration (no UA/SCm correction)"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (q_charge * cross_vB) / proton_mass
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_SgrAStarGWTerm(p: dict) -> float:
    """Source15 Sgr A*: GW from spin decay (spin="""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = gw_prefactor * (dOdt * dOdt)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_SgrAStarQuantumTerm(p: dict) -> float:
    """Source15 Sgr A*: quantum uncertainty (ℏ/√(Δx·Δp))"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (hbar / sqrt_unc) * integral_psi * (2 * math.pi / t_Hubble)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_SgrAStarFluidTerm(p: dict) -> float:
    """Source15 Sgr A*: accretion disk fluid dynamics (ρ_fluid × V)"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (rho_fluid * V * ug1_t) / Mt
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_SgrAStarOscillatoryTerm(p: dict) -> float:
    """Source15 Sgr A*: oscillatory waves (A="""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = term_osc1 + term_osc2
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_SgrAStarDarkMatterTerm(p: dict) -> float:
    """Source15 Sgr A*: DM + precession (angle="""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = term_dm_force_like / Mt
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_StarbirthCoreTerm(p: dict) -> float:
    """Source16 Starbirth: Base gravity with M(t) star formation, H(z), B corrections"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = ug1_t * corr_H * corr_B
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_StarbirthLambdaTerm(p: dict) -> float:
    """Source16 Starbirth: Lambda c^2 / 3"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (lambda_val * c_val * c_val) / 3.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_StarbirthUQFFTerm(p: dict) -> float:
    """Source16 Starbirth: UQFF Ug with f_TRZ"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (Ug1 + Ug2 + Ug3 + Ug4) * (1 + f_trz)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_StarbirthEMTerm(p: dict) -> float:
    """Source16 Starbirth: Scaled EM with UA correction"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (em_base * corr_UA) * em_scale
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_StarbirthQuantumTerm(p: dict) -> float:
    """Source16 Starbirth: Quantum uncertainty hbar/sqrt(dx*dp)"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (h_val / sqrt_unc) * psi_int * (2 * math.pi / t_hub)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_StarbirthFluidTerm(p: dict) -> float:
    """Source16 Starbirth: Fluid dynamics term"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (rho_f * V * ug1_t) / Mt
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_StarbirthOscillatoryTerm(p: dict) -> float:
    """Source16 Starbirth: Standing + traveling waves"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = term1 + term2
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_StarbirthDarkMatterTerm(p: dict) -> float:
    """Source16 Starbirth: DM + density perturbations (M_DM="""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = term_dm_force_like / Mt
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_StarbirthWindTerm(p: dict) -> float:
    """Source16 Starbirth: Stellar wind feedback (pressure/density)"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = wind_pressure / rho_f
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_Westerlund2CoreTerm(p: dict) -> float:
    """Source17 Westerlund2: Base gravity with M(t) star formation, H(z), B corrections"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = ug1_t * corr_H * corr_B
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_Westerlund2LambdaTerm(p: dict) -> float:
    """Source17 Westerlund2: Lambda c^2 / 3"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (lambda_val * c_val * c_val) / 3.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_Westerlund2UQFFTerm(p: dict) -> float:
    """Source17 Westerlund2: UQFF Ug with f_TRZ"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (Ug1 + Ug2 + Ug3 + Ug4) * (1 + f_trz)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_Westerlund2EMTerm(p: dict) -> float:
    """Source17 Westerlund2: Scaled EM with UA correction"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (em_base * corr_UA) * em_scale
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_Westerlund2QuantumTerm(p: dict) -> float:
    """Source17 Westerlund2: Quantum uncertainty hbar/sqrt(dx*dp)"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (h_val / sqrt_unc) * psi_int * (2 * math.pi / t_hub)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_Westerlund2FluidTerm(p: dict) -> float:
    """Source17 Westerlund2: Fluid dynamics term"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (rho_f * V * ug1_t) / Mt
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_Westerlund2OscillatoryTerm(p: dict) -> float:
    """Source17 Westerlund2: Standing + traveling waves"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = term1 + term2
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_Westerlund2DarkMatterTerm(p: dict) -> float:
    """Source17 Westerlund2: DM + density perturbations (M_DM="""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = term_dm_force_like / Mt
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_Westerlund2WindTerm(p: dict) -> float:
    """Source17 Westerlund2: Stellar wind feedback (pressure/density)"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = wind_pressure / rho_f
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_PillarsCoreTerm(p: dict) -> float:
    """Source18 Pillars: Base gravity with M(t), H(z), B, erosion E(t) corrections"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = ug1_t * corr_H * corr_B * corr_E
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_PillarsLambdaTerm(p: dict) -> float:
    """Source18 Pillars: Lambda c^2 / 3"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (lambda_val * c_val * c_val) / 3.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_PillarsUQFFTerm(p: dict) -> float:
    """Source18 Pillars: UQFF Ug with f_TRZ"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (Ug1 + Ug2 + Ug3 + Ug4) * (1 + f_trz)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_PillarsEMTerm(p: dict) -> float:
    """Source18 Pillars: Scaled EM with UA correction"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (em_base * corr_UA) * em_scale
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_PillarsQuantumTerm(p: dict) -> float:
    """Source18 Pillars: Quantum uncertainty hbar/sqrt(dx*dp)"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (h_val / sqrt_unc) * psi_int * (2 * math.pi / t_hub)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_PillarsFluidTerm(p: dict) -> float:
    """Source18 Pillars: Fluid dynamics term"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (rho_f * V * ug1_t) / Mt
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_PillarsOscillatoryTerm(p: dict) -> float:
    """Source18 Pillars: Standing + traveling waves"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = term1 + term2
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_PillarsDarkMatterTerm(p: dict) -> float:
    """Source18 Pillars: DM + density perturbations (M_DM="""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = term_dm_force_like / Mt
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_PillarsWindTerm(p: dict) -> float:
    """Source18 Pillars: Stellar wind feedback (pressure/density)"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = wind_pressure / rho_f
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_PillarsErosionTerm(p: dict) -> float:
    """Source18 Pillars: Photoevaporation erosion E(t) = E_0 * e^(-t/tau)"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = -Et * 1e-10
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_EinsteinRingCoreTerm(p: dict) -> float:
    """Source19 EinsteinRing: Base gravity with H(z), B, lensing amplification L(t)"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = ug1 * corr_H * corr_B * corr_L
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_EinsteinRingLambdaTerm(p: dict) -> float:
    """Source19 EinsteinRing: Lambda c^2 / 3"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (lambda_val * c_val * c_val) / 3.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_EinsteinRingUQFFTerm(p: dict) -> float:
    """Source19 EinsteinRing: UQFF Ug with f_TRZ"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (Ug1 + Ug2 + Ug3 + Ug4) * (1 + f_trz)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_EinsteinRingEMTerm(p: dict) -> float:
    """Source19 EinsteinRing: Scaled EM with UA correction"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (em_base * corr_UA) * em_scale
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_EinsteinRingQuantumTerm(p: dict) -> float:
    """Source19 EinsteinRing: Quantum uncertainty hbar/sqrt(dx*dp)"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (h_val / sqrt_unc) * psi_int * (2 * math.pi / t_hub)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_EinsteinRingFluidTerm(p: dict) -> float:
    """Source19 EinsteinRing: Fluid dynamics term"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (rho_f * V * ug1) / mass
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_EinsteinRingOscillatoryTerm(p: dict) -> float:
    """Source19 EinsteinRing: Standing + traveling waves"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = term1 + term2
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_EinsteinRingDarkMatterTerm(p: dict) -> float:
    """Source19 EinsteinRing: DM + density perturbations (M_DM="""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = term_dm_force_like / mass
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_EinsteinRingWindTerm(p: dict) -> float:
    """Source19 EinsteinRing: Intergalactic wind feedback"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = wind_pressure / rho_f
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_NGC2525CoreTerm(p: dict) -> float:
    """Source20 NGC2525: Base gravity with H(z), B corrections"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = ug1 * corr_H * corr_B
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_NGC2525BlackHoleTerm(p: dict) -> float:
    """Source20 NGC2525: Supermassive black hole acceleration (M_BH="""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (g_val * m_bh) / (r_bh_val * r_bh_val)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_NGC2525LambdaTerm(p: dict) -> float:
    """Source20 NGC2525: Lambda c^2 / 3"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (lambda_val * c_val * c_val) / 3.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_NGC2525UQFFTerm(p: dict) -> float:
    """Source20 NGC2525: UQFF Ug with f_TRZ"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (Ug1 + Ug2 + Ug3 + Ug4) * (1 + f_trz)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_NGC2525EMTerm(p: dict) -> float:
    """Source20 NGC2525: Scaled EM with UA correction"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (em_base * corr_UA) * em_scale
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_NGC2525QuantumTerm(p: dict) -> float:
    """Source20 NGC2525: Quantum uncertainty hbar/sqrt(dx*dp)"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (h_val / sqrt_unc) * psi_int * (2 * math.pi / t_hub)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_NGC2525FluidTerm(p: dict) -> float:
    """Source20 NGC2525: Fluid dynamics term"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (rho_f * V * ug1) / mass
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_NGC2525OscillatoryTerm(p: dict) -> float:
    """Source20 NGC2525: Standing + traveling waves"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = term1 + term2
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_NGC2525DarkMatterTerm(p: dict) -> float:
    """Source20 NGC2525: DM + density perturbations (M_DM="""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = term_dm_force_like / mass
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_NGC2525SupernovaTerm(p: dict) -> float:
    """Source20 NGC2525: Supernova mass loss M_SN(t) = M_SN0 * e^(-t/tau)"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = -(g_val * M_SNt) / (radius * radius)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_NGC3603CoreTerm(p: dict) -> float:
    """Source21 NGC3603: Base gravity with M(t) star formation, H(z), B corrections"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = ug1_t * corr_H * corr_B
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_NGC3603LambdaTerm(p: dict) -> float:
    """Source21 NGC3603: Lambda c^2 / 3"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (lambda_val * c_val * c_val) / 3.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_NGC3603UQFFTerm(p: dict) -> float:
    """Source21 NGC3603: UQFF Ug with f_TRZ"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (Ug1 + Ug2 + Ug3 + Ug4) * (1 + f_trz)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_NGC3603EMTerm(p: dict) -> float:
    """Source21 NGC3603: Scaled EM with UA correction"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (em_base * corr_UA) * em_scale
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_NGC3603QuantumTerm(p: dict) -> float:
    """Source21 NGC3603: Quantum uncertainty hbar/sqrt(dx*dp)"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (h_val / sqrt_unc) * psi_int * (2 * math.pi / t_hub)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_NGC3603FluidTerm(p: dict) -> float:
    """Source21 NGC3603: Fluid dynamics term"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (rho_f * V * ug1_t) / Mt
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_NGC3603OscillatoryTerm(p: dict) -> float:
    """Source21 NGC3603: Standing + traveling waves"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = term1 + term2
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_NGC3603DarkMatterTerm(p: dict) -> float:
    """Source21 NGC3603: DM + density perturbations (M_DM="""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = term_dm_force_like / Mt
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_NGC3603WindTerm(p: dict) -> float:
    """Source21 NGC3603: Stellar wind feedback (pressure/density)"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = wind_pressure / rho_f
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_NGC3603CavityPressureTerm(p: dict) -> float:
    """Source21 NGC3603: Cavity pressure P(t) = P0 * e^(-t/tau)"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = Pt / rho_f
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_BubbleNebulaCoreTerm(p: dict) -> float:
    """Source22 BubbleNebula: Core gravity with H(z), B, and E(t) expansion corrections"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = g_base * corr_H * corr_B * corr_E
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_BubbleNebulaLambdaTerm(p: dict) -> float:
    """Source22 BubbleNebula: Cosmological constant Lambda * c^2 / 3"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (Lambda * c_light * c_light) / 3.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_BubbleNebulaUQFFTerm(p: dict) -> float:
    """Source22 BubbleNebula: UQFF Ug with f_TRZ and E(t) correction"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (Ug1 + Ug4) * (1 + f_TRZ) * (1 - Et)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_BubbleNebulaEMTerm(p: dict) -> float:
    """Source22 BubbleNebula: Scaled EM with UA vacuum correction"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (em_base * corr_UA) * scale_EM
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_BubbleNebulaQuantumTerm(p: dict) -> float:
    """Source22 BubbleNebula: Quantum uncertainty hbar / sqrt(dx*dp)"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (hbar / sqrt_unc) * integral_psi * (2 * math.pi / t_Hubble)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_BubbleNebulaFluidTerm(p: dict) -> float:
    """Source22 BubbleNebula: Fluid dynamics (rho * V * g) / M"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (rho_fluid * V * g_base) / M
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_BubbleNebulaOscillatoryTerm(p: dict) -> float:
    """Source22 BubbleNebula: Standing + traveling wave perturbations"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = term1 + term2
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_BubbleNebulaDarkMatterTerm(p: dict) -> float:
    """Source22 BubbleNebula: DM (85%) + density perturbations"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = term_dm_force / M
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_BubbleNebulaWindTerm(p: dict) -> float:
    """Source22 BubbleNebula: Stellar wind feedback (rho * v^2) / rho_fluid"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = wind_pressure / rho_fluid
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_BubbleNebulaExpansionTerm(p: dict) -> float:
    """Source22 BubbleNebula: Expansion correction E(t) = E_0 * (1 - e^(-t/tau))"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = -g_base * Et
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_AntennaeGalaxiesCoreTerm(p: dict) -> float:
    """Source23 AntennaeGalaxies: Core gravity with M(t), H(z), B, and I(t) merger interaction"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = g_base * corr_H * corr_B * corr_I
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_AntennaeGalaxiesLambdaTerm(p: dict) -> float:
    """Source23 AntennaeGalaxies: Cosmological constant Lambda * c^2 / 3"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (Lambda * c_light * c_light) / 3.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_AntennaeGalaxiesUQFFTerm(p: dict) -> float:
    """Source23 AntennaeGalaxies: UQFF Ug with f_TRZ and I(t) merger interaction"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (Ug1 + Ug4) * (1 + f_TRZ) * (1 + It)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_AntennaeGalaxiesEMTerm(p: dict) -> float:
    """Source23 AntennaeGalaxies: Scaled EM with UA vacuum correction"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (em_base * corr_UA) * scale_EM
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_AntennaeGalaxiesQuantumTerm(p: dict) -> float:
    """Source23 AntennaeGalaxies: Quantum uncertainty hbar / sqrt(dx*dp)"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (hbar / sqrt_unc) * integral_psi * (2 * math.pi / t_Hubble)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_AntennaeGalaxiesFluidTerm(p: dict) -> float:
    """Source23 AntennaeGalaxies: Fluid dynamics (rho * V * g) / M(t)"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (rho_fluid * V * g_base) / Mt
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_AntennaeGalaxiesOscillatoryTerm(p: dict) -> float:
    """Source23 AntennaeGalaxies: Standing + traveling wave perturbations"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = term1 + term2
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_AntennaeGalaxiesDarkMatterTerm(p: dict) -> float:
    """Source23 AntennaeGalaxies: DM (85%) + density perturbations"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = term_dm_force / Mt
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_AntennaeGalaxiesWindTerm(p: dict) -> float:
    """Source23 AntennaeGalaxies: Stellar wind feedback (rho * v^2) / rho_fluid"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = wind_pressure / rho_fluid
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_AntennaeGalaxiesMergerInteractionTerm(p: dict) -> float:
    """Source23 AntennaeGalaxies: Merger interaction I(t) = I0 * e^(-t/tau_merger)"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = g_base * It
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_HorseheadNebulaCoreTerm(p: dict) -> float:
    """Source24 HorseheadNebula: Core gravity with H(z), B, and E(t) erosion corrections"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = g_base * corr_H * corr_B * corr_E
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_HorseheadNebulaLambdaTerm(p: dict) -> float:
    """Source24 HorseheadNebula: Cosmological constant Lambda * c^2 / 3"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (Lambda * c_light * c_light) / 3.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_HorseheadNebulaUQFFTerm(p: dict) -> float:
    """Source24 HorseheadNebula: UQFF Ug with f_TRZ and E(t) erosion correction"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (Ug1 + Ug4) * (1 + f_TRZ) * (1 - Et)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_HorseheadNebulaEMTerm(p: dict) -> float:
    """Source24 HorseheadNebula: Scaled EM with UA vacuum correction"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (em_base * corr_UA) * scale_EM
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_HorseheadNebulaQuantumTerm(p: dict) -> float:
    """Source24 HorseheadNebula: Quantum uncertainty hbar / sqrt(dx*dp)"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (hbar / sqrt_unc) * integral_psi * (2 * math.pi / t_Hubble)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_HorseheadNebulaFluidTerm(p: dict) -> float:
    """Source24 HorseheadNebula: Fluid dynamics (rho * V * g) / M"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (rho_fluid * V * g_base) / M
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_HorseheadNebulaOscillatoryTerm(p: dict) -> float:
    """Source24 HorseheadNebula: Standing + traveling wave perturbations"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = term1 + term2
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_HorseheadNebulaDarkMatterTerm(p: dict) -> float:
    """Source24 HorseheadNebula: DM (85%) + density perturbations"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = term_dm_force / M
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_HorseheadNebulaWindTerm(p: dict) -> float:
    """Source24 HorseheadNebula: Stellar wind feedback (rho * v^2) / rho_fluid"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = wind_pressure / rho_fluid
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_HorseheadNebulaErosionTerm(p: dict) -> float:
    """Source24 HorseheadNebula: Erosion correction E(t) = E_0 * (1 - e^(-t/tau))"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = -g_base * Et
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_NGC1275CoreTerm(p: dict) -> float:
    """Source25 NGC1275: Core gravity with H(z), B(t), and F(t) filament support"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = g_base * corr_H * corr_B * corr_F
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_NGC1275BlackHoleTerm(p: dict) -> float:
    """Source25 NGC1275: SMBH gravity term (800M Msun)"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = dpm_ug1_seed(M_BH, r_BH)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_NGC1275LambdaTerm(p: dict) -> float:
    """Source25 NGC1275: Cosmological constant Lambda * c^2 / 3"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (Lambda * c_light * c_light) / 3.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_NGC1275UQFFTerm(p: dict) -> float:
    """Source25 NGC1275: UQFF Ug with f_TRZ, B(t), and F(t) filament support"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (Ug1 + Ug4) * (1 + f_TRZ) * (1 + Ft)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_NGC1275EMTerm(p: dict) -> float:
    """Source25 NGC1275: Scaled EM with UA vacuum correction and B(t)"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (em_base * corr_UA) * scale_EM
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_NGC1275QuantumTerm(p: dict) -> float:
    """Source25 NGC1275: Quantum uncertainty hbar / sqrt(dx*dp)"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (hbar / sqrt_unc) * integral_psi * (2 * math.pi / t_Hubble)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_NGC1275FluidTerm(p: dict) -> float:
    """Source25 NGC1275: Fluid dynamics (rho * V * g) / M"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (rho_fluid * V * g_base) / M
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_NGC1275OscillatoryTerm(p: dict) -> float:
    """Source25 NGC1275: Standing + traveling wave perturbations"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = term1 + term2
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_NGC1275DarkMatterTerm(p: dict) -> float:
    """Source25 NGC1275: DM (85%) + density perturbations"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = term_dm_force / M
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_NGC1275CoolingFlowTerm(p: dict) -> float:
    """Source25 NGC1275: Cooling flow (rho_cool * v_cool^2) / rho_fluid"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = cool_pressure / rho_fluid
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_NGC1275FilamentSupportTerm(p: dict) -> float:
    """Source25 NGC1275: Filament support F(t) = F0 * e^(-t/tau_fil)"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = g_base * Ft
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_HUDFGalaxiesCoreTerm(p: dict) -> float:
    """Source26 HUDFGalaxies: Core gravity with M(t), H(z), B, and I(t) interactions"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = g_base * corr_H * corr_B * corr_I
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_HUDFGalaxiesLambdaTerm(p: dict) -> float:
    """Source26 HUDFGalaxies: Cosmological constant Lambda * c^2 / 3"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (Lambda * c_light * c_light) / 3.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_HUDFGalaxiesUQFFTerm(p: dict) -> float:
    """Source26 HUDFGalaxies: UQFF Ug with f_TRZ and I(t) interactions"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (Ug1 + Ug4) * (1 + f_TRZ) * (1 + It)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_HUDFGalaxiesEMTerm(p: dict) -> float:
    """Source26 HUDFGalaxies: Scaled EM with UA vacuum correction"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (em_base * corr_UA) * scale_EM
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_HUDFGalaxiesQuantumTerm(p: dict) -> float:
    """Source26 HUDFGalaxies: Quantum uncertainty hbar / sqrt(dx*dp)"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (hbar / sqrt_unc) * integral_psi * (2 * math.pi / t_Hubble)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_HUDFGalaxiesFluidTerm(p: dict) -> float:
    """Source26 HUDFGalaxies: Fluid dynamics (rho * V * g) / M(t)"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (rho_fluid * V * g_base) / Mt
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_HUDFGalaxiesOscillatoryTerm(p: dict) -> float:
    """Source26 HUDFGalaxies: Standing + traveling wave perturbations"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = term1 + term2
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_HUDFGalaxiesDarkMatterTerm(p: dict) -> float:
    """Source26 HUDFGalaxies: DM (85%) + density perturbations"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = term_dm_force / Mt
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_HUDFGalaxiesWindTerm(p: dict) -> float:
    """Source26 HUDFGalaxies: Stellar wind feedback (rho * v^2) / rho_fluid"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = wind_pressure / rho_fluid
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_HUDFGalaxiesInteractionTerm(p: dict) -> float:
    """Source26 HUDFGalaxies: Galaxy interaction I(t) = I0 * e^(-t/tau_inter)"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = g_base * It
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_NGC1792CoreTerm(p: dict) -> float:
    """Source27 NGC1792: Core gravity with M(t) starburst, H(z), and B"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = g_base * corr_H * corr_B
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_NGC1792LambdaTerm(p: dict) -> float:
    """Source27 NGC1792: Cosmological constant Lambda * c^2 / 3"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (Lambda * c_light * c_light) / 3.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_NGC1792UQFFTerm(p: dict) -> float:
    """Source27 NGC1792: UQFF Ug with f_TRZ and M(t) starburst"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (Ug1 + Ug4) * (1 + f_TRZ)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_NGC1792EMTerm(p: dict) -> float:
    """Source27 NGC1792: Scaled EM with UA vacuum correction"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (em_base * corr_UA) * scale_EM
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_NGC1792QuantumTerm(p: dict) -> float:
    """Source27 NGC1792: Quantum uncertainty hbar / sqrt(dx*dp)"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (hbar / sqrt_unc) * integral_psi * (2 * math.pi / t_Hubble)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_NGC1792FluidTerm(p: dict) -> float:
    """Source27 NGC1792: Fluid dynamics (rho * V * g) / M(t)"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (rho_fluid * V * g_base) / Mt
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_NGC1792OscillatoryTerm(p: dict) -> float:
    """Source27 NGC1792: Standing + traveling wave perturbations"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = term1 + term2
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_NGC1792DarkMatterTerm(p: dict) -> float:
    """Source27 NGC1792: DM (85%) + density perturbations"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = term_dm_force / Mt
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_NGC1792SupernovaFeedbackTerm(p: dict) -> float:
    """Source27 NGC1792: Supernova feedback (rho * v^2) / rho_fluid"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = wind_pressure / rho_fluid
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_AndromedaCoreTerm(p: dict) -> float:
    """Source28 Andromeda: Core gravity with H(z) blueshift and B"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = g_base * corr_H * corr_B
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_AndromedaBlackHoleTerm(p: dict) -> float:
    """Source28 Andromeda: SMBH gravity term (140M Msun)"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = dpm_ug1_seed(M_BH, r_BH)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_AndromedaLambdaTerm(p: dict) -> float:
    """Source28 Andromeda: Cosmological constant Lambda * c^2 / 3"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (Lambda * c_light * c_light) / 3.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_AndromedaUQFFTerm(p: dict) -> float:
    """Source28 Andromeda: UQFF Ug with f_TRZ"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (Ug1 + Ug4) * (1 + f_TRZ)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_AndromedaEMTerm(p: dict) -> float:
    """Source28 Andromeda: Scaled EM with UA vacuum correction"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (em_base * corr_UA) * scale_EM
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_AndromedaQuantumTerm(p: dict) -> float:
    """Source28 Andromeda: Quantum uncertainty hbar / sqrt(dx*dp)"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (hbar / sqrt_unc) * integral_psi * (2 * math.pi / t_Hubble)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_AndromedaFluidTerm(p: dict) -> float:
    """Source28 Andromeda: Fluid dynamics (rho * V * g) / M"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (rho_fluid * V * g_base) / M
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_AndromedaOscillatoryTerm(p: dict) -> float:
    """Source28 Andromeda: Standing + traveling wave perturbations"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = term1 + term2
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_AndromedaDarkMatterTerm(p: dict) -> float:
    """Source28 Andromeda: DM (80%) + density perturbations"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = term_dm_force / M
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_SombreroCoreTerm(p: dict) -> float:
    """Source29 Sombrero: Core gravity with H(z=0.0063) and B superconductivity"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = g_base * corr_H * corr_B
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_SombreroBlackHoleTerm(p: dict) -> float:
    """Source29 Sombrero: SMBH gravity term (1 billion Msun)"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = dpm_ug1_seed(M_BH, r_BH)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_SombreroLambdaTerm(p: dict) -> float:
    """Source29 Sombrero: Cosmological constant Lambda * c^2 / 3"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (Lambda * c_light * c_light) / 3.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_SombreroUQFFTerm(p: dict) -> float:
    """Source29 Sombrero: UQFF Ug with f_TRZ and superconductivity"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (Ug1 + Ug4) * (1 + f_TRZ)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_SombreroEMTerm(p: dict) -> float:
    """Source29 Sombrero: Scaled EM with UA vacuum correction"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (em_base * corr_UA) * scale_EM
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_SombreroQuantumTerm(p: dict) -> float:
    """Source29 Sombrero: Quantum uncertainty hbar / sqrt(dx*dp)"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (hbar / sqrt_unc) * integral_psi * (2 * math.pi / t_Hubble)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_SombreroFluidTerm(p: dict) -> float:
    """Source29 Sombrero: Fluid dynamics (rho * V * g) / M"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (rho_fluid * V * g_base) / M
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_SombreroOscillatoryTerm(p: dict) -> float:
    """Source29 Sombrero: Standing + traveling wave perturbations"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = term1 + term2
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_SombreroDarkMatterTerm(p: dict) -> float:
    """Source29 Sombrero: DM (20%) + density perturbations"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = term_dm_force / M
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_SombreroDustTerm(p: dict) -> float:
    """Source29 Sombrero: Dust drag D_dust (prominent dust lane)"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = D_dust
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_SaturnCoreTerm(p: dict) -> float:
    """Source30 Saturn: Core gravity (Saturn + Sun) with B superconductivity"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (g_saturn + g_sun) * corr_B
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_SaturnLambdaTerm(p: dict) -> float:
    """Source30 Saturn: Cosmological constant Lambda * c^2 / 3"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (Lambda * c_light * c_light) / 3.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_SaturnUQFFTerm(p: dict) -> float:
    """Source30 Saturn: UQFF Ug with f_TRZ"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (Ug1 + Ug4) * (1 + f_TRZ)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_SaturnEMTerm(p: dict) -> float:
    """Source30 Saturn: Scaled EM with UA vacuum correction"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (em_base * corr_UA) * scale_EM
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_SaturnQuantumTerm(p: dict) -> float:
    """Source30 Saturn: Quantum uncertainty hbar / sqrt(dx*dp)"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (hbar / sqrt_unc) * integral_psi * (2 * math.pi / t_Hubble)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_SaturnFluidTerm(p: dict) -> float:
    """Source30 Saturn: Fluid dynamics (rho * V * g) / M"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (rho_fluid * V * g_base) / M
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_SaturnOscillatoryTerm(p: dict) -> float:
    """Source30 Saturn: Standing + traveling wave perturbations"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = term1 + term2
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_SaturnDarkMatterTerm(p: dict) -> float:
    """Source30 Saturn: Visible mass with density perturbations (M_DM=0)"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = term_dm_force / M
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_SaturnRingTidalTerm(p: dict) -> float:
    """Source30 Saturn: Ring tidal force G * M_ring / r_ring^2"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = dpm_ug1_seed(M_ring, r_ring)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_SaturnWindTerm(p: dict) -> float:
    """Source30 Saturn: Atmospheric wind v_wind^2 * 1e-12"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = v_wind * v_wind * wind_scale
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_M16CoreTerm(p: dict) -> float:
    """Source31 M16: Core gravity with M(t) star formation and erosion"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = g_base * corr_H * corr_B
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_M16LambdaTerm(p: dict) -> float:
    """Source31 M16: Cosmological constant Lambda * c^2 / 3"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (Lambda * c_light * c_light) / 3.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_M16UQFFTerm(p: dict) -> float:
    """Source31 M16: UQFF Ug with f_TRZ and M(t)"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (Ug1 + Ug4) * (1 + f_TRZ)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_M16EMTerm(p: dict) -> float:
    """Source31 M16: Scaled EM with UA vacuum correction (v_gas=100 km/s)"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (em_base * corr_UA) * scale_EM
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_M16QuantumTerm(p: dict) -> float:
    """Source31 M16: Quantum uncertainty hbar / sqrt(dx*dp)"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (hbar / sqrt_unc) * integral_psi * (2 * math.pi / t_Hubble)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_M16FluidTerm(p: dict) -> float:
    """Source31 M16: Fluid dynamics with M(t)"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (rho_fluid * V * g_base) / M_t
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_M16OscillatoryTerm(p: dict) -> float:
    """Source31 M16: Standing + traveling wave perturbations"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = term1 + term2
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_M16DarkMatterTerm(p: dict) -> float:
    """Source31 M16: Visible mass with density perturbations (M_DM=0)"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = term_dm_force / M_t
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_M16StarFormationTerm(p: dict) -> float:
    """Source31 M16: Star formation M_sf(t) = (SFR * t) / M0"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = M_sf
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_M16RadiationErosionTerm(p: dict) -> float:
    """Source31 M16: Radiation erosion E_rad(t) = E0 * (1 - exp(-t/tau))"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = E0 * (1 - math.exp(-t / tau_erode))
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_CrabCoreTerm(p: dict) -> float:
    """Source32 Crab: Core gravity with r(t) expansion"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = g_base * corr_H * corr_B
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_CrabLambdaTerm(p: dict) -> float:
    """Source32 Crab: Cosmological constant Lambda * c^2 / 3"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (Lambda * c_light * c_light) / 3.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_CrabUQFFTerm(p: dict) -> float:
    """Source32 Crab: UQFF Ug with f_TRZ and r(t)"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (Ug1 + Ug4) * (1 + f_TRZ)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_CrabEMTerm(p: dict) -> float:
    """Source32 Crab: Scaled EM with UA correction (v_shock=1500 km/s)"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (em_base * corr_UA) * scale_EM
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_CrabQuantumTerm(p: dict) -> float:
    """Source32 Crab: Quantum uncertainty hbar / sqrt(dx*dp)"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (hbar / sqrt_unc) * integral_psi * (2 * math.pi / t_Hubble)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_CrabFluidTerm(p: dict) -> float:
    """Source32 Crab: Fluid dynamics with r(t)"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (rho_fluid * V * g_base) / M
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_CrabOscillatoryTerm(p: dict) -> float:
    """Source32 Crab: Standing + traveling wave perturbations"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = term1 + term2
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_CrabDarkMatterTerm(p: dict) -> float:
    """Source32 Crab: Visible mass with density perturbations (M_DM=0)"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = term_dm_force / M
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_CrabPulsarWindTerm(p: dict) -> float:
    """Source32 Crab: Pulsar wind a_wind = [P/(4πr²) * (1+v/c)] / ρ"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = a_wind * scale_wind
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_CrabMagneticTerm(p: dict) -> float:
    """Source32 Crab: Magnetic M_mag = (q * v_shock * B) / m_e"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (q * v_shock * B / m_e) * scale_mag
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_SGR1745CoreTerm(p: dict) -> float:
    """Source33 SGR1745: Core gravity with extreme B superconductivity"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = g_base * corr_H * corr_B
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_SGR1745LambdaTerm(p: dict) -> float:
    """Source33 SGR1745: Cosmological constant Lambda * c^2 / 3"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (Lambda * c_light * c_light) / 3.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_SGR1745UQFFTerm(p: dict) -> float:
    """Source33 SGR1745: UQFF Ug with f_TRZ and extreme B"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (Ug1 + Ug4) * (1 + f_TRZ)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_SGR1745EMTerm(p: dict) -> float:
    """Source33 SGR1745: Scaled EM with extreme B=2e10 T"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (em_base * corr_UA) * scale_EM
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_SGR1745QuantumTerm(p: dict) -> float:
    """Source33 SGR1745: Quantum uncertainty hbar / sqrt(dx*dp)"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (hbar / sqrt_unc) * integral_psi * (2 * math.pi / t_Hubble)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_SGR1745FluidTerm(p: dict) -> float:
    """Source33 SGR1745: Crust dynamics rho_crust = 1e17 kg/m^3"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (rho_crust * V * g_base) / M
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_SGR1745OscillatoryTerm(p: dict) -> float:
    """Source33 SGR1745: Pulsations omega=1.67 rad/s (P=3.76s)"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = term1 + term2
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_SGR1745DarkMatterTerm(p: dict) -> float:
    """Source33 SGR1745: Visible mass with density perturbations (M_DM=0)"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = term_dm_force / M
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_SGR1745_DPMTerm(p: dict) -> float:
    """Source34 SGR1745-UQFF: DPM resonance a_DPM = (I/A) * (ω₁-ω₂)"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (I / A) * (omega_1 - omega_2)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_SGR1745_THzTerm(p: dict) -> float:
    """Source34 SGR1745-UQFF: THz pipeline a_THz = (f·E_vac·v·a_DPM)/(E_ISM·c)"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (f_THz * E_vac_neb * v_exp * a_DPM) / (E_vac_ISM * c_light)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_SGR1745_VacDiffTerm(p: dict) -> float:
    """Source34 SGR1745-UQFF: Vacuum differential a_vac_diff"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (E0 * f_vac_diff * V_sys) / (h * f_vac_diff) * a_DPM
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_SGR1745_SuperFreqTerm(p: dict) -> float:
    """Source34 SGR1745-UQFF: Superconductor frequency a_super_freq"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (h * f_super * f_DPM * a_DPM) / (E_vac_ISM * c_light)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_SGR1745_AetherResTerm(p: dict) -> float:
    """Source34 SGR1745-UQFF: Aether resonance a_aether_res"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = f_aether * 1e-8 * f_DPM * (1 + f_TRZ) * a_DPM
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_SGR1745_Ug4iTerm(p: dict) -> float:
    """Source34 SGR1745-UQFF: Reactive U_g4i term"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (f_sc * Ug1 * f_react * a_DPM) / (E_vac_ISM * c_light)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_SGR1745_QuantumFreqTerm(p: dict) -> float:
    """Source34 SGR1745-UQFF: Quantum frequency a_quantum_freq"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (f_quantum * E_vac_neb * a_DPM) / (E_vac_ISM * c_light)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_SGR1745_AetherFreqTerm(p: dict) -> float:
    """Source34 SGR1745-UQFF: Aether frequency a_Aether_freq"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (f_Aether * E_vac_neb * a_DPM) / (E_vac_ISM * c_light)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_SGR1745_FluidFreqTerm(p: dict) -> float:
    """Source34 SGR1745-UQFF: Fluid frequency a_fluid_freq"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (f_fluid * E_vac_neb * V_sys) / (E_vac_ISM * c_light)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_SGR1745_ExpFreqTerm(p: dict) -> float:
    """Source34 SGR1745-UQFF: Expansion frequency a_exp_freq"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (f_exp * E_vac_neb * a_DPM) / (E_vac_ISM * c_light)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_SgrA_DPMTerm(p: dict) -> float:
    """Source35 SgrA*-UQFF: DPM resonance (scaled for SMBH)"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (I / A) * (omega_1 - omega_2)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_SgrA_THzTerm(p: dict) -> float:
    """Source35 SgrA*-UQFF: THz pipeline (f=1e9 Hz for SMBH)"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (f_THz * E_vac_neb * v_exp * a_DPM) / (E_vac_ISM * c_light)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_SgrA_VacDiffTerm(p: dict) -> float:
    """Source35 SgrA*-UQFF: Vacuum differential"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (E0 * f_vac_diff * V_sys) / (h * f_vac_diff) * a_DPM
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_SgrA_SuperFreqTerm(p: dict) -> float:
    """Source35 SgrA*-UQFF: Superconductor frequency"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (h * f_super * f_DPM * a_DPM) / (E_vac_ISM * c_light)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_SgrA_AetherResTerm(p: dict) -> float:
    """Source35 SgrA*-UQFF: Aether resonance"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = f_aether * 1e-8 * f_DPM * (1 + f_TRZ) * a_DPM
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_SgrA_Ug4iTerm(p: dict) -> float:
    """Source35 SgrA*-UQFF: Reactive U_g4i"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (f_sc * Ug1 * f_react * a_DPM) / (E_vac_ISM * c_light)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_SgrA_QuantumFreqTerm(p: dict) -> float:
    """Source35 SgrA*-UQFF: Quantum frequency"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (f_quantum * E_vac_neb * a_DPM) / (E_vac_ISM * c_light)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_SgrA_AetherFreqTerm(p: dict) -> float:
    """Source35 SgrA*-UQFF: Aether frequency"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (f_Aether * E_vac_neb * a_DPM) / (E_vac_ISM * c_light)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_SgrA_FluidFreqTerm(p: dict) -> float:
    """Source35 SgrA*-UQFF: Fluid frequency"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (f_fluid * E_vac_neb * V_sys) / (E_vac_ISM * c_light)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_SgrA_ExpFreqTerm(p: dict) -> float:
    """Source35 SgrA*-UQFF: Expansion frequency"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (f_exp * E_vac_neb * a_DPM) / (E_vac_ISM * c_light)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_YoungStarsCore(p: dict) -> float:
    """Source36 YoungStars: Core gravity with star formation mass factor M_SF(t)"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (dpm_ug1_seed(M, r)) * (1.0 + M_SF) * expansion * sc_correction
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_YoungStarsLambda(p: dict) -> float:
    """Source36 YoungStars: Cosmological constant term"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = Lambda * c * c / 3.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_YoungStarsUQFF(p: dict) -> float:
    """Source36 YoungStars: UQFF sum (Ug1-4) with outflow velocity SC term"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = Ug1 + Ug2 + Ug3 + Ug4
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_YoungStarsEM(p: dict) -> float:
    """Source36 YoungStars: EM Lorentz with vacuum ratio correction"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (q * v_out * B / m_p) * vac_ratio
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_YoungStarsQuantum(p: dict) -> float:
    """Source36 YoungStars: Quantum uncertainty term"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (hbar / unc) * integral_psi * (2.0 * 3.141592653589793 / t_Hubble)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_YoungStarsFluid(p: dict) -> float:
    """Source36 YoungStars: Fluid dynamics term"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = rho_fluid * V * g_base
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_YoungStarsOscillatory(p: dict) -> float:
    """Source36 YoungStars: Resonant oscillatory term (standing + traveling)"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = standing + traveling_real
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_YoungStarsDarkMatter(p: dict) -> float:
    """Source36 YoungStars: Dark matter perturbation term"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (M_visible + M_DM) * (delta_rho / rho + 3.0 * G * M / (r * r * r))
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_YoungStarsOutflowPressure(p: dict) -> float:
    """Source36 YoungStars: Outflow pressure P=rho*v^2*(1+t/tau) UNIQUE"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = rho_fluid * v_out * v_out * (1.0 + t / t_evolve)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_YoungStarsStarFormation(p: dict) -> float:
    """Source36 YoungStars: Star formation mass factor M_SF=SFR*t/M UNIQUE"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (SFR * t_yr) / M
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_BigBangCore(p: dict) -> float:
    """Source37 BigBang: Core gravity with M(t)=M_total*(t/t_H), r(t)=c*t"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (dpm_ug1_seed(M_t, r_t)) * expansion * sc_correction
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_BigBangLambda(p: dict) -> float:
    """Source37 BigBang: Cosmological constant term"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = Lambda * c * c / 3.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_BigBangUg1(p: dict) -> float:
    """Source37 BigBang: Ug1 = G*M(t)/r(t)^2"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (G * M_t) / (r_t * r_t)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_BigBangUg4(p: dict) -> float:
    """Source37 BigBang: Ug4 reaction = Ug1 * f_sc"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = Ug1 * f_sc
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_BigBangQuantum(p: dict) -> float:
    """Source37 BigBang: Quantum uncertainty term"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (hbar / unc) * integral_psi * (2.0 * 3.141592653589793 / t_Hubble)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_BigBangFluid(p: dict) -> float:
    """Source37 BigBang: Fluid dynamics term"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = rho_fluid * V * g_base
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_BigBangOscillatory(p: dict) -> float:
    """Source37 BigBang: Oscillatory resonance term"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = standing + traveling_real
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_BigBangDarkMatter(p: dict) -> float:
    """Source37 BigBang: Dark matter fractional term"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = DM_fraction * g_base
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_BigBangQuantumGravity(p: dict) -> float:
    """Source37 BigBang: Quantum gravity QG=(hbar*c/l_p^2)*(t/t_p) UNIQUE"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (hbar * c / (l_p * l_p)) * (t / t_p)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_BigBangGravitationalWave(p: dict) -> float:
    """Source37 BigBang: Gravitational wave GW=h*c^2/lambda*sin(...) UNIQUE"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = h_strain * (c * c) / lambda_gw * math.math.sin(phase)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_BigBangCosmicEvolution(p: dict) -> float:
    """Source37 BigBang: Cosmic evolution z(t)=t_H/t-1 (redshift proxy) UNIQUE"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = z_t
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_M51Core(p: dict) -> float:
    """Source38 M51: Core gravity with M_SF + expansion + SC + F_env(tidal+SF)"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (dpm_ug1_seed(M, r)) * (1.0 + M_SF) * expansion * sc_correction * (1.0 + F_env)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_M51Ug1Dipole(p: dict) -> float:
    """Source38 M51: Ug1 = I*A*omega*B (BH magnetic dipole) UNIQUE"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = I_dipole * A_dipole * omega_spin * B
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_M51Ug2Superconductor(p: dict) -> float:
    """Source38 M51: Ug2 = B^2/(2*mu_0) (superconductor term) UNIQUE"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (B * B) / (2.0 * mu_0)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_M51Ug3Tidal(p: dict) -> float:
    """Source38 M51: Ug3' = G*M_NGC5195/d^2 (external tidal) UNIQUE"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (G * M_NGC5195) / (d * d)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_M51Ug4Reaction(p: dict) -> float:
    """Source38 M51: Ug4 = k_4*E_react*exp(-0.0005*t) (reaction decay) UNIQUE"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = k_4 * E_react
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_M51UiVacuum(p: dict) -> float:
    """Source38 M51: Ui vacuum interaction = lambda*(rho_SCm/rho_UA)*omega*cos(pi*t) UNIQUE"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = lambda_I * (rho_SCm / rho_UA) * omega_i * math.math.cos(3.141592653589793 * t_n) * (1.0 + F_RZ)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_M51Lambda(p: dict) -> float:
    """Source38 M51: Cosmological constant term"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = Lambda * c * c / 3.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_M51Quantum(p: dict) -> float:
    """Source38 M51: Quantum term with psi_spiral wave function UNIQUE"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (hbar / unc) * integral_psi * (2.0 * 3.141592653589793 / t_Hubble)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_M51Fluid(p: dict) -> float:
    """Source38 M51: Fluid dynamics term"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = rho_fluid * V * g_base
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_M51DarkMatter(p: dict) -> float:
    """Source38 M51: Dark matter with curvature term"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (M_visible + M_DM) * (delta_rho / rho + curvature)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_M51EnvironmentalForces(p: dict) -> float:
    """Source38 M51: F_env = F_tidal + F_SF (environmental forces) UNIQUE"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = F_tidal + F_SF
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_NGC1316Core(p: dict) -> float:
    """Source39 NGC1316: Core with M_merge(t)=1e10*exp(-t/tau) merger mass UNIQUE"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (dpm_ug1_seed(M, r)) * m_factor * expansion * sc_correction
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_NGC1316Ug1Dipole(p: dict) -> float:
    """Source39 NGC1316: Ug1 BH magnetic dipole (M_BH=1.5e9 Msun)"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = I_dipole * A_dipole * omega_spin * B
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_NGC1316Ug2Superconductor(p: dict) -> float:
    """Source39 NGC1316: Ug2 superconductor magnetic energy"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (B * B) / (2.0 * mu_0)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_NGC1316Ug3External(p: dict) -> float:
    """Source39 NGC1316: Ug3 external tidal from spiral companion"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (G * M_spiral) / (d * d)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_NGC1316Ug4Reaction(p: dict) -> float:
    """Source39 NGC1316: Ug4 reaction term"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = k_4 * E_react
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_NGC1316UiVacuum(p: dict) -> float:
    """Source39 NGC1316: Ui vacuum interaction term"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = lambda_I * (rho_SCm / rho_UA) * omega_i * math.math.cos(3.141592653589793 * t) * (1.0 + F_RZ)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_NGC1316Lambda(p: dict) -> float:
    """Source39 NGC1316: Cosmological constant"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = Lambda * c * c / 3.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_NGC1316Quantum(p: dict) -> float:
    """Source39 NGC1316: Quantum with psi_dust wave function UNIQUE"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (hbar / unc) * psi_dust_sq * (2.0 * 3.141592653589793 / t_Hubble)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_NGC1316FluidDust(p: dict) -> float:
    """Source39 NGC1316: Fluid with rho_dust density UNIQUE"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = rho_dust * V * g_base
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_NGC1316DarkMatter(p: dict) -> float:
    """Source39 NGC1316: Dark matter term"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (M_visible + M_DM) * (delta_rho / rho + 3.0 * G * M / (r * r * r))
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_NGC1316MergerForces(p: dict) -> float:
    """Source39 NGC1316: Merger forces = F_tidal + F_cluster UNIQUE"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = F_tidal + F_cluster
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_SMBHBinaryDPMResonance(p: dict) -> float:
    """Source40 SMBHBinary: DPM core resonance f_DPM*rho_vac/c UNIQUE"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = f_DPM * rho_vac_plasm / c
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_SMBHBinaryTHzResonance(p: dict) -> float:
    """Source40 SMBHBinary: THz pipeline resonance f_THz*sin(omega*t) UNIQUE"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = f_THz * math.math.sin(omega * t)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_SMBHBinaryUg4iResonance(p: dict) -> float:
    """Source40 SMBHBinary: U_g4i reactive resonance UNIQUE"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = f_react * lambda_I * (1.0 + f_TRZ) * math.math.cos(omega * t)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_SMBHBinaryPlasmoticVacuum(p: dict) -> float:
    """Source40 SMBHBinary: Plasmotic vacuum energy UNIQUE"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = lambda_I * rho_vac_plasm
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_SMBHBinaryQuantumResonance(p: dict) -> float:
    """Source40 SMBHBinary: Quantum resonance f_quantum/unc UNIQUE"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = f_quantum / unc
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_SMBHBinaryFluidResonance(p: dict) -> float:
    """Source40 SMBHBinary: Fluid resonance f_fluid*(rho/rho_ref) UNIQUE"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = f_fluid * (rho / rho_ref)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_SMBHBinaryOscillatoryResonance(p: dict) -> float:
    """Source40 SMBHBinary: Oscillatory resonance f*exp(-t/tau)*|psi|^2 UNIQUE"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = f_super * math.math.exp(-t / t_coal) * psi_sq
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_SMBHBinaryExpansionResonance(p: dict) -> float:
    """Source40 SMBHBinary: Cosmic expansion resonance (Aether freq) UNIQUE"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = f_Aether
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_SMBHBinaryCoalescence(p: dict) -> float:
    """Source40 SMBHBinary: Binary coalescence dynamics (2PN waveform) UNIQUE"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (dpm_ug1_seed(M_chirp, c)) * tau_factor
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_BackgroundAetherMinkowski(p: dict) -> float:
    """Source41 BackgroundAether: Minkowski metric g_μν=[1,-1,-1,-1] UNIQUE"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_BackgroundAetherPerturbedMetric(p: dict) -> float:
    """Source41 BackgroundAether: Perturbed metric A=g+eta*T_s UNIQUE"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = eta * T_s
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_BackgroundAetherStressEnergy(p: dict) -> float:
    """Source41 BackgroundAether: Stress-energy tensor T_s=1.123e7 J/m³ UNIQUE"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = T_s
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_BackgroundAetherCoupling(p: dict) -> float:
    """Source41 BackgroundAether: Coupling parameter eta~1e-22 UNIQUE"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = eta
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_HeavisideFraction(p: dict) -> float:
    """Source42 Heaviside: f_Heaviside = 0.01 (unitless threshold fraction)"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = f_Heaviside
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_HeavisideFactor(p: dict) -> float:
    """Source42 Heaviside: (1 + 10^13 × f_H) = 1 + 1e11 amplification UNIQUE"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1.0 + scale * f_Heaviside
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_HeavisideUmBase(p: dict) -> float:
    """Source42 Heaviside: U_m base = (μ/r)(1-e^(-γt cos(πt_n)))φ̂·P_SCm·E_react"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (mu_j / r_j) * one_minus_exp * phi_hat_j * P_SCm * E_react
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_HeavisideUmWithHeaviside(p: dict) -> float:
    """Source42 Heaviside: U_m × (1+10^13·f_H) × (1+f_quasi) UNIQUE amplification"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = base * heaviside_factor * quasi_factor
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_HeavisideUmWithoutHeaviside(p: dict) -> float:
    """Source42 Heaviside: U_m without Heaviside (baseline comparison)"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = base * quasi_factor
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_HeavisideQuasiFactor(p: dict) -> float:
    """Source42 Heaviside: (1 + f_quasi) quasi-scaling factor"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1.0 + f_quasi
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_HeavisideExponentialDecay(p: dict) -> float:
    """Source42 Heaviside: (1 - e^(-γt cos(πt_n))) time-modulated decay"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1.0 - math.math.exp(exp_arg)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_HeavisideMagneticMoment(p: dict) -> float:
    """Source42 Heaviside: μ_j/r_j magnetic moment density"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = mu_j / r_j
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_HeavisideAmplificationRatio(p: dict) -> float:
    """Source42 Heaviside: Amplification ratio ~1e11 UNIQUE threshold effect"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = heaviside_factor
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_HydrogenResonance_Amplitude(p: dict) -> float:
    """Source43 HydrogenRes: Nuclear resonance amplitude A_res for PToE UNIQUE"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = A_res
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_HydrogenResonance_Frequency(p: dict) -> float:
    """Source43 HydrogenRes: Resonance frequency f_res from binding energy UNIQUE"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = f_res
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_HydrogenResonance_DeepPairing(p: dict) -> float:
    """Source43 HydrogenRes: Deep pairing U_dp between nuclear components UNIQUE"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = U_dp
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_HydrogenResonance_NuclearCoupling(p: dict) -> float:
    """Source43 HydrogenRes: Nuclear coupling k_nuc with N/Z ratio UNIQUE"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = k_nuc
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_HydrogenResonance_ShellCorrection(p: dict) -> float:
    """Source43 HydrogenRes: Shell correction S_shell from magic numbers UNIQUE"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = S_shell
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_HydrogenResonance_Buoyancy(p: dict) -> float:
    """Source43 HydrogenRes: Enhanced buoyancy with nuclear pairing+magic UNIQUE"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = buoyancy
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_HydrogenResonance_Superconductive(p: dict) -> float:
    """Source43 HydrogenRes: SC with quantum coherence+tunneling UNIQUE"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = SC_enhanced
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_HydrogenResonance_PairingEnergy(p: dict) -> float:
    """Source43 HydrogenRes: Pairing energy δ_pair for even-odd nuclei UNIQUE"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = pairing_term
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_HydrogenResonance_MagicStability(p: dict) -> float:
    """Source43 HydrogenRes: Magic number stability enhancement UNIQUE"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = stability_factor
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_HydrogenResonance_TunnelingProb(p: dict) -> float:
    """Source43 HydrogenRes: Coulomb barrier tunneling probability UNIQUE"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = tunneling_prob
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_HydrogenResonance_Integrand(p: dict) -> float:
    """Source43 HydrogenRes: Full H_res integrand formula UNIQUE"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = integrand
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_HydrogenResonance_Full(p: dict) -> float:
    """Source43 HydrogenRes: Complete H_res = integrand × x2 UNIQUE"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = H_res
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_SurfaceMagnetic_Oscillation(p: dict) -> float:
    """Source43 SurfaceMag: Solar magnetic field oscillation B_j UNIQUE"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = B_j
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_SurfaceMagnetic_MinField(p: dict) -> float:
    """Source43 SurfaceMag: Minimum magnetic field B_s_min (quiet Sun) UNIQUE"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = B_s_min
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_SurfaceMagnetic_MaxField(p: dict) -> float:
    """Source43 SurfaceMag: Maximum magnetic field B_s_max (sunspot) UNIQUE"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = B_s_max
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_SurfaceMagnetic_CycleReference(p: dict) -> float:
    """Source43 SurfaceMag: Solar cycle reference field B_ref UNIQUE"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = B_ref
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_Source10_F_U_Bi_iTerm(p: dict) -> float:
    """UQFF Core buoyancy force from source10.cpp"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = integrand * x_2
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_Source10_g_UQFFTerm(p: dict) -> float:
    """Compressed UQFF gravity field"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = G_const * M / (r * r) * (1.0 + 0.1 * math.math.sin(1e-10 * t))
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_Source10_DPM_ResonanceTerm(p: dict) -> float:
    """Dark matter resonance term"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = amp * math.math.sin(freq * t)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_Source10_E_cmTerm(p: dict) -> float:
    """Center of mass energy calculation"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = M * c * c * (gamma - 1.0)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_Source11_Ug1Term(p: dict) -> float:
    """U_g1 gravity term from celestial body dynamics"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = k1 * mu_s * grad_Ms_r * math.math.exp(-alpha * t) * defect
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_Source11_Ug2Term(p: dict) -> float:
    """U_g2 gravity term with solar wind modulation"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = k2 * (QA + QUA) * Ms / (r * r) * S * wind_mod * HSCm * Ereact
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_Source11_Ug3Term(p: dict) -> float:
    """U_g3 magnetic coupling term"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = k3 * Bj * math.math.cos(omega_s_t * t * math.pi) * Pcore * Ereact
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_Source11_Ug4Term(p: dict) -> float:
    """U_g4 black hole contribution term"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = k4 * rho_v * C_concentration * Mbh / dg * decay * cycle * (1 + f_feedback)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_Source11_UmTerm(p: dict) -> float:
    """Universal magnetism term with string contributions"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = single * num_strings * PSCm * Ereact
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_UQFFModule5_DarkMatterHaloTerm(p: dict) -> float:
    """NFW dark matter halo contribution"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_UQFFModule5_VacuumEnergyTerm(p: dict) -> float:
    """Vacuum energy fluctuation term"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_Source5_CompressedBaseTerm(p: dict) -> float:
    """MUGE compressed base gravity calculation"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = G_const * M / r
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_Source5_CompressedExpansionTerm(p: dict) -> float:
    """MUGE compressed expansion (Hubble flow)"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = H0 * r * math.math.exp(H0 * t)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_Source5_CompressedQuantumTerm(p: dict) -> float:
    """Compressed quantum uncertainty term"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_Source5_CompressedCosmTerm(p: dict) -> float:
    """Compressed cosmological constant (Lambda)"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = -(Lambda * r * r * r) / 3.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_Source5_CompressedFluidTerm(p: dict) -> float:
    """Compressed fluid dynamics contribution"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 4.0 * math.pi * 6.674e-11 * fluid_factor / (3.0 * M)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_Source5_CompressedPerturbationTerm(p: dict) -> float:
    """Compressed metric perturbation term"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = delta_metric * math.math.cos(omega_pert * t)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_Source5_ResonanceMUGETerm(p: dict) -> float:
    """MUGE resonance term with quality factor"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = resonance
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_Source5_CompressedSuperAdjTerm(p: dict) -> float:
    """Compressed supercritical magnetic adjustment"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = adjustment_scale * math.math.log(B_field / B_crit)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_Source5_CompressedEnvTerm(p: dict) -> float:
    """Compressed environmental thermal term"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = scale * k_B * T_env * (1.0 + 0.1 * math.math.sin(1e-18 * t))
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_Source5_CompressedUgSumTerm(p: dict) -> float:
    """Total compressed Ug gravity sum"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = Ug1 + Ug2 + Ug3 + Ug4
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_ESO137_BuoyancyTerm(p: dict) -> float:
    """ESO137-001 buoyancy force"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = M * B0 * rho_gas / (r * r)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_ESO137_CompressedTerm(p: dict) -> float:
    """ESO137-001 compressed integrand"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = G_const * M / r * math.math.exp(-1e-15 * t)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_ESO137_ResonantTerm(p: dict) -> float:
    """ESO137-001 DPM resonance"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = DPM * math.math.sin(omega0 * t)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_ESO137_SuperconductiveTerm(p: dict) -> float:
    """ESO137-001 superconductive term"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = rho_vac_SCm * math.math.cos(omega_s * t)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_ESO137_LENRTerm(p: dict) -> float:
    """ESO137-001 LENR resonance term"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = k_LENR * math.math.sin(omega_LENR * t)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_ESO137_CompressedGTerm(p: dict) -> float:
    """ESO137-001 compressed g(r,t)"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = G_const * M / (r * r) * (1.0 + 0.1 * math.math.sin(1e-15 * t))
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_ESO137_Q_waveTerm(p: dict) -> float:
    """ESO137-001 quantum wave"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_ESO137_Ub1Term(p: dict) -> float:
    """ESO137-001 Ub1 component"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = beta_i * Omega_g
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_ESO137_UiTerm(p: dict) -> float:
    """ESO137-001 Ui unified field"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = lambda_i * rho_vac_UA * math.math.sin(1e-10 * t)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_NGC1365_BuoyancyTerm(p: dict) -> float:
    """NGC1365 barred spiral buoyancy"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = M * B0 * rho_gas / (r * r)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_NGC1365_CompressedTerm(p: dict) -> float:
    """NGC1365 compressed integrand"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = G_const * M / r * math.math.exp(-1e-15 * t)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_NGC1365_LENRTerm(p: dict) -> float:
    """NGC1365 LENR resonance"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = k_LENR * math.math.sin(omega_LENR * t)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_Vela_BuoyancyTerm(p: dict) -> float:
    """Vela Pulsar buoyancy force"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = M * B0 * rho_gas / (r * r)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_Vela_CompressedTerm(p: dict) -> float:
    """Vela compressed integrand"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = G_const * M / r * math.math.exp(-1e-12 * t)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_Vela_LENRTerm(p: dict) -> float:
    """Vela LENR resonance"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = k_LENR * math.math.sin(omega_LENR * t)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_ASASSN14li_BuoyancyTerm(p: dict) -> float:
    """ASASSN-14li TDE buoyancy"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = M * B0 * rho_gas / (r * r)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_ASASSN14li_CompressedTerm(p: dict) -> float:
    """ASASSN-14li compressed integrand"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = G_const * M / r * math.math.exp(-1e-13 * t)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_ASASSN14li_LENRTerm(p: dict) -> float:
    """ASASSN-14li LENR resonance"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = k_LENR * math.math.sin(omega_LENR * t)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_ElGordo_BuoyancyTerm(p: dict) -> float:
    """El Gordo cluster buoyancy"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = M * B0 * rho_gas / (r * r)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_ElGordo_CompressedTerm(p: dict) -> float:
    """El Gordo compressed integrand"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = G_const * M / r * math.math.exp(-1e-16 * t)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_ElGordo_LENRTerm(p: dict) -> float:
    """El Gordo LENR resonance"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = k_LENR * math.math.sin(omega_LENR * t)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_Heaviside_FactorTerm(p: dict) -> float:
    """Heaviside step function"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_Heaviside_UmBaseTerm(p: dict) -> float:
    """Um base calculation"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_Heliosphere_H_SCmTerm(p: dict) -> float:
    """Heliosphere thickness H_SCm"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_UgIndex_U_giTerm(p: dict) -> float:
    """Indexed U_gi component"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = base * pow(0.9, i)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_UgIndex_K_iTerm(p: dict) -> float:
    """Coupling constant K_i"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = k_base * (1.0 + 0.1 * i)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_Inertia_Lambda_iTerm(p: dict) -> float:
    """Inertia coupling λ_i"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = lambda_base * math.math.exp(-0.01 * i)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_Inertia_U_iTerm(p: dict) -> float:
    """Inertial field U_i"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = rho_vac_UA * V_infl
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_MagMoment_Mu_jTerm(p: dict) -> float:
    """Magnetic moment μ_j"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = B_j * pow(Rs, 3)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_MagMoment_B_jTerm(p: dict) -> float:
    """Time-varying B_j field"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = B_base + 0.4 * math.math.sin(omega_c * t) + SCm_contrib
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_BlackHole_M_bhTerm(p: dict) -> float:
    """Black hole mass"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = Mbh_solar * M_sun
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_BlackHole_U_b1Term(p: dict) -> float:
    """Black hole U_b1 term"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_NegativeTime_CosPiTnTerm(p: dict) -> float:
    """cos(π t_n) negative time"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = math.math.cos(math.pi * tn)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_NegativeTime_ExpTermTerm(p: dict) -> float:
    """exp(-γt cos(πt_n))"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = math.math.exp(-gamma * t * math.math.cos(math.pi * tn))
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_PiConstant_MuJExampleTerm(p: dict) -> float:
    """μ_j with π modulation"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = B_j * pow(Rs, 3)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_CorePenetration_P_coreTerm(p: dict) -> float:
    """Core penetration probability"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1.0 / (1.0 + math.math.exp(-10.0 * (math.math.cos(theta) - 0.5)))
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_QuasiLong_FactorTerm(p: dict) -> float:
    """Quasi-longitudinal geometric factor"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = math.math.cos(theta) * math.math.cos(phi)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_OuterBubble_S_r_RbTerm(p: dict) -> float:
    """Outer field bubble step function S(r-R_b)"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_OuterBubble_U_g2Term(p: dict) -> float:
    """U_g2 with outer field bubble step function"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = k2 * S * M_s / (r * r)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_OuterBubble_R_bTerm(p: dict) -> float:
    """Radius of outer field bubble (100 AU)"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1.496e13
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_OuterBubble_R_bInAUTerm(p: dict) -> float:
    """Outer field bubble radius in AU"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 100.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_Reciprocation_Gamma_sTerm(p: dict) -> float:
    """Reciprocation decay rate γ in s⁻¹"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = gamma_day / day_to_s
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_Reciprocation_CosPiTnTerm(p: dict) -> float:
    """Negative time oscillation cos(π t_n)"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = math.math.cos(math.pi * t_n)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_Reciprocation_ExpTermTerm(p: dict) -> float:
    """Reciprocation exponential decay term"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = math.math.exp(-gamma_day * t_day * math.math.cos(math.pi * t_n))
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_Reciprocation_OneMinusExpTerm(p: dict) -> float:
    """U_m reciprocation factor 1-exp(-γt cos(πt_n))"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1.0 - math.math.exp(-gamma_day * t_day * math.math.cos(math.pi * t_n))
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_Reciprocation_Gamma_dayTerm(p: dict) -> float:
    """Reciprocation decay rate in day⁻¹"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.00005
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_Reciprocation_UmExampleTerm(p: dict) -> float:
    """U_m with reciprocation decay"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = mu_over_rj * factor
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_ScmPenetration_UmBaseTerm(p: dict) -> float:
    """Base U_m term before SCm penetration"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = mu_over_rj
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_ScmPenetration_UmContributionTerm(p: dict) -> float:
    """U_m with [SCm] penetration factor"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = mu_over_rj * P_SCm
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_ScmPenetration_P_SCmTerm(p: dict) -> float:
    """[SCm] superconducting penetration factor"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_ScmPenetration_UmPlanetTerm(p: dict) -> float:
    """U_m for planets with reduced P_SCm"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = mu_over_rj * P_SCm_planet
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_ScmReactivity_Kappa_sTerm(p: dict) -> float:
    """[SCm] reactivity decay rate κ in s⁻¹"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = kappa_day / day_to_s
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_ScmReactivity_E_reactTerm(p: dict) -> float:
    """[SCm] reactivity energy E_react with decay"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = E_react_0 * math.math.exp(-kappa_day * t_day)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_ScmReactivity_UmExampleTerm(p: dict) -> float:
    """U_m with [SCm] reactivity decay"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = E_react * 1e-10
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_ScmReactivity_Kappa_dayTerm(p: dict) -> float:
    """[SCm] reactivity decay rate in day⁻¹"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0005
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_SolarCycle_Omega_cTerm(p: dict) -> float:
    """Solar cycle frequency ω_c"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 2.0 * math.pi / period
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_SolarCycle_SinOmegaCTTerm(p: dict) -> float:
    """Solar cycle oscillation sin(ω_c t)"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = math.math.sin(omega_c * t)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_SolarCycle_MuJExampleTerm(p: dict) -> float:
    """Magnetic moment μ_j with solar cycle modulation"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = B_j * pow(R_s, 3)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_SolarCycle_PeriodYearsTerm(p: dict) -> float:
    """Solar cycle period in years"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = period_s / year_to_s
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_SolarCycle_B_j_ModulatedTerm(p: dict) -> float:
    """Surface magnetic field B_j with solar cycle"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = B_j_base + amplitude * math.math.sin(omega_c * t)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_SolarWindMod_ModulationFactorTerm(p: dict) -> float:
    """Solar wind modulation (1 + δ_sw v_sw)"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1.0 + delta_sw * v_sw
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_SolarWindMod_U_g2Term(p: dict) -> float:
    """U_g2 with solar wind modulation"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = modulation * M_s / (r * r)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_SolarWindMod_Delta_swTerm(p: dict) -> float:
    """Solar wind modulation parameter δ_sw"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.01
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_SolarWindMod_U_g2_no_modTerm(p: dict) -> float:
    """U_g2 without solar wind modulation"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = M_s / (r * r)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_SolarWindVel_ModulationFactorTerm(p: dict) -> float:
    """Velocity-based modulation factor"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1.0 + delta_sw * v_sw
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_SolarWindVel_U_g2Term(p: dict) -> float:
    """U_g2 with velocity modulation"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = modulation * M_s / (r * r)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_SolarWindVel_V_swTerm(p: dict) -> float:
    """Solar wind velocity v_sw"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 5e5
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_SolarWindVel_V_swKmSTerm(p: dict) -> float:
    """Solar wind velocity in km/s"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 500.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_SolarWindVel_U_g2_no_swTerm(p: dict) -> float:
    """U_g2 without solar wind velocity"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = M_s / (r * r)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_StellarMass_M_sOverR2Term(p: dict) -> float:
    """Stellar mass per area M_s/r²"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = M_s / (r * r)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_StellarMass_U_g1Term(p: dict) -> float:
    """U_g1 with stellar mass"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = k1 * M_s / (r * r)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_StellarMass_U_g2Term(p: dict) -> float:
    """U_g2 with stellar mass"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = k2 * M_s / (r * r)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_StellarMass_M_sTerm(p: dict) -> float:
    """Stellar mass M_s (1 M_☉)"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1.989e30
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_StellarMass_M_sInMsunTerm(p: dict) -> float:
    """Stellar mass in solar masses"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_StellarRot_Omega_s_tTerm(p: dict) -> float:
    """Time-dependent rotation rate ω_s(t)"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = omega_s
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_StellarRot_U_g3Term(p: dict) -> float:
    """U_g3 with rotation modulation"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = B_j * math.math.cos(omega_s * t) * P_core * E_react
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_StellarRot_U_iTerm(p: dict) -> float:
    """U_i with rotation and negative time"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = omega_s * math.math.cos(math.pi * t_n) * Lambda_i
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_StellarRot_Omega_sTerm(p: dict) -> float:
    """Stellar rotation rate ω_s"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 2.5e-6
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_StellarRot_Period_daysTerm(p: dict) -> float:
    """Rotation period in days"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (2.0 * math.pi / omega_s) / day_to_s
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_StepFunc_S_r_RbTerm(p: dict) -> float:
    """Step function for field activation"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_StepFunc_U_g2Term(p: dict) -> float:
    """U_g2 activated by step function"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = S * M_s / (r * r)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_StressEnergy_T_sTerm(p: dict) -> float:
    """Stress-energy tensor scalar T_s"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = T_s_base + rho_vac_A
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_StressEnergy_A_mu_nuTerm(p: dict) -> float:
    """Perturbed metric A_μν"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1.0 + epsilon * T_s
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_StressEnergy_PerturbationTerm(p: dict) -> float:
    """Metric perturbation ε T_s"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = epsilon * T_s
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_SurfaceMagField_B_jTerm(p: dict) -> float:
    """Magnetic field B_j scaled by surface field"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = B_j_base * (B_s / B_ref)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_SurfaceMagField_U_g3_exampleTerm(p: dict) -> float:
    """U_g3 with surface field modulation"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = B_j * math.math.cos(omega_s * t) * P_core * E_react
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_SurfaceMagField_B_s_minTerm(p: dict) -> float:
    """Minimum surface magnetic field B_s"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1e-4
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_SurfaceMagField_B_s_maxTerm(p: dict) -> float:
    """Maximum surface magnetic field B_s"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.4
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_SurfaceTemp_B_j_hypotheticalTerm(p: dict) -> float:
    """Hypothetical B_j with temperature scaling"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = B_j_base * (T_s / T_s_ref)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_SurfaceTemp_U_g3_exampleTerm(p: dict) -> float:
    """U_g3 with temperature scaling"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = B_j * math.math.cos(omega_s * t) * P_core * E_react
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_SurfaceTemp_T_sTerm(p: dict) -> float:
    """Surface temperature T_s"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 5778.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_TimeReversalZone_TRZFactorTerm(p: dict) -> float:
    """Time-reversal zone factor f_TRZ"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.1
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_TimeReversalZone_U_i_baseTerm(p: dict) -> float:
    """Base U_i without TRZ enhancement"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = omega_s * math.math.cos(math.pi * t_n) * Lambda_i
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_TimeReversalZone_U_iTerm(p: dict) -> float:
    """U_i with TRZ enhancement"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (1.0 + f_TRZ) * base
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_TimeReversalZone_F_TRZTerm(p: dict) -> float:
    """TRZ enhancement factor (1 + f_TRZ)"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1.0 + f_TRZ
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_TimeReversalZone_U_i_no_TRZTerm(p: dict) -> float:
    """U_i without TRZ"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = omega_s * math.math.cos(math.pi * t_n) * Lambda_i
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_Ug1Defect_Delta_defTerm(p: dict) -> float:
    """Ug1 defect factor δ_def"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = amplitude * math.math.sin(omega_def * t_day)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_Ug1Defect_U_g1Term(p: dict) -> float:
    """U_g1 with defect modulation"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (1.0 + delta_def) * k1 * M_s / (r * r)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_Ug1Defect_Period_yearsTerm(p: dict) -> float:
    """Defect oscillation period in years"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (2.0 * math.pi / omega_def) / year_to_day
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_Ug3DiskVec_PhiHat_jTerm(p: dict) -> float:
    """Disk plane unit vector φ̂_j"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = math.math.sqrt(x * x + y * y)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_Ug3DiskVec_UmBaseTerm(p: dict) -> float:
    """Base U_m term"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = mu_over_rj
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_Ug3DiskVec_UmContributionTerm(p: dict) -> float:
    """U_m with disk vector contribution"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = mu_over_rj * phi_hat_magnitude
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_Ug3DiskVec_PhiHatMagnitudeTerm(p: dict) -> float:
    """Magnitude of φ̂_j (unit vector)"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_AetherVacDensity_Rho_vac_ATerm(p: dict) -> float:
    """Aether vacuum energy density ρ_vac,A"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1e-23
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_AetherVacDensity_T_s_contributionTerm(p: dict) -> float:
    """T_s with aether contribution"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = T_s_base + rho_vac_A
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_AetherVacDensity_A_mu_nuTerm(p: dict) -> float:
    """Metric perturbation ε T_s from aether"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = epsilon * T_s
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_AetherVacDensity_Epsilon_factorTerm(p: dict) -> float:
    """Perturbation parameter ε"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1e-22
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_UnivInertiaVac_Rho_vac_UiTerm(p: dict) -> float:
    """Universal Inertia vacuum density ρ_vac,Ui"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 2.84e-36
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_UnivInertiaVac_U_i_baseTerm(p: dict) -> float:
    """Base U_i inertial term"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = omega_s * math.math.cos(math.pi * t_n) * Lambda_i
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_UnivInertiaVac_U_iTerm(p: dict) -> float:
    """U_i with vacuum density scaling"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (1.0 + f_TRZ) * base * vac_product
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_ScmVacDensity_Rho_vac_SCmTerm(p: dict) -> float:
    """[SCm] vacuum energy density ρ_vac,SCm"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 7.09e-37
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_ScmVacDensity_U_g2_exampleTerm(p: dict) -> float:
    """U_g2 with SCm vacuum scaling"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = S_factor * modulation * (M_s / (r * r)) * rho_vac_SCm * E_react
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_ScmVacDensity_U_i_exampleTerm(p: dict) -> float:
    """U_i with SCm vacuum coupling"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (1.0 + f_TRZ) * base * vac_product
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_UaVacDensity_Rho_vac_UATerm(p: dict) -> float:
    """[UA] vacuum energy density ρ_vac,UA"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 7.09e-36
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_UaVacDensity_U_g2_exampleTerm(p: dict) -> float:
    """U_g2 with UA vacuum scaling"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = S_factor * modulation * (M_s / (r * r)) * rho_vac_UA * E_react
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_UaVacDensity_U_i_exampleTerm(p: dict) -> float:
    """U_i with UA vacuum coupling"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (1.0 + f_TRZ) * base * vac_product
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_UnivInertiaVac2_U_i_baseTerm(p: dict) -> float:
    """U_i base (source130 variant)"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = omega_s * math.math.cos(math.pi * t_n) * Lambda_i
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_UnivInertiaVac2_U_iTerm(p: dict) -> float:
    """U_i full (source130 variant)"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (1.0 + f_TRZ) * base * vac_product
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_ScmVelocity_V_scmTerm(p: dict) -> float:
    """[SCm] velocity v_SCm"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1e8
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_ScmVelocity_E_react_baseTerm(p: dict) -> float:
    """Base E_react energy"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (rho_SCm * v_scm * v_scm) / rho_A
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_ScmVelocity_E_reactTerm(p: dict) -> float:
    """E_react with decay"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = E_base * math.math.exp(-kappa * t_day)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_ScmVelocity_UmExampleTerm(p: dict) -> float:
    """U_m with E_react"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = mu_over_r * E_react
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_ButterflyNebula_DPM_momentumTerm(p: dict) -> float:
    """DPM momentum term"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = M / (r * r)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_ButterflyNebula_DPM_gravityTerm(p: dict) -> float:
    """DPM gravity contribution"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = dpm_ug1_seed(M, r)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_ButterflyNebula_LENR_termTerm(p: dict) -> float:
    """LENR resonance factor"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = rho_LENR / rho_0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_ButterflyNebula_F_U_BiTerm(p: dict) -> float:
    """Total UQFF force"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = integrand * x2
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_CentaurusA_DPM_momentumTerm(p: dict) -> float:
    """DPM momentum NGC5128"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = M / (r * r)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_CentaurusA_F_U_BiTerm(p: dict) -> float:
    """NGC5128 UQFF force"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = integrand * x2
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_Abell2256_IntegrandTerm(p: dict) -> float:
    """Abell 2256 integrand"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = M / (r * r * r)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_Abell2256_DPM_resonanceTerm(p: dict) -> float:
    """DPM resonance oscillation"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = math.math.cos(omega * t)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_Abell2256_X2Term(p: dict) -> float:
    """Quadratic root x2"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_Abell2256_LENR_termTerm(p: dict) -> float:
    """LENR resonance"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1.0 / rho_0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_Abell2256_Q_waveTerm(p: dict) -> float:
    """Quantum wave term"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = math.math.sin(omega * t)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_Abell2256_Ub1Term(p: dict) -> float:
    """U_b1 buoyancy"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = rho_vac * 1e50
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_Abell2256_UiTerm(p: dict) -> float:
    """Universal Inertia"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = omega_s * 1e-40
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_Abell2256_FTerm(p: dict) -> float:
    """Total force F"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = integrand * x2
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_Abell2256_CompressedTerm(p: dict) -> float:
    """Compressed gravity g"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (M / r) * 1e-10
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_Abell2256_ResonantTerm(p: dict) -> float:
    """Resonant mode"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = math.math.cos(omega * t) * 1e-5
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_Abell2256_BuoyancyTerm(p: dict) -> float:
    """Buoyancy term"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = rho_vac * 1e55
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_Abell2256_SuperconductiveTerm(p: dict) -> float:
    """Superconductive term"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = E_react * 1e-30
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_Abell2256_CompressedGTerm(p: dict) -> float:
    """Compressed G field"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = dpm_ug1_seed(M, r)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_ASASSN14li_IntegrandTerm(p: dict) -> float:
    """ASASSN-14li integrand"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = M / (r * r * r)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_ASASSN14li_DPM_resonanceTerm(p: dict) -> float:
    """TDE resonance"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = math.math.cos(omega * t)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_ASASSN14li_X2Term(p: dict) -> float:
    """Quadratic root"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1e12
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_ASASSN14li_LENR_termTerm(p: dict) -> float:
    """LENR TDE"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1.0 / rho_0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_ASASSN14li_Q_waveTerm(p: dict) -> float:
    """Quantum wave"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = math.math.sin(omega * t)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_ASASSN14li_FTerm(p: dict) -> float:
    """TDE force"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = integrand * x2
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_CentaurusA_X2Term(p: dict) -> float:
    """CenA quadratic root"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1e12
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_CentaurusA_LENR_termTerm(p: dict) -> float:
    """CenA LENR"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1.0 / rho_0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_CentaurusA_FTerm(p: dict) -> float:
    """CenA total force"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = integrand * x2
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_CentaurusA_CompressedTerm(p: dict) -> float:
    """CenA compressed g"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (M / r) * 1e-10
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_CrabNebula_IntegrandTerm(p: dict) -> float:
    """Crab integrand"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = M / (r * r * r)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_CrabNebula_LENR_termTerm(p: dict) -> float:
    """Crab LENR"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1.0 / rho_0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_CrabNebula_FTerm(p: dict) -> float:
    """Crab total force"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = integrand * x2
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_CrabNebula_CompressedTerm(p: dict) -> float:
    """Crab compressed g"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (M / r) * 1e-10
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_ElGordo_IntegrandTerm(p: dict) -> float:
    """El Gordo integrand"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = M / (r * r * r)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_ElGordo_LENR_termTerm(p: dict) -> float:
    """El Gordo LENR"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1.0 / rho_0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_ElGordo_FTerm(p: dict) -> float:
    """El Gordo total force"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = integrand * x2
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_ESO137_FTerm(p: dict) -> float:
    """ESO 137-001 total force"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = integrand * x2
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_IC2163_IntegrandTerm(p: dict) -> float:
    """IC 2163 integrand"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = M / (r * r * r)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_IC2163_LENR_termTerm(p: dict) -> float:
    """IC 2163 LENR"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1.0 / rho_0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_IC2163_FTerm(p: dict) -> float:
    """IC 2163 total force"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = integrand * x2
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_IC2163_CompressedTerm(p: dict) -> float:
    """IC 2163 compressed g"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (M / r) * 1e-10
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_J1610_IntegrandTerm(p: dict) -> float:
    """Integrand M/r³ for J1610+1811 high-z quasar (z=3.122)"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = M / (r * r * r)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_J1610_LENR_termTerm(p: dict) -> float:
    """LENR resonance: 1/ρ₀ for J1610 (dominant term)"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1e15
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_J1610_FTerm(p: dict) -> float:
    """Total UQFF force for J1610+1811"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1.99e-2 * (-1.35e172)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_J1610_CompressedTerm(p: dict) -> float:
    """Compressed gravity for high-z quasar"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (1.73e40 / 9.63e20) * 1e-10
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_JupiterAurorae_IntegrandTerm(p: dict) -> float:
    """Integrand for Jupiter auroral physics (Io plasma torus)"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1.898e27 / (7.1492e7 * 7.1492e7 * 7.1492e7)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_JupiterAurorae_LENR_termTerm(p: dict) -> float:
    """LENR resonance for Jupiter (magnetospheric coupling)"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1e12
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_JupiterAurorae_FTerm(p: dict) -> float:
    """Total UQFF force for Jupiter aurorae"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 5.19e11 * (-3.40e172)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_JupiterAurorae_CompressedTerm(p: dict) -> float:
    """Compressed g for Jupiter"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (1.898e27 / 7.1492e7) * 1e-10
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_LagoonNebula_IntegrandTerm(p: dict) -> float:
    """Integrand for Lagoon Nebula (star formation region)"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1e36 / (2.36e17 * 2.36e17 * 2.36e17)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_LagoonNebula_LENR_termTerm(p: dict) -> float:
    """LENR for Lagoon Nebula"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1e12
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_LagoonNebula_FTerm(p: dict) -> float:
    """Total UQFF force for Lagoon Nebula"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 7.61e-2 * (-3.40e172)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_LagoonNebula_CompressedTerm(p: dict) -> float:
    """Compressed g for Lagoon Nebula"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (1e36 / 2.36e17) * 1e-10
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_LagoonNebulaVar_IntegrandTerm(p: dict) -> float:
    """Lagoon Nebula variant integrand"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1e36 / (2.36e17 * 2.36e17 * 2.36e17)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_LagoonNebulaVar_LENR_termTerm(p: dict) -> float:
    """Lagoon variant LENR"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1e12
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_LagoonNebulaVar_FTerm(p: dict) -> float:
    """Lagoon variant total force"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 7.61e-2 * (-3.40e172)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_LagoonNebulaVar_CompressedTerm(p: dict) -> float:
    """Lagoon variant compressed g"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (1e36 / 2.36e17) * 1e-10
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_M87Jet_IntegrandTerm(p: dict) -> float:
    """Integrand for M87 relativistic jet (~0.5c, EHT SMBH M=6.5e9 M☉)"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1.29e40 / (4.63e19 * 4.63e19 * 4.63e19)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_M87Jet_LENR_termTerm(p: dict) -> float:
    """LENR for M87 jet (superluminal knots)"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1e15
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_M87Jet_FTerm(p: dict) -> float:
    """Total UQFF force for M87 jet"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1.30e1 * (-1.35e172)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_M87Jet_CompressedTerm(p: dict) -> float:
    """Compressed g for M87 jet"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (1.29e40 / 4.63e19) * 1e-10
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_NGC1365_IntegrandTerm(p: dict) -> float:
    """Integrand for NGC 1365 double-barred Seyfert (z=0.0055)"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 7.17e41 / (9.46e20 * 9.46e20 * 9.46e20)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_NGC1365_LENR_termTerm(p: dict) -> float:
    """LENR for NGC 1365 (AGN X-ray variability)"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1e15
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_NGC1365_FTerm(p: dict) -> float:
    """Total UQFF force for NGC 1365"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 8.47e-1 * (-1.35e172)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_NGC2207_IntegrandTerm(p: dict) -> float:
    """Integrand for NGC 2207 colliding galaxies (tidal bridge)"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 3.978e40 / (4.40e20 * 4.40e20 * 4.40e20)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_NGC2207_LENR_termTerm(p: dict) -> float:
    """LENR for NGC 2207 (interaction-driven)"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1e12
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_NGC2207_FTerm(p: dict) -> float:
    """Total UQFF force for NGC 2207"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 4.67e-1 * (-3.40e172)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_NGC2207_CompressedTerm(p: dict) -> float:
    """Compressed g for NGC 2207"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (3.978e40 / 4.40e20) * 1e-10
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_RAquarii_IntegrandTerm(p: dict) -> float:
    """Integrand for R Aquarii symbiotic binary (Mira variable + white dwarf)"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 3.978e30 / (2.18e15 * 2.18e15 * 2.18e15)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_RAquarii_LENR_termTerm(p: dict) -> float:
    """LENR for R Aquarii (mass transfer jets)"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1e12
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_RAquarii_FTerm(p: dict) -> float:
    """Total UQFF force for R Aquarii"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 3.84e0 * (-3.40e172)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_RAquarii_CompressedTerm(p: dict) -> float:
    """Compressed g for R Aquarii"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (3.978e30 / 2.18e15) * 1e-10
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_SgrAStar_IntegrandTerm(p: dict) -> float:
    """Integrand for Sagittarius A* (Galactic Center, Nobel 2020)"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 8.56e36 / (6.17e18 * 6.17e18 * 6.17e18)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_SgrAStar_LENR_termTerm(p: dict) -> float:
    """LENR for Sgr A* (flare activity)"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1e15
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_SgrAStar_FTerm(p: dict) -> float:
    """Total UQFF force for Sgr A*"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 3.65e-2 * (-1.35e172)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_SgrAStar_CompressedTerm(p: dict) -> float:
    """Compressed g for Sgr A*"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (8.56e36 / 6.17e18) * 1e-10
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_SPTCLJ2215_IntegrandTerm(p: dict) -> float:
    """Integrand for SPT-CL J2215 galaxy cluster (z=0.7)"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1.46e45 / (3.09e22 * 3.09e22 * 3.09e22)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_SPTCLJ2215_LENR_termTerm(p: dict) -> float:
    """LENR for SPT-CL J2215"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1e15
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_SPTCLJ2215_FTerm(p: dict) -> float:
    """Total UQFF force for SPT-CL J2215"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 4.95e-1 * (-1.35e172)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_SPTCLJ2215_CompressedTerm(p: dict) -> float:
    """Compressed g for SPT-CL J2215"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (1.46e45 / 3.09e22) * 1e-10
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_StephanQuintet_IntegrandTerm(p: dict) -> float:
    """Integrand for Stephan's Quintet compact group (JWST NIRCam target)"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 2e39 / (3.09e22 * 3.09e22 * 3.09e22)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_StephanQuintet_LENR_termTerm(p: dict) -> float:
    """LENR for Stephan's Quintet"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1e15
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_StephanQuintet_FTerm(p: dict) -> float:
    """Total UQFF force for Stephan's Quintet"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 6.79e-7 * (-1.35e172)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_StephanQuintet_CompressedTerm(p: dict) -> float:
    """Compressed g for Stephan's Quintet"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (2e39 / 3.09e22) * 1e-10
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_VelaPulsar_IntegrandTerm(p: dict) -> float:
    """Integrand for Vela Pulsar (89 ms period, Gum Nebula)"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 2.8e30 / (1.7e17 * 1.7e17 * 1.7e17)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_VelaPulsar_LENR_termTerm(p: dict) -> float:
    """LENR for Vela Pulsar"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1e12
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_VelaPulsar_FTerm(p: dict) -> float:
    """Total UQFF force for Vela Pulsar"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 5.71e-5 * (-3.40e172)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_VelaPulsar_CompressedTerm(p: dict) -> float:
    """Compressed g for Vela Pulsar"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (2.8e30 / 1.7e17) * 1e-10
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_NGC4993_IntegrandTerm(p: dict) -> float:
    """NGC 4993 (neutron star merger host)"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 5e40 / (4e20 * 4e20 * 4e20)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_NGC4993_LENR_termTerm(p: dict) -> float:
    """LENR for NGC 4993"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1e15
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_NGC4993_FTerm(p: dict) -> float:
    """Total force NGC 4993"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 7.81e-1 * (-1.35e172)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_NGC4993_CompressedTerm(p: dict) -> float:
    """Compressed g NGC 4993"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (5e40 / 4e20) * 1e-10
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_NGC6240_IntegrandTerm(p: dict) -> float:
    """NGC 6240 (merging dual AGN)"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 8e40 / (6e20 * 6e20 * 6e20)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_NGC6240_LENR_termTerm(p: dict) -> float:
    """LENR for NGC 6240"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1e15
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_NGC6240_FTerm(p: dict) -> float:
    """Total force NGC 6240"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 3.70e-1 * (-1.35e172)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_NGC6240_CompressedTerm(p: dict) -> float:
    """Compressed g NGC 6240"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (8e40 / 6e20) * 1e-10
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_OmegaCentauri_IntegrandTerm(p: dict) -> float:
    """Omega Centauri (largest MW globular)"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 8e36 / (4e17 * 4e17 * 4e17)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_OmegaCentauri_LENR_termTerm(p: dict) -> float:
    """LENR for Omega Centauri"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1e12
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_OmegaCentauri_FTerm(p: dict) -> float:
    """Total force Omega Centauri"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1.25e-1 * (-3.40e172)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_OmegaCentauri_CompressedTerm(p: dict) -> float:
    """Compressed g Omega Centauri"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (8e36 / 4e17) * 1e-10
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_OrionNebula_IntegrandTerm(p: dict) -> float:
    """Orion Nebula (stellar nursery)"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 2e36 / (3e17 * 3e17 * 3e17)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_OrionNebula_LENR_termTerm(p: dict) -> float:
    """LENR for Orion Nebula"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1e12
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_OrionNebula_FTerm(p: dict) -> float:
    """Total force Orion Nebula"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 7.41e-2 * (-3.40e172)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_OrionNebula_CompressedTerm(p: dict) -> float:
    """Compressed g Orion Nebula"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (2e36 / 3e17) * 1e-10
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_Perseus_IntegrandTerm(p: dict) -> float:
    """Perseus Cluster (Chandra cavities)"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 2.5e45 / (5e22 * 5e22 * 5e22)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_Perseus_LENR_termTerm(p: dict) -> float:
    """LENR for Perseus"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1e15
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_Perseus_FTerm(p: dict) -> float:
    """Total force Perseus"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 2.00e-1 * (-1.35e172)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_Perseus_CompressedTerm(p: dict) -> float:
    """Compressed g Perseus"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (2.5e45 / 5e22) * 1e-10
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_PolarRing_IntegrandTerm(p: dict) -> float:
    """Polar Ring Galaxy (NGC 4650A)"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1e41 / (8e20 * 8e20 * 8e20)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_PolarRing_LENR_termTerm(p: dict) -> float:
    """LENR for Polar Ring"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1e15
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_PolarRing_FTerm(p: dict) -> float:
    """Total force Polar Ring"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1.95e-1 * (-1.35e172)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_PolarRing_CompressedTerm(p: dict) -> float:
    """Compressed g Polar Ring"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (1e41 / 8e20) * 1e-10
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_QuasarJ0313_IntegrandTerm(p: dict) -> float:
    """Quasar J0313 (earliest known z=7.64)"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 3e40 / (1e21 * 1e21 * 1e21)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_QuasarJ0313_LENR_termTerm(p: dict) -> float:
    """LENR for J0313"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1e15
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_QuasarJ0313_FTerm(p: dict) -> float:
    """Total force J0313"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 3.00e-2 * (-1.35e172)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_QuasarJ0313_CompressedTerm(p: dict) -> float:
    """Compressed g J0313"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (3e40 / 1e21) * 1e-10
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_RXJ1713_IntegrandTerm(p: dict) -> float:
    """RX J1713 SNR (TeV gamma-ray)"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 5e30 / (2e17 * 2e17 * 2e17)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_RXJ1713_LENR_termTerm(p: dict) -> float:
    """LENR for RX J1713"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1e12
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_RXJ1713_FTerm(p: dict) -> float:
    """Total force RX J1713"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 6.25e-5 * (-3.40e172)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_RXJ1713_CompressedTerm(p: dict) -> float:
    """Compressed g RX J1713"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (5e30 / 2e17) * 1e-10
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_BuoyancyAstro_IntegrandTerm(p: dict) -> float:
    """Multi-system buoyancy integrand (5 systems)"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = M / (r * r * r)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_BuoyancyAstro_LENR_termTerm(p: dict) -> float:
    """LENR for buoyancy systems"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1e15
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_BuoyancyAstro_FTerm(p: dict) -> float:
    """Total force buoyancy astro"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 3.04e1 * (-1.35e172)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_BuoyancyAstro_CompressedTerm(p: dict) -> float:
    """Compressed g buoyancy astro"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (2.785e30 / 3.09e15) * 1e-10
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_BuoyancyCNB_IntegrandTerm(p: dict) -> float:
    """CNB neutrino integrand Centaurus A"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1.094e38 / (6.17e17 * 6.17e17 * 6.17e17)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_BuoyancyCNB_LENR_termTerm(p: dict) -> float:
    """LENR for CNB"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1e15
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_BuoyancyCNB_FTerm(p: dict) -> float:
    """Total force CNB"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 4.66e-1 * (-1.35e172)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_BuoyancyCNB_CompressedTerm(p: dict) -> float:
    """Compressed g CNB"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (1.094e38 / 6.17e17) * 1e-10
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_AstroSystems163_IntegrandTerm(p: dict) -> float:
    """NGC/AT2024tvd integrand"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 5e40 / (5e20 * 5e20 * 5e20)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_AstroSystems163_LENR_termTerm(p: dict) -> float:
    """LENR for NGC multi"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1e15
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_AstroSystems163_FTerm(p: dict) -> float:
    """Total force NGC multi"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 4.00e-1 * (-1.35e172)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_AstroSystems163_CompressedTerm(p: dict) -> float:
    """Compressed g NGC multi"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (5e40 / 5e20) * 1e-10
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_NebulaTriadic_IntegrandTerm(p: dict) -> float:
    """Nebula triadic integrand (gas ionization)"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 2e36 / (4e17 * 4e17 * 4e17)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_NebulaTriadic_LENR_termTerm(p: dict) -> float:
    """LENR for nebulae"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1e12
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_NebulaTriadic_FTerm(p: dict) -> float:
    """Total force nebula triadic"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 3.13e-2 * (-3.40e172)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_NebulaTriadic_CompressedTerm(p: dict) -> float:
    """Compressed g nebula triadic"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (2e36 / 4e17) * 1e-10
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_BuoyancyModule_IntegrandTerm(p: dict) -> float:
    """M74/Eagle/M84 integrand"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 7.17e41 / (9.46e20 * 9.46e20 * 9.46e20)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_BuoyancyModule_LENR_termTerm(p: dict) -> float:
    """LENR for M74/M16/M84"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1e15
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_BuoyancyModule_FTerm(p: dict) -> float:
    """Total force M74/M16/M84"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 8.47e-1 * (-1.35e172)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_BuoyancyModule_CompressedTerm(p: dict) -> float:
    """Compressed g M74/M16/M84"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (7.17e41 / 9.46e20) * 1e-10
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_UQFF8Astro_IntegrandTerm(p: dict) -> float:
    """8+1 astro systems integrand (26 quantum states)"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1e41 / (7e20 * 7e20 * 7e20)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_UQFF8Astro_LENR_termTerm(p: dict) -> float:
    """LENR for 8 astro"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1e15
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_UQFF8Astro_FTerm(p: dict) -> float:
    """Total force 8 astro"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 2.92e-1 * (-1.35e172)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_UQFF8Astro_CompressedTerm(p: dict) -> float:
    """Compressed g 8 astro"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (1e41 / 7e20) * 1e-10
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_UQFFCore_IntegrandTerm(p: dict) -> float:
    """UQFF Core M82/IC418/Canis"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 3e41 / (6e20 * 6e20 * 6e20)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_UQFFCore_LENR_termTerm(p: dict) -> float:
    """LENR for UQFF Core"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1e15
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_UQFFCore_FTerm(p: dict) -> float:
    """Total force UQFF Core"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1.39e-1 * (-1.35e172)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_UQFFCore_CompressedTerm(p: dict) -> float:
    """Compressed g UQFF Core"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (3e41 / 6e20) * 1e-10
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_Buoyancy168_IntegrandTerm(p: dict) -> float:
    """SN 1006 buoyancy integrand"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1e31 / (1e18 * 1e18 * 1e18)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_Buoyancy168_LENR_termTerm(p: dict) -> float:
    """LENR for SN 1006"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1e12
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_Buoyancy168_FTerm(p: dict) -> float:
    """Total force SN 1006"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1.00e-5 * (-3.40e172)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_Buoyancy168_CompressedTerm(p: dict) -> float:
    """Compressed g SN 1006"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (1e31 / 1e18) * 1e-10
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_CassiniBuoyancy_IntegrandTerm(p: dict) -> float:
    """Cassini Saturn U_Bi/U_Ii/U_Mi"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 5.68e26 / (5.82e7 * 5.82e7 * 5.82e7)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_CassiniBuoyancy_LENR_termTerm(p: dict) -> float:
    """LENR for Cassini"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1e12
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_CassiniBuoyancy_FTerm(p: dict) -> float:
    """Total force Cassini"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 2.88e12 * (-3.40e172)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_CassiniBuoyancy_CompressedTerm(p: dict) -> float:
    """Compressed g Cassini"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (5.68e26 / 5.82e7) * 1e-10
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_MultiAstro_IntegrandTerm(p: dict) -> float:
    """11 multi-astro systems (DPM + ACP)"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1e41 / (8e20 * 8e20 * 8e20)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_MultiAstro_LENR_termTerm(p: dict) -> float:
    """LENR for multi-astro"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1e15
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_MultiAstro_FTerm(p: dict) -> float:
    """Total force multi-astro"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1.95e-1 * (-1.35e172)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_MultiAstro_CompressedTerm(p: dict) -> float:
    """Compressed g multi-astro"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (1e41 / 8e20) * 1e-10
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_EightAstro_IntegrandTerm(p: dict) -> float:
    """8 LMC/NGC systems (AFGL/NGC346)"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 3e37 / (4.5e18 * 4.5e18 * 4.5e18)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_EightAstro_LENR_termTerm(p: dict) -> float:
    """LENR for 8 astro"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1e15
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_EightAstro_FTerm(p: dict) -> float:
    """Total force 8 astro"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 3.29e-2 * (-1.35e172)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_EightAstro_CompressedTerm(p: dict) -> float:
    """Compressed g 8 astro"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (3e37 / 4.5e18) * 1e-10
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_NineteenAstro_IntegrandTerm(p: dict) -> float:
    """19 systems 26D polynomial (NGC2264/UGC10214/M42/Tarantula)"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1e41 / (1e21 * 1e21 * 1e21)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_NineteenAstro_LENR_termTerm(p: dict) -> float:
    """LENR for 19 astro (26 quantum states)"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1e15
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_NineteenAstro_FTerm(p: dict) -> float:
    """Total force 19 astro 26D"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1.00e-2 * (-1.35e172)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_NineteenAstro_CompressedTerm(p: dict) -> float:
    """Compressed g 19 astro 26D"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (1e41 / 1e21) * 1e-10
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_WolframFieldUnity_IntegrandTerm(p: dict) -> float:
    """Wolfram hypergraph emergent spacetime (26 quantum states, PI infinity decoder)"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1e40 / (1e20 * 1e20 * 1e20)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_WolframFieldUnity_LENR_termTerm(p: dict) -> float:
    """LENR for Wolfram hypergraph (causal invariance)"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1e15
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_WolframFieldUnity_FTerm(p: dict) -> float:
    """Total force Wolfram (multiway quantum evolution, sacred time constants)"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1.00e0 * (-1.35e172)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_WolframFieldUnity_CompressedTerm(p: dict) -> float:
    """Compressed g Wolfram (312-digit PI decoder, Mayan/Biblical time)"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (1e40 / 1e20) * 1e-10
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_J1610_DPM_resonanceTerm(p: dict) -> float:
    """J1610 DPM magnetic resonance g·μB·B₀/(ℏ·ω₀)"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (g * muB * B0) / (hbar * omega0)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_J1610_Q_waveTerm(p: dict) -> float:
    """J1610 quantum wave function ψ(t)"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1e12 * math.math.sin(1e-15 * t)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_J1610_Ub1Term(p: dict) -> float:
    """J1610 universal buoyancy U_b1"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.6 * 1e-6 * 7.09e-36
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_J1610_UiTerm(p: dict) -> float:
    """J1610 universal inertia U_i(t)"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1e12 * math.math.exp(-t / 1e16)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_J1610_X2Term(p: dict) -> float:
    """J1610 quadratic root x₂ for UQFF"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = -1.35e172
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_J1610_ActivationTerm(p: dict) -> float:
    """J1610 activation force at 300 Hz"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1e-6 * math.math.cos(2 * 3.14159 * 300 * t + 3.14159 / 4)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_J1610_DirectedEnergyTerm(p: dict) -> float:
    """J1610 directed energy coupling to X-ray luminosity"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1e-30 * 1e39
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_J1610_NeutronTerm(p: dict) -> float:
    """J1610 neutron coupling k·σ"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1e10 * 1e-4
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_J1610_RelativisticTerm(p: dict) -> float:
    """J1610 relativistic coherence (E_cm_astro/E_cm)²"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1e-10 * pow(1.24e24 / 3.0264e-8, 2)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_J1610_NeutrinoTerm(p: dict) -> float:
    """J1610 neutrino background force"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 9.07e-42
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_M87Jet_DPM_resonanceTerm(p: dict) -> float:
    """M87 jet DPM resonance"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (2.0 * 9.274e-24 * 1e-5) / (1.0546e-34 * 1e-15)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_M87Jet_Q_waveTerm(p: dict) -> float:
    """M87 quantum wave"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1e12 * math.math.sin(1e-15 * t)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_M87Jet_Ub1Term(p: dict) -> float:
    """M87 U_b1"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.6 * 1e-6 * 7.09e-36
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_M87Jet_UiTerm(p: dict) -> float:
    """M87 inertia"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1e12 * math.math.exp(-t / 1e16)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_M87Jet_X2Term(p: dict) -> float:
    """M87 x₂ root"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = -1.35e172
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_M87Jet_ActivationTerm(p: dict) -> float:
    """M87 activation"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1e-6 * math.math.cos(2 * 3.14159 * 300 * t)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_M87Jet_DirectedEnergyTerm(p: dict) -> float:
    """M87 directed energy"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1e-30 * 1e34
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_M87Jet_NeutronTerm(p: dict) -> float:
    """M87 neutron"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1e10 * 1e-4
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_M87Jet_RelativisticTerm(p: dict) -> float:
    """M87 relativistic"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1e-10 * pow(1.24e24 / 3.0264e-8, 2)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_M87Jet_NeutrinoTerm(p: dict) -> float:
    """M87 neutrino"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 9.07e-42
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_SgrAStar_DPM_resonanceTerm(p: dict) -> float:
    """Sgr A* DPM magnetic resonance"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (2.0 * 9.274e-24 * 1e-5) / (1.0546e-34 * 1e-15)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_SgrAStar_Q_waveTerm(p: dict) -> float:
    """Sgr A* quantum wave"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1e12 * math.math.sin(1e-15 * t)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_SgrAStar_Ub1Term(p: dict) -> float:
    """Sgr A* U_b1"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.6 * 1e-6 * 7.09e-36
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_SgrAStar_UiTerm(p: dict) -> float:
    """Sgr A* inertia"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1e12 * math.math.exp(-t / 1e16)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_SgrAStar_X2Term(p: dict) -> float:
    """Sgr A* quadratic root"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = -1.35e172
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_SgrAStar_ActivationTerm(p: dict) -> float:
    """Sgr A* activation"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1e-6 * math.math.cos(2 * 3.14159 * 300 * t)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_SgrAStar_DirectedEnergyTerm(p: dict) -> float:
    """Sgr A* directed energy"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1e-30 * 1e36
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_SgrAStar_NeutronTerm(p: dict) -> float:
    """Sgr A* neutron"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1e10 * 1e-4
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_SgrAStar_RelativisticTerm(p: dict) -> float:
    """Sgr A* relativistic"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1e-10 * pow(1.24e24 / 3.0264e-8, 2)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_SgrAStar_NeutrinoTerm(p: dict) -> float:
    """Sgr A* neutrino"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 9.07e-42
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_NGC1365_DPM_resonanceTerm(p: dict) -> float:
    """NGC1365 DPM resonance"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1.76e6
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_NGC1365_Q_waveTerm(p: dict) -> float:
    """NGC1365 quantum wave"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1e12 * math.math.sin(1e-15 * t)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_NGC1365_Ub1Term(p: dict) -> float:
    """NGC1365 U_b1"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 4.25e-42
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_NGC1365_UiTerm(p: dict) -> float:
    """NGC1365 inertia"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1e12 * math.math.exp(-t / 1e16)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_NGC1365_X2Term(p: dict) -> float:
    """NGC1365 x₂"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = -1.35e172
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_NGC1365_ActivationTerm(p: dict) -> float:
    """NGC1365 activation"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1e-6 * math.math.cos(600 * 3.14159 * t)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_NGC1365_DirectedEnergyTerm(p: dict) -> float:
    """NGC1365 directed energy"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1e-30 * 1e40
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_NGC1365_NeutronTerm(p: dict) -> float:
    """NGC1365 neutron"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1e6
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_NGC1365_RelativisticTerm(p: dict) -> float:
    """NGC1365 relativistic"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1.69e26
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_NGC1365_NeutrinoTerm(p: dict) -> float:
    """NGC1365 neutrino"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 9.07e-42
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_VelaPulsar_DPM_resonanceTerm(p: dict) -> float:
    """Vela Pulsar DPM resonance (89ms period)"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1.76e6
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_VelaPulsar_Q_waveTerm(p: dict) -> float:
    """Vela quantum wave (11.2 Hz)"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1e12 * math.math.sin(2 * 3.14159 / 0.089 * t)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_VelaPulsar_Ub1Term(p: dict) -> float:
    """Vela U_b1"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 4.25e-42
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_VelaPulsar_UiTerm(p: dict) -> float:
    """Vela inertia"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1e12 * math.math.exp(-t / 1e16)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_VelaPulsar_X2Term(p: dict) -> float:
    """Vela x₂ (pulsar variant)"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = -3.40e172
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_VelaPulsar_ActivationTerm(p: dict) -> float:
    """Vela activation (pulse frequency)"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1e-6 * math.math.cos(2 * 3.14159 * 11.2 * t)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_VelaPulsar_DirectedEnergyTerm(p: dict) -> float:
    """Vela directed energy"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1e-30 * 1e32
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_VelaPulsar_NeutronTerm(p: dict) -> float:
    """Vela neutron"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1e10 * 1e-4
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_VelaPulsar_RelativisticTerm(p: dict) -> float:
    """Vela relativistic"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1.69e26
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_VelaPulsar_NeutrinoTerm(p: dict) -> float:
    """Vela neutrino"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 9.07e-42
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_NGC2207_DPM_resonanceTerm(p: dict) -> float:
    """NGC2207 collision DPM"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1.76e6
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_NGC2207_Q_waveTerm(p: dict) -> float:
    """NGC2207 tidal quantum wave"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1e12 * math.math.sin(1e-15 * t)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_NGC2207_Ub1Term(p: dict) -> float:
    """NGC2207 U_b1"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 4.25e-42
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_NGC2207_UiTerm(p: dict) -> float:
    """NGC2207 inertia"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1e12 * math.math.exp(-t / 1e16)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_NGC2207_X2Term(p: dict) -> float:
    """NGC2207 x₂"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = -3.40e172
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_NGC2207_ActivationTerm(p: dict) -> float:
    """NGC2207 activation"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1e-6 * math.math.cos(600 * 3.14159 * t)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_NGC2207_DirectedEnergyTerm(p: dict) -> float:
    """NGC2207 directed energy"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1e-30 * 1e38
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_NGC2207_NeutronTerm(p: dict) -> float:
    """NGC2207 neutron"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1e6
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_NGC2207_RelativisticTerm(p: dict) -> float:
    """NGC2207 relativistic"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1.69e26
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_NGC2207_NeutrinoTerm(p: dict) -> float:
    """NGC2207 neutrino"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 9.07e-42
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_PerseusCluster_DPM_resonanceTerm(p: dict) -> float:
    """Perseus Abell 426 DPM resonance"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1.76e6
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_PerseusCluster_Q_waveTerm(p: dict) -> float:
    """Perseus quantum wave"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1e12 * math.math.sin(1e-15 * t)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_PerseusCluster_Ub1Term(p: dict) -> float:
    """Perseus U_b1"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 4.25e-42
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_PerseusCluster_UiTerm(p: dict) -> float:
    """Perseus inertia"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1e12 * math.math.exp(-t / 1e16)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_PerseusCluster_X2Term(p: dict) -> float:
    """Perseus x₂"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = -3.40e172
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_PerseusCluster_ActivationTerm(p: dict) -> float:
    """Perseus activation"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1e-6 * math.math.cos(600 * 3.14159 * t)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_PerseusCluster_DirectedEnergyTerm(p: dict) -> float:
    """Perseus directed energy"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1e-30 * 1e36
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_PerseusCluster_NeutronTerm(p: dict) -> float:
    """Perseus neutron"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1e6
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_PerseusCluster_RelativisticTerm(p: dict) -> float:
    """Perseus relativistic"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1.69e26
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_PerseusCluster_NeutrinoTerm(p: dict) -> float:
    """Perseus neutrino"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 9.07e-42
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_OrionM42_DPM_resonanceTerm(p: dict) -> float:
    """Orion M42 DPM resonance"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1.76e6
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_OrionM42_Q_waveTerm(p: dict) -> float:
    """Orion quantum wave"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1e12 * math.math.sin(1e-15 * t)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_OrionM42_Ub1Term(p: dict) -> float:
    """Orion U_b1"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 4.25e-42
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_OrionM42_UiTerm(p: dict) -> float:
    """Orion inertia"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1e12 * math.math.exp(-t / 1e16)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_OrionM42_X2Term(p: dict) -> float:
    """Orion x₂"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = -3.40e172
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_OrionM42_ActivationTerm(p: dict) -> float:
    """Orion activation"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1e-6 * math.math.cos(600 * 3.14159 * t)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_OrionM42_DirectedEnergyTerm(p: dict) -> float:
    """Orion directed energy"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1e-30 * 1e33
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_OrionM42_NeutronTerm(p: dict) -> float:
    """Orion neutron"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1e6
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_OrionM42_RelativisticTerm(p: dict) -> float:
    """Orion relativistic"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1.69e26
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_OrionM42_NeutrinoTerm(p: dict) -> float:
    """Orion neutrino"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 9.07e-42
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_LagoonM8_DPM_resonanceTerm(p: dict) -> float:
    """Lagoon M8 HII DPM resonance"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1.76e6
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_LagoonM8_Q_waveTerm(p: dict) -> float:
    """Lagoon quantum wave"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1e12 * math.math.sin(1e-15 * t)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_LagoonM8_Ub1Term(p: dict) -> float:
    """Lagoon U_b1"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 4.25e-42
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_LagoonM8_UiTerm(p: dict) -> float:
    """Lagoon inertia"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1e12 * math.math.exp(-t / 1e16)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_LagoonM8_X2Term(p: dict) -> float:
    """Lagoon x₂"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = -3.40e172
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_LagoonM8_ActivationTerm(p: dict) -> float:
    """Lagoon activation"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1e-6 * math.math.cos(600 * 3.14159 * t)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_LagoonM8_DirectedEnergyTerm(p: dict) -> float:
    """Lagoon directed energy"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1e-30 * 1e33
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_LagoonM8_NeutronTerm(p: dict) -> float:
    """Lagoon neutron"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1e6
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_LagoonM8_RelativisticTerm(p: dict) -> float:
    """Lagoon relativistic"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1.69e26
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_LagoonM8_NeutrinoTerm(p: dict) -> float:
    """Lagoon neutrino"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 9.07e-42
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_ESO137_G_timeTerm(p: dict) -> float:
    """ESO137 time-dependent gravity g(r,t)"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (dpm_ug1_seed(M, r)) * (1 + 0.1 * math.math.sin(1e-15 * t))
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_NGC1365A_G_timeTerm(p: dict) -> float:
    """NGC1365 g(r,t) with AGN variability"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (6.674e-11 * 1e40 / 1e38) * (1 + 0.1 * math.math.sin(1e-15 * t))
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_NGC5728_G_timeTerm(p: dict) -> float:
    """NGC5728 g(r,t)"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (6.674e-11 * 5e39 / 1e38) * (1 + 0.05 * math.math.cos(2e-15 * t))
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_Vela_G_timeTerm(p: dict) -> float:
    """Vela Pulsar g(t) at 11.2 Hz"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (6.674e-11 * 2e30 / 1e10) * (1 + 0.2 * math.math.sin(2 * 3.14159 / 0.089 * t))
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_M82_G_timeTerm(p: dict) -> float:
    """M82 Cigar Galaxy g(r,t)"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (6.674e-11 * 3e37 / 1e20) * (1 + 0.15 * math.math.sin(1e-14 * t))
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_Carina_G_timeTerm(p: dict) -> float:
    """Carina Nebula g(r,t)"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (6.674e-11 * 1e36 / 5e18) * (1 + 0.08 * math.math.sin(5e-15 * t))
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_TadpoleGalaxy_G_timeTerm(p: dict) -> float:
    """Tadpole Galaxy g(r,t) with tidal tail"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (6.674e-11 * 2e41 / 1e21) * (1 + 0.12 * math.math.cos(1e-16 * t))
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_MiceGalaxies_G_timeTerm(p: dict) -> float:
    """Mice Galaxies g(r,t) interaction"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (6.674e-11 * 1.5e41 / 8e20) * (1 + 0.1 * math.math.sin(2e-16 * t))
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_NGC2264_G_timeTerm(p: dict) -> float:
    """NGC2264 Christmas Tree g(r,t)"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (6.674e-11 * 8e35 / 3e18) * (1 + 0.06 * math.math.sin(1e-14 * t))
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_M42_G_timeTerm(p: dict) -> float:
    """Orion M42 g(r,t)"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (6.674e-11 * 2e33 / 4e17) * (1 + 0.05 * math.math.cos(3e-15 * t))
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_CrabNebula_G_timeTerm(p: dict) -> float:
    """Crab Nebula g(t) 30 Hz pulsar"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (6.674e-11 * 2e30 / 2e16) * (1 + 0.3 * math.math.sin(2 * 3.14159 / 0.033 * t))
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_BlackWidowPulsar_G_timeTerm(p: dict) -> float:
    """Black Widow Pulsar g(t) 622 Hz"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (6.674e-11 * 1.5e30 / 1e9) * (1 + 0.4 * math.math.sin(2 * 3.14159 * 622 * t))
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_Redback_G_timeTerm(p: dict) -> float:
    """Redback Pulsar g(t) 317 Hz"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (6.674e-11 * 1.4e30 / 5e8) * (1 + 0.35 * math.math.cos(2 * 3.14159 * 317 * t))
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_Fermi_G_timeTerm(p: dict) -> float:
    """Fermi Bubbles g(r,t)"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (6.674e-11 * 1e30 / 1e10) * (1 + 0.25 * math.math.sin(1e-12 * t))
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_Holmberg15A_G_timeTerm(p: dict) -> float:
    """Holmberg 15A supermassive BH g(r,t)"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (6.674e-11 * 4e41 / 1e22) * (1 + 0.05 * math.math.sin(1e-17 * t))
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_NineteenSystems_G_timeTerm(p: dict) -> float:
    """19-system master g(r,t) 26D polynomial"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (6.674e-11 * 1e40 / 1e20) * (1 + 0.1 * math.math.sin(1e-15 * t))
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_WolframHypergraph_G_timeTerm(p: dict) -> float:
    """Wolfram hypergraph g(t) with PI infinity decoder"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (1e40 / 1e40) * (1 + 0.01 * math.math.sin(3.14159265358979323846 * 1e-15 * t))
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_NGC253_G_timeTerm(p: dict) -> float:
    """NGC253 Sculptor Galaxy g(r,t)"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (6.674e-11 * 3e37 / 9e19) * (1 + 0.12 * math.math.sin(5e-15 * t))
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_M87Jet_G_timeTerm(p: dict) -> float:
    """M87 jet g(r,t) with precession"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (6.674e-11 * 6.5e39 / 1e17) * (1 + 0.2 * math.math.sin(1e-13 * t))
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_Centaurus_G_timeTerm(p: dict) -> float:
    """Centaurus A g(r,t)"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (6.674e-11 * 5.5e37 / 3e19) * (1 + 0.1 * math.math.cos(2e-15 * t))
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_Antlia_G_timeTerm(p: dict) -> float:
    """Antlia Dwarf g(r,t)"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (6.674e-11 * 1e39 / 5e19) * (1 + 0.08 * math.math.sin(1e-15 * t))
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_Fornax_G_timeTerm(p: dict) -> float:
    """Fornax Cluster g(r,t)"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (6.674e-11 * 2e39 / 6e19) * (1 + 0.07 * math.math.sin(1e-15 * t))
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_Sculptor_G_timeTerm(p: dict) -> float:
    """Sculptor Dwarf g(r,t)"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (6.674e-11 * 1.5e38 / 2e19) * (1 + 0.06 * math.math.cos(2e-15 * t))
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_LeoI_G_timeTerm(p: dict) -> float:
    """Leo I Dwarf g(r,t)"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (6.674e-11 * 2e37 / 8e18) * (1 + 0.05 * math.math.sin(3e-15 * t))
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_LeoII_G_timeTerm(p: dict) -> float:
    """Leo II g(r,t)"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (6.674e-11 * 1e37 / 7e18) * (1 + 0.04 * math.math.sin(1e-14 * t))
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_UrsaMinor_G_timeTerm(p: dict) -> float:
    """Ursa Minor g(r,t)"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (6.674e-11 * 5e36 / 2e18) * (1 + 0.03 * math.math.cos(5e-15 * t))
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_Draco_G_timeTerm(p: dict) -> float:
    """Draco Dwarf g(r,t)"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (6.674e-11 * 3e36 / 8e17) * (1 + 0.02 * math.math.sin(1e-13 * t))
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_Sextans_G_timeTerm(p: dict) -> float:
    """Sextans Dwarf g(r,t)"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (6.674e-11 * 4e36 / 9e17) * (1 + 0.04 * math.math.sin(2e-14 * t))
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_Carina2_G_timeTerm(p: dict) -> float:
    """Carina II g(r,t)"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (6.674e-11 * 2e36 / 5e17) * (1 + 0.05 * math.math.cos(1e-14 * t))
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_Reticulum_G_timeTerm(p: dict) -> float:
    """Reticulum II g(r,t)"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (6.674e-11 * 1.5e36 / 3e17) * (1 + 0.06 * math.math.sin(5e-15 * t))
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_Tucana_G_timeTerm(p: dict) -> float:
    """Tucana II g(r,t)"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (6.674e-11 * 1e36 / 2e17) * (1 + 0.07 * math.math.sin(1e-14 * t))
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_Horologium_G_timeTerm(p: dict) -> float:
    """Horologium I g(r,t)"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (6.674e-11 * 8e35 / 1.5e17) * (1 + 0.08 * math.math.cos(2e-14 * t))
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_Grus_G_timeTerm(p: dict) -> float:
    """Grus I g(r,t)"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (6.674e-11 * 5e35 / 1e17) * (1 + 0.05 * math.math.sin(3e-15 * t))
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_Phoenix_G_timeTerm(p: dict) -> float:
    """Phoenix Dwarf g(r,t)"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (6.674e-11 * 3e35 / 8e16) * (1 + 0.04 * math.math.sin(1e-13 * t))
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_Eridanus_G_timeTerm(p: dict) -> float:
    """Eridanus II g(r,t)"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (6.674e-11 * 2e35 / 5e16) * (1 + 0.06 * math.math.cos(2e-14 * t))
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_Aquarius_G_timeTerm(p: dict) -> float:
    """Aquarius II g(r,t)"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (6.674e-11 * 1.5e35 / 3e16) * (1 + 0.03 * math.math.sin(1e-12 * t))
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_Pisces_G_timeTerm(p: dict) -> float:
    """Pisces II g(r,t)"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (6.674e-11 * 1e35 / 2e16) * (1 + 0.05 * math.math.sin(5e-15 * t))
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_Pegasus_G_timeTerm(p: dict) -> float:
    """Pegasus III g(r,t)"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (6.674e-11 * 8e34 / 1e16) * (1 + 0.04 * math.math.cos(1e-14 * t))
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_Andromeda_G_timeTerm(p: dict) -> float:
    """Andromeda M31 g(r,t)"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (6.674e-11 * 1e42 / 7e20) * (1 + 0.02 * math.math.sin(1e-16 * t))
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_Triangulum_G_timeTerm(p: dict) -> float:
    """Triangulum M33 g(r,t)"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (6.674e-11 * 5e40 / 2e20) * (1 + 0.03 * math.math.sin(5e-16 * t))
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_LMC_G_timeTerm(p: dict) -> float:
    """Large Magellanic Cloud g(r,t)"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (6.674e-11 * 1e40 / 5e19) * (1 + 0.1 * math.math.sin(1e-15 * t))
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_J1610_G_timeTerm(p: dict) -> float:
    """J1610 quasar g(r,t) z=3.122"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (6.674e-11 * 1.73e40 / 9.63e20) * (1 + 0.15 * math.math.sin(1e-15 * t))
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_JupiterAurorae_G_timeTerm(p: dict) -> float:
    """Jupiter aurorae g(r,t) Io plasma"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (6.674e-11 * 1.898e27 / 7.15e7) * (1 + 0.05 * math.math.sin(2 * 3.14159 / 3.55e4 * t))
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_LagoonM8HII_G_timeTerm(p: dict) -> float:
    """Lagoon M8 HII region g(r,t)"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (6.674e-11 * 1e36 / 1e18) * (1 + 0.08 * math.math.sin(1e-14 * t))
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_M87JetEHT_G_timeTerm(p: dict) -> float:
    """M87 EHT jet g(r,t) v~0.5c"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (6.674e-11 * 1.29e40 / 4.63e19) * (1 + 0.2 * math.math.sin(1e-13 * t))
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_NGC1365Seyfert_G_timeTerm(p: dict) -> float:
    """NGC1365 Seyfert g(r,t)"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (6.674e-11 * 2e39 / 1e19) * (1 + 0.12 * math.math.sin(5e-15 * t))
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_NGC2207IC2163_G_timeTerm(p: dict) -> float:
    """NGC2207/IC2163 collision g(r,t)"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (6.674e-11 * 3e40 / 2e20) * (1 + 0.1 * math.math.cos(1e-16 * t))
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_RAquarii_G_timeTerm(p: dict) -> float:
    """R Aquarii symbiotic g(r,t) 44yr period"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (6.674e-11 * 2e30 / 4e11) * (1 + 0.3 * math.math.sin(2 * 3.14159 / (44 * 365 * 86400) * t))
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_SgrAStarGC_G_timeTerm(p: dict) -> float:
    """Sgr A* Galactic Center g(r,t) flares"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (6.674e-11 * 8.56e36 / 6.17e18) * (1 + 0.25 * math.math.sin(1e-12 * t))
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_SPTCLJ2215_G_timeTerm(p: dict) -> float:
    """SPT-CL J2215 cluster g(r,t) z=0.7"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (6.674e-11 * 1e44 / 1e22) * (1 + 0.05 * math.math.sin(1e-17 * t))
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_StephanQuintet_G_timeTerm(p: dict) -> float:
    """Stephan's Quintet g(r,t) JWST"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (6.674e-11 * 5e40 / 2e20) * (1 + 0.08 * math.math.cos(1e-16 * t))
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_VelaPulsarSNR_G_timeTerm(p: dict) -> float:
    """Vela SNR g(t) 89ms pulsar"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (6.674e-11 * 2e30 / 3e16) * (1 + 0.35 * math.math.sin(2 * 3.14159 / 0.089 * t))
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_NGC4993GW170817_G_timeTerm(p: dict) -> float:
    """NGC4993 GW170817 kilonova g(r,t) NS merger"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (6.674e-11 * 5e38 / 4e19) * (1 + 0.5 * math.math.exp(-t / 1e7))
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_NGC6240DualAGN_G_timeTerm(p: dict) -> float:
    """NGC6240 dual AGN g(r,t)"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (6.674e-11 * 3e39 / 1e19) * (1 + 0.15 * math.math.sin(1e-14 * t))
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_OmegaCentauri_G_timeTerm(p: dict) -> float:
    """Omega Centauri globular g(r,t)"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (6.674e-11 * 4e36 / 5e17) * (1 + 0.02 * math.math.sin(1e-13 * t))
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_OrionM42Trapezium_G_timeTerm(p: dict) -> float:
    """Orion Trapezium g(r,t)"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (6.674e-11 * 2e33 / 4e17) * (1 + 0.06 * math.math.sin(1e-14 * t))
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_PerseusAbell426_G_timeTerm(p: dict) -> float:
    """Perseus Abell 426 cluster g(r,t)"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (6.674e-11 * 1e45 / 5e21) * (1 + 0.03 * math.math.sin(1e-17 * t))
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_PolarRingNGC4650A_G_timeTerm(p: dict) -> float:
    """Polar Ring NGC4650A g(r,t)"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (6.674e-11 * 2e39 / 8e19) * (1 + 0.1 * math.math.cos(1e-15 * t))
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_QuasarJ0313_G_timeTerm(p: dict) -> float:
    """Quasar J0313 z=7.64 earliest g(r,t)"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (6.674e-11 * 1.6e39 / 1e21) * (1 + 0.2 * math.math.sin(1e-15 * t))
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_RXJ1713TeVSNR_G_timeTerm(p: dict) -> float:
    """RX J1713 TeV SNR g(r,t)"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (6.674e-11 * 1e31 / 2e17) * (1 + 0.4 * math.math.sin(1e-11 * t))
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_SMC_G_timeTerm(p: dict) -> float:
    """Small Magellanic Cloud g(r,t)"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (6.674e-11 * 7e39 / 6e19) * (1 + 0.08 * math.math.sin(1e-15 * t))
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_BuoyancyAstro5_G_timeTerm(p: dict) -> float:
    """5-system buoyancy g(r,t)"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (6.674e-11 * 1e40 / 1e20) * (1 + 0.1 * math.math.sin(1e-15 * t))
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_BuoyancyCNB_G_timeTerm(p: dict) -> float:
    """Centaurus A CNB g(r,t)"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (6.674e-11 * 5e37 / 3e19) * (1 + 0.05 * math.math.sin(1e-16 * t))
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_AstroSystems163_G_timeTerm(p: dict) -> float:
    """NGC 685/3507/3511 + TDE g(r,t)"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (6.674e-11 * 2e39 / 5e19) * (1 + 0.08 * math.math.cos(1e-15 * t))
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_NebulaTriadic_G_timeTerm(p: dict) -> float:
    """Triadic nebula g(r,t) ionization"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (6.674e-11 * 1e35 / 1e18) * (1 + 0.06 * math.math.sin(1e-14 * t))
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_BuoyancyModule_G_timeTerm(p: dict) -> float:
    """M74/Eagle/M84 buoyancy g(r,t)"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (6.674e-11 * 3e37 / 1e19) * (1 + 0.07 * math.math.sin(1e-15 * t))
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_UQFF8Astro_G_timeTerm(p: dict) -> float:
    """8-system 26-state g(r,t)"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (6.674e-11 * 1e40 / 1e20) * (1 + 0.1 * math.math.sin(1e-15 * t))
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_UQFFCore_G_timeTerm(p: dict) -> float:
    """M82/IC418/Canis core g(r,t)"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (6.674e-11 * 2e37 / 5e18) * (1 + 0.09 * math.math.sin(1e-14 * t))
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_Buoyancy168_G_timeTerm(p: dict) -> float:
    """SN1006/EtaCar/Chandra g(r,t)"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (6.674e-11 * 1e31 / 2e17) * (1 + 0.15 * math.math.sin(1e-12 * t))
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_CassiniBuoyancy_G_timeTerm(p: dict) -> float:
    """Saturn Cassini g(r,t) 29.5yr orbit"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (6.674e-11 * 5.68e26 / 1.4e8) * (1 + 0.02 * math.math.sin(2 * 3.14159 / (29.5 * 365 * 86400) * t))
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_MultiAstro_G_timeTerm(p: dict) -> float:
    """11-system NGC/Cassini/ESO g(r,t)"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (6.674e-11 * 1e40 / 1e20) * (1 + 0.08 * math.math.sin(1e-15 * t))
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_EightAstro_G_timeTerm(p: dict) -> float:
    """AFGL5180/NGC346/LMC g(r,t)"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (6.674e-11 * 1e36 / 1e18) * (1 + 0.07 * math.math.sin(1e-14 * t))
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_NineteenAstro26D_G_timeTerm(p: dict) -> float:
    """19-system 26D master g(r,t)"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (6.674e-11 * 1e40 / 1e20) * (1 + 0.1 * math.math.sin(1e-15 * t))
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_WolframFieldUnity_G_timeTerm(p: dict) -> float:
    """Wolfram hypergraph g(t) 312-digit PI infinity decoder"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (1e40 / 1e40) * (1 + 0.01 * math.math.sin(3.14159265358979323846 * 1e-15 * t))
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_ESO137_HeavisideFactor(p: dict) -> float:
    """ESO137 Heaviside threshold amplification factor 1+10^13*f"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1.0 + scale * f_Heaviside
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_ESO137_UmBase(p: dict) -> float:
    """ESO137 U_m magnetic energy base (no Heaviside/Quasi)"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = mu_over_rj * one_minus_exp * 1.0 * 1.0 * E_react
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_ESO137_UmContribution(p: dict) -> float:
    """ESO137 full U_m with Heaviside & Quasi amplification"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = base * heaviside_f * quasi_f
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_NGC1365A_H_SCm(p: dict) -> float:
    """NGC1365A heliosphere thickness factor H_SCm"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_NGC1365A_U_g2(p: dict) -> float:
    """NGC1365A U_g2 with heliosphere thickness H_SCm"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = k_2 * (rho_sum * M_s / (r * r)) * S_r_Rb * swirl_factor * H_SCm * E_react
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_NGC1365A_SwirlFactor(p: dict) -> float:
    """NGC1365A solar wind swirl factor (1 + delta*v)"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1.0 + delta_sw * v_sw
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_NGC5728_U_gi(p: dict) -> float:
    """NGC5728 indexed gravitational field U_gi"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = k_i * (rho_vac_UA * M_s / (r * r)) * E_react
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_NGC5728_K_i(p: dict) -> float:
    """NGC5728 coupling constant k_i for U_gi summation"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_NGC5728_SumKUgi(p: dict) -> float:
    """NGC5728 total Σk_i·U_gi gravity sum (i=1 to 4)"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = sum
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_MagneticMoment_Mu_j(p: dict) -> float:
    """Time-dependent magnetic moment μ_j(t) with solar cycle"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = mu_0 * (1.0 + 0.3 * math.math.sin(omega_c * t))
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_MagneticMoment_B_j(p: dict) -> float:
    """Magnetic field strength B_j(t) = μ_j/r³"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = mu_j / (r * r * r)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_MagneticMoment_UmContrib(p: dict) -> float:
    """U_m magnetic energy with time-varying μ_j(t)"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (mu_j / r_j) * one_minus_exp * 1.0 * 1.0 * E_react
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_BlackHole_M_bh(p: dict) -> float:
    """Galactic black hole mass M_bh"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1e37
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_BlackHole_M_bhInMsun(p: dict) -> float:
    """Black hole mass in solar masses M_bh/M_sun"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = M_bh / M_sun
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_BlackHole_U_b1(p: dict) -> float:
    """U_b1 buoyancy term with galactic SMBH"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (M_bh / D_g) * E_react
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_NegativeTime_T_n(p: dict) -> float:
    """Reciprocation time t_n = -t for time-reversal symmetry"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = -t
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_NegativeTime_CosPiTn(p: dict) -> float:
    """Cosine of reciprocation phase cos(π·t_n)"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = math.math.cos(3.14159 * t_n)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_NegativeTime_OneMinusExp(p: dict) -> float:
    """Time buildup factor (1 - exp) with t_n modulation"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1.0 - math.math.exp(exp_arg)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_PiConstant_Pi(p: dict) -> float:
    """Pi constant π (3.14159..., Wolfram 312-digit precision)"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 3.14159265358979323846
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_PiConstant_CosPiTn(p: dict) -> float:
    """Pi-based cosine phase cos(π·t_n)"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = math.math.cos(3.14159265358979323846 * t_n)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_PiConstant_SinOmegaCT(p: dict) -> float:
    """Solar cycle sine modulation sin(ω_c·t)"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = math.math.sin(omega_c * t)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_Decay_Gamma_s(p: dict) -> float:
    """Reciprocation decay rate γ in s^-1"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = gamma_day / 86400.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_Decay_ExpTerm(p: dict) -> float:
    """Exponential decay term exp(-γt·cos(πt_n))"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = math.math.exp(exp_arg)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_Decay_OneMinusExp(p: dict) -> float:
    """Time buildup (1 - exp(-γt·cos(πt_n)))"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1.0 - math.math.exp(exp_arg)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_ScmPenetration_P_SCm(p: dict) -> float:
    """SCm penetration factor P_SCm"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_ScmPenetration_UmPlanet(p: dict) -> float:
    """Planetary U_m with reduced P_SCm"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = U_m_base * P_SCm_planet
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_ScmReactivity_Kappa_day(p: dict) -> float:
    """Reactivity decay rate (day^-1)"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0005
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_ScmReactivity_E_reactDecay(p: dict) -> float:
    """E_react exponential decay"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = E_0 * math.math.exp(-kappa_day * t_day)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_SolarCycle_Omega_c(p: dict) -> float:
    """Solar cycle angular frequency ω_c"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 2.0 * 3.14159 / T_cycle
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_SolarCycle_PeriodYears(p: dict) -> float:
    """Solar cycle period (years)"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 11.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_SolarCycle_MuJ_Modulated(p: dict) -> float:
    """Magnetic moment with 11-year modulation"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (1000.0 + 0.4 * math.math.sin(omega_c * t)) * mu_0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_SolarCycle_B_j_Modulated(p: dict) -> float:
    """Surface magnetic field with solar cycle"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = B_0 * (1.0 + 0.3 * math.math.sin(omega_c * t))
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_SolarWind_Delta_sw(p: dict) -> float:
    """Solar wind modulation δ_sw"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.01
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_SolarWind_ModulationFactor(p: dict) -> float:
    """Modulation factor (1 + δ_sw)"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (1.0 + delta_sw)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_SolarWind_U_g2_NoMod(p: dict) -> float:
    """U_g2 without solar wind modulation"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (G * M_s) / (r * r)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_SolarWindVelocity_V_sw(p: dict) -> float:
    """Solar wind velocity v_sw"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 5e5
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_SolarWindVelocity_V_swKmS(p: dict) -> float:
    """Solar wind velocity (km/s)"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 500.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_SolarWindVelocity_U_g2_NoSW(p: dict) -> float:
    """U_g2 with v_sw=0"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (G * M_s) / (r * r)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_StellarMass_M_s(p: dict) -> float:
    """Stellar mass M_s"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1.989e30
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_StellarMass_M_sInMsun(p: dict) -> float:
    """Stellar mass in solar masses"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_ReactorEfficiencyTerm(p: dict) -> float:
    """Ereact: Core reactivity efficiency = (rho_SCm * v_SCm^2 / rho_A) * exp(-kappa*t)"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = energy_ratio * math.math.exp(-kappa * t)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_MuSTerm(p: dict) -> float:
    """mu_s: Magnetic dipole moment = Bs(t) * Rs^3"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = Bs * Rs * Rs * Rs
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_GradMsRTerm(p: dict) -> float:
    """grad_Ms_r: Surface gravity gradient = G*Ms/Rs^2"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = G_SOURCE4 * M / (R * R)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_BjTerm(p: dict) -> float:
    """Bj: Magnetic string field = 1e-3 + 0.4*sin(omega_c*t) + rho_SCm*1e-18"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1e-3 + 0.4 * math.math.sin(omega_c * t) + rho_SCm * 1e-18
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_OmegaSTTerm(p: dict) -> float:
    """omega_s_t: String rotation frequency = omega_s - 0.4e-6*sin(omega_c*t)"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = omega_s - 0.4e-6 * math.math.sin(omega_c * t)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_MuJTerm(p: dict) -> float:
    """mu_j: String magnetic dipole moment = Bj(t) * Rs^3"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = Bj * Rs * Rs * Rs
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_UniversalGravity1Term(p: dict) -> float:
    """Ug1: Magnetic dipole-gradient gravity with defect modulation"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = k1 * mu_s * grad_Ms_r * math.math.exp(-alpha * t) * math.math.cos(PI_SOURCE4 * tn) * defect
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_UniversalGravity2Term(p: dict) -> float:
    """Ug2: Charge-reactivity gravity with solar wind modulation"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = k2 * (QA + QUA) * M / (r * r) * S * wind_mod * HSCm * Ereact
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_UniversalGravity3Term(p: dict) -> float:
    """Ug3: Magnetic string rotation gravity"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = k3 * Bj * math.math.cos(omega_s_t * t * PI_SOURCE4) * Pcore * Ereact
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_UniversalGravity4Term(p: dict) -> float:
    """Ug4: Vacuum energy concentration gravity"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = k4 * rho_v * C_concentration * Mbh / dg * decay * cycle * (1 + f_feedback)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_UniversalBuoyancyTerm(p: dict) -> float:
    """Ubi: Universal buoyancy from galactic rotation"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = -beta_i * Ugi * Omega_g * Mbh / dg * wind_mod * UUA * math.math.cos(PI_SOURCE4 * tn)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_UniversalMagnetismTerm(p: dict) -> float:
    """Um: Billion magnetic strings"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = single * num_strings * PSCm * Ereact
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_UniversalAetherTerm(p: dict) -> float:
    """A_mu_nu: Cosmic aether metric tensor trace"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = -2.0 + 4.0 * mod
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_UnifiedFieldTerm(p: dict) -> float:
    """FU: Complete unified field (sum_Ugi + sum_Ubi + Um + A_mu_nu_trace)"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = sum_Ugi + sum_Ubi + Um + A_scalar
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_MUGECompressedBaseTerm(p: dict) -> float:
    """Compressed MUGE base term: G*M/r^2 (Newtonian gravitational acceleration)"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = G_SOURCE4 * M / (r * r)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_MUGEExpansionTerm(p: dict) -> float:
    """Hubble expansion factor: 1 + H0*t where H0 = 2.269e-18 s^-1"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1.0 + H0_SOURCE4 * t_sys
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_MUGESuperAdjustmentTerm(p: dict) -> float:
    """Superconductive magnetic adjustment: 1 - B/Bcrit"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1.0 - B / Bcrit
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_MUGEEnvelopeTerm(p: dict) -> float:
    """Envelope modulation factor (currently neutral = 1.0)"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_MUGEUgSumTerm(p: dict) -> float:
    """Sum of Ug1-4 components (extensible)"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_MUGECosmologicalTerm(p: dict) -> float:
    """Cosmological constant term: Lambda*c^2/3 (dark energy acceleration)"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = Lambda_SOURCE4 * c_SOURCE4 * c_SOURCE4 / 3.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_MUGEQuantumTerm(p: dict) -> float:
    """Quantum uncertainty term: (hbar/Delta_xp)*integral_psi*(2*PI/tHubble)"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (hbar_SOURCE4 / Delta_x_p) * integral_psi * (2.0 * PI_SOURCE4 / tHubble)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_MUGEFluidTerm(p: dict) -> float:
    """Fluid dynamics term: rho_fluid * Vsys * g_local (Navier-Stokes coupling)"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = rho_fluid * Vsys * g_local
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_MUGEPerturbationTerm(p: dict) -> float:
    """Dark matter perturbation: (M+M_DM)*(delta_rho/rho + 3*G*M/r^3)"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (M + M_DM) * (delta_rho_rho + 3.0 * G_SOURCE4 * M / (r * r * r))
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_MUGEResonanceADPMTerm(p: dict) -> float:
    """Base DPM acceleration: aDPM = FDPM * fDPM * Evac_neb * c_res * Vsys"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = FDPM * fDPM * Evac_neb * c_res * Vsys
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_MUGEResonanceATHzTerm(p: dict) -> float:
    """THz frequency contribution: aTHz = fTHz * Evac_neb * vexp * aDPM / (Evac_ISM * c_res)"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_MUGEResonanceAvacDiffTerm(p: dict) -> float:
    """Vacuum energy differential: avac_diff = Delta_Evac * vexp^2 * aDPM / (Evac_neb * c_res^2)"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_MUGEResonanceASuperFreqTerm(p: dict) -> float:
    """Superconductive frequency resonance: asuper_freq = Fsuper * fTHz * aDPM / (Evac_neb * c_res)"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_MUGEResonanceAAetherResTerm(p: dict) -> float:
    """Aether resonance coupling: aaether_res = UA_SCM * omega_i * fTHz * aDPM * (1 + fTRZ)"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = UA_SCM * omega_i * fTHz * aDPM * (1.0 + fTRZ)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_MUGEResonanceUg4iTerm(p: dict) -> float:
    """Reactor gravity component: Ug4i = k4_res * Ereact * freact * aDPM / (Evac_neb * c_res)"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_MUGEResonanceAQuantumFreqTerm(p: dict) -> float:
    """Quantum frequency contribution: aquantum_freq = fquantum * Evac_neb * aDPM / (Evac_ISM * c_res)"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_MUGEResonanceAAetherFreqTerm(p: dict) -> float:
    """Aether frequency component: aAether_freq = fAether * Evac_neb * aDPM / (Evac_ISM * c_res)"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_MUGEResonanceAFluidFreqTerm(p: dict) -> float:
    """Fluid dynamics frequency: afluid_freq = ffluid * Evac_neb * Vsys / (Evac_ISM * c_res)"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_MUGEResonanceOscTerm(p: dict) -> float:
    """Oscillatory term (simplified to zero in current implementation)"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_MUGEResonanceAExpFreqTerm(p: dict) -> float:
    """Expansion frequency (Hubble): aexp_freq = fexp * Evac_neb * aDPM / (Evac_ISM * c_res), fexp = 2*PI*H"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_MUGEResonanceFTRZTerm(p: dict) -> float:
    """TRZ factor component (pass-through): returns fTRZ parameter directly"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_MUGEResonanceWormholeTerm(p: dict) -> float:
    """Wormhole metric contribution: a_wormhole = f_worm * Evac_neb / (b^2 + r^2)"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_TauLeptonDipoleTerm(p: dict) -> float:
    """Tau anomalous magnetic moment: a_tau = a_tau_SM + a_tau_BSM (arXiv:2506.15245)"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = total
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_CKMVcbTerm(p: dict) -> float:
    """CKM matrix element |V_cb| = (39.2+/-0.9)e-3 from Belle II B->D l nu (arXiv:2506.15256)"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_LFVBranchingTerm(p: dict) -> float:
    """LFV branching ratio B0->K*0 tau+/-e-/+: BR < 5.9e-6 at 90% CL (arXiv:2506.15347)"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = params.at("BR_theory")
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_VectorLikeQuarkTerm(p: dict) -> float:
    """VLQ coupling kappa(m): singlet T [0.22,0.52], triplet TBY [0.14,0.46] (arXiv:2506.15515)"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = kappa_min + mass_frac * (kappa_max - kappa_min)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_UnifiedFieldTerm_SOURCE4(p: dict) -> float:
    """FU: Complete unified field (sum_Ugi + sum_Ubi + Um + A_mu_nu_trace)"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = sum_Ugi + sum_Ubi + Um + A_scalar
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_MUGESuperAdjTerm(p: dict) -> float:
    """Superconductive magnetic adjustment: 1 - B/Bcrit"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_MUGECosmTerm(p: dict) -> float:
    """Cosmological constant term: Lambda*c^2/3"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = Lambda_SOURCE4 * c_SOURCE4 * c_SOURCE4 / 3.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_SGR1745MagnetarTerm(p: dict) -> float:
    """SGR 1745-2900: Magnetar system (I=1e21, M=2.984e30 kg, B=1e10 T)"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = compute_resonance_MUGE_SOURCE4(sgr1745_SOURCE4, res_params_default_SOURCE4)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_SagittariusAStarTerm(p: dict) -> float:
    """Sagittarius A*: Supermassive black hole (M=8.155e36 kg, M_DM=1e37 kg)"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = compute_resonance_MUGE_SOURCE4(sagA_SOURCE4, res_params_default_SOURCE4)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_TapestryStarbirthTerm(p: dict) -> float:
    """Tapestry of Blazing Starbirth: Nebula (M=1.989e35 kg, Vsys=1e53 m^3)"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = compute_resonance_MUGE_SOURCE4(tapestry_SOURCE4, res_params_default_SOURCE4)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_Westerlund2ClusterTerm(p: dict) -> float:
    """Westerlund 2: Stellar cluster (similar to Tapestry parameters)"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = compute_resonance_MUGE_SOURCE4(westerlund_SOURCE4, res_params_default_SOURCE4)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_PillarsCreationTerm(p: dict) -> float:
    """Pillars of Creation: Molecular cloud (M=1.989e32 kg, r=1 ly)"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = compute_resonance_MUGE_SOURCE4(pillars_SOURCE4, res_params_default_SOURCE4)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_RingsRelativityTerm(p: dict) -> float:
    """Rings of Relativity: Cosmological structure (M=1.989e36 kg, z=0.01)"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = compute_resonance_MUGE_SOURCE4(rings_SOURCE4, res_params_default_SOURCE4)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_StudentGuideUniverseTerm(p: dict) -> float:
    """Student's Guide to the Universe: Observable universe (M=1e53 kg, r=10 Gly)"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = compute_resonance_MUGE_SOURCE4(student_guide_SOURCE4, res_params_default_SOURCE4)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_CompressedMUGETerm_SOURCE4(p: dict) -> float:
    """Compressed MUGE: 9-term gravity equation wrapper"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = compute_compressed_MUGE_SOURCE4(system)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_ResonanceMUGETerm_SOURCE4(p: dict) -> float:
    """Resonance MUGE: 13-term + wormhole resonance equation wrapper"""
    # Type: PhysicsTerm
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = compute_resonance_MUGE_SOURCE4(system, res_params_default_SOURCE4)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_getDynamicParameter(p: dict) -> float:
    """Function: getDynamicParameter"""
    # Type: function
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_getLearningRate(p: dict) -> float:
    """Function: getLearningRate"""
    # Type: function
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = learningRate
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_computeWithNested(p: dict) -> float:
    """Function: computeWithNested"""
    # Type: function
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = result
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_computeModule(p: dict) -> float:
    """Function: computeModule"""
    # Type: function
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_getGlobalParameter(p: dict) -> float:
    """Function: getGlobalParameter"""
    # Type: function
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_correlationCoefficient(p: dict) -> float:
    """Function: correlationCoefficient"""
    # Type: function
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_calculateDipMomentumEnergy(p: dict) -> float:
    """Function: calculateDipMomentumEnergy"""
    # Type: function
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = E_DPM_i
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_compute_E_cm(p: dict) -> float:
    """Function: compute_E_cm"""
    # Type: function
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = p.M * c_light * c_light
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_dpm_life_proportion(p: dict) -> float:
    """Function: dpm_life_proportion"""
    # Type: function
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = modulated
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_F_U_Bi_i(p: dict) -> float:
    """Function: F_U_Bi_i"""
    # Type: function
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = F_U_Bi_i_result
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_compressed_g(p: dict) -> float:
    """Function: compressed_g"""
    # Type: function
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = g_total
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_F_jet_rel(p: dict) -> float:
    """Function: F_jet_rel"""
    # Type: function
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = p.F_rel * gamma
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_E_acc_rel(p: dict) -> float:
    """Function: E_acc_rel"""
    # Type: function
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = p.M * c_light * c_light * p.v / (2 * c_light)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_F_drag_rel(p: dict) -> float:
    """Function: F_drag_rel"""
    # Type: function
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.5 * p.rho_vac_UA * p.v * p.v * math.pi * p.r * p.r
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_F_gw_rel(p: dict) -> float:
    """Function: F_gw_rel"""
    # Type: function
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = G * p.M * p.M / (c_light * c_light * c_light * c_light * p.r) * p.omega0 * p.omega0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_computeH_SCm(p: dict) -> float:
    """Function: computeH_SCm"""
    # Type: function
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["H_SCm"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_computeU_g2(p: dict) -> float:
    """Function: computeU_g2"""
    # Type: function
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = k_2 * (rho_sum * M_s / (r * r)) * S_r_Rb * swirl_factor * H_SCm * E_react
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_computeU_g2_no_H(p: dict) -> float:
    """Function: computeU_g2_no_H"""
    # Type: function
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = result
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_getH_SCm(p: dict) -> float:
    """Function: getH_SCm"""
    # Type: function
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables.at("H_SCm")
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_computeU_gi(p: dict) -> float:
    """Function: computeU_gi"""
    # Type: function
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables[key]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_computeK_i(p: dict) -> float:
    """Function: computeK_i"""
    # Type: function
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = k_values[0]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_computeKUgi(p: dict) -> float:
    """Function: computeKUgi"""
    # Type: function
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = computeK_i(i) * computeU_gi(i)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_computeSumKUgi(p: dict) -> float:
    """Function: computeSumKUgi"""
    # Type: function
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = sum
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_getComponentContribution(p: dict) -> float:
    """Function: getComponentContribution"""
    # Type: function
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = computeKUgi(i)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_getTotalForce(p: dict) -> float:
    """Function: getTotalForce"""
    # Type: function
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = computeSumKUgi()
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_computeLambda_i(p: dict) -> float:
    """Function: computeLambda_i"""
    # Type: function
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["lambda"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_computeU_i(p: dict) -> float:
    """Function: computeU_i"""
    # Type: function
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = lambda_i * rho_sc * rho_ua * omega_s_t * cos_term * trz_factor
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_computeInertiaTerm(p: dict) -> float:
    """Function: computeInertiaTerm"""
    # Type: function
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = -computeLambda_i(i) * u_i * e_react
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_computeSumInertiaTerms(p: dict) -> float:
    """Function: computeSumInertiaTerms"""
    # Type: function
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = sum
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_getInertiaResistance(p: dict) -> float:
    """Function: getInertiaResistance"""
    # Type: function
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = computeInertiaTerm(component, 0.0)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_getTotalInertia(p: dict) -> float:
    """Function: getTotalInertia"""
    # Type: function
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = computeSumInertiaTerms(0.0)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_computeMu_j(p: dict) -> float:
    """Function: computeMu_j"""
    # Type: function
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = B_j * base_mu
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_computeUmContrib(p: dict) -> float:
    """Function: computeUmContrib"""
    # Type: function
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = mu_j / (r_j * r_j * r_j)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_computeB_j(p: dict) -> float:
    """Function: computeB_j"""
    # Type: function
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = base + amplitude * math.math.sin(omega_c * t)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_computeUg3Contrib(p: dict) -> float:
    """Function: computeUg3Contrib"""
    # Type: function
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = mu_j / (r_j * r_j * r_j)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_getMagneticMoment(p: dict) -> float:
    """Function: getMagneticMoment"""
    # Type: function
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = computeMu_j(1, t)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_getFieldStrength(p: dict) -> float:
    """Function: getFieldStrength"""
    # Type: function
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = computeB_j(t)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_computeM_bhInMsun(p: dict) -> float:
    """Function: computeM_bhInMsun"""
    # Type: function
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["M_bh"] / variables["M_sun"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_computeMbhOverDg(p: dict) -> float:
    """Function: computeMbhOverDg"""
    # Type: function
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["M_bh"] / variables["d_g"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_computeU_b1(p: dict) -> float:
    """Function: computeU_b1"""
    # Type: function
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (mbh / (dg * dg)) * cos_term * sw_term * decay * feedback
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_computeU_g4(p: dict) -> float:
    """Function: computeU_g4"""
    # Type: function
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = computeU_b1() * 1e6
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_computeM_bh(p: dict) -> float:
    """Function: computeM_bh"""
    # Type: function
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["M_bh"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_getBlackHoleMass(p: dict) -> float:
    """Function: getBlackHoleMass"""
    # Type: function
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["M_bh"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_getBlackHoleMassSolarUnits(p: dict) -> float:
    """Function: getBlackHoleMassSolarUnits"""
    # Type: function
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = computeM_bhInMsun()
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_getUb1Contribution(p: dict) -> float:
    """Function: getUb1Contribution"""
    # Type: function
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = computeU_b1()
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_computeT_n(p: dict) -> float:
    """Function: computeT_n"""
    # Type: function
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = t - vars["t_0"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_computeCosPiTn(p: dict) -> float:
    """Function: computeCosPiTn"""
    # Type: function
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = math.math.cos(vars["pi"] * computeT_n(t))
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_computeExpTerm(p: dict) -> float:
    """Function: computeExpTerm"""
    # Type: function
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = math.math.exp(-g * t * computeCosPiTn(t))
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_computeOneMinusExp(p: dict) -> float:
    """Function: computeOneMinusExp"""
    # Type: function
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1.0 - computeExpTerm(g, t)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_getTnValue(p: dict) -> float:
    """Function: getTnValue"""
    # Type: function
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = computeT_n(t)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_computePi(p: dict) -> float:
    """Function: computePi"""
    # Type: function
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = vars["pi"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_computeCosPiTn_1(p: dict) -> float:
    """Function: computeCosPiTn"""
    # Type: function
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = math.math.cos(computePi() * tn)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_computeSinOmegaCT(p: dict) -> float:
    """Function: computeSinOmegaCT"""
    # Type: function
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = math.math.sin(vars["omega_c"] * t)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_getPiValue(p: dict) -> float:
    """Function: getPiValue"""
    # Type: function
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = vars["pi"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_computeP_core(p: dict) -> float:
    """Function: computeP_core"""
    # Type: function
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = vars["P_core"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_computeU_g3(p: dict) -> float:
    """Function: computeU_g3"""
    # Type: function
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = vars["k_3"] * vars["B_j"] * math.math.cos(vars["omega_s"] * t * vars["pi"]) * computeP_core() * vars["E_react"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_computeF_quasi(p: dict) -> float:
    """Function: computeF_quasi"""
    # Type: function
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = vars["f_quasi"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_computeQuasiFactor(p: dict) -> float:
    """Function: computeQuasiFactor"""
    # Type: function
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1.0 + vars["f_quasi"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_getQuasiWaveFactor(p: dict) -> float:
    """Function: getQuasiWaveFactor"""
    # Type: function
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = vars["f_quasi"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_computeR_b(p: dict) -> float:
    """Function: computeR_b"""
    # Type: function
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = vars["R_b"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_computeR_bInAU(p: dict) -> float:
    """Function: computeR_bInAU"""
    # Type: function
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = vars["R_b"] / vars["AU"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_computeS_r_Rb(p: dict) -> float:
    """Function: computeS_r_Rb"""
    # Type: function
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_getBubbleRadius(p: dict) -> float:
    """Function: getBubbleRadius"""
    # Type: function
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = vars["R_b"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_computeGamma_day(p: dict) -> float:
    """Function: computeGamma_day"""
    # Type: function
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = vars["gamma_day"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_computeGamma_s(p: dict) -> float:
    """Function: computeGamma_s"""
    # Type: function
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = vars["gamma_day"] / vars["day_to_s"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_computeExpTerm_1(p: dict) -> float:
    """Function: computeExpTerm"""
    # Type: function
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = math.math.exp(-vars["gamma_day"] * t_day * cos_term)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_computeOneMinusExp_1(p: dict) -> float:
    """Function: computeOneMinusExp"""
    # Type: function
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1.0 - computeExpTerm(t_day, tn)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_getDecayRate(p: dict) -> float:
    """Function: getDecayRate"""
    # Type: function
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = vars["gamma_day"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_getTimescale(p: dict) -> float:
    """Function: getTimescale"""
    # Type: function
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1.0 / vars["gamma_day"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_computeP_SCm(p: dict) -> float:
    """Function: computeP_SCm"""
    # Type: function
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = vars["P_SCm"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_getSCmPenetration(p: dict) -> float:
    """Function: getSCmPenetration"""
    # Type: function
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = vars["P_SCm"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_computeAlpha(p: dict) -> float:
    """Function: computeAlpha"""
    # Type: function
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = vars["alpha"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_computeE_react(p: dict) -> float:
    """Function: computeE_react"""
    # Type: function
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = vars["E_react_0"] * math.math.exp(-vars["alpha"] * t)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_getDecayConstant(p: dict) -> float:
    """Function: getDecayConstant"""
    # Type: function
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = vars["alpha"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_computeOmega_c(p: dict) -> float:
    """Function: computeOmega_c"""
    # Type: function
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = vars["omega_c"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_computeSinOmegaCT_1(p: dict) -> float:
    """Function: computeSinOmegaCT"""
    # Type: function
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = math.math.sin(vars["omega_c"] * t)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_computeModulationFactor(p: dict) -> float:
    """Function: computeModulationFactor"""
    # Type: function
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1.0 + vars["gamma_sw"] * vars["rho_vac_sw"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_getGammaSw(p: dict) -> float:
    """Function: getGammaSw"""
    # Type: function
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = vars["gamma_sw"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_computeV_sw(p: dict) -> float:
    """Function: computeV_sw"""
    # Type: function
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = vars["v_sw"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_computeGammaSwVsw(p: dict) -> float:
    """Function: computeGammaSwVsw"""
    # Type: function
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = vars["gamma_sw"] * vars["v_sw"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_computeS_rRb(p: dict) -> float:
    """Function: computeS_rRb"""
    # Type: function
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_computeHeavisideFactor(p: dict) -> float:
    """Function: computeHeavisideFactor"""
    # Type: function
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1.0 + 1e13 * vars["f_Heaviside"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_computeT_00(p: dict) -> float:
    """Function: computeT_00"""
    # Type: function
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = vars["T_00"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_computeTrace(p: dict) -> float:
    """Function: computeTrace"""
    # Type: function
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = vars["T_00"] - vars["T_11"] - vars["T_22"] - vars["T_33"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_computeM(p: dict) -> float:
    """Function: computeM"""
    # Type: function
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = vars["M"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_computeMInSolarMasses(p: dict) -> float:
    """Function: computeMInSolarMasses"""
    # Type: function
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = vars["M"] / vars["M_sun"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_computeOmega_s(p: dict) -> float:
    """Function: computeOmega_s"""
    # Type: function
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = vars["omega_s"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_computeRotationPeriod(p: dict) -> float:
    """Function: computeRotationPeriod"""
    # Type: function
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 2.0 * 3.14159265359 / vars["omega_s"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_computeB_j_1(p: dict) -> float:
    """Function: computeB_j"""
    # Type: function
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = vars["B_base"] + vars["amplitude"] * math.math.sin(vars["omega_c"] * t)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_computeT_eff(p: dict) -> float:
    """Function: computeT_eff"""
    # Type: function
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = vars["T_eff"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_computeLuminosity(p: dict) -> float:
    """Function: computeLuminosity"""
    # Type: function
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 4.0 * 3.14159265359 * radius * radius * vars["sigma_SB"] * pow(vars["T_eff"], 4)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_computeF_TRZ(p: dict) -> float:
    """Function: computeF_TRZ"""
    # Type: function
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = vars["f_TRZ"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_computeTRZFactor(p: dict) -> float:
    """Function: computeTRZFactor"""
    # Type: function
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1.0 + vars["f_TRZ"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_computeU_g1(p: dict) -> float:
    """Function: computeU_g1"""
    # Type: function
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = vars["k_1"] * vars["U_g1_base"] * vars["defect_factor"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_computeU_g3_1(p: dict) -> float:
    """Function: computeU_g3"""
    # Type: function
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = vars["k_3"] * vars["U_g3_base"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_computeRho_vac_aether(p: dict) -> float:
    """Function: computeRho_vac_aether"""
    # Type: function
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = vars["rho_vac_aether"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_computeLambda_UI(p: dict) -> float:
    """Function: computeLambda_UI"""
    # Type: function
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = vars["lambda_UI"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_computeRho_vac_UI(p: dict) -> float:
    """Function: computeRho_vac_UI"""
    # Type: function
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = vars["rho_vac_UI"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_computeRho_vac_SCm(p: dict) -> float:
    """Function: computeRho_vac_SCm"""
    # Type: function
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = vars["rho_vac_SCm"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_computeRho_vac_UA(p: dict) -> float:
    """Function: computeRho_vac_UA"""
    # Type: function
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = vars["rho_vac_UA"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_computeV_SCm(p: dict) -> float:
    """Function: computeV_SCm"""
    # Type: function
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = vars["v_SCm"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_computeB_total(p: dict) -> float:
    """Function: computeB_total"""
    # Type: function
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = vars["B_total"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_computeFlux(p: dict) -> float:
    """Function: computeFlux"""
    # Type: function
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = vars["phi_m"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_computeF_U(p: dict) -> float:
    """Function: computeF_U"""
    # Type: function
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = v["M"] / (r * r) * math.math.exp(-t / 1e10)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_computeJetPower(p: dict) -> float:
    """Function: computeJetPower"""
    # Type: function
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = v["M_bh"] * 1e-3
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_computeCollisionEnergy(p: dict) -> float:
    """Function: computeCollisionEnergy"""
    # Type: function
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.5 * v["M_cluster"] * v["v_collision"] * v["v_collision"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_computeTidalRadius(p: dict) -> float:
    """Function: computeTidalRadius"""
    # Type: function
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = pow(v["M_bh"] / v["M_star"], 1.0 / 3.0) * 7e8
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_computeAccretionLuminosity(p: dict) -> float:
    """Function: computeAccretionLuminosity"""
    # Type: function
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = v["accretion_rate"] * v["M_bh"] * 9e16 * 0.1
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_computePulsarSpindown(p: dict) -> float:
    """Function: computePulsarSpindown"""
    # Type: function
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = v["omega_pulsar"] * v["omega_pulsar"] * v["M"] * 1e-8
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_computeClusterMass(p: dict) -> float:
    """Function: computeClusterMass"""
    # Type: function
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = v["M_total"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_computeRamPressure(p: dict) -> float:
    """Function: computeRamPressure"""
    # Type: function
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1e-27 * v["v_rel"] * v["v_rel"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_step_function_SOURCE4(p: dict) -> float:
    """Function: step_function_SOURCE4"""
    # Type: function
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_compute_Ereact_SOURCE4(p: dict) -> float:
    """Function: compute_Ereact_SOURCE4"""
    # Type: function
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (rho_SCm * v_SCm * v_SCm / rho_A) * math.math.exp(-kappa * t)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_compute_mu_s_SOURCE4(p: dict) -> float:
    """Function: compute_mu_s_SOURCE4"""
    # Type: function
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = Bs_t * pow(Rs, 3)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_compute_grad_Ms_r_SOURCE4(p: dict) -> float:
    """Function: compute_grad_Ms_r_SOURCE4"""
    # Type: function
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = G_SOURCE4 * Ms / (Rs * Rs)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_compute_Bj_SOURCE4(p: dict) -> float:
    """Function: compute_Bj_SOURCE4"""
    # Type: function
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1e-3 + 0.4 * math.math.sin(omega_c * t) + SCm_contrib
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_compute_omega_s_t_SOURCE4(p: dict) -> float:
    """Function: compute_omega_s_t_SOURCE4"""
    # Type: function
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = omega_s - 0.4e-6 * math.math.sin(omega_c * t)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_compute_mu_j_SOURCE4(p: dict) -> float:
    """Function: compute_mu_j_SOURCE4"""
    # Type: function
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = Bj * pow(Rs, 3)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_compute_Ug1_SOURCE4(p: dict) -> float:
    """Function: compute_Ug1_SOURCE4"""
    # Type: function
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = k1 * mu_s * grad_Ms_r * math.math.exp(-alpha * t) * math.math.cos(PI_SOURCE4 * tn) * defect
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_compute_Ug2_SOURCE4(p: dict) -> float:
    """Function: compute_Ug2_SOURCE4"""
    # Type: function
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = k2 * (QA + body.QUA) * body.Ms / (r * r) * S * wind_mod * HSCm * Ereact
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_compute_Ug3_SOURCE4(p: dict) -> float:
    """Function: compute_Ug3_SOURCE4"""
    # Type: function
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = k3 * Bj * math.math.cos(omega_s_t * t * PI_SOURCE4) * body.Pcore * Ereact
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_compute_Ug4_SOURCE4(p: dict) -> float:
    """Function: compute_Ug4_SOURCE4"""
    # Type: function
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = k4 * rho_v * C_concentration * Mbh / dg * decay * cycle * (1 + f_feedback)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_compute_Ubi_SOURCE4(p: dict) -> float:
    """Function: compute_Ubi_SOURCE4"""
    # Type: function
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = -beta_i * Ugi * Omega_g * Mbh / dg * wind_mod * UUA * math.math.cos(PI_SOURCE4 * tn)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_compute_Um_SOURCE4(p: dict) -> float:
    """Function: compute_Um_SOURCE4"""
    # Type: function
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = single * num_strings * body.PSCm * Ereact
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_compute_FU_SOURCE4(p: dict) -> float:
    """Function: compute_FU_SOURCE4"""
    # Type: function
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = sum_Ug + Ubi + Um
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_compute_compressed_base_SOURCE4(p: dict) -> float:
    """Function: compute_compressed_base_SOURCE4"""
    # Type: function
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = G_SOURCE4 * sys.M / (sys.r * sys.r)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_compute_compressed_expansion_SOURCE4(p: dict) -> float:
    """Function: compute_compressed_expansion_SOURCE4"""
    # Type: function
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1.0 + H0 * sys.t
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_compute_compressed_super_adj_SOURCE4(p: dict) -> float:
    """Function: compute_compressed_super_adj_SOURCE4"""
    # Type: function
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1.0 - sys.B / sys.Bcrit
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_compute_compressed_env_SOURCE4(p: dict) -> float:
    """Function: compute_compressed_env_SOURCE4"""
    # Type: function
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_compute_compressed_Ug_sum_SOURCE4(p: dict) -> float:
    """Function: compute_compressed_Ug_sum_SOURCE4"""
    # Type: function
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_compute_compressed_cosm_SOURCE4(p: dict) -> float:
    """Function: compute_compressed_cosm_SOURCE4"""
    # Type: function
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = Lambda * c_SOURCE4 * c_SOURCE4 / 3.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_compute_compressed_quantum_SOURCE4(p: dict) -> float:
    """Function: compute_compressed_quantum_SOURCE4"""
    # Type: function
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (hbar / Delta_x_p) * integral_psi * (2 * PI_SOURCE4 / tHubble)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_compute_compressed_fluid_SOURCE4(p: dict) -> float:
    """Function: compute_compressed_fluid_SOURCE4"""
    # Type: function
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = sys.rho_fluid * sys.Vsys * sys.g_local
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_compute_compressed_perturbation_SOURCE4(p: dict) -> float:
    """Function: compute_compressed_perturbation_SOURCE4"""
    # Type: function
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (sys.M + sys.M_DM) * (sys.delta_rho_rho + 3 * G_SOURCE4 * sys.M / (sys.r * sys.r * sys.r))
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_compute_compressed_MUGE_SOURCE4(p: dict) -> float:
    """Function: compute_compressed_MUGE_SOURCE4"""
    # Type: function
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = adjusted_base + Ug_sum + cosm + quantum + fluid + perturbation
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_compute_aDPM_SOURCE4(p: dict) -> float:
    """Function: compute_aDPM_SOURCE4"""
    # Type: function
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = FDPM * res.fDPM * res.Evac_neb * res.c_res * sys.Vsys
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_compute_aTHz_SOURCE4(p: dict) -> float:
    """Function: compute_aTHz_SOURCE4"""
    # Type: function
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_compute_avac_diff_SOURCE4(p: dict) -> float:
    """Function: compute_avac_diff_SOURCE4"""
    # Type: function
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_compute_asuper_freq_SOURCE4(p: dict) -> float:
    """Function: compute_asuper_freq_SOURCE4"""
    # Type: function
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_compute_aaether_res_SOURCE4(p: dict) -> float:
    """Function: compute_aaether_res_SOURCE4"""
    # Type: function
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = res.UA_SCM * res.omega_i * res.fTHz * aDPM * (1 + res.fTRZ)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_compute_Ug4i_SOURCE4(p: dict) -> float:
    """Function: compute_Ug4i_SOURCE4"""
    # Type: function
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_compute_aquantum_freq_SOURCE4(p: dict) -> float:
    """Function: compute_aquantum_freq_SOURCE4"""
    # Type: function
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_compute_aAether_freq_SOURCE4(p: dict) -> float:
    """Function: compute_aAether_freq_SOURCE4"""
    # Type: function
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_compute_afluid_freq_SOURCE4(p: dict) -> float:
    """Function: compute_afluid_freq_SOURCE4"""
    # Type: function
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_compute_Osc_term_SOURCE4(p: dict) -> float:
    """Function: compute_Osc_term_SOURCE4"""
    # Type: function
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_compute_aexp_freq_SOURCE4(p: dict) -> float:
    """Function: compute_aexp_freq_SOURCE4"""
    # Type: function
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_compute_a_wormhole_SOURCE4(p: dict) -> float:
    """Function: compute_a_wormhole_SOURCE4"""
    # Type: function
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = f_worm * Evac_neb * (1.0 / (b * b + r * r))
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_compute_resonance_MUGE_SOURCE4(p: dict) -> float:
    """Function: compute_resonance_MUGE_SOURCE4"""
    # Type: function
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_computeTotalMass(p: dict) -> float:
    """Function: computeTotalMass"""
    # Type: function
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = v["M_IC2163"] + v["M_NGC2207"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_computeLuminosity_1(p: dict) -> float:
    """Function: computeLuminosity"""
    # Type: function
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1e47
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_computeMagneticMoment(p: dict) -> float:
    """Function: computeMagneticMoment"""
    # Type: function
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = v["B_pole"] * pow(7.14e7, 3)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_computeDensity(p: dict) -> float:
    """Function: computeDensity"""
    # Type: function
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = v["M"] / (4.0 / 3.0 * 3.14159 * pow(v["R"], 3))
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_computeStarFormationRate(p: dict) -> float:
    """Function: computeStarFormationRate"""
    # Type: function
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = v["SFR"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_computeJetPower_1(p: dict) -> float:
    """Function: computeJetPower"""
    # Type: function
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = v["L_jet"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_computeBarStrength(p: dict) -> float:
    """Function: computeBarStrength"""
    # Type: function
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = v["M_bh"] / v["M_galaxy"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_computeKineticEnergy(p: dict) -> float:
    """Function: computeKineticEnergy"""
    # Type: function
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.5 * v["M"] * v["v_collision"] * v["v_collision"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_computeTotalMass_1(p: dict) -> float:
    """Function: computeTotalMass"""
    # Type: function
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = v["M_giant"] + v["M_wd"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_computeSchwarzschildRadius(p: dict) -> float:
    """Function: computeSchwarzschildRadius"""
    # Type: function
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = v["R_s"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_computeClusterMass_1(p: dict) -> float:
    """Function: computeClusterMass"""
    # Type: function
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = v["M_cluster"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_computeAverageMass(p: dict) -> float:
    """Function: computeAverageMass"""
    # Type: function
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = v["M_total"] / v["N_galaxies"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_computeOmega(p: dict) -> float:
    """Function: computeOmega"""
    # Type: function
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 2.0 * 3.14159265359 / v["P_spin"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_computeGasTemperature(p: dict) -> float:
    """Function: computeGasTemperature"""
    # Type: function
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = v["T_gas"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_computeDipoleVortexSpecies(p: dict) -> float:
    """Function: computeDipoleVortexSpecies"""
    # Type: function
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1.618033988749895 * sysParams[sys + "_M"] / sysParams[sys + "_r"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_getFrameworkVersion(p: dict) -> float:
    """Function: getFrameworkVersion"""
    # Type: function
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = std.real(vars["version"])
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_calculate_F_LENR(p: dict) -> float:
    """Function: calculate_F_LENR"""
    # Type: function
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = constants["K_LENR"] * pow(ratio, 2)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_calculate_F_act(p: dict) -> float:
    """Function: calculate_F_act"""
    # Type: function
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = constants["K_ACT"] * math.math.cos(constants["OMEGA_ACT"] * t)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_calculate_F_DE(p: dict) -> float:
    """Function: calculate_F_DE"""
    # Type: function
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = constants["K_DE"] * L_X
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_DPM_resonance(p: dict) -> float:
    """Function: DPM_resonance"""
    # Type: function
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (constants["G_FACTOR"] * constants["MU_B"] * B0) / (constants["H"] * omega0)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_calculate_F_res(p: dict) -> float:
    """Function: calculate_F_res"""
    # Type: function
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 2 * constants["Q"] * B0 * V_val * cos_theta * dpm_res
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_calculate_F_neutron(p: dict) -> float:
    """Function: calculate_F_neutron"""
    # Type: function
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = constants["K_NEUTRON"] * constants["SIGMA_N"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_calculate_F_rel(p: dict) -> float:
    """Function: calculate_F_rel"""
    # Type: function
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = constants["K_REL"] * pow(ratio, 2) * constants["F_REL_BASE"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_calculate_integrand(p: dict) -> float:
    """Function: calculate_integrand"""
    # Type: function
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = -constants["F0"] + momentum_term + gravity_term + vac_term + f_lenr + f_act + f_de + f_res + f_neutron + f_rel
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_solve_x2(p: dict) -> float:
    """Function: solve_x2"""
    # Type: function
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (-b - math.math.sqrt(discriminant)) / (2 * a)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_calculate_F_U_Bi_i(p: dict) -> float:
    """Function: calculate_F_U_Bi_i"""
    # Type: function
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = integrand * x2
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_calculate_master_buoyancy(p: dict) -> float:
    """Function: calculate_master_buoyancy"""
    # Type: function
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_getDynamicParameter_1(p: dict) -> float:
    """Function: getDynamicParameter"""
    # Type: function
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_computeDynamicContribution(p: dict) -> float:
    """Function: computeDynamicContribution"""
    # Type: function
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_getDynamicParameter_2(p: dict) -> float:
    """Function: getDynamicParameter"""
    # Type: function
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_getDynamicParameter_3(p: dict) -> float:
    """Function: getDynamicParameter"""
    # Type: function
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_Hubble_correction(p: dict) -> float:
    """Function: Hubble_correction"""
    # Type: function
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1.0 + z
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_getDynamicParameter_4(p: dict) -> float:
    """Function: getDynamicParameter"""
    # Type: function
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_calculate_gravity_compressed(p: dict) -> float:
    """Function: calculate_gravity_compressed"""
    # Type: function
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = sum / static_cast(NUM_STATES_S115)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_calculate_resonance(p: dict) -> float:
    """Function: calculate_resonance"""
    # Type: function
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (sum_r / static_cast(NUM_STATES_S115)) * (1.0 - 0.05 * params.z)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_simulate_DPM_creation(p: dict) -> float:
    """Function: simulate_DPM_creation"""
    # Type: function
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = vacuum_density * avg_ua_sc * static_cast(NUM_STATES_S115)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_evaluate_26D_polynomial(p: dict) -> float:
    """Function: evaluate_26D_polynomial"""
    # Type: function
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = result
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_getDynamicParameter_5(p: dict) -> float:
    """Function: getDynamicParameter"""
    # Type: function
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_simulateDPMCreation(p: dict) -> float:
    """Function: simulateDPMCreation"""
    # Type: function
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = core_.simulate_DPM_creation(vacuum_density)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_evaluate26DPolynomial(p: dict) -> float:
    """Function: evaluate26DPolynomial"""
    # Type: function
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = core_.evaluate_26D_polynomial(coeffs, x)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_getDynamicParameter_6(p: dict) -> float:
    """Function: getDynamicParameter"""
    # Type: function
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = core_.getDynamicParameter(name, defaultValue)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_evaluate26DPoly_S116(p: dict) -> float:
    """Function: evaluate26DPoly_S116"""
    # Type: function
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = result
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_uqffBuoyantGravity_S116(p: dict) -> float:
    """Function: uqffBuoyantGravity_S116"""
    # Type: function
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = poly * (1.0 + sfr) * math.math.sin(PI_MATH_S116 / 26.0)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_measureDimension(p: dict) -> float:
    """Function: measureDimension"""
    # Type: function
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = engine.measureDimension(center, radius)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_measureBuoyantGravity(p: dict) -> float:
    """Function: measureBuoyantGravity"""
    # Type: function
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = engine.measureBuoyantGravity(center)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_measureConsciousnessField(p: dict) -> float:
    """Function: measureConsciousnessField"""
    # Type: function
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = engine.measureConsciousnessField()
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_evaluateUnityPolynomial(p: dict) -> float:
    """Function: evaluateUnityPolynomial"""
    # Type: function
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = engine.evaluateUnityPolynomial(coeffs, x)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_getDynamicParameter_7(p: dict) -> float:
    """Function: getDynamicParameter"""
    # Type: function
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = engine.getDynamicParameter(name, defaultValue)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_computeDynamicContribution_1(p: dict) -> float:
    """Function: computeDynamicContribution"""
    # Type: function
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = engine.computeDynamicContribution(t)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_compute_F_U_Bi_i(p: dict) -> float:
    """Function: compute_F_U_Bi_i"""
    # Type: function
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = result
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_compute_g_UQFF(p: dict) -> float:
    """Function: compute_g_UQFF"""
    # Type: function
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = result
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_compute_DPM_resonance(p: dict) -> float:
    """Function: compute_DPM_resonance"""
    # Type: function
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = adjusted
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_noise(p: dict) -> float:
    """Function: noise"""
    # Type: function
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_fade(p: dict) -> float:
    """Function: fade"""
    # Type: function
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = t * t * t * (t * (t * 6 - 15) + 10)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_lerp(p: dict) -> float:
    """Function: lerp"""
    # Type: function
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = a + t * (b - a)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_grad(p: dict) -> float:
    """Function: grad"""
    # Type: function
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_get_k1(p: dict) -> float:
    """Function: get_k1"""
    # Type: function
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = k1_
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_get_ki(p: dict) -> float:
    """Function: get_ki"""
    # Type: function
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = ki_
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_get_km(p: dict) -> float:
    """Function: get_km"""
    # Type: function
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = km_
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_get_ke(p: dict) -> float:
    """Function: get_ke"""
    # Type: function
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = ke_
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_get_k_eta(p: dict) -> float:
    """Function: get_k_eta"""
    # Type: function
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = k_eta_
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_get_sfr(p: dict) -> float:
    """Function: get_sfr"""
    # Type: function
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = sfr_
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_get_wind_vel(p: dict) -> float:
    """Function: get_wind_vel"""
    # Type: function
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = wind_vel_
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_get_mag_field(p: dict) -> float:
    """Function: get_mag_field"""
    # Type: function
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = mag_field_
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_get_f_Ub_scale(p: dict) -> float:
    """Function: get_f_Ub_scale"""
    # Type: function
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = f_Ub_scale_
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_get_M(p: dict) -> float:
    """Function: get_M"""
    # Type: function
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = M_
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_get_r(p: dict) -> float:
    """Function: get_r"""
    # Type: function
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = r_
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_cos_theta(p: dict) -> float:
    """Function: cos_theta"""
    # Type: function
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = math.math.cos(math.pi / 4.0)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_Hubble_correction_1(p: dict) -> float:
    """Function: Hubble_correction"""
    # Type: function
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1.0 + z
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_Hubble_correction_2(p: dict) -> float:
    """Function: Hubble_correction"""
    # Type: function
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1.0 + z
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_noise_1(p: dict) -> float:
    """Function: noise"""
    # Type: function
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_fade_1(p: dict) -> float:
    """Function: fade"""
    # Type: function
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = t * t * t * (t * (t * 6 - 15) + 10)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_lerp_1(p: dict) -> float:
    """Function: lerp"""
    # Type: function
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = a + t * (b - a)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_grad_1(p: dict) -> float:
    """Function: grad"""
    # Type: function
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_newtonMethod(p: dict) -> float:
    """Function: newtonMethod"""
    # Type: function
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_defaultValue(p: dict) -> float:
    """Inline equation: defaultValue"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_defaultValue_1(p: dict) -> float:
    """Inline equation: defaultValue"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_value(p: dict) -> float:
    """Inline equation: value"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_minVal(p: dict) -> float:
    """Inline equation: minVal"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_sum(p: dict) -> float:
    """Inline equation: sum"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = allValues[i].second + allValues[j].second
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_maxAbs(p: dict) -> float:
    """Inline equation: maxAbs"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = max(abs(allValues[i].second), abs(allValues[j].second))
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_val(p: dict) -> float:
    """Inline equation: val"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_amp(p: dict) -> float:
    """Inline equation: amp"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_rho_vac(p: dict) -> float:
    """Inline equation: rho_vac"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_coupling(p: dict) -> float:
    """Inline equation: coupling"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("coupling", 1.0)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_strength(p: dict) -> float:
    """Inline equation: strength"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_M(p: dict) -> float:
    """Inline equation: M"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_r(p: dict) -> float:
    """Inline equation: r"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_alpha(p: dict) -> float:
    """Inline equation: alpha"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("alpha", 1.0)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_mass(p: dict) -> float:
    """Inline equation: mass"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_rho_0(p: dict) -> float:
    """Inline equation: rho_0"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = M_halo / (4.0 * math.pi * r_scale * r_scale * r_scale * (math.log(2.0) - 0.5))
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_e_scale(p: dict) -> float:
    """Inline equation: e_scale"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_strength_1(p: dict) -> float:
    """Inline equation: strength"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_M_1(p: dict) -> float:
    """Inline equation: M"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_r_1(p: dict) -> float:
    """Inline equation: r"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_temp(p: dict) -> float:
    """Inline equation: temp"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_DPM_momentum(p: dict) -> float:
    """Inline equation: DPM_momentum"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("DPM_momentum", 0.93)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_DPM_gravity(p: dict) -> float:
    """Inline equation: DPM_gravity"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("DPM_gravity", 1.0)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_DPM_stability(p: dict) -> float:
    """Inline equation: DPM_stability"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("DPM_stability", 0.01)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_LENR_term(p: dict) -> float:
    """Inline equation: LENR_term"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = k_LENR * pow(omega_LENR / system_omega0, 2.0)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_term_mom(p: dict) -> float:
    """Inline equation: term_mom"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (m_e * c * c / (system_r * system_r)) * DPM_momentum * math.cos(theta)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_term_grav(p: dict) -> float:
    """Inline equation: term_grav"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (dpm_ug1_seed(system_M, system_r)) * DPM_gravity
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_term_vac(p: dict) -> float:
    """Inline equation: term_vac"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 7.09e-36 * DPM_stability
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_DPM_resonance(p: dict) -> float:
    """Inline equation: DPM_resonance"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (g_Lande * mu_B * system_B0) / (hbar * system_omega0)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_term_res(p: dict) -> float:
    """Inline equation: term_res"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 2.0 * q * system_B0 * V * math.sin(theta) * DPM_resonance
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_gravity_compressed(p: dict) -> float:
    """Inline equation: gravity_compressed"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = dpm_ug1_seed(system_M, system_r)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_beta_i(p: dict) -> float:
    """Inline equation: beta_i"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("beta_i", 1.0)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_V_infl(p: dict) -> float:
    """Inline equation: V_infl"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("V_infl", 1e-6)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_rho_vac_A(p: dict) -> float:
    """Inline equation: rho_vac_A"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("rho_vac_A", 1e-30)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_a_universal(p: dict) -> float:
    """Inline equation: a_universal"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("a_universal", 1e12)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_buoyancy(p: dict) -> float:
    """Inline equation: buoyancy"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = beta_i * V_infl * rho_vac_A * a_universal
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_F_rel(p: dict) -> float:
    """Inline equation: F_rel"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("F_rel", 4.30e33)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_x2(p: dict) -> float:
    """Inline equation: x2"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("x2", -1.35e172)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_B(p: dict) -> float:
    """Inline equation: B"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_B_1(p: dict) -> float:
    """Inline equation: B"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("B0", B0)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_omega(p: dict) -> float:
    """Inline equation: omega"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("omega0", omega0)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_k(p: dict) -> float:
    """Inline equation: k"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_k_1(p: dict) -> float:
    """Inline equation: k"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("k_LENR", k_LENR)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_omega_L(p: dict) -> float:
    """Inline equation: omega_L"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("omega_LENR", omega_LENR)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_omega_0(p: dict) -> float:
    """Inline equation: omega_0"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("omega0", omega0)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_M_2(p: dict) -> float:
    """Inline equation: M"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_M_3(p: dict) -> float:
    """Inline equation: M"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("M", M_bh)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_t_scale(p: dict) -> float:
    """Inline equation: t_scale"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("t", 1e16)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_L(p: dict) -> float:
    """Inline equation: L"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_L_1(p: dict) -> float:
    """Inline equation: L"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("L_peak", L_peak)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_t_p(p: dict) -> float:
    """Inline equation: t_p"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("t_peak", t_peak)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_gravity(p: dict) -> float:
    """Inline equation: gravity"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = dpm_ug1_seed(system_M, system_r)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_ionization(p: dict) -> float:
    """Inline equation: ionization"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("ionization_fraction", 0.5)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_v_exp(p: dict) -> float:
    """Inline equation: v_exp"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("expansion_velocity", 20000.0)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_P_thermal(p: dict) -> float:
    """Inline equation: P_thermal"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = rho_gas * k_B * system_T / m_p
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_P_rad(p: dict) -> float:
    """Inline equation: P_rad"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = system_L_X / (4.0 * math.pi * system_r * system_r * c)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_F_expansion(p: dict) -> float:
    """Inline equation: F_expansion"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 4.0 * math.pi * system_r * system_r * rho_gas * v_exp * v_exp
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_F_ionization(p: dict) -> float:
    """Inline equation: F_ionization"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = ionization * P_thermal * 4.0 * math.pi * system_r * system_r
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_gas_nebula(p: dict) -> float:
    """Inline equation: gas_nebula"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = F_expansion + F_ionization + P_rad * system_r * system_r
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_L_2(p: dict) -> float:
    """Inline equation: L"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_ionization_rate(p: dict) -> float:
    """Inline equation: ionization_rate"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("L_X", L_X) / photon_energy
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_n_gas(p: dict) -> float:
    """Inline equation: n_gas"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("rho_gas", rho_gas) / m_p
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_V(p: dict) -> float:
    """Inline equation: V"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (4.0 / 3.0) * math.pi * r_nebula * r_nebula * r_nebula
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_ion_frac(p: dict) -> float:
    """Inline equation: ion_frac"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = min(1.0, ionization_rate / (n_gas * V))
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_r_init(p: dict) -> float:
    """Inline equation: r_init"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_r_initial(p: dict) -> float:
    """Inline equation: r_initial"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("r0", r0)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_v_exp_1(p: dict) -> float:
    """Inline equation: v_exp"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("v_expansion", v_expansion)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_t0(p: dict) -> float:
    """Inline equation: t0"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("t0", 1e16)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_r_t(p: dict) -> float:
    """Inline equation: r_t"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = r_initial * (1.0 + v_exp * (t - t0) / r_initial)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_M_4(p: dict) -> float:
    """Inline equation: M"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("M", 1e41)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_E_expansion(p: dict) -> float:
    """Inline equation: E_expansion"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.5 * M * v_exp * v_exp
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_DPM_res(p: dict) -> float:
    """Inline equation: DPM_res"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (g_Lande * mu_B * system_B0) / (hbar * system_omega0)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_LENR(p: dict) -> float:
    """Inline equation: LENR"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = k_LENR * pow(omega_LENR / system_omega0, 2.0)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_term_mom_1(p: dict) -> float:
    """Inline equation: term_mom"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (m_e * c * c / (system_r * system_r)) * 0.93 * 0.707
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_term_grav_1(p: dict) -> float:
    """Inline equation: term_grav"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (dpm_ug1_seed(system_M, system_r)) * 1.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_term_res_1(p: dict) -> float:
    """Inline equation: term_res"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 2.0 * q * system_B0 * 1e-3 * 0.707 * DPM_res
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_term_neut(p: dict) -> float:
    """Inline equation: term_neut"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = k_neutron * sigma_n
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_term_rel(p: dict) -> float:
    """Inline equation: term_rel"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = k_rel * pow(E_cm_astro / E_cm, 2.0)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_integrand(p: dict) -> float:
    """Inline equation: integrand"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_beta_i_1(p: dict) -> float:
    """Inline equation: beta_i"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("beta_i", 0.6)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_V_infl_1(p: dict) -> float:
    """Inline equation: V_infl"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("V_infl_UA", 1e-6)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_rho_vac_1(p: dict) -> float:
    """Inline equation: rho_vac"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("rho_vac_A", 1e-30)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_a_univ(p: dict) -> float:
    """Inline equation: a_univ"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("a_universal", 1e12)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_lambda_i(p: dict) -> float:
    """Inline equation: lambda_i"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("lambda_i", 1.0)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_rho_sc(p: dict) -> float:
    """Inline equation: rho_sc"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("rho_vac_SCm", 7.09e-37)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_rho_ua(p: dict) -> float:
    """Inline equation: rho_ua"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("rho_vac_UA", 7.09e-36)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_omega_s(p: dict) -> float:
    """Inline equation: omega_s"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("omega_s", 2.5e-6)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_t_scale_1(p: dict) -> float:
    """Inline equation: t_scale"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("t_scale", 1e16)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_f_TRZ(p: dict) -> float:
    """Inline equation: f_TRZ"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("f_TRZ", 0.1)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_k_neutron(p: dict) -> float:
    """Inline equation: k_neutron"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("k_neutron", 1e10)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_sigma_n(p: dict) -> float:
    """Inline equation: sigma_n"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("sigma_n", 1e-4)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_gas_nebula_1(p: dict) -> float:
    """Inline equation: gas_nebula"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1e-23 * system_r * system_r * 1e-10
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_term_rel_1(p: dict) -> float:
    """Inline equation: term_rel"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1e-10 * pow(1.24e24 / 3.0264e-8, 2.0)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_integrand_1(p: dict) -> float:
    """Inline equation: integrand"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_buoyancy_1(p: dict) -> float:
    """Inline equation: buoyancy"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1.0 * 1e-6 * 1e-30 * 1e12
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_dipole_base(p: dict) -> float:
    """Inline equation: dipole_base"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("dipole_base", 1.0)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_phase(p: dict) -> float:
    """Inline equation: phase"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 2.0 * math.pi * golden_ratio * 1.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_beta_i_2(p: dict) -> float:
    """Inline equation: beta_i"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("beta_i_triadic", 1.0)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_k1(p: dict) -> float:
    """Inline equation: k1"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("k1", 1.0)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_ki(p: dict) -> float:
    """Inline equation: ki"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("ki", 1.0)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_f_nu(p: dict) -> float:
    """Inline equation: f_nu"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1.0 + math.math.sin(math.pi * nu_THz / 1e12)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_geom_factor(p: dict) -> float:
    """Inline equation: geom_factor"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = math.math.sin(theta) * math.math.cos(phi)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_exp_barrier(p: dict) -> float:
    """Inline equation: exp_barrier"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = math.math.exp(-R_EB / r)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_U_g1(p: dict) -> float:
    """Inline equation: U_g1"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = k1 * f_UA_prime * f_SCm * R_EB * f_nu * geom_factor * exp_barrier / (r * r)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_U_g3(p: dict) -> float:
    """Inline equation: U_g3"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = ki * f_UA_prime * nu_THz * R_EB * geom_factor / (r * r)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_R_EB(p: dict) -> float:
    """Inline equation: R_EB"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("R_EB", 1.0)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_r_2(p: dict) -> float:
    """Inline equation: r"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("r_distance", 1e20)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_U_m(p: dict) -> float:
    """Inline equation: U_m"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("U_m", 7.97717e-22)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_rho_vac_UA(p: dict) -> float:
    """Inline equation: rho_vac_UA"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("rho_vac_UA", 1e-27)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_n(p: dict) -> float:
    """Inline equation: n"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("quantum_state_n", 26.0)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_exp_ssq(p: dict) -> float:
    """Inline equation: exp_ssq"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = math.math.exp(-SSQ * n / N_QUANTUM)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_exp_pi_t(p: dict) -> float:
    """Inline equation: exp_pi_t"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = math.math.exp(-(math.pi - t))
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_mass_1(p: dict) -> float:
    """Inline equation: mass"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_r_3(p: dict) -> float:
    """Inline equation: r"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_gravitational(p: dict) -> float:
    """Inline equation: gravitational"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = G_const * body_mass / (r * r)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_oscillation(p: dict) -> float:
    """Inline equation: oscillation"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = math.cos(math.pi * t / 1e6)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_mass_2(p: dict) -> float:
    """Inline equation: mass"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_r_4(p: dict) -> float:
    """Inline equation: r"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_base_gravity(p: dict) -> float:
    """Inline equation: base_gravity"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = G_const * system_mass / (r * r)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_compressed(p: dict) -> float:
    """Inline equation: compressed"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = base_gravity * compression_factor
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_time_mod(p: dict) -> float:
    """Inline equation: time_mod"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1.0 + 0.1 * math.sin(t / 1e7)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_velocity(p: dict) -> float:
    """Inline equation: velocity"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_gamma(p: dict) -> float:
    """Inline equation: gamma"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1.0 / math.sqrt(1.0 - (jet_velocity * jet_velocity) / (c * c))
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_scm_density(p: dict) -> float:
    """Inline equation: scm_density"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_scm_d(p: dict) -> float:
    """Inline equation: scm_d"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("rho_SCm", rho_SCm)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_v(p: dict) -> float:
    """Inline equation: v"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("v_SCm", v_SCm)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_rho(p: dict) -> float:
    """Inline equation: rho"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("rho_A", rho_A)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_k_2(p: dict) -> float:
    """Inline equation: k"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("kappa", kappa)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_B_field(p: dict) -> float:
    """Inline equation: B_field"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_B_2(p: dict) -> float:
    """Inline equation: B"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("Bs_avg", Bs_avg)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_omega_1(p: dict) -> float:
    """Inline equation: omega"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("omega_c", omega_c)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_R(p: dict) -> float:
    """Inline equation: R"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("Rs", Rs)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_scm(p: dict) -> float:
    """Inline equation: scm"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("SCm_contrib", SCm_contrib)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_Bs_t(p: dict) -> float:
    """Inline equation: Bs_t"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = B + 0.4 * math.sin(omega * t) + scm
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_B0(p: dict) -> float:
    """Inline equation: B0"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_B0_1(p: dict) -> float:
    """Inline equation: B0"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("B_base", B_base)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_beta(p: dict) -> float:
    """Inline equation: beta"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_Ugi(p: dict) -> float:
    """Inline equation: Ugi"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_tn(p: dict) -> float:
    """Inline equation: tn"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_beta_1(p: dict) -> float:
    """Inline equation: beta"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("beta_i", beta_i)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_omega_2(p: dict) -> float:
    """Inline equation: omega"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("Omega_g", Omega_g)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_M_5(p: dict) -> float:
    """Inline equation: M"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("Mbh", Mbh)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_d(p: dict) -> float:
    """Inline equation: d"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("dg", dg)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_eps(p: dict) -> float:
    """Inline equation: eps"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("epsilon_sw", epsilon_sw)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_rho_1(p: dict) -> float:
    """Inline equation: rho"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("rho_sw", rho_sw)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_ua(p: dict) -> float:
    """Inline equation: ua"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("UUA", UUA)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_M_6(p: dict) -> float:
    """Inline equation: M"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_r_5(p: dict) -> float:
    """Inline equation: r"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_hubble(p: dict) -> float:
    """Inline equation: hubble"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_H(p: dict) -> float:
    """Inline equation: H"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("H0", H0)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_B_3(p: dict) -> float:
    """Inline equation: B"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_Bcrit(p: dict) -> float:
    """Inline equation: Bcrit"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_lambda(p: dict) -> float:
    """Inline equation: lambda"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_L_3(p: dict) -> float:
    """Inline equation: L"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("Lambda", Lambda)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_uncertainty(p: dict) -> float:
    """Inline equation: uncertainty"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_dx_dp(p: dict) -> float:
    """Inline equation: dx_dp"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("delta_x_p", delta_x_p)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_psi(p: dict) -> float:
    """Inline equation: psi"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("integral_psi", integral_psi)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_t_hub(p: dict) -> float:
    """Inline equation: t_hub"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("t_Hubble", t_Hubble)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_rho_2(p: dict) -> float:
    """Inline equation: rho"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_V_1(p: dict) -> float:
    """Inline equation: V"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_g(p: dict) -> float:
    """Inline equation: g"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_M_7(p: dict) -> float:
    """Inline equation: M"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_M_DM(p: dict) -> float:
    """Inline equation: M_DM"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_delta_rho_rho(p: dict) -> float:
    """Inline equation: delta_rho_rho"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_pert2(p: dict) -> float:
    """Inline equation: pert2"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 3 * G * M / (r * r * r)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_halo_mass(p: dict) -> float:
    """Inline equation: halo_mass"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_x(p: dict) -> float:
    """Inline equation: x"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = r / dm_scale_radius
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_rho_0_1(p: dict) -> float:
    """Inline equation: rho_0"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = dm_halo_mass / (4.0 * math.pi * dm_scale_radius * dm_scale_radius * dm_scale_radius * (math.log(2.0) - 0.5))
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_dm_contrib(p: dict) -> float:
    """Inline equation: dm_contrib"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = G * dm_halo_mass * math.log(1 + x) / (r * x)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_vac_contrib(p: dict) -> float:
    """Inline equation: vac_contrib"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = vacuum_energy_scale * (1.0 + 0.1 * math.sin(1e-10 * t))
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_freq(p: dict) -> float:
    """Inline equation: freq"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_base_gravity_1(p: dict) -> float:
    """Inline equation: base_gravity"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = dpm_ug1_seed(system_intensity, r)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_resonance_mod(p: dict) -> float:
    """Inline equation: resonance_mod"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1.0 + 0.2 * math.cos(2 * math.pi * resonance_freq * t)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_state_evolution(p: dict) -> float:
    """Inline equation: state_evolution"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = math.exp(-t / 1e8) * math.sin(t / 1e6)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_base_rot(p: dict) -> float:
    """Inline equation: base_rot"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_visc(p: dict) -> float:
    """Inline equation: visc"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_uqff_g(p: dict) -> float:
    """Inline equation: uqff_g"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_force_jet(p: dict) -> float:
    """Inline equation: force_jet"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_diffusion_term(p: dict) -> float:
    """Inline equation: diffusion_term"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = viscosity * dt * grid_size * grid_size
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_pressure_projection(p: dict) -> float:
    """Inline equation: pressure_projection"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1.0 / (4.0 * diffusion_term + 1.0)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_velocity_1(p: dict) -> float:
    """Inline equation: velocity"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = force_jet * uqff_g * pressure_projection / (1.0 + diffusion_term)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_coupling_1(p: dict) -> float:
    """Inline equation: coupling"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_modulation(p: dict) -> float:
    """Inline equation: modulation"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = eta * Ts00 * math.cos(math.pi * tn)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_k1_val(p: dict) -> float:
    """Inline equation: k1_val"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_Ubi_sum(p: dict) -> float:
    """Inline equation: Ubi_sum"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = -beta_i * Ug_sum * Omega_g * Mbh / dg * UUA * math.cos(math.pi * tn)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_A_scalar(p: dict) -> float:
    """Inline equation: A_scalar"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 4.0 * 1e-22 * (1.27e3 + 1.11e7) * math.cos(math.pi * tn)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_f_dpm(p: dict) -> float:
    """Inline equation: f_dpm"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_I(p: dict) -> float:
    """Inline equation: I"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_A(p: dict) -> float:
    """Inline equation: A"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_omega1(p: dict) -> float:
    """Inline equation: omega1"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_omega2(p: dict) -> float:
    """Inline equation: omega2"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_Vsys(p: dict) -> float:
    """Inline equation: Vsys"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_FDPM(p: dict) -> float:
    """Inline equation: FDPM"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = I * A * (omega1 - omega2)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_f_thz(p: dict) -> float:
    """Inline equation: f_thz"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_vexp(p: dict) -> float:
    """Inline equation: vexp"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_aDPM(p: dict) -> float:
    """Inline equation: aDPM"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_delta_E(p: dict) -> float:
    """Inline equation: delta_E"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_F_sc(p: dict) -> float:
    """Inline equation: F_sc"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_ua_scm(p: dict) -> float:
    """Inline equation: ua_scm"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_f_q(p: dict) -> float:
    """Inline equation: f_q"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_f_a(p: dict) -> float:
    """Inline equation: f_a"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_f_w(p: dict) -> float:
    """Inline equation: f_w"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_r_6(p: dict) -> float:
    """Inline equation: r"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_mass_3(p: dict) -> float:
    """Inline equation: mass"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_Bs_t_1(p: dict) -> float:
    """Inline equation: Bs_t"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = Bs_avg + 0.4 * math.sin(omega_c * t) + SCm_contrib
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_defect(p: dict) -> float:
    """Inline equation: defect"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1.0 + delta_def * math.sin(0.001 * t)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_mass_4(p: dict) -> float:
    """Inline equation: mass"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_Ereact(p: dict) -> float:
    """Inline equation: Ereact"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (SCm_density * v_SCm * v_SCm / rho_A) * math.exp(-kappa * t)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_S(p: dict) -> float:
    """Inline equation: S"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_wind_mod(p: dict) -> float:
    """Inline equation: wind_mod"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1.0 + delta_sw * v_sw
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_radius(p: dict) -> float:
    """Inline equation: radius"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_theta(p: dict) -> float:
    """Inline equation: theta"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_omega_s_t(p: dict) -> float:
    """Inline equation: omega_s_t"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = omega_s - 0.4e-6 * math.sin(omega_c * t)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_Bj(p: dict) -> float:
    """Inline equation: Bj"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1e-3 + 0.4 * math.sin(omega_c * t) + SCm_contrib
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_vacuum_density(p: dict) -> float:
    """Inline equation: vacuum_density"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_radius_1(p: dict) -> float:
    """Inline equation: radius"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_decay(p: dict) -> float:
    """Inline equation: decay"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1.0 - math.exp(-gamma * t * math.cos(math.pi * tn))
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_single(p: dict) -> float:
    """Inline equation: single"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = mu_j / rj * decay * phi_hat
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_coupling_2(p: dict) -> float:
    """Inline equation: coupling"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_mod(p: dict) -> float:
    """Inline equation: mod"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = eta * Ts00 * math.cos(math.pi * tn)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_trace(p: dict) -> float:
    """Inline equation: trace"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (1.0 + mod) + (-1.0 + mod) + (-1.0 + mod) + (-1.0 + mod)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_inertia(p: dict) -> float:
    """Inline equation: inertia"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_compression(p: dict) -> float:
    """Inline equation: compression"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = Vsys / (Vsys + expansion)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_g_compressed(p: dict) -> float:
    """Inline equation: g_compressed"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = dpm_ug1_seed(A, r_sys) * compression * fluid_mod
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_inertia_1(p: dict) -> float:
    """Inline equation: inertia"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_vac_factor(p: dict) -> float:
    """Inline equation: vac_factor"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = Delta_Evac / Evac_neb
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_inertia_2(p: dict) -> float:
    """Inline equation: inertia"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_asuper_freq(p: dict) -> float:
    """Inline equation: asuper_freq"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = aDPM * Fsuper * math.sin(2 * math.pi * fDPM * t)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_inertia_3(p: dict) -> float:
    """Inline equation: inertia"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_aaether_res(p: dict) -> float:
    """Inline equation: aaether_res"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = aDPM * UA_SCM * fAether / fDPM
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_inertia_4(p: dict) -> float:
    """Inline equation: inertia"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_aquantum_freq(p: dict) -> float:
    """Inline equation: aquantum_freq"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = aDPM * fquantum / fDPM * math.cos(2 * math.pi * fquantum * t)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_scale(p: dict) -> float:
    """Inline equation: scale"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_base_value(p: dict) -> float:
    """Inline equation: base_value"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_config_influence(p: dict) -> float:
    """Inline equation: config_influence"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = scaling_factor * base_value * (1.0 + 0.05 * math.sin(t / 1e6))
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_integ(p: dict) -> float:
    """Inline equation: integ"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_neutron_factor(p: dict) -> float:
    """Inline equation: neutron_factor"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_f_TRZ_1(p: dict) -> float:
    """Inline equation: f_TRZ"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_term2(p: dict) -> float:
    """Inline equation: term2"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = k_LENR * activation_energy * math.exp(-t / 1e6)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_term3(p: dict) -> float:
    """Inline equation: term3"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = k_DE + resonance_coupling * neutron_factor
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_term4(p: dict) -> float:
    """Inline equation: term4"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = rel_factor * (1.0 + f_TRZ)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_F_U_Bi_i(p: dict) -> float:
    """Inline equation: F_U_Bi_i"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = term1 + term2 + term3 + term4
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_k_3(p: dict) -> float:
    """Inline equation: k"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_F_vac_rep(p: dict) -> float:
    """Inline equation: F_vac_rep"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = k_vac * delta_rho_vac * M_vac * v_vac
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_k_4(p: dict) -> float:
    """Inline equation: k"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_freq_ratio(p: dict) -> float:
    """Inline equation: freq_ratio"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = omega_thz / omega_0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_F_thz_shock(p: dict) -> float:
    """Inline equation: F_thz_shock"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = k_thz * freq_ratio * freq_ratio * neutron_factor * conduit_scale
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_k_5(p: dict) -> float:
    """Inline equation: k"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_F_conduit(p: dict) -> float:
    """Inline equation: F_conduit"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = k_conduit * (H_abundance * water_state) * neutron_factor
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_k_6(p: dict) -> float:
    """Inline equation: k"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_F_spooky(p: dict) -> float:
    """Inline equation: F_spooky"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = k_spooky * (string_wave / omega_0)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_g_1(p: dict) -> float:
    """Inline equation: g"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_base(p: dict) -> float:
    """Inline equation: base"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = g_muB_B0 / h_omega0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_Lambda_term(p: dict) -> float:
    """Inline equation: Lambda_term"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (Lambda * c_light * c_light) / 3.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_result(p: dict) -> float:
    """Inline equation: result"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = sum_Ug + Lambda_term
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_m_bh(p: dict) -> float:
    """Inline equation: m_bh"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_M_8(p: dict) -> float:
    """Inline equation: M"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_Hz(p: dict) -> float:
    """Inline equation: Hz"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_B0_2(p: dict) -> float:
    """Inline equation: B0"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_B_crit(p: dict) -> float:
    """Inline equation: B_crit"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_term1(p: dict) -> float:
    """Inline equation: term1"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = ug1_base * corr_H * corr_B
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_term_BH(p: dict) -> float:
    """Inline equation: term_BH"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (G * M_BH) / (r_BH * r_BH)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_Lambda(p: dict) -> float:
    """Inline equation: Lambda"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_rho_vac_UA_1(p: dict) -> float:
    """Inline equation: rho_vac_UA"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_rho_vac_SCm(p: dict) -> float:
    """Inline equation: rho_vac_SCm"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_term2_1(p: dict) -> float:
    """Inline equation: term2"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = ug1_base + Ug2 + Ug3 + Ug4
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_c_light(p: dict) -> float:
    """Inline equation: c_light"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_q_charge(p: dict) -> float:
    """Inline equation: q_charge"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_v_surf(p: dict) -> float:
    """Inline equation: v_surf"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_proton_mass(p: dict) -> float:
    """Inline equation: proton_mass"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_scale_EM(p: dict) -> float:
    """Inline equation: scale_EM"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_em_base(p: dict) -> float:
    """Inline equation: em_base"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (q_charge * cross_vB) / proton_mass
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_p_init(p: dict) -> float:
    """Inline equation: p_init"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_dOdt(p: dict) -> float:
    """Inline equation: dOdt"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = omega0 * (-1.0 / tau_Omega) * math.exp(-t / tau_Omega)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_gw_prefactor(p: dict) -> float:
    """Inline equation: gw_prefactor"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (G * M * M) / (pow(c_light, 4) * r)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_hbar(p: dict) -> float:
    """Inline equation: hbar"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_delta_x(p: dict) -> float:
    """Inline equation: delta_x"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_delta_p(p: dict) -> float:
    """Inline equation: delta_p"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_integral_psi(p: dict) -> float:
    """Inline equation: integral_psi"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_t_Hubble(p: dict) -> float:
    """Inline equation: t_Hubble"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_sqrt_unc(p: dict) -> float:
    """Inline equation: sqrt_unc"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = math.sqrt(delta_x * delta_p)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_G(p: dict) -> float:
    """Inline equation: G"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_rho_fluid(p: dict) -> float:
    """Inline equation: rho_fluid"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_V_2(p: dict) -> float:
    """Inline equation: V"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (4.0 / 3.0) * math.pi * r * r * r
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_a(p: dict) -> float:
    """Inline equation: a"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_term_osc1(p: dict) -> float:
    """Inline equation: term_osc1"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 2 * A_osc * math.cos(k_osc * x_pos) * math.cos(omega_osc * t)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_arg(p: dict) -> float:
    """Inline equation: arg"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = k_osc * x_pos - omega_osc * t
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_term_osc2(p: dict) -> float:
    """Inline equation: term_osc2"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (2 * math.pi / t_Hubble_gyr) * A_osc * math.cos(arg)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_dm_factor(p: dict) -> float:
    """Inline equation: dm_factor"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_term_dm_force_like(p: dict) -> float:
    """Inline equation: term_dm_force_like"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (M + M_dm) * (pert1 + pert2)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_mu0(p: dict) -> float:
    """Inline equation: mu0"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_M_mag(p: dict) -> float:
    """Inline equation: M_mag"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (B0 * B0 / (2 * mu0)) * V
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_l0(p: dict) -> float:
    """Inline equation: l0"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_exp_term(p: dict) -> float:
    """Inline equation: exp_term"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = math.exp(-t / tau_decay)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_cum_D(p: dict) -> float:
    """Inline equation: cum_D"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = L0_W * tau_decay * (1 - exp_term)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_f_trz(p: dict) -> float:
    """Inline equation: f_trz"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_r_7(p: dict) -> float:
    """Inline equation: r"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_H0(p: dict) -> float:
    """Inline equation: H0"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_B0_3(p: dict) -> float:
    """Inline equation: B0"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_tau_B(p: dict) -> float:
    """Inline equation: tau_B"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_Bt(p: dict) -> float:
    """Inline equation: Bt"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = B0 * math.exp(-t / tau_B)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_Ug4(p: dict) -> float:
    """Inline equation: Ug4"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = Ug1 * (1 - Bt / B_crit)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_term2_2(p: dict) -> float:
    """Inline equation: term2"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (Ug1 + Ug2 + Ug3 + Ug4) * (1 + f_TRZ_factor)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_scale_EM_1(p: dict) -> float:
    """Inline equation: scale_EM"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_corr_UA(p: dict) -> float:
    """Inline equation: corr_UA"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1 + (rho_vac_UA / rho_vac_SCm)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_p_init_1(p: dict) -> float:
    """Inline equation: p_init"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_delta_x_1(p: dict) -> float:
    """Inline equation: delta_x"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_delta_p_1(p: dict) -> float:
    """Inline equation: delta_p"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_t_Hubble_1(p: dict) -> float:
    """Inline equation: t_Hubble"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_rho_fluid_1(p: dict) -> float:
    """Inline equation: rho_fluid"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_a_1(p: dict) -> float:
    """Inline equation: a"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_term_osc2_1(p: dict) -> float:
    """Inline equation: term_osc2"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (2 * math.pi / t_Hubble) * A_osc * math.cos(k_osc * x_pos - omega_osc * t)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_dm_factor_1(p: dict) -> float:
    """Inline equation: dm_factor"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_f_trz_1(p: dict) -> float:
    """Inline equation: f_trz"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_M_initial(p: dict) -> float:
    """Inline equation: M_initial"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_r_8(p: dict) -> float:
    """Inline equation: r"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_B0_G(p: dict) -> float:
    """Inline equation: B0_G"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_tau_B_1(p: dict) -> float:
    """Inline equation: tau_B"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_M_dot(p: dict) -> float:
    """Inline equation: M_dot"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = M_dot_0 * math.exp(-t / tau_acc)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_Mt(p: dict) -> float:
    """Inline equation: Mt"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = M_initial * (1 + M_dot)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_B_G(p: dict) -> float:
    """Inline equation: B_G"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = B0_G * math.exp(-t / tau_B)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_term1_1(p: dict) -> float:
    """Inline equation: term1"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = ug1_t * corr_H * corr_B
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_spin(p: dict) -> float:
    """Inline equation: spin"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_M_dot_0(p: dict) -> float:
    """Inline equation: M_dot_0"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_tau_acc(p: dict) -> float:
    """Inline equation: tau_acc"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_omega0(p: dict) -> float:
    """Inline equation: omega0"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = spin_factor * c_light / r
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_gw_prefactor_1(p: dict) -> float:
    """Inline equation: gw_prefactor"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (G * Mt * Mt) / (pow(c_light, 4) * r)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_rho_fluid_2(p: dict) -> float:
    """Inline equation: rho_fluid"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_a_2(p: dict) -> float:
    """Inline equation: a"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_dm_factor_2(p: dict) -> float:
    """Inline equation: dm_factor"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_sin_prec(p: dict) -> float:
    """Inline equation: sin_prec"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = math.sin(precession_angle_deg * math.pi / 180.0)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_pert2_1(p: dict) -> float:
    """Inline equation: pert2"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 3 * G * Mt / (r * r * r)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_term_dm_force_like_1(p: dict) -> float:
    """Inline equation: term_dm_force_like"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (Mt + M_dm) * (pert1 + pert2 * sin_prec)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_g_val(p: dict) -> float:
    """Inline equation: g_val"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_mass_5(p: dict) -> float:
    """Inline equation: mass"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_radius_2(p: dict) -> float:
    """Inline equation: radius"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_hubble_1(p: dict) -> float:
    """Inline equation: hubble"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_b_field(p: dict) -> float:
    """Inline equation: b_field"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_b_crit_val(p: dict) -> float:
    """Inline equation: b_crit_val"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_mdot_factor(p: dict) -> float:
    """Inline equation: mdot_factor"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_tau_sf(p: dict) -> float:
    """Inline equation: tau_sf"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_M_dot_1(p: dict) -> float:
    """Inline equation: M_dot"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = mdot_factor * math.exp(-t / tau_sf)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_ug1_t(p: dict) -> float:
    """Inline equation: ug1_t"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (g_val * Mt) / (radius * radius)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_corr_B(p: dict) -> float:
    """Inline equation: corr_B"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1 - b_field / b_crit_val
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_lambda_val(p: dict) -> float:
    """Inline equation: lambda_val"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_c_val(p: dict) -> float:
    """Inline equation: c_val"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_f_trz_2(p: dict) -> float:
    """Inline equation: f_trz"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_Ug1(p: dict) -> float:
    """Inline equation: Ug1"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (g_val * Mt) / (radius * radius)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_q_val(p: dict) -> float:
    """Inline equation: q_val"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_v_gas(p: dict) -> float:
    """Inline equation: v_gas"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_m_proton(p: dict) -> float:
    """Inline equation: m_proton"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_rho_ua_1(p: dict) -> float:
    """Inline equation: rho_ua"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_rho_scm(p: dict) -> float:
    """Inline equation: rho_scm"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_em_scale(p: dict) -> float:
    """Inline equation: em_scale"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_em_base_1(p: dict) -> float:
    """Inline equation: em_base"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (q_val * cross_vB) / m_proton
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_corr_UA_1(p: dict) -> float:
    """Inline equation: corr_UA"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1 + (rho_ua / rho_scm)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_h_val(p: dict) -> float:
    """Inline equation: h_val"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_dx(p: dict) -> float:
    """Inline equation: dx"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_dp(p: dict) -> float:
    """Inline equation: dp"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_psi_int(p: dict) -> float:
    """Inline equation: psi_int"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_t_hub_1(p: dict) -> float:
    """Inline equation: t_hub"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_rho_f(p: dict) -> float:
    """Inline equation: rho_f"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_V_3(p: dict) -> float:
    """Inline equation: V"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (4.0 / 3.0) * math.pi * radius * radius * radius
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_amp_1(p: dict) -> float:
    """Inline equation: amp"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_k_val(p: dict) -> float:
    """Inline equation: k_val"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_omega_val(p: dict) -> float:
    """Inline equation: omega_val"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_x_val(p: dict) -> float:
    """Inline equation: x_val"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_t_hub_gyr(p: dict) -> float:
    """Inline equation: t_hub_gyr"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_term1_2(p: dict) -> float:
    """Inline equation: term1"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 2 * amp * math.cos(k_val * x_val) * math.cos(omega_val * t)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_arg_1(p: dict) -> float:
    """Inline equation: arg"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = k_val * x_val - omega_val * t
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_term2_3(p: dict) -> float:
    """Inline equation: term2"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (2 * math.pi / t_hub_gyr) * amp * math.cos(arg)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_m_dm_fac(p: dict) -> float:
    """Inline equation: m_dm_fac"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_delta_rho(p: dict) -> float:
    """Inline equation: delta_rho"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_pert2_2(p: dict) -> float:
    """Inline equation: pert2"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 3 * g_val * Mt / (radius * radius * radius)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_term_dm_force_like_2(p: dict) -> float:
    """Inline equation: term_dm_force_like"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (Mt + M_dm) * (pert1 + pert2)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_rho_w(p: dict) -> float:
    """Inline equation: rho_w"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_v_w(p: dict) -> float:
    """Inline equation: v_w"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_e0(p: dict) -> float:
    """Inline equation: e0"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_tau_eros(p: dict) -> float:
    """Inline equation: tau_eros"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_Et(p: dict) -> float:
    """Inline equation: Et"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = e0 * math.exp(-t / tau_eros)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_mass_6(p: dict) -> float:
    """Inline equation: mass"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_hz_val(p: dict) -> float:
    """Inline equation: hz_val"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_l_fac(p: dict) -> float:
    """Inline equation: l_fac"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_ug1(p: dict) -> float:
    """Inline equation: ug1"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (g_val * mass) / (radius * radius)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_L_t(p: dict) -> float:
    """Inline equation: L_t"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (g_val * mass) / (c_val * c_val * radius) * l_fac
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_Ug1_1(p: dict) -> float:
    """Inline equation: Ug1"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (g_val * mass) / (radius * radius)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_pert2_3(p: dict) -> float:
    """Inline equation: pert2"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 3 * g_val * mass / (radius * radius * radius)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_term_dm_force_like_3(p: dict) -> float:
    """Inline equation: term_dm_force_like"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (mass + M_dm) * (pert1 + pert2)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_M_9(p: dict) -> float:
    """Inline equation: M"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1.0000225e10 * 1.989e30
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_m_bh_1(p: dict) -> float:
    """Inline equation: m_bh"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_r_bh_val(p: dict) -> float:
    """Inline equation: r_bh_val"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_m_sn0(p: dict) -> float:
    """Inline equation: m_sn0"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_tau_sn(p: dict) -> float:
    """Inline equation: tau_sn"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_M_SNt(p: dict) -> float:
    """Inline equation: M_SNt"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = m_sn0 * math.exp(-t / tau_sn)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_mass0(p: dict) -> float:
    """Inline equation: mass0"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_Mt_1(p: dict) -> float:
    """Inline equation: Mt"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = mass0 * (1 + M_dot)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_p0(p: dict) -> float:
    """Inline equation: p0"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_tau_expansion(p: dict) -> float:
    """Inline equation: tau_expansion"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_Pt(p: dict) -> float:
    """Inline equation: Pt"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = p0 * math.exp(-t / tau_expansion)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_mass_7(p: dict) -> float:
    """Inline equation: mass"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_lambda_1(p: dict) -> float:
    """Inline equation: lambda"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_mass_8(p: dict) -> float:
    """Inline equation: mass"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_charge(p: dict) -> float:
    """Inline equation: charge"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_em_base_2(p: dict) -> float:
    """Inline equation: em_base"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (q * cross_vB) / m_p
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_dx_1(p: dict) -> float:
    """Inline equation: dx"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_mass_9(p: dict) -> float:
    """Inline equation: mass"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_A_1(p: dict) -> float:
    """Inline equation: A"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_term2_4(p: dict) -> float:
    """Inline equation: term2"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (2 * math.pi / t_Hubble_gyr) * A_osc * math.cos(arg)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_mass_10(p: dict) -> float:
    """Inline equation: mass"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_term_dm_force(p: dict) -> float:
    """Inline equation: term_dm_force"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (M + M_dm) * (pert1 + pert2)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_rho_w_1(p: dict) -> float:
    """Inline equation: rho_w"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_mass_11(p: dict) -> float:
    """Inline equation: mass"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_mass0_1(p: dict) -> float:
    """Inline equation: mass0"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_It(p: dict) -> float:
    """Inline equation: It"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = I0 * math.exp(-t / tau_merger)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_mass0_2(p: dict) -> float:
    """Inline equation: mass0"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_charge_1(p: dict) -> float:
    """Inline equation: charge"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_dx_2(p: dict) -> float:
    """Inline equation: dx"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_mass0_3(p: dict) -> float:
    """Inline equation: mass0"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_A_2(p: dict) -> float:
    """Inline equation: A"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_mass0_4(p: dict) -> float:
    """Inline equation: mass0"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_term_dm_force_1(p: dict) -> float:
    """Inline equation: term_dm_force"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (Mt + M_dm) * (pert1 + pert2)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_rho_w_2(p: dict) -> float:
    """Inline equation: rho_w"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_mass0_5(p: dict) -> float:
    """Inline equation: mass0"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_mass_12(p: dict) -> float:
    """Inline equation: mass"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_mass_13(p: dict) -> float:
    """Inline equation: mass"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_charge_2(p: dict) -> float:
    """Inline equation: charge"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_dx_3(p: dict) -> float:
    """Inline equation: dx"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_mass_14(p: dict) -> float:
    """Inline equation: mass"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_A_3(p: dict) -> float:
    """Inline equation: A"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_mass_15(p: dict) -> float:
    """Inline equation: mass"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_mass_16(p: dict) -> float:
    """Inline equation: mass"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_mass_17(p: dict) -> float:
    """Inline equation: mass"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_Ft(p: dict) -> float:
    """Inline equation: Ft"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = F0 * math.exp(-t / tau_fil)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_M_bh(p: dict) -> float:
    """Inline equation: M_bh"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_mass_18(p: dict) -> float:
    """Inline equation: mass"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_charge_3(p: dict) -> float:
    """Inline equation: charge"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_dx_4(p: dict) -> float:
    """Inline equation: dx"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_mass_19(p: dict) -> float:
    """Inline equation: mass"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_A_4(p: dict) -> float:
    """Inline equation: A"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_mass_20(p: dict) -> float:
    """Inline equation: mass"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_rho_c(p: dict) -> float:
    """Inline equation: rho_c"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_mass_21(p: dict) -> float:
    """Inline equation: mass"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_mass0_6(p: dict) -> float:
    """Inline equation: mass0"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_It_1(p: dict) -> float:
    """Inline equation: It"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = I0 * math.exp(-t / tau_inter)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_mass0_7(p: dict) -> float:
    """Inline equation: mass0"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_charge_4(p: dict) -> float:
    """Inline equation: charge"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_mass0_8(p: dict) -> float:
    """Inline equation: mass0"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_A_5(p: dict) -> float:
    """Inline equation: A"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_mass0_9(p: dict) -> float:
    """Inline equation: mass0"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_rho_w_3(p: dict) -> float:
    """Inline equation: rho_w"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_mass0_10(p: dict) -> float:
    """Inline equation: mass0"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_mass0_11(p: dict) -> float:
    """Inline equation: mass0"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_mass0_12(p: dict) -> float:
    """Inline equation: mass0"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_charge_5(p: dict) -> float:
    """Inline equation: charge"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_mass0_13(p: dict) -> float:
    """Inline equation: mass0"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_A_6(p: dict) -> float:
    """Inline equation: A"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_mass0_14(p: dict) -> float:
    """Inline equation: mass0"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_mass_22(p: dict) -> float:
    """Inline equation: mass"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_M_bh_1(p: dict) -> float:
    """Inline equation: M_bh"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_mass_23(p: dict) -> float:
    """Inline equation: mass"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_charge_6(p: dict) -> float:
    """Inline equation: charge"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_dx_5(p: dict) -> float:
    """Inline equation: dx"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_mass_24(p: dict) -> float:
    """Inline equation: mass"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_A_7(p: dict) -> float:
    """Inline equation: A"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_mass_25(p: dict) -> float:
    """Inline equation: mass"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_mass_26(p: dict) -> float:
    """Inline equation: mass"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_M_bh_2(p: dict) -> float:
    """Inline equation: M_bh"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_lambda_2(p: dict) -> float:
    """Inline equation: lambda"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_mass_27(p: dict) -> float:
    """Inline equation: mass"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_charge_7(p: dict) -> float:
    """Inline equation: charge"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_dx_6(p: dict) -> float:
    """Inline equation: dx"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_mass_28(p: dict) -> float:
    """Inline equation: mass"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_A_8(p: dict) -> float:
    """Inline equation: A"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_mass_29(p: dict) -> float:
    """Inline equation: mass"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_drag(p: dict) -> float:
    """Inline equation: drag"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_mass_30(p: dict) -> float:
    """Inline equation: mass"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_g_sun(p: dict) -> float:
    """Inline equation: g_sun"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = dpm_ug1_seed(M_Sun, r_orbit)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_mass_31(p: dict) -> float:
    """Inline equation: mass"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_charge_8(p: dict) -> float:
    """Inline equation: charge"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_dx_7(p: dict) -> float:
    """Inline equation: dx"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_mass_32(p: dict) -> float:
    """Inline equation: mass"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_A_9(p: dict) -> float:
    """Inline equation: A"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_mass_33(p: dict) -> float:
    """Inline equation: mass"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_M_r(p: dict) -> float:
    """Inline equation: M_r"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_v_1(p: dict) -> float:
    """Inline equation: v"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_mass_34(p: dict) -> float:
    """Inline equation: mass"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_mass_35(p: dict) -> float:
    """Inline equation: mass"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_charge_9(p: dict) -> float:
    """Inline equation: charge"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_dx_8(p: dict) -> float:
    """Inline equation: dx"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_mass_36(p: dict) -> float:
    """Inline equation: mass"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_A_10(p: dict) -> float:
    """Inline equation: A"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_mass_37(p: dict) -> float:
    """Inline equation: mass"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_pert2_4(p: dict) -> float:
    """Inline equation: pert2"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 3 * G * M_t / (r * r * r)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_term_dm_force_2(p: dict) -> float:
    """Inline equation: term_dm_force"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (M_t + M_dm) * (pert1 + pert2)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_sfr(p: dict) -> float:
    """Inline equation: sfr"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_E_0(p: dict) -> float:
    """Inline equation: E_0"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_mass_38(p: dict) -> float:
    """Inline equation: mass"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_g_base(p: dict) -> float:
    """Inline equation: g_base"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = dpm_ug1_seed(M, r_t)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_mass_39(p: dict) -> float:
    """Inline equation: mass"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_Ug1_2(p: dict) -> float:
    """Inline equation: Ug1"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = dpm_ug1_seed(M, r_t)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_charge_10(p: dict) -> float:
    """Inline equation: charge"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_em_base_3(p: dict) -> float:
    """Inline equation: em_base"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (q * cross_vB) / m_e
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_dx_9(p: dict) -> float:
    """Inline equation: dx"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_mass_40(p: dict) -> float:
    """Inline equation: mass"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_V_4(p: dict) -> float:
    """Inline equation: V"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (4.0 / 3.0) * math.pi * r_t * r_t * r_t
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_A_11(p: dict) -> float:
    """Inline equation: A"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_mass_41(p: dict) -> float:
    """Inline equation: mass"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_pert2_5(p: dict) -> float:
    """Inline equation: pert2"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 3 * G * M / (r_t * r_t * r_t)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_P(p: dict) -> float:
    """Inline equation: P"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_wind_pressure(p: dict) -> float:
    """Inline equation: wind_pressure"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = P_pulsar / (4 * math.pi * r_t * r_t)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_rel_corr(p: dict) -> float:
    """Inline equation: rel_corr"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1 + v_shock / c_light
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_a_wind(p: dict) -> float:
    """Inline equation: a_wind"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (wind_pressure * rel_corr) / rho
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_charge_11(p: dict) -> float:
    """Inline equation: charge"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_mass_42(p: dict) -> float:
    """Inline equation: mass"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_mass_43(p: dict) -> float:
    """Inline equation: mass"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_charge_12(p: dict) -> float:
    """Inline equation: charge"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_mass_44(p: dict) -> float:
    """Inline equation: mass"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_A_12(p: dict) -> float:
    """Inline equation: A"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_mass_45(p: dict) -> float:
    """Inline equation: mass"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_current(p: dict) -> float:
    """Inline equation: current"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_f(p: dict) -> float:
    """Inline equation: f"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_E_0_1(p: dict) -> float:
    """Inline equation: E_0"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_planck(p: dict) -> float:
    """Inline equation: planck"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_f_a_1(p: dict) -> float:
    """Inline equation: f_a"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_f_s(p: dict) -> float:
    """Inline equation: f_s"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_f_q_1(p: dict) -> float:
    """Inline equation: f_q"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_f_A(p: dict) -> float:
    """Inline equation: f_A"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_f_f(p: dict) -> float:
    """Inline equation: f_f"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_f_e(p: dict) -> float:
    """Inline equation: f_e"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_current_1(p: dict) -> float:
    """Inline equation: current"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_f_1(p: dict) -> float:
    """Inline equation: f"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_E_0_2(p: dict) -> float:
    """Inline equation: E_0"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_planck_1(p: dict) -> float:
    """Inline equation: planck"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_f_a_2(p: dict) -> float:
    """Inline equation: f_a"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_f_s_1(p: dict) -> float:
    """Inline equation: f_s"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_f_q_2(p: dict) -> float:
    """Inline equation: f_q"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_f_A_1(p: dict) -> float:
    """Inline equation: f_A"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_f_f_1(p: dict) -> float:
    """Inline equation: f_f"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_f_e_1(p: dict) -> float:
    """Inline equation: f_e"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_M_10(p: dict) -> float:
    """Inline equation: M"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_r_9(p: dict) -> float:
    """Inline equation: r"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_SFR(p: dict) -> float:
    """Inline equation: SFR"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_expansion(p: dict) -> float:
    """Inline equation: expansion"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1.0 + H0 * t * math.math.sqrt(0.3 * pow(1.0 + z, 3) + 0.7)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_B_4(p: dict) -> float:
    """Inline equation: B"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_v_out(p: dict) -> float:
    """Inline equation: v_out"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_Ug2(p: dict) -> float:
    """Inline equation: Ug2"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (v_out * v_out) / r
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_vac_ratio(p: dict) -> float:
    """Inline equation: vac_ratio"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1.0 + (rho_UA / rho_SCm)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_unc(p: dict) -> float:
    """Inline equation: unc"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = math.math.sqrt(Delta_x * Delta_p)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_rho_fluid_3(p: dict) -> float:
    """Inline equation: rho_fluid"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_x_1(p: dict) -> float:
    """Inline equation: x"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_omega_3(p: dict) -> float:
    """Inline equation: omega"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 2.0 * 3.141592653589793 * 1.411e16
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_standing(p: dict) -> float:
    """Inline equation: standing"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 2.0 * A * math.math.cos(k * x) * math.math.cos(omega * t)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_traveling_real(p: dict) -> float:
    """Inline equation: traveling_real"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (2.0 * 3.141592653589793 / 13.8) * traveling.real()
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_t_evolve(p: dict) -> float:
    """Inline equation: t_evolve"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_M_total(p: dict) -> float:
    """Inline equation: M_total"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_M_t(p: dict) -> float:
    """Inline equation: M_t"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = M_total * (t / t_Hubble)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_z_t(p: dict) -> float:
    """Inline equation: z_t"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (t_Hubble / t) - 1.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_H0_1(p: dict) -> float:
    """Inline equation: H0"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 70.0 * 1e3 / 3.0857e22
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_Hz_1(p: dict) -> float:
    """Inline equation: Hz"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = H0 * math.math.sqrt(Omega_m * pow(1.0 + z_t, 3) + Omega_Lambda)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_Ug1_3(p: dict) -> float:
    """Inline equation: Ug1"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (G * M_t) / (r_t * r_t)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_g_base_1(p: dict) -> float:
    """Inline equation: g_base"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (dpm_ug1_seed(M_t, r_t))
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_l_p(p: dict) -> float:
    """Inline equation: l_p"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_h_strain(p: dict) -> float:
    """Inline equation: h_strain"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_lambda_gw(p: dict) -> float:
    """Inline equation: lambda_gw"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_phase_1(p: dict) -> float:
    """Inline equation: phase"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (2.0 * 3.141592653589793 / lambda_gw) * r_t - (2.0 * 3.141592653589793 / year_s) * t
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_M_11(p: dict) -> float:
    """Inline equation: M"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_r_10(p: dict) -> float:
    """Inline equation: r"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_SFR_1(p: dict) -> float:
    """Inline equation: SFR"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_B_5(p: dict) -> float:
    """Inline equation: B"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_M_NGC5195(p: dict) -> float:
    """Inline equation: M_NGC5195"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_d_NGC5195(p: dict) -> float:
    """Inline equation: d_NGC5195"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_F_tidal(p: dict) -> float:
    """Inline equation: F_tidal"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (G * M_NGC5195) / (d_NGC5195 * d_NGC5195)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_F_SF(p: dict) -> float:
    """Inline equation: F_SF"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (SFR * M_sun / year_s) / (M * 1e-10)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_M_BH(p: dict) -> float:
    """Inline equation: M_BH"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_r_BH(p: dict) -> float:
    """Inline equation: r_BH"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 2.95e3 * (M_BH / M_sun)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_I_dipole(p: dict) -> float:
    """Inline equation: I_dipole"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.4 * M_BH * r_BH * r_BH
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_A_dipole(p: dict) -> float:
    """Inline equation: A_dipole"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 3.141592653589793 * r_BH * r_BH
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_omega_spin(p: dict) -> float:
    """Inline equation: omega_spin"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 2.0 * 3.141592653589793 / 1e4
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_d_1(p: dict) -> float:
    """Inline equation: d"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_E_react(p: dict) -> float:
    """Inline equation: E_react"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1e15 * math.math.exp(-0.0005 * t)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_omega_spiral(p: dict) -> float:
    """Inline equation: omega_spiral"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 2.0 * 3.141592653589793 / (2e8 * year_s)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_integral_psi_1(p: dict) -> float:
    """Inline equation: integral_psi"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = std.norm(psi_spiral)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_curvature(p: dict) -> float:
    """Inline equation: curvature"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 3.0 * G * M / (r * r * r)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_M_12(p: dict) -> float:
    """Inline equation: M"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_tau_merge(p: dict) -> float:
    """Inline equation: tau_merge"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_M_merge(p: dict) -> float:
    """Inline equation: M_merge"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1e10 * M_sun * math.math.exp(-t / tau_merge)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_m_factor(p: dict) -> float:
    """Inline equation: m_factor"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1.0 + (M_merge / M)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_M_BH_1(p: dict) -> float:
    """Inline equation: M_BH"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_omega_spin_1(p: dict) -> float:
    """Inline equation: omega_spin"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 2.0 * 3.141592653589793 / 1e5
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_M_spiral(p: dict) -> float:
    """Inline equation: M_spiral"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_d_2(p: dict) -> float:
    """Inline equation: d"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_psi_dust_sq(p: dict) -> float:
    """Inline equation: psi_dust_sq"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = A * A * math.math.exp(-r * r / (sigma_dust * sigma_dust))
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_rho_dust(p: dict) -> float:
    """Inline equation: rho_dust"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_d_spiral(p: dict) -> float:
    """Inline equation: d_spiral"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_F_tidal_1(p: dict) -> float:
    """Inline equation: F_tidal"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (G * M_spiral) / (d_spiral * d_spiral)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_F_cluster(p: dict) -> float:
    """Inline equation: F_cluster"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = k_cluster * M_cluster
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_f_DPM(p: dict) -> float:
    """Inline equation: f_DPM"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_rho_vac_plasm(p: dict) -> float:
    """Inline equation: rho_vac_plasm"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_f_THz(p: dict) -> float:
    """Inline equation: f_THz"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_f_react(p: dict) -> float:
    """Inline equation: f_react"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_f_quantum(p: dict) -> float:
    """Inline equation: f_quantum"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_unc_1(p: dict) -> float:
    """Inline equation: unc"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = math.math.sqrt(Delta_x * hbar / Delta_x)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_f_fluid(p: dict) -> float:
    """Inline equation: f_fluid"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_rho_3(p: dict) -> float:
    """Inline equation: rho"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_f_super(p: dict) -> float:
    """Inline equation: f_super"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_t_coal(p: dict) -> float:
    """Inline equation: t_coal"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_r_11(p: dict) -> float:
    """Inline equation: r"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_omega_4(p: dict) -> float:
    """Inline equation: omega"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 2.0 * 3.141592653589793 * f_super
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_f_Aether(p: dict) -> float:
    """Inline equation: f_Aether"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_M1(p: dict) -> float:
    """Inline equation: M1"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_M2(p: dict) -> float:
    """Inline equation: M2"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_M_chirp(p: dict) -> float:
    """Inline equation: M_chirp"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = pow(M1 * M2, 0.6) / pow(M_total, 0.2)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_eta(p: dict) -> float:
    """Inline equation: eta"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_T_s(p: dict) -> float:
    """Inline equation: T_s"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = T_s_base + rho_vac_A
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_f_Heaviside(p: dict) -> float:
    """Inline equation: f_Heaviside"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_scale_1(p: dict) -> float:
    """Inline equation: scale"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_mu_j(p: dict) -> float:
    """Inline equation: mu_j"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_r_j(p: dict) -> float:
    """Inline equation: r_j"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_gamma_1(p: dict) -> float:
    """Inline equation: gamma"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_t_n(p: dict) -> float:
    """Inline equation: t_n"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_phi_hat_j(p: dict) -> float:
    """Inline equation: phi_hat_j"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_P_SCm(p: dict) -> float:
    """Inline equation: P_SCm"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_E_react_1(p: dict) -> float:
    """Inline equation: E_react"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_exp_arg(p: dict) -> float:
    """Inline equation: exp_arg"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = -gamma * t * math.math.cos(pi * t_n)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_one_minus_exp(p: dict) -> float:
    """Inline equation: one_minus_exp"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1.0 - math.math.exp(exp_arg)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_base_1(p: dict) -> float:
    """Inline equation: base"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (mu_j / r_j) * one_minus_exp * phi_hat_j * P_SCm * E_react
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_heaviside_factor(p: dict) -> float:
    """Inline equation: heaviside_factor"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1.0 + scale * f_Heaviside
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_f_quasi(p: dict) -> float:
    """Inline equation: f_quasi"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_k_A(p: dict) -> float:
    """Inline equation: k_A"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_Z(p: dict) -> float:
    """Inline equation: Z"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_A_13(p: dict) -> float:
    """Inline equation: A"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_A_H(p: dict) -> float:
    """Inline equation: A_H"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_delta_pair(p: dict) -> float:
    """Inline equation: delta_pair"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_A_res(p: dict) -> float:
    """Inline equation: A_res"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = k_A * Z * (A / A_H) * (1.0 + delta_pair)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_E_bind(p: dict) -> float:
    """Inline equation: E_bind"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_h(p: dict) -> float:
    """Inline equation: h"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_f_res(p: dict) -> float:
    """Inline equation: f_res"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (E_J / h) * (A_H / A)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_k_7(p: dict) -> float:
    """Inline equation: k"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_A1(p: dict) -> float:
    """Inline equation: A1"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_A2(p: dict) -> float:
    """Inline equation: A2"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_f_dp(p: dict) -> float:
    """Inline equation: f_dp"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_phi_dp(p: dict) -> float:
    """Inline equation: phi_dp"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_U_dp(p: dict) -> float:
    """Inline equation: U_dp"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = k * (A1 * A2 / (f_dp * f_dp)) * math.cos(phi_dp)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_k_0(p: dict) -> float:
    """Inline equation: k_0"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_N(p: dict) -> float:
    """Inline equation: N"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_k_nuc(p: dict) -> float:
    """Inline equation: k_nuc"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_S_scale(p: dict) -> float:
    """Inline equation: S_scale"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_S_shell(p: dict) -> float:
    """Inline equation: S_shell"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = S_scale * (Z_magic + N_magic)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_S_shell_1(p: dict) -> float:
    """Inline equation: S_shell"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_r_nuc(p: dict) -> float:
    """Inline equation: r_nuc"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1.2e-15 * pow(A, 1.0 / 3.0)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_volume(p: dict) -> float:
    """Inline equation: volume"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (4.0 / 3.0) * math.pi * pow(r_nuc, 3)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_binding_contrib(p: dict) -> float:
    """Inline equation: binding_contrib"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = E_J / (volume * rho_nuc)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_range_factor(p: dict) -> float:
    """Inline equation: range_factor"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = math.exp(-r_nuc / 1.4e-15)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_buoyancy_2(p: dict) -> float:
    """Inline equation: buoyancy"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = S_shell + binding_contrib * (1.0 + pairing) * magic_enhance * range_factor
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_SC_m(p: dict) -> float:
    """Inline equation: SC_m"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_U_dp_1(p: dict) -> float:
    """Inline equation: U_dp"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_phase_2(p: dict) -> float:
    """Inline equation: phase"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 2.0 * math.pi * nu_nuc * t
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_tunneling(p: dict) -> float:
    """Inline equation: tunneling"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = math.exp(-V_barrier / (13.6 * math.sqrt(A)))
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_SC_enhanced(p: dict) -> float:
    """Inline equation: SC_enhanced"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = SC_m * coherence * tunneling * isotope_factor * spin_coupling + U_dp
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_tunneling_prob(p: dict) -> float:
    """Inline equation: tunneling_prob"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = math.exp(-V_barrier / (13.6 * math.sqrt(A)))
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_A_res_1(p: dict) -> float:
    """Inline equation: A_res"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_f_res_1(p: dict) -> float:
    """Inline equation: f_res"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_k_nuc_1(p: dict) -> float:
    """Inline equation: k_nuc"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_integrand_2(p: dict) -> float:
    """Inline equation: integrand"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_integrand_3(p: dict) -> float:
    """Inline equation: integrand"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_x2_1(p: dict) -> float:
    """Inline equation: x2"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_B_s(p: dict) -> float:
    """Inline equation: B_s"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_B_ref(p: dict) -> float:
    """Inline equation: B_ref"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_k_3_1(p: dict) -> float:
    """Inline equation: k_3"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_omega_s_1(p: dict) -> float:
    """Inline equation: omega_s"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_B_j(p: dict) -> float:
    """Inline equation: B_j"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = B_s * pow(B_s / B_ref, k_3) * math.cos(omega_s * t)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_B_s_min(p: dict) -> float:
    """Inline equation: B_s_min"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_B_s_max(p: dict) -> float:
    """Inline equation: B_s_max"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_solar_cycle_period(p: dict) -> float:
    """Inline equation: solar_cycle_period"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_cycle_phase(p: dict) -> float:
    """Inline equation: cycle_phase"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = fmod(t, solar_cycle_period) / solar_cycle_period
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_cycle_factor(p: dict) -> float:
    """Inline equation: cycle_factor"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.5 * (1.0 + math.cos(2.0 * math.pi * cycle_phase))
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_B_ref_1(p: dict) -> float:
    """Inline equation: B_ref"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = B_s_max * (0.1 + 0.9 * cycle_factor)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_mean_x(p: dict) -> float:
    """Inline equation: mean_x"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = accumulate(x.begin(), x.end(), 0.0) / x.size()
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_mean_y(p: dict) -> float:
    """Inline equation: mean_y"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = accumulate(y.begin(), y.end(), 0.0) / y.size()
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_numerator(p: dict) -> float:
    """Inline equation: numerator"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_SCm_i(p: dict) -> float:
    """Inline equation: SCm_i"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = layerIndex * layerIndex
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_E_DPM_i(p: dict) -> float:
    """Inline equation: E_DPM_i"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (hbar * c_light / (r_i * r_i)) * Q_i * SCm_i
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_life_proportion(p: dict) -> float:
    """Inline equation: life_proportion"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = p.DPM_stability * (p.DPM_momentum / p.DPM_gravity)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_modulated(p: dict) -> float:
    """Inline equation: modulated"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = life_proportion * E_cm
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_F_LENR(p: dict) -> float:
    """Inline equation: F_LENR"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = p.k_LENR * pow(omega_LENR / p.omega0, 2) * Q_wave
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_F_act(p: dict) -> float:
    """Inline equation: F_act"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = p.k_act * pow(omega_act / p.omega0, 2)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_F_DE(p: dict) -> float:
    """Inline equation: F_DE"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = p.k_DE * p.M * p.v * p.v / p.r
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_F_neutron(p: dict) -> float:
    """Inline equation: F_neutron"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = p.k_neutron * n_neutron * p.sigma_n
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_Delta_rho_vac(p: dict) -> float:
    """Inline equation: Delta_rho_vac"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = p.rho_vac_UA - p.rho_vac_SCm
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_F_vac_rep_1(p: dict) -> float:
    """Inline equation: F_vac_rep"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = p.k_vac * Delta_rho_vac * p.M * p.v
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_F_thz_shock_1(p: dict) -> float:
    """Inline equation: F_thz_shock"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = p.k_thz * pow(p.omega_thz / p.omega0, 2) * p.neutron_factor * p.conduit_scale
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_F_conduit_1(p: dict) -> float:
    """Inline equation: F_conduit"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = p.k_conduit * (p.H_abundance * p.water_state) * p.neutron_factor
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_F_spooky_1(p: dict) -> float:
    """Inline equation: F_spooky"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = p.k_spooky * (p.string_wave / p.omega0)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_integrand_4(p: dict) -> float:
    """Inline equation: integrand"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = F_LENR + F_act + F_DE + F_neutron + F_relativistic + F_vac_rep + F_thz_shock + F_conduit + F_spooky
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_c_quad(p: dict) -> float:
    """Inline equation: c_quad"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = p.V_void_fraction * 1e12
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_discriminant(p: dict) -> float:
    """Inline equation: discriminant"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = b_quad * b_quad - 4 * a_quad * c_quad
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_x_2(p: dict) -> float:
    """Inline equation: x_2"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_f_i(p: dict) -> float:
    """Inline equation: f_i"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = omega_i / (2 * math.pi)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_Ug1_i(p: dict) -> float:
    """Inline equation: Ug1_i"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = E_DPM_i / (r_i * r_i) * p.rho_vac_UA * f_TRZ_i
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_Ug2_i(p: dict) -> float:
    """Inline equation: Ug2_i"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = E_DPM_i / (r_i * r_i) * SCm_i * f_Um_i
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_Ug3_i(p: dict) -> float:
    """Inline equation: Ug3_i"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (hbar * omega_i / 2) * Q_i * math.cos(2 * math.pi * f_i * p.t) / r_i
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_Ug4_i(p: dict) -> float:
    """Inline equation: Ug4_i"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (dpm_ug1_seed(M_i, r_i)) * (1 + alpha_i) * SCm_i
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_g_layer(p: dict) -> float:
    """Inline equation: g_layer"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = Ug1_i + Ug2_i + Ug3_i + Ug4_i
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_gamma_2(p: dict) -> float:
    """Inline equation: gamma"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1.0 / math.sqrt(1.0 - (p.v * p.v) / (c_light * c_light))
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_alpha_g(p: dict) -> float:
    """Inline equation: alpha_g"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = G * M_sun * M_sun / (hbar * c_light)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_error(p: dict) -> float:
    """Inline equation: error"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = abs(observed_L_X - predicted_L_X) / observed_L_X * 100
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_mutationRate(p: dict) -> float:
    """Inline equation: mutationRate"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_error_1(p: dict) -> float:
    """Inline equation: error"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = observed[i] - predicted[i]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_adjustment(p: dict) -> float:
    """Inline equation: adjustment"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = -learning_rate * mse
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_M_13(p: dict) -> float:
    """Inline equation: M"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("M", 2e30)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_r_12(p: dict) -> float:
    """Inline equation: r"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("r", 7e8)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_B0_4(p: dict) -> float:
    """Inline equation: B0"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("B0", 1e-4)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_x_2_1(p: dict) -> float:
    """Inline equation: x_2"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = r * r / (M + 1e-10)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_freq_1(p: dict) -> float:
    """Inline equation: freq"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("omega_DPM", 1e12)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_amp_2(p: dict) -> float:
    """Inline equation: amp"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("DPM_amplitude", 1e-10)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_v_2(p: dict) -> float:
    """Inline equation: v"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("v", 1e5)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_gamma_3(p: dict) -> float:
    """Inline equation: gamma"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1.0 / math.math.sqrt(1.0 - (v * v) / (c * c))
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_Ms(p: dict) -> float:
    """Inline equation: Ms"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("Ms", 2e30)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_Rs(p: dict) -> float:
    """Inline equation: Rs"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("Rs", 7e8)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_Bs_avg(p: dict) -> float:
    """Inline equation: Bs_avg"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("Bs_avg", 1e-4)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_omega_c(p: dict) -> float:
    """Inline equation: omega_c"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("omega_c", 1e-5)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_r_13(p: dict) -> float:
    """Inline equation: r"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("r", 1e9)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_k1_1(p: dict) -> float:
    """Inline equation: k1"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("k1", 1.5)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_alpha_1(p: dict) -> float:
    """Inline equation: alpha"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("alpha", 0.001)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_delta_def(p: dict) -> float:
    """Inline equation: delta_def"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("delta_def", 0.01)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_Bs_t_2(p: dict) -> float:
    """Inline equation: Bs_t"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = Bs_avg + 0.4 * math.math.sin(omega_c * t) + 1e3
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_mu_s(p: dict) -> float:
    """Inline equation: mu_s"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = Bs_t * pow(Rs, 3)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_grad_Ms_r(p: dict) -> float:
    """Inline equation: grad_Ms_r"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = G_const * Ms / (Rs * Rs)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_defect_1(p: dict) -> float:
    """Inline equation: defect"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1.0 + delta_def * math.math.sin(0.001 * t)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_Rb(p: dict) -> float:
    """Inline equation: Rb"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("Rb", 1e11)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_QA(p: dict) -> float:
    """Inline equation: QA"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("QA", 1e-10)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_QUA(p: dict) -> float:
    """Inline equation: QUA"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("QUA", 1e-10)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_k2(p: dict) -> float:
    """Inline equation: k2"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("k2", 1.2)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_v_sw(p: dict) -> float:
    """Inline equation: v_sw"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("v_sw", 5e5)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_delta_sw(p: dict) -> float:
    """Inline equation: delta_sw"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("delta_sw", 0.01)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_HSCm(p: dict) -> float:
    """Inline equation: HSCm"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("HSCm", 1.0)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_rho_SCm(p: dict) -> float:
    """Inline equation: rho_SCm"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("rho_SCm", 1e-10)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_v_SCm(p: dict) -> float:
    """Inline equation: v_SCm"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("v_SCm", 0.99 * 3e8)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_rho_A(p: dict) -> float:
    """Inline equation: rho_A"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("rho_A", 1e-23)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_kappa(p: dict) -> float:
    """Inline equation: kappa"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("kappa", 0.0005)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_Ereact_1(p: dict) -> float:
    """Inline equation: Ereact"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (rho_SCm * v_SCm * v_SCm / rho_A) * math.math.exp(-kappa * t)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_omega_s_2(p: dict) -> float:
    """Inline equation: omega_s"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("omega_s", 1e-5)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_Pcore(p: dict) -> float:
    """Inline equation: Pcore"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("Pcore", 1.0)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_k3(p: dict) -> float:
    """Inline equation: k3"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("k3", 1.8)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_omega_s_t_1(p: dict) -> float:
    """Inline equation: omega_s_t"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = omega_s - 0.4e-6 * math.math.sin(omega_c * t)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_Bj_1(p: dict) -> float:
    """Inline equation: Bj"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1e-3 + 0.4 * math.math.sin(omega_c * t) + 1e3
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_rho_v(p: dict) -> float:
    """Inline equation: rho_v"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("rho_v", 6e-27)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_C_concentration(p: dict) -> float:
    """Inline equation: C_concentration"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("C_concentration", 1.0)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_Mbh(p: dict) -> float:
    """Inline equation: Mbh"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("Mbh", 8.15e36)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_dg(p: dict) -> float:
    """Inline equation: dg"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("dg", 2.55e20)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_f_feedback(p: dict) -> float:
    """Inline equation: f_feedback"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("f_feedback", 0.1)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_k4(p: dict) -> float:
    """Inline equation: k4"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("k4", 2.0)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_tn_1(p: dict) -> float:
    """Inline equation: tn"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("tn", 0.0)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_decay_1(p: dict) -> float:
    """Inline equation: decay"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = math.math.exp(-alpha * t)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_cycle(p: dict) -> float:
    """Inline equation: cycle"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = math.math.cos(math.pi * tn)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_rj(p: dict) -> float:
    """Inline equation: rj"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("rj", 1e9)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_gamma_4(p: dict) -> float:
    """Inline equation: gamma"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("gamma", 0.00005)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_phi_hat(p: dict) -> float:
    """Inline equation: phi_hat"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("phi_hat", 1.0)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_num_strings(p: dict) -> float:
    """Inline equation: num_strings"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("num_strings", 1e9)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_PSCm(p: dict) -> float:
    """Inline equation: PSCm"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("PSCm", 1.0)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_mu_j_1(p: dict) -> float:
    """Inline equation: mu_j"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = Bj * pow(Rs, 3)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_decay_2(p: dict) -> float:
    """Inline equation: decay"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1.0 - math.math.exp(-gamma * t * math.math.cos(math.pi * tn))
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_M_halo(p: dict) -> float:
    """Inline equation: M_halo"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("M_halo", 1e42)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_r_scale(p: dict) -> float:
    """Inline equation: r_scale"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("r_scale", 1e21)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_r_14(p: dict) -> float:
    """Inline equation: r"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("r", 1e20)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_rho_0_2(p: dict) -> float:
    """Inline equation: rho_0"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = M_halo / (4.0 * math.pi * r_scale * r_scale * r_scale * (math.math.log(2.0) - 0.5))
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_E_vac_scale(p: dict) -> float:
    """Inline equation: E_vac_scale"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("E_vac_scale", 7.09e-36)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_lambda_3(p: dict) -> float:
    """Inline equation: lambda"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("lambda", 1.0)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_H0_2(p: dict) -> float:
    """Inline equation: H0"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("H0", 2.269e-18)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_hbar_1(p: dict) -> float:
    """Inline equation: hbar"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("hbar", 1.0546e-34)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_Delta_x_p(p: dict) -> float:
    """Inline equation: Delta_x_p"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("Delta_x_p", 1e-68)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_integral_psi_2(p: dict) -> float:
    """Inline equation: integral_psi"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("integral_psi", 2.176e-18)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_tHubble(p: dict) -> float:
    """Inline equation: tHubble"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("tHubble", 4.35e17)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_time_factor(p: dict) -> float:
    """Inline equation: time_factor"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = integral_psi / tHubble
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_Lambda_1(p: dict) -> float:
    """Inline equation: Lambda"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("Lambda", 1.1e-52)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_rho_4(p: dict) -> float:
    """Inline equation: rho"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("rho", 1e-27)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_P_1(p: dict) -> float:
    """Inline equation: P"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("P", 1e-15)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_fluid_factor(p: dict) -> float:
    """Inline equation: fluid_factor"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (rho + 3.0 * P / (3e8 * 3e8)) * r * r * r
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_delta_metric(p: dict) -> float:
    """Inline equation: delta_metric"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("delta_metric", 1e-10)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_omega_pert(p: dict) -> float:
    """Inline equation: omega_pert"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("omega_pert", 1e-8)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_omega_res(p: dict) -> float:
    """Inline equation: omega_res"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("omega_res", 1e-5)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_Q_factor(p: dict) -> float:
    """Inline equation: Q_factor"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("Q_factor", 100.0)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_amplitude(p: dict) -> float:
    """Inline equation: amplitude"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("amplitude", 1e-10)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_resonance(p: dict) -> float:
    """Inline equation: resonance"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = amplitude * math.math.sin(omega_res * t) / (1.0 + (omega_res * omega_res) / (Q_factor * Q_factor))
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_B_crit_1(p: dict) -> float:
    """Inline equation: B_crit"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("B_crit", 4.4e13)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_B_field_1(p: dict) -> float:
    """Inline equation: B_field"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("B_field", 1e-4)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_adjustment_scale(p: dict) -> float:
    """Inline equation: adjustment_scale"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("adjustment_scale", 1e-5)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_T_env(p: dict) -> float:
    """Inline equation: T_env"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("T_env", 2.7)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_scale_2(p: dict) -> float:
    """Inline equation: scale"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("env_scale", 1e-30)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_Ug1_4(p: dict) -> float:
    """Inline equation: Ug1"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("Ug1", 0.0)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_Ug2_1(p: dict) -> float:
    """Inline equation: Ug2"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("Ug2", 0.0)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_Ug3(p: dict) -> float:
    """Inline equation: Ug3"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("Ug3", 0.0)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_Ug4_1(p: dict) -> float:
    """Inline equation: Ug4"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("Ug4", 0.0)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_Omega_g(p: dict) -> float:
    """Inline equation: Omega_g"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("Omega_g", 1.0)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_mu_j_2(p: dict) -> float:
    """Inline equation: mu_j"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("mu_j", 1e10)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_rho_sw(p: dict) -> float:
    """Inline equation: rho_sw"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("rho_sw", 1e-20)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_base_2(p: dict) -> float:
    """Inline equation: base"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("Ug_base", 1e10)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_k_base(p: dict) -> float:
    """Inline equation: k_base"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("k_base", 1.0)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_lambda_base(p: dict) -> float:
    """Inline equation: lambda_base"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("lambda_base", 1.0)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_rho_vac_UA_2(p: dict) -> float:
    """Inline equation: rho_vac_UA"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("rho_vac_UA", 7.09e-36)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_B_j_1(p: dict) -> float:
    """Inline equation: B_j"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("B_j", 1e-3)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_Mbh_solar(p: dict) -> float:
    """Inline equation: Mbh_solar"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("Mbh_solar", 4.1e6)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_B_j_2(p: dict) -> float:
    """Inline equation: B_j"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1e-3 + 0.4 * math.math.sin(omega_c * t) + 1e3
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_theta_1(p: dict) -> float:
    """Inline equation: theta"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("theta", math.pi / 4)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_phi(p: dict) -> float:
    """Inline equation: phi"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("phi", math.pi / 4)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_r_15(p: dict) -> float:
    """Inline equation: r"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("r", 1.5e13)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_R_b(p: dict) -> float:
    """Inline equation: R_b"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("R_b", 1.496e13)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_k2_1(p: dict) -> float:
    """Inline equation: k2"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("k2", 1.0)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_M_s(p: dict) -> float:
    """Inline equation: M_s"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("M_s", 1.989e30)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_S_1(p: dict) -> float:
    """Inline equation: S"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_gamma_day(p: dict) -> float:
    """Inline equation: gamma_day"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("gamma_day", 0.00005)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_t_n_1(p: dict) -> float:
    """Inline equation: t_n"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("t_n", 0.0)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_t_day(p: dict) -> float:
    """Inline equation: t_day"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("t_day", 1000.0)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_mu_over_rj(p: dict) -> float:
    """Inline equation: mu_over_rj"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("mu_over_rj", 2.26e10)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_factor(p: dict) -> float:
    """Inline equation: factor"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1.0 - math.math.exp(-gamma_day * t_day * math.math.cos(math.pi * t_n))
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_P_SCm_1(p: dict) -> float:
    """Inline equation: P_SCm"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("P_SCm", 1.0)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_P_SCm_planet(p: dict) -> float:
    """Inline equation: P_SCm_planet"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("P_SCm_planet", 1e-3)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_kappa_day(p: dict) -> float:
    """Inline equation: kappa_day"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("kappa_day", 0.0005)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_t_day_1(p: dict) -> float:
    """Inline equation: t_day"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("t_day", 0.0)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_E_react_0(p: dict) -> float:
    """Inline equation: E_react_0"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("E_react_0", 1e46)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_E_react_2(p: dict) -> float:
    """Inline equation: E_react"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = E_react_0 * math.math.exp(-kappa_day * t_day)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_period(p: dict) -> float:
    """Inline equation: period"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("period", 3.96e8)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_omega_c_1(p: dict) -> float:
    """Inline equation: omega_c"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 2.0 * math.pi / period
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_B_j_base(p: dict) -> float:
    """Inline equation: B_j_base"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("B_j_base", 1e3)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_R_s(p: dict) -> float:
    """Inline equation: R_s"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("R_s", 7e8)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_B_j_3(p: dict) -> float:
    """Inline equation: B_j"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = B_j_base + 0.4 * math.math.sin(omega_c * t)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_period_s(p: dict) -> float:
    """Inline equation: period_s"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("period", 3.96e8)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_amplitude_1(p: dict) -> float:
    """Inline equation: amplitude"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("amplitude", 0.4)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_r_16(p: dict) -> float:
    """Inline equation: r"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("r", 1.496e13)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_modulation_1(p: dict) -> float:
    """Inline equation: modulation"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1.0 + delta_sw * v_sw
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_B_j_4(p: dict) -> float:
    """Inline equation: B_j"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("B_j", 1e3)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_P_core(p: dict) -> float:
    """Inline equation: P_core"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("P_core", 1.0)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_E_react_3(p: dict) -> float:
    """Inline equation: E_react"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("E_react", 1e46)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_Lambda_i(p: dict) -> float:
    """Inline equation: Lambda_i"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("Lambda_i", 1.0)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_T_s_base(p: dict) -> float:
    """Inline equation: T_s_base"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("T_s_base", 1e7)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_rho_vac_A_1(p: dict) -> float:
    """Inline equation: rho_vac_A"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("rho_vac_A", 1.23e6)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_epsilon(p: dict) -> float:
    """Inline equation: epsilon"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("epsilon", 1e-22)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_B_s_1(p: dict) -> float:
    """Inline equation: B_s"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("B_s", 1e-4)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_B_ref_2(p: dict) -> float:
    """Inline equation: B_ref"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("B_ref", 0.4)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_B_j_5(p: dict) -> float:
    """Inline equation: B_j"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = B_j_base * (B_s / B_ref)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_T_s_1(p: dict) -> float:
    """Inline equation: T_s"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("T_s", 5778.0)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_T_s_ref(p: dict) -> float:
    """Inline equation: T_s_ref"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("T_s_ref", 5778.0)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_B_j_6(p: dict) -> float:
    """Inline equation: B_j"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = B_j_base * (T_s / T_s_ref)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_base_3(p: dict) -> float:
    """Inline equation: base"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = omega_s * math.math.cos(math.pi * t_n) * Lambda_i
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_amplitude_2(p: dict) -> float:
    """Inline equation: amplitude"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("amplitude", 0.01)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_omega_def(p: dict) -> float:
    """Inline equation: omega_def"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("omega_def", 0.001)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_t_day_2(p: dict) -> float:
    """Inline equation: t_day"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("t_day", t / 86400.0)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_r_17(p: dict) -> float:
    """Inline equation: r"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("r", 1.496e11)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_delta_def_1(p: dict) -> float:
    """Inline equation: delta_def"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = amplitude * math.math.sin(omega_def * t_day)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_theta_j(p: dict) -> float:
    """Inline equation: theta_j"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("theta_j", 0.0)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_phi_hat_magnitude(p: dict) -> float:
    """Inline equation: phi_hat_magnitude"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("phi_hat_magnitude", 1.0)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_T_s_base_1(p: dict) -> float:
    """Inline equation: T_s_base"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("T_s_base", 1.11e7)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_rho_vac_A_2(p: dict) -> float:
    """Inline equation: rho_vac_A"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("rho_vac_A", 1e-23)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_T_s_2(p: dict) -> float:
    """Inline equation: T_s"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("T_s", 1.123e7)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_rho_vac_SCm_1(p: dict) -> float:
    """Inline equation: rho_vac_SCm"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("rho_vac_SCm", 7.09e-37)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_vac_product(p: dict) -> float:
    """Inline equation: vac_product"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = math.math.sqrt(rho_vac_SCm * rho_vac_UA)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_S_factor(p: dict) -> float:
    """Inline equation: S_factor"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("S_factor", 1.0)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_modulation_2(p: dict) -> float:
    """Inline equation: modulation"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("modulation", 5001.0)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_rho_SCm_1(p: dict) -> float:
    """Inline equation: rho_SCm"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("rho_vac_SCm", 7.09e-37)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_rho_A_1(p: dict) -> float:
    """Inline equation: rho_A"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("rho_vac_A", 1e-23)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_v_scm(p: dict) -> float:
    """Inline equation: v_scm"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("v_scm", 1e8)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_E_base(p: dict) -> float:
    """Inline equation: E_base"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("E_react_base", 1e46)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_mu_over_r(p: dict) -> float:
    """Inline equation: mu_over_r"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("mu_over_rj", 2.26e10)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_M_14(p: dict) -> float:
    """Inline equation: M"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("M", 1.27e30)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_r_18(p: dict) -> float:
    """Inline equation: r"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("r", 3.22e19)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_rho_0_3(p: dict) -> float:
    """Inline equation: rho_0"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("rho_0", 1e-15)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_rho_LENR(p: dict) -> float:
    """Inline equation: rho_LENR"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("rho_LENR", 1e8)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_integrand_5(p: dict) -> float:
    """Inline equation: integrand"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("integrand", 1e200)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_x2_2(p: dict) -> float:
    """Inline equation: x2"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("x2", 1e12)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_M_15(p: dict) -> float:
    """Inline equation: M"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("M", 1.09e40)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_r_19(p: dict) -> float:
    """Inline equation: r"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("r", 1.17e23)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_integrand_6(p: dict) -> float:
    """Inline equation: integrand"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("integrand", 1e205)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_M_16(p: dict) -> float:
    """Inline equation: M"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("M", 1.23e45)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_r_20(p: dict) -> float:
    """Inline equation: r"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("r", 3.93e22)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_omega_5(p: dict) -> float:
    """Inline equation: omega"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("omega", 1e-15)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_a_3(p: dict) -> float:
    """Inline equation: a"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("a", 1.0)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_b(p: dict) -> float:
    """Inline equation: b"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("b", -1e12)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_c(p: dict) -> float:
    """Inline equation: c"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("c", 1e24)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_omega_6(p: dict) -> float:
    """Inline equation: omega"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("omega", 1e-12)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_rho_vac_2(p: dict) -> float:
    """Inline equation: rho_vac"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("rho_vac_UA", 7.09e-36)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_integrand_7(p: dict) -> float:
    """Inline equation: integrand"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("integrand", 1e210)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_M_17(p: dict) -> float:
    """Inline equation: M"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("M", 1.989e37)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_r_21(p: dict) -> float:
    """Inline equation: r"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("r", 3.09e18)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_rho_0_4(p: dict) -> float:
    """Inline equation: rho_0"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("rho_0", 1e-12)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_integrand_8(p: dict) -> float:
    """Inline equation: integrand"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("integrand", 1e190)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_M_18(p: dict) -> float:
    """Inline equation: M"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("M", 4e41)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_r_22(p: dict) -> float:
    """Inline equation: r"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("r", 3.09e21)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_M_19(p: dict) -> float:
    """Inline equation: M"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("M", 1e31)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_r_23(p: dict) -> float:
    """Inline equation: r"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("r", 4.73e16)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_integrand_9(p: dict) -> float:
    """Inline equation: integrand"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("integrand", 1e150)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_M_20(p: dict) -> float:
    """Inline equation: M"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("M", 4.97e45)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_r_24(p: dict) -> float:
    """Inline equation: r"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("r", 3.09e22)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_integrand_10(p: dict) -> float:
    """Inline equation: integrand"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("integrand", 1e212)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_integrand_11(p: dict) -> float:
    """Inline equation: integrand"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("integrand", 1e202)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_M_21(p: dict) -> float:
    """Inline equation: M"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("M", 1.989e40)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_r_25(p: dict) -> float:
    """Inline equation: r"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("r", 3.09e20)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_M_22(p: dict) -> float:
    """Inline equation: M"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("M", 1.73e40)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_r_26(p: dict) -> float:
    """Inline equation: r"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("r", 9.63e20)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_M_23(p: dict) -> float:
    """Inline equation: M"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("M", 2.785e30)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_r_27(p: dict) -> float:
    """Inline equation: r"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getDynamicParameter("r", 3.09e15)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_g_2(p: dict) -> float:
    """Inline equation: g"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_M_24(p: dict) -> float:
    """Inline equation: M"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_exp_arg_1(p: dict) -> float:
    """Inline equation: exp_arg"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = -gamma * t * math.math.cos(3.14159 * 0.0)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_base_4(p: dict) -> float:
    """Inline equation: base"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = mu_over_rj * one_minus_exp * 1.0 * 1.0 * E_react
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_k_1_1(p: dict) -> float:
    """Inline equation: k_1"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_rho_UA(p: dict) -> float:
    """Inline equation: rho_UA"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_M_s_1(p: dict) -> float:
    """Inline equation: M_s"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_omega_c_2(p: dict) -> float:
    """Inline equation: omega_c"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 2 * 3.14159 / (11.0 * 365.25 * 86400.0)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_mu_j_3(p: dict) -> float:
    """Inline equation: mu_j"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = mu_0 * (1.0 + 0.3 * math.math.sin(omega_c * t))
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_exp_arg_2(p: dict) -> float:
    """Inline equation: exp_arg"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = -gamma * t * math.math.cos(3.14159 * t_n)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_omega_c_3(p: dict) -> float:
    """Inline equation: omega_c"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 2 * 3.14159265358979323846 / (11.0 * 365.25 * 86400.0)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_T_cycle(p: dict) -> float:
    """Inline equation: T_cycle"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 11.0 * 365.25 * 86400.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_corr(p: dict) -> float:
    """Inline equation: corr"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = StatisticalAnalyzer.correlationCoefficient(all_masses, all_forces)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_outline(p: dict) -> float:
    """Inline equation: outline"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = uqff_egg.GetSphericalOutline()
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_final_outline(p: dict) -> float:
    """Inline equation: final_outline"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = uqff_egg.GetSphericalOutline()
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_r_s_w(p: dict) -> float:
    """Inline equation: r_s_w"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 2.0 * 6.674e-11 * M_bh_w / (2.998e8 * 2.998e8)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_T_hawking_w(p: dict) -> float:
    """Inline equation: T_hawking_w"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1.055e-34 * 2.998e8 * 2.998e8 * 2.998e8 / (8.0 * math.pi * 6.674e-11 * M_bh_w * 1.381e-23)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_A_horizon_w(p: dict) -> float:
    """Inline equation: A_horizon_w"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 4.0 * math.pi * r_s_w * r_s_w
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_S_BH_w(p: dict) -> float:
    """Inline equation: S_BH_w"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = A_horizon_w / (4.0 * 6.674e-11)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_r_s(p: dict) -> float:
    """Inline equation: r_s"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 2.0 * 6.674e-11 * M_bh / (2.998e8 * 2.998e8)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_T_hawking(p: dict) -> float:
    """Inline equation: T_hawking"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1.055e-34 * 2.998e8 * 2.998e8 * 2.998e8 / (8.0 * math.pi * 6.674e-11 * M_bh * 1.381e-23)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_A_horizon(p: dict) -> float:
    """Inline equation: A_horizon"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 4.0 * math.pi * r_s * r_s
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_S_BH(p: dict) -> float:
    """Inline equation: S_BH"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = A_horizon / (4.0 * 6.674e-11)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_channel_capacity(p: dict) -> float:
    """Inline equation: channel_capacity"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = S_BH / (26.0 * 26.0)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_rho_sum(p: dict) -> float:
    """Inline equation: rho_sum"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["rho_sum"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_S_r_Rb(p: dict) -> float:
    """Inline equation: S_r_Rb"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["S_r_Rb"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_swirl_factor(p: dict) -> float:
    """Inline equation: swirl_factor"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["swirl_factor"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_E_react_4(p: dict) -> float:
    """Inline equation: E_react"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["E_react"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_result_1(p: dict) -> float:
    """Inline equation: result"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = computeU_g2(t, t_n)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_U_g2_helio(p: dict) -> float:
    """Inline equation: U_g2_helio"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = g_heliosphere_module.computeU_g2(current_time, t_negative)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_H_factor(p: dict) -> float:
    """Inline equation: H_factor"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = g_heliosphere_module.getH_SCm()
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_total_ug(p: dict) -> float:
    """Inline equation: total_ug"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = g_ug_index_module.computeSumKUgi()
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_ug2_contribution(p: dict) -> float:
    """Inline equation: ug2_contribution"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = g_ug_index_module.getComponentContribution(2)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_rho_sc_1(p: dict) -> float:
    """Inline equation: rho_sc"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["rho_vac_SCm"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_rho_ua_2(p: dict) -> float:
    """Inline equation: rho_ua"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["rho_vac_UA"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_omega_s_t_2(p: dict) -> float:
    """Inline equation: omega_s_t"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["omega_s"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_cos_term(p: dict) -> float:
    """Inline equation: cos_term"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = math.math.cos(variables["pi"] * variables["t_n"])
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_trz_factor(p: dict) -> float:
    """Inline equation: trz_factor"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1.0 + variables["f_TRZ"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_e_react(p: dict) -> float:
    """Inline equation: e_react"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["E_react"] * math.math.exp(-variables["alpha_decay"] * t)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_t(p: dict) -> float:
    """Inline equation: t"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_term(p: dict) -> float:
    """Inline equation: term"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = computeInertiaTerm(i, t)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_base_mu(p: dict) -> float:
    """Inline equation: base_mu"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["base_mu"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_omega_c_4(p: dict) -> float:
    """Inline equation: omega_c"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["omega_c"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_amplitude_3(p: dict) -> float:
    """Inline equation: amplitude"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["amplitude"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_base_5(p: dict) -> float:
    """Inline equation: base"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["base_field"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_t_1(p: dict) -> float:
    """Inline equation: t"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_sw_term(p: dict) -> float:
    """Inline equation: sw_term"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1.0 + variables["gamma_sw"] * variables["rho_vac_sw"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_decay_3(p: dict) -> float:
    """Inline equation: decay"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = math.math.exp(-variables["gamma"] * 0.0)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_feedback(p: dict) -> float:
    """Inline equation: feedback"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1.0 + variables["f_feedback"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_M_solar(p: dict) -> float:
    """Inline equation: M_solar"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = computeM_bhInMsun()
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_cos_term_1(p: dict) -> float:
    """Inline equation: cos_term"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = math.math.cos(vars["pi"] * tn)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_k1_SOURCE4(p: dict) -> float:
    """Inline equation: k1_SOURCE4"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_SCm_contrib(p: dict) -> float:
    """Inline equation: SCm_contrib"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_SCm_contrib_1(p: dict) -> float:
    """Inline equation: SCm_contrib"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_SCm_contrib_2(p: dict) -> float:
    """Inline equation: SCm_contrib"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_mu_s_1(p: dict) -> float:
    """Inline equation: mu_s"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = compute_mu_s_SOURCE4(t, body.Bs_avg, body.omega_c, body.Rs)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_grad_Ms_r_1(p: dict) -> float:
    """Inline equation: grad_Ms_r"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = compute_grad_Ms_r_SOURCE4(body.Ms, body.Rs)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_Ereact_2(p: dict) -> float:
    """Inline equation: Ereact"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = compute_Ereact_SOURCE4(t, body.SCm_density, v_SCm_SOURCE4, rho_A, kappa)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_S_2(p: dict) -> float:
    """Inline equation: S"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = step_function_SOURCE4(r, body.Rb)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_omega_s_t_3(p: dict) -> float:
    """Inline equation: omega_s_t"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = compute_omega_s_t_SOURCE4(t, body.omega_s, body.omega_c)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_Bj_2(p: dict) -> float:
    """Inline equation: Bj"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = compute_Bj_SOURCE4(t, body.omega_c)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_cycle_1(p: dict) -> float:
    """Inline equation: cycle"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = math.math.cos(PI_SOURCE4 * tn)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_wind_mod_1(p: dict) -> float:
    """Inline equation: wind_mod"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1.0 + epsilon_sw * rho_sw
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_phi_hat_1(p: dict) -> float:
    """Inline equation: phi_hat"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_mu_j_4(p: dict) -> float:
    """Inline equation: mu_j"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = compute_mu_j_SOURCE4(t, body.omega_c, body.Rs)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_decay_4(p: dict) -> float:
    """Inline equation: decay"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1.0 - math.math.exp(-gamma * t * math.math.cos(PI_SOURCE4 * tn))
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_Ug1_5(p: dict) -> float:
    """Inline equation: Ug1"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = compute_Ug1_SOURCE4(body, r, t, tn, alpha_SOURCE4, delta_def_SOURCE4, k1_SOURCE4)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_Ug2_2(p: dict) -> float:
    """Inline equation: Ug2"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = compute_Ug2_SOURCE4(body, r, t, tn, k2_SOURCE4, QA_SOURCE4, delta_sw_SOURCE4, 
                                      v_sw_SOURCE4, HSCm_SOURCE4, rho_A_SOURCE4, kappa_SOURCE4)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_Ug3_1(p: dict) -> float:
    """Inline equation: Ug3"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = compute_Ug3_SOURCE4(body, r, t, tn, theta, rho_A_SOURCE4, kappa_SOURCE4, k3_SOURCE4)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_Ug4_2(p: dict) -> float:
    """Inline equation: Ug4"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = compute_Ug4_SOURCE4(t, tn, rho_v_SOURCE4, C_concentration_SOURCE4, 
                                      Mbh_SOURCE4, dg_SOURCE4, alpha_SOURCE4, f_feedback_SOURCE4, k4_SOURCE4)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_sum_Ug(p: dict) -> float:
    """Inline equation: sum_Ug"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = Ug1 + Ug2 + Ug3 + Ug4
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_Ubi(p: dict) -> float:
    """Inline equation: Ubi"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = compute_Ubi_SOURCE4(sum_Ug, beta_i_SOURCE4, Omega_g_SOURCE4, Mbh_SOURCE4, dg_SOURCE4,
                                      epsilon_sw_SOURCE4, rho_sw_SOURCE4, UUA_SOURCE4, tn)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_Um(p: dict) -> float:
    """Inline equation: Um"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = compute_Um_SOURCE4(body, t, tn, body.Rb, gamma_SOURCE4, rho_A_SOURCE4, 
                                    kappa_SOURCE4, num_strings_SOURCE4)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_H0_3(p: dict) -> float:
    """Inline equation: H0"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_Lambda_2(p: dict) -> float:
    """Inline equation: Lambda"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_hbar_2(p: dict) -> float:
    """Inline equation: hbar"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_base_6(p: dict) -> float:
    """Inline equation: base"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = compute_compressed_base_SOURCE4(sys)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_expansion_1(p: dict) -> float:
    """Inline equation: expansion"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = compute_compressed_expansion_SOURCE4(sys)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_super_adj(p: dict) -> float:
    """Inline equation: super_adj"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = compute_compressed_super_adj_SOURCE4(sys)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_env(p: dict) -> float:
    """Inline equation: env"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = compute_compressed_env_SOURCE4()
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_adjusted_base(p: dict) -> float:
    """Inline equation: adjusted_base"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = base * expansion * super_adj * env
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_Ug_sum(p: dict) -> float:
    """Inline equation: Ug_sum"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = compute_compressed_Ug_sum_SOURCE4()
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_cosm(p: dict) -> float:
    """Inline equation: cosm"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = compute_compressed_cosm_SOURCE4()
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_quantum(p: dict) -> float:
    """Inline equation: quantum"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = compute_compressed_quantum_SOURCE4()
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_fluid(p: dict) -> float:
    """Inline equation: fluid"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = compute_compressed_fluid_SOURCE4(sys)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_perturbation(p: dict) -> float:
    """Inline equation: perturbation"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = compute_compressed_perturbation_SOURCE4(sys)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_FDPM_1(p: dict) -> float:
    """Inline equation: FDPM"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = sys.I * sys.A * (sys.omega1 - sys.omega2)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_Ereact_3(p: dict) -> float:
    """Inline equation: Ereact"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1046 * math.math.exp(-0.0005 * sys.t)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_H_z(p: dict) -> float:
    """Inline equation: H_z"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_fexp(p: dict) -> float:
    """Inline equation: fexp"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 2 * PI_SOURCE4 * H_z * sys.t
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_b_1(p: dict) -> float:
    """Inline equation: b"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_aDPM_1(p: dict) -> float:
    """Inline equation: aDPM"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = compute_aDPM_SOURCE4(sys, res)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_aTHz(p: dict) -> float:
    """Inline equation: aTHz"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = compute_aTHz_SOURCE4(aDPM, sys, res)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_avac_diff(p: dict) -> float:
    """Inline equation: avac_diff"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = compute_avac_diff_SOURCE4(aDPM, sys, res)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_asuper_freq_1(p: dict) -> float:
    """Inline equation: asuper_freq"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = compute_asuper_freq_SOURCE4(aDPM, res)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_aaether_res_1(p: dict) -> float:
    """Inline equation: aaether_res"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = compute_aaether_res_SOURCE4(aDPM, res)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_Ug4i(p: dict) -> float:
    """Inline equation: Ug4i"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = compute_Ug4i_SOURCE4(aDPM, sys, res)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_aquantum_freq_1(p: dict) -> float:
    """Inline equation: aquantum_freq"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = compute_aquantum_freq_SOURCE4(aDPM, res)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_aAether_freq(p: dict) -> float:
    """Inline equation: aAether_freq"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = compute_aAether_freq_SOURCE4(aDPM, res)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_afluid_freq(p: dict) -> float:
    """Inline equation: afluid_freq"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = compute_afluid_freq_SOURCE4(sys, res)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_Osc_term(p: dict) -> float:
    """Inline equation: Osc_term"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = compute_Osc_term_SOURCE4()
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_aexp_freq(p: dict) -> float:
    """Inline equation: aexp_freq"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = compute_aexp_freq_SOURCE4(aDPM, sys, res)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_a_wormhole(p: dict) -> float:
    """Inline equation: a_wormhole"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = compute_a_wormhole_SOURCE4(sys.r, sys.b, sys.f_worm, res.Evac_neb)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_rho_SCm_2(p: dict) -> float:
    """Inline equation: rho_SCm"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_energy_ratio(p: dict) -> float:
    """Inline equation: energy_ratio"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (rho_SCm * v_SCm * v_SCm) / rho_A
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_Bs(p: dict) -> float:
    """Inline equation: Bs"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_Rs_1(p: dict) -> float:
    """Inline equation: Rs"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_M_25(p: dict) -> float:
    """Inline equation: M"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_R_1(p: dict) -> float:
    """Inline equation: R"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_omega_c_5(p: dict) -> float:
    """Inline equation: omega_c"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 2 * PI_SOURCE4 / (11 * 365.25 * 86400)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_rho_SCm_3(p: dict) -> float:
    """Inline equation: rho_SCm"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_omega_s_3(p: dict) -> float:
    """Inline equation: omega_s"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_omega_c_6(p: dict) -> float:
    """Inline equation: omega_c"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_Bj_3(p: dict) -> float:
    """Inline equation: Bj"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_mu_s_2(p: dict) -> float:
    """Inline equation: mu_s"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_grad_Ms_r_2(p: dict) -> float:
    """Inline equation: grad_Ms_r"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_tn_2(p: dict) -> float:
    """Inline equation: tn"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_QUA_1(p: dict) -> float:
    """Inline equation: QUA"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_M_26(p: dict) -> float:
    """Inline equation: M"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_r_28(p: dict) -> float:
    """Inline equation: r"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_Ereact_4(p: dict) -> float:
    """Inline equation: Ereact"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_S_3(p: dict) -> float:
    """Inline equation: S"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_omega_s_t_4(p: dict) -> float:
    """Inline equation: omega_s_t"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_Pcore_1(p: dict) -> float:
    """Inline equation: Pcore"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_Mbh_1(p: dict) -> float:
    """Inline equation: Mbh"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_dg_1(p: dict) -> float:
    """Inline equation: dg"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_Ugi_1(p: dict) -> float:
    """Inline equation: Ugi"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_rho_sw_1(p: dict) -> float:
    """Inline equation: rho_sw"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_mu_j_5(p: dict) -> float:
    """Inline equation: mu_j"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_rj_1(p: dict) -> float:
    """Inline equation: rj"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_PSCm_1(p: dict) -> float:
    """Inline equation: PSCm"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_mod_1(p: dict) -> float:
    """Inline equation: mod"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = eta * Ts00 * math.math.cos(PI_SOURCE4 * tn)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_sum_Ugi(p: dict) -> float:
    """Inline equation: sum_Ugi"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_sum_Ubi(p: dict) -> float:
    """Inline equation: sum_Ubi"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_Um_1(p: dict) -> float:
    """Inline equation: Um"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_A_scalar_1(p: dict) -> float:
    """Inline equation: A_scalar"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_M_27(p: dict) -> float:
    """Inline equation: M"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_r_29(p: dict) -> float:
    """Inline equation: r"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_t_sys(p: dict) -> float:
    """Inline equation: t_sys"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_B_6(p: dict) -> float:
    """Inline equation: B"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_Bcrit_1(p: dict) -> float:
    """Inline equation: Bcrit"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_rho_fluid_4(p: dict) -> float:
    """Inline equation: rho_fluid"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_Vsys_1(p: dict) -> float:
    """Inline equation: Vsys"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_g_local(p: dict) -> float:
    """Inline equation: g_local"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_M_DM_1(p: dict) -> float:
    """Inline equation: M_DM"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_delta_rho_rho_1(p: dict) -> float:
    """Inline equation: delta_rho_rho"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_I_1(p: dict) -> float:
    """Inline equation: I"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_A_14(p: dict) -> float:
    """Inline equation: A"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_omega1_1(p: dict) -> float:
    """Inline equation: omega1"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_omega2_1(p: dict) -> float:
    """Inline equation: omega2"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_fDPM(p: dict) -> float:
    """Inline equation: fDPM"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_Evac_neb(p: dict) -> float:
    """Inline equation: Evac_neb"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_c_res(p: dict) -> float:
    """Inline equation: c_res"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_Vsys_2(p: dict) -> float:
    """Inline equation: Vsys"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_aDPM_2(p: dict) -> float:
    """Inline equation: aDPM"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_fTHz(p: dict) -> float:
    """Inline equation: fTHz"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_vexp_1(p: dict) -> float:
    """Inline equation: vexp"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_Evac_ISM(p: dict) -> float:
    """Inline equation: Evac_ISM"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_Delta_Evac(p: dict) -> float:
    """Inline equation: Delta_Evac"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_Fsuper(p: dict) -> float:
    """Inline equation: Fsuper"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_UA_SCM(p: dict) -> float:
    """Inline equation: UA_SCM"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_omega_i(p: dict) -> float:
    """Inline equation: omega_i"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_fTRZ(p: dict) -> float:
    """Inline equation: fTRZ"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_k4_res(p: dict) -> float:
    """Inline equation: k4_res"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_freact(p: dict) -> float:
    """Inline equation: freact"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_Ereact_5(p: dict) -> float:
    """Inline equation: Ereact"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1046.0 * math.math.exp(-0.0005 * t)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_fquantum(p: dict) -> float:
    """Inline equation: fquantum"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_fAether(p: dict) -> float:
    """Inline equation: fAether"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_ffluid(p: dict) -> float:
    """Inline equation: ffluid"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_H_z_1(p: dict) -> float:
    """Inline equation: H_z"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_fexp_1(p: dict) -> float:
    """Inline equation: fexp"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 2.0 * PI_SOURCE4 * H_z * t
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_r_30(p: dict) -> float:
    """Inline equation: r"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_b_2(p: dict) -> float:
    """Inline equation: b"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_f_worm(p: dict) -> float:
    """Inline equation: f_worm"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_a_tau_BSM(p: dict) -> float:
    """Inline equation: a_tau_BSM"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_total(p: dict) -> float:
    """Inline equation: total"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = a_tau_SM + a_tau_BSM
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_total_1(p: dict) -> float:
    """Inline equation: total"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = a_tau_SM + params.at("a_tau_BSM")
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_V_cb(p: dict) -> float:
    """Inline equation: V_cb"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_V_cb_1(p: dict) -> float:
    """Inline equation: V_cb"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = params.at("V_cb_override")
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_m_VLQ(p: dict) -> float:
    """Inline equation: m_VLQ"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_mass_frac(p: dict) -> float:
    """Inline equation: mass_frac"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (m_VLQ - m_VLQ_min) / (m_VLQ_max - m_VLQ_min)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_kappa_min(p: dict) -> float:
    """Inline equation: kappa_min"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_kappa_max(p: dict) -> float:
    """Inline equation: kappa_max"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_B_val(p: dict) -> float:
    """Inline equation: B_val"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_Bcrit_val(p: dict) -> float:
    """Inline equation: Bcrit_val"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_a_4(p: dict) -> float:
    """Inline equation: a"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = dt_ns * diff * N * N
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_x_3(p: dict) -> float:
    """Inline equation: x"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = i - dt_ns * N * u[IX_SOURCE4(i, j, N)]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_y(p: dict) -> float:
    """Inline equation: y"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = j - dt_ns * N * v[IX_SOURCE4(i, j, N)]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_s1(p: dict) -> float:
    """Inline equation: s1"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_t1(p: dict) -> float:
    """Inline equation: t1"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_uqff_g_1(p: dict) -> float:
    """Inline equation: uqff_g"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_FU(p: dict) -> float:
    """Inline equation: FU"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = compute_FU_SOURCE4(sun, r_test, 0.0, 0.0, 0.0)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_g_c_sgr(p: dict) -> float:
    """Inline equation: g_c_sgr"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = compute_compressed_MUGE_SOURCE4(sgr1745_SOURCE4)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_g_r_sgr(p: dict) -> float:
    """Inline equation: g_r_sgr"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = compute_resonance_MUGE_SOURCE4(sgr1745_SOURCE4, res_params_default_SOURCE4)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_g_c_saga(p: dict) -> float:
    """Inline equation: g_c_saga"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = compute_compressed_MUGE_SOURCE4(sagA_SOURCE4)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_g_r_saga(p: dict) -> float:
    """Inline equation: g_r_saga"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = compute_resonance_MUGE_SOURCE4(sagA_SOURCE4, res_params_default_SOURCE4)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_g_c_tap(p: dict) -> float:
    """Inline equation: g_c_tap"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = compute_compressed_MUGE_SOURCE4(tapestry_SOURCE4)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_g_r_tap(p: dict) -> float:
    """Inline equation: g_r_tap"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = compute_resonance_MUGE_SOURCE4(tapestry_SOURCE4, res_params_default_SOURCE4)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_g_c_west(p: dict) -> float:
    """Inline equation: g_c_west"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = compute_compressed_MUGE_SOURCE4(westerlund_SOURCE4)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_g_r_west(p: dict) -> float:
    """Inline equation: g_r_west"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = compute_resonance_MUGE_SOURCE4(westerlund_SOURCE4, res_params_default_SOURCE4)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_g_c_pil(p: dict) -> float:
    """Inline equation: g_c_pil"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = compute_compressed_MUGE_SOURCE4(pillars_SOURCE4)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_g_r_pil(p: dict) -> float:
    """Inline equation: g_r_pil"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = compute_resonance_MUGE_SOURCE4(pillars_SOURCE4, res_params_default_SOURCE4)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_g_c_ring(p: dict) -> float:
    """Inline equation: g_c_ring"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = compute_compressed_MUGE_SOURCE4(rings_SOURCE4)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_g_r_ring(p: dict) -> float:
    """Inline equation: g_r_ring"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = compute_resonance_MUGE_SOURCE4(rings_SOURCE4, res_params_default_SOURCE4)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_g_c_guide(p: dict) -> float:
    """Inline equation: g_c_guide"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = compute_compressed_MUGE_SOURCE4(student_guide_SOURCE4)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_g_r_guide(p: dict) -> float:
    """Inline equation: g_r_guide"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = compute_resonance_MUGE_SOURCE4(student_guide_SOURCE4, res_params_default_SOURCE4)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_amplitude_4(p: dict) -> float:
    """Inline equation: amplitude"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_rho_vac_3(p: dict) -> float:
    """Inline equation: rho_vac"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_frequency(p: dict) -> float:
    """Inline equation: frequency"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_coupling_3(p: dict) -> float:
    """Inline equation: coupling"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_M_bh_3(p: dict) -> float:
    """Inline equation: M_bh"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_M_sun(p: dict) -> float:
    """Inline equation: M_sun"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_r_31(p: dict) -> float:
    """Inline equation: r"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_lambda_Q(p: dict) -> float:
    """Inline equation: lambda_Q"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_mass_ratio(p: dict) -> float:
    """Inline equation: mass_ratio"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = pow(M_bh / M_sun, 1.0/3.0)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_M_0(p: dict) -> float:
    """Inline equation: M_0"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_sigma(p: dict) -> float:
    """Inline equation: sigma"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_sigma_0(p: dict) -> float:
    """Inline equation: sigma_0"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_alpha_2(p: dict) -> float:
    """Inline equation: alpha"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_G_1(p: dict) -> float:
    """Inline equation: G"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_M_bulge(p: dict) -> float:
    """Inline equation: M_bulge"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_R_bulge(p: dict) -> float:
    """Inline equation: R_bulge"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_M_bh_4(p: dict) -> float:
    """Inline equation: M_bh"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_r_32(p: dict) -> float:
    """Inline equation: r"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_c_1(p: dict) -> float:
    """Inline equation: c"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_k_8(p: dict) -> float:
    """Inline equation: k"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_E_0_3(p: dict) -> float:
    """Inline equation: E_0"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_tau(p: dict) -> float:
    """Inline equation: tau"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_state_n(p: dict) -> float:
    """Inline equation: state_n"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_shift(p: dict) -> float:
    """Inline equation: shift"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_M_bh_5(p: dict) -> float:
    """Inline equation: M_bh"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_z(p: dict) -> float:
    """Inline equation: z"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_k_B(p: dict) -> float:
    """Inline equation: k_B"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_T(p: dict) -> float:
    """Inline equation: T"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_m_p(p: dict) -> float:
    """Inline equation: m_p"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_B_7(p: dict) -> float:
    """Inline equation: B"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_mu_0(p: dict) -> float:
    """Inline equation: mu_0"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_R_2(p: dict) -> float:
    """Inline equation: R"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_V_bulge(p: dict) -> float:
    """Inline equation: V_bulge"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (4.0 / 3.0) * math.pi * R * R * R
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_v_circ(p: dict) -> float:
    """Inline equation: v_circ"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_t_0(p: dict) -> float:
    """Inline equation: t_0"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_mass_46(p: dict) -> float:
    """Inline equation: mass"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_M_enclosed(p: dict) -> float:
    """Inline equation: M_enclosed"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = M_cluster * (x * x * x) / (1.0 + x) / (1.0 + x)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_temp_1(p: dict) -> float:
    """Inline equation: temp"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_P_ICM(p: dict) -> float:
    """Inline equation: P_ICM"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = n_e * K_BOLTZ * T_K
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_mass_47(p: dict) -> float:
    """Inline equation: mass"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_f_c(p: dict) -> float:
    """Inline equation: f_c"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = math.math.log(1.0 + c_NFW) - c_NFW / (1.0 + c_NFW)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_Phi(p: dict) -> float:
    """Inline equation: Phi"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = -G * M_cluster * math.math.log(1.0 + c_NFW * x) / (x * f_c * R_virial)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_scale_density(p: dict) -> float:
    """Inline equation: scale_density"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_rho_DM(p: dict) -> float:
    """Inline equation: rho_DM"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = rho_s / (x * (1.0 + x) * (1.0 + x))
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_luminosity(p: dict) -> float:
    """Inline equation: luminosity"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_u_jet(p: dict) -> float:
    """Inline equation: u_jet"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = L_jet / (4.0 * math.pi * r * r * v_jet * (Omega_jet / (4.0 * math.pi)))
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_gamma_5(p: dict) -> float:
    """Inline equation: gamma"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1.0 / math.math.sqrt(1.0 - (v_jet * v_jet) / (C_LIGHT * C_LIGHT))
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_mass_48(p: dict) -> float:
    """Inline equation: mass"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_r_tidal(p: dict) -> float:
    """Inline equation: r_tidal"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = r * pow(M_gal / (3.0 * M_enclosed), 1.0 / 3.0)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_a_tidal(p: dict) -> float:
    """Inline equation: a_tidal"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 2.0 * G * M_enclosed * r_tidal / (r * r * r)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_mass_49(p: dict) -> float:
    """Inline equation: mass"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_sigma_virial(p: dict) -> float:
    """Inline equation: sigma_virial"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = math.math.sqrt(G * M_virial / (3.0 * R_virial))
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_virial_ratio(p: dict) -> float:
    """Inline equation: virial_ratio"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = sigma_v / sigma_virial
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_ne_central(p: dict) -> float:
    """Inline equation: ne_central"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_Lambda_T(p: dict) -> float:
    """Inline equation: Lambda_T"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 3e-23 * math.math.sqrt(T_ICM)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_epsilon_X(p: dict) -> float:
    """Inline equation: epsilon_X"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = n_e * n_e * Lambda_T
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_sigma_central(p: dict) -> float:
    """Inline equation: sigma_central"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_A_res_2(p: dict) -> float:
    """Inline equation: A_res"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = pow(A, 1.0 / 3.0)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_F_res(p: dict) -> float:
    """Inline equation: F_res"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_M_28(p: dict) -> float:
    """Inline equation: M"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_r_33(p: dict) -> float:
    """Inline equation: r"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_M_29(p: dict) -> float:
    """Inline equation: M"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_r_34(p: dict) -> float:
    """Inline equation: r"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_cnb(p: dict) -> float:
    """Inline equation: cnb"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = std.real(vars["CNB_coupling"])
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_G_2(p: dict) -> float:
    """Inline equation: G"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = std.real(vars["G"])
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_triadic(p: dict) -> float:
    """Inline equation: triadic"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = std.real(vars["triadic_scale"])
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_comp(p: dict) -> float:
    """Inline equation: comp"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = std.real(vars["compression_factor"])
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_k_9(p: dict) -> float:
    """Inline equation: k"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = std.real(vars["K_R"])
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_rho_ua_3(p: dict) -> float:
    """Inline equation: rho_ua"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = std.real(vars["RHO_VAC_UA"])
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_ratio(p: dict) -> float:
    """Inline equation: ratio"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = omega0 / constants["OMEGA_LENR"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_dpm_res(p: dict) -> float:
    """Inline equation: dpm_res"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = DPM_resonance(B0, omega0)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_cos_theta(p: dict) -> float:
    """Inline equation: cos_theta"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = math.math.cos(constants["math.pi"] / 4.0)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_ratio_1(p: dict) -> float:
    """Inline equation: ratio"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = constants["ECM_ASTRO"] / (constants["ECM"] / 1.602e-19 / 1e9)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_momentum_term(p: dict) -> float:
    """Inline equation: momentum_term"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (constants["ME"] * constants["C"] * constants["C"] / (p.r * p.r)) * dpm_vars["momentum"] * cos_theta
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_gravity_term(p: dict) -> float:
    """Inline equation: gravity_term"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (constants["G"] * p.M / (p.r * p.r)) * dpm_vars["gravity"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_vac_term(p: dict) -> float:
    """Inline equation: vac_term"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = constants["RHO_VAC_UA"] * dpm_vars["stability"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_f_lenr(p: dict) -> float:
    """Inline equation: f_lenr"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = calculate_F_LENR(p.omega0)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_f_act(p: dict) -> float:
    """Inline equation: f_act"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = calculate_F_act(p.t)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_f_de(p: dict) -> float:
    """Inline equation: f_de"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = calculate_F_DE(p.L_X)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_f_res_2(p: dict) -> float:
    """Inline equation: f_res"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = calculate_F_res(p.B0, p.omega0, constants["V"])
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_f_neutron(p: dict) -> float:
    """Inline equation: f_neutron"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = calculate_F_neutron()
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_integrand_12(p: dict) -> float:
    """Inline equation: integrand"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = calculate_integrand(p)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_a_5(p: dict) -> float:
    """Inline equation: a"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_b_3(p: dict) -> float:
    """Inline equation: b"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 2.51e-5 + calculate_F_neutron() / (p.r * p.r) + constants["PHASE"] + constants["PHASE"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_c_2(p: dict) -> float:
    """Inline equation: c"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = -3.06e175 + 1e-29 / (p.r * p.r) + constants["CURVATURE"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_f_bi_i(p: dict) -> float:
    """Inline equation: f_bi_i"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = calculate_F_U_Bi_i(p)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_omega_7(p: dict) -> float:
    """Inline equation: omega"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 2 * constants["math.pi"] / params.rotation_period
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_mod_2(p: dict) -> float:
    """Inline equation: mod"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = math.math.cos(vars.phi) * (1.0 + math.math.sin(constants["math.pi"] * vars.nu_THz / constants["NU_THz"]))
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_k1_2(p: dict) -> float:
    """Inline equation: k1"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_geom(p: dict) -> float:
    """Inline equation: geom"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_mod_3(p: dict) -> float:
    """Inline equation: mod"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = math.math.cos(vars.phi) * (1.0 + math.math.sin(PI_S114 * vars.nu_THz / NU_THz_S114))
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_t_2(p: dict) -> float:
    """Inline equation: t"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_k1_3(p: dict) -> float:
    """Inline equation: k1"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_t_global(p: dict) -> float:
    """Inline equation: t_global"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_defaultValue_2(p: dict) -> float:
    """Inline equation: defaultValue"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_t_global_1(p: dict) -> float:
    """Inline equation: t_global"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_k1_4(p: dict) -> float:
    """Inline equation: k1"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_e_dpm_i(p: dict) -> float:
    """Inline equation: e_dpm_i"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = k1_ * vars.Q_i[i - 1] * vars.f_UA_prime[i - 1].real() * vars.f_SCm[i - 1].real() * math.math.sin(vars.theta_i[i - 1])
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_r_i_sq(p: dict) -> float:
    """Inline equation: r_i_sq"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = vars.r_i[i - 1] * vars.r_i[i - 1]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_mod_i(p: dict) -> float:
    """Inline equation: mod_i"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = vars.f_TRZ_i[i - 1] * vars.f_Um_i[i - 1]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_g_base_2(p: dict) -> float:
    """Inline equation: g_base"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = calculate_gravity_compressed(vars, params)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_omega_i_val(p: dict) -> float:
    """Inline equation: omega_i_val"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = H_Z_BASE_S115 * static_cast(i)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_g_i(p: dict) -> float:
    """Inline equation: g_i"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (vars.Q_i[i - 1] / static_cast(NUM_STATES_S115)) * g_base
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_cos_term_2(p: dict) -> float:
    """Inline equation: cos_term"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = math.math.cos(omega_i_val * t)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_r_ug_i(p: dict) -> float:
    """Inline equation: r_ug_i"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = g_i * M_SF_S115 * cos_term * vars.f_Ub.real()
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_g_3(p: dict) -> float:
    """Inline equation: g"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = calculate_gravity_compressed(vars, params)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_r_35(p: dict) -> float:
    """Inline equation: r"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = calculate_resonance(vars, params, t)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_t_3(p: dict) -> float:
    """Inline equation: t"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = sys.get_params().t_age
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_t_4(p: dict) -> float:
    """Inline equation: t"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_vacuum_density_1(p: dict) -> float:
    """Inline equation: vacuum_density"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_defaultValue_3(p: dict) -> float:
    """Inline equation: defaultValue"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_seed_orbit(p: dict) -> float:
    """Inline equation: seed_orbit"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_poly(p: dict) -> float:
    """Inline equation: poly"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = evaluate26DPoly_S116(pi_patterns, 1.0 / (r * r))
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_defaultValue_4(p: dict) -> float:
    """Inline equation: defaultValue"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_I_UNIT(p: dict) -> float:
    """Inline equation: I_UNIT"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = std.complex(0.0, 1.0)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_a_6(p: dict) -> float:
    """Inline equation: a"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = std.stod(bounds.substr(0, comma))
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_b_4(p: dict) -> float:
    """Inline equation: b"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = std.stod(bounds.substr(comma + 1))
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_val_1(p: dict) -> float:
    """Inline equation: val"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = stod(line.substr(eq_pos + 1))
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_random_scale(p: dict) -> float:
    """Inline equation: random_scale"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = dis(rng) * scaling_factors["resonance"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_term2_5(p: dict) -> float:
    """Inline equation: term2"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = scaling_factors["LENR"] * activation_term * math.exp(-t / 1e6)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_term3_1(p: dict) -> float:
    """Inline equation: term3"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = DE_term + resonance_term * neutron_factor
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_term4_1(p: dict) -> float:
    """Inline equation: term4"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = rel_term * (1 + f_TRZ)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_result_2(p: dict) -> float:
    """Inline equation: result"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = term1 + term2 + term3 + term4
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_quantum_term(p: dict) -> float:
    """Inline equation: quantum_term"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (hbar / math.sqrt(delta_x * delta_p)) * integral_psi * (2 * math.pi / t_Hubble)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_result_3(p: dict) -> float:
    """Inline equation: result"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = sum_Ug + Lambda_term + quantum_term
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_h_omega0(p: dict) -> float:
    """Inline equation: h_omega0"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = h_planck * omega_0_base
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_amp_3(p: dict) -> float:
    """Inline equation: amp"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_strength_2(p: dict) -> float:
    """Inline equation: strength"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_amp_4(p: dict) -> float:
    """Inline equation: amp"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_strength_3(p: dict) -> float:
    """Inline equation: strength"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_amp_5(p: dict) -> float:
    """Inline equation: amp"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_strength_4(p: dict) -> float:
    """Inline equation: strength"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_t_day_3(p: dict) -> float:
    """Inline equation: t_day"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_num(p: dict) -> float:
    """Inline equation: num"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_value_1(p: dict) -> float:
    """Inline equation: value"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = std.stod(matches[1])
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_converted(p: dict) -> float:
    """Inline equation: converted"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_guess(p: dict) -> float:
    """Inline equation: guess"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = min_b + (max_b - min_b) * (i / 9.0)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_f_val(p: dict) -> float:
    """Inline equation: f_val"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_x_4(p: dict) -> float:
    """Inline equation: x"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = min_x + i * (max_x - min_x) / 99.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_x0(p: dict) -> float:
    """Inline equation: x0"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = params["x0"], v0 = params["v0"], a = params["a"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_dt(p: dict) -> float:
    """Inline equation: dt"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_procVal(p: dict) -> float:
    """Inline equation: procVal"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_out(p: dict) -> float:
    """Inline equation: out"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_t_5(p: dict) -> float:
    """Inline equation: t"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_term3_2(p: dict) -> float:
    """Inline equation: term3"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (Lambda * c_light * c_light) / 3.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_term5(p: dict) -> float:
    """Inline equation: term5"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = gw_prefactor * (dOdt * dOdt)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_term_q(p: dict) -> float:
    """Inline equation: term_q"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (hbar / sqrt_unc) * integral_psi * (2 * math.pi / t_Hubble)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_term_fluid(p: dict) -> float:
    """Inline equation: term_fluid"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (rho_fluid * V * ug1_base) / M
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_term_osc(p: dict) -> float:
    """Inline equation: term_osc"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = term_osc1 + term_osc2
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_term_DM(p: dict) -> float:
    """Inline equation: term_DM"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = term_dm_force_like / M
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_cum_D_1(p: dict) -> float:
    """Inline equation: cum_D"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = compute_cumulative_D(t)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_t_example(p: dict) -> float:
    """Inline equation: t_example"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1.0 * 365.25 * 24 * 3600
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_amp_6(p: dict) -> float:
    """Inline equation: amp"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_strength_5(p: dict) -> float:
    """Inline equation: strength"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_term4_2(p: dict) -> float:
    """Inline equation: term4"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (em_base * corr_UA) * scale_EM
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_em_base_4(p: dict) -> float:
    """Inline equation: em_base"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = q_charge * cross_vB / 1.673e-27
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_term_fluid_1(p: dict) -> float:
    """Inline equation: term_fluid"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (rho_fluid * V * ug1_t) / Mt
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_term_DM_1(p: dict) -> float:
    """Inline equation: term_DM"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = term_dm_force_like / Mt
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_amp_7(p: dict) -> float:
    """Inline equation: amp"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_strength_6(p: dict) -> float:
    """Inline equation: strength"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_M_dot_2(p: dict) -> float:
    """Inline equation: M_dot"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = M_dot_factor * math.exp(-t / tau_SF)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_wind_pressure_1(p: dict) -> float:
    """Inline equation: wind_pressure"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = rho_wind * v_wind * v_wind
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_term_wind(p: dict) -> float:
    """Inline equation: term_wind"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = wind_pressure / rho_fluid
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_k1_5(p: dict) -> float:
    """Inline equation: k1"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_geom_factor_1(p: dict) -> float:
    """Inline equation: geom_factor"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_exp_barrier_1(p: dict) -> float:
    """Inline equation: exp_barrier"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = math.math.exp(-v.R_EB / v.r)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_term_1(p: dict) -> float:
    """Inline equation: term"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = k1_ * v.f_UA_prime * v.f_SCm * v.R_EB * f_nu * geom_factor * exp_barrier / (v.r * v.r)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_term1_3(p: dict) -> float:
    """Inline equation: term1"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = ki_ * vars.f_UA_prime * vars.nu_THz * vars.R_EB
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_term2_6(p: dict) -> float:
    """Inline equation: term2"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = km_ * vars.f_SCm * vars.nu_res
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_term3_3(p: dict) -> float:
    """Inline equation: term3"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = ke_ * (vars.f_UA_prime * vars.f_SCm) * vars.nu_THz
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_combined(p: dict) -> float:
    """Inline equation: combined"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = term1 + term2 + term3
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_geom_factor_2(p: dict) -> float:
    """Inline equation: geom_factor"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = math.math.sin(vars.theta) * math.math.cos(vars.phi) * f_nu_THz(vars.nu_THz)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_rho_vac_SCm_2(p: dict) -> float:
    """Inline equation: rho_vac_SCm"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_heaviside_term(p: dict) -> float:
    """Inline equation: heaviside_term"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1.0 + 1e13 * heaviside(t - 1.0)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_rho_vac_UA_3(p: dict) -> float:
    """Inline equation: rho_vac_UA"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_rho_vac_UA_4(p: dict) -> float:
    """Inline equation: rho_vac_UA"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_field_term(p: dict) -> float:
    """Inline equation: field_term"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = U_m_val / rho_vac_UA
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_M_30(p: dict) -> float:
    """Inline equation: M"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_F_ug1(p: dict) -> float:
    """Inline equation: F_ug1"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = core.calculate_U_g1(vars, SPHERICAL)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_F_ug3(p: dict) -> float:
    """Inline equation: F_ug3"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = core.calculate_U_g3(avg_var) * core.calculate_f_Ub(f_Ub_scale_)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_k1_6(p: dict) -> float:
    """Inline equation: k1"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_k1_7(p: dict) -> float:
    """Inline equation: k1"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_t_global_2(p: dict) -> float:
    """Inline equation: t_global"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_defaultValue_5(p: dict) -> float:
    """Inline equation: defaultValue"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_t_global_3(p: dict) -> float:
    """Inline equation: t_global"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_t_6(p: dict) -> float:
    """Inline equation: t"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_vacuum_density_2(p: dict) -> float:
    """Inline equation: vacuum_density"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_defaultValue_6(p: dict) -> float:
    """Inline equation: defaultValue"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_defaultValue_7(p: dict) -> float:
    """Inline equation: defaultValue"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_term1_4(p: dict) -> float:
    """Inline equation: term1"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = ug1_t * corr_H * corr_B * corr_E
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_Hz_kms(p: dict) -> float:
    """Inline equation: Hz_kms"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 70 * math.sqrt(0.3 * pow(1 + z_lens, 3) + 0.7)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_term1_5(p: dict) -> float:
    """Inline equation: term1"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = ug1_base * corr_H * corr_B * corr_L
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_Hz_kms_1(p: dict) -> float:
    """Inline equation: Hz_kms"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 70 * math.sqrt(0.3 * pow(1 + z_gal, 3) + 0.7)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_term_SN(p: dict) -> float:
    """Inline equation: term_SN"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = -(G * MSNt) / (r * r)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_term1_6(p: dict) -> float:
    """Inline equation: term1"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = ug1_base * corr_H * corr_B * corr_E
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_M_dot_3(p: dict) -> float:
    """Inline equation: M_dot"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = SFR_factor * math.exp(-t / tau_SF)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_term1_7(p: dict) -> float:
    """Inline equation: term1"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = ug1_t * corr_H * corr_B * corr_I
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_term_feedback(p: dict) -> float:
    """Inline equation: term_feedback"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = wind_pressure / rho_fluid
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_term1_8(p: dict) -> float:
    """Inline equation: term1"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = ug1_base * corr_H * corr_B * corr_F
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_cool_pressure(p: dict) -> float:
    """Inline equation: cool_pressure"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = rho_cool * v_cool * v_cool
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_term_cool(p: dict) -> float:
    """Inline equation: term_cool"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = cool_pressure / rho_fluid
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_Hz_kms_2(p: dict) -> float:
    """Inline equation: Hz_kms"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 70 * math.sqrt(0.3 * pow(1 + z_avg, 3) + 0.7)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_amp_8(p: dict) -> float:
    """Inline equation: amp"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_strength_7(p: dict) -> float:
    """Inline equation: strength"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_M_31(p: dict) -> float:
    """Inline equation: M"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_r_36(p: dict) -> float:
    """Inline equation: r"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_tolerance(p: dict) -> float:
    """Inline equation: tolerance"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_currentValue(p: dict) -> float:
    """Inline equation: currentValue"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getVariable(observable)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_error_2(p: dict) -> float:
    """Inline equation: error"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = targetValue - currentValue
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_gradient(p: dict) -> float:
    """Inline equation: gradient"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = computeGradient(param, observable)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_adjustment_1(p: dict) -> float:
    """Inline equation: adjustment"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = learningRate * error / (gradient + 1e-10)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_originalObservable(p: dict) -> float:
    """Inline equation: originalObservable"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getVariable(observable)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_perturbedObservable(p: dict) -> float:
    """Inline equation: perturbedObservable"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = getVariable(observable)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_feedbackParameter(p: dict) -> float:
    """Inline equation: feedbackParameter"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_evolution_factor(p: dict) -> float:
    """Inline equation: evolution_factor"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = math.math.exp(-dt / evolution_timescale)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_scaleFactor(p: dict) -> float:
    """Inline equation: scaleFactor"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = obsValue / (currentValue + 1e-30)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_uqff_g_2(p: dict) -> float:
    """Inline equation: uqff_g"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_mag(p: dict) -> float:
    """Inline equation: mag"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = math.math.sqrt(u[IX(i, j)] * u[IX(i, j)] + v[IX(i, j)] * v[IX(i, j)])
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_rho_0_5(p: dict) -> float:
    """Inline equation: rho_0"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = M_halo / (4.0 * 3.14159265359 * r_scale * r_scale * r_scale * (math.math.log(2.0) - 0.5))
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_default_val(p: dict) -> float:
    """Inline equation: default_val"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_original(p: dict) -> float:
    """Inline equation: original"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = compute_Ug1(body, r, t, tn, alpha, delta_def, k1)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_dynamic(p: dict) -> float:
    """Inline equation: dynamic"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = computeDynamicContributions(params)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_original_1(p: dict) -> float:
    """Inline equation: original"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = compute_Ug2(body, r, t, tn, k2, QA, delta_sw, v_sw, HSCm, rho_A, kappa)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_original_2(p: dict) -> float:
    """Inline equation: original"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = compute_Ug3(body, r, t, tn, theta, rho_A, kappa, k3)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_original_3(p: dict) -> float:
    """Inline equation: original"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_df_val(p: dict) -> float:
    """Inline equation: df_val"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_val_2(p: dict) -> float:
    """Inline equation: val"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = std.stod(line.substr(eq_pos + 1))
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_sqrt_ratio(p: dict) -> float:
    """Inline equation: sqrt_ratio"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = math.sqrt(p.rho_astro / p.rho_LEP)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_ratio2(p: dict) -> float:
    """Inline equation: ratio2"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = F_U_Bi / belly_button
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_E_DPM_i_1(p: dict) -> float:
    """Inline equation: E_DPM_i"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (h_bar * c / r_i_sq) * Q_i * SCm_i
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_Ug1_i_1(p: dict) -> float:
    """Inline equation: Ug1_i"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (E_DPM_i / r_i_sq) * p.rho_vac_UA * f_TRZ_i
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_Ug2_i_1(p: dict) -> float:
    """Inline equation: Ug2_i"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (E_DPM_i / r_i_sq) * SCm_i * f_Um_i
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_f_i_1(p: dict) -> float:
    """Inline equation: f_i"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = p.omega0 / (2 * math.pi)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_cos_term_3(p: dict) -> float:
    """Inline equation: cos_term"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = math.cos(2 * math.pi * f_i * p.t)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_Ug3_i_1(p: dict) -> float:
    """Inline equation: Ug3_i"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (h_bar * p.omega0 / 2.0) * Q_i * cos_term / r_i
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_Ug4i_i(p: dict) -> float:
    """Inline equation: Ug4i_i"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (G * M_i / r_i_sq) * (1.0 + p.alpha_i) * SCm_i
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_layer_g(p: dict) -> float:
    """Inline equation: layer_g"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = Ug1_i + Ug2_i + Ug3_i + Ug4i_i
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_piPhase(p: dict) -> float:
    """Inline equation: piPhase"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (time * PI_FREQ) - (2 * math.pi)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_scaleFactor_1(p: dict) -> float:
    """Inline equation: scaleFactor"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1 + 0.1 * math.sin(piPhase)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_orbitSpeed(p: dict) -> float:
    """Inline equation: orbitSpeed"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = BIO_QUANTUM_FREQ / 1000.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_negativeTimeEffect(p: dict) -> float:
    """Inline equation: negativeTimeEffect"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_freq_2(p: dict) -> float:
    """Inline equation: freq"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_num_plasmoids(p: dict) -> float:
    """Inline equation: num_plasmoids"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_brightness(p: dict) -> float:
    """Inline equation: brightness"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = math.sin(time * math.pi / (END_TIME - START_TIME))
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_M_s_2(p: dict) -> float:
    """Inline equation: M_s"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_Ug(p: dict) -> float:
    """Inline equation: Ug"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = dpm_ug1_seed(M_s, r_max)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_Um_2(p: dict) -> float:
    """Inline equation: Um"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (mu_0 * mu_s * omega_s) / (4 * math.pi * r_max * r_max)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_Ui(p: dict) -> float:
    """Inline equation: Ui"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = Q_A / (4 * math.pi * epsilon_0 * R_b * R_b)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_Ua(p: dict) -> float:
    """Inline equation: Ua"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (Omega_g * M_bh) / d_g
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_num_plasmoids_1(p: dict) -> float:
    """Inline equation: num_plasmoids"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_mu_over_rj_1(p: dict) -> float:
    """Inline equation: mu_over_rj"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["mu_j"] / variables["r_j"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_exp_arg_3(p: dict) -> float:
    """Inline equation: exp_arg"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = -variables["gamma"] * t * math.math.cos(variables["pi"] * variables["t_n"])
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_phi_hat_2(p: dict) -> float:
    """Inline equation: phi_hat"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["phi_hat_j"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_base_7(p: dict) -> float:
    """Inline equation: base"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = computeUmBase(j, t)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_heaviside_f(p: dict) -> float:
    """Inline equation: heaviside_f"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = computeHeavisideFactor()
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_quasi_f(p: dict) -> float:
    """Inline equation: quasi_f"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1.0 + variables["f_quasi"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_orig_f(p: dict) -> float:
    """Inline equation: orig_f"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["f_Heaviside"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_result_4(p: dict) -> float:
    """Inline equation: result"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = computeUmContribution(j, t)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_um_with(p: dict) -> float:
    """Inline equation: um_with"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = computeUmContribution(j, t)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_um_without(p: dict) -> float:
    """Inline equation: um_without"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = computeUmWithNoHeaviside(j, t)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_amplification(p: dict) -> float:
    """Inline equation: amplification"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = um_with / um_without
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_heav_factor(p: dict) -> float:
    """Inline equation: heav_factor"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = mod.computeHeavisideFactor()
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_u_g2(p: dict) -> float:
    """Inline equation: u_g2"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = mod.computeU_g2(0.0, 0.0)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_sum_1(p: dict) -> float:
    """Inline equation: sum"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = mod.computeSumKUgi()
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_e_react_1(p: dict) -> float:
    """Inline equation: e_react"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["E_react"] * math.math.exp( - variables["alpha_decay"] * t )
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_sum_2(p: dict) -> float:
    """Inline equation: sum"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = mod.computeSumInertiaTerms(0.0)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_sin_term(p: dict) -> float:
    """Inline equation: sin_term"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = math.math.sin(variables["omega_c"] * t)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_b_j(p: dict) -> float:
    """Inline equation: b_j"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["B_j"] + 0.4 * sin_term
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_heaviside_f_1(p: dict) -> float:
    """Inline equation: heaviside_f"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1.0 + variables["scale_Heaviside"] * variables["f_Heaviside"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_cos_term_4(p: dict) -> float:
    """Inline equation: cos_term"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = math.math.cos(variables["omega_c"] * t * variables["pi"])
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_e_react_2(p: dict) -> float:
    """Inline equation: e_react"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["E_react"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_um(p: dict) -> float:
    """Inline equation: um"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = computeUmContrib(j, t)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_ug3(p: dict) -> float:
    """Inline equation: ug3"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = computeUg3Contrib(t)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_beta_1_1(p: dict) -> float:
    """Inline equation: beta_1"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["beta_1"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_Omega_g_1(p: dict) -> float:
    """Inline equation: Omega_g"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["Omega_g"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_swirl_factor_1(p: dict) -> float:
    """Inline equation: swirl_factor"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1.0 + variables["epsilon_sw"] * variables["rho_vac_sw"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_rho_vac_SCm_3(p: dict) -> float:
    """Inline equation: rho_vac_SCm"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["rho_vac_SCm"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_exp_term_1(p: dict) -> float:
    """Inline equation: exp_term"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = math.math.exp(-variables["alpha"] * variables["t_n"])
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_feedback_factor(p: dict) -> float:
    """Inline equation: feedback_factor"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1.0 + variables["f_feedback"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_m_sun(p: dict) -> float:
    """Inline equation: m_sun"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = mod.computeM_bhInMsun()
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_arg_2(p: dict) -> float:
    """Inline equation: arg"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = -gamma * t * cos_pi_tn
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_one_minus_exp_1(p: dict) -> float:
    """Inline equation: one_minus_exp"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = computeOneMinusExp(gamma, t)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_heaviside_f_2(p: dict) -> float:
    """Inline equation: heaviside_f"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["heaviside_f"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_quasi_f_1(p: dict) -> float:
    """Inline equation: quasi_f"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["quasi_f"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_exp_pos(p: dict) -> float:
    """Inline equation: exp_pos"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = computeExpTerm(gamma, t)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_one_minus_pos(p: dict) -> float:
    """Inline equation: one_minus_pos"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = computeOneMinusExp(gamma, t)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_um_pos(p: dict) -> float:
    """Inline equation: um_pos"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = computeUmExample(t)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_exp_neg(p: dict) -> float:
    """Inline equation: exp_neg"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = computeExpTerm(gamma, t)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_one_minus_neg(p: dict) -> float:
    """Inline equation: one_minus_neg"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = computeOneMinusExp(gamma, t)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_um_neg(p: dict) -> float:
    """Inline equation: um_neg"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = computeUmExample(t)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_sin_omega(p: dict) -> float:
    """Inline equation: sin_omega"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = computeSinOmegaCT(t)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_b_j_1(p: dict) -> float:
    """Inline equation: b_j"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["B_j"] + 0.4 * sin_omega
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_mu(p: dict) -> float:
    """Inline equation: mu"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = mod.computeMuJExample(0.0)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_cos_term_5(p: dict) -> float:
    """Inline equation: cos_term"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = math.math.cos(variables["omega_s"] * t * variables["pi"])
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_orig_p(p: dict) -> float:
    """Inline equation: orig_p"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["P_core"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_p(p: dict) -> float:
    """Inline equation: p"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = mod.computeP_core()
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_u_g3_sun(p: dict) -> float:
    """Inline equation: u_g3_sun"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = mod.computeU_g3(0.0)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_quasi_f_2(p: dict) -> float:
    """Inline equation: quasi_f"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = computeQuasiFactor()
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_heaviside_f_3(p: dict) -> float:
    """Inline equation: heaviside_f"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["heaviside_factor"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_orig_f_1(p: dict) -> float:
    """Inline equation: orig_f"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["f_quasi"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_um_without_1(p: dict) -> float:
    """Inline equation: um_without"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = computeUmWithNoQuasi(j, t)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_percent_increase(p: dict) -> float:
    """Inline equation: percent_increase"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = ((um_with - um_without) / um_without) * 100.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_quasi_f_3(p: dict) -> float:
    """Inline equation: quasi_f"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = mod.computeQuasiFactor()
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_SCm_contrib_3(p: dict) -> float:
    """Inline equation: SCm_contrib"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_SCm_contrib_4(p: dict) -> float:
    """Inline equation: SCm_contrib"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_SCm_contrib_5(p: dict) -> float:
    """Inline equation: SCm_contrib"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_mu_s_3(p: dict) -> float:
    """Inline equation: mu_s"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = compute_mu_s(t, body.Bs_avg, body.omega_c, body.Rs)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_grad_Ms_r_3(p: dict) -> float:
    """Inline equation: grad_Ms_r"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = compute_grad_Ms_r(body.Ms, body.Rs)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_Ereact_6(p: dict) -> float:
    """Inline equation: Ereact"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = compute_Ereact(t, body.SCm_density, v_SCm, rho_A, kappa)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_S_4(p: dict) -> float:
    """Inline equation: S"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = step_function(r, body.Rb)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_omega_s_t_5(p: dict) -> float:
    """Inline equation: omega_s_t"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = compute_omega_s_t(t, body.omega_s, body.omega_c)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_Bj_4(p: dict) -> float:
    """Inline equation: Bj"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = compute_Bj(t, body.omega_c)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_mu_j_6(p: dict) -> float:
    """Inline equation: mu_j"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = compute_mu_j(t, body.omega_c, body.Rs)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_decay_5(p: dict) -> float:
    """Inline equation: decay"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1.0 - math.math.exp(-gamma * t * math.math.cos(math.pi * tn))
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_mod_4(p: dict) -> float:
    """Inline equation: mod"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = eta * Ts00 * math.math.cos(math.pi * tn)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_Ug1_6(p: dict) -> float:
    """Inline equation: Ug1"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = compute_Ug1(body, r, t, tn, alpha, delta_def, k1)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_Ug2_3(p: dict) -> float:
    """Inline equation: Ug2"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = compute_Ug2(body, r, t, tn, k2, QA, delta_sw, v_sw, HSCm, rho_A, kappa)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_Ug3_2(p: dict) -> float:
    """Inline equation: Ug3"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = compute_Ug3(body, r, t, tn, theta, rho_A, kappa, k3)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_Ug4_3(p: dict) -> float:
    """Inline equation: Ug4"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = compute_Ug4(t, tn, rho_v, C_concentration, Mbh, dg, alpha, f_feedback, k4)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_sum_Ugi_1(p: dict) -> float:
    """Inline equation: sum_Ugi"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = Ug1 + Ug2 + Ug3 + Ug4
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_Ubi1(p: dict) -> float:
    """Inline equation: Ubi1"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = compute_Ubi(Ug1, beta_i, Omega_g, Mbh, dg, epsilon_sw, rho_sw, UUA, tn)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_Ubi2(p: dict) -> float:
    """Inline equation: Ubi2"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = compute_Ubi(Ug2, beta_i, Omega_g, Mbh, dg, epsilon_sw, rho_sw, UUA, tn)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_Ubi3(p: dict) -> float:
    """Inline equation: Ubi3"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = compute_Ubi(Ug3, beta_i, Omega_g, Mbh, dg, epsilon_sw, rho_sw, UUA, tn)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_Ubi4(p: dict) -> float:
    """Inline equation: Ubi4"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = compute_Ubi(Ug4, beta_i, Omega_g, Mbh, dg, epsilon_sw, rho_sw, UUA, tn)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_sum_Ubi_1(p: dict) -> float:
    """Inline equation: sum_Ubi"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = Ubi1 + Ubi2 + Ubi3 + Ubi4
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_Um_3(p: dict) -> float:
    """Inline equation: Um"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = compute_Um(body, t, tn, body.Rb, gamma, rho_A, kappa, num_strings)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_A_scalar_2(p: dict) -> float:
    """Inline equation: A_scalar"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = A[0][0] + A[1][1] + A[2][2] + A[3][3]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_expected(p: dict) -> float:
    """Inline equation: expected"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = Physics.G * test_sys.M / (test_sys.r * test_sys.r)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_result_5(p: dict) -> float:
    """Inline equation: result"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = MUGE.compute_compressed_base(test_sys)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_u_g2_1(p: dict) -> float:
    """Inline equation: u_g2"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = mod.computeU_g2(1.5e13)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_cos_pi_tn(p: dict) -> float:
    """Inline equation: cos_pi_tn"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = computeCosPiTn(t_n)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_arg_3(p: dict) -> float:
    """Inline equation: arg"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = -computeGamma_day() * t_day * cos_pi_tn
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_one_minus_exp_2(p: dict) -> float:
    """Inline equation: one_minus_exp"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = computeOneMinusExp(t_day, t_n)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_cos_pi(p: dict) -> float:
    """Inline equation: cos_pi"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = computeCosPiTn(t_n)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_exp_val(p: dict) -> float:
    """Inline equation: exp_val"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = computeExpTerm(t_day, t_n)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_one_minus(p: dict) -> float:
    """Inline equation: one_minus"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = computeOneMinusExp(t_day, t_n)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_um_ex(p: dict) -> float:
    """Inline equation: um_ex"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = computeUmExample(t_day, t_n)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_gamma_6(p: dict) -> float:
    """Inline equation: gamma"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = mod.computeGamma_day()
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_result_6(p: dict) -> float:
    """Inline equation: result"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = computeUmContribution(t)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_um_sun(p: dict) -> float:
    """Inline equation: um_sun"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = mod.computeUmContribution(0.0)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_arg_4(p: dict) -> float:
    """Inline equation: arg"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = -computeKappa_day() * t_day
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_e_react_3(p: dict) -> float:
    """Inline equation: e_react"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = computeE_react(t_day)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_one_minus_exp_3(p: dict) -> float:
    """Inline equation: one_minus_exp"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["one_minus_exp"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_um_ex_1(p: dict) -> float:
    """Inline equation: um_ex"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = computeUmExample(t_day)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_fraction(p: dict) -> float:
    """Inline equation: fraction"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = e_react / variables["E_react_base"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_kappa_1(p: dict) -> float:
    """Inline equation: kappa"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = mod.computeKappa_day()
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_omega_8(p: dict) -> float:
    """Inline equation: omega"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = mod.computeOmega_c()
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_s_step(p: dict) -> float:
    """Inline equation: s_step"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_mod_factor(p: dict) -> float:
    """Inline equation: mod_factor"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = computeModulationFactor()
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_orig_delta(p: dict) -> float:
    """Inline equation: orig_delta"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["delta_sw"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_mod_f(p: dict) -> float:
    """Inline equation: mod_f"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = mod.computeModulationFactor()
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_u_g2_2(p: dict) -> float:
    """Inline equation: u_g2"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = mod.computeU_g2(1.496e13)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_m_over_r2(p: dict) -> float:
    """Inline equation: m_over_r2"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = computeM_sOverR2(r)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_m_sun_1(p: dict) -> float:
    """Inline equation: m_sun"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = mod.computeM_sInMsun()
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_period_s_1(p: dict) -> float:
    """Inline equation: period_s"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 2.0 * math.pi / computeOmega_s()
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_p_core(p: dict) -> float:
    """Inline equation: p_core"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["P_core"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_lambda_i_1(p: dict) -> float:
    """Inline equation: lambda_i"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["lambda_i"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_omega_s_t_6(p: dict) -> float:
    """Inline equation: omega_s_t"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = computeOmega_s_t(t)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_cos_pi_tn_1(p: dict) -> float:
    """Inline equation: cos_pi_tn"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = math.math.cos(variables["pi"] * t_n)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_omega_9(p: dict) -> float:
    """Inline equation: omega"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = mod.computeOmega_s()
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_u_g3(p: dict) -> float:
    """Inline equation: u_g3"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = mod.computeU_g3(0.0)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_s(p: dict) -> float:
    """Inline equation: s"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = mod.computeS_r_Rb(1.5e13)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_u_g2_3(p: dict) -> float:
    """Inline equation: u_g2"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = mod.computeU_g2(1e11)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_pert(p: dict) -> float:
    """Inline equation: pert"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = computePerturbation()
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_base_b(p: dict) -> float:
    """Inline equation: base_b"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["B_ref"] + 0.4 * math.math.sin(variables["omega_s"] * t)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_b_min(p: dict) -> float:
    """Inline equation: b_min"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = mod.computeB_s_min()
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_u_g3_1(p: dict) -> float:
    """Inline equation: u_g3"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = mod.computeU_g3_example(0.0, 1e-4)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_b_j_2(p: dict) -> float:
    """Inline equation: b_j"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = computeB_j_hypothetical(t, T_s)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_u_g3_2(p: dict) -> float:
    """Inline equation: u_g3"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = mod.computeU_g3_example(0.0, 10000.0)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_rho_product(p: dict) -> float:
    """Inline equation: rho_product"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["rho_product"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_base_8(p: dict) -> float:
    """Inline equation: base"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = computeU_i_base(t, t_n)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_u_i_without(p: dict) -> float:
    """Inline equation: u_i_without"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = computeU_i_no_TRZ(t, t_n)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_percent_increase_1(p: dict) -> float:
    """Inline equation: percent_increase"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = ((u_i_with - u_i_without) / u_i_without) * 100.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_grad_ms_r(p: dict) -> float:
    """Inline equation: grad_ms_r"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["M_s"] / (r * r)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_exp_term_2(p: dict) -> float:
    """Inline equation: exp_term"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = math.math.exp(-variables["alpha"] * t_day)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_cos_tn(p: dict) -> float:
    """Inline equation: cos_tn"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = math.math.cos(variables["pi"] * variables["t_n"])
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_defect_factor(p: dict) -> float:
    """Inline equation: defect_factor"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1.0 + computeDelta_def(t_day)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_delta(p: dict) -> float:
    """Inline equation: delta"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = mod.computeDelta_def(0.0)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_u_g1(p: dict) -> float:
    """Inline equation: u_g1"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = mod.computeU_g1(1570.8, 1.496e11)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_theta_2(p: dict) -> float:
    """Inline equation: theta"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["theta_j"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_phi_mag(p: dict) -> float:
    """Inline equation: phi_mag"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = computePhiHatMagnitude(1)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_mag_1(p: dict) -> float:
    """Inline equation: mag"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = computePhiHatMagnitude(j)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_um_1(p: dict) -> float:
    """Inline equation: um"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = computeUmContribution(t, j)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_rho_5(p: dict) -> float:
    """Inline equation: rho"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = mod.computeRho_vac_A()
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_rho_6(p: dict) -> float:
    """Inline equation: rho"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = mod.computeRho_vac_Ui()
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_u_i(p: dict) -> float:
    """Inline equation: u_i"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = mod.computeU_i(0.0, 0.0)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_rho_sc_2(p: dict) -> float:
    """Inline equation: rho_sc"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = computeRho_vac_SCm()
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_rho_7(p: dict) -> float:
    """Inline equation: rho"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = mod.computeRho_vac_SCm()
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_u_g2_4(p: dict) -> float:
    """Inline equation: u_g2"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = mod.computeU_g2_base(1.496e13)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_rho_ua_4(p: dict) -> float:
    """Inline equation: rho_ua"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = computeRho_vac_UA()
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_rho_8(p: dict) -> float:
    """Inline equation: rho"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = mod.computeRho_vac_UA()
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_arg_5(p: dict) -> float:
    """Inline equation: arg"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = -variables["kappa_day"] * t_day
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_m_e_c2(p: dict) -> float:
    """Inline equation: m_e_c2"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["m_e"] * pow(variables["c"], 2)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_ratio_2(p: dict) -> float:
    """Inline equation: ratio"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = pow(variables["omega_LENR"] / variables["omega_0"], 2)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_q_v_B(p: dict) -> float:
    """Inline equation: q_v_B"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 2 * variables["q"] * variables["B_0"] * variables["V"] * math.math.sin(variables["theta"])
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_g_mu_B(p: dict) -> float:
    """Inline equation: g_mu_B"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["g"] * variables["mu_B"] * variables["B_0"] / (variables["hbar"] * variables["omega_0"])
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_ratio_3(p: dict) -> float:
    """Inline equation: ratio"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = pow(E_cm_eff / variables["E_cm"], 2)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_dx_10(p: dict) -> float:
    """Inline equation: dx"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (x2 - x1) / n_points
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_weight(p: dict) -> float:
    """Inline equation: weight"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_force(p: dict) -> float:
    """Inline equation: force"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = mod.computeF_U_Bi(x1, x2, t)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_g_4(p: dict) -> float:
    """Inline equation: g"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["g_Lande"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_omega0_1(p: dict) -> float:
    """Inline equation: omega0"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["omega0"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_k_10(p: dict) -> float:
    """Inline equation: k"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["k_LENR"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_omegaL(p: dict) -> float:
    """Inline equation: omegaL"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["omega_LENR"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_cos_theta_1(p: dict) -> float:
    """Inline equation: cos_theta"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = math.cos(variables["theta"].real())
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_sin_theta(p: dict) -> float:
    """Inline equation: sin_theta"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = math.sin(variables["theta"].real())
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_cos_act(p: dict) -> float:
    """Inline equation: cos_act"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = math.cos(variables["omega_act"].real() * t_user + variables["phi"].real())
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_term_mom_2(p: dict) -> float:
    """Inline equation: term_mom"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (variables["m_e"] * pow(variables["c"], 2) / pow(variables["r"], 2)) * variables["DPM_momentum"] * cos_theta
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_term_grav_2(p: dict) -> float:
    """Inline equation: term_grav"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (variables["G"] * variables["M"] / pow(variables["r"], 2)) * variables["DPM_gravity"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_term_vac_1(p: dict) -> float:
    """Inline equation: term_vac"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["rho_vac_UA"] * variables["DPM_stability"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_term_act(p: dict) -> float:
    """Inline equation: term_act"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["k_act"] * cos_act
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_term_DE(p: dict) -> float:
    """Inline equation: term_DE"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["k_DE"] * variables["L_X"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_term_res_2(p: dict) -> float:
    """Inline equation: term_res"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = cdouble(2.0) * variables["q"] * variables["B0"] * variables["V"] * sin_theta * computeDPM_resonance()
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_term_neut_1(p: dict) -> float:
    """Inline equation: term_neut"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["k_neutron"] * variables["sigma_n"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_term_rel_2(p: dict) -> float:
    """Inline equation: term_rel"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["k_rel"] * pow(variables["E_cm_astro"] / variables["E_cm"], 2)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_term_neutrino(p: dict) -> float:
    """Inline equation: term_neutrino"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["F_neutrino"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_disc(p: dict) -> float:
    """Inline equation: disc"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = math.sqrt(b * b - cdouble(4.0) * a * c)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_integ_1(p: dict) -> float:
    """Inline equation: integ"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = computeIntegrand(t)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_beta_2(p: dict) -> float:
    """Inline equation: beta"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["beta_i"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_V_5(p: dict) -> float:
    """Inline equation: V"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["V_infl_UA"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_rho_9(p: dict) -> float:
    """Inline equation: rho"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["rho_vac_A"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_a_7(p: dict) -> float:
    """Inline equation: a"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["a_universal"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_tn_3(p: dict) -> float:
    """Inline equation: tn"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = t / variables["t_scale"].real()
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_lambda_4(p: dict) -> float:
    """Inline equation: lambda"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["lambda_i"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_omega_s_4(p: dict) -> float:
    """Inline equation: omega_s"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["omega_s"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_G_val(p: dict) -> float:
    """Inline equation: G_val"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["G"].real()
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_M_val(p: dict) -> float:
    """Inline equation: M_val"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["M"].real()
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_rho_10(p: dict) -> float:
    """Inline equation: rho"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["rho_gas"].real()
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_r_val(p: dict) -> float:
    """Inline equation: r_val"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["r"].real()
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_kB(p: dict) -> float:
    """Inline equation: kB"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["k_B"].real()
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_m_e_val(p: dict) -> float:
    """Inline equation: m_e_val"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["m_e"].real()
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_c_val_1(p: dict) -> float:
    """Inline equation: c_val"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["c"].real()
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_term1_9(p: dict) -> float:
    """Inline equation: term1"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = -(G_val * M_val * rho) / r_val
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_term2_7(p: dict) -> float:
    """Inline equation: term2"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = -(kB * T_val * rho) / (m_e_val * c_val * c_val)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_term3_4(p: dict) -> float:
    """Inline equation: term3"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = dpm_curv * pow(c_val, 4) / (G_val * r_val * r_val)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_mu0_val(p: dict) -> float:
    """Inline equation: mu0_val"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["mu0"].real()
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_B_val_1(p: dict) -> float:
    """Inline equation: B_val"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["B0"].real()
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_dpm_res_1(p: dict) -> float:
    """Inline equation: dpm_res"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = computeDPM_resonance()
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_term1_10(p: dict) -> float:
    """Inline equation: term1"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.5 * mu0_val * B_val * B_val * dpm_res
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_term2_8(p: dict) -> float:
    """Inline equation: term2"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.5 * rho * v * v * dpm_phase * t_val
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_F(p: dict) -> float:
    """Inline equation: F"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = abell2256.computeF(t)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_term_res_3(p: dict) -> float:
    """Inline equation: term_res"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 2 * variables["q"] * variables["B0"] * variables["V"] * sin_theta * computeDPM_resonance()
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_term1_11(p: dict) -> float:
    """Inline equation: term1"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = - (G_val * M_val * rho) / r_val
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_term2_9(p: dict) -> float:
    """Inline equation: term2"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = - (kB * T_val * rho) / (m_e_val * c_val * c_val)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_disc_1(p: dict) -> float:
    """Inline equation: disc"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = math.sqrt(b * b - 4 * a * c)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_cos_term_6(p: dict) -> float:
    """Inline equation: cos_term"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = math.cos(variables["pi"].real() * tn)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_delta_1(p: dict) -> float:
    """Inline equation: delta"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = original * (delta_percent / 100.0)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_sensitivity(p: dict) -> float:
    """Inline equation: sensitivity"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = abs(perturbed - baseline) / abs(baseline)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_t_current(p: dict) -> float:
    """Inline equation: t_current"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables.at("t").real()
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_F_total(p: dict) -> float:
    """Inline equation: F_total"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_F_compressed(p: dict) -> float:
    """Inline equation: F_compressed"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_DPM_res_1(p: dict) -> float:
    """Inline equation: DPM_res"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_Ub1(p: dict) -> float:
    """Inline equation: Ub1"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_Ui_1(p: dict) -> float:
    """Inline equation: Ui"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_g_comp(p: dict) -> float:
    """Inline equation: g_comp"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_Q_wave(p: dict) -> float:
    """Inline equation: Q_wave"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_M_val_1(p: dict) -> float:
    """Inline equation: M_val"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables.at("M").real()
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_r_val_1(p: dict) -> float:
    """Inline equation: r_val"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables.at("r").real()
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_L_X_val(p: dict) -> float:
    """Inline equation: L_X_val"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables.at("L_X").real()
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_L_X_val_1(p: dict) -> float:
    """Inline equation: L_X_val"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["L_X"].real()
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_F_approach(p: dict) -> float:
    """Inline equation: F_approach"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = ngc2207.computeF(1.26e15)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_F_starburst(p: dict) -> float:
    """Inline equation: F_starburst"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = ngc2207.computeF(1.26e15)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_F_tidal_2(p: dict) -> float:
    """Inline equation: F_tidal"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = ngc2207.computeF(1.26e15)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_F_quiet(p: dict) -> float:
    """Inline equation: F_quiet"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = ngc2207.computeF(1.26e15)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_M_var(p: dict) -> float:
    """Inline equation: M_var"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variations[i]["M"].real()
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_V_var(p: dict) -> float:
    """Inline equation: V_var"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variations[i]["V"].real()
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_F_1(p: dict) -> float:
    """Inline equation: F"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = const_cast(mod).computeF(1.26e15)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_F_before_evolution(p: dict) -> float:
    """Inline equation: F_before_evolution"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = ngc2207.computeF(1.26e15)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_F_after_evolution(p: dict) -> float:
    """Inline equation: F_after_evolution"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = ngc2207.computeF(1.26e15)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_rho_vac_4(p: dict) -> float:
    """Inline equation: rho_vac"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["rho_vac_UA"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_rho_A_2(p: dict) -> float:
    """Inline equation: rho_A"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["rho_vac_A"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_base_buoyancy(p: dict) -> float:
    """Inline equation: base_buoyancy"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = beta * V * rho_A * a
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_rho_ICM(p: dict) -> float:
    """Inline equation: rho_ICM"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["rho_gas"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_pressure_term(p: dict) -> float:
    """Inline equation: pressure_term"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = rho_ICM * k_B * T_ICM / (variables["m_e"] * c * c)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_radio_enhancement(p: dict) -> float:
    """Inline equation: radio_enhancement"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = math.math.sqrt(L_X / 1e37) * math.math.sqrt(B0 / 1e-9)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_merger_factor(p: dict) -> float:
    """Inline equation: merger_factor"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = velocity_dispersion * velocity_dispersion / (c * c)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_dm_factor_3(p: dict) -> float:
    """Inline equation: dm_factor"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = math.math.log10(M500 / 1e45)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_relativistic_pressure(p: dict) -> float:
    """Inline equation: relativistic_pressure"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["k_rel"] * pressure_term * merger_factor
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_pi_val(p: dict) -> float:
    """Inline equation: pi_val"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["pi"].real()
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_magnetic_energy(p: dict) -> float:
    """Inline equation: magnetic_energy"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = B0 * B0 / (2.0 * mu0)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_turbulence_factor(p: dict) -> float:
    """Inline equation: turbulence_factor"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = turbulent_velocity / variables["c"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_time_evolution(p: dict) -> float:
    """Inline equation: time_evolution"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = math.math.exp(-t / (2.0 * merger_age))
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_synchrotron_scaling(p: dict) -> float:
    """Inline equation: synchrotron_scaling"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = math.math.sqrt(L_X * magnetic_energy)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_shock_acceleration(p: dict) -> float:
    """Inline equation: shock_acceleration"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["k_rel"] * turbulence_factor * turbulence_factor
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_dm_coupling(p: dict) -> float:
    """Inline equation: dm_coupling"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = M / (4.0 * pi_val * r * r * r)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_base_ui(p: dict) -> float:
    """Inline equation: base_ui"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_cluster_enhancement(p: dict) -> float:
    """Inline equation: cluster_enhancement"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = magnetic_energy * turbulence_factor * time_evolution * shock_acceleration
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_radio_coupling(p: dict) -> float:
    """Inline equation: radio_coupling"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = synchrotron_scaling * dm_coupling
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_current_value(p: dict) -> float:
    """Inline equation: current_value"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables[observable].real()
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_error_3(p: dict) -> float:
    """Inline equation: error"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = abs(current_value - target_value) / target_value
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_adjustment_2(p: dict) -> float:
    """Inline equation: adjustment"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = learning_rate * (target_value - current_value) / gradient
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_evolution_factor_1(p: dict) -> float:
    """Inline equation: evolution_factor"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = math.math.exp(-dt / merger_timescale)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_T_ICM_factor(p: dict) -> float:
    """Inline equation: T_ICM_factor"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1.0 + 0.01 * math.math.sin(2 * math.pi * dt / merger_timescale)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_scaling(p: dict) -> float:
    """Inline equation: scaling"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = obs.second / variables[obs.first].real()
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_real_part(p: dict) -> float:
    """Inline equation: real_part"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = std.stod(value_str.substr(0, comma_pos))
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_imag_part(p: dict) -> float:
    """Inline equation: imag_part"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = std.stod(value_str.substr(comma_pos + 1))
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_delta_2(p: dict) -> float:
    """Inline equation: delta"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = original_value * 1e-6
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_new_target(p: dict) -> float:
    """Inline equation: new_target"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = computeF(variables["t"].real())
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_delta_pair_1(p: dict) -> float:
    """Inline equation: delta_pair"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["delta_pair"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_S_scale_1(p: dict) -> float:
    """Inline equation: S_scale"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["S_shell_scale"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_f_res_3(p: dict) -> float:
    """Inline equation: f_res"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = computeF_res(E_bind * 1.602e-19, A)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_U_dp_2(p: dict) -> float:
    """Inline equation: U_dp"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = computeU_dp(A, 1, 1e15, 0.0)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_k_nuc_2(p: dict) -> float:
    """Inline equation: k_nuc"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = computeK_nuc(A - Z, Z)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_S_shell_2(p: dict) -> float:
    """Inline equation: S_shell"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = computeS_shell(0, 0)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_sin_term_1(p: dict) -> float:
    """Inline equation: sin_term"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = math.sin(2 * math.pi * f_res.real() * t)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_term2_10(p: dict) -> float:
    """Inline equation: term2"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = U_dp * variables["SC_m"] * k_nuc
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_integ_2(p: dict) -> float:
    """Inline equation: integ"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = computeH_res_integrand(t, Z, A)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_M_val_2(p: dict) -> float:
    """Inline equation: M_val"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = static_cast(A) * 1.67e-27
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_r_val_2(p: dict) -> float:
    """Inline equation: r_val"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = pow(3 * M_val / (4 * math.pi * rho), 1.0/3.0)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_kB_val(p: dict) -> float:
    """Inline equation: kB_val"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["k_B"].real()
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_term2_11(p: dict) -> float:
    """Inline equation: term2"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = - (kB_val * T_val * rho) / (m_e_val * c_val * c_val)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_r_nucleus(p: dict) -> float:
    """Inline equation: r_nucleus"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1.2e-15 * pow(static_cast(A), 1.0/3.0)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_volume_nuclear(p: dict) -> float:
    """Inline equation: volume_nuclear"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (4.0/3.0) * math.pi * pow(r_nucleus, 3)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_binding_contribution(p: dict) -> float:
    """Inline equation: binding_contribution"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = E_bind * 1.602e-19 / (volume_nuclear * rho_nuclear)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_pairing_term(p: dict) -> float:
    """Inline equation: pairing_term"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["delta_pair"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_range_factor_1(p: dict) -> float:
    """Inline equation: range_factor"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = math.math.exp(-r_nucleus / 1.4e-15)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_phase_3(p: dict) -> float:
    """Inline equation: phase"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 2.0 * math.pi * nuclear_frequency * t
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_quantum_coherence(p: dict) -> float:
    """Inline equation: quantum_coherence"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = math.math.exp(cdouble(0.0, phase))
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_barrier_height(p: dict) -> float:
    """Inline equation: barrier_height"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1e6 * static_cast(Z)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_tunneling_factor(p: dict) -> float:
    """Inline equation: tunneling_factor"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = math.math.exp(-barrier_height / (13.6 * math.sqrt(static_cast(A))))
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_spin_coupling(p: dict) -> float:
    """Inline equation: spin_coupling"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_U_dp_3(p: dict) -> float:
    """Inline equation: U_dp"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = computeU_dp(A, 1, variables["f_dp"].real(), variables["phi_dp"].real())
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_evolution_factor_2(p: dict) -> float:
    """Inline equation: evolution_factor"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = math.math.exp(-dt / nuclear_timescale)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_shell_evolution(p: dict) -> float:
    """Inline equation: shell_evolution"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1.0 + 0.01 * math.math.cos(dt * 1e12)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_scaling_1(p: dict) -> float:
    """Inline equation: scaling"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = data.second / 7.8e6
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_Z_mag(p: dict) -> float:
    """Inline equation: Z_mag"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["Z_magic"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_N_mag(p: dict) -> float:
    """Inline equation: N_mag"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["N_magic"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_pair_effect(p: dict) -> float:
    """Inline equation: pair_effect"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["delta_pair"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_delta_3(p: dict) -> float:
    """Inline equation: delta"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = original_value * 1e-8
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_new_target_1(p: dict) -> float:
    """Inline equation: new_target"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = computeHRes(1, 1, 0.0)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_current_value_1(p: dict) -> float:
    """Inline equation: current_value"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables[observable]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_cycle_phase_1(p: dict) -> float:
    """Inline equation: cycle_phase"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = fmod(variables["t"], variables["solar_cycle_period"]) / variables["solar_cycle_period"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_cycle_factor_1(p: dict) -> float:
    """Inline equation: cycle_factor"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.5 * (1.0 + math.math.cos(2 * math.pi * cycle_phase))
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_diffusion_decay(p: dict) -> float:
    """Inline equation: diffusion_decay"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = math.math.exp(-dt * variables["magnetic_diffusion"] / 1e6)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_convection_enhancement(p: dict) -> float:
    """Inline equation: convection_enhancement"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1.0 + 0.1 * variables["convection_velocity"] / 1e3
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_scaling_2(p: dict) -> float:
    """Inline equation: scaling"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = data.second / variables["B_s_max"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_term_LENR(p: dict) -> float:
    """Inline equation: term_LENR"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = computeLENRTerm(system)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_term_res_4(p: dict) -> float:
    """Inline equation: term_res"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 2 * variables["q"] * variables["B0"] * variables["V"] * sin_theta * computeDPM_resonance(system)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_term_sweet(p: dict) -> float:
    """Inline equation: term_sweet"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["rho_vac_UA"] * variables["DPM_stability"] * variables["V"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_term_kozima(p: dict) -> float:
    """Inline equation: term_kozima"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["k_neutron"] * variables["sigma_n"] * (variables["omega_LENR"] / variables["omega0"])
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_integ_3(p: dict) -> float:
    """Inline equation: integ"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = computeIntegrand(t, system)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_dpm_res_2(p: dict) -> float:
    """Inline equation: dpm_res"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = computeDPM_resonance(system)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_vacuum_term(p: dict) -> float:
    """Inline equation: vacuum_term"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = rho_vac * c * c / 3.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_obs_scaling(p: dict) -> float:
    """Inline equation: obs_scaling"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = math.math.sqrt(L_X / 1e30) * math.math.sqrt(B0 / 1e-9)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_mass_scale(p: dict) -> float:
    """Inline equation: mass_scale"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = math.math.log10(M.real() / 1e30)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_luminosity_factor(p: dict) -> float:
    """Inline equation: luminosity_factor"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = math.math.sqrt(L_X / 1e30)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_enhancement(p: dict) -> float:
    """Inline equation: enhancement"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = mass_scale * magnetic_coupling * luminosity_factor
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_evolution_timescale(p: dict) -> float:
    """Inline equation: evolution_timescale"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["t"].real()
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_omega_10(p: dict) -> float:
    """Inline equation: omega"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["omega0"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_new_target_2(p: dict) -> float:
    """Inline equation: new_target"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = computeFBi("auto", 0.0)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_integrand_13(p: dict) -> float:
    """Inline equation: integrand"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = computeIntegrand(t, system)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_momentum_term_1(p: dict) -> float:
    """Inline equation: momentum_term"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (variables["m_e"] * variables["c"] * variables["c"] / (variables["r"] * variables["r"])) * variables["DPM_momentum"] * cos_theta
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_gravity_term_1(p: dict) -> float:
    """Inline equation: gravity_term"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (variables["G"] * variables["M"] / (variables["r"] * variables["r"])) * variables["DPM_gravity"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_f_bi(p: dict) -> float:
    """Inline equation: f_bi"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = -variables["F0"] + momentum_term + gravity_term + f_bi_i
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_f_lenr_1(p: dict) -> float:
    """Inline equation: f_lenr"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = computeLENRTerm(system)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_f_act_1(p: dict) -> float:
    """Inline equation: f_act"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["k_act"] * math.cos(variables["omega_act"].real() * t)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_f_de_1(p: dict) -> float:
    """Inline equation: f_de"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["k_DE"] * variables["L_X"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_f_neutron_1(p: dict) -> float:
    """Inline equation: f_neutron"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["k_neutron"] * variables["sigma_n"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_f_rel(p: dict) -> float:
    """Inline equation: f_rel"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["k_rel"] * pow(variables["E_cm_astro_local_adj_eff_enhanced"].real() / variables["E_cm"].real(), 2.0)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_f_neutrino(p: dict) -> float:
    """Inline equation: f_neutrino"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["k_neutrino"] * variables["sigma_CNB"] * variables["n_CNB"] * variables["E_CNB"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_f_res_4(p: dict) -> float:
    """Inline equation: f_res"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 2.0 * variables["q"].real() * variables["B0"].real() * variables["V"].real() * sin_theta * dpm_res
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_f_relativ(p: dict) -> float:
    """Inline equation: f_relativ"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["k_relativistic"] * pow(variables["V"].real() / variables["c"].real(), 2.0) * variables["F0"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_f_sweet(p: dict) -> float:
    """Inline equation: f_sweet"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["k_Sweet"] * variables["rho_vac_UA"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_f_kozima(p: dict) -> float:
    """Inline equation: f_kozima"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["k_Kozima"] * variables["sigma_n"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_vac_term_1(p: dict) -> float:
    """Inline equation: vac_term"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["rho_vac_UA"] * variables["DPM_stability"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_integrand_14(p: dict) -> float:
    """Inline equation: integrand"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = -variables["F0"] + momentum_term + gravity_term + vac_term + f_lenr + f_act + f_de + f_res + f_neutron + f_rel + f_neutrino + f_relativ + f_sweet + f_kozima
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_g_lande(p: dict) -> float:
    """Inline equation: g_lande"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["g_Lande"].real()
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_mu_b(p: dict) -> float:
    """Inline equation: mu_b"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["mu_B"].real()
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_b0(p: dict) -> float:
    """Inline equation: b0"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["B0"].real()
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_hbar_omega0(p: dict) -> float:
    """Inline equation: hbar_omega0"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["hbar"].real() * variables["omega0"].real()
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_r_real(p: dict) -> float:
    """Inline equation: r_real"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["r"].real()
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_t_val(p: dict) -> float:
    """Inline equation: t_val"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["T"].real()
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_m(p: dict) -> float:
    """Inline equation: m"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["M"].real()
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_term1_num(p: dict) -> float:
    """Inline equation: term1_num"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["a_eps_coeff"].real() * variables["q"].real()
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_term1_denom(p: dict) -> float:
    """Inline equation: term1_denom"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 4.0 * pi_val * variables["epsilon0"].real() * r2 * t_val
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_term1_12(p: dict) -> float:
    """Inline equation: term1"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = term1_num / term1_denom
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_term2_12(p: dict) -> float:
    """Inline equation: term2"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["G"].real() * m / r2
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_term3_5(p: dict) -> float:
    """Inline equation: term3"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = pow(variables["c"].real(), 4.0) * variables["k_10_13"].real() / r2 * variables["DPM_light"].real()
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_a_8(p: dict) -> float:
    """Inline equation: a"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_term_b1(p: dict) -> float:
    """Inline equation: term_b1"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["k_b_term"].real()
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_term_b3(p: dict) -> float:
    """Inline equation: term_b3"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 2.0 * variables["phase"].real()
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_b_5(p: dict) -> float:
    """Inline equation: b"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_term_c1(p: dict) -> float:
    """Inline equation: term_c1"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["c_constant"].real()
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_term_c2(p: dict) -> float:
    """Inline equation: term_c2"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["c_inv_r2_coeff"].real() / r2
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_term_c3(p: dict) -> float:
    """Inline equation: term_c3"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["curvature"].real()
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_c_3(p: dict) -> float:
    """Inline equation: c"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_disc_2(p: dict) -> float:
    """Inline equation: disc"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = b * b - 4.0 * a * c
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_sqrt_disc(p: dict) -> float:
    """Inline equation: sqrt_disc"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_omega0_real(p: dict) -> float:
    """Inline equation: omega0_real"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["omega0"].real()
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_sigma_CNB(p: dict) -> float:
    """Inline equation: sigma_CNB"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["sigma_CNB"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_CNB_coupling(p: dict) -> float:
    """Inline equation: CNB_coupling"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = sigma_CNB * n_CNB * E_CNB
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_base_buoyancy_1(p: dict) -> float:
    """Inline equation: base_buoyancy"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = rho_vac * c * c / 3.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_enhancement_1(p: dict) -> float:
    """Inline equation: enhancement"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1.0 + CNB_coupling / (rho_vac * c * c)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_scaling_3(p: dict) -> float:
    """Inline equation: scaling"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = math.math.sqrt(M / (r * r * r))
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_k_neutrino(p: dict) -> float:
    """Inline equation: k_neutrino"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["k_neutrino"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_coupling_4(p: dict) -> float:
    """Inline equation: coupling"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = k_neutrino * sigma_CNB * E_CNB
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_omega_11(p: dict) -> float:
    """Inline equation: omega"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 2.0 * variables["pi"] * E_CNB / hbar
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_coherence(p: dict) -> float:
    """Inline equation: coherence"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = math.math.exp(cdouble(0.0, omega.real() * t))
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_mass_scale_1(p: dict) -> float:
    """Inline equation: mass_scale"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = M / (4.0 * variables["pi"] * r * r * r)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_cnb_oscillation(p: dict) -> float:
    """Inline equation: cnb_oscillation"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.6 * math.math.sin(variables["omega_s"] * t)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_thermal_factor(p: dict) -> float:
    """Inline equation: thermal_factor"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1.0 + variables["thermal_coupling"] * variables["CNB_temperature"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_base_b_1(p: dict) -> float:
    """Inline equation: base_b"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["B_ref"] + cnb_oscillation * thermal_factor
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_coupling_strength(p: dict) -> float:
    """Inline equation: coupling_strength"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["CNB_coupling"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_density_factor(p: dict) -> float:
    """Inline equation: density_factor"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["CNB_density"] / 4e8
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_temperature_factor(p: dict) -> float:
    """Inline equation: temperature_factor"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["CNB_temperature"] / 1.95
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_cnb_enhancement(p: dict) -> float:
    """Inline equation: cnb_enhancement"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1.0 + computeCNB_MagneticCoupling(b_j, variables["neutrino_flux"])
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_evolution_factor_3(p: dict) -> float:
    """Inline equation: evolution_factor"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = math.math.exp(-dt / variables["evolution_timescale"])
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_cnb_factor(p: dict) -> float:
    """Inline equation: cnb_factor"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["CNB_density"] / 4e8
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_diffusion_decay_1(p: dict) -> float:
    """Inline equation: diffusion_decay"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = math.math.exp(-dt * variables["magnetic_diffusion"] / 1e4)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_convection_enhancement_1(p: dict) -> float:
    """Inline equation: convection_enhancement"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1.0 + 0.15 * variables["convection_velocity"] / 2.5e3
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_scaling_4(p: dict) -> float:
    """Inline equation: scaling"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = data.second / variables["CNB_temperature"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_new_target_3(p: dict) -> float:
    """Inline equation: new_target"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = computeB_j(variables["t"], variables["B_ref"])
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_base_Ub1(p: dict) -> float:
    """Inline equation: base_Ub1"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["beta_1"] * variables["rho_vac"] * pow(variables["c"], 2)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_time_modulation(p: dict) -> float:
    """Inline equation: time_modulation"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1.0 + 0.15 * math.math.sin(variables["omega_obs"] * variables["t_obs"])
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_distance_factor(p: dict) -> float:
    """Inline equation: distance_factor"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = pow(variables["M104_dist"] / (10 * 3.086e22), -0.3)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_cluster_factor(p: dict) -> float:
    """Inline equation: cluster_factor"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1.0 + 0.25 * math.math.log(variables["NGC4839_mass"] / (1e12 * variables["M_s"]))
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_sf_enhancement(p: dict) -> float:
    """Inline equation: sf_enhancement"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1.0 + 0.8 * math.math.tanh(variables["NGC346_mass"] / (1e5 * variables["M_s"]))
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_spiral_factor(p: dict) -> float:
    """Inline equation: spiral_factor"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1.0 + 0.35 * math.math.cos(2 * variables["omega_obs"] * variables["t_obs"])
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_base_Ui(p: dict) -> float:
    """Inline equation: base_Ui"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["beta_i"] * variables["rho_vac"] * pow(variables["c"], 2)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_coupling_strength_1(p: dict) -> float:
    """Inline equation: coupling_strength"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["alpha"] * math.math.sqrt(Ub1 * Ui)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_base_frequency(p: dict) -> float:
    """Inline equation: base_frequency"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["c"] / (2 * math.pi * variables["R_s"])
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_base_X2(p: dict) -> float:
    """Inline equation: base_X2"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = pow(variables["h"] * variables["c"] / (variables["k_B"] * 2.7), 2)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_base_Q(p: dict) -> float:
    """Inline equation: base_Q"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["rho_vac"] * pow(variables["c"], 3) / variables["h"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_distance_modulation(p: dict) -> float:
    """Inline equation: distance_modulation"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = math.math.sin(variables["c"] * variables["t_obs"] / variables["M104_dist"])
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_cluster_wave(p: dict) -> float:
    """Inline equation: cluster_wave"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = math.math.cos(variables["c"] * variables["t_obs"] / variables["NGC4839_dist"])
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_obs_oscillation(p: dict) -> float:
    """Inline equation: obs_oscillation"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.5 * math.math.sin(variables["omega_s"] * t)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_thermal_factor_1(p: dict) -> float:
    """Inline equation: thermal_factor"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1.0 + variables["thermal_coupling"] * pow(variables["flux_density"] / 1e-26, 0.3)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_base_b_2(p: dict) -> float:
    """Inline equation: base_b"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["B_ref"] + obs_oscillation * thermal_factor
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_coupling_strength_2(p: dict) -> float:
    """Inline equation: coupling_strength"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["luminosity_coupling"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_spectral_factor(p: dict) -> float:
    """Inline equation: spectral_factor"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = pow(variables["observation_frequency"] / 1.4e9, variables["spectral_index"])
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_flux_factor(p: dict) -> float:
    """Inline equation: flux_factor"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["flux_density"] / 1e-26
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_obs_enhancement(p: dict) -> float:
    """Inline equation: obs_enhancement"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1.0 + computeObservationalCoupling(b_j, variables["flux_density"] * 1e26)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_obs_factor(p: dict) -> float:
    """Inline equation: obs_factor"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["flux_density"] / 1e-26
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_frequency_enhancement(p: dict) -> float:
    """Inline equation: frequency_enhancement"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1.0 + 0.1 * variables["convection_velocity"] / 2e3
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_scaling_5(p: dict) -> float:
    """Inline equation: scaling"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = data.second / variables["flux_density"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_freq_factor(p: dict) -> float:
    """Inline equation: freq_factor"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = data.second / 1.4e9
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_freq_factor_1(p: dict) -> float:
    """Inline equation: freq_factor"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["observation_frequency"] / 1.4e9
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_stellar_oscillation(p: dict) -> float:
    """Inline equation: stellar_oscillation"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.8 * math.math.sin(variables["omega_s"] * t)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_rotation_factor(p: dict) -> float:
    """Inline equation: rotation_factor"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1.0 + 0.3 * math.math.sin(variables["stellar_rotation"] * t)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_nuclear_factor(p: dict) -> float:
    """Inline equation: nuclear_factor"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1.0 + 0.1 * math.math.sin(t / (365.25 * 86400))
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_flare_factor(p: dict) -> float:
    """Inline equation: flare_factor"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1.0 + variables["flare_frequency"] * math.math.exp(-std.fmod(t, 86400) / 7200)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_thermal_factor_2(p: dict) -> float:
    """Inline equation: thermal_factor"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1.0 + variables["thermal_coupling"] * variables["chromospheric_heating"] / 2.5e4
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_wind_factor(p: dict) -> float:
    """Inline equation: wind_factor"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1.0 + 0.05 * variables["stellar_wind_velocity"] / 4e5
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_base_b_3(p: dict) -> float:
    """Inline equation: base_b"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["B_ref"] + stellar_oscillation * rotation_factor * nuclear_factor * flare_factor * thermal_factor * wind_factor
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_rotation_factor_1(p: dict) -> float:
    """Inline equation: rotation_factor"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["stellar_rotation"] / 3.2e-6
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_nuclear_factor_1(p: dict) -> float:
    """Inline equation: nuclear_factor"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["nuclear_burning_rate"] / 3.8e26
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_wind_factor_1(p: dict) -> float:
    """Inline equation: wind_factor"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["stellar_wind_velocity"] / 4e5
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_thermal_gradient(p: dict) -> float:
    """Inline equation: thermal_gradient"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["coronal_heating"] / variables["chromospheric_heating"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_stellar_enhancement(p: dict) -> float:
    """Inline equation: stellar_enhancement"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1.0 + computeStellarCoupling(b_j, variables["nuclear_burning_rate"])
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_flare_factor_1(p: dict) -> float:
    """Inline equation: flare_factor"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1.0 + variables["flare_frequency"] * math.math.sin(t / 3600)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_cme_factor(p: dict) -> float:
    """Inline equation: cme_factor"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1.0 + 0.02 * math.math.sin(t / (7 * 86400))
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_stellar_factor(p: dict) -> float:
    """Inline equation: stellar_factor"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["nuclear_burning_rate"] / 3.8e26
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_rotation_decay(p: dict) -> float:
    """Inline equation: rotation_decay"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = math.math.exp(-dt / 1e13)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_scaling_6(p: dict) -> float:
    """Inline equation: scaling"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = data.second / variables["nuclear_burning_rate"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_f_neutrino_1(p: dict) -> float:
    """Inline equation: f_neutrino"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["k_neutrino"] * variables["L_X"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_integrand_15(p: dict) -> float:
    """Inline equation: integrand"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = -variables["F0"] + momentum_term + gravity_term + vac_term + f_lenr + f_act + f_de + f_res + f_neutron + f_rel + f_relativ + f_neutrino + f_sweet + f_kozima
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_result_7(p: dict) -> float:
    """Inline equation: result"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (g * muB * B / (hbar * omega0)).real()
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_term_res_5(p: dict) -> float:
    """Inline equation: term_res"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 2.0 * variables["q"] * variables["B0"] * variables["V"] * sin_theta * computeDPM_resonance(system)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_term_rel_3(p: dict) -> float:
    """Inline equation: term_rel"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["k_rel"] * pow(variables["E_cm_astro"] / variables["E_cm"], 2.0)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_disc_3(p: dict) -> float:
    """Inline equation: disc"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = math.sqrt(b * b - 4.0 * a * c)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_gravity_compressed_1(p: dict) -> float:
    """Inline equation: gravity_compressed"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = computeGravityCompressed(system)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_resonance_1(p: dict) -> float:
    """Inline equation: resonance"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = computeResonanceUr(1, 1, system)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_buoyancy_3(p: dict) -> float:
    """Inline equation: buoyancy"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = computeBuoyancyUbi(system)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_result_8(p: dict) -> float:
    """Inline equation: result"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (integ * x2_val) + gravity_compressed + resonance + buoyancy
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_T_val(p: dict) -> float:
    """Inline equation: T_val"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["T"].real()
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_term3_6(p: dict) -> float:
    """Inline equation: term3"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = dpm_curv * pow(c_val, 4.0) / (G_val * r_val * r_val)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_sum_real(p: dict) -> float:
    """Inline equation: sum_real"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_min_r(p: dict) -> float:
    """Inline equation: min_r"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = computation_history[0].real()
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_max_r(p: dict) -> float:
    """Inline equation: max_r"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = computation_history[0].real()
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_var_real(p: dict) -> float:
    """Inline equation: var_real"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_F1(p: dict) -> float:
    """Inline equation: F1"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = computeMasterEquations(system, t)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_F2(p: dict) -> float:
    """Inline equation: F2"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = computeMasterEquations(system, t + dt)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_dpm_mom_update(p: dict) -> float:
    """Inline equation: dpm_mom_update"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["DPM_momentum"] * (1.0 + 0.01 * dF_dt.real() / abs(F1))
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_M_dot_4(p: dict) -> float:
    """Inline equation: M_dot"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.1 * variables["M"].real() / variables["t"].real()
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_L_Edd(p: dict) -> float:
    """Inline equation: L_Edd"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1.26e38 * (variables["M"].real() / 1.989e30)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_L_acc(p: dict) -> float:
    """Inline equation: L_acc"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = eta * M_dot * c_val * c_val
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_dt_norm(p: dict) -> float:
    """Inline equation: dt_norm"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (t - t_peak) / t_peak
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_L_peak(p: dict) -> float:
    """Inline equation: L_peak"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["L_X"].real()
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_L_tde(p: dict) -> float:
    """Inline equation: L_tde"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = L_peak * math.exp(-abs(dt_norm) / 0.3) * pow(1.0 + abs(dt_norm), -5.0 / 3.0)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_rho_gas(p: dict) -> float:
    """Inline equation: rho_gas"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["rho_gas"].real()
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_T_1(p: dict) -> float:
    """Inline equation: T"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["T"].real()
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_r_37(p: dict) -> float:
    """Inline equation: r"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["r"].real()
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_k_B_1(p: dict) -> float:
    """Inline equation: k_B"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["k_B"].real()
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_m_p_1(p: dict) -> float:
    """Inline equation: m_p"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["m_p"].real()
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_ionization_1(p: dict) -> float:
    """Inline equation: ionization"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["ionization_fraction"].real()
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_v_exp_2(p: dict) -> float:
    """Inline equation: v_exp"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["expansion_velocity"].real()
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_P_thermal_1(p: dict) -> float:
    """Inline equation: P_thermal"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = rho_gas * k_B * T / m_p
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_L_X(p: dict) -> float:
    """Inline equation: L_X"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["L_X"].real()
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_P_rad_1(p: dict) -> float:
    """Inline equation: P_rad"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = L_X / (4.0 * math.pi * r * r * c_val)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_F_expansion_1(p: dict) -> float:
    """Inline equation: F_expansion"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 4.0 * math.pi * r * r * rho_gas * v_exp * v_exp
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_F_ionization_1(p: dict) -> float:
    """Inline equation: F_ionization"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = ionization * P_thermal * 4.0 * math.pi * r * r
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_gas_nebula_2(p: dict) -> float:
    """Inline equation: gas_nebula"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = computeGasNebulaIntegration(system, t_user)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_r0(p: dict) -> float:
    """Inline equation: r0"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["r"].real()
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_t0_1(p: dict) -> float:
    """Inline equation: t0"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["t"].real()
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_r_t_1(p: dict) -> float:
    """Inline equation: r_t"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = r0 * (1.0 + v_exp * (t - t0) / r0)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_rho_t(p: dict) -> float:
    """Inline equation: rho_t"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = rho_gas * pow(r0 / r_t, 3.0)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_q_val_1(p: dict) -> float:
    """Inline equation: q_val"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["q"].real()
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_ionization_rate_1(p: dict) -> float:
    """Inline equation: ionization_rate"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = L_X / photon_energy
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_ion_frac_1(p: dict) -> float:
    """Inline equation: ion_frac"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = min(1.0, ionization_rate / (n_gas * 4.0 * math.pi * r * r * r / 3.0))
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_term_res_6(p: dict) -> float:
    """Inline equation: term_res"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = cdouble(2.0, 0.0) * variables["q"] * variables["B0"] * variables["V"] * sin_theta * computeDPM_resonance(system)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_disc_4(p: dict) -> float:
    """Inline equation: disc"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = math.sqrt(b * b - cdouble(4.0, 0.0) * a * c)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_F_2(p: dict) -> float:
    """Inline equation: F"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = module.computeFBi(sys, t_test)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_U_bi(p: dict) -> float:
    """Inline equation: U_bi"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = module.computeBuoyancy("M74")
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_U_sc(p: dict) -> float:
    """Inline equation: U_sc"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = module.computeSuperconductive("M84", t_test)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_g_comp_1(p: dict) -> float:
    """Inline equation: g_comp"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = module.computeCompressedG("CentaurusA", t_test)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_DPM_res_2(p: dict) -> float:
    """Inline equation: DPM_res"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = module.computeResonant("M16")
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_integrand_16(p: dict) -> float:
    """Inline equation: integrand"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = module.computeCompressed("SupernovaSurvey", t_test)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_rho_11(p: dict) -> float:
    """Inline equation: rho"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["rho_gas"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_gas_nebula_3(p: dict) -> float:
    """Inline equation: gas_nebula"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = computeGasNebulaIntegration(system)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_qs(p: dict) -> float:
    """Inline equation: qs"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = computeQuantumState(n)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_F_3(p: dict) -> float:
    """Inline equation: F"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = module.computeMasterEquations(sys, t_test)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_q_sum(p: dict) -> float:
    """Inline equation: q_sum"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = module.getQuantumStateSum()
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_qs_1(p: dict) -> float:
    """Inline equation: qs"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = module.computeQuantumState(n)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_dipole(p: dict) -> float:
    """Inline equation: dipole"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = module.computeDipoleVortexSpecies(sys)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_U_bi_1(p: dict) -> float:
    """Inline equation: U_bi"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = module.computeBuoyancy("NGC7027")
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_gas_contrib(p: dict) -> float:
    """Inline equation: gas_contrib"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = module.computeGasNebulaIntegration("M57")
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_F_cassini(p: dict) -> float:
    """Inline equation: F_cassini"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = module.computeMasterEquations("Cassini", 1e6)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_geom_factor_3(p: dict) -> float:
    """Inline equation: geom_factor"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_rho_vac_SCm_4(p: dict) -> float:
    """Inline equation: rho_vac_SCm"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_rho_vac_UA_5(p: dict) -> float:
    """Inline equation: rho_vac_UA"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_rho_vac_UA_6(p: dict) -> float:
    """Inline equation: rho_vac_UA"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_u_m(p: dict) -> float:
    """Inline equation: u_m"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = core.calculate_U_m(1.0, 1e20, 26)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_e_field(p: dict) -> float:
    """Inline equation: e_field"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = core.calculate_E(u_m, 1e20)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_eta_1(p: dict) -> float:
    """Inline equation: eta"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = core.calculate_eta(26.0, 1.0, u_m)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_u_g1_1(p: dict) -> float:
    """Inline equation: u_g1"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = core.calculate_U_g1(vars, SPHERICAL)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_force_m82(p: dict) -> float:
    """Inline equation: force_m82"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = m82.calculate_master_force(vars, core)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_force_ic418(p: dict) -> float:
    """Inline equation: force_ic418"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = ic418.calculate_master_force(vars, core)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_force_canis(p: dict) -> float:
    """Inline equation: force_canis"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = canis_major.calculate_master_force(vars, core)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_force_ngc6302(p: dict) -> float:
    """Inline equation: force_ngc6302"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = ngc6302.calculate_master_force(vars, core)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_force_ngc7027(p: dict) -> float:
    """Inline equation: force_ngc7027"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = ngc7027.calculate_master_force(vars, core)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_u_g1_spherical(p: dict) -> float:
    """Inline equation: u_g1_spherical"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = core.calculate_U_g1(vars, SPHERICAL)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_u_g1_toroidal(p: dict) -> float:
    """Inline equation: u_g1_toroidal"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = core.calculate_U_g1(vars, TOROIDAL)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_ratio_4(p: dict) -> float:
    """Inline equation: ratio"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = omega0 / OMEGA_LENR
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_ratio_5(p: dict) -> float:
    """Inline equation: ratio"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = ECM_ASTRO / (ECM / 1.602e-19 / 1e9)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_momentum_term_2(p: dict) -> float:
    """Inline equation: momentum_term"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (ME * C * C / (params.r * params.r)) * dpm.momentum * cos_theta()
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_gravity_term_2(p: dict) -> float:
    """Inline equation: gravity_term"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (G * params.M / (params.r * params.r)) * dpm.gravity
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_vac_term_2(p: dict) -> float:
    """Inline equation: vac_term"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = RHO_VAC_UA * dpm.stability
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_f_lenr_2(p: dict) -> float:
    """Inline equation: f_lenr"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = calculate_F_LENR(params.omega0)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_f_act_2(p: dict) -> float:
    """Inline equation: f_act"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = calculate_F_act(params.t)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_f_de_2(p: dict) -> float:
    """Inline equation: f_de"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = calculate_F_DE(params.L_X)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_f_res_5(p: dict) -> float:
    """Inline equation: f_res"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = calculate_F_res(params.B0, params.omega0)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_base_9(p: dict) -> float:
    """Inline equation: base"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = -F0 + momentum_term + gravity_term + vac_term + f_lenr + f_act + f_de + f_res + f_neutron + f_rel
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_f_bi_i_1(p: dict) -> float:
    """Inline equation: f_bi_i"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = calculate_F_U_Bi_i(params, dpm)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_integrand_17(p: dict) -> float:
    """Inline equation: integrand"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = calculate_integrand(params, dpm)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_a_9(p: dict) -> float:
    """Inline equation: a"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_b_6(p: dict) -> float:
    """Inline equation: b"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 2.51e-5 + calculate_F_neutron() / (params.r * params.r) + PHASE + PHASE
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_c_4(p: dict) -> float:
    """Inline equation: c"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = -3.06e175 + 1e-29 / (params.r * params.r) + CURVATURE
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_buoyancy_sn(p: dict) -> float:
    """Inline equation: buoyancy_sn"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = sn1006.calculate_master_buoyancy(core)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_f_lenr_3(p: dict) -> float:
    """Inline equation: f_lenr"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = core.calculate_F_LENR(1e-12)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_f_rel_1(p: dict) -> float:
    """Inline equation: f_rel"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = core.calculate_F_rel()
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_buoyancy_eta(p: dict) -> float:
    """Inline equation: buoyancy_eta"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = eta.calculate_master_buoyancy(core)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_omega_12(p: dict) -> float:
    """Inline equation: omega"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 2 * math.pi / 10.7 * 3600
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_t_global_4(p: dict) -> float:
    """Inline equation: t_global"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_defaultValue_8(p: dict) -> float:
    """Inline equation: defaultValue"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_t_global_5(p: dict) -> float:
    """Inline equation: t_global"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_ua_sc(p: dict) -> float:
    """Inline equation: ua_sc"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = vars.f_UA_prime[i - 1].real() * vars.f_SCm[i - 1].real()
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_sin_theta_1(p: dict) -> float:
    """Inline equation: sin_theta"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = math.math.sin(vars.theta_i[i - 1])
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_omega_i_val_1(p: dict) -> float:
    """Inline equation: omega_i_val"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = H_Z_BASE * static_cast(i)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_g_i_1(p: dict) -> float:
    """Inline equation: g_i"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (vars.Q_i[i - 1] / static_cast(NUM_STATES)) * g_base
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_r_ug_i_1(p: dict) -> float:
    """Inline equation: r_ug_i"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = g_i * M_SF * cos_term * vars.f_Ub.real()
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_t_7(p: dict) -> float:
    """Inline equation: t"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_vacuum_density_3(p: dict) -> float:
    """Inline equation: vacuum_density"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_defaultValue_9(p: dict) -> float:
    """Inline equation: defaultValue"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_seed_orbit_1(p: dict) -> float:
    """Inline equation: seed_orbit"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_poly_1(p: dict) -> float:
    """Inline equation: poly"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = evaluate26DPoly(pi_patterns, 1.0 / (r * r))
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_defaultValue_10(p: dict) -> float:
    """Inline equation: defaultValue"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_Hz_kms_3(p: dict) -> float:
    """Inline equation: Hz_kms"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["H0"] * math.math.sqrt(variables["Omega_m"] * pow(1.0 + variables["z"], 3) + variables["Omega_Lambda"])
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_Ug1_7(p: dict) -> float:
    """Inline equation: Ug1"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (variables["G"] * variables["M"]) / (variables["r"] * variables["r"])
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_unc_2(p: dict) -> float:
    """Inline equation: unc"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = math.math.sqrt(variables["Delta_x"] * variables["Delta_p"])
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_integral_val(p: dict) -> float:
    """Inline equation: integral_val"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["integral_psi"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_cos_term_7(p: dict) -> float:
    """Inline equation: cos_term"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 2 * variables["A"] * math.math.cos(variables["k"] * variables["x"]) * math.math.cos(variables["omega"] * t)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_exp_factor(p: dict) -> float:
    """Inline equation: exp_factor"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (2 * variables["pi"] / 13.8)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_pert_1(p: dict) -> float:
    """Inline equation: pert"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["delta_rho"] / variables["rho"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_curv(p: dict) -> float:
    """Inline equation: curv"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 3 * variables["G"] * variables["M"] / (variables["r"] * variables["r"] * variables["r"])
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_tr_factor(p: dict) -> float:
    """Inline equation: tr_factor"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1.0 + variables["f_TRZ"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_g_base_3(p: dict) -> float:
    """Inline equation: g_base"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (variables["G"] * variables["M"] / (variables["r"] * variables["r"])) * expansion * tr_factor
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_lambda_term(p: dict) -> float:
    """Inline equation: lambda_term"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["Lambda"] * (variables["c"] * variables["c"]) / 3.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_quantum_term_1(p: dict) -> float:
    """Inline equation: quantum_term"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = computeQuantumTerm(variables["t_Hubble"])
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_em_term(p: dict) -> float:
    """Inline equation: em_term"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["q"] * variables["v_orbit"] * variables["B"] * (1.0 + (7.09e-36 / 7.09e-37)) * variables["scale_macro"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_fluid_term(p: dict) -> float:
    """Inline equation: fluid_term"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = computeFluidTerm(g_base)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_resonant_term(p: dict) -> float:
    """Inline equation: resonant_term"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = computeResonantTerm(t)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_force_dust(p: dict) -> float:
    """Inline equation: force_dust"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["rho_dust"] * (variables["v_orbit"] * variables["v_orbit"])
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_a_dust(p: dict) -> float:
    """Inline equation: a_dust"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (force_dust / variables["rho_mass"]) * variables["scale_macro"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_sc_correction(p: dict) -> float:
    """Inline equation: sc_correction"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1.0 - (variables["B"] / variables["B_crit"])
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_g_base_4(p: dict) -> float:
    """Inline equation: g_base"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = ((variables["G"] * variables["M"] / (variables["r"] * variables["r"])) * expansion * sc_correction) * tr_factor
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_g_BH(p: dict) -> float:
    """Inline equation: g_BH"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (variables["G"] * variables["M_BH"]) / (variables["r_BH"] * variables["r_BH"])
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_em_base_5(p: dict) -> float:
    """Inline equation: em_base"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["q"] * variables["v_orbit"] * variables["B"] / 1.673e-27
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_em_term_1(p: dict) -> float:
    """Inline equation: em_term"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = em_base * (1.0 + (7.09e-36 / 7.09e-37)) * variables["scale_macro"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_g_sun_1(p: dict) -> float:
    """Inline equation: g_sun"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (variables["G"] * variables["M_Sun"] / (variables["r_orbit"] * variables["r_orbit"])) * expansion * tr_factor
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_g_saturn_base(p: dict) -> float:
    """Inline equation: g_saturn_base"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (variables["G"] * variables["M"] / (variables["r"] * variables["r"]))
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_g_saturn(p: dict) -> float:
    """Inline equation: g_saturn"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = g_saturn_base * sc_correction
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_T_ring(p: dict) -> float:
    """Inline equation: T_ring"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (variables["G"] * variables["M_ring"]) / (variables["r_ring"] * variables["r_ring"])
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_em_base_6(p: dict) -> float:
    """Inline equation: em_base"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["q"] * variables["v_wind"] * variables["B"] / 1.673e-27
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_fluid_term_1(p: dict) -> float:
    """Inline equation: fluid_term"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = computeFluidTerm(g_saturn)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_t_yr(p: dict) -> float:
    """Inline equation: t_yr"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = t / variables["year_to_s"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_tau_s(p: dict) -> float:
    """Inline equation: tau_s"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["tau_erode_yr"] * variables["year_to_s"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_msf_factor(p: dict) -> float:
    """Inline equation: msf_factor"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = computeMsfFactor(t)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_m_factor_1(p: dict) -> float:
    """Inline equation: m_factor"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (1.0 + msf_factor) * (1.0 - e_rad)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_g_base_5(p: dict) -> float:
    """Inline equation: g_base"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (variables["G"] * variables["M"] * m_factor / (variables["r"] * variables["r"])) * expansion * sc_correction * tr_factor
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_em_base_7(p: dict) -> float:
    """Inline equation: em_base"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["q"] * variables["v_gas"] * variables["B"] / 1.673e-27
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_r_38(p: dict) -> float:
    """Inline equation: r"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["r0"] + variables["v_exp"] * variables["t"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_Ug1_8(p: dict) -> float:
    """Inline equation: Ug1"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (variables["G"] * variables["M"]) / (r * r)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_curv_1(p: dict) -> float:
    """Inline equation: curv"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 3 * variables["G"] * variables["M"] / (r * r * r)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_pressure(p: dict) -> float:
    """Inline equation: pressure"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (variables["P_pulsar"] / (4 * variables["pi"] * r * r)) * (1.0 + variables["v_shock"] / variables["c"])
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_force_1(p: dict) -> float:
    """Inline equation: force"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["q"] * variables["v_shock"] * variables["B"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_r_39(p: dict) -> float:
    """Inline equation: r"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["r0"] + variables["v_exp"] * t
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_g_base_6(p: dict) -> float:
    """Inline equation: g_base"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (variables["G"] * variables["M"] / (r * r)) * expansion * sc_correction * tr_factor
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_em_base_8(p: dict) -> float:
    """Inline equation: em_base"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["q"] * variables["v_shock"] * variables["B"] / 1.673e-27
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_g_base_7(p: dict) -> float:
    """Inline equation: g_base"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (variables["G"] * variables["M"] / (variables["r"] * variables["r"])) * expansion * sc_correction * tr_factor
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_em_base_9(p: dict) -> float:
    """Inline equation: em_base"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["q"] * variables["v_spin"] * variables["B"] / 1.673e-27
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_F_DPM(p: dict) -> float:
    """Inline equation: F_DPM"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["I"] * variables["A"] * (variables["omega_1"] - variables["omega_2"])
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_a_vac_diff(p: dict) -> float:
    """Inline equation: a_vac_diff"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = computeVacDiffTerm()
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_a_super(p: dict) -> float:
    """Inline equation: a_super"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = computeSuperFreqTerm()
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_a_aether_res(p: dict) -> float:
    """Inline equation: a_aether_res"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = computeAetherResTerm()
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_a_quantum(p: dict) -> float:
    """Inline equation: a_quantum"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = computeQuantumFreqTerm()
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_a_aether_freq(p: dict) -> float:
    """Inline equation: a_aether_freq"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = computeAetherFreqTerm()
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_a_fluid(p: dict) -> float:
    """Inline equation: a_fluid"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = computeFluidFreqTerm()
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_a_exp(p: dict) -> float:
    """Inline equation: a_exp"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = computeExpFreqTerm()
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_g_sum(p: dict) -> float:
    """Inline equation: g_sum"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = a_DPM + a_THz + a_vac_diff + a_super + a_aether_res + a_u_g4i + a_quantum + a_aether_freq + a_fluid + a_osc + a_exp
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_Ug1_9(p: dict) -> float:
    """Inline equation: Ug1"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (6.6743e-11 * variables["M"]) / (variables["r"] * variables["r"])
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_F_DPM_1(p: dict) -> float:
    """Inline equation: F_DPM"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["I"] * variables["A_vort"] * (variables["omega_1"] - variables["omega_2"])
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_a_DPM_res(p: dict) -> float:
    """Inline equation: a_DPM_res"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = computeDPMResTerm()
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_E_vac_ISM(p: dict) -> float:
    """Inline equation: E_vac_ISM"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["E_vac"] / 10.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_cos_term_8(p: dict) -> float:
    """Inline equation: cos_term"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 2 * variables["A"] * math.math.cos(variables["k"] * variables["x"]) * math.math.cos(variables["omega_osc"] * t)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_a_THz_res(p: dict) -> float:
    """Inline equation: a_THz_res"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = computeTHzResTerm()
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_a_u_g4i_res(p: dict) -> float:
    """Inline equation: a_u_g4i_res"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = computeU_g4iResTerm()
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_a_osc_res(p: dict) -> float:
    """Inline equation: a_osc_res"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = computeOscResTerm(t)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_a_sc_freq(p: dict) -> float:
    """Inline equation: a_sc_freq"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = computeSCFreqTerm()
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_res_term(p: dict) -> float:
    """Inline equation: res_term"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = computeResonanceTerm(t)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_sc_corr(p: dict) -> float:
    """Inline equation: sc_corr"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = computeSuperconductiveCorrection(B)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_g_res_sc(p: dict) -> float:
    """Inline equation: g_res_sc"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = mod.computeFullUQFFResSC(t, B)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_a_DPM(p: dict) -> float:
    """Inline equation: a_DPM"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (F_DPM * variables["f_DPM"] * variables["E_vac"]) / (variables["c"] * variables["V_sys"])
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_a_THz(p: dict) -> float:
    """Inline equation: a_THz"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (variables["f_THz"] * variables["E_vac"] * variables["v_exp"] * a_DPM) / (variables["E_vac"] / 10 * variables["c"])
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_a_vac_diff_1(p: dict) -> float:
    """Inline equation: a_vac_diff"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (variables["E_0"] * variables["f_vac_diff"] * variables["V_sys"] * a_DPM) / variables["hbar"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_a_super_1(p: dict) -> float:
    """Inline equation: a_super"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (variables["hbar"] * variables["f_super"] * variables["f_DPM"] * a_DPM) / (variables["E_vac"] * variables["c"])
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_a_DPM_1(p: dict) -> float:
    """Inline equation: a_DPM"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (variables["I"] * variables["A_vort"] * (variables["omega_1"] - variables["omega_2"]) * variables["f_DPM"] * variables["E_vac"]) / (variables["c"] * variables["V_sys"])
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_a_aether(p: dict) -> float:
    """Inline equation: a_aether"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["f_aether"] * 1e-8 * variables["f_DPM"] * (1 + variables["f_TRZ"]) * a_DPM
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_a_u_g4i(p: dict) -> float:
    """Inline equation: a_u_g4i"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["f_sc"] * Ug1_proxy * variables["f_react"] * a_DPM / (variables["E_vac"] * variables["c"])
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_a_osc(p: dict) -> float:
    """Inline equation: a_osc"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = cos_term + exp_factor * real_exp
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_a_quantum_1(p: dict) -> float:
    """Inline equation: a_quantum"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (variables["f_quantum"] * variables["E_vac"] * a_DPM) / (variables["E_vac"] / 10 * variables["c"])
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_a_fluid_1(p: dict) -> float:
    """Inline equation: a_fluid"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (variables["f_fluid"] * variables["E_vac"] * variables["V"]) / (variables["E_vac"] / 10 * variables["c"])
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_a_exp_1(p: dict) -> float:
    """Inline equation: a_exp"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (variables["f_exp"] * variables["E_vac"] * a_DPM) / (variables["E_vac"] / 10 * variables["c"])
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_comp_1(p: dict) -> float:
    """Inline equation: comp"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = computeCompressedTerm()
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_res(p: dict) -> float:
    """Inline equation: res"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = computeResonanceTerm(t)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_sc_int(p: dict) -> float:
    """Inline equation: sc_int"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = computeSCIntegrated(B)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_g_comp_res(p: dict) -> float:
    """Inline equation: g_comp_res"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = mod.computeCompressedResTerm(t, B)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_r_t_2(p: dict) -> float:
    """Inline equation: r_t"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["r0"] + variables["v_exp"] * variables["t"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_V_sys_t(p: dict) -> float:
    """Inline equation: V_sys_t"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (4.0 / 3.0) * variables["pi"] * pow(r_t, 3)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_a_quantum_res(p: dict) -> float:
    """Inline equation: a_quantum_res"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = computeQuantumResTerm()
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_a_fluid_res(p: dict) -> float:
    """Inline equation: a_fluid_res"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = computeFluidResTerm()
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_a_exp_res(p: dict) -> float:
    """Inline equation: a_exp_res"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = computeExpResTerm()
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_sc_int_1(p: dict) -> float:
    """Inline equation: sc_int"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = computeSCResIntegrated(B)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_res_sum(p: dict) -> float:
    """Inline equation: res_sum"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = a_DPM_res + a_THz_res + a_aether_res + a_u_g4i_res + a_quantum_res + a_fluid_res + a_osc_res + a_exp_res
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_SCm_contrib_6(p: dict) -> float:
    """Inline equation: SCm_contrib"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_SCm_contrib_7(p: dict) -> float:
    """Inline equation: SCm_contrib"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_SCm_contrib_8(p: dict) -> float:
    """Inline equation: SCm_contrib"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_phi_hat_3(p: dict) -> float:
    """Inline equation: phi_hat"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_tolerance_1(p: dict) -> float:
    """Inline equation: tolerance"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_feedbackParameter_1(p: dict) -> float:
    """Inline equation: feedbackParameter"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_uqff_g_3(p: dict) -> float:
    """Inline equation: uqff_g"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_H0_4(p: dict) -> float:
    """Inline equation: H0"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_a_fluid_2(p: dict) -> float:
    """Inline equation: a_fluid"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (variables["f_fluid"] * variables["E_vac"] * variables["V"] * a_DPM) / (variables["E_vac"] / 10 * variables["c"])
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_em_base_10(p: dict) -> float:
    """Inline equation: em_base"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["q"] * variables["v_exp"] * variables["B"] / 1.673e-27
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_em_base_11(p: dict) -> float:
    """Inline equation: em_base"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["q"] * variables["v_orbital"] * variables["B"] / 9.11e-31
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_a_quantum_orbital_res(p: dict) -> float:
    """Inline equation: a_quantum_orbital_res"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = computeQuantumOrbitalResTerm()
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_res_sum_1(p: dict) -> float:
    """Inline equation: res_sum"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = a_DPM_res + a_THz_res + a_aether_res + a_u_g4i_res + a_quantum_orbital_res + a_osc_res
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_g_res(p: dict) -> float:
    """Inline equation: g_res"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = mod.computeResonanceTerm(t, B)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_flux(p: dict) -> float:
    """Inline equation: flux"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["L_H36"] / (4 * variables["pi"] * variables["r"] * variables["r"] * variables["c"])
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_Hz_kms_4(p: dict) -> float:
    """Inline equation: Hz_kms"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["H0"] * math.math.sqrt(variables["Omega_m"] * pow(1.0 + z, 3) + variables["Omega_Lambda"])
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_torque_base(p: dict) -> float:
    """Inline equation: torque_base"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (variables["G"] * variables["M_gas"] * variables["M"]) / (variables["r"] * variables["r"])
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_flux_1(p: dict) -> float:
    """Inline equation: flux"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["L_SN"] / (4 * variables["pi"] * variables["r"] * variables["r"] * variables["c"])
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_g_base_8(p: dict) -> float:
    """Inline equation: g_base"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = ((variables["G"] * variables["M"] / (variables["r"] * variables["r"])) * expansion * sc_correction * tr_factor) * (1.0 + t_spiral)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_lambda_term_1(p: dict) -> float:
    """Inline equation: lambda_term"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["Lambda"] * (variables["c"] * variables["c"] * variables["Omega_Lambda"]) / 3.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_em_base_12(p: dict) -> float:
    """Inline equation: em_base"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["q"] * variables["v_rot"] * variables["B"] / 1.673e-27
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_Ug1_10(p: dict) -> float:
    """Inline equation: Ug1"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (6.6743e-11 * 3.98e30) / (1.42e16 * 1.42e16)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_Ug2_4(p: dict) -> float:
    """Inline equation: Ug2"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = pow(vexp, 2) / r
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_Ug4_4(p: dict) -> float:
    """Inline equation: Ug4"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = Ug1 * variables["f_sc"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_M_vis(p: dict) -> float:
    """Inline equation: M_vis"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["M_visible"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_pert_mass(p: dict) -> float:
    """Inline equation: pert_mass"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (M_vis + M_dm) * pert
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_w_stellar(p: dict) -> float:
    """Inline equation: w_stellar"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = computeW_stellar(t)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_em_base_13(p: dict) -> float:
    """Inline equation: em_base"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["q"] * variables["v_exp"] * variables["B"] / variables["m_p"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_vac_ratio_1(p: dict) -> float:
    """Inline equation: vac_ratio"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1.0 + variables["rho_vac_UA"] / variables["rho_vac_SCm"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_em_term_2(p: dict) -> float:
    """Inline equation: em_term"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = em_base * vac_ratio
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_a_THz_1(p: dict) -> float:
    """Inline equation: a_THz"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (variables["f_THz"] * variables["E_vac"] * variables["v_exp"] * a_DPM) / (variables["E_vac_ISM"] * variables["c"])
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_a_super_2(p: dict) -> float:
    """Inline equation: a_super"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (variables["hbar"] * variables["f_super"] * variables["f_DPM"] * a_DPM) / (variables["E_vac_ISM"] * variables["c"])
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_a_quantum_2(p: dict) -> float:
    """Inline equation: a_quantum"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (variables["f_quantum"] * variables["E_vac"] * a_DPM) / (variables["E_vac_ISM"] * variables["c"])
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_a_fluid_3(p: dict) -> float:
    """Inline equation: a_fluid"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (variables["f_fluid"] * variables["E_vac"] * variables["V"] * a_DPM) / (variables["E_vac_ISM"] * variables["c"])
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_a_exp_2(p: dict) -> float:
    """Inline equation: a_exp"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (variables["f_exp"] * variables["E_vac"] * a_DPM) / (variables["E_vac_ISM"] * variables["c"])
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_g_comp_res_1(p: dict) -> float:
    """Inline equation: g_comp_res"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = mod.computeFullUQFF34(system_id, t, B)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_default_val_1(p: dict) -> float:
    """Inline equation: default_val"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_base_10(p: dict) -> float:
    """Inline equation: base"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = compute_compressed_base(sys)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_expansion_2(p: dict) -> float:
    """Inline equation: expansion"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = compute_compressed_expansion(sys)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_super_adj_1(p: dict) -> float:
    """Inline equation: super_adj"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = compute_compressed_super_adj(sys)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_env_1(p: dict) -> float:
    """Inline equation: env"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = compute_compressed_env()
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_Ug_sum_1(p: dict) -> float:
    """Inline equation: Ug_sum"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = compute_compressed_Ug_sum()
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_cosm_1(p: dict) -> float:
    """Inline equation: cosm"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = compute_compressed_cosm()
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_quantum_1(p: dict) -> float:
    """Inline equation: quantum"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = compute_compressed_quantum()
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_fluid_1(p: dict) -> float:
    """Inline equation: fluid"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = compute_compressed_fluid(sys)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_perturbation_1(p: dict) -> float:
    """Inline equation: perturbation"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = compute_compressed_perturbation(sys)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_fexp_2(p: dict) -> float:
    """Inline equation: fexp"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 2 * math.pi * H_z * sys.t
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_V_gal(p: dict) -> float:
    """Inline equation: V_gal"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = compute_volume(1.543e21)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_V_stellar(p: dict) -> float:
    """Inline equation: V_stellar"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = compute_volume(9.46e16)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_V_sombrero(p: dict) -> float:
    """Inline equation: V_sombrero"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = compute_volume(4.73e20)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_V_saturn(p: dict) -> float:
    """Inline equation: V_saturn"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = compute_volume(6.027e7)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_V_crab(p: dict) -> float:
    """Inline equation: V_crab"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = compute_volume(5.203e16)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_V_guide(p: dict) -> float:
    """Inline equation: V_guide"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = compute_volume(1.496e11)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_V_lagoon(p: dict) -> float:
    """Inline equation: V_lagoon"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = compute_volume(5.203e17)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_V_spirals(p: dict) -> float:
    """Inline equation: V_spirals"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = compute_volume(1.543e21)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_V_ngc(p: dict) -> float:
    """Inline equation: V_ngc"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = compute_volume(1.514e16)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_V_orion(p: dict) -> float:
    """Inline equation: V_orion"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = compute_volume(1.135e17)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_grav_base(p: dict) -> float:
    """Inline equation: grav_base"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (dpm_ug1_seed(MM, rr)) * local_vars["one_plus_H_t"] * local_vars["B_adjust"] * local_vars["one_plus_F_env"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_orb_grav(p: dict) -> float:
    """Inline equation: orb_grav"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (G * local_vars["M_sun"] / (local_vars["r_orbit"] * local_vars["r_orbit"])) * local_vars["one_plus_H_t"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_muge(p: dict) -> float:
    """Inline equation: muge"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_delta_omega(p: dict) -> float:
    """Inline equation: delta_omega"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = local_vars["omega1"] - local_vars["omega2"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_F_DPM_2(p: dict) -> float:
    """Inline equation: F_DPM"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = II * AA * delta_omega
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_a_DPM_2(p: dict) -> float:
    """Inline equation: a_DPM"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = F_DPM * local_vars["f_DPM"] * E_vac_neb / (c * local_vars["V"])
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_a_THz_2(p: dict) -> float:
    """Inline equation: a_THz"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = local_vars["f_DPM"] * E_vac_neb * local_vars["v_exp"] * a_DPM / (E_vac_ISM * c)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_a_vac_diff_2(p: dict) -> float:
    """Inline equation: a_vac_diff"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = Delta_E_vac * pow(local_vars["v_exp"], 2) * a_DPM / (E_vac_neb * c * c)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_a_super_freq(p: dict) -> float:
    """Inline equation: a_super_freq"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = F_super * local_vars["f_DPM"] * a_DPM / (E_vac_neb * c)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_a_aether_res_1(p: dict) -> float:
    """Inline equation: a_aether_res"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = k_4 * omega_i * local_vars["f_DPM"] * a_DPM * (1 + UA_SC_m * 0.1)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_a_quantum_freq(p: dict) -> float:
    """Inline equation: a_quantum_freq"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = f_quantum * E_vac_neb * a_DPM / (E_vac_ISM * c)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_a_Aether_freq(p: dict) -> float:
    """Inline equation: a_Aether_freq"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = f_Aether * E_vac_neb * a_DPM / (E_vac_ISM * c)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_f_fluid_1(p: dict) -> float:
    """Inline equation: f_fluid"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (G * local_vars["M"] / (local_vars["r"] * local_vars["r"])) / (2 * pi)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_a_fluid_freq(p: dict) -> float:
    """Inline equation: a_fluid_freq"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = f_fluid * E_vac_neb * local_vars["V"] / (E_vac_ISM * c)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_f_exp(p: dict) -> float:
    """Inline equation: f_exp"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = local_vars["H_z"] * local_vars["t"] / (2 * pi)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_a_exp_freq(p: dict) -> float:
    """Inline equation: a_exp_freq"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = f_exp * E_vac_neb * a_DPM / (E_vac_ISM * c)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_muge_1(p: dict) -> float:
    """Inline equation: muge"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_sqrt_unc_1(p: dict) -> float:
    """Inline equation: sqrt_unc"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = math.math.sqrt(variables["Delta_x_Delta_p"])
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_V_6(p: dict) -> float:
    """Inline equation: V"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (4.0 / 3.0) * variables["pi"] * pow(r, 3)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_pert_2(p: dict) -> float:
    """Inline equation: pert"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["delta_rho_over_rho"] + 3 * variables["G"] * variables["M"] / pow(variables["r"], 3)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_env_factor(p: dict) -> float:
    """Inline equation: env_factor"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1.0 + variables["F_env"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_g_base_9(p: dict) -> float:
    """Inline equation: g_base"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (variables["G"] * variables["M"] / (variables["r"] * variables["r"])) * expansion * sc_correction * env_factor
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_dm_pert_term(p: dict) -> float:
    """Inline equation: dm_pert_term"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = computeDMPertTerm()
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_t_8(p: dict) -> float:
    """Inline equation: t"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = mod.variables["t_default"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_Ug2_5(p: dict) -> float:
    """Inline equation: Ug2"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = pow(vout, 2) / r
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_p_outflow(p: dict) -> float:
    """Inline equation: p_outflow"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = computeP_outflow(t)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_em_base_14(p: dict) -> float:
    """Inline equation: em_base"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["q"] * variables["v_out"] * variables["B"] / variables["m_p"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_Hz_kms_5(p: dict) -> float:
    """Inline equation: Hz_kms"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["H0"] * math.math.sqrt(variables["Omega_m"] * pow(1.0 + z_t, 3) + variables["Omega_Lambda"])
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_M_t_1(p: dict) -> float:
    """Inline equation: M_t"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = computeM_t(variables["t"])
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_phase_4(p: dict) -> float:
    """Inline equation: phase"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (2 * variables["pi"] / variables["lambda_gw"]) * r_t - (2 * variables["pi"] / variables["year_to_s"]) * t
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_g_base_10(p: dict) -> float:
    """Inline equation: g_base"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (variables["G"] * M_t / (r_t * r_t)) * expansion * sc_correction * tr_factor
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_dm_pert_term_1(p: dict) -> float:
    """Inline equation: dm_pert_term"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = computeDMTerm(g_base)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_dm_term(p: dict) -> float:
    """Inline equation: dm_term"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = computeDMTerm(g_base)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_gw_term(p: dict) -> float:
    """Inline equation: gw_term"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = computeGWTerm(r_t, t)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_t_9(p: dict) -> float:
    """Inline equation: t"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = mod.variables["t_Hubble"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_D_t(p: dict) -> float:
    """Inline equation: D_t"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = math.math.exp(-t / (1e3 * variables["year_to_s"]))
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_E_t(p: dict) -> float:
    """Inline equation: E_t"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1.0 - math.math.exp(-t / (2e6 * variables["year_to_s"]))
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_L_t_1(p: dict) -> float:
    """Inline equation: L_t"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1.0 + 0.1 * math.math.sin(2 * variables["pi"] * t / variables["t_Hubble"])
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_integral_val_1(p: dict) -> float:
    """Inline equation: integral_val"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["integral_psi_total"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_Ug3_prime(p: dict) -> float:
    """Inline equation: Ug3_prime"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_pert_3(p: dict) -> float:
    """Inline equation: pert"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["delta_rho_over_rho"] + 3 * variables["G"] * variables["M"] / pow(r, 3)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_g_base_11(p: dict) -> float:
    """Inline equation: g_base"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (variables["G"] * variables["M"] * m_factor / (r * r)) * expansion * sc_correction * f_env
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_dm_pert_term_2(p: dict) -> float:
    """Inline equation: dm_pert_term"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = computeDMPertTerm(r)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_uqff_g_4(p: dict) -> float:
    """Inline equation: uqff_g"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = compute_resonance_MUGE(sagA, res)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_min(p: dict) -> float:
    """Inline equation: min"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_max(p: dict) -> float:
    """Inline equation: max"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_mean(p: dict) -> float:
    """Inline equation: mean"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = sum / values.size()
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_FU_1(p: dict) -> float:
    """Inline equation: FU"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = compute_FU(body, r, t, tn, theta)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_compressed_g(p: dict) -> float:
    """Inline equation: compressed_g"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = compute_compressed_MUGE(sys)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_resonance_g(p: dict) -> float:
    """Inline equation: resonance_g"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_min_1(p: dict) -> float:
    """Inline equation: min"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = values[0], max = values[0], sum = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_D_t_1(p: dict) -> float:
    """Inline equation: D_t"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = math.math.exp(-t_yr / 1e3)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_BH_term(p: dict) -> float:
    """Inline equation: BH_term"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (variables["G"] * variables["M_ext"]) / (variables["r_ext"] * variables["r_ext"])
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_E_t_1(p: dict) -> float:
    """Inline equation: E_t"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1.0 - math.math.exp(-t_yr / 2e6)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_M_SN_t(p: dict) -> float:
    """Inline equation: M_SN_t"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["M_SN"] * (1.0 - math.math.exp(-t_yr / 1e8))
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_P_t(p: dict) -> float:
    """Inline equation: P_t"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1.0 * math.math.exp(-t_yr / 3e6)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_E_t_2(p: dict) -> float:
    """Inline equation: E_t"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1.0 - math.math.exp(-t_yr / 4e6)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_M_merge_t(p: dict) -> float:
    """Inline equation: M_merge_t"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.1 * variables["M"] * (1.0 - math.math.exp(-t_yr / 5e8))
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_E_t_3(p: dict) -> float:
    """Inline equation: E_t"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1.0 - math.math.exp(-t_yr / 1e6)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_F_fil(p: dict) -> float:
    """Inline equation: F_fil"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1e-10 * variables["B"] * variables["r"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_F_BH(p: dict) -> float:
    """Inline equation: F_BH"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (variables["G"] * variables["M_ext"]) / (variables["r_ext"] * variables["r_ext"])
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_F_sn(p: dict) -> float:
    """Inline equation: F_sn"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["M_SN"] * math.math.exp(-t_yr / 8e8)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_M_evo_t(p: dict) -> float:
    """Inline equation: M_evo_t"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.01 * variables["M"] * (t / variables["t_Hubble"])
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_frame_rate_inv(p: dict) -> float:
    """Inline equation: frame_rate_inv"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1.0 / variables["fps"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_t_minus(p: dict) -> float:
    """Inline equation: t_minus"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = computeTminus(variables["t_n"])
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_Ug1_11(p: dict) -> float:
    """Inline equation: Ug1"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["k1"] * (variables["G"] * variables["M_bh"] / (r * r)) * math.math.exp(-variables["gamma"] * t_minus) * math.math.cos(variables["pi"] * variables["t_n"])
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_beta_term(p: dict) -> float:
    """Inline equation: beta_term"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["beta1"] * Ug1 * variables["Omega_g"] * variables["E_react"] / variables["M_bh"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_exp_cos(p: dict) -> float:
    """Inline equation: exp_cos"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1 - math.math.exp(-variables["gamma"] * t_minus) * math.math.cos(variables["pi"] * variables["t_n"])
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_Um1(p: dict) -> float:
    """Inline equation: Um1"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (variables["mu1"] / r) * exp_cos * pow(variables["phi1"], 1) * variables["rho_vac_Um"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_metric(p: dict) -> float:
    """Inline equation: metric"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = computeMetricTerm()
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_ub(p: dict) -> float:
    """Inline equation: ub"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = computeUbTerm(t_minus)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_vac_sc(p: dict) -> float:
    """Inline equation: vac_sc"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = computeVacEnergy("SCm")
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_vac_ua(p: dict) -> float:
    """Inline equation: vac_ua"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = computeVacEnergy("UA")
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_spin_factor(p: dict) -> float:
    """Inline equation: spin_factor"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = math.math.cos(variables["omega_s"] * t) * variables["T_s"] * variables["B_s"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_sc_factor(p: dict) -> float:
    """Inline equation: sc_factor"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["SCm"] * variables["SCm_prime"] * variables["UA"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_fu_ext(p: dict) -> float:
    """Inline equation: fu_ext"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = computeFUExtension(t)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_up_total(p: dict) -> float:
    """Inline equation: up_total"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = ug + um + metric + ub
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_plasmoids(p: dict) -> float:
    """Inline equation: plasmoids"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = computePlasmoidCount(t)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_energy_frame(p: dict) -> float:
    """Inline equation: energy_frame"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["energy_per_frame"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_ug3_1(p: dict) -> float:
    """Inline equation: ug3"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1.0 * variables["M_stars"] * 3.38e20 / pow(r, 3) * math.math.cos(theta) * 1.0 * pow(10, 46) * pow(1.0 + computeNonLocalTerm(t, variables["n26"]), n)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_non_local(p: dict) -> float:
    """Inline equation: non_local"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = computeNonLocalTerm(t, variables["n26"])
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_dx_11(p: dict) -> float:
    """Inline equation: dx"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = star_positions[j].first - star_positions[i].first
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_dy(p: dict) -> float:
    """Inline equation: dy"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = star_positions[j].second - star_positions[i].second
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_e_field_1(p: dict) -> float:
    """Inline equation: e_field"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["k_eta"] * variables["e"] * variables["Omega"] / variables["m_e"] * math.math.sqrt(variables["n_e"] * variables["sigma"] * variables["v"])
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_eta_2(p: dict) -> float:
    """Inline equation: eta"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["k_eta"] * variables["n_e"] * variables["sigma"] * variables["v"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_m_H(p: dict) -> float:
    """Inline equation: m_H"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["k_Higgs"] * 125.0 * variables["mu"] * variables["kappa_F"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_ug3_2(p: dict) -> float:
    """Inline equation: ug3"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = computeUg3(t, r, variables["theta"], variables["n"])
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_T_2(p: dict) -> float:
    """Inline equation: T"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = ug3 / variables["E_vac_neb"] * variables["T_scale"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_e_field_2(p: dict) -> float:
    """Inline equation: e_field"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = computeElectricField()
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_eta_3(p: dict) -> float:
    """Inline equation: eta"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = computeNeutronRate()
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_trans_E(p: dict) -> float:
    """Inline equation: trans_E"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = computeTransmutationEnergy()
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_T_star(p: dict) -> float:
    """Inline equation: T_star"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = computeStarFormationTemp(t, variables["r_NGC"])
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_v_rad(p: dict) -> float:
    """Inline equation: v_rad"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = computeRadialVelocity(variables["delta_lambda_over_lambda"])
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_E_neut(p: dict) -> float:
    """Inline equation: E_neut"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = computeNeutrinoProto(t)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_decay_6(p: dict) -> float:
    """Inline equation: decay"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = computeUniversalDecay(t)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_E_DNA(p: dict) -> float:
    """Inline equation: E_DNA"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = computeDNAEnergy(t)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_buoy(p: dict) -> float:
    """Inline equation: buoy"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = computeBuoyancyRatio(variables["V_little"], variables["V_big"])
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_ug3_3(p: dict) -> float:
    """Inline equation: ug3"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = computeUg3(t, variables["r_NGC"], variables["theta"], variables["n"])
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_T_3(p: dict) -> float:
    """Inline equation: T"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = computeStarFormationTemp(t, variables["r_NGC"])
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_acc_lenr(p: dict) -> float:
    """Inline equation: acc_lenr"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = computeAccuracy("LENR_CELL")
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_acc_higgs(p: dict) -> float:
    """Inline equation: acc_higgs"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = computeAccuracy("HIGGS_PHYSICS")
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_geo_angle(p: dict) -> float:
    """Inline equation: geo_angle"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = computeGeometricCondition(stars)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_non_local_1(p: dict) -> float:
    """Inline equation: non_local"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = computeNonLocalExp(t, variables["n26"])
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_rho_UA_SCm(p: dict) -> float:
    """Inline equation: rho_UA_SCm"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1e-23 * pow(0.1, 1) * math.math.exp(-1) * math.math.exp(-variables["pi"])
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_exp_cos_1(p: dict) -> float:
    """Inline equation: exp_cos"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1 - math.math.exp(-0.00005) * math.math.cos(variables["pi"] * 0)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_E_react_t(p: dict) -> float:
    """Inline equation: E_react_t"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["E_react"] * math.math.exp(-0.0005) * 1.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_factor_1(p: dict) -> float:
    """Inline equation: factor"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (1 + 1e13 * 0.01) * (1 + 0.01)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_rho_UA_SCm_1(p: dict) -> float:
    """Inline equation: rho_UA_SCm"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1e-23 * pow(0.1, n) * math.math.exp(-1) * math.math.exp(-variables["pi"])
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_omega_H_t(p: dict) -> float:
    """Inline equation: omega_H_t"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["omega_H"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_E_react_t_1(p: dict) -> float:
    """Inline equation: E_react_t"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["E_react"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_Um_val(p: dict) -> float:
    """Inline equation: Um_val"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = computeUm(variables["t"])
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_ug3_4(p: dict) -> float:
    """Inline equation: ug3"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = computeUg3(t, 1e3, 0.0, 1)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_E(p: dict) -> float:
    """Inline equation: E"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = computeElectricField()
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_eta_4(p: dict) -> float:
    """Inline equation: eta"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = computeNeutronRate(t)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_S2(p: dict) -> float:
    """Inline equation: S2"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = computePiSeriesS(2)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_buoy_sum(p: dict) -> float:
    """Inline equation: buoy_sum"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = computeBuoyancySeries(variables["x_buoy"])
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_Q(p: dict) -> float:
    """Inline equation: Q"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = computeTransmutationQ()
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_ug3_5(p: dict) -> float:
    """Inline equation: ug3"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = computeUg3(t, variables["r"], variables["theta"], variables["n_ug"])
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_br_ww(p: dict) -> float:
    """Inline equation: br_ww"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = computeBranchingRatio("WW")
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_vac_ratio_2(p: dict) -> float:
    """Inline equation: vac_ratio"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = computeVacDensityRatio()
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_q_scale(p: dict) -> float:
    """Inline equation: q_scale"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = computeQuantumScalingFactor()
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_sin_term_2(p: dict) -> float:
    """Inline equation: sin_term"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = math.math.sin(variables["k"] * r - variables["omega"] * t)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_exp_non(p: dict) -> float:
    """Inline equation: exp_non"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = computeNonLocalExp(variables["alpha"], r, variables["r0"])
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_partial_t(p: dict) -> float:
    """Inline equation: partial_t"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = -variables["omega"] * std.imag(psi)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_grad_term(p: dict) -> float:
    """Inline equation: grad_term"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["omega_m"] * variables["r"] * std.real(psi)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_cos_term_9(p: dict) -> float:
    """Inline equation: cos_term"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = math.math.cos(variables["pi"] * t_n)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_ratio_6(p: dict) -> float:
    """Inline equation: ratio"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["rho_vac_SCm"] / variables["rho_vac_UA"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_omega_i_t(p: dict) -> float:
    """Inline equation: omega_i_t"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["omega_i"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_pot(p: dict) -> float:
    """Inline equation: pot"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.5 * variables["m"] * pow(variables["omega_r"], 2) * pow(x, 2)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_quant(p: dict) -> float:
    """Inline equation: quant"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["hbar"] * variables["omega_r"] * (n + 0.5)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_E0(p: dict) -> float:
    """Inline equation: E0"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["E_aether"] * variables["V"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_q_factor(p: dict) -> float:
    """Inline equation: q_factor"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["quantum_state_factor"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_rad_factor(p: dict) -> float:
    """Inline equation: rad_factor"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["radial_factor"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_wave_factor(p: dict) -> float:
    """Inline equation: wave_factor"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["wave_type_factor"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_higgs_f(p: dict) -> float:
    """Inline equation: higgs_f"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["higgs_factor"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_prec_f(p: dict) -> float:
    """Inline equation: prec_f"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["precession_factor"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_scale_f(p: dict) -> float:
    """Inline equation: scale_f"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["scaling_factor"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_non_local_2(p: dict) -> float:
    """Inline equation: non_local"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = computeNonLocalExp(0.00005, t, 0.0)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_exp_cos_2(p: dict) -> float:
    """Inline equation: exp_cos"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1 - math.math.exp(-0.00005 * t) * math.math.cos(variables["pi"] * 0)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_phi_tw(p: dict) -> float:
    """Inline equation: phi_tw"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = computeTwistPhase(t)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_B_p(p: dict) -> float:
    """Inline equation: B_p"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = computePseudoMonopoleB(variables["r"])
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_Ui_2(p: dict) -> float:
    """Inline equation: Ui"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = computeUniversalInertia(t, variables["t_n"])
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_E_b(p: dict) -> float:
    """Inline equation: E_b"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = computeBosonicEnergy(0.0, 0)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_H_m(p: dict) -> float:
    """Inline equation: H_m"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = computeMagneticHamiltonian(variables["mu_mag"], variables["B"])
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_Um_v(p: dict) -> float:
    """Inline equation: Um_v"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = computeUm(t, variables["r"], 1)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_Ug3_v(p: dict) -> float:
    """Inline equation: Ug3_v"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = computeUg3(t, variables["r"], variables["theta"], 1)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_qsf(p: dict) -> float:
    """Inline equation: qsf"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["quantum_state_factor"] * (n_levels / 4.0)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_rdf(p: dict) -> float:
    """Inline equation: rdf"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["radial_factor"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_wtff(p: dict) -> float:
    """Inline equation: wtff"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["wave_type_factor"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_hff(p: dict) -> float:
    """Inline equation: hff"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["higgs_factor"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_ptf(p: dict) -> float:
    """Inline equation: ptf"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["precession_factor"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_qsff(p: dict) -> float:
    """Inline equation: qsff"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["scaling_factor"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_E_w(p: dict) -> float:
    """Inline equation: E_w"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = E0 * qsf * rdf * wtff * hff * ptf * qsff
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_proofset(p: dict) -> float:
    """Inline equation: proofset"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = computeThreeLegProofset(E_w)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_vac_r(p: dict) -> float:
    """Inline equation: vac_r"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = computeVacDensityRatio()
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_q_s(p: dict) -> float:
    """Inline equation: q_s"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = computeQuantumScalingFactor()
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_Ug3_v_1(p: dict) -> float:
    """Inline equation: Ug3_v"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = computeUg3(t, variables["r"], 0.0, 1)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_spatial_f(p: dict) -> float:
    """Inline equation: spatial_f"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["spatial_config"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_comp_f(p: dict) -> float:
    """Inline equation: comp_f"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["compression"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_higgs_f_1(p: dict) -> float:
    """Inline equation: higgs_f"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = computeHiggsFactor()
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_prec_f_1(p: dict) -> float:
    """Inline equation: prec_f"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = computePrecessionFactor()
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_q_scale_1(p: dict) -> float:
    """Inline equation: q_scale"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = computeQuantumScaling()
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_cons_leg(p: dict) -> float:
    """Inline equation: cons_leg"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = computeConservation(E_input, E_input)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_vac_leg(p: dict) -> float:
    """Inline equation: vac_leg"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = computeVacDensityRatio()
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_q_leg(p: dict) -> float:
    """Inline equation: q_leg"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = computeQuantumEnergy()
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_non_local_3(p: dict) -> float:
    """Inline equation: non_local"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = math.math.exp(-(variables["pi"] + t))
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_cos_term_10(p: dict) -> float:
    """Inline equation: cos_term"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = math.math.cos(2.5e-6 * t * variables["pi"])
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_E_sp(p: dict) -> float:
    """Inline equation: E_sp"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = computeEspace(static_cast(variables["layers"]))
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_proofset_1(p: dict) -> float:
    """Inline equation: proofset"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = computeThreeLegProofset(E_sp)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_E_sp_1(p: dict) -> float:
    """Inline equation: E_sp"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = E0_val * spatial_f * comp_f * layer_f * higgs_f * prec_f * q_scale
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_cons_leg_1(p: dict) -> float:
    """Inline equation: cons_leg"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = computeConservation(E_sp, E_sp)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_proofset_2(p: dict) -> float:
    """Inline equation: proofset"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = E_sp * cons_leg + vac_leg + q_leg
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_Hz_kms_6(p: dict) -> float:
    """Inline equation: Hz_kms"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["H0"] * math.math.sqrt(variables["Omega_m"] * pow(1.0 + z_val, 3) + variables["Omega_Lambda"])
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_f_wind(p: dict) -> float:
    """Inline equation: f_wind"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["F_wind"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_f_erode(p: dict) -> float:
    """Inline equation: f_erode"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["F_erode"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_f_lensing(p: dict) -> float:
    """Inline equation: f_lensing"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["F_lensing"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_f_decay(p: dict) -> float:
    """Inline equation: f_decay"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["F_decay"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_f_coll(p: dict) -> float:
    """Inline equation: f_coll"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["F_coll"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_f_merge(p: dict) -> float:
    """Inline equation: f_merge"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["F_merge"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_mag_term(p: dict) -> float:
    """Inline equation: mag_term"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["q"] * variables["v"] * variables["B"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_standing_1(p: dict) -> float:
    """Inline equation: standing"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 2 * variables["A"] * math.math.cos(variables["k"] * variables["x"]) * math.math.cos(variables["omega"] * t)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_quantum_wave(p: dict) -> float:
    """Inline equation: quantum_wave"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (2 * variables["pi"] / 13.8) * exp_term.real()
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_psi_tot(p: dict) -> float:
    """Inline equation: psi_tot"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = computePsiTotal(variables["t"])
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_Htz(p: dict) -> float:
    """Inline equation: Htz"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = computeHtz(variables["z"])
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_g_base_12(p: dict) -> float:
    """Inline equation: g_base"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (variables["G"] * variables["M"] * m_factor / (variables["r"] * variables["r"])) * expansion * sc_correction * (1.0 + f_env)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_expected_1(p: dict) -> float:
    """Inline equation: expected"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = G * test_sys.M / (test_sys.r * test_sys.r)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_result_9(p: dict) -> float:
    """Inline equation: result"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = compute_compressed_base(test_sys)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_result_10(p: dict) -> float:
    """Inline equation: result"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = compute_compressed_expansion(test_sys)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_result_11(p: dict) -> float:
    """Inline equation: result"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = compute_compressed_super_adj(test_sys)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_result_12(p: dict) -> float:
    """Inline equation: result"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = compute_compressed_fluid(test_sys)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_result_13(p: dict) -> float:
    """Inline equation: result"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = compute_compressed_env()
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_result_14(p: dict) -> float:
    """Inline equation: result"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = compute_compressed_Ug_sum()
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_expected_2(p: dict) -> float:
    """Inline equation: expected"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1.1e-52 * c * c / 3.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_result_15(p: dict) -> float:
    """Inline equation: result"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = compute_compressed_cosm()
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_expected_3(p: dict) -> float:
    """Inline equation: expected"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (1.0546e-34 / 1e-68) * 2.176e-18 * (2 * math.pi / 4.35e17)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_result_16(p: dict) -> float:
    """Inline equation: result"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = compute_compressed_quantum()
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_expected_4(p: dict) -> float:
    """Inline equation: expected"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = test_sys.M * (1e-5 + 3 * G * test_sys.M / (1e4 * 1e4 * 1e4))
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_result_17(p: dict) -> float:
    """Inline equation: result"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = compute_compressed_perturbation(test_sys)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_FDPM_2(p: dict) -> float:
    """Inline equation: FDPM"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = test_sys.I * test_sys.A * (test_sys.omega1 - test_sys.omega2)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_expected_5(p: dict) -> float:
    """Inline equation: expected"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = FDPM * res.fDPM * res.Evac_neb * res.c_res * test_sys.Vsys
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_result_18(p: dict) -> float:
    """Inline equation: result"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = compute_aDPM(test_sys, res)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_result_19(p: dict) -> float:
    """Inline equation: result"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = compute_aTHz(aDPM, test_sys, res)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_result_20(p: dict) -> float:
    """Inline equation: result"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = compute_avac_diff(aDPM, test_sys, res)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_result_21(p: dict) -> float:
    """Inline equation: result"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = compute_asuper_freq(aDPM, res)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_result_22(p: dict) -> float:
    """Inline equation: result"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = compute_aaether_res(aDPM, res)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_result_23(p: dict) -> float:
    """Inline equation: result"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = compute_Ug4i(aDPM, test_sys, res)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_result_24(p: dict) -> float:
    """Inline equation: result"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = compute_aquantum_freq(aDPM, res)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_result_25(p: dict) -> float:
    """Inline equation: result"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = compute_aAether_freq(aDPM, res)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_result_26(p: dict) -> float:
    """Inline equation: result"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = compute_afluid_freq(test_sys, res)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_F_tidal_3(p: dict) -> float:
    """Inline equation: F_tidal"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (variables["G"] * variables["M_NGC5195"]) / (variables["d_NGC5195"] * variables["d_NGC5195"])
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_F_SF_1(p: dict) -> float:
    """Inline equation: F_SF"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["k_SF"] * variables["SFR"] / 1.989e30
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_mu_dipole(p: dict) -> float:
    """Inline equation: mu_dipole"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["I_dipole"] * variables["A_dipole"] * variables["omega_spin"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_B_super(p: dict) -> float:
    """Inline equation: B_super"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["mu_0"] * variables["H_aether"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_E_react_5(p: dict) -> float:
    """Inline equation: E_react"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1e46 * math.math.exp(-0.0005 * t)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_psi_int_1(p: dict) -> float:
    """Inline equation: psi_int"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = computePsiIntegral(r, variables["t"])
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_pert_4(p: dict) -> float:
    """Inline equation: pert"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["delta_rho_over_rho"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_Ug_base(p: dict) -> float:
    """Inline equation: Ug_base"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (variables["G"] * variables["M"]) / (r * r)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_Hz_2(p: dict) -> float:
    """Inline equation: Hz"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = computeHtz(variables["z"])
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_g_base_13(p: dict) -> float:
    """Inline equation: g_base"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (variables["G"] * variables["M"] * m_factor / (r * r)) * expansion * sc_correction * (1.0 + f_env) * tr_factor
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_ug_sum(p: dict) -> float:
    """Inline equation: ug_sum"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = computeUgSum(r) - g_base
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_quantum_term_2(p: dict) -> float:
    """Inline equation: quantum_term"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = computeQuantumTerm(variables["t_Hubble"], r)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_F_tidal_4(p: dict) -> float:
    """Inline equation: F_tidal"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (variables["G"] * variables["M_spiral"]) / (variables["d_spiral"] * variables["d_spiral"])
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_F_cluster_1(p: dict) -> float:
    """Inline equation: F_cluster"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["k_cluster"] * (variables["M_cluster"] / 1.989e30)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_m_factor_2(p: dict) -> float:
    """Inline equation: m_factor"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1.0 + m_merge / variables["M0"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_grad_term_1(p: dict) -> float:
    """Inline equation: grad_term"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["M_s"] / (r * r * r)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_exp_decay(p: dict) -> float:
    """Inline equation: exp_decay"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = math.math.exp(-variables["alpha"] * t)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_cos_phase(p: dict) -> float:
    """Inline equation: cos_phase"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = math.math.cos(variables["pi"] * variables["t_n"])
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_delta_4(p: dict) -> float:
    """Inline equation: delta"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["delta_def"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_rho_dust_1(p: dict) -> float:
    """Inline equation: rho_dust"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = computeRhodust(r, t)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_i_base(p: dict) -> float:
    """Inline equation: i_base"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = computeIechoBase(r)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_trz(p: dict) -> float:
    """Inline equation: trz"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = computeTRZCorrection()
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_ua_sc_1(p: dict) -> float:
    """Inline equation: ua_sc"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = computeUAscCorrection()
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_r_40(p: dict) -> float:
    """Inline equation: r"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = mod.variables["c"] * t
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_I_2(p: dict) -> float:
    """Inline equation: I"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = mod.computeIecho(t, r)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_F_bar(p: dict) -> float:
    """Inline equation: F_bar"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.1 * (variables["G"] * variables["M"]) / (variables["r"] * variables["r"])
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_F_wave(p: dict) -> float:
    """Inline equation: F_wave"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["rho_fluid"] * pow(variables["v_arm"], 2)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_M_bar(p: dict) -> float:
    """Inline equation: M_bar"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.2 * variables["M"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_r_bar(p: dict) -> float:
    """Inline equation: r_bar"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.3 * variables["r"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_yr_s(p: dict) -> float:
    """Inline equation: yr_s"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["year_to_s"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_Hz_kms_7(p: dict) -> float:
    """Inline equation: Hz_kms"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["H0"] * math.math.sqrt(variables["Omega_m"] * pow(1 + z_val, 3) + variables["Omega_Lambda"])
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_psi_1(p: dict) -> float:
    """Inline equation: psi"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = computePsiTotal(variables["t"])
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_curv_2(p: dict) -> float:
    """Inline equation: curv"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 3 * variables["G"] * variables["M"] / (pow(variables["r"], 3))
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_rho_d(p: dict) -> float:
    """Inline equation: rho_d"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["rho_fluid"] * math.math.exp(-1.0 * (variables["G"] * variables["M"] / (variables["r"] * variables["r"])))
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_sc(p: dict) -> float:
    """Inline equation: sc"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1 - variables["B"] / variables["B_crit"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_msf(p: dict) -> float:
    """Inline equation: msf"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = computeMsfFactor(t)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_g_base_14(p: dict) -> float:
    """Inline equation: g_base"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (variables["G"] * variables["M"] * mfact / (variables["r"] * variables["r"])) * expansion * sc * (1 + fenv)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_lambda_t(p: dict) -> float:
    """Inline equation: lambda_t"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["Lambda"] * variables["c"] * variables["c"] / 3
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_qterm(p: dict) -> float:
    """Inline equation: qterm"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = computeQuantumTerm(variables["t_Hubble"])
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_fterm(p: dict) -> float:
    """Inline equation: fterm"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = computeFluidTerm(g_base)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_g_5(p: dict) -> float:
    """Inline equation: g"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = mod.computeG(1e9*3.156e7)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_F_wind(p: dict) -> float:
    """Inline equation: F_wind"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["rho_fluid"] * pow(variables["v_wind"], 2)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_F_erode(p: dict) -> float:
    """Inline equation: F_erode"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.05 * (t / (3e6 * variables["year_to_s"]))
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_E_react_6(p: dict) -> float:
    """Inline equation: E_react"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1e40 * math.math.exp(-0.0005 * t)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_F_tidal_5(p: dict) -> float:
    """Inline equation: F_tidal"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (variables["G"] * variables["M_dwarf"]) / (variables["d_dwarf"] * variables["d_dwarf"])
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_F_tail(p: dict) -> float:
    """Inline equation: F_tail"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["rho_fluid"] * pow(variables["v_tail"], 2)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_F_tidal_6(p: dict) -> float:
    """Inline equation: F_tidal"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (variables["G"] * variables["M_B"]) / (variables["d"] * variables["d"])
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_F_bridge(p: dict) -> float:
    """Inline equation: F_bridge"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["rho_fluid"] * pow(variables["v_rel"], 2)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_h_eff(p: dict) -> float:
    """Inline equation: h_eff"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = computeHeffz(variables["z"])
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_ug2_thz(p: dict) -> float:
    """Inline equation: ug2_thz"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = computeUg2THz(variables["t"])
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_psi_2(p: dict) -> float:
    """Inline equation: psi"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = computePsiIntegral(variables["r"], t)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_f_super_1(p: dict) -> float:
    """Inline equation: f_super"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = computeFreqSuper(t)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_f_react_1(p: dict) -> float:
    """Inline equation: f_react"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = computeFreqReact(t)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_rho_12(p: dict) -> float:
    """Inline equation: rho"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["rho_fil"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_f_fluid_2(p: dict) -> float:
    """Inline equation: f_fluid"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = computeFreqFluid(rho)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_f_quantum_1(p: dict) -> float:
    """Inline equation: f_quantum"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = computeFreqQuantum(unc)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_f_aether(p: dict) -> float:
    """Inline equation: f_aether"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = computeFreqAether()
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_f_res_6(p: dict) -> float:
    """Inline equation: f_res"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = computeResonanceTerm(t) / (2 * variables["pi"])
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_f_thz_1(p: dict) -> float:
    """Inline equation: f_thz"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = computeTHzHoleTerm(t)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_f_total(p: dict) -> float:
    """Inline equation: f_total"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = f_super + f_fluid + f_quantum + f_aether + f_react + f_res + f_dpm + f_thz + ug4i
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_psi_3(p: dict) -> float:
    """Inline equation: psi"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = computePsiIntegral(variables["r_init"], t)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_f_fluid_3(p: dict) -> float:
    """Inline equation: f_fluid"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = computeFreqFluid(variables["rho"])
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_F_collapse(p: dict) -> float:
    """Inline equation: F_collapse"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["rho_gas"] * pow(variables["v_rad"], 2)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_ug3_6(p: dict) -> float:
    """Inline equation: ug3"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = computeUg3(variables["t"])
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_ui(p: dict) -> float:
    """Inline equation: ui"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = computeUi(variables["t"])
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_um_2(p: dict) -> float:
    """Inline equation: um"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = computeUm(variables["t"])
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_H0_5(p: dict) -> float:
    """Inline equation: H0"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 70.0 / (3.086e19 * 1e3)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_rho_vac_ua_prime(p: dict) -> float:
    """Inline equation: rho_vac_ua_prime"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["rho_vac_UA_prime"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_rho_vac_scm(p: dict) -> float:
    """Inline equation: rho_vac_scm"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["rho_vac_SCm"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_rho_vac_ua(p: dict) -> float:
    """Inline equation: rho_vac_ua"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["rho_vac_UA"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_term2_13(p: dict) -> float:
    """Inline equation: term2"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1.0 - math.math.exp(-variables["gamma"] * t / (24 * 3600) * math.math.cos(variables["pi"] * variables["t_n"]))
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_factor_2(p: dict) -> float:
    """Inline equation: factor"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["P_scm"] * computeEReact(t) * (1.0 + 1e13 * variables["f_heaviside"]) * (1.0 + variables["f_quasi"])
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_r_41(p: dict) -> float:
    """Inline equation: r"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["R_bulge"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_ug1_1(p: dict) -> float:
    """Inline equation: ug1"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = computeUg1(t, r, M_s, n)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_omega_s_5(p: dict) -> float:
    """Inline equation: omega_s"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = computeOmegaSGalactic(sigma_val)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_g_total(p: dict) -> float:
    """Inline equation: g_total"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = um + ug1 + omega_s * variables["k_galactic"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_g_6(p: dict) -> float:
    """Inline equation: g"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = mod.computeG(t, sigma)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_G_F_scaled(p: dict) -> float:
    """Inline equation: G_F_scaled"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["G_F"] * pow(1.973e-7, -2)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_m_tilde(p: dict) -> float:
    """Inline equation: m_tilde"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = beta_val * variables["m_e"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_theta_3(p: dict) -> float:
    """Inline equation: theta"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_mu_1(p: dict) -> float:
    """Inline equation: mu"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (1e3 + 0.4 * math.math.sin(2 * variables["pi"] / 3.96e8 * t)) * 3.38e20
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_term2_14(p: dict) -> float:
    """Inline equation: term2"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1.0 - math.math.exp(-variables["gamma"] * t / 86400 * math.math.cos(variables["pi"] * variables["t_n"]))
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_delta_n(p: dict) -> float:
    """Inline equation: delta_n"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["phi"] * pow(2 * variables["pi"], n / 6.0)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_W(p: dict) -> float:
    """Inline equation: W"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["Q_threshold"] + computeElectricField(variables["Omega"]) * variables["e"] * variables["r"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_eta_5(p: dict) -> float:
    """Inline equation: eta"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = mod.computeNeutronRate(t)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_term2_15(p: dict) -> float:
    """Inline equation: term2"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1.0 - math.math.exp(-variables["gamma"] * (t / 86400) * math.math.cos(variables["pi"] * variables["t_n"]))
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_non_local_4(p: dict) -> float:
    """Inline equation: non_local"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = computeNonLocalExp(n, t)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_exp_inner(p: dict) -> float:
    """Inline equation: exp_inner"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = math.math.exp(-variables["pi"] - t / variables["year_to_s"])
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_base_11(p: dict) -> float:
    """Inline equation: base"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = pow(variables["S_S_q"], n) * pow(2, 6)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_eta_6(p: dict) -> float:
    """Inline equation: eta"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = mod.computeEta(t, n)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_Hz_3(p: dict) -> float:
    """Inline equation: Hz"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["H0"] * math.math.sqrt(variables["Omega_m"] * pow(1 + z, 3) + variables["Omega_Lambda"])
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_cos_term_11(p: dict) -> float:
    """Inline equation: cos_term"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 2 * A * math.math.cos(k * x) * math.math.cos(omega * t)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_v_3(p: dict) -> float:
    """Inline equation: v"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["v_wind"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_Hz_t(p: dict) -> float:
    """Inline equation: Hz_t"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = computeH(t, variables["z"])
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_lambda_term_2(p: dict) -> float:
    """Inline equation: lambda_term"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = computeLambdaTerm()
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_quantum_term_3(p: dict) -> float:
    """Inline equation: quantum_term"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = computeQuantumTerm()
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_sys_term(p: dict) -> float:
    """Inline equation: sys_term"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = computeSystemSpecificTerm(t)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_omega_13(p: dict) -> float:
    """Inline equation: omega"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["f_osc"] * 2 * variables["pi"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_aSuperFreq(p: dict) -> float:
    """Inline equation: aSuperFreq"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = computeASuperFreq()
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_aAetherRes(p: dict) -> float:
    """Inline equation: aAetherRes"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = computeAAetherRes()
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_aQuantumFreq(p: dict) -> float:
    """Inline equation: aQuantumFreq"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = computeAQuantumFreq()
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_aAetherFreq(p: dict) -> float:
    """Inline equation: aAetherFreq"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = computeAAetherFreq()
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_aFluidFreq(p: dict) -> float:
    """Inline equation: aFluidFreq"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = computeAFluidFreq()
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_g_comp_2(p: dict) -> float:
    """Inline equation: g_comp"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = mod.computeG_compressed(t)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_g_res_1(p: dict) -> float:
    """Inline equation: g_res"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = mod.computeG_resonance(t)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_Hz_val(p: dict) -> float:
    """Inline equation: Hz_val"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = computeHz(variables["z"])
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_evac_neb(p: dict) -> float:
    """Inline equation: evac_neb"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["Evac_neb"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_evac_ism(p: dict) -> float:
    """Inline equation: evac_ism"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["Evac_ISM"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_delta_evac(p: dict) -> float:
    """Inline equation: delta_evac"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["Delta_Evac"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_f_super_2(p: dict) -> float:
    """Inline equation: f_super"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["F_super"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_ua_scm_1(p: dict) -> float:
    """Inline equation: ua_scm"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["UA_SCm"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_omega_i_1(p: dict) -> float:
    """Inline equation: omega_i"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["omega_i"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_f_react_2(p: dict) -> float:
    """Inline equation: f_react"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["f_react"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_f_quantum_2(p: dict) -> float:
    """Inline equation: f_quantum"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["f_quantum"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_f_aether_1(p: dict) -> float:
    """Inline equation: f_aether"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["f_Aether"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_f_fluid_4(p: dict) -> float:
    """Inline equation: f_fluid"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["f_fluid"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_a_super_freq_1(p: dict) -> float:
    """Inline equation: a_super_freq"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = computeASuperFreq()
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_a_aether_res_2(p: dict) -> float:
    """Inline equation: a_aether_res"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = computeAAetherRes()
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_a_quantum_freq_1(p: dict) -> float:
    """Inline equation: a_quantum_freq"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = computeAQuantumFreq()
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_a_aether_freq_1(p: dict) -> float:
    """Inline equation: a_aether_freq"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = computeAAetherFreq()
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_a_fluid_freq_1(p: dict) -> float:
    """Inline equation: a_fluid_freq"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = computeAFluidFreq()
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_g_7(p: dict) -> float:
    """Inline equation: g"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = mod.computeG_resonance(t)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_one_plus_z(p: dict) -> float:
    """Inline equation: one_plus_z"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1.0 + variables["z"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_Hz_kms_8(p: dict) -> float:
    """Inline equation: Hz_kms"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["H0"] * math.math.sqrt(variables["Omega_m"] * pow(one_plus_z, 3) + variables["Omega_Lambda"])
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_force_per_area(p: dict) -> float:
    """Inline equation: force_per_area"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["rho_dust"] * pow(variables["v_orbit"], 2)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_a_dust_base(p: dict) -> float:
    """Inline equation: a_dust_base"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = force_per_area / variables["rho_mass"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_mag_vB(p: dict) -> float:
    """Inline equation: mag_vB"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["v_orbit"] * variables["B"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_force_2(p: dict) -> float:
    """Inline equation: force"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["q"] * mag_vB
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_vac_ratio_3(p: dict) -> float:
    """Inline equation: vac_ratio"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["rho_vac_UA"] / variables["rho_vac_SCm"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_g_grav(p: dict) -> float:
    """Inline equation: g_grav"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (variables["G"] * variables["M"] / (variables["r"] * variables["r"])) * expansion_factor * tr_factor
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_g_BH_1(p: dict) -> float:
    """Inline equation: g_BH"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["G"] * variables["M_BH"] / (variables["r_BH"] * variables["r_BH"])
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_t_10(p: dict) -> float:
    """Inline equation: t"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = i * 2.0 * variables["Gyr"] * variables["year_to_s"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_t_11(p: dict) -> float:
    """Inline equation: t"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 10.0 * 1e9 * 3.156e7
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_pert_5(p: dict) -> float:
    """Inline equation: pert"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = mod.computePerturbation()
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_SCm_contrib_9(p: dict) -> float:
    """Inline equation: SCm_contrib"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_SCm_contrib_10(p: dict) -> float:
    """Inline equation: SCm_contrib"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_SCm_contrib_11(p: dict) -> float:
    """Inline equation: SCm_contrib"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_FU_2(p: dict) -> float:
    """Inline equation: FU"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = CoAnQi.Physics.compute_FU(body, r, t, tn, theta)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_compressed_g_1(p: dict) -> float:
    """Inline equation: compressed_g"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = CoAnQi.MUGE.compute_compressed_MUGE(sys)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_resonance_g_1(p: dict) -> float:
    """Inline equation: resonance_g"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 0.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_ua_base(p: dict) -> float:
    """Inline equation: ua_base"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["UA_amount"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_breakdown(p: dict) -> float:
    """Inline equation: breakdown"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = math.math.exp(-variables["decay_rate"] * variables["t_pre_bigbang"])
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_attraction(p: dict) -> float:
    """Inline equation: attraction"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["a_over_b"] * (scm * ua) / (variables["r"] * variables["r"])
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_M_bh_over_d_g(p: dict) -> float:
    """Inline equation: M_bh_over_d_g"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["M_bh"] / variables["d_g"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_mod_factor_1(p: dict) -> float:
    """Inline equation: mod_factor"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = mod.computeModulationFactor()
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_sum_3(p: dict) -> float:
    """Inline equation: sum"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = mod.computeSumK_Ugi()
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_mu_j_7(p: dict) -> float:
    """Inline equation: mu_j"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = computeMu_j(j, variables["t_n"])
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_exp_term_3(p: dict) -> float:
    """Inline equation: exp_term"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = math.math.exp( - variables["gamma"] * t * math.math.cos(variables["pi"] * variables["t_n"]) )
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_phi_hat_4(p: dict) -> float:
    """Inline equation: phi_hat"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["phi_hat_1"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_heaviside_factor_1(p: dict) -> float:
    """Inline equation: heaviside_factor"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1.0 + 1e13 * variables["f_Heaviside"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_quasi_factor(p: dict) -> float:
    """Inline equation: quasi_factor"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = 1.0 + variables["f_quasi"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_cos_term_12(p: dict) -> float:
    """Inline equation: cos_term"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = math.math.cos(variables["Omega_g"] * variables["t_n"] * variables["pi"])
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_rho_sum_1(p: dict) -> float:
    """Inline equation: rho_sum"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["rho_vac_SCm"] + variables["rho_vac_UA"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_M_s_over_d_g(p: dict) -> float:
    """Inline equation: M_s_over_d_g"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["M_s"] / variables["d_g"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_um_contrib(p: dict) -> float:
    """Inline equation: um_contrib"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = computeUmContribution(j, t)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_ug3_7(p: dict) -> float:
    """Inline equation: ug3"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = computeUg3Contribution()
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_dg_ly(p: dict) -> float:
    """Inline equation: dg_ly"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = mod.computeDgInLy()
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_factor_3(p: dict) -> float:
    """Inline equation: factor"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = pow(10.0, computeDeltaM_BH())
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_initial(p: dict) -> float:
    """Inline equation: initial"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["M_bh_initial"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_M_bh_6(p: dict) -> float:
    """Inline equation: M_bh"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = computeM_bh_final()
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_f_feedback_1(p: dict) -> float:
    """Inline equation: f_feedback"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = computeF_feedback()
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_exp_term_4(p: dict) -> float:
    """Inline equation: exp_term"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = math.math.exp( - alpha * t )
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_cos_term_13(p: dict) -> float:
    """Inline equation: cos_term"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = math.math.cos( pi * t_n )
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_orig_f_2(p: dict) -> float:
    """Inline equation: orig_f"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = variables["f_feedback"]
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_result_27(p: dict) -> float:
    """Inline equation: result"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = computeU_g4(t, t_n)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_u_with(p: dict) -> float:
    """Inline equation: u_with"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = computeU_g4(t, t_n)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_u_without(p: dict) -> float:
    """Inline equation: u_without"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = computeU_g4_no_feedback(t, t_n)
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_delta_percent(p: dict) -> float:
    """Inline equation: delta_percent"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = ((u_with - u_without) / u_without) * 100.0
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_m_final(p: dict) -> float:
    """Inline equation: m_final"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = mod.computeM_bh_final()
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_norm_factor(p: dict) -> float:
    """Inline equation: norm_factor"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (variables["rho_vac_SCm"] + variables["rho_vac_UA"])
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0

def _compute_inline_norm(p: dict) -> float:
    """Inline equation: norm"""
    # Type: inline
    r = p.get("r", 1.0)
    t = p.get("t", 0.0)
    M = p.get("M", CONSTANTS.get("M_sun", 1.989e30))
    G = CONSTANTS.get("G", 6.6743e-11)
    c = CONSTANTS.get("c", 2.998e8)
    try:
        result = (variables["rho_vac_SCm"] + variables["rho_vac_UA"])
        return float(result) if result == result else 0.0
    except Exception:
        return 0.0


# =============================================================================
# EQUATION REGISTRY
# =============================================================================

ALL_EQUATIONS = {
    "DynamicVacuumTerm": PhysicsTerm("DynamicVacuumTerm", "Time-varying vacuum energy", _compute_DynamicVacuumTerm),
    "QuantumCouplingTerm": PhysicsTerm("QuantumCouplingTerm", "Non-local quantum effects", _compute_QuantumCouplingTerm),
    "DarkMatterHaloTerm": PhysicsTerm("DarkMatterHaloTerm", "NFW dark matter halo contribution (G*M*ln(1+x)/(r*x))", _compute_DarkMatterHaloTerm),
    "VacuumEnergyTerm": PhysicsTerm("VacuumEnergyTerm", "Vacuum energy fluctuation (lambda*E_vac*(1 + 0.1*sin(t)))", _compute_VacuumEnergyTerm),
    "QuantumEntanglementTerm": PhysicsTerm("QuantumEntanglementTerm", "Non-local quantum entanglement (hbar^2/(M*r^2)*cos(t/1e6))", _compute_QuantumEntanglementTerm),
    "CosmicNeutrinoTerm": PhysicsTerm("CosmicNeutrinoTerm", "CNB energy density contribution (n_nu * k_B * T_cnb / r^2)", _compute_CosmicNeutrinoTerm),
    "MultiSystemUQFFTerm": PhysicsTerm("MultiSystemUQFFTerm", "Multi-system UQFF (NGC685/3507/3511/AT2024tvd) with DPM resonance, LENR, gravity", _compute_MultiSystemUQFFTerm),
    "DPMResonanceTerm": PhysicsTerm("DPMResonanceTerm", "DPM quantum magnetic resonance (Lande factor based)", _compute_DPMResonanceTerm),
    "LENRExtendedTerm": PhysicsTerm("LENRExtendedTerm", "Extended LENR with frequency ratio squared scaling", _compute_LENRExtendedTerm),
    "SMBHAccretionTerm": PhysicsTerm("SMBHAccretionTerm", "SMBH accretion disk luminosity (radiative efficiency model)", _compute_SMBHAccretionTerm),
    "TDETerm": PhysicsTerm("TDETerm", "Tidal Disruption Event with exponential + power-law decay", _compute_TDETerm),
    "NebulaUQFFTerm": PhysicsTerm("NebulaUQFFTerm", "Nebula UQFF with gas expansion, ionization, and radiation pressure", _compute_NebulaUQFFTerm),
    "GasIonizationTerm": PhysicsTerm("GasIonizationTerm", "Gas ionization fraction (Stromgren sphere model)", _compute_GasIonizationTerm),
    "NebulaExpansionTerm": PhysicsTerm("NebulaExpansionTerm", "Nebula expansion with time-dependent radius and density", _compute_NebulaExpansionTerm),
    "BuoyancyUQFFTerm": PhysicsTerm("BuoyancyUQFFTerm", "Buoyancy UQFF for ", _compute_BuoyancyUQFFTerm),
    "InflationBuoyancyTerm": PhysicsTerm("InflationBuoyancyTerm", "Inflation-driven buoyancy force", _compute_InflationBuoyancyTerm),
    "SuperconductiveTerm": PhysicsTerm("SuperconductiveTerm", "Time-dependent superconductivity with oscillating vacuum density", _compute_SuperconductiveTerm),
    "NeutronScatteringTerm": PhysicsTerm("NeutronScatteringTerm", "Neutron scattering cross-section contribution", _compute_NeutronScatteringTerm),
    "AstroSystemUQFFTerm": PhysicsTerm("AstroSystemUQFFTerm", "Astro System UQFF for ", _compute_AstroSystemUQFFTerm),
    "DipoleVortexTerm": PhysicsTerm("DipoleVortexTerm", "Dipole vortex species determination with golden ratio cycle", _compute_DipoleVortexTerm),
    "QuantumState26Term": PhysicsTerm("QuantumState26Term", "26-state quantum system with alphabet-like scaling", _compute_QuantumState26Term),
    "TriadicScaleTerm": PhysicsTerm("TriadicScaleTerm", "Triadic UQFF scaling with enhanced beta_i = 1.0", _compute_TriadicScaleTerm),
    "UQFFMasterTerm": PhysicsTerm("UQFFMasterTerm", "UQFF Master Force for ", _compute_UQFFMasterTerm),
    "ElectrostaticBarrierTerm": PhysicsTerm("ElectrostaticBarrierTerm", "Exponential barrier penetration term for electrostatic force", _compute_ElectrostaticBarrierTerm),
    "ElectricFieldTerm": PhysicsTerm("ElectricFieldTerm", "Electric field derived from Universal Magnetism", _compute_ElectricFieldTerm),
    "NeutronProductionTerm": PhysicsTerm("NeutronProductionTerm", "Neutron production rate with 26 quantum states", _compute_NeutronProductionTerm),
    "CelestialBodyTerm": PhysicsTerm("CelestialBodyTerm", "Source4 celestial body simulation for ", _compute_CelestialBodyTerm),
    "MUGETerm": PhysicsTerm("MUGETerm", "Source4 MUGE compressed gravity calculation", _compute_MUGETerm),
    "QuasarJetTerm": PhysicsTerm("QuasarJetTerm", "Source4 quasar jet Navier-Stokes simulation", _compute_QuasarJetTerm),
    "ReactorEnergyTerm": PhysicsTerm("ReactorEnergyTerm", "Source4: SCm reactor efficiency with exponential time decay", _compute_ReactorEnergyTerm),
    "MagneticDipoleTerm": PhysicsTerm("MagneticDipoleTerm", "Source4: Time-varying magnetic dipole moment with SCm enhancement", _compute_MagneticDipoleTerm),
    "MagneticJetFieldTerm": PhysicsTerm("MagneticJetFieldTerm", "Source4: Oscillating magnetic field in stellar jets", _compute_MagneticJetFieldTerm),
    "UnifiedBuoyancyTerm": PhysicsTerm("UnifiedBuoyancyTerm", "Source4: Galactic buoyancy force from Ug components", _compute_UnifiedBuoyancyTerm),
    "CompressedMUGEBaseTerm": PhysicsTerm("CompressedMUGEBaseTerm", "Source4: Compressed MUGE base gravitational term", _compute_CompressedMUGEBaseTerm),
    "CompressedMUGEExpansionTerm": PhysicsTerm("CompressedMUGEExpansionTerm", "Source4: Hubble expansion correction for compressed MUGE", _compute_CompressedMUGEExpansionTerm),
    "SuperconductiveAdjustmentTerm": PhysicsTerm("SuperconductiveAdjustmentTerm", "Source4: Magnetic superconductivity adjustment factor", _compute_SuperconductiveAdjustmentTerm),
    "CosmologicalConstantTerm": PhysicsTerm("CosmologicalConstantTerm", "Source4: Cosmological constant dark energy term", _compute_CosmologicalConstantTerm),
    "QuantumUncertaintyTerm": PhysicsTerm("QuantumUncertaintyTerm", "Source4: Heisenberg uncertainty principle quantum term", _compute_QuantumUncertaintyTerm),
    "FluidDynamicsTerm": PhysicsTerm("FluidDynamicsTerm", "Source4: Fluid dynamics contribution to gravity", _compute_FluidDynamicsTerm),
    "DensityPerturbationTerm": PhysicsTerm("DensityPerturbationTerm", "Source4: Visible + dark matter density perturbation", _compute_DensityPerturbationTerm),
    "UQFFModule5Term": PhysicsTerm("UQFFModule5Term", "Source5 self-expanding UQFF with dark matter halo and vacuum energy", _compute_UQFFModule5Term),
    "ResonanceMUGETerm": PhysicsTerm("ResonanceMUGETerm", "Source5 resonance-based MUGE with superconductive enhancement", _compute_ResonanceMUGETerm),
    "StateExportTerm": PhysicsTerm("StateExportTerm", "Source5 cross-module state export and communication", _compute_StateExportTerm),
    "TimeVaryingRotationTerm": PhysicsTerm("TimeVaryingRotationTerm", "Source5 time-varying rotation rate with cyclic modulation", _compute_TimeVaryingRotationTerm),
    "NavierStokesFluidTerm": PhysicsTerm("NavierStokesFluidTerm", "Source5 Navier-Stokes fluid dynamics with diffusion, advection, projection", _compute_NavierStokesFluidTerm),
    "SpacetimeMetricModulationTerm": PhysicsTerm("SpacetimeMetricModulationTerm", "Source5 spacetime metric modulation: A_mu_nu = g_mu_nu + eta * Ts00 * cos(pi*tn)", _compute_SpacetimeMetricModulationTerm),
    "FullUnifiedFieldTerm": PhysicsTerm("FullUnifiedFieldTerm", "Source5 complete unified field: FU = sum(Ugi) + sum(Ubi) + Um + A_mu_nu", _compute_FullUnifiedFieldTerm),
    "ResonanceMUGE_DPMTerm": PhysicsTerm("ResonanceMUGE_DPMTerm", "Source5 resonance MUGE DPM term: fundamental dipolar moment resonance", _compute_ResonanceMUGE_DPMTerm),
    "ResonanceMUGE_THzTerm": PhysicsTerm("ResonanceMUGE_THzTerm", "Source5 resonance MUGE THz frequency term", _compute_ResonanceMUGE_THzTerm),
    "VacuumEnergyDifferentialTerm": PhysicsTerm("VacuumEnergyDifferentialTerm", "Source5 vacuum energy differential between nebula and ISM", _compute_VacuumEnergyDifferentialTerm),
    "SuperconductiveFrequencyTerm": PhysicsTerm("SuperconductiveFrequencyTerm", "Source5 superconductive frequency enhancement term", _compute_SuperconductiveFrequencyTerm),
    "AetherResonanceTerm": PhysicsTerm("AetherResonanceTerm", "Source5 Universal Aether resonance with TRZ modulation", _compute_AetherResonanceTerm),
    "QuantumFrequencyTerm": PhysicsTerm("QuantumFrequencyTerm", "Source5 quantum frequency coupling term", _compute_QuantumFrequencyTerm),
    "AetherFrequencyTerm": PhysicsTerm("AetherFrequencyTerm", "Source5 Aether frequency term", _compute_AetherFrequencyTerm),
    "WormholeContributionTerm": PhysicsTerm("WormholeContributionTerm", "Source5 wormhole spacetime contribution term", _compute_WormholeContributionTerm),
    "UnifiedFieldUg1Term": PhysicsTerm("UnifiedFieldUg1Term", "Source6 unified field Ug1: magnetic dipole and mass gradient with decay", _compute_UnifiedFieldUg1Term),
    "UnifiedFieldUg2Term": PhysicsTerm("UnifiedFieldUg2Term", "Source6 unified field Ug2: UA charge and SCm interaction with stellar wind", _compute_UnifiedFieldUg2Term),
    "UnifiedFieldUg3Term": PhysicsTerm("UnifiedFieldUg3Term", "Source6 unified field Ug3: magnetic jet and core penetration with rotation", _compute_UnifiedFieldUg3Term),
    "UnifiedFieldUg4Term": PhysicsTerm("UnifiedFieldUg4Term", "Source6 unified field Ug4: galactic vacuum concentration with feedback", _compute_UnifiedFieldUg4Term),
    "UnifiedFieldUmTerm": PhysicsTerm("UnifiedFieldUmTerm", "Source6 unified field Um: magnetic strings with multi-layer contribution", _compute_UnifiedFieldUmTerm),
    "SpacetimeMetricTerm": PhysicsTerm("SpacetimeMetricTerm", "Source6 spacetime metric A_mu_nu perturbation with stress-energy coupling", _compute_SpacetimeMetricTerm),
    "CompressedMUGETerm": PhysicsTerm("CompressedMUGETerm", "Source6 compressed MUGE: multi-layer universal gravity with compression", _compute_CompressedMUGETerm),
    "ResonanceMUGE_VacuumDiffTerm": PhysicsTerm("ResonanceMUGE_VacuumDiffTerm", "Source7 resonance MUGE vacuum: nebula-ISM vacuum energy differential", _compute_ResonanceMUGE_VacuumDiffTerm),
    "ResonanceMUGE_SuperFreqTerm": PhysicsTerm("ResonanceMUGE_SuperFreqTerm", "Source7 resonance MUGE superconductivity: frequency-based resonance", _compute_ResonanceMUGE_SuperFreqTerm),
    "ResonanceMUGE_AetherResTerm": PhysicsTerm("ResonanceMUGE_AetherResTerm", "Source7 resonance MUGE aether: universal aether resonance coupling", _compute_ResonanceMUGE_AetherResTerm),
    "ResonanceMUGE_QuantumFreqTerm": PhysicsTerm("ResonanceMUGE_QuantumFreqTerm", "Source7 resonance MUGE quantum: quantum frequency modulation", _compute_ResonanceMUGE_QuantumFreqTerm),
    "YAMLConfigTerm": PhysicsTerm("YAMLConfigTerm", "Source7 YAML configuration: enhanced data loading with YAML support", _compute_YAMLConfigTerm),
    "UQFFCoreBuoyancyTerm": PhysicsTerm("UQFFCoreBuoyancyTerm", "Source10 UQFF core: buoyancy force with LENR, activation, DE, resonance", _compute_UQFFCoreBuoyancyTerm),
    "VacuumRepulsionTerm": PhysicsTerm("VacuumRepulsionTerm", "Source10 vacuum repulsion: surface tension spike/drop analogy", _compute_VacuumRepulsionTerm),
    "THzShockCommunicationTerm": PhysicsTerm("THzShockCommunicationTerm", "Source10 THz shock: 26-layer tail star formation with terahertz communication", _compute_THzShockCommunicationTerm),
    "ConduitFormationTerm": PhysicsTerm("ConduitFormationTerm", "Source10 conduit: H + H2O abundance leading to COx formation", _compute_ConduitFormationTerm),
    "SpookyActionTerm": PhysicsTerm("SpookyActionTerm", "Source10 spooky action: quantum string/wave entanglement at distance", _compute_SpookyActionTerm),
    "DPMResonanceEnergyTerm": PhysicsTerm("DPMResonanceEnergyTerm", "Source10 DPM resonance: energy density from hydrogen g-factor and Bohr magneton", _compute_DPMResonanceEnergyTerm),
    "Triadic26LayerTerm": PhysicsTerm("Triadic26LayerTerm", "Source10 triadic: compressed UQFF with 26 gravitational layers (Ug1-4)", _compute_Triadic26LayerTerm),
    "MagnetarCoreTerm": PhysicsTerm("MagnetarCoreTerm", "Source13 magnetar: base gravity + H(z) + B corrections + BH (M_BH=", _compute_MagnetarCoreTerm),
    "MagnetarLambdaTerm": PhysicsTerm("MagnetarLambdaTerm", "Source13 magnetar: cosmological constant Lambda/3 term", _compute_MagnetarLambdaTerm),
    "MagnetarEMTerm": PhysicsTerm("MagnetarEMTerm", "Source13 magnetar: electromagnetic v×B coupling (Lorentz force)", _compute_MagnetarEMTerm),
    "MagnetarGWTerm": PhysicsTerm("MagnetarGWTerm", "Source13 magnetar: gravitational wave emission (spindown dΩ/dt)^2", _compute_MagnetarGWTerm),
    "MagnetarQuantumTerm": PhysicsTerm("MagnetarQuantumTerm", "Source13 magnetar: quantum uncertainty (ℏ/√(Δx·Δp)) contribution", _compute_MagnetarQuantumTerm),
    "MagnetarFluidTerm": PhysicsTerm("MagnetarFluidTerm", "Source13 magnetar: magnetospheric fluid dynamics (ρ_fluid × V)", _compute_MagnetarFluidTerm),
    "MagnetarOscillatoryTerm": PhysicsTerm("MagnetarOscillatoryTerm", "Source13 magnetar: oscillatory perturbations (standing + traveling waves)", _compute_MagnetarOscillatoryTerm),
    "MagnetarDarkMatterTerm": PhysicsTerm("MagnetarDarkMatterTerm", "Source13 magnetar: dark matter halo + density perturbations (M_DM=", _compute_MagnetarDarkMatterTerm),
    "MagnetarMagneticEnergyTerm": PhysicsTerm("MagnetarMagneticEnergyTerm", "Source13 magnetar: magnetic field energy (B²/2μ₀ × V)", _compute_MagnetarMagneticEnergyTerm),
    "MagnetarDecayTerm": PhysicsTerm("MagnetarDecayTerm", "Source13 magnetar: cumulative decay energy (L₀ × τ_decay × [1-e^(-t/τ)])", _compute_MagnetarDecayTerm),
    "Magnetar0501CoreTerm": PhysicsTerm("Magnetar0501CoreTerm", "Source14 SGR 0501+4516: base gravity + H(z) + B corrections + f_TRZ time-reversa", _compute_Magnetar0501CoreTerm),
    "Magnetar0501LambdaTerm": PhysicsTerm("Magnetar0501LambdaTerm", "Source14 SGR 0501+4516: cosmological constant Lambda/3", _compute_Magnetar0501LambdaTerm),
    "Magnetar0501EMTerm": PhysicsTerm("Magnetar0501EMTerm", "Source14 SGR 0501+4516: scaled EM v×B with UA/SCm vacuum correction", _compute_Magnetar0501EMTerm),
    "Magnetar0501GWTerm": PhysicsTerm("Magnetar0501GWTerm", "Source14 SGR 0501+4516: gravitational wave (dΩ/dt)^2, P_init=", _compute_Magnetar0501GWTerm),
    "Magnetar0501QuantumTerm": PhysicsTerm("Magnetar0501QuantumTerm", "Source14 SGR 0501+4516: quantum uncertainty (ℏ/√(Δx·Δp))", _compute_Magnetar0501QuantumTerm),
    "Magnetar0501FluidTerm": PhysicsTerm("Magnetar0501FluidTerm", "Source14 SGR 0501+4516: magnetospheric fluid (ρ_fluid × V × g / M)", _compute_Magnetar0501FluidTerm),
    "Magnetar0501OscillatoryTerm": PhysicsTerm("Magnetar0501OscillatoryTerm", "Source14 SGR 0501+4516: oscillatory waves (standing + traveling), A=", _compute_Magnetar0501OscillatoryTerm),
    "Magnetar0501DarkMatterTerm": PhysicsTerm("Magnetar0501DarkMatterTerm", "Source14 SGR 0501+4516: dark matter + density perturbations (M_DM=", _compute_Magnetar0501DarkMatterTerm),
    "SgrAStarCoreTerm": PhysicsTerm("SgrAStarCoreTerm", "Source15 Sgr A*: SMBH with M(t) growth + H(z) + B + f_TRZ (M_dot_0=", _compute_SgrAStarCoreTerm),
    "SgrAStarLambdaTerm": PhysicsTerm("SgrAStarLambdaTerm", "Source15 Sgr A*: cosmological constant Lambda/3", _compute_SgrAStarLambdaTerm),
    "SgrAStarEMTerm": PhysicsTerm("SgrAStarEMTerm", "Source15 Sgr A*: EM v×B acceleration (no UA/SCm correction)", _compute_SgrAStarEMTerm),
    "SgrAStarGWTerm": PhysicsTerm("SgrAStarGWTerm", "Source15 Sgr A*: GW from spin decay (spin=", _compute_SgrAStarGWTerm),
    "SgrAStarQuantumTerm": PhysicsTerm("SgrAStarQuantumTerm", "Source15 Sgr A*: quantum uncertainty (ℏ/√(Δx·Δp))", _compute_SgrAStarQuantumTerm),
    "SgrAStarFluidTerm": PhysicsTerm("SgrAStarFluidTerm", "Source15 Sgr A*: accretion disk fluid dynamics (ρ_fluid × V)", _compute_SgrAStarFluidTerm),
    "SgrAStarOscillatoryTerm": PhysicsTerm("SgrAStarOscillatoryTerm", "Source15 Sgr A*: oscillatory waves (A=", _compute_SgrAStarOscillatoryTerm),
    "SgrAStarDarkMatterTerm": PhysicsTerm("SgrAStarDarkMatterTerm", "Source15 Sgr A*: DM + precession (angle=", _compute_SgrAStarDarkMatterTerm),
    "StarbirthCoreTerm": PhysicsTerm("StarbirthCoreTerm", "Source16 Starbirth: Base gravity with M(t) star formation, H(z), B corrections", _compute_StarbirthCoreTerm),
    "StarbirthLambdaTerm": PhysicsTerm("StarbirthLambdaTerm", "Source16 Starbirth: Lambda c^2 / 3", _compute_StarbirthLambdaTerm),
    "StarbirthUQFFTerm": PhysicsTerm("StarbirthUQFFTerm", "Source16 Starbirth: UQFF Ug with f_TRZ", _compute_StarbirthUQFFTerm),
    "StarbirthEMTerm": PhysicsTerm("StarbirthEMTerm", "Source16 Starbirth: Scaled EM with UA correction", _compute_StarbirthEMTerm),
    "StarbirthQuantumTerm": PhysicsTerm("StarbirthQuantumTerm", "Source16 Starbirth: Quantum uncertainty hbar/sqrt(dx*dp)", _compute_StarbirthQuantumTerm),
    "StarbirthFluidTerm": PhysicsTerm("StarbirthFluidTerm", "Source16 Starbirth: Fluid dynamics term", _compute_StarbirthFluidTerm),
    "StarbirthOscillatoryTerm": PhysicsTerm("StarbirthOscillatoryTerm", "Source16 Starbirth: Standing + traveling waves", _compute_StarbirthOscillatoryTerm),
    "StarbirthDarkMatterTerm": PhysicsTerm("StarbirthDarkMatterTerm", "Source16 Starbirth: DM + density perturbations (M_DM=", _compute_StarbirthDarkMatterTerm),
    "StarbirthWindTerm": PhysicsTerm("StarbirthWindTerm", "Source16 Starbirth: Stellar wind feedback (pressure/density)", _compute_StarbirthWindTerm),
    "Westerlund2CoreTerm": PhysicsTerm("Westerlund2CoreTerm", "Source17 Westerlund2: Base gravity with M(t) star formation, H(z), B corrections", _compute_Westerlund2CoreTerm),
    "Westerlund2LambdaTerm": PhysicsTerm("Westerlund2LambdaTerm", "Source17 Westerlund2: Lambda c^2 / 3", _compute_Westerlund2LambdaTerm),
    "Westerlund2UQFFTerm": PhysicsTerm("Westerlund2UQFFTerm", "Source17 Westerlund2: UQFF Ug with f_TRZ", _compute_Westerlund2UQFFTerm),
    "Westerlund2EMTerm": PhysicsTerm("Westerlund2EMTerm", "Source17 Westerlund2: Scaled EM with UA correction", _compute_Westerlund2EMTerm),
    "Westerlund2QuantumTerm": PhysicsTerm("Westerlund2QuantumTerm", "Source17 Westerlund2: Quantum uncertainty hbar/sqrt(dx*dp)", _compute_Westerlund2QuantumTerm),
    "Westerlund2FluidTerm": PhysicsTerm("Westerlund2FluidTerm", "Source17 Westerlund2: Fluid dynamics term", _compute_Westerlund2FluidTerm),
    "Westerlund2OscillatoryTerm": PhysicsTerm("Westerlund2OscillatoryTerm", "Source17 Westerlund2: Standing + traveling waves", _compute_Westerlund2OscillatoryTerm),
    "Westerlund2DarkMatterTerm": PhysicsTerm("Westerlund2DarkMatterTerm", "Source17 Westerlund2: DM + density perturbations (M_DM=", _compute_Westerlund2DarkMatterTerm),
    "Westerlund2WindTerm": PhysicsTerm("Westerlund2WindTerm", "Source17 Westerlund2: Stellar wind feedback (pressure/density)", _compute_Westerlund2WindTerm),
    "PillarsCoreTerm": PhysicsTerm("PillarsCoreTerm", "Source18 Pillars: Base gravity with M(t), H(z), B, erosion E(t) corrections", _compute_PillarsCoreTerm),
    "PillarsLambdaTerm": PhysicsTerm("PillarsLambdaTerm", "Source18 Pillars: Lambda c^2 / 3", _compute_PillarsLambdaTerm),
    "PillarsUQFFTerm": PhysicsTerm("PillarsUQFFTerm", "Source18 Pillars: UQFF Ug with f_TRZ", _compute_PillarsUQFFTerm),
    "PillarsEMTerm": PhysicsTerm("PillarsEMTerm", "Source18 Pillars: Scaled EM with UA correction", _compute_PillarsEMTerm),
    "PillarsQuantumTerm": PhysicsTerm("PillarsQuantumTerm", "Source18 Pillars: Quantum uncertainty hbar/sqrt(dx*dp)", _compute_PillarsQuantumTerm),
    "PillarsFluidTerm": PhysicsTerm("PillarsFluidTerm", "Source18 Pillars: Fluid dynamics term", _compute_PillarsFluidTerm),
    "PillarsOscillatoryTerm": PhysicsTerm("PillarsOscillatoryTerm", "Source18 Pillars: Standing + traveling waves", _compute_PillarsOscillatoryTerm),
    "PillarsDarkMatterTerm": PhysicsTerm("PillarsDarkMatterTerm", "Source18 Pillars: DM + density perturbations (M_DM=", _compute_PillarsDarkMatterTerm),
    "PillarsWindTerm": PhysicsTerm("PillarsWindTerm", "Source18 Pillars: Stellar wind feedback (pressure/density)", _compute_PillarsWindTerm),
    "PillarsErosionTerm": PhysicsTerm("PillarsErosionTerm", "Source18 Pillars: Photoevaporation erosion E(t) = E_0 * e^(-t/tau)", _compute_PillarsErosionTerm),
    "EinsteinRingCoreTerm": PhysicsTerm("EinsteinRingCoreTerm", "Source19 EinsteinRing: Base gravity with H(z), B, lensing amplification L(t)", _compute_EinsteinRingCoreTerm),
    "EinsteinRingLambdaTerm": PhysicsTerm("EinsteinRingLambdaTerm", "Source19 EinsteinRing: Lambda c^2 / 3", _compute_EinsteinRingLambdaTerm),
    "EinsteinRingUQFFTerm": PhysicsTerm("EinsteinRingUQFFTerm", "Source19 EinsteinRing: UQFF Ug with f_TRZ", _compute_EinsteinRingUQFFTerm),
    "EinsteinRingEMTerm": PhysicsTerm("EinsteinRingEMTerm", "Source19 EinsteinRing: Scaled EM with UA correction", _compute_EinsteinRingEMTerm),
    "EinsteinRingQuantumTerm": PhysicsTerm("EinsteinRingQuantumTerm", "Source19 EinsteinRing: Quantum uncertainty hbar/sqrt(dx*dp)", _compute_EinsteinRingQuantumTerm),
    "EinsteinRingFluidTerm": PhysicsTerm("EinsteinRingFluidTerm", "Source19 EinsteinRing: Fluid dynamics term", _compute_EinsteinRingFluidTerm),
    "EinsteinRingOscillatoryTerm": PhysicsTerm("EinsteinRingOscillatoryTerm", "Source19 EinsteinRing: Standing + traveling waves", _compute_EinsteinRingOscillatoryTerm),
    "EinsteinRingDarkMatterTerm": PhysicsTerm("EinsteinRingDarkMatterTerm", "Source19 EinsteinRing: DM + density perturbations (M_DM=", _compute_EinsteinRingDarkMatterTerm),
    "EinsteinRingWindTerm": PhysicsTerm("EinsteinRingWindTerm", "Source19 EinsteinRing: Intergalactic wind feedback", _compute_EinsteinRingWindTerm),
    "NGC2525CoreTerm": PhysicsTerm("NGC2525CoreTerm", "Source20 NGC2525: Base gravity with H(z), B corrections", _compute_NGC2525CoreTerm),
    "NGC2525BlackHoleTerm": PhysicsTerm("NGC2525BlackHoleTerm", "Source20 NGC2525: Supermassive black hole acceleration (M_BH=", _compute_NGC2525BlackHoleTerm),
    "NGC2525LambdaTerm": PhysicsTerm("NGC2525LambdaTerm", "Source20 NGC2525: Lambda c^2 / 3", _compute_NGC2525LambdaTerm),
    "NGC2525UQFFTerm": PhysicsTerm("NGC2525UQFFTerm", "Source20 NGC2525: UQFF Ug with f_TRZ", _compute_NGC2525UQFFTerm),
    "NGC2525EMTerm": PhysicsTerm("NGC2525EMTerm", "Source20 NGC2525: Scaled EM with UA correction", _compute_NGC2525EMTerm),
    "NGC2525QuantumTerm": PhysicsTerm("NGC2525QuantumTerm", "Source20 NGC2525: Quantum uncertainty hbar/sqrt(dx*dp)", _compute_NGC2525QuantumTerm),
    "NGC2525FluidTerm": PhysicsTerm("NGC2525FluidTerm", "Source20 NGC2525: Fluid dynamics term", _compute_NGC2525FluidTerm),
    "NGC2525OscillatoryTerm": PhysicsTerm("NGC2525OscillatoryTerm", "Source20 NGC2525: Standing + traveling waves", _compute_NGC2525OscillatoryTerm),
    "NGC2525DarkMatterTerm": PhysicsTerm("NGC2525DarkMatterTerm", "Source20 NGC2525: DM + density perturbations (M_DM=", _compute_NGC2525DarkMatterTerm),
    "NGC2525SupernovaTerm": PhysicsTerm("NGC2525SupernovaTerm", "Source20 NGC2525: Supernova mass loss M_SN(t) = M_SN0 * e^(-t/tau)", _compute_NGC2525SupernovaTerm),
    "NGC3603CoreTerm": PhysicsTerm("NGC3603CoreTerm", "Source21 NGC3603: Base gravity with M(t) star formation, H(z), B corrections", _compute_NGC3603CoreTerm),
    "NGC3603LambdaTerm": PhysicsTerm("NGC3603LambdaTerm", "Source21 NGC3603: Lambda c^2 / 3", _compute_NGC3603LambdaTerm),
    "NGC3603UQFFTerm": PhysicsTerm("NGC3603UQFFTerm", "Source21 NGC3603: UQFF Ug with f_TRZ", _compute_NGC3603UQFFTerm),
    "NGC3603EMTerm": PhysicsTerm("NGC3603EMTerm", "Source21 NGC3603: Scaled EM with UA correction", _compute_NGC3603EMTerm),
    "NGC3603QuantumTerm": PhysicsTerm("NGC3603QuantumTerm", "Source21 NGC3603: Quantum uncertainty hbar/sqrt(dx*dp)", _compute_NGC3603QuantumTerm),
    "NGC3603FluidTerm": PhysicsTerm("NGC3603FluidTerm", "Source21 NGC3603: Fluid dynamics term", _compute_NGC3603FluidTerm),
    "NGC3603OscillatoryTerm": PhysicsTerm("NGC3603OscillatoryTerm", "Source21 NGC3603: Standing + traveling waves", _compute_NGC3603OscillatoryTerm),
    "NGC3603DarkMatterTerm": PhysicsTerm("NGC3603DarkMatterTerm", "Source21 NGC3603: DM + density perturbations (M_DM=", _compute_NGC3603DarkMatterTerm),
    "NGC3603WindTerm": PhysicsTerm("NGC3603WindTerm", "Source21 NGC3603: Stellar wind feedback (pressure/density)", _compute_NGC3603WindTerm),
    "NGC3603CavityPressureTerm": PhysicsTerm("NGC3603CavityPressureTerm", "Source21 NGC3603: Cavity pressure P(t) = P0 * e^(-t/tau)", _compute_NGC3603CavityPressureTerm),
    "BubbleNebulaCoreTerm": PhysicsTerm("BubbleNebulaCoreTerm", "Source22 BubbleNebula: Core gravity with H(z), B, and E(t) expansion corrections", _compute_BubbleNebulaCoreTerm),
    "BubbleNebulaLambdaTerm": PhysicsTerm("BubbleNebulaLambdaTerm", "Source22 BubbleNebula: Cosmological constant Lambda * c^2 / 3", _compute_BubbleNebulaLambdaTerm),
    "BubbleNebulaUQFFTerm": PhysicsTerm("BubbleNebulaUQFFTerm", "Source22 BubbleNebula: UQFF Ug with f_TRZ and E(t) correction", _compute_BubbleNebulaUQFFTerm),
    "BubbleNebulaEMTerm": PhysicsTerm("BubbleNebulaEMTerm", "Source22 BubbleNebula: Scaled EM with UA vacuum correction", _compute_BubbleNebulaEMTerm),
    "BubbleNebulaQuantumTerm": PhysicsTerm("BubbleNebulaQuantumTerm", "Source22 BubbleNebula: Quantum uncertainty hbar / sqrt(dx*dp)", _compute_BubbleNebulaQuantumTerm),
    "BubbleNebulaFluidTerm": PhysicsTerm("BubbleNebulaFluidTerm", "Source22 BubbleNebula: Fluid dynamics (rho * V * g) / M", _compute_BubbleNebulaFluidTerm),
    "BubbleNebulaOscillatoryTerm": PhysicsTerm("BubbleNebulaOscillatoryTerm", "Source22 BubbleNebula: Standing + traveling wave perturbations", _compute_BubbleNebulaOscillatoryTerm),
    "BubbleNebulaDarkMatterTerm": PhysicsTerm("BubbleNebulaDarkMatterTerm", "Source22 BubbleNebula: DM (85%) + density perturbations", _compute_BubbleNebulaDarkMatterTerm),
    "BubbleNebulaWindTerm": PhysicsTerm("BubbleNebulaWindTerm", "Source22 BubbleNebula: Stellar wind feedback (rho * v^2) / rho_fluid", _compute_BubbleNebulaWindTerm),
    "BubbleNebulaExpansionTerm": PhysicsTerm("BubbleNebulaExpansionTerm", "Source22 BubbleNebula: Expansion correction E(t) = E_0 * (1 - e^(-t/tau))", _compute_BubbleNebulaExpansionTerm),
    "AntennaeGalaxiesCoreTerm": PhysicsTerm("AntennaeGalaxiesCoreTerm", "Source23 AntennaeGalaxies: Core gravity with M(t), H(z), B, and I(t) merger inte", _compute_AntennaeGalaxiesCoreTerm),
    "AntennaeGalaxiesLambdaTerm": PhysicsTerm("AntennaeGalaxiesLambdaTerm", "Source23 AntennaeGalaxies: Cosmological constant Lambda * c^2 / 3", _compute_AntennaeGalaxiesLambdaTerm),
    "AntennaeGalaxiesUQFFTerm": PhysicsTerm("AntennaeGalaxiesUQFFTerm", "Source23 AntennaeGalaxies: UQFF Ug with f_TRZ and I(t) merger interaction", _compute_AntennaeGalaxiesUQFFTerm),
    "AntennaeGalaxiesEMTerm": PhysicsTerm("AntennaeGalaxiesEMTerm", "Source23 AntennaeGalaxies: Scaled EM with UA vacuum correction", _compute_AntennaeGalaxiesEMTerm),
    "AntennaeGalaxiesQuantumTerm": PhysicsTerm("AntennaeGalaxiesQuantumTerm", "Source23 AntennaeGalaxies: Quantum uncertainty hbar / sqrt(dx*dp)", _compute_AntennaeGalaxiesQuantumTerm),
    "AntennaeGalaxiesFluidTerm": PhysicsTerm("AntennaeGalaxiesFluidTerm", "Source23 AntennaeGalaxies: Fluid dynamics (rho * V * g) / M(t)", _compute_AntennaeGalaxiesFluidTerm),
    "AntennaeGalaxiesOscillatoryTerm": PhysicsTerm("AntennaeGalaxiesOscillatoryTerm", "Source23 AntennaeGalaxies: Standing + traveling wave perturbations", _compute_AntennaeGalaxiesOscillatoryTerm),
    "AntennaeGalaxiesDarkMatterTerm": PhysicsTerm("AntennaeGalaxiesDarkMatterTerm", "Source23 AntennaeGalaxies: DM (85%) + density perturbations", _compute_AntennaeGalaxiesDarkMatterTerm),
    "AntennaeGalaxiesWindTerm": PhysicsTerm("AntennaeGalaxiesWindTerm", "Source23 AntennaeGalaxies: Stellar wind feedback (rho * v^2) / rho_fluid", _compute_AntennaeGalaxiesWindTerm),
    "AntennaeGalaxiesMergerInteractionTerm": PhysicsTerm("AntennaeGalaxiesMergerInteractionTerm", "Source23 AntennaeGalaxies: Merger interaction I(t) = I0 * e^(-t/tau_merger)", _compute_AntennaeGalaxiesMergerInteractionTerm),
    "HorseheadNebulaCoreTerm": PhysicsTerm("HorseheadNebulaCoreTerm", "Source24 HorseheadNebula: Core gravity with H(z), B, and E(t) erosion correction", _compute_HorseheadNebulaCoreTerm),
    "HorseheadNebulaLambdaTerm": PhysicsTerm("HorseheadNebulaLambdaTerm", "Source24 HorseheadNebula: Cosmological constant Lambda * c^2 / 3", _compute_HorseheadNebulaLambdaTerm),
    "HorseheadNebulaUQFFTerm": PhysicsTerm("HorseheadNebulaUQFFTerm", "Source24 HorseheadNebula: UQFF Ug with f_TRZ and E(t) erosion correction", _compute_HorseheadNebulaUQFFTerm),
    "HorseheadNebulaEMTerm": PhysicsTerm("HorseheadNebulaEMTerm", "Source24 HorseheadNebula: Scaled EM with UA vacuum correction", _compute_HorseheadNebulaEMTerm),
    "HorseheadNebulaQuantumTerm": PhysicsTerm("HorseheadNebulaQuantumTerm", "Source24 HorseheadNebula: Quantum uncertainty hbar / sqrt(dx*dp)", _compute_HorseheadNebulaQuantumTerm),
    "HorseheadNebulaFluidTerm": PhysicsTerm("HorseheadNebulaFluidTerm", "Source24 HorseheadNebula: Fluid dynamics (rho * V * g) / M", _compute_HorseheadNebulaFluidTerm),
    "HorseheadNebulaOscillatoryTerm": PhysicsTerm("HorseheadNebulaOscillatoryTerm", "Source24 HorseheadNebula: Standing + traveling wave perturbations", _compute_HorseheadNebulaOscillatoryTerm),
    "HorseheadNebulaDarkMatterTerm": PhysicsTerm("HorseheadNebulaDarkMatterTerm", "Source24 HorseheadNebula: DM (85%) + density perturbations", _compute_HorseheadNebulaDarkMatterTerm),
    "HorseheadNebulaWindTerm": PhysicsTerm("HorseheadNebulaWindTerm", "Source24 HorseheadNebula: Stellar wind feedback (rho * v^2) / rho_fluid", _compute_HorseheadNebulaWindTerm),
    "HorseheadNebulaErosionTerm": PhysicsTerm("HorseheadNebulaErosionTerm", "Source24 HorseheadNebula: Erosion correction E(t) = E_0 * (1 - e^(-t/tau))", _compute_HorseheadNebulaErosionTerm),
    "NGC1275CoreTerm": PhysicsTerm("NGC1275CoreTerm", "Source25 NGC1275: Core gravity with H(z), B(t), and F(t) filament support", _compute_NGC1275CoreTerm),
    "NGC1275BlackHoleTerm": PhysicsTerm("NGC1275BlackHoleTerm", "Source25 NGC1275: SMBH gravity term (800M Msun)", _compute_NGC1275BlackHoleTerm),
    "NGC1275LambdaTerm": PhysicsTerm("NGC1275LambdaTerm", "Source25 NGC1275: Cosmological constant Lambda * c^2 / 3", _compute_NGC1275LambdaTerm),
    "NGC1275UQFFTerm": PhysicsTerm("NGC1275UQFFTerm", "Source25 NGC1275: UQFF Ug with f_TRZ, B(t), and F(t) filament support", _compute_NGC1275UQFFTerm),
    "NGC1275EMTerm": PhysicsTerm("NGC1275EMTerm", "Source25 NGC1275: Scaled EM with UA vacuum correction and B(t)", _compute_NGC1275EMTerm),
    "NGC1275QuantumTerm": PhysicsTerm("NGC1275QuantumTerm", "Source25 NGC1275: Quantum uncertainty hbar / sqrt(dx*dp)", _compute_NGC1275QuantumTerm),
    "NGC1275FluidTerm": PhysicsTerm("NGC1275FluidTerm", "Source25 NGC1275: Fluid dynamics (rho * V * g) / M", _compute_NGC1275FluidTerm),
    "NGC1275OscillatoryTerm": PhysicsTerm("NGC1275OscillatoryTerm", "Source25 NGC1275: Standing + traveling wave perturbations", _compute_NGC1275OscillatoryTerm),
    "NGC1275DarkMatterTerm": PhysicsTerm("NGC1275DarkMatterTerm", "Source25 NGC1275: DM (85%) + density perturbations", _compute_NGC1275DarkMatterTerm),
    "NGC1275CoolingFlowTerm": PhysicsTerm("NGC1275CoolingFlowTerm", "Source25 NGC1275: Cooling flow (rho_cool * v_cool^2) / rho_fluid", _compute_NGC1275CoolingFlowTerm),
    "NGC1275FilamentSupportTerm": PhysicsTerm("NGC1275FilamentSupportTerm", "Source25 NGC1275: Filament support F(t) = F0 * e^(-t/tau_fil)", _compute_NGC1275FilamentSupportTerm),
    "HUDFGalaxiesCoreTerm": PhysicsTerm("HUDFGalaxiesCoreTerm", "Source26 HUDFGalaxies: Core gravity with M(t), H(z), B, and I(t) interactions", _compute_HUDFGalaxiesCoreTerm),
    "HUDFGalaxiesLambdaTerm": PhysicsTerm("HUDFGalaxiesLambdaTerm", "Source26 HUDFGalaxies: Cosmological constant Lambda * c^2 / 3", _compute_HUDFGalaxiesLambdaTerm),
    "HUDFGalaxiesUQFFTerm": PhysicsTerm("HUDFGalaxiesUQFFTerm", "Source26 HUDFGalaxies: UQFF Ug with f_TRZ and I(t) interactions", _compute_HUDFGalaxiesUQFFTerm),
    "HUDFGalaxiesEMTerm": PhysicsTerm("HUDFGalaxiesEMTerm", "Source26 HUDFGalaxies: Scaled EM with UA vacuum correction", _compute_HUDFGalaxiesEMTerm),
    "HUDFGalaxiesQuantumTerm": PhysicsTerm("HUDFGalaxiesQuantumTerm", "Source26 HUDFGalaxies: Quantum uncertainty hbar / sqrt(dx*dp)", _compute_HUDFGalaxiesQuantumTerm),
    "HUDFGalaxiesFluidTerm": PhysicsTerm("HUDFGalaxiesFluidTerm", "Source26 HUDFGalaxies: Fluid dynamics (rho * V * g) / M(t)", _compute_HUDFGalaxiesFluidTerm),
    "HUDFGalaxiesOscillatoryTerm": PhysicsTerm("HUDFGalaxiesOscillatoryTerm", "Source26 HUDFGalaxies: Standing + traveling wave perturbations", _compute_HUDFGalaxiesOscillatoryTerm),
    "HUDFGalaxiesDarkMatterTerm": PhysicsTerm("HUDFGalaxiesDarkMatterTerm", "Source26 HUDFGalaxies: DM (85%) + density perturbations", _compute_HUDFGalaxiesDarkMatterTerm),
    "HUDFGalaxiesWindTerm": PhysicsTerm("HUDFGalaxiesWindTerm", "Source26 HUDFGalaxies: Stellar wind feedback (rho * v^2) / rho_fluid", _compute_HUDFGalaxiesWindTerm),
    "HUDFGalaxiesInteractionTerm": PhysicsTerm("HUDFGalaxiesInteractionTerm", "Source26 HUDFGalaxies: Galaxy interaction I(t) = I0 * e^(-t/tau_inter)", _compute_HUDFGalaxiesInteractionTerm),
    "NGC1792CoreTerm": PhysicsTerm("NGC1792CoreTerm", "Source27 NGC1792: Core gravity with M(t) starburst, H(z), and B", _compute_NGC1792CoreTerm),
    "NGC1792LambdaTerm": PhysicsTerm("NGC1792LambdaTerm", "Source27 NGC1792: Cosmological constant Lambda * c^2 / 3", _compute_NGC1792LambdaTerm),
    "NGC1792UQFFTerm": PhysicsTerm("NGC1792UQFFTerm", "Source27 NGC1792: UQFF Ug with f_TRZ and M(t) starburst", _compute_NGC1792UQFFTerm),
    "NGC1792EMTerm": PhysicsTerm("NGC1792EMTerm", "Source27 NGC1792: Scaled EM with UA vacuum correction", _compute_NGC1792EMTerm),
    "NGC1792QuantumTerm": PhysicsTerm("NGC1792QuantumTerm", "Source27 NGC1792: Quantum uncertainty hbar / sqrt(dx*dp)", _compute_NGC1792QuantumTerm),
    "NGC1792FluidTerm": PhysicsTerm("NGC1792FluidTerm", "Source27 NGC1792: Fluid dynamics (rho * V * g) / M(t)", _compute_NGC1792FluidTerm),
    "NGC1792OscillatoryTerm": PhysicsTerm("NGC1792OscillatoryTerm", "Source27 NGC1792: Standing + traveling wave perturbations", _compute_NGC1792OscillatoryTerm),
    "NGC1792DarkMatterTerm": PhysicsTerm("NGC1792DarkMatterTerm", "Source27 NGC1792: DM (85%) + density perturbations", _compute_NGC1792DarkMatterTerm),
    "NGC1792SupernovaFeedbackTerm": PhysicsTerm("NGC1792SupernovaFeedbackTerm", "Source27 NGC1792: Supernova feedback (rho * v^2) / rho_fluid", _compute_NGC1792SupernovaFeedbackTerm),
    "AndromedaCoreTerm": PhysicsTerm("AndromedaCoreTerm", "Source28 Andromeda: Core gravity with H(z) blueshift and B", _compute_AndromedaCoreTerm),
    "AndromedaBlackHoleTerm": PhysicsTerm("AndromedaBlackHoleTerm", "Source28 Andromeda: SMBH gravity term (140M Msun)", _compute_AndromedaBlackHoleTerm),
    "AndromedaLambdaTerm": PhysicsTerm("AndromedaLambdaTerm", "Source28 Andromeda: Cosmological constant Lambda * c^2 / 3", _compute_AndromedaLambdaTerm),
    "AndromedaUQFFTerm": PhysicsTerm("AndromedaUQFFTerm", "Source28 Andromeda: UQFF Ug with f_TRZ", _compute_AndromedaUQFFTerm),
    "AndromedaEMTerm": PhysicsTerm("AndromedaEMTerm", "Source28 Andromeda: Scaled EM with UA vacuum correction", _compute_AndromedaEMTerm),
    "AndromedaQuantumTerm": PhysicsTerm("AndromedaQuantumTerm", "Source28 Andromeda: Quantum uncertainty hbar / sqrt(dx*dp)", _compute_AndromedaQuantumTerm),
    "AndromedaFluidTerm": PhysicsTerm("AndromedaFluidTerm", "Source28 Andromeda: Fluid dynamics (rho * V * g) / M", _compute_AndromedaFluidTerm),
    "AndromedaOscillatoryTerm": PhysicsTerm("AndromedaOscillatoryTerm", "Source28 Andromeda: Standing + traveling wave perturbations", _compute_AndromedaOscillatoryTerm),
    "AndromedaDarkMatterTerm": PhysicsTerm("AndromedaDarkMatterTerm", "Source28 Andromeda: DM (80%) + density perturbations", _compute_AndromedaDarkMatterTerm),
    "SombreroCoreTerm": PhysicsTerm("SombreroCoreTerm", "Source29 Sombrero: Core gravity with H(z=0.0063) and B superconductivity", _compute_SombreroCoreTerm),
    "SombreroBlackHoleTerm": PhysicsTerm("SombreroBlackHoleTerm", "Source29 Sombrero: SMBH gravity term (1 billion Msun)", _compute_SombreroBlackHoleTerm),
    "SombreroLambdaTerm": PhysicsTerm("SombreroLambdaTerm", "Source29 Sombrero: Cosmological constant Lambda * c^2 / 3", _compute_SombreroLambdaTerm),
    "SombreroUQFFTerm": PhysicsTerm("SombreroUQFFTerm", "Source29 Sombrero: UQFF Ug with f_TRZ and superconductivity", _compute_SombreroUQFFTerm),
    "SombreroEMTerm": PhysicsTerm("SombreroEMTerm", "Source29 Sombrero: Scaled EM with UA vacuum correction", _compute_SombreroEMTerm),
    "SombreroQuantumTerm": PhysicsTerm("SombreroQuantumTerm", "Source29 Sombrero: Quantum uncertainty hbar / sqrt(dx*dp)", _compute_SombreroQuantumTerm),
    "SombreroFluidTerm": PhysicsTerm("SombreroFluidTerm", "Source29 Sombrero: Fluid dynamics (rho * V * g) / M", _compute_SombreroFluidTerm),
    "SombreroOscillatoryTerm": PhysicsTerm("SombreroOscillatoryTerm", "Source29 Sombrero: Standing + traveling wave perturbations", _compute_SombreroOscillatoryTerm),
    "SombreroDarkMatterTerm": PhysicsTerm("SombreroDarkMatterTerm", "Source29 Sombrero: DM (20%) + density perturbations", _compute_SombreroDarkMatterTerm),
    "SombreroDustTerm": PhysicsTerm("SombreroDustTerm", "Source29 Sombrero: Dust drag D_dust (prominent dust lane)", _compute_SombreroDustTerm),
    "SaturnCoreTerm": PhysicsTerm("SaturnCoreTerm", "Source30 Saturn: Core gravity (Saturn + Sun) with B superconductivity", _compute_SaturnCoreTerm),
    "SaturnLambdaTerm": PhysicsTerm("SaturnLambdaTerm", "Source30 Saturn: Cosmological constant Lambda * c^2 / 3", _compute_SaturnLambdaTerm),
    "SaturnUQFFTerm": PhysicsTerm("SaturnUQFFTerm", "Source30 Saturn: UQFF Ug with f_TRZ", _compute_SaturnUQFFTerm),
    "SaturnEMTerm": PhysicsTerm("SaturnEMTerm", "Source30 Saturn: Scaled EM with UA vacuum correction", _compute_SaturnEMTerm),
    "SaturnQuantumTerm": PhysicsTerm("SaturnQuantumTerm", "Source30 Saturn: Quantum uncertainty hbar / sqrt(dx*dp)", _compute_SaturnQuantumTerm),
    "SaturnFluidTerm": PhysicsTerm("SaturnFluidTerm", "Source30 Saturn: Fluid dynamics (rho * V * g) / M", _compute_SaturnFluidTerm),
    "SaturnOscillatoryTerm": PhysicsTerm("SaturnOscillatoryTerm", "Source30 Saturn: Standing + traveling wave perturbations", _compute_SaturnOscillatoryTerm),
    "SaturnDarkMatterTerm": PhysicsTerm("SaturnDarkMatterTerm", "Source30 Saturn: Visible mass with density perturbations (M_DM=0)", _compute_SaturnDarkMatterTerm),
    "SaturnRingTidalTerm": PhysicsTerm("SaturnRingTidalTerm", "Source30 Saturn: Ring tidal force G * M_ring / r_ring^2", _compute_SaturnRingTidalTerm),
    "SaturnWindTerm": PhysicsTerm("SaturnWindTerm", "Source30 Saturn: Atmospheric wind v_wind^2 * 1e-12", _compute_SaturnWindTerm),
    "M16CoreTerm": PhysicsTerm("M16CoreTerm", "Source31 M16: Core gravity with M(t) star formation and erosion", _compute_M16CoreTerm),
    "M16LambdaTerm": PhysicsTerm("M16LambdaTerm", "Source31 M16: Cosmological constant Lambda * c^2 / 3", _compute_M16LambdaTerm),
    "M16UQFFTerm": PhysicsTerm("M16UQFFTerm", "Source31 M16: UQFF Ug with f_TRZ and M(t)", _compute_M16UQFFTerm),
    "M16EMTerm": PhysicsTerm("M16EMTerm", "Source31 M16: Scaled EM with UA vacuum correction (v_gas=100 km/s)", _compute_M16EMTerm),
    "M16QuantumTerm": PhysicsTerm("M16QuantumTerm", "Source31 M16: Quantum uncertainty hbar / sqrt(dx*dp)", _compute_M16QuantumTerm),
    "M16FluidTerm": PhysicsTerm("M16FluidTerm", "Source31 M16: Fluid dynamics with M(t)", _compute_M16FluidTerm),
    "M16OscillatoryTerm": PhysicsTerm("M16OscillatoryTerm", "Source31 M16: Standing + traveling wave perturbations", _compute_M16OscillatoryTerm),
    "M16DarkMatterTerm": PhysicsTerm("M16DarkMatterTerm", "Source31 M16: Visible mass with density perturbations (M_DM=0)", _compute_M16DarkMatterTerm),
    "M16StarFormationTerm": PhysicsTerm("M16StarFormationTerm", "Source31 M16: Star formation M_sf(t) = (SFR * t) / M0", _compute_M16StarFormationTerm),
    "M16RadiationErosionTerm": PhysicsTerm("M16RadiationErosionTerm", "Source31 M16: Radiation erosion E_rad(t) = E0 * (1 - exp(-t/tau))", _compute_M16RadiationErosionTerm),
    "CrabCoreTerm": PhysicsTerm("CrabCoreTerm", "Source32 Crab: Core gravity with r(t) expansion", _compute_CrabCoreTerm),
    "CrabLambdaTerm": PhysicsTerm("CrabLambdaTerm", "Source32 Crab: Cosmological constant Lambda * c^2 / 3", _compute_CrabLambdaTerm),
    "CrabUQFFTerm": PhysicsTerm("CrabUQFFTerm", "Source32 Crab: UQFF Ug with f_TRZ and r(t)", _compute_CrabUQFFTerm),
    "CrabEMTerm": PhysicsTerm("CrabEMTerm", "Source32 Crab: Scaled EM with UA correction (v_shock=1500 km/s)", _compute_CrabEMTerm),
    "CrabQuantumTerm": PhysicsTerm("CrabQuantumTerm", "Source32 Crab: Quantum uncertainty hbar / sqrt(dx*dp)", _compute_CrabQuantumTerm),
    "CrabFluidTerm": PhysicsTerm("CrabFluidTerm", "Source32 Crab: Fluid dynamics with r(t)", _compute_CrabFluidTerm),
    "CrabOscillatoryTerm": PhysicsTerm("CrabOscillatoryTerm", "Source32 Crab: Standing + traveling wave perturbations", _compute_CrabOscillatoryTerm),
    "CrabDarkMatterTerm": PhysicsTerm("CrabDarkMatterTerm", "Source32 Crab: Visible mass with density perturbations (M_DM=0)", _compute_CrabDarkMatterTerm),
    "CrabPulsarWindTerm": PhysicsTerm("CrabPulsarWindTerm", "Source32 Crab: Pulsar wind a_wind = [P/(4πr²) * (1+v/c)] / ρ", _compute_CrabPulsarWindTerm),
    "CrabMagneticTerm": PhysicsTerm("CrabMagneticTerm", "Source32 Crab: Magnetic M_mag = (q * v_shock * B) / m_e", _compute_CrabMagneticTerm),
    "SGR1745CoreTerm": PhysicsTerm("SGR1745CoreTerm", "Source33 SGR1745: Core gravity with extreme B superconductivity", _compute_SGR1745CoreTerm),
    "SGR1745LambdaTerm": PhysicsTerm("SGR1745LambdaTerm", "Source33 SGR1745: Cosmological constant Lambda * c^2 / 3", _compute_SGR1745LambdaTerm),
    "SGR1745UQFFTerm": PhysicsTerm("SGR1745UQFFTerm", "Source33 SGR1745: UQFF Ug with f_TRZ and extreme B", _compute_SGR1745UQFFTerm),
    "SGR1745EMTerm": PhysicsTerm("SGR1745EMTerm", "Source33 SGR1745: Scaled EM with extreme B=2e10 T", _compute_SGR1745EMTerm),
    "SGR1745QuantumTerm": PhysicsTerm("SGR1745QuantumTerm", "Source33 SGR1745: Quantum uncertainty hbar / sqrt(dx*dp)", _compute_SGR1745QuantumTerm),
    "SGR1745FluidTerm": PhysicsTerm("SGR1745FluidTerm", "Source33 SGR1745: Crust dynamics rho_crust = 1e17 kg/m^3", _compute_SGR1745FluidTerm),
    "SGR1745OscillatoryTerm": PhysicsTerm("SGR1745OscillatoryTerm", "Source33 SGR1745: Pulsations omega=1.67 rad/s (P=3.76s)", _compute_SGR1745OscillatoryTerm),
    "SGR1745DarkMatterTerm": PhysicsTerm("SGR1745DarkMatterTerm", "Source33 SGR1745: Visible mass with density perturbations (M_DM=0)", _compute_SGR1745DarkMatterTerm),
    "SGR1745_DPMTerm": PhysicsTerm("SGR1745_DPMTerm", "Source34 SGR1745-UQFF: DPM resonance a_DPM = (I/A) * (ω₁-ω₂)", _compute_SGR1745_DPMTerm),
    "SGR1745_THzTerm": PhysicsTerm("SGR1745_THzTerm", "Source34 SGR1745-UQFF: THz pipeline a_THz = (f·E_vac·v·a_DPM)/(E_ISM·c)", _compute_SGR1745_THzTerm),
    "SGR1745_VacDiffTerm": PhysicsTerm("SGR1745_VacDiffTerm", "Source34 SGR1745-UQFF: Vacuum differential a_vac_diff", _compute_SGR1745_VacDiffTerm),
    "SGR1745_SuperFreqTerm": PhysicsTerm("SGR1745_SuperFreqTerm", "Source34 SGR1745-UQFF: Superconductor frequency a_super_freq", _compute_SGR1745_SuperFreqTerm),
    "SGR1745_AetherResTerm": PhysicsTerm("SGR1745_AetherResTerm", "Source34 SGR1745-UQFF: Aether resonance a_aether_res", _compute_SGR1745_AetherResTerm),
    "SGR1745_Ug4iTerm": PhysicsTerm("SGR1745_Ug4iTerm", "Source34 SGR1745-UQFF: Reactive U_g4i term", _compute_SGR1745_Ug4iTerm),
    "SGR1745_QuantumFreqTerm": PhysicsTerm("SGR1745_QuantumFreqTerm", "Source34 SGR1745-UQFF: Quantum frequency a_quantum_freq", _compute_SGR1745_QuantumFreqTerm),
    "SGR1745_AetherFreqTerm": PhysicsTerm("SGR1745_AetherFreqTerm", "Source34 SGR1745-UQFF: Aether frequency a_Aether_freq", _compute_SGR1745_AetherFreqTerm),
    "SGR1745_FluidFreqTerm": PhysicsTerm("SGR1745_FluidFreqTerm", "Source34 SGR1745-UQFF: Fluid frequency a_fluid_freq", _compute_SGR1745_FluidFreqTerm),
    "SGR1745_ExpFreqTerm": PhysicsTerm("SGR1745_ExpFreqTerm", "Source34 SGR1745-UQFF: Expansion frequency a_exp_freq", _compute_SGR1745_ExpFreqTerm),
    "SgrA_DPMTerm": PhysicsTerm("SgrA_DPMTerm", "Source35 SgrA*-UQFF: DPM resonance (scaled for SMBH)", _compute_SgrA_DPMTerm),
    "SgrA_THzTerm": PhysicsTerm("SgrA_THzTerm", "Source35 SgrA*-UQFF: THz pipeline (f=1e9 Hz for SMBH)", _compute_SgrA_THzTerm),
    "SgrA_VacDiffTerm": PhysicsTerm("SgrA_VacDiffTerm", "Source35 SgrA*-UQFF: Vacuum differential", _compute_SgrA_VacDiffTerm),
    "SgrA_SuperFreqTerm": PhysicsTerm("SgrA_SuperFreqTerm", "Source35 SgrA*-UQFF: Superconductor frequency", _compute_SgrA_SuperFreqTerm),
    "SgrA_AetherResTerm": PhysicsTerm("SgrA_AetherResTerm", "Source35 SgrA*-UQFF: Aether resonance", _compute_SgrA_AetherResTerm),
    "SgrA_Ug4iTerm": PhysicsTerm("SgrA_Ug4iTerm", "Source35 SgrA*-UQFF: Reactive U_g4i", _compute_SgrA_Ug4iTerm),
    "SgrA_QuantumFreqTerm": PhysicsTerm("SgrA_QuantumFreqTerm", "Source35 SgrA*-UQFF: Quantum frequency", _compute_SgrA_QuantumFreqTerm),
    "SgrA_AetherFreqTerm": PhysicsTerm("SgrA_AetherFreqTerm", "Source35 SgrA*-UQFF: Aether frequency", _compute_SgrA_AetherFreqTerm),
    "SgrA_FluidFreqTerm": PhysicsTerm("SgrA_FluidFreqTerm", "Source35 SgrA*-UQFF: Fluid frequency", _compute_SgrA_FluidFreqTerm),
    "SgrA_ExpFreqTerm": PhysicsTerm("SgrA_ExpFreqTerm", "Source35 SgrA*-UQFF: Expansion frequency", _compute_SgrA_ExpFreqTerm),
    "YoungStarsCore": PhysicsTerm("YoungStarsCore", "Source36 YoungStars: Core gravity with star formation mass factor M_SF(t)", _compute_YoungStarsCore),
    "YoungStarsLambda": PhysicsTerm("YoungStarsLambda", "Source36 YoungStars: Cosmological constant term", _compute_YoungStarsLambda),
    "YoungStarsUQFF": PhysicsTerm("YoungStarsUQFF", "Source36 YoungStars: UQFF sum (Ug1-4) with outflow velocity SC term", _compute_YoungStarsUQFF),
    "YoungStarsEM": PhysicsTerm("YoungStarsEM", "Source36 YoungStars: EM Lorentz with vacuum ratio correction", _compute_YoungStarsEM),
    "YoungStarsQuantum": PhysicsTerm("YoungStarsQuantum", "Source36 YoungStars: Quantum uncertainty term", _compute_YoungStarsQuantum),
    "YoungStarsFluid": PhysicsTerm("YoungStarsFluid", "Source36 YoungStars: Fluid dynamics term", _compute_YoungStarsFluid),
    "YoungStarsOscillatory": PhysicsTerm("YoungStarsOscillatory", "Source36 YoungStars: Resonant oscillatory term (standing + traveling)", _compute_YoungStarsOscillatory),
    "YoungStarsDarkMatter": PhysicsTerm("YoungStarsDarkMatter", "Source36 YoungStars: Dark matter perturbation term", _compute_YoungStarsDarkMatter),
    "YoungStarsOutflowPressure": PhysicsTerm("YoungStarsOutflowPressure", "Source36 YoungStars: Outflow pressure P=rho*v^2*(1+t/tau) UNIQUE", _compute_YoungStarsOutflowPressure),
    "YoungStarsStarFormation": PhysicsTerm("YoungStarsStarFormation", "Source36 YoungStars: Star formation mass factor M_SF=SFR*t/M UNIQUE", _compute_YoungStarsStarFormation),
    "BigBangCore": PhysicsTerm("BigBangCore", "Source37 BigBang: Core gravity with M(t)=M_total*(t/t_H), r(t)=c*t", _compute_BigBangCore),
    "BigBangLambda": PhysicsTerm("BigBangLambda", "Source37 BigBang: Cosmological constant term", _compute_BigBangLambda),
    "BigBangUg1": PhysicsTerm("BigBangUg1", "Source37 BigBang: Ug1 = G*M(t)/r(t)^2", _compute_BigBangUg1),
    "BigBangUg4": PhysicsTerm("BigBangUg4", "Source37 BigBang: Ug4 reaction = Ug1 * f_sc", _compute_BigBangUg4),
    "BigBangQuantum": PhysicsTerm("BigBangQuantum", "Source37 BigBang: Quantum uncertainty term", _compute_BigBangQuantum),
    "BigBangFluid": PhysicsTerm("BigBangFluid", "Source37 BigBang: Fluid dynamics term", _compute_BigBangFluid),
    "BigBangOscillatory": PhysicsTerm("BigBangOscillatory", "Source37 BigBang: Oscillatory resonance term", _compute_BigBangOscillatory),
    "BigBangDarkMatter": PhysicsTerm("BigBangDarkMatter", "Source37 BigBang: Dark matter fractional term", _compute_BigBangDarkMatter),
    "BigBangQuantumGravity": PhysicsTerm("BigBangQuantumGravity", "Source37 BigBang: Quantum gravity QG=(hbar*c/l_p^2)*(t/t_p) UNIQUE", _compute_BigBangQuantumGravity),
    "BigBangGravitationalWave": PhysicsTerm("BigBangGravitationalWave", "Source37 BigBang: Gravitational wave GW=h*c^2/lambda*sin(...) UNIQUE", _compute_BigBangGravitationalWave),
    "BigBangCosmicEvolution": PhysicsTerm("BigBangCosmicEvolution", "Source37 BigBang: Cosmic evolution z(t)=t_H/t-1 (redshift proxy) UNIQUE", _compute_BigBangCosmicEvolution),
    "M51Core": PhysicsTerm("M51Core", "Source38 M51: Core gravity with M_SF + expansion + SC + F_env(tidal+SF)", _compute_M51Core),
    "M51Ug1Dipole": PhysicsTerm("M51Ug1Dipole", "Source38 M51: Ug1 = I*A*omega*B (BH magnetic dipole) UNIQUE", _compute_M51Ug1Dipole),
    "M51Ug2Superconductor": PhysicsTerm("M51Ug2Superconductor", "Source38 M51: Ug2 = B^2/(2*mu_0) (superconductor term) UNIQUE", _compute_M51Ug2Superconductor),
    "M51Ug3Tidal": PhysicsTerm("M51Ug3Tidal", "Source38 M51: Ug3' = G*M_NGC5195/d^2 (external tidal) UNIQUE", _compute_M51Ug3Tidal),
    "M51Ug4Reaction": PhysicsTerm("M51Ug4Reaction", "Source38 M51: Ug4 = k_4*E_react*exp(-0.0005*t) (reaction decay) UNIQUE", _compute_M51Ug4Reaction),
    "M51UiVacuum": PhysicsTerm("M51UiVacuum", "Source38 M51: Ui vacuum interaction = lambda*(rho_SCm/rho_UA)*omega*cos(pi*t) UN", _compute_M51UiVacuum),
    "M51Lambda": PhysicsTerm("M51Lambda", "Source38 M51: Cosmological constant term", _compute_M51Lambda),
    "M51Quantum": PhysicsTerm("M51Quantum", "Source38 M51: Quantum term with psi_spiral wave function UNIQUE", _compute_M51Quantum),
    "M51Fluid": PhysicsTerm("M51Fluid", "Source38 M51: Fluid dynamics term", _compute_M51Fluid),
    "M51DarkMatter": PhysicsTerm("M51DarkMatter", "Source38 M51: Dark matter with curvature term", _compute_M51DarkMatter),
    "M51EnvironmentalForces": PhysicsTerm("M51EnvironmentalForces", "Source38 M51: F_env = F_tidal + F_SF (environmental forces) UNIQUE", _compute_M51EnvironmentalForces),
    "NGC1316Core": PhysicsTerm("NGC1316Core", "Source39 NGC1316: Core with M_merge(t)=1e10*exp(-t/tau) merger mass UNIQUE", _compute_NGC1316Core),
    "NGC1316Ug1Dipole": PhysicsTerm("NGC1316Ug1Dipole", "Source39 NGC1316: Ug1 BH magnetic dipole (M_BH=1.5e9 Msun)", _compute_NGC1316Ug1Dipole),
    "NGC1316Ug2Superconductor": PhysicsTerm("NGC1316Ug2Superconductor", "Source39 NGC1316: Ug2 superconductor magnetic energy", _compute_NGC1316Ug2Superconductor),
    "NGC1316Ug3External": PhysicsTerm("NGC1316Ug3External", "Source39 NGC1316: Ug3 external tidal from spiral companion", _compute_NGC1316Ug3External),
    "NGC1316Ug4Reaction": PhysicsTerm("NGC1316Ug4Reaction", "Source39 NGC1316: Ug4 reaction term", _compute_NGC1316Ug4Reaction),
    "NGC1316UiVacuum": PhysicsTerm("NGC1316UiVacuum", "Source39 NGC1316: Ui vacuum interaction term", _compute_NGC1316UiVacuum),
    "NGC1316Lambda": PhysicsTerm("NGC1316Lambda", "Source39 NGC1316: Cosmological constant", _compute_NGC1316Lambda),
    "NGC1316Quantum": PhysicsTerm("NGC1316Quantum", "Source39 NGC1316: Quantum with psi_dust wave function UNIQUE", _compute_NGC1316Quantum),
    "NGC1316FluidDust": PhysicsTerm("NGC1316FluidDust", "Source39 NGC1316: Fluid with rho_dust density UNIQUE", _compute_NGC1316FluidDust),
    "NGC1316DarkMatter": PhysicsTerm("NGC1316DarkMatter", "Source39 NGC1316: Dark matter term", _compute_NGC1316DarkMatter),
    "NGC1316MergerForces": PhysicsTerm("NGC1316MergerForces", "Source39 NGC1316: Merger forces = F_tidal + F_cluster UNIQUE", _compute_NGC1316MergerForces),
    "SMBHBinaryDPMResonance": PhysicsTerm("SMBHBinaryDPMResonance", "Source40 SMBHBinary: DPM core resonance f_DPM*rho_vac/c UNIQUE", _compute_SMBHBinaryDPMResonance),
    "SMBHBinaryTHzResonance": PhysicsTerm("SMBHBinaryTHzResonance", "Source40 SMBHBinary: THz pipeline resonance f_THz*sin(omega*t) UNIQUE", _compute_SMBHBinaryTHzResonance),
    "SMBHBinaryUg4iResonance": PhysicsTerm("SMBHBinaryUg4iResonance", "Source40 SMBHBinary: U_g4i reactive resonance UNIQUE", _compute_SMBHBinaryUg4iResonance),
    "SMBHBinaryPlasmoticVacuum": PhysicsTerm("SMBHBinaryPlasmoticVacuum", "Source40 SMBHBinary: Plasmotic vacuum energy UNIQUE", _compute_SMBHBinaryPlasmoticVacuum),
    "SMBHBinaryQuantumResonance": PhysicsTerm("SMBHBinaryQuantumResonance", "Source40 SMBHBinary: Quantum resonance f_quantum/unc UNIQUE", _compute_SMBHBinaryQuantumResonance),
    "SMBHBinaryFluidResonance": PhysicsTerm("SMBHBinaryFluidResonance", "Source40 SMBHBinary: Fluid resonance f_fluid*(rho/rho_ref) UNIQUE", _compute_SMBHBinaryFluidResonance),
    "SMBHBinaryOscillatoryResonance": PhysicsTerm("SMBHBinaryOscillatoryResonance", "Source40 SMBHBinary: Oscillatory resonance f*exp(-t/tau)*|psi|^2 UNIQUE", _compute_SMBHBinaryOscillatoryResonance),
    "SMBHBinaryExpansionResonance": PhysicsTerm("SMBHBinaryExpansionResonance", "Source40 SMBHBinary: Cosmic expansion resonance (Aether freq) UNIQUE", _compute_SMBHBinaryExpansionResonance),
    "SMBHBinaryCoalescence": PhysicsTerm("SMBHBinaryCoalescence", "Source40 SMBHBinary: Binary coalescence dynamics (2PN waveform) UNIQUE", _compute_SMBHBinaryCoalescence),
    "BackgroundAetherMinkowski": PhysicsTerm("BackgroundAetherMinkowski", "Source41 BackgroundAether: Minkowski metric g_μν=[1,-1,-1,-1] UNIQUE", _compute_BackgroundAetherMinkowski),
    "BackgroundAetherPerturbedMetric": PhysicsTerm("BackgroundAetherPerturbedMetric", "Source41 BackgroundAether: Perturbed metric A=g+eta*T_s UNIQUE", _compute_BackgroundAetherPerturbedMetric),
    "BackgroundAetherStressEnergy": PhysicsTerm("BackgroundAetherStressEnergy", "Source41 BackgroundAether: Stress-energy tensor T_s=1.123e7 J/m³ UNIQUE", _compute_BackgroundAetherStressEnergy),
    "BackgroundAetherCoupling": PhysicsTerm("BackgroundAetherCoupling", "Source41 BackgroundAether: Coupling parameter eta~1e-22 UNIQUE", _compute_BackgroundAetherCoupling),
    "HeavisideFraction": PhysicsTerm("HeavisideFraction", "Source42 Heaviside: f_Heaviside = 0.01 (unitless threshold fraction)", _compute_HeavisideFraction),
    "HeavisideFactor": PhysicsTerm("HeavisideFactor", "Source42 Heaviside: (1 + 10^13 × f_H) = 1 + 1e11 amplification UNIQUE", _compute_HeavisideFactor),
    "HeavisideUmBase": PhysicsTerm("HeavisideUmBase", "Source42 Heaviside: U_m base = (μ/r)(1-e^(-γt cos(πt_n)))φ̂·P_SCm·E_react", _compute_HeavisideUmBase),
    "HeavisideUmWithHeaviside": PhysicsTerm("HeavisideUmWithHeaviside", "Source42 Heaviside: U_m × (1+10^13·f_H) × (1+f_quasi) UNIQUE amplification", _compute_HeavisideUmWithHeaviside),
    "HeavisideUmWithoutHeaviside": PhysicsTerm("HeavisideUmWithoutHeaviside", "Source42 Heaviside: U_m without Heaviside (baseline comparison)", _compute_HeavisideUmWithoutHeaviside),
    "HeavisideQuasiFactor": PhysicsTerm("HeavisideQuasiFactor", "Source42 Heaviside: (1 + f_quasi) quasi-scaling factor", _compute_HeavisideQuasiFactor),
    "HeavisideExponentialDecay": PhysicsTerm("HeavisideExponentialDecay", "Source42 Heaviside: (1 - e^(-γt cos(πt_n))) time-modulated decay", _compute_HeavisideExponentialDecay),
    "HeavisideMagneticMoment": PhysicsTerm("HeavisideMagneticMoment", "Source42 Heaviside: μ_j/r_j magnetic moment density", _compute_HeavisideMagneticMoment),
    "HeavisideAmplificationRatio": PhysicsTerm("HeavisideAmplificationRatio", "Source42 Heaviside: Amplification ratio ~1e11 UNIQUE threshold effect", _compute_HeavisideAmplificationRatio),
    "HydrogenResonance_Amplitude": PhysicsTerm("HydrogenResonance_Amplitude", "Source43 HydrogenRes: Nuclear resonance amplitude A_res for PToE UNIQUE", _compute_HydrogenResonance_Amplitude),
    "HydrogenResonance_Frequency": PhysicsTerm("HydrogenResonance_Frequency", "Source43 HydrogenRes: Resonance frequency f_res from binding energy UNIQUE", _compute_HydrogenResonance_Frequency),
    "HydrogenResonance_DeepPairing": PhysicsTerm("HydrogenResonance_DeepPairing", "Source43 HydrogenRes: Deep pairing U_dp between nuclear components UNIQUE", _compute_HydrogenResonance_DeepPairing),
    "HydrogenResonance_NuclearCoupling": PhysicsTerm("HydrogenResonance_NuclearCoupling", "Source43 HydrogenRes: Nuclear coupling k_nuc with N/Z ratio UNIQUE", _compute_HydrogenResonance_NuclearCoupling),
    "HydrogenResonance_ShellCorrection": PhysicsTerm("HydrogenResonance_ShellCorrection", "Source43 HydrogenRes: Shell correction S_shell from magic numbers UNIQUE", _compute_HydrogenResonance_ShellCorrection),
    "HydrogenResonance_Buoyancy": PhysicsTerm("HydrogenResonance_Buoyancy", "Source43 HydrogenRes: Enhanced buoyancy with nuclear pairing+magic UNIQUE", _compute_HydrogenResonance_Buoyancy),
    "HydrogenResonance_Superconductive": PhysicsTerm("HydrogenResonance_Superconductive", "Source43 HydrogenRes: SC with quantum coherence+tunneling UNIQUE", _compute_HydrogenResonance_Superconductive),
    "HydrogenResonance_PairingEnergy": PhysicsTerm("HydrogenResonance_PairingEnergy", "Source43 HydrogenRes: Pairing energy δ_pair for even-odd nuclei UNIQUE", _compute_HydrogenResonance_PairingEnergy),
    "HydrogenResonance_MagicStability": PhysicsTerm("HydrogenResonance_MagicStability", "Source43 HydrogenRes: Magic number stability enhancement UNIQUE", _compute_HydrogenResonance_MagicStability),
    "HydrogenResonance_TunnelingProb": PhysicsTerm("HydrogenResonance_TunnelingProb", "Source43 HydrogenRes: Coulomb barrier tunneling probability UNIQUE", _compute_HydrogenResonance_TunnelingProb),
    "HydrogenResonance_Integrand": PhysicsTerm("HydrogenResonance_Integrand", "Source43 HydrogenRes: Full H_res integrand formula UNIQUE", _compute_HydrogenResonance_Integrand),
    "HydrogenResonance_Full": PhysicsTerm("HydrogenResonance_Full", "Source43 HydrogenRes: Complete H_res = integrand × x2 UNIQUE", _compute_HydrogenResonance_Full),
    "SurfaceMagnetic_Oscillation": PhysicsTerm("SurfaceMagnetic_Oscillation", "Source43 SurfaceMag: Solar magnetic field oscillation B_j UNIQUE", _compute_SurfaceMagnetic_Oscillation),
    "SurfaceMagnetic_MinField": PhysicsTerm("SurfaceMagnetic_MinField", "Source43 SurfaceMag: Minimum magnetic field B_s_min (quiet Sun) UNIQUE", _compute_SurfaceMagnetic_MinField),
    "SurfaceMagnetic_MaxField": PhysicsTerm("SurfaceMagnetic_MaxField", "Source43 SurfaceMag: Maximum magnetic field B_s_max (sunspot) UNIQUE", _compute_SurfaceMagnetic_MaxField),
    "SurfaceMagnetic_CycleReference": PhysicsTerm("SurfaceMagnetic_CycleReference", "Source43 SurfaceMag: Solar cycle reference field B_ref UNIQUE", _compute_SurfaceMagnetic_CycleReference),
    "Source10_F_U_Bi_iTerm": PhysicsTerm("Source10_F_U_Bi_iTerm", "UQFF Core buoyancy force from source10.cpp", _compute_Source10_F_U_Bi_iTerm),
    "Source10_g_UQFFTerm": PhysicsTerm("Source10_g_UQFFTerm", "Compressed UQFF gravity field", _compute_Source10_g_UQFFTerm),
    "Source10_DPM_ResonanceTerm": PhysicsTerm("Source10_DPM_ResonanceTerm", "Dark matter resonance term", _compute_Source10_DPM_ResonanceTerm),
    "Source10_E_cmTerm": PhysicsTerm("Source10_E_cmTerm", "Center of mass energy calculation", _compute_Source10_E_cmTerm),
    "Source11_Ug1Term": PhysicsTerm("Source11_Ug1Term", "U_g1 gravity term from celestial body dynamics", _compute_Source11_Ug1Term),
    "Source11_Ug2Term": PhysicsTerm("Source11_Ug2Term", "U_g2 gravity term with solar wind modulation", _compute_Source11_Ug2Term),
    "Source11_Ug3Term": PhysicsTerm("Source11_Ug3Term", "U_g3 magnetic coupling term", _compute_Source11_Ug3Term),
    "Source11_Ug4Term": PhysicsTerm("Source11_Ug4Term", "U_g4 black hole contribution term", _compute_Source11_Ug4Term),
    "Source11_UmTerm": PhysicsTerm("Source11_UmTerm", "Universal magnetism term with string contributions", _compute_Source11_UmTerm),
    "UQFFModule5_DarkMatterHaloTerm": PhysicsTerm("UQFFModule5_DarkMatterHaloTerm", "NFW dark matter halo contribution", _compute_UQFFModule5_DarkMatterHaloTerm),
    "UQFFModule5_VacuumEnergyTerm": PhysicsTerm("UQFFModule5_VacuumEnergyTerm", "Vacuum energy fluctuation term", _compute_UQFFModule5_VacuumEnergyTerm),
    "Source5_CompressedBaseTerm": PhysicsTerm("Source5_CompressedBaseTerm", "MUGE compressed base gravity calculation", _compute_Source5_CompressedBaseTerm),
    "Source5_CompressedExpansionTerm": PhysicsTerm("Source5_CompressedExpansionTerm", "MUGE compressed expansion (Hubble flow)", _compute_Source5_CompressedExpansionTerm),
    "Source5_CompressedQuantumTerm": PhysicsTerm("Source5_CompressedQuantumTerm", "Compressed quantum uncertainty term", _compute_Source5_CompressedQuantumTerm),
    "Source5_CompressedCosmTerm": PhysicsTerm("Source5_CompressedCosmTerm", "Compressed cosmological constant (Lambda)", _compute_Source5_CompressedCosmTerm),
    "Source5_CompressedFluidTerm": PhysicsTerm("Source5_CompressedFluidTerm", "Compressed fluid dynamics contribution", _compute_Source5_CompressedFluidTerm),
    "Source5_CompressedPerturbationTerm": PhysicsTerm("Source5_CompressedPerturbationTerm", "Compressed metric perturbation term", _compute_Source5_CompressedPerturbationTerm),
    "Source5_ResonanceMUGETerm": PhysicsTerm("Source5_ResonanceMUGETerm", "MUGE resonance term with quality factor", _compute_Source5_ResonanceMUGETerm),
    "Source5_CompressedSuperAdjTerm": PhysicsTerm("Source5_CompressedSuperAdjTerm", "Compressed supercritical magnetic adjustment", _compute_Source5_CompressedSuperAdjTerm),
    "Source5_CompressedEnvTerm": PhysicsTerm("Source5_CompressedEnvTerm", "Compressed environmental thermal term", _compute_Source5_CompressedEnvTerm),
    "Source5_CompressedUgSumTerm": PhysicsTerm("Source5_CompressedUgSumTerm", "Total compressed Ug gravity sum", _compute_Source5_CompressedUgSumTerm),
    "ESO137_BuoyancyTerm": PhysicsTerm("ESO137_BuoyancyTerm", "ESO137-001 buoyancy force", _compute_ESO137_BuoyancyTerm),
    "ESO137_CompressedTerm": PhysicsTerm("ESO137_CompressedTerm", "ESO137-001 compressed integrand", _compute_ESO137_CompressedTerm),
    "ESO137_ResonantTerm": PhysicsTerm("ESO137_ResonantTerm", "ESO137-001 DPM resonance", _compute_ESO137_ResonantTerm),
    "ESO137_SuperconductiveTerm": PhysicsTerm("ESO137_SuperconductiveTerm", "ESO137-001 superconductive term", _compute_ESO137_SuperconductiveTerm),
    "ESO137_LENRTerm": PhysicsTerm("ESO137_LENRTerm", "ESO137-001 LENR resonance term", _compute_ESO137_LENRTerm),
    "ESO137_CompressedGTerm": PhysicsTerm("ESO137_CompressedGTerm", "ESO137-001 compressed g(r,t)", _compute_ESO137_CompressedGTerm),
    "ESO137_Q_waveTerm": PhysicsTerm("ESO137_Q_waveTerm", "ESO137-001 quantum wave", _compute_ESO137_Q_waveTerm),
    "ESO137_Ub1Term": PhysicsTerm("ESO137_Ub1Term", "ESO137-001 Ub1 component", _compute_ESO137_Ub1Term),
    "ESO137_UiTerm": PhysicsTerm("ESO137_UiTerm", "ESO137-001 Ui unified field", _compute_ESO137_UiTerm),
    "NGC1365_BuoyancyTerm": PhysicsTerm("NGC1365_BuoyancyTerm", "NGC1365 barred spiral buoyancy", _compute_NGC1365_BuoyancyTerm),
    "NGC1365_CompressedTerm": PhysicsTerm("NGC1365_CompressedTerm", "NGC1365 compressed integrand", _compute_NGC1365_CompressedTerm),
    "NGC1365_LENRTerm": PhysicsTerm("NGC1365_LENRTerm", "NGC1365 LENR resonance", _compute_NGC1365_LENRTerm),
    "Vela_BuoyancyTerm": PhysicsTerm("Vela_BuoyancyTerm", "Vela Pulsar buoyancy force", _compute_Vela_BuoyancyTerm),
    "Vela_CompressedTerm": PhysicsTerm("Vela_CompressedTerm", "Vela compressed integrand", _compute_Vela_CompressedTerm),
    "Vela_LENRTerm": PhysicsTerm("Vela_LENRTerm", "Vela LENR resonance", _compute_Vela_LENRTerm),
    "ASASSN14li_BuoyancyTerm": PhysicsTerm("ASASSN14li_BuoyancyTerm", "ASASSN-14li TDE buoyancy", _compute_ASASSN14li_BuoyancyTerm),
    "ASASSN14li_CompressedTerm": PhysicsTerm("ASASSN14li_CompressedTerm", "ASASSN-14li compressed integrand", _compute_ASASSN14li_CompressedTerm),
    "ASASSN14li_LENRTerm": PhysicsTerm("ASASSN14li_LENRTerm", "ASASSN-14li LENR resonance", _compute_ASASSN14li_LENRTerm),
    "ElGordo_BuoyancyTerm": PhysicsTerm("ElGordo_BuoyancyTerm", "El Gordo cluster buoyancy", _compute_ElGordo_BuoyancyTerm),
    "ElGordo_CompressedTerm": PhysicsTerm("ElGordo_CompressedTerm", "El Gordo compressed integrand", _compute_ElGordo_CompressedTerm),
    "ElGordo_LENRTerm": PhysicsTerm("ElGordo_LENRTerm", "El Gordo LENR resonance", _compute_ElGordo_LENRTerm),
    "Heaviside_FactorTerm": PhysicsTerm("Heaviside_FactorTerm", "Heaviside step function", _compute_Heaviside_FactorTerm),
    "Heaviside_UmBaseTerm": PhysicsTerm("Heaviside_UmBaseTerm", "Um base calculation", _compute_Heaviside_UmBaseTerm),
    "Heliosphere_H_SCmTerm": PhysicsTerm("Heliosphere_H_SCmTerm", "Heliosphere thickness H_SCm", _compute_Heliosphere_H_SCmTerm),
    "UgIndex_U_giTerm": PhysicsTerm("UgIndex_U_giTerm", "Indexed U_gi component", _compute_UgIndex_U_giTerm),
    "UgIndex_K_iTerm": PhysicsTerm("UgIndex_K_iTerm", "Coupling constant K_i", _compute_UgIndex_K_iTerm),
    "Inertia_Lambda_iTerm": PhysicsTerm("Inertia_Lambda_iTerm", "Inertia coupling λ_i", _compute_Inertia_Lambda_iTerm),
    "Inertia_U_iTerm": PhysicsTerm("Inertia_U_iTerm", "Inertial field U_i", _compute_Inertia_U_iTerm),
    "MagMoment_Mu_jTerm": PhysicsTerm("MagMoment_Mu_jTerm", "Magnetic moment μ_j", _compute_MagMoment_Mu_jTerm),
    "MagMoment_B_jTerm": PhysicsTerm("MagMoment_B_jTerm", "Time-varying B_j field", _compute_MagMoment_B_jTerm),
    "BlackHole_M_bhTerm": PhysicsTerm("BlackHole_M_bhTerm", "Black hole mass", _compute_BlackHole_M_bhTerm),
    "BlackHole_U_b1Term": PhysicsTerm("BlackHole_U_b1Term", "Black hole U_b1 term", _compute_BlackHole_U_b1Term),
    "NegativeTime_CosPiTnTerm": PhysicsTerm("NegativeTime_CosPiTnTerm", "cos(π t_n) negative time", _compute_NegativeTime_CosPiTnTerm),
    "NegativeTime_ExpTermTerm": PhysicsTerm("NegativeTime_ExpTermTerm", "exp(-γt cos(πt_n))", _compute_NegativeTime_ExpTermTerm),
    "PiConstant_MuJExampleTerm": PhysicsTerm("PiConstant_MuJExampleTerm", "μ_j with π modulation", _compute_PiConstant_MuJExampleTerm),
    "CorePenetration_P_coreTerm": PhysicsTerm("CorePenetration_P_coreTerm", "Core penetration probability", _compute_CorePenetration_P_coreTerm),
    "QuasiLong_FactorTerm": PhysicsTerm("QuasiLong_FactorTerm", "Quasi-longitudinal geometric factor", _compute_QuasiLong_FactorTerm),
    "OuterBubble_S_r_RbTerm": PhysicsTerm("OuterBubble_S_r_RbTerm", "Outer field bubble step function S(r-R_b)", _compute_OuterBubble_S_r_RbTerm),
    "OuterBubble_U_g2Term": PhysicsTerm("OuterBubble_U_g2Term", "U_g2 with outer field bubble step function", _compute_OuterBubble_U_g2Term),
    "OuterBubble_R_bTerm": PhysicsTerm("OuterBubble_R_bTerm", "Radius of outer field bubble (100 AU)", _compute_OuterBubble_R_bTerm),
    "OuterBubble_R_bInAUTerm": PhysicsTerm("OuterBubble_R_bInAUTerm", "Outer field bubble radius in AU", _compute_OuterBubble_R_bInAUTerm),
    "Reciprocation_Gamma_sTerm": PhysicsTerm("Reciprocation_Gamma_sTerm", "Reciprocation decay rate γ in s⁻¹", _compute_Reciprocation_Gamma_sTerm),
    "Reciprocation_CosPiTnTerm": PhysicsTerm("Reciprocation_CosPiTnTerm", "Negative time oscillation cos(π t_n)", _compute_Reciprocation_CosPiTnTerm),
    "Reciprocation_ExpTermTerm": PhysicsTerm("Reciprocation_ExpTermTerm", "Reciprocation exponential decay term", _compute_Reciprocation_ExpTermTerm),
    "Reciprocation_OneMinusExpTerm": PhysicsTerm("Reciprocation_OneMinusExpTerm", "U_m reciprocation factor 1-exp(-γt cos(πt_n))", _compute_Reciprocation_OneMinusExpTerm),
    "Reciprocation_Gamma_dayTerm": PhysicsTerm("Reciprocation_Gamma_dayTerm", "Reciprocation decay rate in day⁻¹", _compute_Reciprocation_Gamma_dayTerm),
    "Reciprocation_UmExampleTerm": PhysicsTerm("Reciprocation_UmExampleTerm", "U_m with reciprocation decay", _compute_Reciprocation_UmExampleTerm),
    "ScmPenetration_UmBaseTerm": PhysicsTerm("ScmPenetration_UmBaseTerm", "Base U_m term before SCm penetration", _compute_ScmPenetration_UmBaseTerm),
    "ScmPenetration_UmContributionTerm": PhysicsTerm("ScmPenetration_UmContributionTerm", "U_m with [SCm] penetration factor", _compute_ScmPenetration_UmContributionTerm),
    "ScmPenetration_P_SCmTerm": PhysicsTerm("ScmPenetration_P_SCmTerm", "[SCm] superconducting penetration factor", _compute_ScmPenetration_P_SCmTerm),
    "ScmPenetration_UmPlanetTerm": PhysicsTerm("ScmPenetration_UmPlanetTerm", "U_m for planets with reduced P_SCm", _compute_ScmPenetration_UmPlanetTerm),
    "ScmReactivity_Kappa_sTerm": PhysicsTerm("ScmReactivity_Kappa_sTerm", "[SCm] reactivity decay rate κ in s⁻¹", _compute_ScmReactivity_Kappa_sTerm),
    "ScmReactivity_E_reactTerm": PhysicsTerm("ScmReactivity_E_reactTerm", "[SCm] reactivity energy E_react with decay", _compute_ScmReactivity_E_reactTerm),
    "ScmReactivity_UmExampleTerm": PhysicsTerm("ScmReactivity_UmExampleTerm", "U_m with [SCm] reactivity decay", _compute_ScmReactivity_UmExampleTerm),
    "ScmReactivity_Kappa_dayTerm": PhysicsTerm("ScmReactivity_Kappa_dayTerm", "[SCm] reactivity decay rate in day⁻¹", _compute_ScmReactivity_Kappa_dayTerm),
    "SolarCycle_Omega_cTerm": PhysicsTerm("SolarCycle_Omega_cTerm", "Solar cycle frequency ω_c", _compute_SolarCycle_Omega_cTerm),
    "SolarCycle_SinOmegaCTTerm": PhysicsTerm("SolarCycle_SinOmegaCTTerm", "Solar cycle oscillation sin(ω_c t)", _compute_SolarCycle_SinOmegaCTTerm),
    "SolarCycle_MuJExampleTerm": PhysicsTerm("SolarCycle_MuJExampleTerm", "Magnetic moment μ_j with solar cycle modulation", _compute_SolarCycle_MuJExampleTerm),
    "SolarCycle_PeriodYearsTerm": PhysicsTerm("SolarCycle_PeriodYearsTerm", "Solar cycle period in years", _compute_SolarCycle_PeriodYearsTerm),
    "SolarCycle_B_j_ModulatedTerm": PhysicsTerm("SolarCycle_B_j_ModulatedTerm", "Surface magnetic field B_j with solar cycle", _compute_SolarCycle_B_j_ModulatedTerm),
    "SolarWindMod_ModulationFactorTerm": PhysicsTerm("SolarWindMod_ModulationFactorTerm", "Solar wind modulation (1 + δ_sw v_sw)", _compute_SolarWindMod_ModulationFactorTerm),
    "SolarWindMod_U_g2Term": PhysicsTerm("SolarWindMod_U_g2Term", "U_g2 with solar wind modulation", _compute_SolarWindMod_U_g2Term),
    "SolarWindMod_Delta_swTerm": PhysicsTerm("SolarWindMod_Delta_swTerm", "Solar wind modulation parameter δ_sw", _compute_SolarWindMod_Delta_swTerm),
    "SolarWindMod_U_g2_no_modTerm": PhysicsTerm("SolarWindMod_U_g2_no_modTerm", "U_g2 without solar wind modulation", _compute_SolarWindMod_U_g2_no_modTerm),
    "SolarWindVel_ModulationFactorTerm": PhysicsTerm("SolarWindVel_ModulationFactorTerm", "Velocity-based modulation factor", _compute_SolarWindVel_ModulationFactorTerm),
    "SolarWindVel_U_g2Term": PhysicsTerm("SolarWindVel_U_g2Term", "U_g2 with velocity modulation", _compute_SolarWindVel_U_g2Term),
    "SolarWindVel_V_swTerm": PhysicsTerm("SolarWindVel_V_swTerm", "Solar wind velocity v_sw", _compute_SolarWindVel_V_swTerm),
    "SolarWindVel_V_swKmSTerm": PhysicsTerm("SolarWindVel_V_swKmSTerm", "Solar wind velocity in km/s", _compute_SolarWindVel_V_swKmSTerm),
    "SolarWindVel_U_g2_no_swTerm": PhysicsTerm("SolarWindVel_U_g2_no_swTerm", "U_g2 without solar wind velocity", _compute_SolarWindVel_U_g2_no_swTerm),
    "StellarMass_M_sOverR2Term": PhysicsTerm("StellarMass_M_sOverR2Term", "Stellar mass per area M_s/r²", _compute_StellarMass_M_sOverR2Term),
    "StellarMass_U_g1Term": PhysicsTerm("StellarMass_U_g1Term", "U_g1 with stellar mass", _compute_StellarMass_U_g1Term),
    "StellarMass_U_g2Term": PhysicsTerm("StellarMass_U_g2Term", "U_g2 with stellar mass", _compute_StellarMass_U_g2Term),
    "StellarMass_M_sTerm": PhysicsTerm("StellarMass_M_sTerm", "Stellar mass M_s (1 M_☉)", _compute_StellarMass_M_sTerm),
    "StellarMass_M_sInMsunTerm": PhysicsTerm("StellarMass_M_sInMsunTerm", "Stellar mass in solar masses", _compute_StellarMass_M_sInMsunTerm),
    "StellarRot_Omega_s_tTerm": PhysicsTerm("StellarRot_Omega_s_tTerm", "Time-dependent rotation rate ω_s(t)", _compute_StellarRot_Omega_s_tTerm),
    "StellarRot_U_g3Term": PhysicsTerm("StellarRot_U_g3Term", "U_g3 with rotation modulation", _compute_StellarRot_U_g3Term),
    "StellarRot_U_iTerm": PhysicsTerm("StellarRot_U_iTerm", "U_i with rotation and negative time", _compute_StellarRot_U_iTerm),
    "StellarRot_Omega_sTerm": PhysicsTerm("StellarRot_Omega_sTerm", "Stellar rotation rate ω_s", _compute_StellarRot_Omega_sTerm),
    "StellarRot_Period_daysTerm": PhysicsTerm("StellarRot_Period_daysTerm", "Rotation period in days", _compute_StellarRot_Period_daysTerm),
    "StepFunc_S_r_RbTerm": PhysicsTerm("StepFunc_S_r_RbTerm", "Step function for field activation", _compute_StepFunc_S_r_RbTerm),
    "StepFunc_U_g2Term": PhysicsTerm("StepFunc_U_g2Term", "U_g2 activated by step function", _compute_StepFunc_U_g2Term),
    "StressEnergy_T_sTerm": PhysicsTerm("StressEnergy_T_sTerm", "Stress-energy tensor scalar T_s", _compute_StressEnergy_T_sTerm),
    "StressEnergy_A_mu_nuTerm": PhysicsTerm("StressEnergy_A_mu_nuTerm", "Perturbed metric A_μν", _compute_StressEnergy_A_mu_nuTerm),
    "StressEnergy_PerturbationTerm": PhysicsTerm("StressEnergy_PerturbationTerm", "Metric perturbation ε T_s", _compute_StressEnergy_PerturbationTerm),
    "SurfaceMagField_B_jTerm": PhysicsTerm("SurfaceMagField_B_jTerm", "Magnetic field B_j scaled by surface field", _compute_SurfaceMagField_B_jTerm),
    "SurfaceMagField_U_g3_exampleTerm": PhysicsTerm("SurfaceMagField_U_g3_exampleTerm", "U_g3 with surface field modulation", _compute_SurfaceMagField_U_g3_exampleTerm),
    "SurfaceMagField_B_s_minTerm": PhysicsTerm("SurfaceMagField_B_s_minTerm", "Minimum surface magnetic field B_s", _compute_SurfaceMagField_B_s_minTerm),
    "SurfaceMagField_B_s_maxTerm": PhysicsTerm("SurfaceMagField_B_s_maxTerm", "Maximum surface magnetic field B_s", _compute_SurfaceMagField_B_s_maxTerm),
    "SurfaceTemp_B_j_hypotheticalTerm": PhysicsTerm("SurfaceTemp_B_j_hypotheticalTerm", "Hypothetical B_j with temperature scaling", _compute_SurfaceTemp_B_j_hypotheticalTerm),
    "SurfaceTemp_U_g3_exampleTerm": PhysicsTerm("SurfaceTemp_U_g3_exampleTerm", "U_g3 with temperature scaling", _compute_SurfaceTemp_U_g3_exampleTerm),
    "SurfaceTemp_T_sTerm": PhysicsTerm("SurfaceTemp_T_sTerm", "Surface temperature T_s", _compute_SurfaceTemp_T_sTerm),
    "TimeReversalZone_TRZFactorTerm": PhysicsTerm("TimeReversalZone_TRZFactorTerm", "Time-reversal zone factor f_TRZ", _compute_TimeReversalZone_TRZFactorTerm),
    "TimeReversalZone_U_i_baseTerm": PhysicsTerm("TimeReversalZone_U_i_baseTerm", "Base U_i without TRZ enhancement", _compute_TimeReversalZone_U_i_baseTerm),
    "TimeReversalZone_U_iTerm": PhysicsTerm("TimeReversalZone_U_iTerm", "U_i with TRZ enhancement", _compute_TimeReversalZone_U_iTerm),
    "TimeReversalZone_F_TRZTerm": PhysicsTerm("TimeReversalZone_F_TRZTerm", "TRZ enhancement factor (1 + f_TRZ)", _compute_TimeReversalZone_F_TRZTerm),
    "TimeReversalZone_U_i_no_TRZTerm": PhysicsTerm("TimeReversalZone_U_i_no_TRZTerm", "U_i without TRZ", _compute_TimeReversalZone_U_i_no_TRZTerm),
    "Ug1Defect_Delta_defTerm": PhysicsTerm("Ug1Defect_Delta_defTerm", "Ug1 defect factor δ_def", _compute_Ug1Defect_Delta_defTerm),
    "Ug1Defect_U_g1Term": PhysicsTerm("Ug1Defect_U_g1Term", "U_g1 with defect modulation", _compute_Ug1Defect_U_g1Term),
    "Ug1Defect_Period_yearsTerm": PhysicsTerm("Ug1Defect_Period_yearsTerm", "Defect oscillation period in years", _compute_Ug1Defect_Period_yearsTerm),
    "Ug3DiskVec_PhiHat_jTerm": PhysicsTerm("Ug3DiskVec_PhiHat_jTerm", "Disk plane unit vector φ̂_j", _compute_Ug3DiskVec_PhiHat_jTerm),
    "Ug3DiskVec_UmBaseTerm": PhysicsTerm("Ug3DiskVec_UmBaseTerm", "Base U_m term", _compute_Ug3DiskVec_UmBaseTerm),
    "Ug3DiskVec_UmContributionTerm": PhysicsTerm("Ug3DiskVec_UmContributionTerm", "U_m with disk vector contribution", _compute_Ug3DiskVec_UmContributionTerm),
    "Ug3DiskVec_PhiHatMagnitudeTerm": PhysicsTerm("Ug3DiskVec_PhiHatMagnitudeTerm", "Magnitude of φ̂_j (unit vector)", _compute_Ug3DiskVec_PhiHatMagnitudeTerm),
    "AetherVacDensity_Rho_vac_ATerm": PhysicsTerm("AetherVacDensity_Rho_vac_ATerm", "Aether vacuum energy density ρ_vac,A", _compute_AetherVacDensity_Rho_vac_ATerm),
    "AetherVacDensity_T_s_contributionTerm": PhysicsTerm("AetherVacDensity_T_s_contributionTerm", "T_s with aether contribution", _compute_AetherVacDensity_T_s_contributionTerm),
    "AetherVacDensity_A_mu_nuTerm": PhysicsTerm("AetherVacDensity_A_mu_nuTerm", "Metric perturbation ε T_s from aether", _compute_AetherVacDensity_A_mu_nuTerm),
    "AetherVacDensity_Epsilon_factorTerm": PhysicsTerm("AetherVacDensity_Epsilon_factorTerm", "Perturbation parameter ε", _compute_AetherVacDensity_Epsilon_factorTerm),
    "UnivInertiaVac_Rho_vac_UiTerm": PhysicsTerm("UnivInertiaVac_Rho_vac_UiTerm", "Universal Inertia vacuum density ρ_vac,Ui", _compute_UnivInertiaVac_Rho_vac_UiTerm),
    "UnivInertiaVac_U_i_baseTerm": PhysicsTerm("UnivInertiaVac_U_i_baseTerm", "Base U_i inertial term", _compute_UnivInertiaVac_U_i_baseTerm),
    "UnivInertiaVac_U_iTerm": PhysicsTerm("UnivInertiaVac_U_iTerm", "U_i with vacuum density scaling", _compute_UnivInertiaVac_U_iTerm),
    "ScmVacDensity_Rho_vac_SCmTerm": PhysicsTerm("ScmVacDensity_Rho_vac_SCmTerm", "[SCm] vacuum energy density ρ_vac,SCm", _compute_ScmVacDensity_Rho_vac_SCmTerm),
    "ScmVacDensity_U_g2_exampleTerm": PhysicsTerm("ScmVacDensity_U_g2_exampleTerm", "U_g2 with SCm vacuum scaling", _compute_ScmVacDensity_U_g2_exampleTerm),
    "ScmVacDensity_U_i_exampleTerm": PhysicsTerm("ScmVacDensity_U_i_exampleTerm", "U_i with SCm vacuum coupling", _compute_ScmVacDensity_U_i_exampleTerm),
    "UaVacDensity_Rho_vac_UATerm": PhysicsTerm("UaVacDensity_Rho_vac_UATerm", "[UA] vacuum energy density ρ_vac,UA", _compute_UaVacDensity_Rho_vac_UATerm),
    "UaVacDensity_U_g2_exampleTerm": PhysicsTerm("UaVacDensity_U_g2_exampleTerm", "U_g2 with UA vacuum scaling", _compute_UaVacDensity_U_g2_exampleTerm),
    "UaVacDensity_U_i_exampleTerm": PhysicsTerm("UaVacDensity_U_i_exampleTerm", "U_i with UA vacuum coupling", _compute_UaVacDensity_U_i_exampleTerm),
    "UnivInertiaVac2_U_i_baseTerm": PhysicsTerm("UnivInertiaVac2_U_i_baseTerm", "U_i base (source130 variant)", _compute_UnivInertiaVac2_U_i_baseTerm),
    "UnivInertiaVac2_U_iTerm": PhysicsTerm("UnivInertiaVac2_U_iTerm", "U_i full (source130 variant)", _compute_UnivInertiaVac2_U_iTerm),
    "ScmVelocity_V_scmTerm": PhysicsTerm("ScmVelocity_V_scmTerm", "[SCm] velocity v_SCm", _compute_ScmVelocity_V_scmTerm),
    "ScmVelocity_E_react_baseTerm": PhysicsTerm("ScmVelocity_E_react_baseTerm", "Base E_react energy", _compute_ScmVelocity_E_react_baseTerm),
    "ScmVelocity_E_reactTerm": PhysicsTerm("ScmVelocity_E_reactTerm", "E_react with decay", _compute_ScmVelocity_E_reactTerm),
    "ScmVelocity_UmExampleTerm": PhysicsTerm("ScmVelocity_UmExampleTerm", "U_m with E_react", _compute_ScmVelocity_UmExampleTerm),
    "ButterflyNebula_DPM_momentumTerm": PhysicsTerm("ButterflyNebula_DPM_momentumTerm", "DPM momentum term", _compute_ButterflyNebula_DPM_momentumTerm),
    "ButterflyNebula_DPM_gravityTerm": PhysicsTerm("ButterflyNebula_DPM_gravityTerm", "DPM gravity contribution", _compute_ButterflyNebula_DPM_gravityTerm),
    "ButterflyNebula_LENR_termTerm": PhysicsTerm("ButterflyNebula_LENR_termTerm", "LENR resonance factor", _compute_ButterflyNebula_LENR_termTerm),
    "ButterflyNebula_F_U_BiTerm": PhysicsTerm("ButterflyNebula_F_U_BiTerm", "Total UQFF force", _compute_ButterflyNebula_F_U_BiTerm),
    "CentaurusA_DPM_momentumTerm": PhysicsTerm("CentaurusA_DPM_momentumTerm", "DPM momentum NGC5128", _compute_CentaurusA_DPM_momentumTerm),
    "CentaurusA_F_U_BiTerm": PhysicsTerm("CentaurusA_F_U_BiTerm", "NGC5128 UQFF force", _compute_CentaurusA_F_U_BiTerm),
    "Abell2256_IntegrandTerm": PhysicsTerm("Abell2256_IntegrandTerm", "Abell 2256 integrand", _compute_Abell2256_IntegrandTerm),
    "Abell2256_DPM_resonanceTerm": PhysicsTerm("Abell2256_DPM_resonanceTerm", "DPM resonance oscillation", _compute_Abell2256_DPM_resonanceTerm),
    "Abell2256_X2Term": PhysicsTerm("Abell2256_X2Term", "Quadratic root x2", _compute_Abell2256_X2Term),
    "Abell2256_LENR_termTerm": PhysicsTerm("Abell2256_LENR_termTerm", "LENR resonance", _compute_Abell2256_LENR_termTerm),
    "Abell2256_Q_waveTerm": PhysicsTerm("Abell2256_Q_waveTerm", "Quantum wave term", _compute_Abell2256_Q_waveTerm),
    "Abell2256_Ub1Term": PhysicsTerm("Abell2256_Ub1Term", "U_b1 buoyancy", _compute_Abell2256_Ub1Term),
    "Abell2256_UiTerm": PhysicsTerm("Abell2256_UiTerm", "Universal Inertia", _compute_Abell2256_UiTerm),
    "Abell2256_FTerm": PhysicsTerm("Abell2256_FTerm", "Total force F", _compute_Abell2256_FTerm),
    "Abell2256_CompressedTerm": PhysicsTerm("Abell2256_CompressedTerm", "Compressed gravity g", _compute_Abell2256_CompressedTerm),
    "Abell2256_ResonantTerm": PhysicsTerm("Abell2256_ResonantTerm", "Resonant mode", _compute_Abell2256_ResonantTerm),
    "Abell2256_BuoyancyTerm": PhysicsTerm("Abell2256_BuoyancyTerm", "Buoyancy term", _compute_Abell2256_BuoyancyTerm),
    "Abell2256_SuperconductiveTerm": PhysicsTerm("Abell2256_SuperconductiveTerm", "Superconductive term", _compute_Abell2256_SuperconductiveTerm),
    "Abell2256_CompressedGTerm": PhysicsTerm("Abell2256_CompressedGTerm", "Compressed G field", _compute_Abell2256_CompressedGTerm),
    "ASASSN14li_IntegrandTerm": PhysicsTerm("ASASSN14li_IntegrandTerm", "ASASSN-14li integrand", _compute_ASASSN14li_IntegrandTerm),
    "ASASSN14li_DPM_resonanceTerm": PhysicsTerm("ASASSN14li_DPM_resonanceTerm", "TDE resonance", _compute_ASASSN14li_DPM_resonanceTerm),
    "ASASSN14li_X2Term": PhysicsTerm("ASASSN14li_X2Term", "Quadratic root", _compute_ASASSN14li_X2Term),
    "ASASSN14li_LENR_termTerm": PhysicsTerm("ASASSN14li_LENR_termTerm", "LENR TDE", _compute_ASASSN14li_LENR_termTerm),
    "ASASSN14li_Q_waveTerm": PhysicsTerm("ASASSN14li_Q_waveTerm", "Quantum wave", _compute_ASASSN14li_Q_waveTerm),
    "ASASSN14li_FTerm": PhysicsTerm("ASASSN14li_FTerm", "TDE force", _compute_ASASSN14li_FTerm),
    "CentaurusA_X2Term": PhysicsTerm("CentaurusA_X2Term", "CenA quadratic root", _compute_CentaurusA_X2Term),
    "CentaurusA_LENR_termTerm": PhysicsTerm("CentaurusA_LENR_termTerm", "CenA LENR", _compute_CentaurusA_LENR_termTerm),
    "CentaurusA_FTerm": PhysicsTerm("CentaurusA_FTerm", "CenA total force", _compute_CentaurusA_FTerm),
    "CentaurusA_CompressedTerm": PhysicsTerm("CentaurusA_CompressedTerm", "CenA compressed g", _compute_CentaurusA_CompressedTerm),
    "CrabNebula_IntegrandTerm": PhysicsTerm("CrabNebula_IntegrandTerm", "Crab integrand", _compute_CrabNebula_IntegrandTerm),
    "CrabNebula_LENR_termTerm": PhysicsTerm("CrabNebula_LENR_termTerm", "Crab LENR", _compute_CrabNebula_LENR_termTerm),
    "CrabNebula_FTerm": PhysicsTerm("CrabNebula_FTerm", "Crab total force", _compute_CrabNebula_FTerm),
    "CrabNebula_CompressedTerm": PhysicsTerm("CrabNebula_CompressedTerm", "Crab compressed g", _compute_CrabNebula_CompressedTerm),
    "ElGordo_IntegrandTerm": PhysicsTerm("ElGordo_IntegrandTerm", "El Gordo integrand", _compute_ElGordo_IntegrandTerm),
    "ElGordo_LENR_termTerm": PhysicsTerm("ElGordo_LENR_termTerm", "El Gordo LENR", _compute_ElGordo_LENR_termTerm),
    "ElGordo_FTerm": PhysicsTerm("ElGordo_FTerm", "El Gordo total force", _compute_ElGordo_FTerm),
    "ESO137_FTerm": PhysicsTerm("ESO137_FTerm", "ESO 137-001 total force", _compute_ESO137_FTerm),
    "IC2163_IntegrandTerm": PhysicsTerm("IC2163_IntegrandTerm", "IC 2163 integrand", _compute_IC2163_IntegrandTerm),
    "IC2163_LENR_termTerm": PhysicsTerm("IC2163_LENR_termTerm", "IC 2163 LENR", _compute_IC2163_LENR_termTerm),
    "IC2163_FTerm": PhysicsTerm("IC2163_FTerm", "IC 2163 total force", _compute_IC2163_FTerm),
    "IC2163_CompressedTerm": PhysicsTerm("IC2163_CompressedTerm", "IC 2163 compressed g", _compute_IC2163_CompressedTerm),
    "J1610_IntegrandTerm": PhysicsTerm("J1610_IntegrandTerm", "Integrand M/r³ for J1610+1811 high-z quasar (z=3.122)", _compute_J1610_IntegrandTerm),
    "J1610_LENR_termTerm": PhysicsTerm("J1610_LENR_termTerm", "LENR resonance: 1/ρ₀ for J1610 (dominant term)", _compute_J1610_LENR_termTerm),
    "J1610_FTerm": PhysicsTerm("J1610_FTerm", "Total UQFF force for J1610+1811", _compute_J1610_FTerm),
    "J1610_CompressedTerm": PhysicsTerm("J1610_CompressedTerm", "Compressed gravity for high-z quasar", _compute_J1610_CompressedTerm),
    "JupiterAurorae_IntegrandTerm": PhysicsTerm("JupiterAurorae_IntegrandTerm", "Integrand for Jupiter auroral physics (Io plasma torus)", _compute_JupiterAurorae_IntegrandTerm),
    "JupiterAurorae_LENR_termTerm": PhysicsTerm("JupiterAurorae_LENR_termTerm", "LENR resonance for Jupiter (magnetospheric coupling)", _compute_JupiterAurorae_LENR_termTerm),
    "JupiterAurorae_FTerm": PhysicsTerm("JupiterAurorae_FTerm", "Total UQFF force for Jupiter aurorae", _compute_JupiterAurorae_FTerm),
    "JupiterAurorae_CompressedTerm": PhysicsTerm("JupiterAurorae_CompressedTerm", "Compressed g for Jupiter", _compute_JupiterAurorae_CompressedTerm),
    "LagoonNebula_IntegrandTerm": PhysicsTerm("LagoonNebula_IntegrandTerm", "Integrand for Lagoon Nebula (star formation region)", _compute_LagoonNebula_IntegrandTerm),
    "LagoonNebula_LENR_termTerm": PhysicsTerm("LagoonNebula_LENR_termTerm", "LENR for Lagoon Nebula", _compute_LagoonNebula_LENR_termTerm),
    "LagoonNebula_FTerm": PhysicsTerm("LagoonNebula_FTerm", "Total UQFF force for Lagoon Nebula", _compute_LagoonNebula_FTerm),
    "LagoonNebula_CompressedTerm": PhysicsTerm("LagoonNebula_CompressedTerm", "Compressed g for Lagoon Nebula", _compute_LagoonNebula_CompressedTerm),
    "LagoonNebulaVar_IntegrandTerm": PhysicsTerm("LagoonNebulaVar_IntegrandTerm", "Lagoon Nebula variant integrand", _compute_LagoonNebulaVar_IntegrandTerm),
    "LagoonNebulaVar_LENR_termTerm": PhysicsTerm("LagoonNebulaVar_LENR_termTerm", "Lagoon variant LENR", _compute_LagoonNebulaVar_LENR_termTerm),
    "LagoonNebulaVar_FTerm": PhysicsTerm("LagoonNebulaVar_FTerm", "Lagoon variant total force", _compute_LagoonNebulaVar_FTerm),
    "LagoonNebulaVar_CompressedTerm": PhysicsTerm("LagoonNebulaVar_CompressedTerm", "Lagoon variant compressed g", _compute_LagoonNebulaVar_CompressedTerm),
    "M87Jet_IntegrandTerm": PhysicsTerm("M87Jet_IntegrandTerm", "Integrand for M87 relativistic jet (~0.5c, EHT SMBH M=6.5e9 M☉)", _compute_M87Jet_IntegrandTerm),
    "M87Jet_LENR_termTerm": PhysicsTerm("M87Jet_LENR_termTerm", "LENR for M87 jet (superluminal knots)", _compute_M87Jet_LENR_termTerm),
    "M87Jet_FTerm": PhysicsTerm("M87Jet_FTerm", "Total UQFF force for M87 jet", _compute_M87Jet_FTerm),
    "M87Jet_CompressedTerm": PhysicsTerm("M87Jet_CompressedTerm", "Compressed g for M87 jet", _compute_M87Jet_CompressedTerm),
    "NGC1365_IntegrandTerm": PhysicsTerm("NGC1365_IntegrandTerm", "Integrand for NGC 1365 double-barred Seyfert (z=0.0055)", _compute_NGC1365_IntegrandTerm),
    "NGC1365_LENR_termTerm": PhysicsTerm("NGC1365_LENR_termTerm", "LENR for NGC 1365 (AGN X-ray variability)", _compute_NGC1365_LENR_termTerm),
    "NGC1365_FTerm": PhysicsTerm("NGC1365_FTerm", "Total UQFF force for NGC 1365", _compute_NGC1365_FTerm),
    "NGC2207_IntegrandTerm": PhysicsTerm("NGC2207_IntegrandTerm", "Integrand for NGC 2207 colliding galaxies (tidal bridge)", _compute_NGC2207_IntegrandTerm),
    "NGC2207_LENR_termTerm": PhysicsTerm("NGC2207_LENR_termTerm", "LENR for NGC 2207 (interaction-driven)", _compute_NGC2207_LENR_termTerm),
    "NGC2207_FTerm": PhysicsTerm("NGC2207_FTerm", "Total UQFF force for NGC 2207", _compute_NGC2207_FTerm),
    "NGC2207_CompressedTerm": PhysicsTerm("NGC2207_CompressedTerm", "Compressed g for NGC 2207", _compute_NGC2207_CompressedTerm),
    "RAquarii_IntegrandTerm": PhysicsTerm("RAquarii_IntegrandTerm", "Integrand for R Aquarii symbiotic binary (Mira variable + white dwarf)", _compute_RAquarii_IntegrandTerm),
    "RAquarii_LENR_termTerm": PhysicsTerm("RAquarii_LENR_termTerm", "LENR for R Aquarii (mass transfer jets)", _compute_RAquarii_LENR_termTerm),
    "RAquarii_FTerm": PhysicsTerm("RAquarii_FTerm", "Total UQFF force for R Aquarii", _compute_RAquarii_FTerm),
    "RAquarii_CompressedTerm": PhysicsTerm("RAquarii_CompressedTerm", "Compressed g for R Aquarii", _compute_RAquarii_CompressedTerm),
    "SgrAStar_IntegrandTerm": PhysicsTerm("SgrAStar_IntegrandTerm", "Integrand for Sagittarius A* (Galactic Center, Nobel 2020)", _compute_SgrAStar_IntegrandTerm),
    "SgrAStar_LENR_termTerm": PhysicsTerm("SgrAStar_LENR_termTerm", "LENR for Sgr A* (flare activity)", _compute_SgrAStar_LENR_termTerm),
    "SgrAStar_FTerm": PhysicsTerm("SgrAStar_FTerm", "Total UQFF force for Sgr A*", _compute_SgrAStar_FTerm),
    "SgrAStar_CompressedTerm": PhysicsTerm("SgrAStar_CompressedTerm", "Compressed g for Sgr A*", _compute_SgrAStar_CompressedTerm),
    "SPTCLJ2215_IntegrandTerm": PhysicsTerm("SPTCLJ2215_IntegrandTerm", "Integrand for SPT-CL J2215 galaxy cluster (z=0.7)", _compute_SPTCLJ2215_IntegrandTerm),
    "SPTCLJ2215_LENR_termTerm": PhysicsTerm("SPTCLJ2215_LENR_termTerm", "LENR for SPT-CL J2215", _compute_SPTCLJ2215_LENR_termTerm),
    "SPTCLJ2215_FTerm": PhysicsTerm("SPTCLJ2215_FTerm", "Total UQFF force for SPT-CL J2215", _compute_SPTCLJ2215_FTerm),
    "SPTCLJ2215_CompressedTerm": PhysicsTerm("SPTCLJ2215_CompressedTerm", "Compressed g for SPT-CL J2215", _compute_SPTCLJ2215_CompressedTerm),
    "StephanQuintet_IntegrandTerm": PhysicsTerm("StephanQuintet_IntegrandTerm", "Integrand for Stephan's Quintet compact group (JWST NIRCam target)", _compute_StephanQuintet_IntegrandTerm),
    "StephanQuintet_LENR_termTerm": PhysicsTerm("StephanQuintet_LENR_termTerm", "LENR for Stephan's Quintet", _compute_StephanQuintet_LENR_termTerm),
    "StephanQuintet_FTerm": PhysicsTerm("StephanQuintet_FTerm", "Total UQFF force for Stephan's Quintet", _compute_StephanQuintet_FTerm),
    "StephanQuintet_CompressedTerm": PhysicsTerm("StephanQuintet_CompressedTerm", "Compressed g for Stephan's Quintet", _compute_StephanQuintet_CompressedTerm),
    "VelaPulsar_IntegrandTerm": PhysicsTerm("VelaPulsar_IntegrandTerm", "Integrand for Vela Pulsar (89 ms period, Gum Nebula)", _compute_VelaPulsar_IntegrandTerm),
    "VelaPulsar_LENR_termTerm": PhysicsTerm("VelaPulsar_LENR_termTerm", "LENR for Vela Pulsar", _compute_VelaPulsar_LENR_termTerm),
    "VelaPulsar_FTerm": PhysicsTerm("VelaPulsar_FTerm", "Total UQFF force for Vela Pulsar", _compute_VelaPulsar_FTerm),
    "VelaPulsar_CompressedTerm": PhysicsTerm("VelaPulsar_CompressedTerm", "Compressed g for Vela Pulsar", _compute_VelaPulsar_CompressedTerm),
    "NGC4993_IntegrandTerm": PhysicsTerm("NGC4993_IntegrandTerm", "NGC 4993 (neutron star merger host)", _compute_NGC4993_IntegrandTerm),
    "NGC4993_LENR_termTerm": PhysicsTerm("NGC4993_LENR_termTerm", "LENR for NGC 4993", _compute_NGC4993_LENR_termTerm),
    "NGC4993_FTerm": PhysicsTerm("NGC4993_FTerm", "Total force NGC 4993", _compute_NGC4993_FTerm),
    "NGC4993_CompressedTerm": PhysicsTerm("NGC4993_CompressedTerm", "Compressed g NGC 4993", _compute_NGC4993_CompressedTerm),
    "NGC6240_IntegrandTerm": PhysicsTerm("NGC6240_IntegrandTerm", "NGC 6240 (merging dual AGN)", _compute_NGC6240_IntegrandTerm),
    "NGC6240_LENR_termTerm": PhysicsTerm("NGC6240_LENR_termTerm", "LENR for NGC 6240", _compute_NGC6240_LENR_termTerm),
    "NGC6240_FTerm": PhysicsTerm("NGC6240_FTerm", "Total force NGC 6240", _compute_NGC6240_FTerm),
    "NGC6240_CompressedTerm": PhysicsTerm("NGC6240_CompressedTerm", "Compressed g NGC 6240", _compute_NGC6240_CompressedTerm),
    "OmegaCentauri_IntegrandTerm": PhysicsTerm("OmegaCentauri_IntegrandTerm", "Omega Centauri (largest MW globular)", _compute_OmegaCentauri_IntegrandTerm),
    "OmegaCentauri_LENR_termTerm": PhysicsTerm("OmegaCentauri_LENR_termTerm", "LENR for Omega Centauri", _compute_OmegaCentauri_LENR_termTerm),
    "OmegaCentauri_FTerm": PhysicsTerm("OmegaCentauri_FTerm", "Total force Omega Centauri", _compute_OmegaCentauri_FTerm),
    "OmegaCentauri_CompressedTerm": PhysicsTerm("OmegaCentauri_CompressedTerm", "Compressed g Omega Centauri", _compute_OmegaCentauri_CompressedTerm),
    "OrionNebula_IntegrandTerm": PhysicsTerm("OrionNebula_IntegrandTerm", "Orion Nebula (stellar nursery)", _compute_OrionNebula_IntegrandTerm),
    "OrionNebula_LENR_termTerm": PhysicsTerm("OrionNebula_LENR_termTerm", "LENR for Orion Nebula", _compute_OrionNebula_LENR_termTerm),
    "OrionNebula_FTerm": PhysicsTerm("OrionNebula_FTerm", "Total force Orion Nebula", _compute_OrionNebula_FTerm),
    "OrionNebula_CompressedTerm": PhysicsTerm("OrionNebula_CompressedTerm", "Compressed g Orion Nebula", _compute_OrionNebula_CompressedTerm),
    "Perseus_IntegrandTerm": PhysicsTerm("Perseus_IntegrandTerm", "Perseus Cluster (Chandra cavities)", _compute_Perseus_IntegrandTerm),
    "Perseus_LENR_termTerm": PhysicsTerm("Perseus_LENR_termTerm", "LENR for Perseus", _compute_Perseus_LENR_termTerm),
    "Perseus_FTerm": PhysicsTerm("Perseus_FTerm", "Total force Perseus", _compute_Perseus_FTerm),
    "Perseus_CompressedTerm": PhysicsTerm("Perseus_CompressedTerm", "Compressed g Perseus", _compute_Perseus_CompressedTerm),
    "PolarRing_IntegrandTerm": PhysicsTerm("PolarRing_IntegrandTerm", "Polar Ring Galaxy (NGC 4650A)", _compute_PolarRing_IntegrandTerm),
    "PolarRing_LENR_termTerm": PhysicsTerm("PolarRing_LENR_termTerm", "LENR for Polar Ring", _compute_PolarRing_LENR_termTerm),
    "PolarRing_FTerm": PhysicsTerm("PolarRing_FTerm", "Total force Polar Ring", _compute_PolarRing_FTerm),
    "PolarRing_CompressedTerm": PhysicsTerm("PolarRing_CompressedTerm", "Compressed g Polar Ring", _compute_PolarRing_CompressedTerm),
    "QuasarJ0313_IntegrandTerm": PhysicsTerm("QuasarJ0313_IntegrandTerm", "Quasar J0313 (earliest known z=7.64)", _compute_QuasarJ0313_IntegrandTerm),
    "QuasarJ0313_LENR_termTerm": PhysicsTerm("QuasarJ0313_LENR_termTerm", "LENR for J0313", _compute_QuasarJ0313_LENR_termTerm),
    "QuasarJ0313_FTerm": PhysicsTerm("QuasarJ0313_FTerm", "Total force J0313", _compute_QuasarJ0313_FTerm),
    "QuasarJ0313_CompressedTerm": PhysicsTerm("QuasarJ0313_CompressedTerm", "Compressed g J0313", _compute_QuasarJ0313_CompressedTerm),
    "RXJ1713_IntegrandTerm": PhysicsTerm("RXJ1713_IntegrandTerm", "RX J1713 SNR (TeV gamma-ray)", _compute_RXJ1713_IntegrandTerm),
    "RXJ1713_LENR_termTerm": PhysicsTerm("RXJ1713_LENR_termTerm", "LENR for RX J1713", _compute_RXJ1713_LENR_termTerm),
    "RXJ1713_FTerm": PhysicsTerm("RXJ1713_FTerm", "Total force RX J1713", _compute_RXJ1713_FTerm),
    "RXJ1713_CompressedTerm": PhysicsTerm("RXJ1713_CompressedTerm", "Compressed g RX J1713", _compute_RXJ1713_CompressedTerm),
    "BuoyancyAstro_IntegrandTerm": PhysicsTerm("BuoyancyAstro_IntegrandTerm", "Multi-system buoyancy integrand (5 systems)", _compute_BuoyancyAstro_IntegrandTerm),
    "BuoyancyAstro_LENR_termTerm": PhysicsTerm("BuoyancyAstro_LENR_termTerm", "LENR for buoyancy systems", _compute_BuoyancyAstro_LENR_termTerm),
    "BuoyancyAstro_FTerm": PhysicsTerm("BuoyancyAstro_FTerm", "Total force buoyancy astro", _compute_BuoyancyAstro_FTerm),
    "BuoyancyAstro_CompressedTerm": PhysicsTerm("BuoyancyAstro_CompressedTerm", "Compressed g buoyancy astro", _compute_BuoyancyAstro_CompressedTerm),
    "BuoyancyCNB_IntegrandTerm": PhysicsTerm("BuoyancyCNB_IntegrandTerm", "CNB neutrino integrand Centaurus A", _compute_BuoyancyCNB_IntegrandTerm),
    "BuoyancyCNB_LENR_termTerm": PhysicsTerm("BuoyancyCNB_LENR_termTerm", "LENR for CNB", _compute_BuoyancyCNB_LENR_termTerm),
    "BuoyancyCNB_FTerm": PhysicsTerm("BuoyancyCNB_FTerm", "Total force CNB", _compute_BuoyancyCNB_FTerm),
    "BuoyancyCNB_CompressedTerm": PhysicsTerm("BuoyancyCNB_CompressedTerm", "Compressed g CNB", _compute_BuoyancyCNB_CompressedTerm),
    "AstroSystems163_IntegrandTerm": PhysicsTerm("AstroSystems163_IntegrandTerm", "NGC/AT2024tvd integrand", _compute_AstroSystems163_IntegrandTerm),
    "AstroSystems163_LENR_termTerm": PhysicsTerm("AstroSystems163_LENR_termTerm", "LENR for NGC multi", _compute_AstroSystems163_LENR_termTerm),
    "AstroSystems163_FTerm": PhysicsTerm("AstroSystems163_FTerm", "Total force NGC multi", _compute_AstroSystems163_FTerm),
    "AstroSystems163_CompressedTerm": PhysicsTerm("AstroSystems163_CompressedTerm", "Compressed g NGC multi", _compute_AstroSystems163_CompressedTerm),
    "NebulaTriadic_IntegrandTerm": PhysicsTerm("NebulaTriadic_IntegrandTerm", "Nebula triadic integrand (gas ionization)", _compute_NebulaTriadic_IntegrandTerm),
    "NebulaTriadic_LENR_termTerm": PhysicsTerm("NebulaTriadic_LENR_termTerm", "LENR for nebulae", _compute_NebulaTriadic_LENR_termTerm),
    "NebulaTriadic_FTerm": PhysicsTerm("NebulaTriadic_FTerm", "Total force nebula triadic", _compute_NebulaTriadic_FTerm),
    "NebulaTriadic_CompressedTerm": PhysicsTerm("NebulaTriadic_CompressedTerm", "Compressed g nebula triadic", _compute_NebulaTriadic_CompressedTerm),
    "BuoyancyModule_IntegrandTerm": PhysicsTerm("BuoyancyModule_IntegrandTerm", "M74/Eagle/M84 integrand", _compute_BuoyancyModule_IntegrandTerm),
    "BuoyancyModule_LENR_termTerm": PhysicsTerm("BuoyancyModule_LENR_termTerm", "LENR for M74/M16/M84", _compute_BuoyancyModule_LENR_termTerm),
    "BuoyancyModule_FTerm": PhysicsTerm("BuoyancyModule_FTerm", "Total force M74/M16/M84", _compute_BuoyancyModule_FTerm),
    "BuoyancyModule_CompressedTerm": PhysicsTerm("BuoyancyModule_CompressedTerm", "Compressed g M74/M16/M84", _compute_BuoyancyModule_CompressedTerm),
    "UQFF8Astro_IntegrandTerm": PhysicsTerm("UQFF8Astro_IntegrandTerm", "8+1 astro systems integrand (26 quantum states)", _compute_UQFF8Astro_IntegrandTerm),
    "UQFF8Astro_LENR_termTerm": PhysicsTerm("UQFF8Astro_LENR_termTerm", "LENR for 8 astro", _compute_UQFF8Astro_LENR_termTerm),
    "UQFF8Astro_FTerm": PhysicsTerm("UQFF8Astro_FTerm", "Total force 8 astro", _compute_UQFF8Astro_FTerm),
    "UQFF8Astro_CompressedTerm": PhysicsTerm("UQFF8Astro_CompressedTerm", "Compressed g 8 astro", _compute_UQFF8Astro_CompressedTerm),
    "UQFFCore_IntegrandTerm": PhysicsTerm("UQFFCore_IntegrandTerm", "UQFF Core M82/IC418/Canis", _compute_UQFFCore_IntegrandTerm),
    "UQFFCore_LENR_termTerm": PhysicsTerm("UQFFCore_LENR_termTerm", "LENR for UQFF Core", _compute_UQFFCore_LENR_termTerm),
    "UQFFCore_FTerm": PhysicsTerm("UQFFCore_FTerm", "Total force UQFF Core", _compute_UQFFCore_FTerm),
    "UQFFCore_CompressedTerm": PhysicsTerm("UQFFCore_CompressedTerm", "Compressed g UQFF Core", _compute_UQFFCore_CompressedTerm),
    "Buoyancy168_IntegrandTerm": PhysicsTerm("Buoyancy168_IntegrandTerm", "SN 1006 buoyancy integrand", _compute_Buoyancy168_IntegrandTerm),
    "Buoyancy168_LENR_termTerm": PhysicsTerm("Buoyancy168_LENR_termTerm", "LENR for SN 1006", _compute_Buoyancy168_LENR_termTerm),
    "Buoyancy168_FTerm": PhysicsTerm("Buoyancy168_FTerm", "Total force SN 1006", _compute_Buoyancy168_FTerm),
    "Buoyancy168_CompressedTerm": PhysicsTerm("Buoyancy168_CompressedTerm", "Compressed g SN 1006", _compute_Buoyancy168_CompressedTerm),
    "CassiniBuoyancy_IntegrandTerm": PhysicsTerm("CassiniBuoyancy_IntegrandTerm", "Cassini Saturn U_Bi/U_Ii/U_Mi", _compute_CassiniBuoyancy_IntegrandTerm),
    "CassiniBuoyancy_LENR_termTerm": PhysicsTerm("CassiniBuoyancy_LENR_termTerm", "LENR for Cassini", _compute_CassiniBuoyancy_LENR_termTerm),
    "CassiniBuoyancy_FTerm": PhysicsTerm("CassiniBuoyancy_FTerm", "Total force Cassini", _compute_CassiniBuoyancy_FTerm),
    "CassiniBuoyancy_CompressedTerm": PhysicsTerm("CassiniBuoyancy_CompressedTerm", "Compressed g Cassini", _compute_CassiniBuoyancy_CompressedTerm),
    "MultiAstro_IntegrandTerm": PhysicsTerm("MultiAstro_IntegrandTerm", "11 multi-astro systems (DPM + ACP)", _compute_MultiAstro_IntegrandTerm),
    "MultiAstro_LENR_termTerm": PhysicsTerm("MultiAstro_LENR_termTerm", "LENR for multi-astro", _compute_MultiAstro_LENR_termTerm),
    "MultiAstro_FTerm": PhysicsTerm("MultiAstro_FTerm", "Total force multi-astro", _compute_MultiAstro_FTerm),
    "MultiAstro_CompressedTerm": PhysicsTerm("MultiAstro_CompressedTerm", "Compressed g multi-astro", _compute_MultiAstro_CompressedTerm),
    "EightAstro_IntegrandTerm": PhysicsTerm("EightAstro_IntegrandTerm", "8 LMC/NGC systems (AFGL/NGC346)", _compute_EightAstro_IntegrandTerm),
    "EightAstro_LENR_termTerm": PhysicsTerm("EightAstro_LENR_termTerm", "LENR for 8 astro", _compute_EightAstro_LENR_termTerm),
    "EightAstro_FTerm": PhysicsTerm("EightAstro_FTerm", "Total force 8 astro", _compute_EightAstro_FTerm),
    "EightAstro_CompressedTerm": PhysicsTerm("EightAstro_CompressedTerm", "Compressed g 8 astro", _compute_EightAstro_CompressedTerm),
    "NineteenAstro_IntegrandTerm": PhysicsTerm("NineteenAstro_IntegrandTerm", "19 systems 26D polynomial (NGC2264/UGC10214/M42/Tarantula)", _compute_NineteenAstro_IntegrandTerm),
    "NineteenAstro_LENR_termTerm": PhysicsTerm("NineteenAstro_LENR_termTerm", "LENR for 19 astro (26 quantum states)", _compute_NineteenAstro_LENR_termTerm),
    "NineteenAstro_FTerm": PhysicsTerm("NineteenAstro_FTerm", "Total force 19 astro 26D", _compute_NineteenAstro_FTerm),
    "NineteenAstro_CompressedTerm": PhysicsTerm("NineteenAstro_CompressedTerm", "Compressed g 19 astro 26D", _compute_NineteenAstro_CompressedTerm),
    "WolframFieldUnity_IntegrandTerm": PhysicsTerm("WolframFieldUnity_IntegrandTerm", "Wolfram hypergraph emergent spacetime (26 quantum states, PI infinity decoder)", _compute_WolframFieldUnity_IntegrandTerm),
    "WolframFieldUnity_LENR_termTerm": PhysicsTerm("WolframFieldUnity_LENR_termTerm", "LENR for Wolfram hypergraph (causal invariance)", _compute_WolframFieldUnity_LENR_termTerm),
    "WolframFieldUnity_FTerm": PhysicsTerm("WolframFieldUnity_FTerm", "Total force Wolfram (multiway quantum evolution, sacred time constants)", _compute_WolframFieldUnity_FTerm),
    "WolframFieldUnity_CompressedTerm": PhysicsTerm("WolframFieldUnity_CompressedTerm", "Compressed g Wolfram (312-digit PI decoder, Mayan/Biblical time)", _compute_WolframFieldUnity_CompressedTerm),
    "J1610_DPM_resonanceTerm": PhysicsTerm("J1610_DPM_resonanceTerm", "J1610 DPM magnetic resonance g·μB·B₀/(ℏ·ω₀)", _compute_J1610_DPM_resonanceTerm),
    "J1610_Q_waveTerm": PhysicsTerm("J1610_Q_waveTerm", "J1610 quantum wave function ψ(t)", _compute_J1610_Q_waveTerm),
    "J1610_Ub1Term": PhysicsTerm("J1610_Ub1Term", "J1610 universal buoyancy U_b1", _compute_J1610_Ub1Term),
    "J1610_UiTerm": PhysicsTerm("J1610_UiTerm", "J1610 universal inertia U_i(t)", _compute_J1610_UiTerm),
    "J1610_X2Term": PhysicsTerm("J1610_X2Term", "J1610 quadratic root x₂ for UQFF", _compute_J1610_X2Term),
    "J1610_ActivationTerm": PhysicsTerm("J1610_ActivationTerm", "J1610 activation force at 300 Hz", _compute_J1610_ActivationTerm),
    "J1610_DirectedEnergyTerm": PhysicsTerm("J1610_DirectedEnergyTerm", "J1610 directed energy coupling to X-ray luminosity", _compute_J1610_DirectedEnergyTerm),
    "J1610_NeutronTerm": PhysicsTerm("J1610_NeutronTerm", "J1610 neutron coupling k·σ", _compute_J1610_NeutronTerm),
    "J1610_RelativisticTerm": PhysicsTerm("J1610_RelativisticTerm", "J1610 relativistic coherence (E_cm_astro/E_cm)²", _compute_J1610_RelativisticTerm),
    "J1610_NeutrinoTerm": PhysicsTerm("J1610_NeutrinoTerm", "J1610 neutrino background force", _compute_J1610_NeutrinoTerm),
    "M87Jet_DPM_resonanceTerm": PhysicsTerm("M87Jet_DPM_resonanceTerm", "M87 jet DPM resonance", _compute_M87Jet_DPM_resonanceTerm),
    "M87Jet_Q_waveTerm": PhysicsTerm("M87Jet_Q_waveTerm", "M87 quantum wave", _compute_M87Jet_Q_waveTerm),
    "M87Jet_Ub1Term": PhysicsTerm("M87Jet_Ub1Term", "M87 U_b1", _compute_M87Jet_Ub1Term),
    "M87Jet_UiTerm": PhysicsTerm("M87Jet_UiTerm", "M87 inertia", _compute_M87Jet_UiTerm),
    "M87Jet_X2Term": PhysicsTerm("M87Jet_X2Term", "M87 x₂ root", _compute_M87Jet_X2Term),
    "M87Jet_ActivationTerm": PhysicsTerm("M87Jet_ActivationTerm", "M87 activation", _compute_M87Jet_ActivationTerm),
    "M87Jet_DirectedEnergyTerm": PhysicsTerm("M87Jet_DirectedEnergyTerm", "M87 directed energy", _compute_M87Jet_DirectedEnergyTerm),
    "M87Jet_NeutronTerm": PhysicsTerm("M87Jet_NeutronTerm", "M87 neutron", _compute_M87Jet_NeutronTerm),
    "M87Jet_RelativisticTerm": PhysicsTerm("M87Jet_RelativisticTerm", "M87 relativistic", _compute_M87Jet_RelativisticTerm),
    "M87Jet_NeutrinoTerm": PhysicsTerm("M87Jet_NeutrinoTerm", "M87 neutrino", _compute_M87Jet_NeutrinoTerm),
    "SgrAStar_DPM_resonanceTerm": PhysicsTerm("SgrAStar_DPM_resonanceTerm", "Sgr A* DPM magnetic resonance", _compute_SgrAStar_DPM_resonanceTerm),
    "SgrAStar_Q_waveTerm": PhysicsTerm("SgrAStar_Q_waveTerm", "Sgr A* quantum wave", _compute_SgrAStar_Q_waveTerm),
    "SgrAStar_Ub1Term": PhysicsTerm("SgrAStar_Ub1Term", "Sgr A* U_b1", _compute_SgrAStar_Ub1Term),
    "SgrAStar_UiTerm": PhysicsTerm("SgrAStar_UiTerm", "Sgr A* inertia", _compute_SgrAStar_UiTerm),
    "SgrAStar_X2Term": PhysicsTerm("SgrAStar_X2Term", "Sgr A* quadratic root", _compute_SgrAStar_X2Term),
    "SgrAStar_ActivationTerm": PhysicsTerm("SgrAStar_ActivationTerm", "Sgr A* activation", _compute_SgrAStar_ActivationTerm),
    "SgrAStar_DirectedEnergyTerm": PhysicsTerm("SgrAStar_DirectedEnergyTerm", "Sgr A* directed energy", _compute_SgrAStar_DirectedEnergyTerm),
    "SgrAStar_NeutronTerm": PhysicsTerm("SgrAStar_NeutronTerm", "Sgr A* neutron", _compute_SgrAStar_NeutronTerm),
    "SgrAStar_RelativisticTerm": PhysicsTerm("SgrAStar_RelativisticTerm", "Sgr A* relativistic", _compute_SgrAStar_RelativisticTerm),
    "SgrAStar_NeutrinoTerm": PhysicsTerm("SgrAStar_NeutrinoTerm", "Sgr A* neutrino", _compute_SgrAStar_NeutrinoTerm),
    "NGC1365_DPM_resonanceTerm": PhysicsTerm("NGC1365_DPM_resonanceTerm", "NGC1365 DPM resonance", _compute_NGC1365_DPM_resonanceTerm),
    "NGC1365_Q_waveTerm": PhysicsTerm("NGC1365_Q_waveTerm", "NGC1365 quantum wave", _compute_NGC1365_Q_waveTerm),
    "NGC1365_Ub1Term": PhysicsTerm("NGC1365_Ub1Term", "NGC1365 U_b1", _compute_NGC1365_Ub1Term),
    "NGC1365_UiTerm": PhysicsTerm("NGC1365_UiTerm", "NGC1365 inertia", _compute_NGC1365_UiTerm),
    "NGC1365_X2Term": PhysicsTerm("NGC1365_X2Term", "NGC1365 x₂", _compute_NGC1365_X2Term),
    "NGC1365_ActivationTerm": PhysicsTerm("NGC1365_ActivationTerm", "NGC1365 activation", _compute_NGC1365_ActivationTerm),
    "NGC1365_DirectedEnergyTerm": PhysicsTerm("NGC1365_DirectedEnergyTerm", "NGC1365 directed energy", _compute_NGC1365_DirectedEnergyTerm),
    "NGC1365_NeutronTerm": PhysicsTerm("NGC1365_NeutronTerm", "NGC1365 neutron", _compute_NGC1365_NeutronTerm),
    "NGC1365_RelativisticTerm": PhysicsTerm("NGC1365_RelativisticTerm", "NGC1365 relativistic", _compute_NGC1365_RelativisticTerm),
    "NGC1365_NeutrinoTerm": PhysicsTerm("NGC1365_NeutrinoTerm", "NGC1365 neutrino", _compute_NGC1365_NeutrinoTerm),
    "VelaPulsar_DPM_resonanceTerm": PhysicsTerm("VelaPulsar_DPM_resonanceTerm", "Vela Pulsar DPM resonance (89ms period)", _compute_VelaPulsar_DPM_resonanceTerm),
    "VelaPulsar_Q_waveTerm": PhysicsTerm("VelaPulsar_Q_waveTerm", "Vela quantum wave (11.2 Hz)", _compute_VelaPulsar_Q_waveTerm),
    "VelaPulsar_Ub1Term": PhysicsTerm("VelaPulsar_Ub1Term", "Vela U_b1", _compute_VelaPulsar_Ub1Term),
    "VelaPulsar_UiTerm": PhysicsTerm("VelaPulsar_UiTerm", "Vela inertia", _compute_VelaPulsar_UiTerm),
    "VelaPulsar_X2Term": PhysicsTerm("VelaPulsar_X2Term", "Vela x₂ (pulsar variant)", _compute_VelaPulsar_X2Term),
    "VelaPulsar_ActivationTerm": PhysicsTerm("VelaPulsar_ActivationTerm", "Vela activation (pulse frequency)", _compute_VelaPulsar_ActivationTerm),
    "VelaPulsar_DirectedEnergyTerm": PhysicsTerm("VelaPulsar_DirectedEnergyTerm", "Vela directed energy", _compute_VelaPulsar_DirectedEnergyTerm),
    "VelaPulsar_NeutronTerm": PhysicsTerm("VelaPulsar_NeutronTerm", "Vela neutron", _compute_VelaPulsar_NeutronTerm),
    "VelaPulsar_RelativisticTerm": PhysicsTerm("VelaPulsar_RelativisticTerm", "Vela relativistic", _compute_VelaPulsar_RelativisticTerm),
    "VelaPulsar_NeutrinoTerm": PhysicsTerm("VelaPulsar_NeutrinoTerm", "Vela neutrino", _compute_VelaPulsar_NeutrinoTerm),
    "NGC2207_DPM_resonanceTerm": PhysicsTerm("NGC2207_DPM_resonanceTerm", "NGC2207 collision DPM", _compute_NGC2207_DPM_resonanceTerm),
    "NGC2207_Q_waveTerm": PhysicsTerm("NGC2207_Q_waveTerm", "NGC2207 tidal quantum wave", _compute_NGC2207_Q_waveTerm),
    "NGC2207_Ub1Term": PhysicsTerm("NGC2207_Ub1Term", "NGC2207 U_b1", _compute_NGC2207_Ub1Term),
    "NGC2207_UiTerm": PhysicsTerm("NGC2207_UiTerm", "NGC2207 inertia", _compute_NGC2207_UiTerm),
    "NGC2207_X2Term": PhysicsTerm("NGC2207_X2Term", "NGC2207 x₂", _compute_NGC2207_X2Term),
    "NGC2207_ActivationTerm": PhysicsTerm("NGC2207_ActivationTerm", "NGC2207 activation", _compute_NGC2207_ActivationTerm),
    "NGC2207_DirectedEnergyTerm": PhysicsTerm("NGC2207_DirectedEnergyTerm", "NGC2207 directed energy", _compute_NGC2207_DirectedEnergyTerm),
    "NGC2207_NeutronTerm": PhysicsTerm("NGC2207_NeutronTerm", "NGC2207 neutron", _compute_NGC2207_NeutronTerm),
    "NGC2207_RelativisticTerm": PhysicsTerm("NGC2207_RelativisticTerm", "NGC2207 relativistic", _compute_NGC2207_RelativisticTerm),
    "NGC2207_NeutrinoTerm": PhysicsTerm("NGC2207_NeutrinoTerm", "NGC2207 neutrino", _compute_NGC2207_NeutrinoTerm),
    "PerseusCluster_DPM_resonanceTerm": PhysicsTerm("PerseusCluster_DPM_resonanceTerm", "Perseus Abell 426 DPM resonance", _compute_PerseusCluster_DPM_resonanceTerm),
    "PerseusCluster_Q_waveTerm": PhysicsTerm("PerseusCluster_Q_waveTerm", "Perseus quantum wave", _compute_PerseusCluster_Q_waveTerm),
    "PerseusCluster_Ub1Term": PhysicsTerm("PerseusCluster_Ub1Term", "Perseus U_b1", _compute_PerseusCluster_Ub1Term),
    "PerseusCluster_UiTerm": PhysicsTerm("PerseusCluster_UiTerm", "Perseus inertia", _compute_PerseusCluster_UiTerm),
    "PerseusCluster_X2Term": PhysicsTerm("PerseusCluster_X2Term", "Perseus x₂", _compute_PerseusCluster_X2Term),
    "PerseusCluster_ActivationTerm": PhysicsTerm("PerseusCluster_ActivationTerm", "Perseus activation", _compute_PerseusCluster_ActivationTerm),
    "PerseusCluster_DirectedEnergyTerm": PhysicsTerm("PerseusCluster_DirectedEnergyTerm", "Perseus directed energy", _compute_PerseusCluster_DirectedEnergyTerm),
    "PerseusCluster_NeutronTerm": PhysicsTerm("PerseusCluster_NeutronTerm", "Perseus neutron", _compute_PerseusCluster_NeutronTerm),
    "PerseusCluster_RelativisticTerm": PhysicsTerm("PerseusCluster_RelativisticTerm", "Perseus relativistic", _compute_PerseusCluster_RelativisticTerm),
    "PerseusCluster_NeutrinoTerm": PhysicsTerm("PerseusCluster_NeutrinoTerm", "Perseus neutrino", _compute_PerseusCluster_NeutrinoTerm),
    "OrionM42_DPM_resonanceTerm": PhysicsTerm("OrionM42_DPM_resonanceTerm", "Orion M42 DPM resonance", _compute_OrionM42_DPM_resonanceTerm),
    "OrionM42_Q_waveTerm": PhysicsTerm("OrionM42_Q_waveTerm", "Orion quantum wave", _compute_OrionM42_Q_waveTerm),
    "OrionM42_Ub1Term": PhysicsTerm("OrionM42_Ub1Term", "Orion U_b1", _compute_OrionM42_Ub1Term),
    "OrionM42_UiTerm": PhysicsTerm("OrionM42_UiTerm", "Orion inertia", _compute_OrionM42_UiTerm),
    "OrionM42_X2Term": PhysicsTerm("OrionM42_X2Term", "Orion x₂", _compute_OrionM42_X2Term),
    "OrionM42_ActivationTerm": PhysicsTerm("OrionM42_ActivationTerm", "Orion activation", _compute_OrionM42_ActivationTerm),
    "OrionM42_DirectedEnergyTerm": PhysicsTerm("OrionM42_DirectedEnergyTerm", "Orion directed energy", _compute_OrionM42_DirectedEnergyTerm),
    "OrionM42_NeutronTerm": PhysicsTerm("OrionM42_NeutronTerm", "Orion neutron", _compute_OrionM42_NeutronTerm),
    "OrionM42_RelativisticTerm": PhysicsTerm("OrionM42_RelativisticTerm", "Orion relativistic", _compute_OrionM42_RelativisticTerm),
    "OrionM42_NeutrinoTerm": PhysicsTerm("OrionM42_NeutrinoTerm", "Orion neutrino", _compute_OrionM42_NeutrinoTerm),
    "LagoonM8_DPM_resonanceTerm": PhysicsTerm("LagoonM8_DPM_resonanceTerm", "Lagoon M8 HII DPM resonance", _compute_LagoonM8_DPM_resonanceTerm),
    "LagoonM8_Q_waveTerm": PhysicsTerm("LagoonM8_Q_waveTerm", "Lagoon quantum wave", _compute_LagoonM8_Q_waveTerm),
    "LagoonM8_Ub1Term": PhysicsTerm("LagoonM8_Ub1Term", "Lagoon U_b1", _compute_LagoonM8_Ub1Term),
    "LagoonM8_UiTerm": PhysicsTerm("LagoonM8_UiTerm", "Lagoon inertia", _compute_LagoonM8_UiTerm),
    "LagoonM8_X2Term": PhysicsTerm("LagoonM8_X2Term", "Lagoon x₂", _compute_LagoonM8_X2Term),
    "LagoonM8_ActivationTerm": PhysicsTerm("LagoonM8_ActivationTerm", "Lagoon activation", _compute_LagoonM8_ActivationTerm),
    "LagoonM8_DirectedEnergyTerm": PhysicsTerm("LagoonM8_DirectedEnergyTerm", "Lagoon directed energy", _compute_LagoonM8_DirectedEnergyTerm),
    "LagoonM8_NeutronTerm": PhysicsTerm("LagoonM8_NeutronTerm", "Lagoon neutron", _compute_LagoonM8_NeutronTerm),
    "LagoonM8_RelativisticTerm": PhysicsTerm("LagoonM8_RelativisticTerm", "Lagoon relativistic", _compute_LagoonM8_RelativisticTerm),
    "LagoonM8_NeutrinoTerm": PhysicsTerm("LagoonM8_NeutrinoTerm", "Lagoon neutrino", _compute_LagoonM8_NeutrinoTerm),
    "ESO137_G_timeTerm": PhysicsTerm("ESO137_G_timeTerm", "ESO137 time-dependent gravity g(r,t)", _compute_ESO137_G_timeTerm),
    "NGC1365A_G_timeTerm": PhysicsTerm("NGC1365A_G_timeTerm", "NGC1365 g(r,t) with AGN variability", _compute_NGC1365A_G_timeTerm),
    "NGC5728_G_timeTerm": PhysicsTerm("NGC5728_G_timeTerm", "NGC5728 g(r,t)", _compute_NGC5728_G_timeTerm),
    "Vela_G_timeTerm": PhysicsTerm("Vela_G_timeTerm", "Vela Pulsar g(t) at 11.2 Hz", _compute_Vela_G_timeTerm),
    "M82_G_timeTerm": PhysicsTerm("M82_G_timeTerm", "M82 Cigar Galaxy g(r,t)", _compute_M82_G_timeTerm),
    "Carina_G_timeTerm": PhysicsTerm("Carina_G_timeTerm", "Carina Nebula g(r,t)", _compute_Carina_G_timeTerm),
    "TadpoleGalaxy_G_timeTerm": PhysicsTerm("TadpoleGalaxy_G_timeTerm", "Tadpole Galaxy g(r,t) with tidal tail", _compute_TadpoleGalaxy_G_timeTerm),
    "MiceGalaxies_G_timeTerm": PhysicsTerm("MiceGalaxies_G_timeTerm", "Mice Galaxies g(r,t) interaction", _compute_MiceGalaxies_G_timeTerm),
    "NGC2264_G_timeTerm": PhysicsTerm("NGC2264_G_timeTerm", "NGC2264 Christmas Tree g(r,t)", _compute_NGC2264_G_timeTerm),
    "M42_G_timeTerm": PhysicsTerm("M42_G_timeTerm", "Orion M42 g(r,t)", _compute_M42_G_timeTerm),
    "CrabNebula_G_timeTerm": PhysicsTerm("CrabNebula_G_timeTerm", "Crab Nebula g(t) 30 Hz pulsar", _compute_CrabNebula_G_timeTerm),
    "BlackWidowPulsar_G_timeTerm": PhysicsTerm("BlackWidowPulsar_G_timeTerm", "Black Widow Pulsar g(t) 622 Hz", _compute_BlackWidowPulsar_G_timeTerm),
    "Redback_G_timeTerm": PhysicsTerm("Redback_G_timeTerm", "Redback Pulsar g(t) 317 Hz", _compute_Redback_G_timeTerm),
    "Fermi_G_timeTerm": PhysicsTerm("Fermi_G_timeTerm", "Fermi Bubbles g(r,t)", _compute_Fermi_G_timeTerm),
    "Holmberg15A_G_timeTerm": PhysicsTerm("Holmberg15A_G_timeTerm", "Holmberg 15A supermassive BH g(r,t)", _compute_Holmberg15A_G_timeTerm),
    "NineteenSystems_G_timeTerm": PhysicsTerm("NineteenSystems_G_timeTerm", "19-system master g(r,t) 26D polynomial", _compute_NineteenSystems_G_timeTerm),
    "WolframHypergraph_G_timeTerm": PhysicsTerm("WolframHypergraph_G_timeTerm", "Wolfram hypergraph g(t) with PI infinity decoder", _compute_WolframHypergraph_G_timeTerm),
    "NGC253_G_timeTerm": PhysicsTerm("NGC253_G_timeTerm", "NGC253 Sculptor Galaxy g(r,t)", _compute_NGC253_G_timeTerm),
    "M87Jet_G_timeTerm": PhysicsTerm("M87Jet_G_timeTerm", "M87 jet g(r,t) with precession", _compute_M87Jet_G_timeTerm),
    "Centaurus_G_timeTerm": PhysicsTerm("Centaurus_G_timeTerm", "Centaurus A g(r,t)", _compute_Centaurus_G_timeTerm),
    "Antlia_G_timeTerm": PhysicsTerm("Antlia_G_timeTerm", "Antlia Dwarf g(r,t)", _compute_Antlia_G_timeTerm),
    "Fornax_G_timeTerm": PhysicsTerm("Fornax_G_timeTerm", "Fornax Cluster g(r,t)", _compute_Fornax_G_timeTerm),
    "Sculptor_G_timeTerm": PhysicsTerm("Sculptor_G_timeTerm", "Sculptor Dwarf g(r,t)", _compute_Sculptor_G_timeTerm),
    "LeoI_G_timeTerm": PhysicsTerm("LeoI_G_timeTerm", "Leo I Dwarf g(r,t)", _compute_LeoI_G_timeTerm),
    "LeoII_G_timeTerm": PhysicsTerm("LeoII_G_timeTerm", "Leo II g(r,t)", _compute_LeoII_G_timeTerm),
    "UrsaMinor_G_timeTerm": PhysicsTerm("UrsaMinor_G_timeTerm", "Ursa Minor g(r,t)", _compute_UrsaMinor_G_timeTerm),
    "Draco_G_timeTerm": PhysicsTerm("Draco_G_timeTerm", "Draco Dwarf g(r,t)", _compute_Draco_G_timeTerm),
    "Sextans_G_timeTerm": PhysicsTerm("Sextans_G_timeTerm", "Sextans Dwarf g(r,t)", _compute_Sextans_G_timeTerm),
    "Carina2_G_timeTerm": PhysicsTerm("Carina2_G_timeTerm", "Carina II g(r,t)", _compute_Carina2_G_timeTerm),
    "Reticulum_G_timeTerm": PhysicsTerm("Reticulum_G_timeTerm", "Reticulum II g(r,t)", _compute_Reticulum_G_timeTerm),
    "Tucana_G_timeTerm": PhysicsTerm("Tucana_G_timeTerm", "Tucana II g(r,t)", _compute_Tucana_G_timeTerm),
    "Horologium_G_timeTerm": PhysicsTerm("Horologium_G_timeTerm", "Horologium I g(r,t)", _compute_Horologium_G_timeTerm),
    "Grus_G_timeTerm": PhysicsTerm("Grus_G_timeTerm", "Grus I g(r,t)", _compute_Grus_G_timeTerm),
    "Phoenix_G_timeTerm": PhysicsTerm("Phoenix_G_timeTerm", "Phoenix Dwarf g(r,t)", _compute_Phoenix_G_timeTerm),
    "Eridanus_G_timeTerm": PhysicsTerm("Eridanus_G_timeTerm", "Eridanus II g(r,t)", _compute_Eridanus_G_timeTerm),
    "Aquarius_G_timeTerm": PhysicsTerm("Aquarius_G_timeTerm", "Aquarius II g(r,t)", _compute_Aquarius_G_timeTerm),
    "Pisces_G_timeTerm": PhysicsTerm("Pisces_G_timeTerm", "Pisces II g(r,t)", _compute_Pisces_G_timeTerm),
    "Pegasus_G_timeTerm": PhysicsTerm("Pegasus_G_timeTerm", "Pegasus III g(r,t)", _compute_Pegasus_G_timeTerm),
    "Andromeda_G_timeTerm": PhysicsTerm("Andromeda_G_timeTerm", "Andromeda M31 g(r,t)", _compute_Andromeda_G_timeTerm),
    "Triangulum_G_timeTerm": PhysicsTerm("Triangulum_G_timeTerm", "Triangulum M33 g(r,t)", _compute_Triangulum_G_timeTerm),
    "LMC_G_timeTerm": PhysicsTerm("LMC_G_timeTerm", "Large Magellanic Cloud g(r,t)", _compute_LMC_G_timeTerm),
    "J1610_G_timeTerm": PhysicsTerm("J1610_G_timeTerm", "J1610 quasar g(r,t) z=3.122", _compute_J1610_G_timeTerm),
    "JupiterAurorae_G_timeTerm": PhysicsTerm("JupiterAurorae_G_timeTerm", "Jupiter aurorae g(r,t) Io plasma", _compute_JupiterAurorae_G_timeTerm),
    "LagoonM8HII_G_timeTerm": PhysicsTerm("LagoonM8HII_G_timeTerm", "Lagoon M8 HII region g(r,t)", _compute_LagoonM8HII_G_timeTerm),
    "M87JetEHT_G_timeTerm": PhysicsTerm("M87JetEHT_G_timeTerm", "M87 EHT jet g(r,t) v~0.5c", _compute_M87JetEHT_G_timeTerm),
    "NGC1365Seyfert_G_timeTerm": PhysicsTerm("NGC1365Seyfert_G_timeTerm", "NGC1365 Seyfert g(r,t)", _compute_NGC1365Seyfert_G_timeTerm),
    "NGC2207IC2163_G_timeTerm": PhysicsTerm("NGC2207IC2163_G_timeTerm", "NGC2207/IC2163 collision g(r,t)", _compute_NGC2207IC2163_G_timeTerm),
    "RAquarii_G_timeTerm": PhysicsTerm("RAquarii_G_timeTerm", "R Aquarii symbiotic g(r,t) 44yr period", _compute_RAquarii_G_timeTerm),
    "SgrAStarGC_G_timeTerm": PhysicsTerm("SgrAStarGC_G_timeTerm", "Sgr A* Galactic Center g(r,t) flares", _compute_SgrAStarGC_G_timeTerm),
    "SPTCLJ2215_G_timeTerm": PhysicsTerm("SPTCLJ2215_G_timeTerm", "SPT-CL J2215 cluster g(r,t) z=0.7", _compute_SPTCLJ2215_G_timeTerm),
    "StephanQuintet_G_timeTerm": PhysicsTerm("StephanQuintet_G_timeTerm", "Stephan's Quintet g(r,t) JWST", _compute_StephanQuintet_G_timeTerm),
    "VelaPulsarSNR_G_timeTerm": PhysicsTerm("VelaPulsarSNR_G_timeTerm", "Vela SNR g(t) 89ms pulsar", _compute_VelaPulsarSNR_G_timeTerm),
    "NGC4993GW170817_G_timeTerm": PhysicsTerm("NGC4993GW170817_G_timeTerm", "NGC4993 GW170817 kilonova g(r,t) NS merger", _compute_NGC4993GW170817_G_timeTerm),
    "NGC6240DualAGN_G_timeTerm": PhysicsTerm("NGC6240DualAGN_G_timeTerm", "NGC6240 dual AGN g(r,t)", _compute_NGC6240DualAGN_G_timeTerm),
    "OmegaCentauri_G_timeTerm": PhysicsTerm("OmegaCentauri_G_timeTerm", "Omega Centauri globular g(r,t)", _compute_OmegaCentauri_G_timeTerm),
    "OrionM42Trapezium_G_timeTerm": PhysicsTerm("OrionM42Trapezium_G_timeTerm", "Orion Trapezium g(r,t)", _compute_OrionM42Trapezium_G_timeTerm),
    "PerseusAbell426_G_timeTerm": PhysicsTerm("PerseusAbell426_G_timeTerm", "Perseus Abell 426 cluster g(r,t)", _compute_PerseusAbell426_G_timeTerm),
    "PolarRingNGC4650A_G_timeTerm": PhysicsTerm("PolarRingNGC4650A_G_timeTerm", "Polar Ring NGC4650A g(r,t)", _compute_PolarRingNGC4650A_G_timeTerm),
    "QuasarJ0313_G_timeTerm": PhysicsTerm("QuasarJ0313_G_timeTerm", "Quasar J0313 z=7.64 earliest g(r,t)", _compute_QuasarJ0313_G_timeTerm),
    "RXJ1713TeVSNR_G_timeTerm": PhysicsTerm("RXJ1713TeVSNR_G_timeTerm", "RX J1713 TeV SNR g(r,t)", _compute_RXJ1713TeVSNR_G_timeTerm),
    "SMC_G_timeTerm": PhysicsTerm("SMC_G_timeTerm", "Small Magellanic Cloud g(r,t)", _compute_SMC_G_timeTerm),
    "BuoyancyAstro5_G_timeTerm": PhysicsTerm("BuoyancyAstro5_G_timeTerm", "5-system buoyancy g(r,t)", _compute_BuoyancyAstro5_G_timeTerm),
    "BuoyancyCNB_G_timeTerm": PhysicsTerm("BuoyancyCNB_G_timeTerm", "Centaurus A CNB g(r,t)", _compute_BuoyancyCNB_G_timeTerm),
    "AstroSystems163_G_timeTerm": PhysicsTerm("AstroSystems163_G_timeTerm", "NGC 685/3507/3511 + TDE g(r,t)", _compute_AstroSystems163_G_timeTerm),
    "NebulaTriadic_G_timeTerm": PhysicsTerm("NebulaTriadic_G_timeTerm", "Triadic nebula g(r,t) ionization", _compute_NebulaTriadic_G_timeTerm),
    "BuoyancyModule_G_timeTerm": PhysicsTerm("BuoyancyModule_G_timeTerm", "M74/Eagle/M84 buoyancy g(r,t)", _compute_BuoyancyModule_G_timeTerm),
    "UQFF8Astro_G_timeTerm": PhysicsTerm("UQFF8Astro_G_timeTerm", "8-system 26-state g(r,t)", _compute_UQFF8Astro_G_timeTerm),
    "UQFFCore_G_timeTerm": PhysicsTerm("UQFFCore_G_timeTerm", "M82/IC418/Canis core g(r,t)", _compute_UQFFCore_G_timeTerm),
    "Buoyancy168_G_timeTerm": PhysicsTerm("Buoyancy168_G_timeTerm", "SN1006/EtaCar/Chandra g(r,t)", _compute_Buoyancy168_G_timeTerm),
    "CassiniBuoyancy_G_timeTerm": PhysicsTerm("CassiniBuoyancy_G_timeTerm", "Saturn Cassini g(r,t) 29.5yr orbit", _compute_CassiniBuoyancy_G_timeTerm),
    "MultiAstro_G_timeTerm": PhysicsTerm("MultiAstro_G_timeTerm", "11-system NGC/Cassini/ESO g(r,t)", _compute_MultiAstro_G_timeTerm),
    "EightAstro_G_timeTerm": PhysicsTerm("EightAstro_G_timeTerm", "AFGL5180/NGC346/LMC g(r,t)", _compute_EightAstro_G_timeTerm),
    "NineteenAstro26D_G_timeTerm": PhysicsTerm("NineteenAstro26D_G_timeTerm", "19-system 26D master g(r,t)", _compute_NineteenAstro26D_G_timeTerm),
    "WolframFieldUnity_G_timeTerm": PhysicsTerm("WolframFieldUnity_G_timeTerm", "Wolfram hypergraph g(t) 312-digit PI infinity decoder", _compute_WolframFieldUnity_G_timeTerm),
    "ESO137_HeavisideFactor": PhysicsTerm("ESO137_HeavisideFactor", "ESO137 Heaviside threshold amplification factor 1+10^13*f", _compute_ESO137_HeavisideFactor),
    "ESO137_UmBase": PhysicsTerm("ESO137_UmBase", "ESO137 U_m magnetic energy base (no Heaviside/Quasi)", _compute_ESO137_UmBase),
    "ESO137_UmContribution": PhysicsTerm("ESO137_UmContribution", "ESO137 full U_m with Heaviside & Quasi amplification", _compute_ESO137_UmContribution),
    "NGC1365A_H_SCm": PhysicsTerm("NGC1365A_H_SCm", "NGC1365A heliosphere thickness factor H_SCm", _compute_NGC1365A_H_SCm),
    "NGC1365A_U_g2": PhysicsTerm("NGC1365A_U_g2", "NGC1365A U_g2 with heliosphere thickness H_SCm", _compute_NGC1365A_U_g2),
    "NGC1365A_SwirlFactor": PhysicsTerm("NGC1365A_SwirlFactor", "NGC1365A solar wind swirl factor (1 + delta*v)", _compute_NGC1365A_SwirlFactor),
    "NGC5728_U_gi": PhysicsTerm("NGC5728_U_gi", "NGC5728 indexed gravitational field U_gi", _compute_NGC5728_U_gi),
    "NGC5728_K_i": PhysicsTerm("NGC5728_K_i", "NGC5728 coupling constant k_i for U_gi summation", _compute_NGC5728_K_i),
    "NGC5728_SumKUgi": PhysicsTerm("NGC5728_SumKUgi", "NGC5728 total Σk_i·U_gi gravity sum (i=1 to 4)", _compute_NGC5728_SumKUgi),
    "MagneticMoment_Mu_j": PhysicsTerm("MagneticMoment_Mu_j", "Time-dependent magnetic moment μ_j(t) with solar cycle", _compute_MagneticMoment_Mu_j),
    "MagneticMoment_B_j": PhysicsTerm("MagneticMoment_B_j", "Magnetic field strength B_j(t) = μ_j/r³", _compute_MagneticMoment_B_j),
    "MagneticMoment_UmContrib": PhysicsTerm("MagneticMoment_UmContrib", "U_m magnetic energy with time-varying μ_j(t)", _compute_MagneticMoment_UmContrib),
    "BlackHole_M_bh": PhysicsTerm("BlackHole_M_bh", "Galactic black hole mass M_bh", _compute_BlackHole_M_bh),
    "BlackHole_M_bhInMsun": PhysicsTerm("BlackHole_M_bhInMsun", "Black hole mass in solar masses M_bh/M_sun", _compute_BlackHole_M_bhInMsun),
    "BlackHole_U_b1": PhysicsTerm("BlackHole_U_b1", "U_b1 buoyancy term with galactic SMBH", _compute_BlackHole_U_b1),
    "NegativeTime_T_n": PhysicsTerm("NegativeTime_T_n", "Reciprocation time t_n = -t for time-reversal symmetry", _compute_NegativeTime_T_n),
    "NegativeTime_CosPiTn": PhysicsTerm("NegativeTime_CosPiTn", "Cosine of reciprocation phase cos(π·t_n)", _compute_NegativeTime_CosPiTn),
    "NegativeTime_OneMinusExp": PhysicsTerm("NegativeTime_OneMinusExp", "Time buildup factor (1 - exp) with t_n modulation", _compute_NegativeTime_OneMinusExp),
    "PiConstant_Pi": PhysicsTerm("PiConstant_Pi", "Pi constant π (3.14159..., Wolfram 312-digit precision)", _compute_PiConstant_Pi),
    "PiConstant_CosPiTn": PhysicsTerm("PiConstant_CosPiTn", "Pi-based cosine phase cos(π·t_n)", _compute_PiConstant_CosPiTn),
    "PiConstant_SinOmegaCT": PhysicsTerm("PiConstant_SinOmegaCT", "Solar cycle sine modulation sin(ω_c·t)", _compute_PiConstant_SinOmegaCT),
    "Decay_Gamma_s": PhysicsTerm("Decay_Gamma_s", "Reciprocation decay rate γ in s^-1", _compute_Decay_Gamma_s),
    "Decay_ExpTerm": PhysicsTerm("Decay_ExpTerm", "Exponential decay term exp(-γt·cos(πt_n))", _compute_Decay_ExpTerm),
    "Decay_OneMinusExp": PhysicsTerm("Decay_OneMinusExp", "Time buildup (1 - exp(-γt·cos(πt_n)))", _compute_Decay_OneMinusExp),
    "ScmPenetration_P_SCm": PhysicsTerm("ScmPenetration_P_SCm", "SCm penetration factor P_SCm", _compute_ScmPenetration_P_SCm),
    "ScmPenetration_UmPlanet": PhysicsTerm("ScmPenetration_UmPlanet", "Planetary U_m with reduced P_SCm", _compute_ScmPenetration_UmPlanet),
    "ScmReactivity_Kappa_day": PhysicsTerm("ScmReactivity_Kappa_day", "Reactivity decay rate (day^-1)", _compute_ScmReactivity_Kappa_day),
    "ScmReactivity_E_reactDecay": PhysicsTerm("ScmReactivity_E_reactDecay", "E_react exponential decay", _compute_ScmReactivity_E_reactDecay),
    "SolarCycle_Omega_c": PhysicsTerm("SolarCycle_Omega_c", "Solar cycle angular frequency ω_c", _compute_SolarCycle_Omega_c),
    "SolarCycle_PeriodYears": PhysicsTerm("SolarCycle_PeriodYears", "Solar cycle period (years)", _compute_SolarCycle_PeriodYears),
    "SolarCycle_MuJ_Modulated": PhysicsTerm("SolarCycle_MuJ_Modulated", "Magnetic moment with 11-year modulation", _compute_SolarCycle_MuJ_Modulated),
    "SolarCycle_B_j_Modulated": PhysicsTerm("SolarCycle_B_j_Modulated", "Surface magnetic field with solar cycle", _compute_SolarCycle_B_j_Modulated),
    "SolarWind_Delta_sw": PhysicsTerm("SolarWind_Delta_sw", "Solar wind modulation δ_sw", _compute_SolarWind_Delta_sw),
    "SolarWind_ModulationFactor": PhysicsTerm("SolarWind_ModulationFactor", "Modulation factor (1 + δ_sw)", _compute_SolarWind_ModulationFactor),
    "SolarWind_U_g2_NoMod": PhysicsTerm("SolarWind_U_g2_NoMod", "U_g2 without solar wind modulation", _compute_SolarWind_U_g2_NoMod),
    "SolarWindVelocity_V_sw": PhysicsTerm("SolarWindVelocity_V_sw", "Solar wind velocity v_sw", _compute_SolarWindVelocity_V_sw),
    "SolarWindVelocity_V_swKmS": PhysicsTerm("SolarWindVelocity_V_swKmS", "Solar wind velocity (km/s)", _compute_SolarWindVelocity_V_swKmS),
    "SolarWindVelocity_U_g2_NoSW": PhysicsTerm("SolarWindVelocity_U_g2_NoSW", "U_g2 with v_sw=0", _compute_SolarWindVelocity_U_g2_NoSW),
    "StellarMass_M_s": PhysicsTerm("StellarMass_M_s", "Stellar mass M_s", _compute_StellarMass_M_s),
    "StellarMass_M_sInMsun": PhysicsTerm("StellarMass_M_sInMsun", "Stellar mass in solar masses", _compute_StellarMass_M_sInMsun),
    "ReactorEfficiencyTerm": PhysicsTerm("ReactorEfficiencyTerm", "Ereact: Core reactivity efficiency = (rho_SCm * v_SCm^2 / rho_A) * exp(-kappa*t)", _compute_ReactorEfficiencyTerm),
    "MuSTerm": PhysicsTerm("MuSTerm", "mu_s: Magnetic dipole moment = Bs(t) * Rs^3", _compute_MuSTerm),
    "GradMsRTerm": PhysicsTerm("GradMsRTerm", "grad_Ms_r: Surface gravity gradient = G*Ms/Rs^2", _compute_GradMsRTerm),
    "BjTerm": PhysicsTerm("BjTerm", "Bj: Magnetic string field = 1e-3 + 0.4*sin(omega_c*t) + rho_SCm*1e-18", _compute_BjTerm),
    "OmegaSTTerm": PhysicsTerm("OmegaSTTerm", "omega_s_t: String rotation frequency = omega_s - 0.4e-6*sin(omega_c*t)", _compute_OmegaSTTerm),
    "MuJTerm": PhysicsTerm("MuJTerm", "mu_j: String magnetic dipole moment = Bj(t) * Rs^3", _compute_MuJTerm),
    "UniversalGravity1Term": PhysicsTerm("UniversalGravity1Term", "Ug1: Magnetic dipole-gradient gravity with defect modulation", _compute_UniversalGravity1Term),
    "UniversalGravity2Term": PhysicsTerm("UniversalGravity2Term", "Ug2: Charge-reactivity gravity with solar wind modulation", _compute_UniversalGravity2Term),
    "UniversalGravity3Term": PhysicsTerm("UniversalGravity3Term", "Ug3: Magnetic string rotation gravity", _compute_UniversalGravity3Term),
    "UniversalGravity4Term": PhysicsTerm("UniversalGravity4Term", "Ug4: Vacuum energy concentration gravity", _compute_UniversalGravity4Term),
    "UniversalBuoyancyTerm": PhysicsTerm("UniversalBuoyancyTerm", "Ubi: Universal buoyancy from galactic rotation", _compute_UniversalBuoyancyTerm),
    "UniversalMagnetismTerm": PhysicsTerm("UniversalMagnetismTerm", "Um: Billion magnetic strings", _compute_UniversalMagnetismTerm),
    "UniversalAetherTerm": PhysicsTerm("UniversalAetherTerm", "A_mu_nu: Cosmic aether metric tensor trace", _compute_UniversalAetherTerm),
    "UnifiedFieldTerm": PhysicsTerm("UnifiedFieldTerm", "FU: Complete unified field (sum_Ugi + sum_Ubi + Um + A_mu_nu_trace)", _compute_UnifiedFieldTerm),
    "MUGECompressedBaseTerm": PhysicsTerm("MUGECompressedBaseTerm", "Compressed MUGE base term: G*M/r^2 (Newtonian gravitational acceleration)", _compute_MUGECompressedBaseTerm),
    "MUGEExpansionTerm": PhysicsTerm("MUGEExpansionTerm", "Hubble expansion factor: 1 + H0*t where H0 = 2.269e-18 s^-1", _compute_MUGEExpansionTerm),
    "MUGESuperAdjustmentTerm": PhysicsTerm("MUGESuperAdjustmentTerm", "Superconductive magnetic adjustment: 1 - B/Bcrit", _compute_MUGESuperAdjustmentTerm),
    "MUGEEnvelopeTerm": PhysicsTerm("MUGEEnvelopeTerm", "Envelope modulation factor (currently neutral = 1.0)", _compute_MUGEEnvelopeTerm),
    "MUGEUgSumTerm": PhysicsTerm("MUGEUgSumTerm", "Sum of Ug1-4 components (extensible)", _compute_MUGEUgSumTerm),
    "MUGECosmologicalTerm": PhysicsTerm("MUGECosmologicalTerm", "Cosmological constant term: Lambda*c^2/3 (dark energy acceleration)", _compute_MUGECosmologicalTerm),
    "MUGEQuantumTerm": PhysicsTerm("MUGEQuantumTerm", "Quantum uncertainty term: (hbar/Delta_xp)*integral_psi*(2*PI/tHubble)", _compute_MUGEQuantumTerm),
    "MUGEFluidTerm": PhysicsTerm("MUGEFluidTerm", "Fluid dynamics term: rho_fluid * Vsys * g_local (Navier-Stokes coupling)", _compute_MUGEFluidTerm),
    "MUGEPerturbationTerm": PhysicsTerm("MUGEPerturbationTerm", "Dark matter perturbation: (M+M_DM)*(delta_rho/rho + 3*G*M/r^3)", _compute_MUGEPerturbationTerm),
    "MUGEResonanceADPMTerm": PhysicsTerm("MUGEResonanceADPMTerm", "Base DPM acceleration: aDPM = FDPM * fDPM * Evac_neb * c_res * Vsys", _compute_MUGEResonanceADPMTerm),
    "MUGEResonanceATHzTerm": PhysicsTerm("MUGEResonanceATHzTerm", "THz frequency contribution: aTHz = fTHz * Evac_neb * vexp * aDPM / (Evac_ISM * c", _compute_MUGEResonanceATHzTerm),
    "MUGEResonanceAvacDiffTerm": PhysicsTerm("MUGEResonanceAvacDiffTerm", "Vacuum energy differential: avac_diff = Delta_Evac * vexp^2 * aDPM / (Evac_neb *", _compute_MUGEResonanceAvacDiffTerm),
    "MUGEResonanceASuperFreqTerm": PhysicsTerm("MUGEResonanceASuperFreqTerm", "Superconductive frequency resonance: asuper_freq = Fsuper * fTHz * aDPM / (Evac_", _compute_MUGEResonanceASuperFreqTerm),
    "MUGEResonanceAAetherResTerm": PhysicsTerm("MUGEResonanceAAetherResTerm", "Aether resonance coupling: aaether_res = UA_SCM * omega_i * fTHz * aDPM * (1 + f", _compute_MUGEResonanceAAetherResTerm),
    "MUGEResonanceUg4iTerm": PhysicsTerm("MUGEResonanceUg4iTerm", "Reactor gravity component: Ug4i = k4_res * Ereact * freact * aDPM / (Evac_neb * ", _compute_MUGEResonanceUg4iTerm),
    "MUGEResonanceAQuantumFreqTerm": PhysicsTerm("MUGEResonanceAQuantumFreqTerm", "Quantum frequency contribution: aquantum_freq = fquantum * Evac_neb * aDPM / (Ev", _compute_MUGEResonanceAQuantumFreqTerm),
    "MUGEResonanceAAetherFreqTerm": PhysicsTerm("MUGEResonanceAAetherFreqTerm", "Aether frequency component: aAether_freq = fAether * Evac_neb * aDPM / (Evac_ISM", _compute_MUGEResonanceAAetherFreqTerm),
    "MUGEResonanceAFluidFreqTerm": PhysicsTerm("MUGEResonanceAFluidFreqTerm", "Fluid dynamics frequency: afluid_freq = ffluid * Evac_neb * Vsys / (Evac_ISM * c", _compute_MUGEResonanceAFluidFreqTerm),
    "MUGEResonanceOscTerm": PhysicsTerm("MUGEResonanceOscTerm", "Oscillatory term (simplified to zero in current implementation)", _compute_MUGEResonanceOscTerm),
    "MUGEResonanceAExpFreqTerm": PhysicsTerm("MUGEResonanceAExpFreqTerm", "Expansion frequency (Hubble): aexp_freq = fexp * Evac_neb * aDPM / (Evac_ISM * c", _compute_MUGEResonanceAExpFreqTerm),
    "MUGEResonanceFTRZTerm": PhysicsTerm("MUGEResonanceFTRZTerm", "TRZ factor component (pass-through): returns fTRZ parameter directly", _compute_MUGEResonanceFTRZTerm),
    "MUGEResonanceWormholeTerm": PhysicsTerm("MUGEResonanceWormholeTerm", "Wormhole metric contribution: a_wormhole = f_worm * Evac_neb / (b^2 + r^2)", _compute_MUGEResonanceWormholeTerm),
    "TauLeptonDipoleTerm": PhysicsTerm("TauLeptonDipoleTerm", "Tau anomalous magnetic moment: a_tau = a_tau_SM + a_tau_BSM (arXiv:2506.15245)", _compute_TauLeptonDipoleTerm),
    "CKMVcbTerm": PhysicsTerm("CKMVcbTerm", "CKM matrix element |V_cb| = (39.2+/-0.9)e-3 from Belle II B->D l nu (arXiv:2506.", _compute_CKMVcbTerm),
    "LFVBranchingTerm": PhysicsTerm("LFVBranchingTerm", "LFV branching ratio B0->K*0 tau+/-e-/+: BR < 5.9e-6 at 90% CL (arXiv:2506.15347)", _compute_LFVBranchingTerm),
    "VectorLikeQuarkTerm": PhysicsTerm("VectorLikeQuarkTerm", "VLQ coupling kappa(m): singlet T [0.22,0.52], triplet TBY [0.14,0.46] (arXiv:250", _compute_VectorLikeQuarkTerm),
    "UnifiedFieldTerm_SOURCE4": PhysicsTerm("UnifiedFieldTerm_SOURCE4", "FU: Complete unified field (sum_Ugi + sum_Ubi + Um + A_mu_nu_trace)", _compute_UnifiedFieldTerm_SOURCE4),
    "MUGESuperAdjTerm": PhysicsTerm("MUGESuperAdjTerm", "Superconductive magnetic adjustment: 1 - B/Bcrit", _compute_MUGESuperAdjTerm),
    "MUGECosmTerm": PhysicsTerm("MUGECosmTerm", "Cosmological constant term: Lambda*c^2/3", _compute_MUGECosmTerm),
    "SGR1745MagnetarTerm": PhysicsTerm("SGR1745MagnetarTerm", "SGR 1745-2900: Magnetar system (I=1e21, M=2.984e30 kg, B=1e10 T)", _compute_SGR1745MagnetarTerm),
    "SagittariusAStarTerm": PhysicsTerm("SagittariusAStarTerm", "Sagittarius A*: Supermassive black hole (M=8.155e36 kg, M_DM=1e37 kg)", _compute_SagittariusAStarTerm),
    "TapestryStarbirthTerm": PhysicsTerm("TapestryStarbirthTerm", "Tapestry of Blazing Starbirth: Nebula (M=1.989e35 kg, Vsys=1e53 m^3)", _compute_TapestryStarbirthTerm),
    "Westerlund2ClusterTerm": PhysicsTerm("Westerlund2ClusterTerm", "Westerlund 2: Stellar cluster (similar to Tapestry parameters)", _compute_Westerlund2ClusterTerm),
    "PillarsCreationTerm": PhysicsTerm("PillarsCreationTerm", "Pillars of Creation: Molecular cloud (M=1.989e32 kg, r=1 ly)", _compute_PillarsCreationTerm),
    "RingsRelativityTerm": PhysicsTerm("RingsRelativityTerm", "Rings of Relativity: Cosmological structure (M=1.989e36 kg, z=0.01)", _compute_RingsRelativityTerm),
    "StudentGuideUniverseTerm": PhysicsTerm("StudentGuideUniverseTerm", "Student's Guide to the Universe: Observable universe (M=1e53 kg, r=10 Gly)", _compute_StudentGuideUniverseTerm),
    "CompressedMUGETerm_SOURCE4": PhysicsTerm("CompressedMUGETerm_SOURCE4", "Compressed MUGE: 9-term gravity equation wrapper", _compute_CompressedMUGETerm_SOURCE4),
    "ResonanceMUGETerm_SOURCE4": PhysicsTerm("ResonanceMUGETerm_SOURCE4", "Resonance MUGE: 13-term + wormhole resonance equation wrapper", _compute_ResonanceMUGETerm_SOURCE4),
    "getDynamicParameter": PhysicsTerm("getDynamicParameter", "Function: getDynamicParameter", _compute_getDynamicParameter),
    "getLearningRate": PhysicsTerm("getLearningRate", "Function: getLearningRate", _compute_getLearningRate),
    "computeWithNested": PhysicsTerm("computeWithNested", "Function: computeWithNested", _compute_computeWithNested),
    "computeModule": PhysicsTerm("computeModule", "Function: computeModule", _compute_computeModule),
    "getGlobalParameter": PhysicsTerm("getGlobalParameter", "Function: getGlobalParameter", _compute_getGlobalParameter),
    "correlationCoefficient": PhysicsTerm("correlationCoefficient", "Function: correlationCoefficient", _compute_correlationCoefficient),
    "calculateDipMomentumEnergy": PhysicsTerm("calculateDipMomentumEnergy", "Function: calculateDipMomentumEnergy", _compute_calculateDipMomentumEnergy),
    "compute_E_cm": PhysicsTerm("compute_E_cm", "Function: compute_E_cm", _compute_compute_E_cm),
    "dpm_life_proportion": PhysicsTerm("dpm_life_proportion", "Function: dpm_life_proportion", _compute_dpm_life_proportion),
    "F_U_Bi_i": PhysicsTerm("F_U_Bi_i", "Function: F_U_Bi_i", _compute_F_U_Bi_i),
    "compressed_g": PhysicsTerm("compressed_g", "Function: compressed_g", _compute_compressed_g),
    "F_jet_rel": PhysicsTerm("F_jet_rel", "Function: F_jet_rel", _compute_F_jet_rel),
    "E_acc_rel": PhysicsTerm("E_acc_rel", "Function: E_acc_rel", _compute_E_acc_rel),
    "F_drag_rel": PhysicsTerm("F_drag_rel", "Function: F_drag_rel", _compute_F_drag_rel),
    "F_gw_rel": PhysicsTerm("F_gw_rel", "Function: F_gw_rel", _compute_F_gw_rel),
    "computeH_SCm": PhysicsTerm("computeH_SCm", "Function: computeH_SCm", _compute_computeH_SCm),
    "computeU_g2": PhysicsTerm("computeU_g2", "Function: computeU_g2", _compute_computeU_g2),
    "computeU_g2_no_H": PhysicsTerm("computeU_g2_no_H", "Function: computeU_g2_no_H", _compute_computeU_g2_no_H),
    "getH_SCm": PhysicsTerm("getH_SCm", "Function: getH_SCm", _compute_getH_SCm),
    "computeU_gi": PhysicsTerm("computeU_gi", "Function: computeU_gi", _compute_computeU_gi),
    "computeK_i": PhysicsTerm("computeK_i", "Function: computeK_i", _compute_computeK_i),
    "computeKUgi": PhysicsTerm("computeKUgi", "Function: computeKUgi", _compute_computeKUgi),
    "computeSumKUgi": PhysicsTerm("computeSumKUgi", "Function: computeSumKUgi", _compute_computeSumKUgi),
    "getComponentContribution": PhysicsTerm("getComponentContribution", "Function: getComponentContribution", _compute_getComponentContribution),
    "getTotalForce": PhysicsTerm("getTotalForce", "Function: getTotalForce", _compute_getTotalForce),
    "computeLambda_i": PhysicsTerm("computeLambda_i", "Function: computeLambda_i", _compute_computeLambda_i),
    "computeU_i": PhysicsTerm("computeU_i", "Function: computeU_i", _compute_computeU_i),
    "computeInertiaTerm": PhysicsTerm("computeInertiaTerm", "Function: computeInertiaTerm", _compute_computeInertiaTerm),
    "computeSumInertiaTerms": PhysicsTerm("computeSumInertiaTerms", "Function: computeSumInertiaTerms", _compute_computeSumInertiaTerms),
    "getInertiaResistance": PhysicsTerm("getInertiaResistance", "Function: getInertiaResistance", _compute_getInertiaResistance),
    "getTotalInertia": PhysicsTerm("getTotalInertia", "Function: getTotalInertia", _compute_getTotalInertia),
    "computeMu_j": PhysicsTerm("computeMu_j", "Function: computeMu_j", _compute_computeMu_j),
    "computeUmContrib": PhysicsTerm("computeUmContrib", "Function: computeUmContrib", _compute_computeUmContrib),
    "computeB_j": PhysicsTerm("computeB_j", "Function: computeB_j", _compute_computeB_j),
    "computeUg3Contrib": PhysicsTerm("computeUg3Contrib", "Function: computeUg3Contrib", _compute_computeUg3Contrib),
    "getMagneticMoment": PhysicsTerm("getMagneticMoment", "Function: getMagneticMoment", _compute_getMagneticMoment),
    "getFieldStrength": PhysicsTerm("getFieldStrength", "Function: getFieldStrength", _compute_getFieldStrength),
    "computeM_bhInMsun": PhysicsTerm("computeM_bhInMsun", "Function: computeM_bhInMsun", _compute_computeM_bhInMsun),
    "computeMbhOverDg": PhysicsTerm("computeMbhOverDg", "Function: computeMbhOverDg", _compute_computeMbhOverDg),
    "computeU_b1": PhysicsTerm("computeU_b1", "Function: computeU_b1", _compute_computeU_b1),
    "computeU_g4": PhysicsTerm("computeU_g4", "Function: computeU_g4", _compute_computeU_g4),
    "computeM_bh": PhysicsTerm("computeM_bh", "Function: computeM_bh", _compute_computeM_bh),
    "getBlackHoleMass": PhysicsTerm("getBlackHoleMass", "Function: getBlackHoleMass", _compute_getBlackHoleMass),
    "getBlackHoleMassSolarUnits": PhysicsTerm("getBlackHoleMassSolarUnits", "Function: getBlackHoleMassSolarUnits", _compute_getBlackHoleMassSolarUnits),
    "getUb1Contribution": PhysicsTerm("getUb1Contribution", "Function: getUb1Contribution", _compute_getUb1Contribution),
    "computeT_n": PhysicsTerm("computeT_n", "Function: computeT_n", _compute_computeT_n),
    "computeCosPiTn": PhysicsTerm("computeCosPiTn", "Function: computeCosPiTn", _compute_computeCosPiTn),
    "computeExpTerm": PhysicsTerm("computeExpTerm", "Function: computeExpTerm", _compute_computeExpTerm),
    "computeOneMinusExp": PhysicsTerm("computeOneMinusExp", "Function: computeOneMinusExp", _compute_computeOneMinusExp),
    "getTnValue": PhysicsTerm("getTnValue", "Function: getTnValue", _compute_getTnValue),
    "computePi": PhysicsTerm("computePi", "Function: computePi", _compute_computePi),
    "computeCosPiTn_1": PhysicsTerm("computeCosPiTn_1", "Function: computeCosPiTn", _compute_computeCosPiTn_1),
    "computeSinOmegaCT": PhysicsTerm("computeSinOmegaCT", "Function: computeSinOmegaCT", _compute_computeSinOmegaCT),
    "getPiValue": PhysicsTerm("getPiValue", "Function: getPiValue", _compute_getPiValue),
    "computeP_core": PhysicsTerm("computeP_core", "Function: computeP_core", _compute_computeP_core),
    "computeU_g3": PhysicsTerm("computeU_g3", "Function: computeU_g3", _compute_computeU_g3),
    "computeF_quasi": PhysicsTerm("computeF_quasi", "Function: computeF_quasi", _compute_computeF_quasi),
    "computeQuasiFactor": PhysicsTerm("computeQuasiFactor", "Function: computeQuasiFactor", _compute_computeQuasiFactor),
    "getQuasiWaveFactor": PhysicsTerm("getQuasiWaveFactor", "Function: getQuasiWaveFactor", _compute_getQuasiWaveFactor),
    "computeR_b": PhysicsTerm("computeR_b", "Function: computeR_b", _compute_computeR_b),
    "computeR_bInAU": PhysicsTerm("computeR_bInAU", "Function: computeR_bInAU", _compute_computeR_bInAU),
    "computeS_r_Rb": PhysicsTerm("computeS_r_Rb", "Function: computeS_r_Rb", _compute_computeS_r_Rb),
    "getBubbleRadius": PhysicsTerm("getBubbleRadius", "Function: getBubbleRadius", _compute_getBubbleRadius),
    "computeGamma_day": PhysicsTerm("computeGamma_day", "Function: computeGamma_day", _compute_computeGamma_day),
    "computeGamma_s": PhysicsTerm("computeGamma_s", "Function: computeGamma_s", _compute_computeGamma_s),
    "computeExpTerm_1": PhysicsTerm("computeExpTerm_1", "Function: computeExpTerm", _compute_computeExpTerm_1),
    "computeOneMinusExp_1": PhysicsTerm("computeOneMinusExp_1", "Function: computeOneMinusExp", _compute_computeOneMinusExp_1),
    "getDecayRate": PhysicsTerm("getDecayRate", "Function: getDecayRate", _compute_getDecayRate),
    "getTimescale": PhysicsTerm("getTimescale", "Function: getTimescale", _compute_getTimescale),
    "computeP_SCm": PhysicsTerm("computeP_SCm", "Function: computeP_SCm", _compute_computeP_SCm),
    "getSCmPenetration": PhysicsTerm("getSCmPenetration", "Function: getSCmPenetration", _compute_getSCmPenetration),
    "computeAlpha": PhysicsTerm("computeAlpha", "Function: computeAlpha", _compute_computeAlpha),
    "computeE_react": PhysicsTerm("computeE_react", "Function: computeE_react", _compute_computeE_react),
    "getDecayConstant": PhysicsTerm("getDecayConstant", "Function: getDecayConstant", _compute_getDecayConstant),
    "computeOmega_c": PhysicsTerm("computeOmega_c", "Function: computeOmega_c", _compute_computeOmega_c),
    "computeSinOmegaCT_1": PhysicsTerm("computeSinOmegaCT_1", "Function: computeSinOmegaCT", _compute_computeSinOmegaCT_1),
    "computeModulationFactor": PhysicsTerm("computeModulationFactor", "Function: computeModulationFactor", _compute_computeModulationFactor),
    "getGammaSw": PhysicsTerm("getGammaSw", "Function: getGammaSw", _compute_getGammaSw),
    "computeV_sw": PhysicsTerm("computeV_sw", "Function: computeV_sw", _compute_computeV_sw),
    "computeGammaSwVsw": PhysicsTerm("computeGammaSwVsw", "Function: computeGammaSwVsw", _compute_computeGammaSwVsw),
    "computeS_rRb": PhysicsTerm("computeS_rRb", "Function: computeS_rRb", _compute_computeS_rRb),
    "computeHeavisideFactor": PhysicsTerm("computeHeavisideFactor", "Function: computeHeavisideFactor", _compute_computeHeavisideFactor),
    "computeT_00": PhysicsTerm("computeT_00", "Function: computeT_00", _compute_computeT_00),
    "computeTrace": PhysicsTerm("computeTrace", "Function: computeTrace", _compute_computeTrace),
    "computeM": PhysicsTerm("computeM", "Function: computeM", _compute_computeM),
    "computeMInSolarMasses": PhysicsTerm("computeMInSolarMasses", "Function: computeMInSolarMasses", _compute_computeMInSolarMasses),
    "computeOmega_s": PhysicsTerm("computeOmega_s", "Function: computeOmega_s", _compute_computeOmega_s),
    "computeRotationPeriod": PhysicsTerm("computeRotationPeriod", "Function: computeRotationPeriod", _compute_computeRotationPeriod),
    "computeB_j_1": PhysicsTerm("computeB_j_1", "Function: computeB_j", _compute_computeB_j_1),
    "computeT_eff": PhysicsTerm("computeT_eff", "Function: computeT_eff", _compute_computeT_eff),
    "computeLuminosity": PhysicsTerm("computeLuminosity", "Function: computeLuminosity", _compute_computeLuminosity),
    "computeF_TRZ": PhysicsTerm("computeF_TRZ", "Function: computeF_TRZ", _compute_computeF_TRZ),
    "computeTRZFactor": PhysicsTerm("computeTRZFactor", "Function: computeTRZFactor", _compute_computeTRZFactor),
    "computeU_g1": PhysicsTerm("computeU_g1", "Function: computeU_g1", _compute_computeU_g1),
    "computeU_g3_1": PhysicsTerm("computeU_g3_1", "Function: computeU_g3", _compute_computeU_g3_1),
    "computeRho_vac_aether": PhysicsTerm("computeRho_vac_aether", "Function: computeRho_vac_aether", _compute_computeRho_vac_aether),
    "computeLambda_UI": PhysicsTerm("computeLambda_UI", "Function: computeLambda_UI", _compute_computeLambda_UI),
    "computeRho_vac_UI": PhysicsTerm("computeRho_vac_UI", "Function: computeRho_vac_UI", _compute_computeRho_vac_UI),
    "computeRho_vac_SCm": PhysicsTerm("computeRho_vac_SCm", "Function: computeRho_vac_SCm", _compute_computeRho_vac_SCm),
    "computeRho_vac_UA": PhysicsTerm("computeRho_vac_UA", "Function: computeRho_vac_UA", _compute_computeRho_vac_UA),
    "computeV_SCm": PhysicsTerm("computeV_SCm", "Function: computeV_SCm", _compute_computeV_SCm),
    "computeB_total": PhysicsTerm("computeB_total", "Function: computeB_total", _compute_computeB_total),
    "computeFlux": PhysicsTerm("computeFlux", "Function: computeFlux", _compute_computeFlux),
    "computeF_U": PhysicsTerm("computeF_U", "Function: computeF_U", _compute_computeF_U),
    "computeJetPower": PhysicsTerm("computeJetPower", "Function: computeJetPower", _compute_computeJetPower),
    "computeCollisionEnergy": PhysicsTerm("computeCollisionEnergy", "Function: computeCollisionEnergy", _compute_computeCollisionEnergy),
    "computeTidalRadius": PhysicsTerm("computeTidalRadius", "Function: computeTidalRadius", _compute_computeTidalRadius),
    "computeAccretionLuminosity": PhysicsTerm("computeAccretionLuminosity", "Function: computeAccretionLuminosity", _compute_computeAccretionLuminosity),
    "computePulsarSpindown": PhysicsTerm("computePulsarSpindown", "Function: computePulsarSpindown", _compute_computePulsarSpindown),
    "computeClusterMass": PhysicsTerm("computeClusterMass", "Function: computeClusterMass", _compute_computeClusterMass),
    "computeRamPressure": PhysicsTerm("computeRamPressure", "Function: computeRamPressure", _compute_computeRamPressure),
    "step_function_SOURCE4": PhysicsTerm("step_function_SOURCE4", "Function: step_function_SOURCE4", _compute_step_function_SOURCE4),
    "compute_Ereact_SOURCE4": PhysicsTerm("compute_Ereact_SOURCE4", "Function: compute_Ereact_SOURCE4", _compute_compute_Ereact_SOURCE4),
    "compute_mu_s_SOURCE4": PhysicsTerm("compute_mu_s_SOURCE4", "Function: compute_mu_s_SOURCE4", _compute_compute_mu_s_SOURCE4),
    "compute_grad_Ms_r_SOURCE4": PhysicsTerm("compute_grad_Ms_r_SOURCE4", "Function: compute_grad_Ms_r_SOURCE4", _compute_compute_grad_Ms_r_SOURCE4),
    "compute_Bj_SOURCE4": PhysicsTerm("compute_Bj_SOURCE4", "Function: compute_Bj_SOURCE4", _compute_compute_Bj_SOURCE4),
    "compute_omega_s_t_SOURCE4": PhysicsTerm("compute_omega_s_t_SOURCE4", "Function: compute_omega_s_t_SOURCE4", _compute_compute_omega_s_t_SOURCE4),
    "compute_mu_j_SOURCE4": PhysicsTerm("compute_mu_j_SOURCE4", "Function: compute_mu_j_SOURCE4", _compute_compute_mu_j_SOURCE4),
    "compute_Ug1_SOURCE4": PhysicsTerm("compute_Ug1_SOURCE4", "Function: compute_Ug1_SOURCE4", _compute_compute_Ug1_SOURCE4),
    "compute_Ug2_SOURCE4": PhysicsTerm("compute_Ug2_SOURCE4", "Function: compute_Ug2_SOURCE4", _compute_compute_Ug2_SOURCE4),
    "compute_Ug3_SOURCE4": PhysicsTerm("compute_Ug3_SOURCE4", "Function: compute_Ug3_SOURCE4", _compute_compute_Ug3_SOURCE4),
    "compute_Ug4_SOURCE4": PhysicsTerm("compute_Ug4_SOURCE4", "Function: compute_Ug4_SOURCE4", _compute_compute_Ug4_SOURCE4),
    "compute_Ubi_SOURCE4": PhysicsTerm("compute_Ubi_SOURCE4", "Function: compute_Ubi_SOURCE4", _compute_compute_Ubi_SOURCE4),
    "compute_Um_SOURCE4": PhysicsTerm("compute_Um_SOURCE4", "Function: compute_Um_SOURCE4", _compute_compute_Um_SOURCE4),
    "compute_FU_SOURCE4": PhysicsTerm("compute_FU_SOURCE4", "Function: compute_FU_SOURCE4", _compute_compute_FU_SOURCE4),
    "compute_compressed_base_SOURCE4": PhysicsTerm("compute_compressed_base_SOURCE4", "Function: compute_compressed_base_SOURCE4", _compute_compute_compressed_base_SOURCE4),
    "compute_compressed_expansion_SOURCE4": PhysicsTerm("compute_compressed_expansion_SOURCE4", "Function: compute_compressed_expansion_SOURCE4", _compute_compute_compressed_expansion_SOURCE4),
    "compute_compressed_super_adj_SOURCE4": PhysicsTerm("compute_compressed_super_adj_SOURCE4", "Function: compute_compressed_super_adj_SOURCE4", _compute_compute_compressed_super_adj_SOURCE4),
    "compute_compressed_env_SOURCE4": PhysicsTerm("compute_compressed_env_SOURCE4", "Function: compute_compressed_env_SOURCE4", _compute_compute_compressed_env_SOURCE4),
    "compute_compressed_Ug_sum_SOURCE4": PhysicsTerm("compute_compressed_Ug_sum_SOURCE4", "Function: compute_compressed_Ug_sum_SOURCE4", _compute_compute_compressed_Ug_sum_SOURCE4),
    "compute_compressed_cosm_SOURCE4": PhysicsTerm("compute_compressed_cosm_SOURCE4", "Function: compute_compressed_cosm_SOURCE4", _compute_compute_compressed_cosm_SOURCE4),
    "compute_compressed_quantum_SOURCE4": PhysicsTerm("compute_compressed_quantum_SOURCE4", "Function: compute_compressed_quantum_SOURCE4", _compute_compute_compressed_quantum_SOURCE4),
    "compute_compressed_fluid_SOURCE4": PhysicsTerm("compute_compressed_fluid_SOURCE4", "Function: compute_compressed_fluid_SOURCE4", _compute_compute_compressed_fluid_SOURCE4),
    "compute_compressed_perturbation_SOURCE4": PhysicsTerm("compute_compressed_perturbation_SOURCE4", "Function: compute_compressed_perturbation_SOURCE4", _compute_compute_compressed_perturbation_SOURCE4),
    "compute_compressed_MUGE_SOURCE4": PhysicsTerm("compute_compressed_MUGE_SOURCE4", "Function: compute_compressed_MUGE_SOURCE4", _compute_compute_compressed_MUGE_SOURCE4),
    "compute_aDPM_SOURCE4": PhysicsTerm("compute_aDPM_SOURCE4", "Function: compute_aDPM_SOURCE4", _compute_compute_aDPM_SOURCE4),
    "compute_aTHz_SOURCE4": PhysicsTerm("compute_aTHz_SOURCE4", "Function: compute_aTHz_SOURCE4", _compute_compute_aTHz_SOURCE4),
    "compute_avac_diff_SOURCE4": PhysicsTerm("compute_avac_diff_SOURCE4", "Function: compute_avac_diff_SOURCE4", _compute_compute_avac_diff_SOURCE4),
    "compute_asuper_freq_SOURCE4": PhysicsTerm("compute_asuper_freq_SOURCE4", "Function: compute_asuper_freq_SOURCE4", _compute_compute_asuper_freq_SOURCE4),
    "compute_aaether_res_SOURCE4": PhysicsTerm("compute_aaether_res_SOURCE4", "Function: compute_aaether_res_SOURCE4", _compute_compute_aaether_res_SOURCE4),
    "compute_Ug4i_SOURCE4": PhysicsTerm("compute_Ug4i_SOURCE4", "Function: compute_Ug4i_SOURCE4", _compute_compute_Ug4i_SOURCE4),
    "compute_aquantum_freq_SOURCE4": PhysicsTerm("compute_aquantum_freq_SOURCE4", "Function: compute_aquantum_freq_SOURCE4", _compute_compute_aquantum_freq_SOURCE4),
    "compute_aAether_freq_SOURCE4": PhysicsTerm("compute_aAether_freq_SOURCE4", "Function: compute_aAether_freq_SOURCE4", _compute_compute_aAether_freq_SOURCE4),
    "compute_afluid_freq_SOURCE4": PhysicsTerm("compute_afluid_freq_SOURCE4", "Function: compute_afluid_freq_SOURCE4", _compute_compute_afluid_freq_SOURCE4),
    "compute_Osc_term_SOURCE4": PhysicsTerm("compute_Osc_term_SOURCE4", "Function: compute_Osc_term_SOURCE4", _compute_compute_Osc_term_SOURCE4),
    "compute_aexp_freq_SOURCE4": PhysicsTerm("compute_aexp_freq_SOURCE4", "Function: compute_aexp_freq_SOURCE4", _compute_compute_aexp_freq_SOURCE4),
    "compute_a_wormhole_SOURCE4": PhysicsTerm("compute_a_wormhole_SOURCE4", "Function: compute_a_wormhole_SOURCE4", _compute_compute_a_wormhole_SOURCE4),
    "compute_resonance_MUGE_SOURCE4": PhysicsTerm("compute_resonance_MUGE_SOURCE4", "Function: compute_resonance_MUGE_SOURCE4", _compute_compute_resonance_MUGE_SOURCE4),
    "computeTotalMass": PhysicsTerm("computeTotalMass", "Function: computeTotalMass", _compute_computeTotalMass),
    "computeLuminosity_1": PhysicsTerm("computeLuminosity_1", "Function: computeLuminosity", _compute_computeLuminosity_1),
    "computeMagneticMoment": PhysicsTerm("computeMagneticMoment", "Function: computeMagneticMoment", _compute_computeMagneticMoment),
    "computeDensity": PhysicsTerm("computeDensity", "Function: computeDensity", _compute_computeDensity),
    "computeStarFormationRate": PhysicsTerm("computeStarFormationRate", "Function: computeStarFormationRate", _compute_computeStarFormationRate),
    "computeJetPower_1": PhysicsTerm("computeJetPower_1", "Function: computeJetPower", _compute_computeJetPower_1),
    "computeBarStrength": PhysicsTerm("computeBarStrength", "Function: computeBarStrength", _compute_computeBarStrength),
    "computeKineticEnergy": PhysicsTerm("computeKineticEnergy", "Function: computeKineticEnergy", _compute_computeKineticEnergy),
    "computeTotalMass_1": PhysicsTerm("computeTotalMass_1", "Function: computeTotalMass", _compute_computeTotalMass_1),
    "computeSchwarzschildRadius": PhysicsTerm("computeSchwarzschildRadius", "Function: computeSchwarzschildRadius", _compute_computeSchwarzschildRadius),
    "computeClusterMass_1": PhysicsTerm("computeClusterMass_1", "Function: computeClusterMass", _compute_computeClusterMass_1),
    "computeAverageMass": PhysicsTerm("computeAverageMass", "Function: computeAverageMass", _compute_computeAverageMass),
    "computeOmega": PhysicsTerm("computeOmega", "Function: computeOmega", _compute_computeOmega),
    "computeGasTemperature": PhysicsTerm("computeGasTemperature", "Function: computeGasTemperature", _compute_computeGasTemperature),
    "computeDipoleVortexSpecies": PhysicsTerm("computeDipoleVortexSpecies", "Function: computeDipoleVortexSpecies", _compute_computeDipoleVortexSpecies),
    "getFrameworkVersion": PhysicsTerm("getFrameworkVersion", "Function: getFrameworkVersion", _compute_getFrameworkVersion),
    "calculate_F_LENR": PhysicsTerm("calculate_F_LENR", "Function: calculate_F_LENR", _compute_calculate_F_LENR),
    "calculate_F_act": PhysicsTerm("calculate_F_act", "Function: calculate_F_act", _compute_calculate_F_act),
    "calculate_F_DE": PhysicsTerm("calculate_F_DE", "Function: calculate_F_DE", _compute_calculate_F_DE),
    "DPM_resonance": PhysicsTerm("DPM_resonance", "Function: DPM_resonance", _compute_DPM_resonance),
    "calculate_F_res": PhysicsTerm("calculate_F_res", "Function: calculate_F_res", _compute_calculate_F_res),
    "calculate_F_neutron": PhysicsTerm("calculate_F_neutron", "Function: calculate_F_neutron", _compute_calculate_F_neutron),
    "calculate_F_rel": PhysicsTerm("calculate_F_rel", "Function: calculate_F_rel", _compute_calculate_F_rel),
    "calculate_integrand": PhysicsTerm("calculate_integrand", "Function: calculate_integrand", _compute_calculate_integrand),
    "solve_x2": PhysicsTerm("solve_x2", "Function: solve_x2", _compute_solve_x2),
    "calculate_F_U_Bi_i": PhysicsTerm("calculate_F_U_Bi_i", "Function: calculate_F_U_Bi_i", _compute_calculate_F_U_Bi_i),
    "calculate_master_buoyancy": PhysicsTerm("calculate_master_buoyancy", "Function: calculate_master_buoyancy", _compute_calculate_master_buoyancy),
    "getDynamicParameter_1": PhysicsTerm("getDynamicParameter_1", "Function: getDynamicParameter", _compute_getDynamicParameter_1),
    "computeDynamicContribution": PhysicsTerm("computeDynamicContribution", "Function: computeDynamicContribution", _compute_computeDynamicContribution),
    "getDynamicParameter_2": PhysicsTerm("getDynamicParameter_2", "Function: getDynamicParameter", _compute_getDynamicParameter_2),
    "getDynamicParameter_3": PhysicsTerm("getDynamicParameter_3", "Function: getDynamicParameter", _compute_getDynamicParameter_3),
    "Hubble_correction": PhysicsTerm("Hubble_correction", "Function: Hubble_correction", _compute_Hubble_correction),
    "getDynamicParameter_4": PhysicsTerm("getDynamicParameter_4", "Function: getDynamicParameter", _compute_getDynamicParameter_4),
    "calculate_gravity_compressed": PhysicsTerm("calculate_gravity_compressed", "Function: calculate_gravity_compressed", _compute_calculate_gravity_compressed),
    "calculate_resonance": PhysicsTerm("calculate_resonance", "Function: calculate_resonance", _compute_calculate_resonance),
    "simulate_DPM_creation": PhysicsTerm("simulate_DPM_creation", "Function: simulate_DPM_creation", _compute_simulate_DPM_creation),
    "evaluate_26D_polynomial": PhysicsTerm("evaluate_26D_polynomial", "Function: evaluate_26D_polynomial", _compute_evaluate_26D_polynomial),
    "getDynamicParameter_5": PhysicsTerm("getDynamicParameter_5", "Function: getDynamicParameter", _compute_getDynamicParameter_5),
    "simulateDPMCreation": PhysicsTerm("simulateDPMCreation", "Function: simulateDPMCreation", _compute_simulateDPMCreation),
    "evaluate26DPolynomial": PhysicsTerm("evaluate26DPolynomial", "Function: evaluate26DPolynomial", _compute_evaluate26DPolynomial),
    "getDynamicParameter_6": PhysicsTerm("getDynamicParameter_6", "Function: getDynamicParameter", _compute_getDynamicParameter_6),
    "evaluate26DPoly_S116": PhysicsTerm("evaluate26DPoly_S116", "Function: evaluate26DPoly_S116", _compute_evaluate26DPoly_S116),
    "uqffBuoyantGravity_S116": PhysicsTerm("uqffBuoyantGravity_S116", "Function: uqffBuoyantGravity_S116", _compute_uqffBuoyantGravity_S116),
    "measureDimension": PhysicsTerm("measureDimension", "Function: measureDimension", _compute_measureDimension),
    "measureBuoyantGravity": PhysicsTerm("measureBuoyantGravity", "Function: measureBuoyantGravity", _compute_measureBuoyantGravity),
    "measureConsciousnessField": PhysicsTerm("measureConsciousnessField", "Function: measureConsciousnessField", _compute_measureConsciousnessField),
    "evaluateUnityPolynomial": PhysicsTerm("evaluateUnityPolynomial", "Function: evaluateUnityPolynomial", _compute_evaluateUnityPolynomial),
    "getDynamicParameter_7": PhysicsTerm("getDynamicParameter_7", "Function: getDynamicParameter", _compute_getDynamicParameter_7),
    "computeDynamicContribution_1": PhysicsTerm("computeDynamicContribution_1", "Function: computeDynamicContribution", _compute_computeDynamicContribution_1),
    "compute_F_U_Bi_i": PhysicsTerm("compute_F_U_Bi_i", "Function: compute_F_U_Bi_i", _compute_compute_F_U_Bi_i),
    "compute_g_UQFF": PhysicsTerm("compute_g_UQFF", "Function: compute_g_UQFF", _compute_compute_g_UQFF),
    "compute_DPM_resonance": PhysicsTerm("compute_DPM_resonance", "Function: compute_DPM_resonance", _compute_compute_DPM_resonance),
    "noise": PhysicsTerm("noise", "Function: noise", _compute_noise),
    "fade": PhysicsTerm("fade", "Function: fade", _compute_fade),
    "lerp": PhysicsTerm("lerp", "Function: lerp", _compute_lerp),
    "grad": PhysicsTerm("grad", "Function: grad", _compute_grad),
    "get_k1": PhysicsTerm("get_k1", "Function: get_k1", _compute_get_k1),
    "get_ki": PhysicsTerm("get_ki", "Function: get_ki", _compute_get_ki),
    "get_km": PhysicsTerm("get_km", "Function: get_km", _compute_get_km),
    "get_ke": PhysicsTerm("get_ke", "Function: get_ke", _compute_get_ke),
    "get_k_eta": PhysicsTerm("get_k_eta", "Function: get_k_eta", _compute_get_k_eta),
    "get_sfr": PhysicsTerm("get_sfr", "Function: get_sfr", _compute_get_sfr),
    "get_wind_vel": PhysicsTerm("get_wind_vel", "Function: get_wind_vel", _compute_get_wind_vel),
    "get_mag_field": PhysicsTerm("get_mag_field", "Function: get_mag_field", _compute_get_mag_field),
    "get_f_Ub_scale": PhysicsTerm("get_f_Ub_scale", "Function: get_f_Ub_scale", _compute_get_f_Ub_scale),
    "get_M": PhysicsTerm("get_M", "Function: get_M", _compute_get_M),
    "get_r": PhysicsTerm("get_r", "Function: get_r", _compute_get_r),
    "cos_theta": PhysicsTerm("cos_theta", "Function: cos_theta", _compute_cos_theta),
    "Hubble_correction_1": PhysicsTerm("Hubble_correction_1", "Function: Hubble_correction", _compute_Hubble_correction_1),
    "Hubble_correction_2": PhysicsTerm("Hubble_correction_2", "Function: Hubble_correction", _compute_Hubble_correction_2),
    "noise_1": PhysicsTerm("noise_1", "Function: noise", _compute_noise_1),
    "fade_1": PhysicsTerm("fade_1", "Function: fade", _compute_fade_1),
    "lerp_1": PhysicsTerm("lerp_1", "Function: lerp", _compute_lerp_1),
    "grad_1": PhysicsTerm("grad_1", "Function: grad", _compute_grad_1),
    "newtonMethod": PhysicsTerm("newtonMethod", "Function: newtonMethod", _compute_newtonMethod),
    "inline_defaultValue": PhysicsTerm("inline_defaultValue", "Inline equation: defaultValue", _compute_inline_defaultValue),
    "inline_defaultValue_1": PhysicsTerm("inline_defaultValue_1", "Inline equation: defaultValue", _compute_inline_defaultValue_1),
    "inline_value": PhysicsTerm("inline_value", "Inline equation: value", _compute_inline_value),
    "inline_minVal": PhysicsTerm("inline_minVal", "Inline equation: minVal", _compute_inline_minVal),
    "inline_sum": PhysicsTerm("inline_sum", "Inline equation: sum", _compute_inline_sum),
    "inline_maxAbs": PhysicsTerm("inline_maxAbs", "Inline equation: maxAbs", _compute_inline_maxAbs),
    "inline_val": PhysicsTerm("inline_val", "Inline equation: val", _compute_inline_val),
    "inline_amp": PhysicsTerm("inline_amp", "Inline equation: amp", _compute_inline_amp),
    "inline_rho_vac": PhysicsTerm("inline_rho_vac", "Inline equation: rho_vac", _compute_inline_rho_vac),
    "inline_coupling": PhysicsTerm("inline_coupling", "Inline equation: coupling", _compute_inline_coupling),
    "inline_strength": PhysicsTerm("inline_strength", "Inline equation: strength", _compute_inline_strength),
    "inline_M": PhysicsTerm("inline_M", "Inline equation: M", _compute_inline_M),
    "inline_r": PhysicsTerm("inline_r", "Inline equation: r", _compute_inline_r),
    "inline_alpha": PhysicsTerm("inline_alpha", "Inline equation: alpha", _compute_inline_alpha),
    "inline_mass": PhysicsTerm("inline_mass", "Inline equation: mass", _compute_inline_mass),
    "inline_rho_0": PhysicsTerm("inline_rho_0", "Inline equation: rho_0", _compute_inline_rho_0),
    "inline_e_scale": PhysicsTerm("inline_e_scale", "Inline equation: e_scale", _compute_inline_e_scale),
    "inline_strength_1": PhysicsTerm("inline_strength_1", "Inline equation: strength", _compute_inline_strength_1),
    "inline_M_1": PhysicsTerm("inline_M_1", "Inline equation: M", _compute_inline_M_1),
    "inline_r_1": PhysicsTerm("inline_r_1", "Inline equation: r", _compute_inline_r_1),
    "inline_temp": PhysicsTerm("inline_temp", "Inline equation: temp", _compute_inline_temp),
    "inline_DPM_momentum": PhysicsTerm("inline_DPM_momentum", "Inline equation: DPM_momentum", _compute_inline_DPM_momentum),
    "inline_DPM_gravity": PhysicsTerm("inline_DPM_gravity", "Inline equation: DPM_gravity", _compute_inline_DPM_gravity),
    "inline_DPM_stability": PhysicsTerm("inline_DPM_stability", "Inline equation: DPM_stability", _compute_inline_DPM_stability),
    "inline_LENR_term": PhysicsTerm("inline_LENR_term", "Inline equation: LENR_term", _compute_inline_LENR_term),
    "inline_term_mom": PhysicsTerm("inline_term_mom", "Inline equation: term_mom", _compute_inline_term_mom),
    "inline_term_grav": PhysicsTerm("inline_term_grav", "Inline equation: term_grav", _compute_inline_term_grav),
    "inline_term_vac": PhysicsTerm("inline_term_vac", "Inline equation: term_vac", _compute_inline_term_vac),
    "inline_DPM_resonance": PhysicsTerm("inline_DPM_resonance", "Inline equation: DPM_resonance", _compute_inline_DPM_resonance),
    "inline_term_res": PhysicsTerm("inline_term_res", "Inline equation: term_res", _compute_inline_term_res),
    "inline_gravity_compressed": PhysicsTerm("inline_gravity_compressed", "Inline equation: gravity_compressed", _compute_inline_gravity_compressed),
    "inline_beta_i": PhysicsTerm("inline_beta_i", "Inline equation: beta_i", _compute_inline_beta_i),
    "inline_V_infl": PhysicsTerm("inline_V_infl", "Inline equation: V_infl", _compute_inline_V_infl),
    "inline_rho_vac_A": PhysicsTerm("inline_rho_vac_A", "Inline equation: rho_vac_A", _compute_inline_rho_vac_A),
    "inline_a_universal": PhysicsTerm("inline_a_universal", "Inline equation: a_universal", _compute_inline_a_universal),
    "inline_buoyancy": PhysicsTerm("inline_buoyancy", "Inline equation: buoyancy", _compute_inline_buoyancy),
    "inline_F_rel": PhysicsTerm("inline_F_rel", "Inline equation: F_rel", _compute_inline_F_rel),
    "inline_x2": PhysicsTerm("inline_x2", "Inline equation: x2", _compute_inline_x2),
    "inline_B": PhysicsTerm("inline_B", "Inline equation: B", _compute_inline_B),
    "inline_B_1": PhysicsTerm("inline_B_1", "Inline equation: B", _compute_inline_B_1),
    "inline_omega": PhysicsTerm("inline_omega", "Inline equation: omega", _compute_inline_omega),
    "inline_k": PhysicsTerm("inline_k", "Inline equation: k", _compute_inline_k),
    "inline_k_1": PhysicsTerm("inline_k_1", "Inline equation: k", _compute_inline_k_1),
    "inline_omega_L": PhysicsTerm("inline_omega_L", "Inline equation: omega_L", _compute_inline_omega_L),
    "inline_omega_0": PhysicsTerm("inline_omega_0", "Inline equation: omega_0", _compute_inline_omega_0),
    "inline_M_2": PhysicsTerm("inline_M_2", "Inline equation: M", _compute_inline_M_2),
    "inline_M_3": PhysicsTerm("inline_M_3", "Inline equation: M", _compute_inline_M_3),
    "inline_t_scale": PhysicsTerm("inline_t_scale", "Inline equation: t_scale", _compute_inline_t_scale),
    "inline_L": PhysicsTerm("inline_L", "Inline equation: L", _compute_inline_L),
    "inline_L_1": PhysicsTerm("inline_L_1", "Inline equation: L", _compute_inline_L_1),
    "inline_t_p": PhysicsTerm("inline_t_p", "Inline equation: t_p", _compute_inline_t_p),
    "inline_gravity": PhysicsTerm("inline_gravity", "Inline equation: gravity", _compute_inline_gravity),
    "inline_ionization": PhysicsTerm("inline_ionization", "Inline equation: ionization", _compute_inline_ionization),
    "inline_v_exp": PhysicsTerm("inline_v_exp", "Inline equation: v_exp", _compute_inline_v_exp),
    "inline_P_thermal": PhysicsTerm("inline_P_thermal", "Inline equation: P_thermal", _compute_inline_P_thermal),
    "inline_P_rad": PhysicsTerm("inline_P_rad", "Inline equation: P_rad", _compute_inline_P_rad),
    "inline_F_expansion": PhysicsTerm("inline_F_expansion", "Inline equation: F_expansion", _compute_inline_F_expansion),
    "inline_F_ionization": PhysicsTerm("inline_F_ionization", "Inline equation: F_ionization", _compute_inline_F_ionization),
    "inline_gas_nebula": PhysicsTerm("inline_gas_nebula", "Inline equation: gas_nebula", _compute_inline_gas_nebula),
    "inline_L_2": PhysicsTerm("inline_L_2", "Inline equation: L", _compute_inline_L_2),
    "inline_ionization_rate": PhysicsTerm("inline_ionization_rate", "Inline equation: ionization_rate", _compute_inline_ionization_rate),
    "inline_n_gas": PhysicsTerm("inline_n_gas", "Inline equation: n_gas", _compute_inline_n_gas),
    "inline_V": PhysicsTerm("inline_V", "Inline equation: V", _compute_inline_V),
    "inline_ion_frac": PhysicsTerm("inline_ion_frac", "Inline equation: ion_frac", _compute_inline_ion_frac),
    "inline_r_init": PhysicsTerm("inline_r_init", "Inline equation: r_init", _compute_inline_r_init),
    "inline_r_initial": PhysicsTerm("inline_r_initial", "Inline equation: r_initial", _compute_inline_r_initial),
    "inline_v_exp_1": PhysicsTerm("inline_v_exp_1", "Inline equation: v_exp", _compute_inline_v_exp_1),
    "inline_t0": PhysicsTerm("inline_t0", "Inline equation: t0", _compute_inline_t0),
    "inline_r_t": PhysicsTerm("inline_r_t", "Inline equation: r_t", _compute_inline_r_t),
    "inline_M_4": PhysicsTerm("inline_M_4", "Inline equation: M", _compute_inline_M_4),
    "inline_E_expansion": PhysicsTerm("inline_E_expansion", "Inline equation: E_expansion", _compute_inline_E_expansion),
    "inline_DPM_res": PhysicsTerm("inline_DPM_res", "Inline equation: DPM_res", _compute_inline_DPM_res),
    "inline_LENR": PhysicsTerm("inline_LENR", "Inline equation: LENR", _compute_inline_LENR),
    "inline_term_mom_1": PhysicsTerm("inline_term_mom_1", "Inline equation: term_mom", _compute_inline_term_mom_1),
    "inline_term_grav_1": PhysicsTerm("inline_term_grav_1", "Inline equation: term_grav", _compute_inline_term_grav_1),
    "inline_term_res_1": PhysicsTerm("inline_term_res_1", "Inline equation: term_res", _compute_inline_term_res_1),
    "inline_term_neut": PhysicsTerm("inline_term_neut", "Inline equation: term_neut", _compute_inline_term_neut),
    "inline_term_rel": PhysicsTerm("inline_term_rel", "Inline equation: term_rel", _compute_inline_term_rel),
    "inline_integrand": PhysicsTerm("inline_integrand", "Inline equation: integrand", _compute_inline_integrand),
    "inline_beta_i_1": PhysicsTerm("inline_beta_i_1", "Inline equation: beta_i", _compute_inline_beta_i_1),
    "inline_V_infl_1": PhysicsTerm("inline_V_infl_1", "Inline equation: V_infl", _compute_inline_V_infl_1),
    "inline_rho_vac_1": PhysicsTerm("inline_rho_vac_1", "Inline equation: rho_vac", _compute_inline_rho_vac_1),
    "inline_a_univ": PhysicsTerm("inline_a_univ", "Inline equation: a_univ", _compute_inline_a_univ),
    "inline_lambda_i": PhysicsTerm("inline_lambda_i", "Inline equation: lambda_i", _compute_inline_lambda_i),
    "inline_rho_sc": PhysicsTerm("inline_rho_sc", "Inline equation: rho_sc", _compute_inline_rho_sc),
    "inline_rho_ua": PhysicsTerm("inline_rho_ua", "Inline equation: rho_ua", _compute_inline_rho_ua),
    "inline_omega_s": PhysicsTerm("inline_omega_s", "Inline equation: omega_s", _compute_inline_omega_s),
    "inline_t_scale_1": PhysicsTerm("inline_t_scale_1", "Inline equation: t_scale", _compute_inline_t_scale_1),
    "inline_f_TRZ": PhysicsTerm("inline_f_TRZ", "Inline equation: f_TRZ", _compute_inline_f_TRZ),
    "inline_k_neutron": PhysicsTerm("inline_k_neutron", "Inline equation: k_neutron", _compute_inline_k_neutron),
    "inline_sigma_n": PhysicsTerm("inline_sigma_n", "Inline equation: sigma_n", _compute_inline_sigma_n),
    "inline_gas_nebula_1": PhysicsTerm("inline_gas_nebula_1", "Inline equation: gas_nebula", _compute_inline_gas_nebula_1),
    "inline_term_rel_1": PhysicsTerm("inline_term_rel_1", "Inline equation: term_rel", _compute_inline_term_rel_1),
    "inline_integrand_1": PhysicsTerm("inline_integrand_1", "Inline equation: integrand", _compute_inline_integrand_1),
    "inline_buoyancy_1": PhysicsTerm("inline_buoyancy_1", "Inline equation: buoyancy", _compute_inline_buoyancy_1),
    "inline_dipole_base": PhysicsTerm("inline_dipole_base", "Inline equation: dipole_base", _compute_inline_dipole_base),
    "inline_phase": PhysicsTerm("inline_phase", "Inline equation: phase", _compute_inline_phase),
    "inline_beta_i_2": PhysicsTerm("inline_beta_i_2", "Inline equation: beta_i", _compute_inline_beta_i_2),
    "inline_k1": PhysicsTerm("inline_k1", "Inline equation: k1", _compute_inline_k1),
    "inline_ki": PhysicsTerm("inline_ki", "Inline equation: ki", _compute_inline_ki),
    "inline_f_nu": PhysicsTerm("inline_f_nu", "Inline equation: f_nu", _compute_inline_f_nu),
    "inline_geom_factor": PhysicsTerm("inline_geom_factor", "Inline equation: geom_factor", _compute_inline_geom_factor),
    "inline_exp_barrier": PhysicsTerm("inline_exp_barrier", "Inline equation: exp_barrier", _compute_inline_exp_barrier),
    "inline_U_g1": PhysicsTerm("inline_U_g1", "Inline equation: U_g1", _compute_inline_U_g1),
    "inline_U_g3": PhysicsTerm("inline_U_g3", "Inline equation: U_g3", _compute_inline_U_g3),
    "inline_R_EB": PhysicsTerm("inline_R_EB", "Inline equation: R_EB", _compute_inline_R_EB),
    "inline_r_2": PhysicsTerm("inline_r_2", "Inline equation: r", _compute_inline_r_2),
    "inline_U_m": PhysicsTerm("inline_U_m", "Inline equation: U_m", _compute_inline_U_m),
    "inline_rho_vac_UA": PhysicsTerm("inline_rho_vac_UA", "Inline equation: rho_vac_UA", _compute_inline_rho_vac_UA),
    "inline_n": PhysicsTerm("inline_n", "Inline equation: n", _compute_inline_n),
    "inline_exp_ssq": PhysicsTerm("inline_exp_ssq", "Inline equation: exp_ssq", _compute_inline_exp_ssq),
    "inline_exp_pi_t": PhysicsTerm("inline_exp_pi_t", "Inline equation: exp_pi_t", _compute_inline_exp_pi_t),
    "inline_mass_1": PhysicsTerm("inline_mass_1", "Inline equation: mass", _compute_inline_mass_1),
    "inline_r_3": PhysicsTerm("inline_r_3", "Inline equation: r", _compute_inline_r_3),
    "inline_gravitational": PhysicsTerm("inline_gravitational", "Inline equation: gravitational", _compute_inline_gravitational),
    "inline_oscillation": PhysicsTerm("inline_oscillation", "Inline equation: oscillation", _compute_inline_oscillation),
    "inline_mass_2": PhysicsTerm("inline_mass_2", "Inline equation: mass", _compute_inline_mass_2),
    "inline_r_4": PhysicsTerm("inline_r_4", "Inline equation: r", _compute_inline_r_4),
    "inline_base_gravity": PhysicsTerm("inline_base_gravity", "Inline equation: base_gravity", _compute_inline_base_gravity),
    "inline_compressed": PhysicsTerm("inline_compressed", "Inline equation: compressed", _compute_inline_compressed),
    "inline_time_mod": PhysicsTerm("inline_time_mod", "Inline equation: time_mod", _compute_inline_time_mod),
    "inline_velocity": PhysicsTerm("inline_velocity", "Inline equation: velocity", _compute_inline_velocity),
    "inline_gamma": PhysicsTerm("inline_gamma", "Inline equation: gamma", _compute_inline_gamma),
    "inline_scm_density": PhysicsTerm("inline_scm_density", "Inline equation: scm_density", _compute_inline_scm_density),
    "inline_scm_d": PhysicsTerm("inline_scm_d", "Inline equation: scm_d", _compute_inline_scm_d),
    "inline_v": PhysicsTerm("inline_v", "Inline equation: v", _compute_inline_v),
    "inline_rho": PhysicsTerm("inline_rho", "Inline equation: rho", _compute_inline_rho),
    "inline_k_2": PhysicsTerm("inline_k_2", "Inline equation: k", _compute_inline_k_2),
    "inline_B_field": PhysicsTerm("inline_B_field", "Inline equation: B_field", _compute_inline_B_field),
    "inline_B_2": PhysicsTerm("inline_B_2", "Inline equation: B", _compute_inline_B_2),
    "inline_omega_1": PhysicsTerm("inline_omega_1", "Inline equation: omega", _compute_inline_omega_1),
    "inline_R": PhysicsTerm("inline_R", "Inline equation: R", _compute_inline_R),
    "inline_scm": PhysicsTerm("inline_scm", "Inline equation: scm", _compute_inline_scm),
    "inline_Bs_t": PhysicsTerm("inline_Bs_t", "Inline equation: Bs_t", _compute_inline_Bs_t),
    "inline_B0": PhysicsTerm("inline_B0", "Inline equation: B0", _compute_inline_B0),
    "inline_B0_1": PhysicsTerm("inline_B0_1", "Inline equation: B0", _compute_inline_B0_1),
    "inline_beta": PhysicsTerm("inline_beta", "Inline equation: beta", _compute_inline_beta),
    "inline_Ugi": PhysicsTerm("inline_Ugi", "Inline equation: Ugi", _compute_inline_Ugi),
    "inline_tn": PhysicsTerm("inline_tn", "Inline equation: tn", _compute_inline_tn),
    "inline_beta_1": PhysicsTerm("inline_beta_1", "Inline equation: beta", _compute_inline_beta_1),
    "inline_omega_2": PhysicsTerm("inline_omega_2", "Inline equation: omega", _compute_inline_omega_2),
    "inline_M_5": PhysicsTerm("inline_M_5", "Inline equation: M", _compute_inline_M_5),
    "inline_d": PhysicsTerm("inline_d", "Inline equation: d", _compute_inline_d),
    "inline_eps": PhysicsTerm("inline_eps", "Inline equation: eps", _compute_inline_eps),
    "inline_rho_1": PhysicsTerm("inline_rho_1", "Inline equation: rho", _compute_inline_rho_1),
    "inline_ua": PhysicsTerm("inline_ua", "Inline equation: ua", _compute_inline_ua),
    "inline_M_6": PhysicsTerm("inline_M_6", "Inline equation: M", _compute_inline_M_6),
    "inline_r_5": PhysicsTerm("inline_r_5", "Inline equation: r", _compute_inline_r_5),
    "inline_hubble": PhysicsTerm("inline_hubble", "Inline equation: hubble", _compute_inline_hubble),
    "inline_H": PhysicsTerm("inline_H", "Inline equation: H", _compute_inline_H),
    "inline_B_3": PhysicsTerm("inline_B_3", "Inline equation: B", _compute_inline_B_3),
    "inline_Bcrit": PhysicsTerm("inline_Bcrit", "Inline equation: Bcrit", _compute_inline_Bcrit),
    "inline_lambda": PhysicsTerm("inline_lambda", "Inline equation: lambda", _compute_inline_lambda),
    "inline_L_3": PhysicsTerm("inline_L_3", "Inline equation: L", _compute_inline_L_3),
    "inline_uncertainty": PhysicsTerm("inline_uncertainty", "Inline equation: uncertainty", _compute_inline_uncertainty),
    "inline_dx_dp": PhysicsTerm("inline_dx_dp", "Inline equation: dx_dp", _compute_inline_dx_dp),
    "inline_psi": PhysicsTerm("inline_psi", "Inline equation: psi", _compute_inline_psi),
    "inline_t_hub": PhysicsTerm("inline_t_hub", "Inline equation: t_hub", _compute_inline_t_hub),
    "inline_rho_2": PhysicsTerm("inline_rho_2", "Inline equation: rho", _compute_inline_rho_2),
    "inline_V_1": PhysicsTerm("inline_V_1", "Inline equation: V", _compute_inline_V_1),
    "inline_g": PhysicsTerm("inline_g", "Inline equation: g", _compute_inline_g),
    "inline_M_7": PhysicsTerm("inline_M_7", "Inline equation: M", _compute_inline_M_7),
    "inline_M_DM": PhysicsTerm("inline_M_DM", "Inline equation: M_DM", _compute_inline_M_DM),
    "inline_delta_rho_rho": PhysicsTerm("inline_delta_rho_rho", "Inline equation: delta_rho_rho", _compute_inline_delta_rho_rho),
    "inline_pert2": PhysicsTerm("inline_pert2", "Inline equation: pert2", _compute_inline_pert2),
    "inline_halo_mass": PhysicsTerm("inline_halo_mass", "Inline equation: halo_mass", _compute_inline_halo_mass),
    "inline_x": PhysicsTerm("inline_x", "Inline equation: x", _compute_inline_x),
    "inline_rho_0_1": PhysicsTerm("inline_rho_0_1", "Inline equation: rho_0", _compute_inline_rho_0_1),
    "inline_dm_contrib": PhysicsTerm("inline_dm_contrib", "Inline equation: dm_contrib", _compute_inline_dm_contrib),
    "inline_vac_contrib": PhysicsTerm("inline_vac_contrib", "Inline equation: vac_contrib", _compute_inline_vac_contrib),
    "inline_freq": PhysicsTerm("inline_freq", "Inline equation: freq", _compute_inline_freq),
    "inline_base_gravity_1": PhysicsTerm("inline_base_gravity_1", "Inline equation: base_gravity", _compute_inline_base_gravity_1),
    "inline_resonance_mod": PhysicsTerm("inline_resonance_mod", "Inline equation: resonance_mod", _compute_inline_resonance_mod),
    "inline_state_evolution": PhysicsTerm("inline_state_evolution", "Inline equation: state_evolution", _compute_inline_state_evolution),
    "inline_base_rot": PhysicsTerm("inline_base_rot", "Inline equation: base_rot", _compute_inline_base_rot),
    "inline_visc": PhysicsTerm("inline_visc", "Inline equation: visc", _compute_inline_visc),
    "inline_uqff_g": PhysicsTerm("inline_uqff_g", "Inline equation: uqff_g", _compute_inline_uqff_g),
    "inline_force_jet": PhysicsTerm("inline_force_jet", "Inline equation: force_jet", _compute_inline_force_jet),
    "inline_diffusion_term": PhysicsTerm("inline_diffusion_term", "Inline equation: diffusion_term", _compute_inline_diffusion_term),
    "inline_pressure_projection": PhysicsTerm("inline_pressure_projection", "Inline equation: pressure_projection", _compute_inline_pressure_projection),
    "inline_velocity_1": PhysicsTerm("inline_velocity_1", "Inline equation: velocity", _compute_inline_velocity_1),
    "inline_coupling_1": PhysicsTerm("inline_coupling_1", "Inline equation: coupling", _compute_inline_coupling_1),
    "inline_modulation": PhysicsTerm("inline_modulation", "Inline equation: modulation", _compute_inline_modulation),
    "inline_k1_val": PhysicsTerm("inline_k1_val", "Inline equation: k1_val", _compute_inline_k1_val),
    "inline_Ubi_sum": PhysicsTerm("inline_Ubi_sum", "Inline equation: Ubi_sum", _compute_inline_Ubi_sum),
    "inline_A_scalar": PhysicsTerm("inline_A_scalar", "Inline equation: A_scalar", _compute_inline_A_scalar),
    "inline_f_dpm": PhysicsTerm("inline_f_dpm", "Inline equation: f_dpm", _compute_inline_f_dpm),
    "inline_I": PhysicsTerm("inline_I", "Inline equation: I", _compute_inline_I),
    "inline_A": PhysicsTerm("inline_A", "Inline equation: A", _compute_inline_A),
    "inline_omega1": PhysicsTerm("inline_omega1", "Inline equation: omega1", _compute_inline_omega1),
    "inline_omega2": PhysicsTerm("inline_omega2", "Inline equation: omega2", _compute_inline_omega2),
    "inline_Vsys": PhysicsTerm("inline_Vsys", "Inline equation: Vsys", _compute_inline_Vsys),
    "inline_FDPM": PhysicsTerm("inline_FDPM", "Inline equation: FDPM", _compute_inline_FDPM),
    "inline_f_thz": PhysicsTerm("inline_f_thz", "Inline equation: f_thz", _compute_inline_f_thz),
    "inline_vexp": PhysicsTerm("inline_vexp", "Inline equation: vexp", _compute_inline_vexp),
    "inline_aDPM": PhysicsTerm("inline_aDPM", "Inline equation: aDPM", _compute_inline_aDPM),
    "inline_delta_E": PhysicsTerm("inline_delta_E", "Inline equation: delta_E", _compute_inline_delta_E),
    "inline_F_sc": PhysicsTerm("inline_F_sc", "Inline equation: F_sc", _compute_inline_F_sc),
    "inline_ua_scm": PhysicsTerm("inline_ua_scm", "Inline equation: ua_scm", _compute_inline_ua_scm),
    "inline_f_q": PhysicsTerm("inline_f_q", "Inline equation: f_q", _compute_inline_f_q),
    "inline_f_a": PhysicsTerm("inline_f_a", "Inline equation: f_a", _compute_inline_f_a),
    "inline_f_w": PhysicsTerm("inline_f_w", "Inline equation: f_w", _compute_inline_f_w),
    "inline_r_6": PhysicsTerm("inline_r_6", "Inline equation: r", _compute_inline_r_6),
    "inline_mass_3": PhysicsTerm("inline_mass_3", "Inline equation: mass", _compute_inline_mass_3),
    "inline_Bs_t_1": PhysicsTerm("inline_Bs_t_1", "Inline equation: Bs_t", _compute_inline_Bs_t_1),
    "inline_defect": PhysicsTerm("inline_defect", "Inline equation: defect", _compute_inline_defect),
    "inline_mass_4": PhysicsTerm("inline_mass_4", "Inline equation: mass", _compute_inline_mass_4),
    "inline_Ereact": PhysicsTerm("inline_Ereact", "Inline equation: Ereact", _compute_inline_Ereact),
    "inline_S": PhysicsTerm("inline_S", "Inline equation: S", _compute_inline_S),
    "inline_wind_mod": PhysicsTerm("inline_wind_mod", "Inline equation: wind_mod", _compute_inline_wind_mod),
    "inline_radius": PhysicsTerm("inline_radius", "Inline equation: radius", _compute_inline_radius),
    "inline_theta": PhysicsTerm("inline_theta", "Inline equation: theta", _compute_inline_theta),
    "inline_omega_s_t": PhysicsTerm("inline_omega_s_t", "Inline equation: omega_s_t", _compute_inline_omega_s_t),
    "inline_Bj": PhysicsTerm("inline_Bj", "Inline equation: Bj", _compute_inline_Bj),
    "inline_vacuum_density": PhysicsTerm("inline_vacuum_density", "Inline equation: vacuum_density", _compute_inline_vacuum_density),
    "inline_radius_1": PhysicsTerm("inline_radius_1", "Inline equation: radius", _compute_inline_radius_1),
    "inline_decay": PhysicsTerm("inline_decay", "Inline equation: decay", _compute_inline_decay),
    "inline_single": PhysicsTerm("inline_single", "Inline equation: single", _compute_inline_single),
    "inline_coupling_2": PhysicsTerm("inline_coupling_2", "Inline equation: coupling", _compute_inline_coupling_2),
    "inline_mod": PhysicsTerm("inline_mod", "Inline equation: mod", _compute_inline_mod),
    "inline_trace": PhysicsTerm("inline_trace", "Inline equation: trace", _compute_inline_trace),
    "inline_inertia": PhysicsTerm("inline_inertia", "Inline equation: inertia", _compute_inline_inertia),
    "inline_compression": PhysicsTerm("inline_compression", "Inline equation: compression", _compute_inline_compression),
    "inline_g_compressed": PhysicsTerm("inline_g_compressed", "Inline equation: g_compressed", _compute_inline_g_compressed),
    "inline_inertia_1": PhysicsTerm("inline_inertia_1", "Inline equation: inertia", _compute_inline_inertia_1),
    "inline_vac_factor": PhysicsTerm("inline_vac_factor", "Inline equation: vac_factor", _compute_inline_vac_factor),
    "inline_inertia_2": PhysicsTerm("inline_inertia_2", "Inline equation: inertia", _compute_inline_inertia_2),
    "inline_asuper_freq": PhysicsTerm("inline_asuper_freq", "Inline equation: asuper_freq", _compute_inline_asuper_freq),
    "inline_inertia_3": PhysicsTerm("inline_inertia_3", "Inline equation: inertia", _compute_inline_inertia_3),
    "inline_aaether_res": PhysicsTerm("inline_aaether_res", "Inline equation: aaether_res", _compute_inline_aaether_res),
    "inline_inertia_4": PhysicsTerm("inline_inertia_4", "Inline equation: inertia", _compute_inline_inertia_4),
    "inline_aquantum_freq": PhysicsTerm("inline_aquantum_freq", "Inline equation: aquantum_freq", _compute_inline_aquantum_freq),
    "inline_scale": PhysicsTerm("inline_scale", "Inline equation: scale", _compute_inline_scale),
    "inline_base_value": PhysicsTerm("inline_base_value", "Inline equation: base_value", _compute_inline_base_value),
    "inline_config_influence": PhysicsTerm("inline_config_influence", "Inline equation: config_influence", _compute_inline_config_influence),
    "inline_integ": PhysicsTerm("inline_integ", "Inline equation: integ", _compute_inline_integ),
    "inline_neutron_factor": PhysicsTerm("inline_neutron_factor", "Inline equation: neutron_factor", _compute_inline_neutron_factor),
    "inline_f_TRZ_1": PhysicsTerm("inline_f_TRZ_1", "Inline equation: f_TRZ", _compute_inline_f_TRZ_1),
    "inline_term2": PhysicsTerm("inline_term2", "Inline equation: term2", _compute_inline_term2),
    "inline_term3": PhysicsTerm("inline_term3", "Inline equation: term3", _compute_inline_term3),
    "inline_term4": PhysicsTerm("inline_term4", "Inline equation: term4", _compute_inline_term4),
    "inline_F_U_Bi_i": PhysicsTerm("inline_F_U_Bi_i", "Inline equation: F_U_Bi_i", _compute_inline_F_U_Bi_i),
    "inline_k_3": PhysicsTerm("inline_k_3", "Inline equation: k", _compute_inline_k_3),
    "inline_F_vac_rep": PhysicsTerm("inline_F_vac_rep", "Inline equation: F_vac_rep", _compute_inline_F_vac_rep),
    "inline_k_4": PhysicsTerm("inline_k_4", "Inline equation: k", _compute_inline_k_4),
    "inline_freq_ratio": PhysicsTerm("inline_freq_ratio", "Inline equation: freq_ratio", _compute_inline_freq_ratio),
    "inline_F_thz_shock": PhysicsTerm("inline_F_thz_shock", "Inline equation: F_thz_shock", _compute_inline_F_thz_shock),
    "inline_k_5": PhysicsTerm("inline_k_5", "Inline equation: k", _compute_inline_k_5),
    "inline_F_conduit": PhysicsTerm("inline_F_conduit", "Inline equation: F_conduit", _compute_inline_F_conduit),
    "inline_k_6": PhysicsTerm("inline_k_6", "Inline equation: k", _compute_inline_k_6),
    "inline_F_spooky": PhysicsTerm("inline_F_spooky", "Inline equation: F_spooky", _compute_inline_F_spooky),
    "inline_g_1": PhysicsTerm("inline_g_1", "Inline equation: g", _compute_inline_g_1),
    "inline_base": PhysicsTerm("inline_base", "Inline equation: base", _compute_inline_base),
    "inline_Lambda_term": PhysicsTerm("inline_Lambda_term", "Inline equation: Lambda_term", _compute_inline_Lambda_term),
    "inline_result": PhysicsTerm("inline_result", "Inline equation: result", _compute_inline_result),
    "inline_m_bh": PhysicsTerm("inline_m_bh", "Inline equation: m_bh", _compute_inline_m_bh),
    "inline_M_8": PhysicsTerm("inline_M_8", "Inline equation: M", _compute_inline_M_8),
    "inline_Hz": PhysicsTerm("inline_Hz", "Inline equation: Hz", _compute_inline_Hz),
    "inline_B0_2": PhysicsTerm("inline_B0_2", "Inline equation: B0", _compute_inline_B0_2),
    "inline_B_crit": PhysicsTerm("inline_B_crit", "Inline equation: B_crit", _compute_inline_B_crit),
    "inline_term1": PhysicsTerm("inline_term1", "Inline equation: term1", _compute_inline_term1),
    "inline_term_BH": PhysicsTerm("inline_term_BH", "Inline equation: term_BH", _compute_inline_term_BH),
    "inline_Lambda": PhysicsTerm("inline_Lambda", "Inline equation: Lambda", _compute_inline_Lambda),
    "inline_rho_vac_UA_1": PhysicsTerm("inline_rho_vac_UA_1", "Inline equation: rho_vac_UA", _compute_inline_rho_vac_UA_1),
    "inline_rho_vac_SCm": PhysicsTerm("inline_rho_vac_SCm", "Inline equation: rho_vac_SCm", _compute_inline_rho_vac_SCm),
    "inline_term2_1": PhysicsTerm("inline_term2_1", "Inline equation: term2", _compute_inline_term2_1),
    "inline_c_light": PhysicsTerm("inline_c_light", "Inline equation: c_light", _compute_inline_c_light),
    "inline_q_charge": PhysicsTerm("inline_q_charge", "Inline equation: q_charge", _compute_inline_q_charge),
    "inline_v_surf": PhysicsTerm("inline_v_surf", "Inline equation: v_surf", _compute_inline_v_surf),
    "inline_proton_mass": PhysicsTerm("inline_proton_mass", "Inline equation: proton_mass", _compute_inline_proton_mass),
    "inline_scale_EM": PhysicsTerm("inline_scale_EM", "Inline equation: scale_EM", _compute_inline_scale_EM),
    "inline_em_base": PhysicsTerm("inline_em_base", "Inline equation: em_base", _compute_inline_em_base),
    "inline_p_init": PhysicsTerm("inline_p_init", "Inline equation: p_init", _compute_inline_p_init),
    "inline_dOdt": PhysicsTerm("inline_dOdt", "Inline equation: dOdt", _compute_inline_dOdt),
    "inline_gw_prefactor": PhysicsTerm("inline_gw_prefactor", "Inline equation: gw_prefactor", _compute_inline_gw_prefactor),
    "inline_hbar": PhysicsTerm("inline_hbar", "Inline equation: hbar", _compute_inline_hbar),
    "inline_delta_x": PhysicsTerm("inline_delta_x", "Inline equation: delta_x", _compute_inline_delta_x),
    "inline_delta_p": PhysicsTerm("inline_delta_p", "Inline equation: delta_p", _compute_inline_delta_p),
    "inline_integral_psi": PhysicsTerm("inline_integral_psi", "Inline equation: integral_psi", _compute_inline_integral_psi),
    "inline_t_Hubble": PhysicsTerm("inline_t_Hubble", "Inline equation: t_Hubble", _compute_inline_t_Hubble),
    "inline_sqrt_unc": PhysicsTerm("inline_sqrt_unc", "Inline equation: sqrt_unc", _compute_inline_sqrt_unc),
    "inline_G": PhysicsTerm("inline_G", "Inline equation: G", _compute_inline_G),
    "inline_rho_fluid": PhysicsTerm("inline_rho_fluid", "Inline equation: rho_fluid", _compute_inline_rho_fluid),
    "inline_V_2": PhysicsTerm("inline_V_2", "Inline equation: V", _compute_inline_V_2),
    "inline_a": PhysicsTerm("inline_a", "Inline equation: a", _compute_inline_a),
    "inline_term_osc1": PhysicsTerm("inline_term_osc1", "Inline equation: term_osc1", _compute_inline_term_osc1),
    "inline_arg": PhysicsTerm("inline_arg", "Inline equation: arg", _compute_inline_arg),
    "inline_term_osc2": PhysicsTerm("inline_term_osc2", "Inline equation: term_osc2", _compute_inline_term_osc2),
    "inline_dm_factor": PhysicsTerm("inline_dm_factor", "Inline equation: dm_factor", _compute_inline_dm_factor),
    "inline_term_dm_force_like": PhysicsTerm("inline_term_dm_force_like", "Inline equation: term_dm_force_like", _compute_inline_term_dm_force_like),
    "inline_mu0": PhysicsTerm("inline_mu0", "Inline equation: mu0", _compute_inline_mu0),
    "inline_M_mag": PhysicsTerm("inline_M_mag", "Inline equation: M_mag", _compute_inline_M_mag),
    "inline_l0": PhysicsTerm("inline_l0", "Inline equation: l0", _compute_inline_l0),
    "inline_exp_term": PhysicsTerm("inline_exp_term", "Inline equation: exp_term", _compute_inline_exp_term),
    "inline_cum_D": PhysicsTerm("inline_cum_D", "Inline equation: cum_D", _compute_inline_cum_D),
    "inline_f_trz": PhysicsTerm("inline_f_trz", "Inline equation: f_trz", _compute_inline_f_trz),
    "inline_r_7": PhysicsTerm("inline_r_7", "Inline equation: r", _compute_inline_r_7),
    "inline_H0": PhysicsTerm("inline_H0", "Inline equation: H0", _compute_inline_H0),
    "inline_B0_3": PhysicsTerm("inline_B0_3", "Inline equation: B0", _compute_inline_B0_3),
    "inline_tau_B": PhysicsTerm("inline_tau_B", "Inline equation: tau_B", _compute_inline_tau_B),
    "inline_Bt": PhysicsTerm("inline_Bt", "Inline equation: Bt", _compute_inline_Bt),
    "inline_Ug4": PhysicsTerm("inline_Ug4", "Inline equation: Ug4", _compute_inline_Ug4),
    "inline_term2_2": PhysicsTerm("inline_term2_2", "Inline equation: term2", _compute_inline_term2_2),
    "inline_scale_EM_1": PhysicsTerm("inline_scale_EM_1", "Inline equation: scale_EM", _compute_inline_scale_EM_1),
    "inline_corr_UA": PhysicsTerm("inline_corr_UA", "Inline equation: corr_UA", _compute_inline_corr_UA),
    "inline_p_init_1": PhysicsTerm("inline_p_init_1", "Inline equation: p_init", _compute_inline_p_init_1),
    "inline_delta_x_1": PhysicsTerm("inline_delta_x_1", "Inline equation: delta_x", _compute_inline_delta_x_1),
    "inline_delta_p_1": PhysicsTerm("inline_delta_p_1", "Inline equation: delta_p", _compute_inline_delta_p_1),
    "inline_t_Hubble_1": PhysicsTerm("inline_t_Hubble_1", "Inline equation: t_Hubble", _compute_inline_t_Hubble_1),
    "inline_rho_fluid_1": PhysicsTerm("inline_rho_fluid_1", "Inline equation: rho_fluid", _compute_inline_rho_fluid_1),
    "inline_a_1": PhysicsTerm("inline_a_1", "Inline equation: a", _compute_inline_a_1),
    "inline_term_osc2_1": PhysicsTerm("inline_term_osc2_1", "Inline equation: term_osc2", _compute_inline_term_osc2_1),
    "inline_dm_factor_1": PhysicsTerm("inline_dm_factor_1", "Inline equation: dm_factor", _compute_inline_dm_factor_1),
    "inline_f_trz_1": PhysicsTerm("inline_f_trz_1", "Inline equation: f_trz", _compute_inline_f_trz_1),
    "inline_M_initial": PhysicsTerm("inline_M_initial", "Inline equation: M_initial", _compute_inline_M_initial),
    "inline_r_8": PhysicsTerm("inline_r_8", "Inline equation: r", _compute_inline_r_8),
    "inline_B0_G": PhysicsTerm("inline_B0_G", "Inline equation: B0_G", _compute_inline_B0_G),
    "inline_tau_B_1": PhysicsTerm("inline_tau_B_1", "Inline equation: tau_B", _compute_inline_tau_B_1),
    "inline_M_dot": PhysicsTerm("inline_M_dot", "Inline equation: M_dot", _compute_inline_M_dot),
    "inline_Mt": PhysicsTerm("inline_Mt", "Inline equation: Mt", _compute_inline_Mt),
    "inline_B_G": PhysicsTerm("inline_B_G", "Inline equation: B_G", _compute_inline_B_G),
    "inline_term1_1": PhysicsTerm("inline_term1_1", "Inline equation: term1", _compute_inline_term1_1),
    "inline_spin": PhysicsTerm("inline_spin", "Inline equation: spin", _compute_inline_spin),
    "inline_M_dot_0": PhysicsTerm("inline_M_dot_0", "Inline equation: M_dot_0", _compute_inline_M_dot_0),
    "inline_tau_acc": PhysicsTerm("inline_tau_acc", "Inline equation: tau_acc", _compute_inline_tau_acc),
    "inline_omega0": PhysicsTerm("inline_omega0", "Inline equation: omega0", _compute_inline_omega0),
    "inline_gw_prefactor_1": PhysicsTerm("inline_gw_prefactor_1", "Inline equation: gw_prefactor", _compute_inline_gw_prefactor_1),
    "inline_rho_fluid_2": PhysicsTerm("inline_rho_fluid_2", "Inline equation: rho_fluid", _compute_inline_rho_fluid_2),
    "inline_a_2": PhysicsTerm("inline_a_2", "Inline equation: a", _compute_inline_a_2),
    "inline_dm_factor_2": PhysicsTerm("inline_dm_factor_2", "Inline equation: dm_factor", _compute_inline_dm_factor_2),
    "inline_sin_prec": PhysicsTerm("inline_sin_prec", "Inline equation: sin_prec", _compute_inline_sin_prec),
    "inline_pert2_1": PhysicsTerm("inline_pert2_1", "Inline equation: pert2", _compute_inline_pert2_1),
    "inline_term_dm_force_like_1": PhysicsTerm("inline_term_dm_force_like_1", "Inline equation: term_dm_force_like", _compute_inline_term_dm_force_like_1),
    "inline_g_val": PhysicsTerm("inline_g_val", "Inline equation: g_val", _compute_inline_g_val),
    "inline_mass_5": PhysicsTerm("inline_mass_5", "Inline equation: mass", _compute_inline_mass_5),
    "inline_radius_2": PhysicsTerm("inline_radius_2", "Inline equation: radius", _compute_inline_radius_2),
    "inline_hubble_1": PhysicsTerm("inline_hubble_1", "Inline equation: hubble", _compute_inline_hubble_1),
    "inline_b_field": PhysicsTerm("inline_b_field", "Inline equation: b_field", _compute_inline_b_field),
    "inline_b_crit_val": PhysicsTerm("inline_b_crit_val", "Inline equation: b_crit_val", _compute_inline_b_crit_val),
    "inline_mdot_factor": PhysicsTerm("inline_mdot_factor", "Inline equation: mdot_factor", _compute_inline_mdot_factor),
    "inline_tau_sf": PhysicsTerm("inline_tau_sf", "Inline equation: tau_sf", _compute_inline_tau_sf),
    "inline_M_dot_1": PhysicsTerm("inline_M_dot_1", "Inline equation: M_dot", _compute_inline_M_dot_1),
    "inline_ug1_t": PhysicsTerm("inline_ug1_t", "Inline equation: ug1_t", _compute_inline_ug1_t),
    "inline_corr_B": PhysicsTerm("inline_corr_B", "Inline equation: corr_B", _compute_inline_corr_B),
    "inline_lambda_val": PhysicsTerm("inline_lambda_val", "Inline equation: lambda_val", _compute_inline_lambda_val),
    "inline_c_val": PhysicsTerm("inline_c_val", "Inline equation: c_val", _compute_inline_c_val),
    "inline_f_trz_2": PhysicsTerm("inline_f_trz_2", "Inline equation: f_trz", _compute_inline_f_trz_2),
    "inline_Ug1": PhysicsTerm("inline_Ug1", "Inline equation: Ug1", _compute_inline_Ug1),
    "inline_q_val": PhysicsTerm("inline_q_val", "Inline equation: q_val", _compute_inline_q_val),
    "inline_v_gas": PhysicsTerm("inline_v_gas", "Inline equation: v_gas", _compute_inline_v_gas),
    "inline_m_proton": PhysicsTerm("inline_m_proton", "Inline equation: m_proton", _compute_inline_m_proton),
    "inline_rho_ua_1": PhysicsTerm("inline_rho_ua_1", "Inline equation: rho_ua", _compute_inline_rho_ua_1),
    "inline_rho_scm": PhysicsTerm("inline_rho_scm", "Inline equation: rho_scm", _compute_inline_rho_scm),
    "inline_em_scale": PhysicsTerm("inline_em_scale", "Inline equation: em_scale", _compute_inline_em_scale),
    "inline_em_base_1": PhysicsTerm("inline_em_base_1", "Inline equation: em_base", _compute_inline_em_base_1),
    "inline_corr_UA_1": PhysicsTerm("inline_corr_UA_1", "Inline equation: corr_UA", _compute_inline_corr_UA_1),
    "inline_h_val": PhysicsTerm("inline_h_val", "Inline equation: h_val", _compute_inline_h_val),
    "inline_dx": PhysicsTerm("inline_dx", "Inline equation: dx", _compute_inline_dx),
    "inline_dp": PhysicsTerm("inline_dp", "Inline equation: dp", _compute_inline_dp),
    "inline_psi_int": PhysicsTerm("inline_psi_int", "Inline equation: psi_int", _compute_inline_psi_int),
    "inline_t_hub_1": PhysicsTerm("inline_t_hub_1", "Inline equation: t_hub", _compute_inline_t_hub_1),
    "inline_rho_f": PhysicsTerm("inline_rho_f", "Inline equation: rho_f", _compute_inline_rho_f),
    "inline_V_3": PhysicsTerm("inline_V_3", "Inline equation: V", _compute_inline_V_3),
    "inline_amp_1": PhysicsTerm("inline_amp_1", "Inline equation: amp", _compute_inline_amp_1),
    "inline_k_val": PhysicsTerm("inline_k_val", "Inline equation: k_val", _compute_inline_k_val),
    "inline_omega_val": PhysicsTerm("inline_omega_val", "Inline equation: omega_val", _compute_inline_omega_val),
    "inline_x_val": PhysicsTerm("inline_x_val", "Inline equation: x_val", _compute_inline_x_val),
    "inline_t_hub_gyr": PhysicsTerm("inline_t_hub_gyr", "Inline equation: t_hub_gyr", _compute_inline_t_hub_gyr),
    "inline_term1_2": PhysicsTerm("inline_term1_2", "Inline equation: term1", _compute_inline_term1_2),
    "inline_arg_1": PhysicsTerm("inline_arg_1", "Inline equation: arg", _compute_inline_arg_1),
    "inline_term2_3": PhysicsTerm("inline_term2_3", "Inline equation: term2", _compute_inline_term2_3),
    "inline_m_dm_fac": PhysicsTerm("inline_m_dm_fac", "Inline equation: m_dm_fac", _compute_inline_m_dm_fac),
    "inline_delta_rho": PhysicsTerm("inline_delta_rho", "Inline equation: delta_rho", _compute_inline_delta_rho),
    "inline_pert2_2": PhysicsTerm("inline_pert2_2", "Inline equation: pert2", _compute_inline_pert2_2),
    "inline_term_dm_force_like_2": PhysicsTerm("inline_term_dm_force_like_2", "Inline equation: term_dm_force_like", _compute_inline_term_dm_force_like_2),
    "inline_rho_w": PhysicsTerm("inline_rho_w", "Inline equation: rho_w", _compute_inline_rho_w),
    "inline_v_w": PhysicsTerm("inline_v_w", "Inline equation: v_w", _compute_inline_v_w),
    "inline_e0": PhysicsTerm("inline_e0", "Inline equation: e0", _compute_inline_e0),
    "inline_tau_eros": PhysicsTerm("inline_tau_eros", "Inline equation: tau_eros", _compute_inline_tau_eros),
    "inline_Et": PhysicsTerm("inline_Et", "Inline equation: Et", _compute_inline_Et),
    "inline_mass_6": PhysicsTerm("inline_mass_6", "Inline equation: mass", _compute_inline_mass_6),
    "inline_hz_val": PhysicsTerm("inline_hz_val", "Inline equation: hz_val", _compute_inline_hz_val),
    "inline_l_fac": PhysicsTerm("inline_l_fac", "Inline equation: l_fac", _compute_inline_l_fac),
    "inline_ug1": PhysicsTerm("inline_ug1", "Inline equation: ug1", _compute_inline_ug1),
    "inline_L_t": PhysicsTerm("inline_L_t", "Inline equation: L_t", _compute_inline_L_t),
    "inline_Ug1_1": PhysicsTerm("inline_Ug1_1", "Inline equation: Ug1", _compute_inline_Ug1_1),
    "inline_pert2_3": PhysicsTerm("inline_pert2_3", "Inline equation: pert2", _compute_inline_pert2_3),
    "inline_term_dm_force_like_3": PhysicsTerm("inline_term_dm_force_like_3", "Inline equation: term_dm_force_like", _compute_inline_term_dm_force_like_3),
    "inline_M_9": PhysicsTerm("inline_M_9", "Inline equation: M", _compute_inline_M_9),
    "inline_m_bh_1": PhysicsTerm("inline_m_bh_1", "Inline equation: m_bh", _compute_inline_m_bh_1),
    "inline_r_bh_val": PhysicsTerm("inline_r_bh_val", "Inline equation: r_bh_val", _compute_inline_r_bh_val),
    "inline_m_sn0": PhysicsTerm("inline_m_sn0", "Inline equation: m_sn0", _compute_inline_m_sn0),
    "inline_tau_sn": PhysicsTerm("inline_tau_sn", "Inline equation: tau_sn", _compute_inline_tau_sn),
    "inline_M_SNt": PhysicsTerm("inline_M_SNt", "Inline equation: M_SNt", _compute_inline_M_SNt),
    "inline_mass0": PhysicsTerm("inline_mass0", "Inline equation: mass0", _compute_inline_mass0),
    "inline_Mt_1": PhysicsTerm("inline_Mt_1", "Inline equation: Mt", _compute_inline_Mt_1),
    "inline_p0": PhysicsTerm("inline_p0", "Inline equation: p0", _compute_inline_p0),
    "inline_tau_expansion": PhysicsTerm("inline_tau_expansion", "Inline equation: tau_expansion", _compute_inline_tau_expansion),
    "inline_Pt": PhysicsTerm("inline_Pt", "Inline equation: Pt", _compute_inline_Pt),
    "inline_mass_7": PhysicsTerm("inline_mass_7", "Inline equation: mass", _compute_inline_mass_7),
    "inline_lambda_1": PhysicsTerm("inline_lambda_1", "Inline equation: lambda", _compute_inline_lambda_1),
    "inline_mass_8": PhysicsTerm("inline_mass_8", "Inline equation: mass", _compute_inline_mass_8),
    "inline_charge": PhysicsTerm("inline_charge", "Inline equation: charge", _compute_inline_charge),
    "inline_em_base_2": PhysicsTerm("inline_em_base_2", "Inline equation: em_base", _compute_inline_em_base_2),
    "inline_dx_1": PhysicsTerm("inline_dx_1", "Inline equation: dx", _compute_inline_dx_1),
    "inline_mass_9": PhysicsTerm("inline_mass_9", "Inline equation: mass", _compute_inline_mass_9),
    "inline_A_1": PhysicsTerm("inline_A_1", "Inline equation: A", _compute_inline_A_1),
    "inline_term2_4": PhysicsTerm("inline_term2_4", "Inline equation: term2", _compute_inline_term2_4),
    "inline_mass_10": PhysicsTerm("inline_mass_10", "Inline equation: mass", _compute_inline_mass_10),
    "inline_term_dm_force": PhysicsTerm("inline_term_dm_force", "Inline equation: term_dm_force", _compute_inline_term_dm_force),
    "inline_rho_w_1": PhysicsTerm("inline_rho_w_1", "Inline equation: rho_w", _compute_inline_rho_w_1),
    "inline_mass_11": PhysicsTerm("inline_mass_11", "Inline equation: mass", _compute_inline_mass_11),
    "inline_mass0_1": PhysicsTerm("inline_mass0_1", "Inline equation: mass0", _compute_inline_mass0_1),
    "inline_It": PhysicsTerm("inline_It", "Inline equation: It", _compute_inline_It),
    "inline_mass0_2": PhysicsTerm("inline_mass0_2", "Inline equation: mass0", _compute_inline_mass0_2),
    "inline_charge_1": PhysicsTerm("inline_charge_1", "Inline equation: charge", _compute_inline_charge_1),
    "inline_dx_2": PhysicsTerm("inline_dx_2", "Inline equation: dx", _compute_inline_dx_2),
    "inline_mass0_3": PhysicsTerm("inline_mass0_3", "Inline equation: mass0", _compute_inline_mass0_3),
    "inline_A_2": PhysicsTerm("inline_A_2", "Inline equation: A", _compute_inline_A_2),
    "inline_mass0_4": PhysicsTerm("inline_mass0_4", "Inline equation: mass0", _compute_inline_mass0_4),
    "inline_term_dm_force_1": PhysicsTerm("inline_term_dm_force_1", "Inline equation: term_dm_force", _compute_inline_term_dm_force_1),
    "inline_rho_w_2": PhysicsTerm("inline_rho_w_2", "Inline equation: rho_w", _compute_inline_rho_w_2),
    "inline_mass0_5": PhysicsTerm("inline_mass0_5", "Inline equation: mass0", _compute_inline_mass0_5),
    "inline_mass_12": PhysicsTerm("inline_mass_12", "Inline equation: mass", _compute_inline_mass_12),
    "inline_mass_13": PhysicsTerm("inline_mass_13", "Inline equation: mass", _compute_inline_mass_13),
    "inline_charge_2": PhysicsTerm("inline_charge_2", "Inline equation: charge", _compute_inline_charge_2),
    "inline_dx_3": PhysicsTerm("inline_dx_3", "Inline equation: dx", _compute_inline_dx_3),
    "inline_mass_14": PhysicsTerm("inline_mass_14", "Inline equation: mass", _compute_inline_mass_14),
    "inline_A_3": PhysicsTerm("inline_A_3", "Inline equation: A", _compute_inline_A_3),
    "inline_mass_15": PhysicsTerm("inline_mass_15", "Inline equation: mass", _compute_inline_mass_15),
    "inline_mass_16": PhysicsTerm("inline_mass_16", "Inline equation: mass", _compute_inline_mass_16),
    "inline_mass_17": PhysicsTerm("inline_mass_17", "Inline equation: mass", _compute_inline_mass_17),
    "inline_Ft": PhysicsTerm("inline_Ft", "Inline equation: Ft", _compute_inline_Ft),
    "inline_M_bh": PhysicsTerm("inline_M_bh", "Inline equation: M_bh", _compute_inline_M_bh),
    "inline_mass_18": PhysicsTerm("inline_mass_18", "Inline equation: mass", _compute_inline_mass_18),
    "inline_charge_3": PhysicsTerm("inline_charge_3", "Inline equation: charge", _compute_inline_charge_3),
    "inline_dx_4": PhysicsTerm("inline_dx_4", "Inline equation: dx", _compute_inline_dx_4),
    "inline_mass_19": PhysicsTerm("inline_mass_19", "Inline equation: mass", _compute_inline_mass_19),
    "inline_A_4": PhysicsTerm("inline_A_4", "Inline equation: A", _compute_inline_A_4),
    "inline_mass_20": PhysicsTerm("inline_mass_20", "Inline equation: mass", _compute_inline_mass_20),
    "inline_rho_c": PhysicsTerm("inline_rho_c", "Inline equation: rho_c", _compute_inline_rho_c),
    "inline_mass_21": PhysicsTerm("inline_mass_21", "Inline equation: mass", _compute_inline_mass_21),
    "inline_mass0_6": PhysicsTerm("inline_mass0_6", "Inline equation: mass0", _compute_inline_mass0_6),
    "inline_It_1": PhysicsTerm("inline_It_1", "Inline equation: It", _compute_inline_It_1),
    "inline_mass0_7": PhysicsTerm("inline_mass0_7", "Inline equation: mass0", _compute_inline_mass0_7),
    "inline_charge_4": PhysicsTerm("inline_charge_4", "Inline equation: charge", _compute_inline_charge_4),
    "inline_mass0_8": PhysicsTerm("inline_mass0_8", "Inline equation: mass0", _compute_inline_mass0_8),
    "inline_A_5": PhysicsTerm("inline_A_5", "Inline equation: A", _compute_inline_A_5),
    "inline_mass0_9": PhysicsTerm("inline_mass0_9", "Inline equation: mass0", _compute_inline_mass0_9),
    "inline_rho_w_3": PhysicsTerm("inline_rho_w_3", "Inline equation: rho_w", _compute_inline_rho_w_3),
    "inline_mass0_10": PhysicsTerm("inline_mass0_10", "Inline equation: mass0", _compute_inline_mass0_10),
    "inline_mass0_11": PhysicsTerm("inline_mass0_11", "Inline equation: mass0", _compute_inline_mass0_11),
    "inline_mass0_12": PhysicsTerm("inline_mass0_12", "Inline equation: mass0", _compute_inline_mass0_12),
    "inline_charge_5": PhysicsTerm("inline_charge_5", "Inline equation: charge", _compute_inline_charge_5),
    "inline_mass0_13": PhysicsTerm("inline_mass0_13", "Inline equation: mass0", _compute_inline_mass0_13),
    "inline_A_6": PhysicsTerm("inline_A_6", "Inline equation: A", _compute_inline_A_6),
    "inline_mass0_14": PhysicsTerm("inline_mass0_14", "Inline equation: mass0", _compute_inline_mass0_14),
    "inline_mass_22": PhysicsTerm("inline_mass_22", "Inline equation: mass", _compute_inline_mass_22),
    "inline_M_bh_1": PhysicsTerm("inline_M_bh_1", "Inline equation: M_bh", _compute_inline_M_bh_1),
    "inline_mass_23": PhysicsTerm("inline_mass_23", "Inline equation: mass", _compute_inline_mass_23),
    "inline_charge_6": PhysicsTerm("inline_charge_6", "Inline equation: charge", _compute_inline_charge_6),
    "inline_dx_5": PhysicsTerm("inline_dx_5", "Inline equation: dx", _compute_inline_dx_5),
    "inline_mass_24": PhysicsTerm("inline_mass_24", "Inline equation: mass", _compute_inline_mass_24),
    "inline_A_7": PhysicsTerm("inline_A_7", "Inline equation: A", _compute_inline_A_7),
    "inline_mass_25": PhysicsTerm("inline_mass_25", "Inline equation: mass", _compute_inline_mass_25),
    "inline_mass_26": PhysicsTerm("inline_mass_26", "Inline equation: mass", _compute_inline_mass_26),
    "inline_M_bh_2": PhysicsTerm("inline_M_bh_2", "Inline equation: M_bh", _compute_inline_M_bh_2),
    "inline_lambda_2": PhysicsTerm("inline_lambda_2", "Inline equation: lambda", _compute_inline_lambda_2),
    "inline_mass_27": PhysicsTerm("inline_mass_27", "Inline equation: mass", _compute_inline_mass_27),
    "inline_charge_7": PhysicsTerm("inline_charge_7", "Inline equation: charge", _compute_inline_charge_7),
    "inline_dx_6": PhysicsTerm("inline_dx_6", "Inline equation: dx", _compute_inline_dx_6),
    "inline_mass_28": PhysicsTerm("inline_mass_28", "Inline equation: mass", _compute_inline_mass_28),
    "inline_A_8": PhysicsTerm("inline_A_8", "Inline equation: A", _compute_inline_A_8),
    "inline_mass_29": PhysicsTerm("inline_mass_29", "Inline equation: mass", _compute_inline_mass_29),
    "inline_drag": PhysicsTerm("inline_drag", "Inline equation: drag", _compute_inline_drag),
    "inline_mass_30": PhysicsTerm("inline_mass_30", "Inline equation: mass", _compute_inline_mass_30),
    "inline_g_sun": PhysicsTerm("inline_g_sun", "Inline equation: g_sun", _compute_inline_g_sun),
    "inline_mass_31": PhysicsTerm("inline_mass_31", "Inline equation: mass", _compute_inline_mass_31),
    "inline_charge_8": PhysicsTerm("inline_charge_8", "Inline equation: charge", _compute_inline_charge_8),
    "inline_dx_7": PhysicsTerm("inline_dx_7", "Inline equation: dx", _compute_inline_dx_7),
    "inline_mass_32": PhysicsTerm("inline_mass_32", "Inline equation: mass", _compute_inline_mass_32),
    "inline_A_9": PhysicsTerm("inline_A_9", "Inline equation: A", _compute_inline_A_9),
    "inline_mass_33": PhysicsTerm("inline_mass_33", "Inline equation: mass", _compute_inline_mass_33),
    "inline_M_r": PhysicsTerm("inline_M_r", "Inline equation: M_r", _compute_inline_M_r),
    "inline_v_1": PhysicsTerm("inline_v_1", "Inline equation: v", _compute_inline_v_1),
    "inline_mass_34": PhysicsTerm("inline_mass_34", "Inline equation: mass", _compute_inline_mass_34),
    "inline_mass_35": PhysicsTerm("inline_mass_35", "Inline equation: mass", _compute_inline_mass_35),
    "inline_charge_9": PhysicsTerm("inline_charge_9", "Inline equation: charge", _compute_inline_charge_9),
    "inline_dx_8": PhysicsTerm("inline_dx_8", "Inline equation: dx", _compute_inline_dx_8),
    "inline_mass_36": PhysicsTerm("inline_mass_36", "Inline equation: mass", _compute_inline_mass_36),
    "inline_A_10": PhysicsTerm("inline_A_10", "Inline equation: A", _compute_inline_A_10),
    "inline_mass_37": PhysicsTerm("inline_mass_37", "Inline equation: mass", _compute_inline_mass_37),
    "inline_pert2_4": PhysicsTerm("inline_pert2_4", "Inline equation: pert2", _compute_inline_pert2_4),
    "inline_term_dm_force_2": PhysicsTerm("inline_term_dm_force_2", "Inline equation: term_dm_force", _compute_inline_term_dm_force_2),
    "inline_sfr": PhysicsTerm("inline_sfr", "Inline equation: sfr", _compute_inline_sfr),
    "inline_E_0": PhysicsTerm("inline_E_0", "Inline equation: E_0", _compute_inline_E_0),
    "inline_mass_38": PhysicsTerm("inline_mass_38", "Inline equation: mass", _compute_inline_mass_38),
    "inline_g_base": PhysicsTerm("inline_g_base", "Inline equation: g_base", _compute_inline_g_base),
    "inline_mass_39": PhysicsTerm("inline_mass_39", "Inline equation: mass", _compute_inline_mass_39),
    "inline_Ug1_2": PhysicsTerm("inline_Ug1_2", "Inline equation: Ug1", _compute_inline_Ug1_2),
    "inline_charge_10": PhysicsTerm("inline_charge_10", "Inline equation: charge", _compute_inline_charge_10),
    "inline_em_base_3": PhysicsTerm("inline_em_base_3", "Inline equation: em_base", _compute_inline_em_base_3),
    "inline_dx_9": PhysicsTerm("inline_dx_9", "Inline equation: dx", _compute_inline_dx_9),
    "inline_mass_40": PhysicsTerm("inline_mass_40", "Inline equation: mass", _compute_inline_mass_40),
    "inline_V_4": PhysicsTerm("inline_V_4", "Inline equation: V", _compute_inline_V_4),
    "inline_A_11": PhysicsTerm("inline_A_11", "Inline equation: A", _compute_inline_A_11),
    "inline_mass_41": PhysicsTerm("inline_mass_41", "Inline equation: mass", _compute_inline_mass_41),
    "inline_pert2_5": PhysicsTerm("inline_pert2_5", "Inline equation: pert2", _compute_inline_pert2_5),
    "inline_P": PhysicsTerm("inline_P", "Inline equation: P", _compute_inline_P),
    "inline_wind_pressure": PhysicsTerm("inline_wind_pressure", "Inline equation: wind_pressure", _compute_inline_wind_pressure),
    "inline_rel_corr": PhysicsTerm("inline_rel_corr", "Inline equation: rel_corr", _compute_inline_rel_corr),
    "inline_a_wind": PhysicsTerm("inline_a_wind", "Inline equation: a_wind", _compute_inline_a_wind),
    "inline_charge_11": PhysicsTerm("inline_charge_11", "Inline equation: charge", _compute_inline_charge_11),
    "inline_mass_42": PhysicsTerm("inline_mass_42", "Inline equation: mass", _compute_inline_mass_42),
    "inline_mass_43": PhysicsTerm("inline_mass_43", "Inline equation: mass", _compute_inline_mass_43),
    "inline_charge_12": PhysicsTerm("inline_charge_12", "Inline equation: charge", _compute_inline_charge_12),
    "inline_mass_44": PhysicsTerm("inline_mass_44", "Inline equation: mass", _compute_inline_mass_44),
    "inline_A_12": PhysicsTerm("inline_A_12", "Inline equation: A", _compute_inline_A_12),
    "inline_mass_45": PhysicsTerm("inline_mass_45", "Inline equation: mass", _compute_inline_mass_45),
    "inline_current": PhysicsTerm("inline_current", "Inline equation: current", _compute_inline_current),
    "inline_f": PhysicsTerm("inline_f", "Inline equation: f", _compute_inline_f),
    "inline_E_0_1": PhysicsTerm("inline_E_0_1", "Inline equation: E_0", _compute_inline_E_0_1),
    "inline_planck": PhysicsTerm("inline_planck", "Inline equation: planck", _compute_inline_planck),
    "inline_f_a_1": PhysicsTerm("inline_f_a_1", "Inline equation: f_a", _compute_inline_f_a_1),
    "inline_f_s": PhysicsTerm("inline_f_s", "Inline equation: f_s", _compute_inline_f_s),
    "inline_f_q_1": PhysicsTerm("inline_f_q_1", "Inline equation: f_q", _compute_inline_f_q_1),
    "inline_f_A": PhysicsTerm("inline_f_A", "Inline equation: f_A", _compute_inline_f_A),
    "inline_f_f": PhysicsTerm("inline_f_f", "Inline equation: f_f", _compute_inline_f_f),
    "inline_f_e": PhysicsTerm("inline_f_e", "Inline equation: f_e", _compute_inline_f_e),
    "inline_current_1": PhysicsTerm("inline_current_1", "Inline equation: current", _compute_inline_current_1),
    "inline_f_1": PhysicsTerm("inline_f_1", "Inline equation: f", _compute_inline_f_1),
    "inline_E_0_2": PhysicsTerm("inline_E_0_2", "Inline equation: E_0", _compute_inline_E_0_2),
    "inline_planck_1": PhysicsTerm("inline_planck_1", "Inline equation: planck", _compute_inline_planck_1),
    "inline_f_a_2": PhysicsTerm("inline_f_a_2", "Inline equation: f_a", _compute_inline_f_a_2),
    "inline_f_s_1": PhysicsTerm("inline_f_s_1", "Inline equation: f_s", _compute_inline_f_s_1),
    "inline_f_q_2": PhysicsTerm("inline_f_q_2", "Inline equation: f_q", _compute_inline_f_q_2),
    "inline_f_A_1": PhysicsTerm("inline_f_A_1", "Inline equation: f_A", _compute_inline_f_A_1),
    "inline_f_f_1": PhysicsTerm("inline_f_f_1", "Inline equation: f_f", _compute_inline_f_f_1),
    "inline_f_e_1": PhysicsTerm("inline_f_e_1", "Inline equation: f_e", _compute_inline_f_e_1),
    "inline_M_10": PhysicsTerm("inline_M_10", "Inline equation: M", _compute_inline_M_10),
    "inline_r_9": PhysicsTerm("inline_r_9", "Inline equation: r", _compute_inline_r_9),
    "inline_SFR": PhysicsTerm("inline_SFR", "Inline equation: SFR", _compute_inline_SFR),
    "inline_expansion": PhysicsTerm("inline_expansion", "Inline equation: expansion", _compute_inline_expansion),
    "inline_B_4": PhysicsTerm("inline_B_4", "Inline equation: B", _compute_inline_B_4),
    "inline_v_out": PhysicsTerm("inline_v_out", "Inline equation: v_out", _compute_inline_v_out),
    "inline_Ug2": PhysicsTerm("inline_Ug2", "Inline equation: Ug2", _compute_inline_Ug2),
    "inline_vac_ratio": PhysicsTerm("inline_vac_ratio", "Inline equation: vac_ratio", _compute_inline_vac_ratio),
    "inline_unc": PhysicsTerm("inline_unc", "Inline equation: unc", _compute_inline_unc),
    "inline_rho_fluid_3": PhysicsTerm("inline_rho_fluid_3", "Inline equation: rho_fluid", _compute_inline_rho_fluid_3),
    "inline_x_1": PhysicsTerm("inline_x_1", "Inline equation: x", _compute_inline_x_1),
    "inline_omega_3": PhysicsTerm("inline_omega_3", "Inline equation: omega", _compute_inline_omega_3),
    "inline_standing": PhysicsTerm("inline_standing", "Inline equation: standing", _compute_inline_standing),
    "inline_traveling_real": PhysicsTerm("inline_traveling_real", "Inline equation: traveling_real", _compute_inline_traveling_real),
    "inline_t_evolve": PhysicsTerm("inline_t_evolve", "Inline equation: t_evolve", _compute_inline_t_evolve),
    "inline_M_total": PhysicsTerm("inline_M_total", "Inline equation: M_total", _compute_inline_M_total),
    "inline_M_t": PhysicsTerm("inline_M_t", "Inline equation: M_t", _compute_inline_M_t),
    "inline_z_t": PhysicsTerm("inline_z_t", "Inline equation: z_t", _compute_inline_z_t),
    "inline_H0_1": PhysicsTerm("inline_H0_1", "Inline equation: H0", _compute_inline_H0_1),
    "inline_Hz_1": PhysicsTerm("inline_Hz_1", "Inline equation: Hz", _compute_inline_Hz_1),
    "inline_Ug1_3": PhysicsTerm("inline_Ug1_3", "Inline equation: Ug1", _compute_inline_Ug1_3),
    "inline_g_base_1": PhysicsTerm("inline_g_base_1", "Inline equation: g_base", _compute_inline_g_base_1),
    "inline_l_p": PhysicsTerm("inline_l_p", "Inline equation: l_p", _compute_inline_l_p),
    "inline_h_strain": PhysicsTerm("inline_h_strain", "Inline equation: h_strain", _compute_inline_h_strain),
    "inline_lambda_gw": PhysicsTerm("inline_lambda_gw", "Inline equation: lambda_gw", _compute_inline_lambda_gw),
    "inline_phase_1": PhysicsTerm("inline_phase_1", "Inline equation: phase", _compute_inline_phase_1),
    "inline_M_11": PhysicsTerm("inline_M_11", "Inline equation: M", _compute_inline_M_11),
    "inline_r_10": PhysicsTerm("inline_r_10", "Inline equation: r", _compute_inline_r_10),
    "inline_SFR_1": PhysicsTerm("inline_SFR_1", "Inline equation: SFR", _compute_inline_SFR_1),
    "inline_B_5": PhysicsTerm("inline_B_5", "Inline equation: B", _compute_inline_B_5),
    "inline_M_NGC5195": PhysicsTerm("inline_M_NGC5195", "Inline equation: M_NGC5195", _compute_inline_M_NGC5195),
    "inline_d_NGC5195": PhysicsTerm("inline_d_NGC5195", "Inline equation: d_NGC5195", _compute_inline_d_NGC5195),
    "inline_F_tidal": PhysicsTerm("inline_F_tidal", "Inline equation: F_tidal", _compute_inline_F_tidal),
    "inline_F_SF": PhysicsTerm("inline_F_SF", "Inline equation: F_SF", _compute_inline_F_SF),
    "inline_M_BH": PhysicsTerm("inline_M_BH", "Inline equation: M_BH", _compute_inline_M_BH),
    "inline_r_BH": PhysicsTerm("inline_r_BH", "Inline equation: r_BH", _compute_inline_r_BH),
    "inline_I_dipole": PhysicsTerm("inline_I_dipole", "Inline equation: I_dipole", _compute_inline_I_dipole),
    "inline_A_dipole": PhysicsTerm("inline_A_dipole", "Inline equation: A_dipole", _compute_inline_A_dipole),
    "inline_omega_spin": PhysicsTerm("inline_omega_spin", "Inline equation: omega_spin", _compute_inline_omega_spin),
    "inline_d_1": PhysicsTerm("inline_d_1", "Inline equation: d", _compute_inline_d_1),
    "inline_E_react": PhysicsTerm("inline_E_react", "Inline equation: E_react", _compute_inline_E_react),
    "inline_omega_spiral": PhysicsTerm("inline_omega_spiral", "Inline equation: omega_spiral", _compute_inline_omega_spiral),
    "inline_integral_psi_1": PhysicsTerm("inline_integral_psi_1", "Inline equation: integral_psi", _compute_inline_integral_psi_1),
    "inline_curvature": PhysicsTerm("inline_curvature", "Inline equation: curvature", _compute_inline_curvature),
    "inline_M_12": PhysicsTerm("inline_M_12", "Inline equation: M", _compute_inline_M_12),
    "inline_tau_merge": PhysicsTerm("inline_tau_merge", "Inline equation: tau_merge", _compute_inline_tau_merge),
    "inline_M_merge": PhysicsTerm("inline_M_merge", "Inline equation: M_merge", _compute_inline_M_merge),
    "inline_m_factor": PhysicsTerm("inline_m_factor", "Inline equation: m_factor", _compute_inline_m_factor),
    "inline_M_BH_1": PhysicsTerm("inline_M_BH_1", "Inline equation: M_BH", _compute_inline_M_BH_1),
    "inline_omega_spin_1": PhysicsTerm("inline_omega_spin_1", "Inline equation: omega_spin", _compute_inline_omega_spin_1),
    "inline_M_spiral": PhysicsTerm("inline_M_spiral", "Inline equation: M_spiral", _compute_inline_M_spiral),
    "inline_d_2": PhysicsTerm("inline_d_2", "Inline equation: d", _compute_inline_d_2),
    "inline_psi_dust_sq": PhysicsTerm("inline_psi_dust_sq", "Inline equation: psi_dust_sq", _compute_inline_psi_dust_sq),
    "inline_rho_dust": PhysicsTerm("inline_rho_dust", "Inline equation: rho_dust", _compute_inline_rho_dust),
    "inline_d_spiral": PhysicsTerm("inline_d_spiral", "Inline equation: d_spiral", _compute_inline_d_spiral),
    "inline_F_tidal_1": PhysicsTerm("inline_F_tidal_1", "Inline equation: F_tidal", _compute_inline_F_tidal_1),
    "inline_F_cluster": PhysicsTerm("inline_F_cluster", "Inline equation: F_cluster", _compute_inline_F_cluster),
    "inline_f_DPM": PhysicsTerm("inline_f_DPM", "Inline equation: f_DPM", _compute_inline_f_DPM),
    "inline_rho_vac_plasm": PhysicsTerm("inline_rho_vac_plasm", "Inline equation: rho_vac_plasm", _compute_inline_rho_vac_plasm),
    "inline_f_THz": PhysicsTerm("inline_f_THz", "Inline equation: f_THz", _compute_inline_f_THz),
    "inline_f_react": PhysicsTerm("inline_f_react", "Inline equation: f_react", _compute_inline_f_react),
    "inline_f_quantum": PhysicsTerm("inline_f_quantum", "Inline equation: f_quantum", _compute_inline_f_quantum),
    "inline_unc_1": PhysicsTerm("inline_unc_1", "Inline equation: unc", _compute_inline_unc_1),
    "inline_f_fluid": PhysicsTerm("inline_f_fluid", "Inline equation: f_fluid", _compute_inline_f_fluid),
    "inline_rho_3": PhysicsTerm("inline_rho_3", "Inline equation: rho", _compute_inline_rho_3),
    "inline_f_super": PhysicsTerm("inline_f_super", "Inline equation: f_super", _compute_inline_f_super),
    "inline_t_coal": PhysicsTerm("inline_t_coal", "Inline equation: t_coal", _compute_inline_t_coal),
    "inline_r_11": PhysicsTerm("inline_r_11", "Inline equation: r", _compute_inline_r_11),
    "inline_omega_4": PhysicsTerm("inline_omega_4", "Inline equation: omega", _compute_inline_omega_4),
    "inline_f_Aether": PhysicsTerm("inline_f_Aether", "Inline equation: f_Aether", _compute_inline_f_Aether),
    "inline_M1": PhysicsTerm("inline_M1", "Inline equation: M1", _compute_inline_M1),
    "inline_M2": PhysicsTerm("inline_M2", "Inline equation: M2", _compute_inline_M2),
    "inline_M_chirp": PhysicsTerm("inline_M_chirp", "Inline equation: M_chirp", _compute_inline_M_chirp),
    "inline_eta": PhysicsTerm("inline_eta", "Inline equation: eta", _compute_inline_eta),
    "inline_T_s": PhysicsTerm("inline_T_s", "Inline equation: T_s", _compute_inline_T_s),
    "inline_f_Heaviside": PhysicsTerm("inline_f_Heaviside", "Inline equation: f_Heaviside", _compute_inline_f_Heaviside),
    "inline_scale_1": PhysicsTerm("inline_scale_1", "Inline equation: scale", _compute_inline_scale_1),
    "inline_mu_j": PhysicsTerm("inline_mu_j", "Inline equation: mu_j", _compute_inline_mu_j),
    "inline_r_j": PhysicsTerm("inline_r_j", "Inline equation: r_j", _compute_inline_r_j),
    "inline_gamma_1": PhysicsTerm("inline_gamma_1", "Inline equation: gamma", _compute_inline_gamma_1),
    "inline_t_n": PhysicsTerm("inline_t_n", "Inline equation: t_n", _compute_inline_t_n),
    "inline_phi_hat_j": PhysicsTerm("inline_phi_hat_j", "Inline equation: phi_hat_j", _compute_inline_phi_hat_j),
    "inline_P_SCm": PhysicsTerm("inline_P_SCm", "Inline equation: P_SCm", _compute_inline_P_SCm),
    "inline_E_react_1": PhysicsTerm("inline_E_react_1", "Inline equation: E_react", _compute_inline_E_react_1),
    "inline_exp_arg": PhysicsTerm("inline_exp_arg", "Inline equation: exp_arg", _compute_inline_exp_arg),
    "inline_one_minus_exp": PhysicsTerm("inline_one_minus_exp", "Inline equation: one_minus_exp", _compute_inline_one_minus_exp),
    "inline_base_1": PhysicsTerm("inline_base_1", "Inline equation: base", _compute_inline_base_1),
    "inline_heaviside_factor": PhysicsTerm("inline_heaviside_factor", "Inline equation: heaviside_factor", _compute_inline_heaviside_factor),
    "inline_f_quasi": PhysicsTerm("inline_f_quasi", "Inline equation: f_quasi", _compute_inline_f_quasi),
    "inline_k_A": PhysicsTerm("inline_k_A", "Inline equation: k_A", _compute_inline_k_A),
    "inline_Z": PhysicsTerm("inline_Z", "Inline equation: Z", _compute_inline_Z),
    "inline_A_13": PhysicsTerm("inline_A_13", "Inline equation: A", _compute_inline_A_13),
    "inline_A_H": PhysicsTerm("inline_A_H", "Inline equation: A_H", _compute_inline_A_H),
    "inline_delta_pair": PhysicsTerm("inline_delta_pair", "Inline equation: delta_pair", _compute_inline_delta_pair),
    "inline_A_res": PhysicsTerm("inline_A_res", "Inline equation: A_res", _compute_inline_A_res),
    "inline_E_bind": PhysicsTerm("inline_E_bind", "Inline equation: E_bind", _compute_inline_E_bind),
    "inline_h": PhysicsTerm("inline_h", "Inline equation: h", _compute_inline_h),
    "inline_f_res": PhysicsTerm("inline_f_res", "Inline equation: f_res", _compute_inline_f_res),
    "inline_k_7": PhysicsTerm("inline_k_7", "Inline equation: k", _compute_inline_k_7),
    "inline_A1": PhysicsTerm("inline_A1", "Inline equation: A1", _compute_inline_A1),
    "inline_A2": PhysicsTerm("inline_A2", "Inline equation: A2", _compute_inline_A2),
    "inline_f_dp": PhysicsTerm("inline_f_dp", "Inline equation: f_dp", _compute_inline_f_dp),
    "inline_phi_dp": PhysicsTerm("inline_phi_dp", "Inline equation: phi_dp", _compute_inline_phi_dp),
    "inline_U_dp": PhysicsTerm("inline_U_dp", "Inline equation: U_dp", _compute_inline_U_dp),
    "inline_k_0": PhysicsTerm("inline_k_0", "Inline equation: k_0", _compute_inline_k_0),
    "inline_N": PhysicsTerm("inline_N", "Inline equation: N", _compute_inline_N),
    "inline_k_nuc": PhysicsTerm("inline_k_nuc", "Inline equation: k_nuc", _compute_inline_k_nuc),
    "inline_S_scale": PhysicsTerm("inline_S_scale", "Inline equation: S_scale", _compute_inline_S_scale),
    "inline_S_shell": PhysicsTerm("inline_S_shell", "Inline equation: S_shell", _compute_inline_S_shell),
    "inline_S_shell_1": PhysicsTerm("inline_S_shell_1", "Inline equation: S_shell", _compute_inline_S_shell_1),
    "inline_r_nuc": PhysicsTerm("inline_r_nuc", "Inline equation: r_nuc", _compute_inline_r_nuc),
    "inline_volume": PhysicsTerm("inline_volume", "Inline equation: volume", _compute_inline_volume),
    "inline_binding_contrib": PhysicsTerm("inline_binding_contrib", "Inline equation: binding_contrib", _compute_inline_binding_contrib),
    "inline_range_factor": PhysicsTerm("inline_range_factor", "Inline equation: range_factor", _compute_inline_range_factor),
    "inline_buoyancy_2": PhysicsTerm("inline_buoyancy_2", "Inline equation: buoyancy", _compute_inline_buoyancy_2),
    "inline_SC_m": PhysicsTerm("inline_SC_m", "Inline equation: SC_m", _compute_inline_SC_m),
    "inline_U_dp_1": PhysicsTerm("inline_U_dp_1", "Inline equation: U_dp", _compute_inline_U_dp_1),
    "inline_phase_2": PhysicsTerm("inline_phase_2", "Inline equation: phase", _compute_inline_phase_2),
    "inline_tunneling": PhysicsTerm("inline_tunneling", "Inline equation: tunneling", _compute_inline_tunneling),
    "inline_SC_enhanced": PhysicsTerm("inline_SC_enhanced", "Inline equation: SC_enhanced", _compute_inline_SC_enhanced),
    "inline_tunneling_prob": PhysicsTerm("inline_tunneling_prob", "Inline equation: tunneling_prob", _compute_inline_tunneling_prob),
    "inline_A_res_1": PhysicsTerm("inline_A_res_1", "Inline equation: A_res", _compute_inline_A_res_1),
    "inline_f_res_1": PhysicsTerm("inline_f_res_1", "Inline equation: f_res", _compute_inline_f_res_1),
    "inline_k_nuc_1": PhysicsTerm("inline_k_nuc_1", "Inline equation: k_nuc", _compute_inline_k_nuc_1),
    "inline_integrand_2": PhysicsTerm("inline_integrand_2", "Inline equation: integrand", _compute_inline_integrand_2),
    "inline_integrand_3": PhysicsTerm("inline_integrand_3", "Inline equation: integrand", _compute_inline_integrand_3),
    "inline_x2_1": PhysicsTerm("inline_x2_1", "Inline equation: x2", _compute_inline_x2_1),
    "inline_B_s": PhysicsTerm("inline_B_s", "Inline equation: B_s", _compute_inline_B_s),
    "inline_B_ref": PhysicsTerm("inline_B_ref", "Inline equation: B_ref", _compute_inline_B_ref),
    "inline_k_3_1": PhysicsTerm("inline_k_3_1", "Inline equation: k_3", _compute_inline_k_3_1),
    "inline_omega_s_1": PhysicsTerm("inline_omega_s_1", "Inline equation: omega_s", _compute_inline_omega_s_1),
    "inline_B_j": PhysicsTerm("inline_B_j", "Inline equation: B_j", _compute_inline_B_j),
    "inline_B_s_min": PhysicsTerm("inline_B_s_min", "Inline equation: B_s_min", _compute_inline_B_s_min),
    "inline_B_s_max": PhysicsTerm("inline_B_s_max", "Inline equation: B_s_max", _compute_inline_B_s_max),
    "inline_solar_cycle_period": PhysicsTerm("inline_solar_cycle_period", "Inline equation: solar_cycle_period", _compute_inline_solar_cycle_period),
    "inline_cycle_phase": PhysicsTerm("inline_cycle_phase", "Inline equation: cycle_phase", _compute_inline_cycle_phase),
    "inline_cycle_factor": PhysicsTerm("inline_cycle_factor", "Inline equation: cycle_factor", _compute_inline_cycle_factor),
    "inline_B_ref_1": PhysicsTerm("inline_B_ref_1", "Inline equation: B_ref", _compute_inline_B_ref_1),
    "inline_mean_x": PhysicsTerm("inline_mean_x", "Inline equation: mean_x", _compute_inline_mean_x),
    "inline_mean_y": PhysicsTerm("inline_mean_y", "Inline equation: mean_y", _compute_inline_mean_y),
    "inline_numerator": PhysicsTerm("inline_numerator", "Inline equation: numerator", _compute_inline_numerator),
    "inline_SCm_i": PhysicsTerm("inline_SCm_i", "Inline equation: SCm_i", _compute_inline_SCm_i),
    "inline_E_DPM_i": PhysicsTerm("inline_E_DPM_i", "Inline equation: E_DPM_i", _compute_inline_E_DPM_i),
    "inline_life_proportion": PhysicsTerm("inline_life_proportion", "Inline equation: life_proportion", _compute_inline_life_proportion),
    "inline_modulated": PhysicsTerm("inline_modulated", "Inline equation: modulated", _compute_inline_modulated),
    "inline_F_LENR": PhysicsTerm("inline_F_LENR", "Inline equation: F_LENR", _compute_inline_F_LENR),
    "inline_F_act": PhysicsTerm("inline_F_act", "Inline equation: F_act", _compute_inline_F_act),
    "inline_F_DE": PhysicsTerm("inline_F_DE", "Inline equation: F_DE", _compute_inline_F_DE),
    "inline_F_neutron": PhysicsTerm("inline_F_neutron", "Inline equation: F_neutron", _compute_inline_F_neutron),
    "inline_Delta_rho_vac": PhysicsTerm("inline_Delta_rho_vac", "Inline equation: Delta_rho_vac", _compute_inline_Delta_rho_vac),
    "inline_F_vac_rep_1": PhysicsTerm("inline_F_vac_rep_1", "Inline equation: F_vac_rep", _compute_inline_F_vac_rep_1),
    "inline_F_thz_shock_1": PhysicsTerm("inline_F_thz_shock_1", "Inline equation: F_thz_shock", _compute_inline_F_thz_shock_1),
    "inline_F_conduit_1": PhysicsTerm("inline_F_conduit_1", "Inline equation: F_conduit", _compute_inline_F_conduit_1),
    "inline_F_spooky_1": PhysicsTerm("inline_F_spooky_1", "Inline equation: F_spooky", _compute_inline_F_spooky_1),
    "inline_integrand_4": PhysicsTerm("inline_integrand_4", "Inline equation: integrand", _compute_inline_integrand_4),
    "inline_c_quad": PhysicsTerm("inline_c_quad", "Inline equation: c_quad", _compute_inline_c_quad),
    "inline_discriminant": PhysicsTerm("inline_discriminant", "Inline equation: discriminant", _compute_inline_discriminant),
    "inline_x_2": PhysicsTerm("inline_x_2", "Inline equation: x_2", _compute_inline_x_2),
    "inline_f_i": PhysicsTerm("inline_f_i", "Inline equation: f_i", _compute_inline_f_i),
    "inline_Ug1_i": PhysicsTerm("inline_Ug1_i", "Inline equation: Ug1_i", _compute_inline_Ug1_i),
    "inline_Ug2_i": PhysicsTerm("inline_Ug2_i", "Inline equation: Ug2_i", _compute_inline_Ug2_i),
    "inline_Ug3_i": PhysicsTerm("inline_Ug3_i", "Inline equation: Ug3_i", _compute_inline_Ug3_i),
    "inline_Ug4_i": PhysicsTerm("inline_Ug4_i", "Inline equation: Ug4_i", _compute_inline_Ug4_i),
    "inline_g_layer": PhysicsTerm("inline_g_layer", "Inline equation: g_layer", _compute_inline_g_layer),
    "inline_gamma_2": PhysicsTerm("inline_gamma_2", "Inline equation: gamma", _compute_inline_gamma_2),
    "inline_alpha_g": PhysicsTerm("inline_alpha_g", "Inline equation: alpha_g", _compute_inline_alpha_g),
    "inline_error": PhysicsTerm("inline_error", "Inline equation: error", _compute_inline_error),
    "inline_mutationRate": PhysicsTerm("inline_mutationRate", "Inline equation: mutationRate", _compute_inline_mutationRate),
    "inline_error_1": PhysicsTerm("inline_error_1", "Inline equation: error", _compute_inline_error_1),
    "inline_adjustment": PhysicsTerm("inline_adjustment", "Inline equation: adjustment", _compute_inline_adjustment),
    "inline_M_13": PhysicsTerm("inline_M_13", "Inline equation: M", _compute_inline_M_13),
    "inline_r_12": PhysicsTerm("inline_r_12", "Inline equation: r", _compute_inline_r_12),
    "inline_B0_4": PhysicsTerm("inline_B0_4", "Inline equation: B0", _compute_inline_B0_4),
    "inline_x_2_1": PhysicsTerm("inline_x_2_1", "Inline equation: x_2", _compute_inline_x_2_1),
    "inline_freq_1": PhysicsTerm("inline_freq_1", "Inline equation: freq", _compute_inline_freq_1),
    "inline_amp_2": PhysicsTerm("inline_amp_2", "Inline equation: amp", _compute_inline_amp_2),
    "inline_v_2": PhysicsTerm("inline_v_2", "Inline equation: v", _compute_inline_v_2),
    "inline_gamma_3": PhysicsTerm("inline_gamma_3", "Inline equation: gamma", _compute_inline_gamma_3),
    "inline_Ms": PhysicsTerm("inline_Ms", "Inline equation: Ms", _compute_inline_Ms),
    "inline_Rs": PhysicsTerm("inline_Rs", "Inline equation: Rs", _compute_inline_Rs),
    "inline_Bs_avg": PhysicsTerm("inline_Bs_avg", "Inline equation: Bs_avg", _compute_inline_Bs_avg),
    "inline_omega_c": PhysicsTerm("inline_omega_c", "Inline equation: omega_c", _compute_inline_omega_c),
    "inline_r_13": PhysicsTerm("inline_r_13", "Inline equation: r", _compute_inline_r_13),
    "inline_k1_1": PhysicsTerm("inline_k1_1", "Inline equation: k1", _compute_inline_k1_1),
    "inline_alpha_1": PhysicsTerm("inline_alpha_1", "Inline equation: alpha", _compute_inline_alpha_1),
    "inline_delta_def": PhysicsTerm("inline_delta_def", "Inline equation: delta_def", _compute_inline_delta_def),
    "inline_Bs_t_2": PhysicsTerm("inline_Bs_t_2", "Inline equation: Bs_t", _compute_inline_Bs_t_2),
    "inline_mu_s": PhysicsTerm("inline_mu_s", "Inline equation: mu_s", _compute_inline_mu_s),
    "inline_grad_Ms_r": PhysicsTerm("inline_grad_Ms_r", "Inline equation: grad_Ms_r", _compute_inline_grad_Ms_r),
    "inline_defect_1": PhysicsTerm("inline_defect_1", "Inline equation: defect", _compute_inline_defect_1),
    "inline_Rb": PhysicsTerm("inline_Rb", "Inline equation: Rb", _compute_inline_Rb),
    "inline_QA": PhysicsTerm("inline_QA", "Inline equation: QA", _compute_inline_QA),
    "inline_QUA": PhysicsTerm("inline_QUA", "Inline equation: QUA", _compute_inline_QUA),
    "inline_k2": PhysicsTerm("inline_k2", "Inline equation: k2", _compute_inline_k2),
    "inline_v_sw": PhysicsTerm("inline_v_sw", "Inline equation: v_sw", _compute_inline_v_sw),
    "inline_delta_sw": PhysicsTerm("inline_delta_sw", "Inline equation: delta_sw", _compute_inline_delta_sw),
    "inline_HSCm": PhysicsTerm("inline_HSCm", "Inline equation: HSCm", _compute_inline_HSCm),
    "inline_rho_SCm": PhysicsTerm("inline_rho_SCm", "Inline equation: rho_SCm", _compute_inline_rho_SCm),
    "inline_v_SCm": PhysicsTerm("inline_v_SCm", "Inline equation: v_SCm", _compute_inline_v_SCm),
    "inline_rho_A": PhysicsTerm("inline_rho_A", "Inline equation: rho_A", _compute_inline_rho_A),
    "inline_kappa": PhysicsTerm("inline_kappa", "Inline equation: kappa", _compute_inline_kappa),
    "inline_Ereact_1": PhysicsTerm("inline_Ereact_1", "Inline equation: Ereact", _compute_inline_Ereact_1),
    "inline_omega_s_2": PhysicsTerm("inline_omega_s_2", "Inline equation: omega_s", _compute_inline_omega_s_2),
    "inline_Pcore": PhysicsTerm("inline_Pcore", "Inline equation: Pcore", _compute_inline_Pcore),
    "inline_k3": PhysicsTerm("inline_k3", "Inline equation: k3", _compute_inline_k3),
    "inline_omega_s_t_1": PhysicsTerm("inline_omega_s_t_1", "Inline equation: omega_s_t", _compute_inline_omega_s_t_1),
    "inline_Bj_1": PhysicsTerm("inline_Bj_1", "Inline equation: Bj", _compute_inline_Bj_1),
    "inline_rho_v": PhysicsTerm("inline_rho_v", "Inline equation: rho_v", _compute_inline_rho_v),
    "inline_C_concentration": PhysicsTerm("inline_C_concentration", "Inline equation: C_concentration", _compute_inline_C_concentration),
    "inline_Mbh": PhysicsTerm("inline_Mbh", "Inline equation: Mbh", _compute_inline_Mbh),
    "inline_dg": PhysicsTerm("inline_dg", "Inline equation: dg", _compute_inline_dg),
    "inline_f_feedback": PhysicsTerm("inline_f_feedback", "Inline equation: f_feedback", _compute_inline_f_feedback),
    "inline_k4": PhysicsTerm("inline_k4", "Inline equation: k4", _compute_inline_k4),
    "inline_tn_1": PhysicsTerm("inline_tn_1", "Inline equation: tn", _compute_inline_tn_1),
    "inline_decay_1": PhysicsTerm("inline_decay_1", "Inline equation: decay", _compute_inline_decay_1),
    "inline_cycle": PhysicsTerm("inline_cycle", "Inline equation: cycle", _compute_inline_cycle),
    "inline_rj": PhysicsTerm("inline_rj", "Inline equation: rj", _compute_inline_rj),
    "inline_gamma_4": PhysicsTerm("inline_gamma_4", "Inline equation: gamma", _compute_inline_gamma_4),
    "inline_phi_hat": PhysicsTerm("inline_phi_hat", "Inline equation: phi_hat", _compute_inline_phi_hat),
    "inline_num_strings": PhysicsTerm("inline_num_strings", "Inline equation: num_strings", _compute_inline_num_strings),
    "inline_PSCm": PhysicsTerm("inline_PSCm", "Inline equation: PSCm", _compute_inline_PSCm),
    "inline_mu_j_1": PhysicsTerm("inline_mu_j_1", "Inline equation: mu_j", _compute_inline_mu_j_1),
    "inline_decay_2": PhysicsTerm("inline_decay_2", "Inline equation: decay", _compute_inline_decay_2),
    "inline_M_halo": PhysicsTerm("inline_M_halo", "Inline equation: M_halo", _compute_inline_M_halo),
    "inline_r_scale": PhysicsTerm("inline_r_scale", "Inline equation: r_scale", _compute_inline_r_scale),
    "inline_r_14": PhysicsTerm("inline_r_14", "Inline equation: r", _compute_inline_r_14),
    "inline_rho_0_2": PhysicsTerm("inline_rho_0_2", "Inline equation: rho_0", _compute_inline_rho_0_2),
    "inline_E_vac_scale": PhysicsTerm("inline_E_vac_scale", "Inline equation: E_vac_scale", _compute_inline_E_vac_scale),
    "inline_lambda_3": PhysicsTerm("inline_lambda_3", "Inline equation: lambda", _compute_inline_lambda_3),
    "inline_H0_2": PhysicsTerm("inline_H0_2", "Inline equation: H0", _compute_inline_H0_2),
    "inline_hbar_1": PhysicsTerm("inline_hbar_1", "Inline equation: hbar", _compute_inline_hbar_1),
    "inline_Delta_x_p": PhysicsTerm("inline_Delta_x_p", "Inline equation: Delta_x_p", _compute_inline_Delta_x_p),
    "inline_integral_psi_2": PhysicsTerm("inline_integral_psi_2", "Inline equation: integral_psi", _compute_inline_integral_psi_2),
    "inline_tHubble": PhysicsTerm("inline_tHubble", "Inline equation: tHubble", _compute_inline_tHubble),
    "inline_time_factor": PhysicsTerm("inline_time_factor", "Inline equation: time_factor", _compute_inline_time_factor),
    "inline_Lambda_1": PhysicsTerm("inline_Lambda_1", "Inline equation: Lambda", _compute_inline_Lambda_1),
    "inline_rho_4": PhysicsTerm("inline_rho_4", "Inline equation: rho", _compute_inline_rho_4),
    "inline_P_1": PhysicsTerm("inline_P_1", "Inline equation: P", _compute_inline_P_1),
    "inline_fluid_factor": PhysicsTerm("inline_fluid_factor", "Inline equation: fluid_factor", _compute_inline_fluid_factor),
    "inline_delta_metric": PhysicsTerm("inline_delta_metric", "Inline equation: delta_metric", _compute_inline_delta_metric),
    "inline_omega_pert": PhysicsTerm("inline_omega_pert", "Inline equation: omega_pert", _compute_inline_omega_pert),
    "inline_omega_res": PhysicsTerm("inline_omega_res", "Inline equation: omega_res", _compute_inline_omega_res),
    "inline_Q_factor": PhysicsTerm("inline_Q_factor", "Inline equation: Q_factor", _compute_inline_Q_factor),
    "inline_amplitude": PhysicsTerm("inline_amplitude", "Inline equation: amplitude", _compute_inline_amplitude),
    "inline_resonance": PhysicsTerm("inline_resonance", "Inline equation: resonance", _compute_inline_resonance),
    "inline_B_crit_1": PhysicsTerm("inline_B_crit_1", "Inline equation: B_crit", _compute_inline_B_crit_1),
    "inline_B_field_1": PhysicsTerm("inline_B_field_1", "Inline equation: B_field", _compute_inline_B_field_1),
    "inline_adjustment_scale": PhysicsTerm("inline_adjustment_scale", "Inline equation: adjustment_scale", _compute_inline_adjustment_scale),
    "inline_T_env": PhysicsTerm("inline_T_env", "Inline equation: T_env", _compute_inline_T_env),
    "inline_scale_2": PhysicsTerm("inline_scale_2", "Inline equation: scale", _compute_inline_scale_2),
    "inline_Ug1_4": PhysicsTerm("inline_Ug1_4", "Inline equation: Ug1", _compute_inline_Ug1_4),
    "inline_Ug2_1": PhysicsTerm("inline_Ug2_1", "Inline equation: Ug2", _compute_inline_Ug2_1),
    "inline_Ug3": PhysicsTerm("inline_Ug3", "Inline equation: Ug3", _compute_inline_Ug3),
    "inline_Ug4_1": PhysicsTerm("inline_Ug4_1", "Inline equation: Ug4", _compute_inline_Ug4_1),
    "inline_Omega_g": PhysicsTerm("inline_Omega_g", "Inline equation: Omega_g", _compute_inline_Omega_g),
    "inline_mu_j_2": PhysicsTerm("inline_mu_j_2", "Inline equation: mu_j", _compute_inline_mu_j_2),
    "inline_rho_sw": PhysicsTerm("inline_rho_sw", "Inline equation: rho_sw", _compute_inline_rho_sw),
    "inline_base_2": PhysicsTerm("inline_base_2", "Inline equation: base", _compute_inline_base_2),
    "inline_k_base": PhysicsTerm("inline_k_base", "Inline equation: k_base", _compute_inline_k_base),
    "inline_lambda_base": PhysicsTerm("inline_lambda_base", "Inline equation: lambda_base", _compute_inline_lambda_base),
    "inline_rho_vac_UA_2": PhysicsTerm("inline_rho_vac_UA_2", "Inline equation: rho_vac_UA", _compute_inline_rho_vac_UA_2),
    "inline_B_j_1": PhysicsTerm("inline_B_j_1", "Inline equation: B_j", _compute_inline_B_j_1),
    "inline_Mbh_solar": PhysicsTerm("inline_Mbh_solar", "Inline equation: Mbh_solar", _compute_inline_Mbh_solar),
    "inline_B_j_2": PhysicsTerm("inline_B_j_2", "Inline equation: B_j", _compute_inline_B_j_2),
    "inline_theta_1": PhysicsTerm("inline_theta_1", "Inline equation: theta", _compute_inline_theta_1),
    "inline_phi": PhysicsTerm("inline_phi", "Inline equation: phi", _compute_inline_phi),
    "inline_r_15": PhysicsTerm("inline_r_15", "Inline equation: r", _compute_inline_r_15),
    "inline_R_b": PhysicsTerm("inline_R_b", "Inline equation: R_b", _compute_inline_R_b),
    "inline_k2_1": PhysicsTerm("inline_k2_1", "Inline equation: k2", _compute_inline_k2_1),
    "inline_M_s": PhysicsTerm("inline_M_s", "Inline equation: M_s", _compute_inline_M_s),
    "inline_S_1": PhysicsTerm("inline_S_1", "Inline equation: S", _compute_inline_S_1),
    "inline_gamma_day": PhysicsTerm("inline_gamma_day", "Inline equation: gamma_day", _compute_inline_gamma_day),
    "inline_t_n_1": PhysicsTerm("inline_t_n_1", "Inline equation: t_n", _compute_inline_t_n_1),
    "inline_t_day": PhysicsTerm("inline_t_day", "Inline equation: t_day", _compute_inline_t_day),
    "inline_mu_over_rj": PhysicsTerm("inline_mu_over_rj", "Inline equation: mu_over_rj", _compute_inline_mu_over_rj),
    "inline_factor": PhysicsTerm("inline_factor", "Inline equation: factor", _compute_inline_factor),
    "inline_P_SCm_1": PhysicsTerm("inline_P_SCm_1", "Inline equation: P_SCm", _compute_inline_P_SCm_1),
    "inline_P_SCm_planet": PhysicsTerm("inline_P_SCm_planet", "Inline equation: P_SCm_planet", _compute_inline_P_SCm_planet),
    "inline_kappa_day": PhysicsTerm("inline_kappa_day", "Inline equation: kappa_day", _compute_inline_kappa_day),
    "inline_t_day_1": PhysicsTerm("inline_t_day_1", "Inline equation: t_day", _compute_inline_t_day_1),
    "inline_E_react_0": PhysicsTerm("inline_E_react_0", "Inline equation: E_react_0", _compute_inline_E_react_0),
    "inline_E_react_2": PhysicsTerm("inline_E_react_2", "Inline equation: E_react", _compute_inline_E_react_2),
    "inline_period": PhysicsTerm("inline_period", "Inline equation: period", _compute_inline_period),
    "inline_omega_c_1": PhysicsTerm("inline_omega_c_1", "Inline equation: omega_c", _compute_inline_omega_c_1),
    "inline_B_j_base": PhysicsTerm("inline_B_j_base", "Inline equation: B_j_base", _compute_inline_B_j_base),
    "inline_R_s": PhysicsTerm("inline_R_s", "Inline equation: R_s", _compute_inline_R_s),
    "inline_B_j_3": PhysicsTerm("inline_B_j_3", "Inline equation: B_j", _compute_inline_B_j_3),
    "inline_period_s": PhysicsTerm("inline_period_s", "Inline equation: period_s", _compute_inline_period_s),
    "inline_amplitude_1": PhysicsTerm("inline_amplitude_1", "Inline equation: amplitude", _compute_inline_amplitude_1),
    "inline_r_16": PhysicsTerm("inline_r_16", "Inline equation: r", _compute_inline_r_16),
    "inline_modulation_1": PhysicsTerm("inline_modulation_1", "Inline equation: modulation", _compute_inline_modulation_1),
    "inline_B_j_4": PhysicsTerm("inline_B_j_4", "Inline equation: B_j", _compute_inline_B_j_4),
    "inline_P_core": PhysicsTerm("inline_P_core", "Inline equation: P_core", _compute_inline_P_core),
    "inline_E_react_3": PhysicsTerm("inline_E_react_3", "Inline equation: E_react", _compute_inline_E_react_3),
    "inline_Lambda_i": PhysicsTerm("inline_Lambda_i", "Inline equation: Lambda_i", _compute_inline_Lambda_i),
    "inline_T_s_base": PhysicsTerm("inline_T_s_base", "Inline equation: T_s_base", _compute_inline_T_s_base),
    "inline_rho_vac_A_1": PhysicsTerm("inline_rho_vac_A_1", "Inline equation: rho_vac_A", _compute_inline_rho_vac_A_1),
    "inline_epsilon": PhysicsTerm("inline_epsilon", "Inline equation: epsilon", _compute_inline_epsilon),
    "inline_B_s_1": PhysicsTerm("inline_B_s_1", "Inline equation: B_s", _compute_inline_B_s_1),
    "inline_B_ref_2": PhysicsTerm("inline_B_ref_2", "Inline equation: B_ref", _compute_inline_B_ref_2),
    "inline_B_j_5": PhysicsTerm("inline_B_j_5", "Inline equation: B_j", _compute_inline_B_j_5),
    "inline_T_s_1": PhysicsTerm("inline_T_s_1", "Inline equation: T_s", _compute_inline_T_s_1),
    "inline_T_s_ref": PhysicsTerm("inline_T_s_ref", "Inline equation: T_s_ref", _compute_inline_T_s_ref),
    "inline_B_j_6": PhysicsTerm("inline_B_j_6", "Inline equation: B_j", _compute_inline_B_j_6),
    "inline_base_3": PhysicsTerm("inline_base_3", "Inline equation: base", _compute_inline_base_3),
    "inline_amplitude_2": PhysicsTerm("inline_amplitude_2", "Inline equation: amplitude", _compute_inline_amplitude_2),
    "inline_omega_def": PhysicsTerm("inline_omega_def", "Inline equation: omega_def", _compute_inline_omega_def),
    "inline_t_day_2": PhysicsTerm("inline_t_day_2", "Inline equation: t_day", _compute_inline_t_day_2),
    "inline_r_17": PhysicsTerm("inline_r_17", "Inline equation: r", _compute_inline_r_17),
    "inline_delta_def_1": PhysicsTerm("inline_delta_def_1", "Inline equation: delta_def", _compute_inline_delta_def_1),
    "inline_theta_j": PhysicsTerm("inline_theta_j", "Inline equation: theta_j", _compute_inline_theta_j),
    "inline_phi_hat_magnitude": PhysicsTerm("inline_phi_hat_magnitude", "Inline equation: phi_hat_magnitude", _compute_inline_phi_hat_magnitude),
    "inline_T_s_base_1": PhysicsTerm("inline_T_s_base_1", "Inline equation: T_s_base", _compute_inline_T_s_base_1),
    "inline_rho_vac_A_2": PhysicsTerm("inline_rho_vac_A_2", "Inline equation: rho_vac_A", _compute_inline_rho_vac_A_2),
    "inline_T_s_2": PhysicsTerm("inline_T_s_2", "Inline equation: T_s", _compute_inline_T_s_2),
    "inline_rho_vac_SCm_1": PhysicsTerm("inline_rho_vac_SCm_1", "Inline equation: rho_vac_SCm", _compute_inline_rho_vac_SCm_1),
    "inline_vac_product": PhysicsTerm("inline_vac_product", "Inline equation: vac_product", _compute_inline_vac_product),
    "inline_S_factor": PhysicsTerm("inline_S_factor", "Inline equation: S_factor", _compute_inline_S_factor),
    "inline_modulation_2": PhysicsTerm("inline_modulation_2", "Inline equation: modulation", _compute_inline_modulation_2),
    "inline_rho_SCm_1": PhysicsTerm("inline_rho_SCm_1", "Inline equation: rho_SCm", _compute_inline_rho_SCm_1),
    "inline_rho_A_1": PhysicsTerm("inline_rho_A_1", "Inline equation: rho_A", _compute_inline_rho_A_1),
    "inline_v_scm": PhysicsTerm("inline_v_scm", "Inline equation: v_scm", _compute_inline_v_scm),
    "inline_E_base": PhysicsTerm("inline_E_base", "Inline equation: E_base", _compute_inline_E_base),
    "inline_mu_over_r": PhysicsTerm("inline_mu_over_r", "Inline equation: mu_over_r", _compute_inline_mu_over_r),
    "inline_M_14": PhysicsTerm("inline_M_14", "Inline equation: M", _compute_inline_M_14),
    "inline_r_18": PhysicsTerm("inline_r_18", "Inline equation: r", _compute_inline_r_18),
    "inline_rho_0_3": PhysicsTerm("inline_rho_0_3", "Inline equation: rho_0", _compute_inline_rho_0_3),
    "inline_rho_LENR": PhysicsTerm("inline_rho_LENR", "Inline equation: rho_LENR", _compute_inline_rho_LENR),
    "inline_integrand_5": PhysicsTerm("inline_integrand_5", "Inline equation: integrand", _compute_inline_integrand_5),
    "inline_x2_2": PhysicsTerm("inline_x2_2", "Inline equation: x2", _compute_inline_x2_2),
    "inline_M_15": PhysicsTerm("inline_M_15", "Inline equation: M", _compute_inline_M_15),
    "inline_r_19": PhysicsTerm("inline_r_19", "Inline equation: r", _compute_inline_r_19),
    "inline_integrand_6": PhysicsTerm("inline_integrand_6", "Inline equation: integrand", _compute_inline_integrand_6),
    "inline_M_16": PhysicsTerm("inline_M_16", "Inline equation: M", _compute_inline_M_16),
    "inline_r_20": PhysicsTerm("inline_r_20", "Inline equation: r", _compute_inline_r_20),
    "inline_omega_5": PhysicsTerm("inline_omega_5", "Inline equation: omega", _compute_inline_omega_5),
    "inline_a_3": PhysicsTerm("inline_a_3", "Inline equation: a", _compute_inline_a_3),
    "inline_b": PhysicsTerm("inline_b", "Inline equation: b", _compute_inline_b),
    "inline_c": PhysicsTerm("inline_c", "Inline equation: c", _compute_inline_c),
    "inline_omega_6": PhysicsTerm("inline_omega_6", "Inline equation: omega", _compute_inline_omega_6),
    "inline_rho_vac_2": PhysicsTerm("inline_rho_vac_2", "Inline equation: rho_vac", _compute_inline_rho_vac_2),
    "inline_integrand_7": PhysicsTerm("inline_integrand_7", "Inline equation: integrand", _compute_inline_integrand_7),
    "inline_M_17": PhysicsTerm("inline_M_17", "Inline equation: M", _compute_inline_M_17),
    "inline_r_21": PhysicsTerm("inline_r_21", "Inline equation: r", _compute_inline_r_21),
    "inline_rho_0_4": PhysicsTerm("inline_rho_0_4", "Inline equation: rho_0", _compute_inline_rho_0_4),
    "inline_integrand_8": PhysicsTerm("inline_integrand_8", "Inline equation: integrand", _compute_inline_integrand_8),
    "inline_M_18": PhysicsTerm("inline_M_18", "Inline equation: M", _compute_inline_M_18),
    "inline_r_22": PhysicsTerm("inline_r_22", "Inline equation: r", _compute_inline_r_22),
    "inline_M_19": PhysicsTerm("inline_M_19", "Inline equation: M", _compute_inline_M_19),
    "inline_r_23": PhysicsTerm("inline_r_23", "Inline equation: r", _compute_inline_r_23),
    "inline_integrand_9": PhysicsTerm("inline_integrand_9", "Inline equation: integrand", _compute_inline_integrand_9),
    "inline_M_20": PhysicsTerm("inline_M_20", "Inline equation: M", _compute_inline_M_20),
    "inline_r_24": PhysicsTerm("inline_r_24", "Inline equation: r", _compute_inline_r_24),
    "inline_integrand_10": PhysicsTerm("inline_integrand_10", "Inline equation: integrand", _compute_inline_integrand_10),
    "inline_integrand_11": PhysicsTerm("inline_integrand_11", "Inline equation: integrand", _compute_inline_integrand_11),
    "inline_M_21": PhysicsTerm("inline_M_21", "Inline equation: M", _compute_inline_M_21),
    "inline_r_25": PhysicsTerm("inline_r_25", "Inline equation: r", _compute_inline_r_25),
    "inline_M_22": PhysicsTerm("inline_M_22", "Inline equation: M", _compute_inline_M_22),
    "inline_r_26": PhysicsTerm("inline_r_26", "Inline equation: r", _compute_inline_r_26),
    "inline_M_23": PhysicsTerm("inline_M_23", "Inline equation: M", _compute_inline_M_23),
    "inline_r_27": PhysicsTerm("inline_r_27", "Inline equation: r", _compute_inline_r_27),
    "inline_g_2": PhysicsTerm("inline_g_2", "Inline equation: g", _compute_inline_g_2),
    "inline_M_24": PhysicsTerm("inline_M_24", "Inline equation: M", _compute_inline_M_24),
    "inline_exp_arg_1": PhysicsTerm("inline_exp_arg_1", "Inline equation: exp_arg", _compute_inline_exp_arg_1),
    "inline_base_4": PhysicsTerm("inline_base_4", "Inline equation: base", _compute_inline_base_4),
    "inline_k_1_1": PhysicsTerm("inline_k_1_1", "Inline equation: k_1", _compute_inline_k_1_1),
    "inline_rho_UA": PhysicsTerm("inline_rho_UA", "Inline equation: rho_UA", _compute_inline_rho_UA),
    "inline_M_s_1": PhysicsTerm("inline_M_s_1", "Inline equation: M_s", _compute_inline_M_s_1),
    "inline_omega_c_2": PhysicsTerm("inline_omega_c_2", "Inline equation: omega_c", _compute_inline_omega_c_2),
    "inline_mu_j_3": PhysicsTerm("inline_mu_j_3", "Inline equation: mu_j", _compute_inline_mu_j_3),
    "inline_exp_arg_2": PhysicsTerm("inline_exp_arg_2", "Inline equation: exp_arg", _compute_inline_exp_arg_2),
    "inline_omega_c_3": PhysicsTerm("inline_omega_c_3", "Inline equation: omega_c", _compute_inline_omega_c_3),
    "inline_T_cycle": PhysicsTerm("inline_T_cycle", "Inline equation: T_cycle", _compute_inline_T_cycle),
    "inline_corr": PhysicsTerm("inline_corr", "Inline equation: corr", _compute_inline_corr),
    "inline_outline": PhysicsTerm("inline_outline", "Inline equation: outline", _compute_inline_outline),
    "inline_final_outline": PhysicsTerm("inline_final_outline", "Inline equation: final_outline", _compute_inline_final_outline),
    "inline_r_s_w": PhysicsTerm("inline_r_s_w", "Inline equation: r_s_w", _compute_inline_r_s_w),
    "inline_T_hawking_w": PhysicsTerm("inline_T_hawking_w", "Inline equation: T_hawking_w", _compute_inline_T_hawking_w),
    "inline_A_horizon_w": PhysicsTerm("inline_A_horizon_w", "Inline equation: A_horizon_w", _compute_inline_A_horizon_w),
    "inline_S_BH_w": PhysicsTerm("inline_S_BH_w", "Inline equation: S_BH_w", _compute_inline_S_BH_w),
    "inline_r_s": PhysicsTerm("inline_r_s", "Inline equation: r_s", _compute_inline_r_s),
    "inline_T_hawking": PhysicsTerm("inline_T_hawking", "Inline equation: T_hawking", _compute_inline_T_hawking),
    "inline_A_horizon": PhysicsTerm("inline_A_horizon", "Inline equation: A_horizon", _compute_inline_A_horizon),
    "inline_S_BH": PhysicsTerm("inline_S_BH", "Inline equation: S_BH", _compute_inline_S_BH),
    "inline_channel_capacity": PhysicsTerm("inline_channel_capacity", "Inline equation: channel_capacity", _compute_inline_channel_capacity),
    "inline_rho_sum": PhysicsTerm("inline_rho_sum", "Inline equation: rho_sum", _compute_inline_rho_sum),
    "inline_S_r_Rb": PhysicsTerm("inline_S_r_Rb", "Inline equation: S_r_Rb", _compute_inline_S_r_Rb),
    "inline_swirl_factor": PhysicsTerm("inline_swirl_factor", "Inline equation: swirl_factor", _compute_inline_swirl_factor),
    "inline_E_react_4": PhysicsTerm("inline_E_react_4", "Inline equation: E_react", _compute_inline_E_react_4),
    "inline_result_1": PhysicsTerm("inline_result_1", "Inline equation: result", _compute_inline_result_1),
    "inline_U_g2_helio": PhysicsTerm("inline_U_g2_helio", "Inline equation: U_g2_helio", _compute_inline_U_g2_helio),
    "inline_H_factor": PhysicsTerm("inline_H_factor", "Inline equation: H_factor", _compute_inline_H_factor),
    "inline_total_ug": PhysicsTerm("inline_total_ug", "Inline equation: total_ug", _compute_inline_total_ug),
    "inline_ug2_contribution": PhysicsTerm("inline_ug2_contribution", "Inline equation: ug2_contribution", _compute_inline_ug2_contribution),
    "inline_rho_sc_1": PhysicsTerm("inline_rho_sc_1", "Inline equation: rho_sc", _compute_inline_rho_sc_1),
    "inline_rho_ua_2": PhysicsTerm("inline_rho_ua_2", "Inline equation: rho_ua", _compute_inline_rho_ua_2),
    "inline_omega_s_t_2": PhysicsTerm("inline_omega_s_t_2", "Inline equation: omega_s_t", _compute_inline_omega_s_t_2),
    "inline_cos_term": PhysicsTerm("inline_cos_term", "Inline equation: cos_term", _compute_inline_cos_term),
    "inline_trz_factor": PhysicsTerm("inline_trz_factor", "Inline equation: trz_factor", _compute_inline_trz_factor),
    "inline_e_react": PhysicsTerm("inline_e_react", "Inline equation: e_react", _compute_inline_e_react),
    "inline_t": PhysicsTerm("inline_t", "Inline equation: t", _compute_inline_t),
    "inline_term": PhysicsTerm("inline_term", "Inline equation: term", _compute_inline_term),
    "inline_base_mu": PhysicsTerm("inline_base_mu", "Inline equation: base_mu", _compute_inline_base_mu),
    "inline_omega_c_4": PhysicsTerm("inline_omega_c_4", "Inline equation: omega_c", _compute_inline_omega_c_4),
    "inline_amplitude_3": PhysicsTerm("inline_amplitude_3", "Inline equation: amplitude", _compute_inline_amplitude_3),
    "inline_base_5": PhysicsTerm("inline_base_5", "Inline equation: base", _compute_inline_base_5),
    "inline_t_1": PhysicsTerm("inline_t_1", "Inline equation: t", _compute_inline_t_1),
    "inline_sw_term": PhysicsTerm("inline_sw_term", "Inline equation: sw_term", _compute_inline_sw_term),
    "inline_decay_3": PhysicsTerm("inline_decay_3", "Inline equation: decay", _compute_inline_decay_3),
    "inline_feedback": PhysicsTerm("inline_feedback", "Inline equation: feedback", _compute_inline_feedback),
    "inline_M_solar": PhysicsTerm("inline_M_solar", "Inline equation: M_solar", _compute_inline_M_solar),
    "inline_cos_term_1": PhysicsTerm("inline_cos_term_1", "Inline equation: cos_term", _compute_inline_cos_term_1),
    "inline_k1_SOURCE4": PhysicsTerm("inline_k1_SOURCE4", "Inline equation: k1_SOURCE4", _compute_inline_k1_SOURCE4),
    "inline_SCm_contrib": PhysicsTerm("inline_SCm_contrib", "Inline equation: SCm_contrib", _compute_inline_SCm_contrib),
    "inline_SCm_contrib_1": PhysicsTerm("inline_SCm_contrib_1", "Inline equation: SCm_contrib", _compute_inline_SCm_contrib_1),
    "inline_SCm_contrib_2": PhysicsTerm("inline_SCm_contrib_2", "Inline equation: SCm_contrib", _compute_inline_SCm_contrib_2),
    "inline_mu_s_1": PhysicsTerm("inline_mu_s_1", "Inline equation: mu_s", _compute_inline_mu_s_1),
    "inline_grad_Ms_r_1": PhysicsTerm("inline_grad_Ms_r_1", "Inline equation: grad_Ms_r", _compute_inline_grad_Ms_r_1),
    "inline_Ereact_2": PhysicsTerm("inline_Ereact_2", "Inline equation: Ereact", _compute_inline_Ereact_2),
    "inline_S_2": PhysicsTerm("inline_S_2", "Inline equation: S", _compute_inline_S_2),
    "inline_omega_s_t_3": PhysicsTerm("inline_omega_s_t_3", "Inline equation: omega_s_t", _compute_inline_omega_s_t_3),
    "inline_Bj_2": PhysicsTerm("inline_Bj_2", "Inline equation: Bj", _compute_inline_Bj_2),
    "inline_cycle_1": PhysicsTerm("inline_cycle_1", "Inline equation: cycle", _compute_inline_cycle_1),
    "inline_wind_mod_1": PhysicsTerm("inline_wind_mod_1", "Inline equation: wind_mod", _compute_inline_wind_mod_1),
    "inline_phi_hat_1": PhysicsTerm("inline_phi_hat_1", "Inline equation: phi_hat", _compute_inline_phi_hat_1),
    "inline_mu_j_4": PhysicsTerm("inline_mu_j_4", "Inline equation: mu_j", _compute_inline_mu_j_4),
    "inline_decay_4": PhysicsTerm("inline_decay_4", "Inline equation: decay", _compute_inline_decay_4),
    "inline_Ug1_5": PhysicsTerm("inline_Ug1_5", "Inline equation: Ug1", _compute_inline_Ug1_5),
    "inline_Ug2_2": PhysicsTerm("inline_Ug2_2", "Inline equation: Ug2", _compute_inline_Ug2_2),
    "inline_Ug3_1": PhysicsTerm("inline_Ug3_1", "Inline equation: Ug3", _compute_inline_Ug3_1),
    "inline_Ug4_2": PhysicsTerm("inline_Ug4_2", "Inline equation: Ug4", _compute_inline_Ug4_2),
    "inline_sum_Ug": PhysicsTerm("inline_sum_Ug", "Inline equation: sum_Ug", _compute_inline_sum_Ug),
    "inline_Ubi": PhysicsTerm("inline_Ubi", "Inline equation: Ubi", _compute_inline_Ubi),
    "inline_Um": PhysicsTerm("inline_Um", "Inline equation: Um", _compute_inline_Um),
    "inline_H0_3": PhysicsTerm("inline_H0_3", "Inline equation: H0", _compute_inline_H0_3),
    "inline_Lambda_2": PhysicsTerm("inline_Lambda_2", "Inline equation: Lambda", _compute_inline_Lambda_2),
    "inline_hbar_2": PhysicsTerm("inline_hbar_2", "Inline equation: hbar", _compute_inline_hbar_2),
    "inline_base_6": PhysicsTerm("inline_base_6", "Inline equation: base", _compute_inline_base_6),
    "inline_expansion_1": PhysicsTerm("inline_expansion_1", "Inline equation: expansion", _compute_inline_expansion_1),
    "inline_super_adj": PhysicsTerm("inline_super_adj", "Inline equation: super_adj", _compute_inline_super_adj),
    "inline_env": PhysicsTerm("inline_env", "Inline equation: env", _compute_inline_env),
    "inline_adjusted_base": PhysicsTerm("inline_adjusted_base", "Inline equation: adjusted_base", _compute_inline_adjusted_base),
    "inline_Ug_sum": PhysicsTerm("inline_Ug_sum", "Inline equation: Ug_sum", _compute_inline_Ug_sum),
    "inline_cosm": PhysicsTerm("inline_cosm", "Inline equation: cosm", _compute_inline_cosm),
    "inline_quantum": PhysicsTerm("inline_quantum", "Inline equation: quantum", _compute_inline_quantum),
    "inline_fluid": PhysicsTerm("inline_fluid", "Inline equation: fluid", _compute_inline_fluid),
    "inline_perturbation": PhysicsTerm("inline_perturbation", "Inline equation: perturbation", _compute_inline_perturbation),
    "inline_FDPM_1": PhysicsTerm("inline_FDPM_1", "Inline equation: FDPM", _compute_inline_FDPM_1),
    "inline_Ereact_3": PhysicsTerm("inline_Ereact_3", "Inline equation: Ereact", _compute_inline_Ereact_3),
    "inline_H_z": PhysicsTerm("inline_H_z", "Inline equation: H_z", _compute_inline_H_z),
    "inline_fexp": PhysicsTerm("inline_fexp", "Inline equation: fexp", _compute_inline_fexp),
    "inline_b_1": PhysicsTerm("inline_b_1", "Inline equation: b", _compute_inline_b_1),
    "inline_aDPM_1": PhysicsTerm("inline_aDPM_1", "Inline equation: aDPM", _compute_inline_aDPM_1),
    "inline_aTHz": PhysicsTerm("inline_aTHz", "Inline equation: aTHz", _compute_inline_aTHz),
    "inline_avac_diff": PhysicsTerm("inline_avac_diff", "Inline equation: avac_diff", _compute_inline_avac_diff),
    "inline_asuper_freq_1": PhysicsTerm("inline_asuper_freq_1", "Inline equation: asuper_freq", _compute_inline_asuper_freq_1),
    "inline_aaether_res_1": PhysicsTerm("inline_aaether_res_1", "Inline equation: aaether_res", _compute_inline_aaether_res_1),
    "inline_Ug4i": PhysicsTerm("inline_Ug4i", "Inline equation: Ug4i", _compute_inline_Ug4i),
    "inline_aquantum_freq_1": PhysicsTerm("inline_aquantum_freq_1", "Inline equation: aquantum_freq", _compute_inline_aquantum_freq_1),
    "inline_aAether_freq": PhysicsTerm("inline_aAether_freq", "Inline equation: aAether_freq", _compute_inline_aAether_freq),
    "inline_afluid_freq": PhysicsTerm("inline_afluid_freq", "Inline equation: afluid_freq", _compute_inline_afluid_freq),
    "inline_Osc_term": PhysicsTerm("inline_Osc_term", "Inline equation: Osc_term", _compute_inline_Osc_term),
    "inline_aexp_freq": PhysicsTerm("inline_aexp_freq", "Inline equation: aexp_freq", _compute_inline_aexp_freq),
    "inline_a_wormhole": PhysicsTerm("inline_a_wormhole", "Inline equation: a_wormhole", _compute_inline_a_wormhole),
    "inline_rho_SCm_2": PhysicsTerm("inline_rho_SCm_2", "Inline equation: rho_SCm", _compute_inline_rho_SCm_2),
    "inline_energy_ratio": PhysicsTerm("inline_energy_ratio", "Inline equation: energy_ratio", _compute_inline_energy_ratio),
    "inline_Bs": PhysicsTerm("inline_Bs", "Inline equation: Bs", _compute_inline_Bs),
    "inline_Rs_1": PhysicsTerm("inline_Rs_1", "Inline equation: Rs", _compute_inline_Rs_1),
    "inline_M_25": PhysicsTerm("inline_M_25", "Inline equation: M", _compute_inline_M_25),
    "inline_R_1": PhysicsTerm("inline_R_1", "Inline equation: R", _compute_inline_R_1),
    "inline_omega_c_5": PhysicsTerm("inline_omega_c_5", "Inline equation: omega_c", _compute_inline_omega_c_5),
    "inline_rho_SCm_3": PhysicsTerm("inline_rho_SCm_3", "Inline equation: rho_SCm", _compute_inline_rho_SCm_3),
    "inline_omega_s_3": PhysicsTerm("inline_omega_s_3", "Inline equation: omega_s", _compute_inline_omega_s_3),
    "inline_omega_c_6": PhysicsTerm("inline_omega_c_6", "Inline equation: omega_c", _compute_inline_omega_c_6),
    "inline_Bj_3": PhysicsTerm("inline_Bj_3", "Inline equation: Bj", _compute_inline_Bj_3),
    "inline_mu_s_2": PhysicsTerm("inline_mu_s_2", "Inline equation: mu_s", _compute_inline_mu_s_2),
    "inline_grad_Ms_r_2": PhysicsTerm("inline_grad_Ms_r_2", "Inline equation: grad_Ms_r", _compute_inline_grad_Ms_r_2),
    "inline_tn_2": PhysicsTerm("inline_tn_2", "Inline equation: tn", _compute_inline_tn_2),
    "inline_QUA_1": PhysicsTerm("inline_QUA_1", "Inline equation: QUA", _compute_inline_QUA_1),
    "inline_M_26": PhysicsTerm("inline_M_26", "Inline equation: M", _compute_inline_M_26),
    "inline_r_28": PhysicsTerm("inline_r_28", "Inline equation: r", _compute_inline_r_28),
    "inline_Ereact_4": PhysicsTerm("inline_Ereact_4", "Inline equation: Ereact", _compute_inline_Ereact_4),
    "inline_S_3": PhysicsTerm("inline_S_3", "Inline equation: S", _compute_inline_S_3),
    "inline_omega_s_t_4": PhysicsTerm("inline_omega_s_t_4", "Inline equation: omega_s_t", _compute_inline_omega_s_t_4),
    "inline_Pcore_1": PhysicsTerm("inline_Pcore_1", "Inline equation: Pcore", _compute_inline_Pcore_1),
    "inline_Mbh_1": PhysicsTerm("inline_Mbh_1", "Inline equation: Mbh", _compute_inline_Mbh_1),
    "inline_dg_1": PhysicsTerm("inline_dg_1", "Inline equation: dg", _compute_inline_dg_1),
    "inline_Ugi_1": PhysicsTerm("inline_Ugi_1", "Inline equation: Ugi", _compute_inline_Ugi_1),
    "inline_rho_sw_1": PhysicsTerm("inline_rho_sw_1", "Inline equation: rho_sw", _compute_inline_rho_sw_1),
    "inline_mu_j_5": PhysicsTerm("inline_mu_j_5", "Inline equation: mu_j", _compute_inline_mu_j_5),
    "inline_rj_1": PhysicsTerm("inline_rj_1", "Inline equation: rj", _compute_inline_rj_1),
    "inline_PSCm_1": PhysicsTerm("inline_PSCm_1", "Inline equation: PSCm", _compute_inline_PSCm_1),
    "inline_mod_1": PhysicsTerm("inline_mod_1", "Inline equation: mod", _compute_inline_mod_1),
    "inline_sum_Ugi": PhysicsTerm("inline_sum_Ugi", "Inline equation: sum_Ugi", _compute_inline_sum_Ugi),
    "inline_sum_Ubi": PhysicsTerm("inline_sum_Ubi", "Inline equation: sum_Ubi", _compute_inline_sum_Ubi),
    "inline_Um_1": PhysicsTerm("inline_Um_1", "Inline equation: Um", _compute_inline_Um_1),
    "inline_A_scalar_1": PhysicsTerm("inline_A_scalar_1", "Inline equation: A_scalar", _compute_inline_A_scalar_1),
    "inline_M_27": PhysicsTerm("inline_M_27", "Inline equation: M", _compute_inline_M_27),
    "inline_r_29": PhysicsTerm("inline_r_29", "Inline equation: r", _compute_inline_r_29),
    "inline_t_sys": PhysicsTerm("inline_t_sys", "Inline equation: t_sys", _compute_inline_t_sys),
    "inline_B_6": PhysicsTerm("inline_B_6", "Inline equation: B", _compute_inline_B_6),
    "inline_Bcrit_1": PhysicsTerm("inline_Bcrit_1", "Inline equation: Bcrit", _compute_inline_Bcrit_1),
    "inline_rho_fluid_4": PhysicsTerm("inline_rho_fluid_4", "Inline equation: rho_fluid", _compute_inline_rho_fluid_4),
    "inline_Vsys_1": PhysicsTerm("inline_Vsys_1", "Inline equation: Vsys", _compute_inline_Vsys_1),
    "inline_g_local": PhysicsTerm("inline_g_local", "Inline equation: g_local", _compute_inline_g_local),
    "inline_M_DM_1": PhysicsTerm("inline_M_DM_1", "Inline equation: M_DM", _compute_inline_M_DM_1),
    "inline_delta_rho_rho_1": PhysicsTerm("inline_delta_rho_rho_1", "Inline equation: delta_rho_rho", _compute_inline_delta_rho_rho_1),
    "inline_I_1": PhysicsTerm("inline_I_1", "Inline equation: I", _compute_inline_I_1),
    "inline_A_14": PhysicsTerm("inline_A_14", "Inline equation: A", _compute_inline_A_14),
    "inline_omega1_1": PhysicsTerm("inline_omega1_1", "Inline equation: omega1", _compute_inline_omega1_1),
    "inline_omega2_1": PhysicsTerm("inline_omega2_1", "Inline equation: omega2", _compute_inline_omega2_1),
    "inline_fDPM": PhysicsTerm("inline_fDPM", "Inline equation: fDPM", _compute_inline_fDPM),
    "inline_Evac_neb": PhysicsTerm("inline_Evac_neb", "Inline equation: Evac_neb", _compute_inline_Evac_neb),
    "inline_c_res": PhysicsTerm("inline_c_res", "Inline equation: c_res", _compute_inline_c_res),
    "inline_Vsys_2": PhysicsTerm("inline_Vsys_2", "Inline equation: Vsys", _compute_inline_Vsys_2),
    "inline_aDPM_2": PhysicsTerm("inline_aDPM_2", "Inline equation: aDPM", _compute_inline_aDPM_2),
    "inline_fTHz": PhysicsTerm("inline_fTHz", "Inline equation: fTHz", _compute_inline_fTHz),
    "inline_vexp_1": PhysicsTerm("inline_vexp_1", "Inline equation: vexp", _compute_inline_vexp_1),
    "inline_Evac_ISM": PhysicsTerm("inline_Evac_ISM", "Inline equation: Evac_ISM", _compute_inline_Evac_ISM),
    "inline_Delta_Evac": PhysicsTerm("inline_Delta_Evac", "Inline equation: Delta_Evac", _compute_inline_Delta_Evac),
    "inline_Fsuper": PhysicsTerm("inline_Fsuper", "Inline equation: Fsuper", _compute_inline_Fsuper),
    "inline_UA_SCM": PhysicsTerm("inline_UA_SCM", "Inline equation: UA_SCM", _compute_inline_UA_SCM),
    "inline_omega_i": PhysicsTerm("inline_omega_i", "Inline equation: omega_i", _compute_inline_omega_i),
    "inline_fTRZ": PhysicsTerm("inline_fTRZ", "Inline equation: fTRZ", _compute_inline_fTRZ),
    "inline_k4_res": PhysicsTerm("inline_k4_res", "Inline equation: k4_res", _compute_inline_k4_res),
    "inline_freact": PhysicsTerm("inline_freact", "Inline equation: freact", _compute_inline_freact),
    "inline_Ereact_5": PhysicsTerm("inline_Ereact_5", "Inline equation: Ereact", _compute_inline_Ereact_5),
    "inline_fquantum": PhysicsTerm("inline_fquantum", "Inline equation: fquantum", _compute_inline_fquantum),
    "inline_fAether": PhysicsTerm("inline_fAether", "Inline equation: fAether", _compute_inline_fAether),
    "inline_ffluid": PhysicsTerm("inline_ffluid", "Inline equation: ffluid", _compute_inline_ffluid),
    "inline_H_z_1": PhysicsTerm("inline_H_z_1", "Inline equation: H_z", _compute_inline_H_z_1),
    "inline_fexp_1": PhysicsTerm("inline_fexp_1", "Inline equation: fexp", _compute_inline_fexp_1),
    "inline_r_30": PhysicsTerm("inline_r_30", "Inline equation: r", _compute_inline_r_30),
    "inline_b_2": PhysicsTerm("inline_b_2", "Inline equation: b", _compute_inline_b_2),
    "inline_f_worm": PhysicsTerm("inline_f_worm", "Inline equation: f_worm", _compute_inline_f_worm),
    "inline_a_tau_BSM": PhysicsTerm("inline_a_tau_BSM", "Inline equation: a_tau_BSM", _compute_inline_a_tau_BSM),
    "inline_total": PhysicsTerm("inline_total", "Inline equation: total", _compute_inline_total),
    "inline_total_1": PhysicsTerm("inline_total_1", "Inline equation: total", _compute_inline_total_1),
    "inline_V_cb": PhysicsTerm("inline_V_cb", "Inline equation: V_cb", _compute_inline_V_cb),
    "inline_V_cb_1": PhysicsTerm("inline_V_cb_1", "Inline equation: V_cb", _compute_inline_V_cb_1),
    "inline_m_VLQ": PhysicsTerm("inline_m_VLQ", "Inline equation: m_VLQ", _compute_inline_m_VLQ),
    "inline_mass_frac": PhysicsTerm("inline_mass_frac", "Inline equation: mass_frac", _compute_inline_mass_frac),
    "inline_kappa_min": PhysicsTerm("inline_kappa_min", "Inline equation: kappa_min", _compute_inline_kappa_min),
    "inline_kappa_max": PhysicsTerm("inline_kappa_max", "Inline equation: kappa_max", _compute_inline_kappa_max),
    "inline_B_val": PhysicsTerm("inline_B_val", "Inline equation: B_val", _compute_inline_B_val),
    "inline_Bcrit_val": PhysicsTerm("inline_Bcrit_val", "Inline equation: Bcrit_val", _compute_inline_Bcrit_val),
    "inline_a_4": PhysicsTerm("inline_a_4", "Inline equation: a", _compute_inline_a_4),
    "inline_x_3": PhysicsTerm("inline_x_3", "Inline equation: x", _compute_inline_x_3),
    "inline_y": PhysicsTerm("inline_y", "Inline equation: y", _compute_inline_y),
    "inline_s1": PhysicsTerm("inline_s1", "Inline equation: s1", _compute_inline_s1),
    "inline_t1": PhysicsTerm("inline_t1", "Inline equation: t1", _compute_inline_t1),
    "inline_uqff_g_1": PhysicsTerm("inline_uqff_g_1", "Inline equation: uqff_g", _compute_inline_uqff_g_1),
    "inline_FU": PhysicsTerm("inline_FU", "Inline equation: FU", _compute_inline_FU),
    "inline_g_c_sgr": PhysicsTerm("inline_g_c_sgr", "Inline equation: g_c_sgr", _compute_inline_g_c_sgr),
    "inline_g_r_sgr": PhysicsTerm("inline_g_r_sgr", "Inline equation: g_r_sgr", _compute_inline_g_r_sgr),
    "inline_g_c_saga": PhysicsTerm("inline_g_c_saga", "Inline equation: g_c_saga", _compute_inline_g_c_saga),
    "inline_g_r_saga": PhysicsTerm("inline_g_r_saga", "Inline equation: g_r_saga", _compute_inline_g_r_saga),
    "inline_g_c_tap": PhysicsTerm("inline_g_c_tap", "Inline equation: g_c_tap", _compute_inline_g_c_tap),
    "inline_g_r_tap": PhysicsTerm("inline_g_r_tap", "Inline equation: g_r_tap", _compute_inline_g_r_tap),
    "inline_g_c_west": PhysicsTerm("inline_g_c_west", "Inline equation: g_c_west", _compute_inline_g_c_west),
    "inline_g_r_west": PhysicsTerm("inline_g_r_west", "Inline equation: g_r_west", _compute_inline_g_r_west),
    "inline_g_c_pil": PhysicsTerm("inline_g_c_pil", "Inline equation: g_c_pil", _compute_inline_g_c_pil),
    "inline_g_r_pil": PhysicsTerm("inline_g_r_pil", "Inline equation: g_r_pil", _compute_inline_g_r_pil),
    "inline_g_c_ring": PhysicsTerm("inline_g_c_ring", "Inline equation: g_c_ring", _compute_inline_g_c_ring),
    "inline_g_r_ring": PhysicsTerm("inline_g_r_ring", "Inline equation: g_r_ring", _compute_inline_g_r_ring),
    "inline_g_c_guide": PhysicsTerm("inline_g_c_guide", "Inline equation: g_c_guide", _compute_inline_g_c_guide),
    "inline_g_r_guide": PhysicsTerm("inline_g_r_guide", "Inline equation: g_r_guide", _compute_inline_g_r_guide),
    "inline_amplitude_4": PhysicsTerm("inline_amplitude_4", "Inline equation: amplitude", _compute_inline_amplitude_4),
    "inline_rho_vac_3": PhysicsTerm("inline_rho_vac_3", "Inline equation: rho_vac", _compute_inline_rho_vac_3),
    "inline_frequency": PhysicsTerm("inline_frequency", "Inline equation: frequency", _compute_inline_frequency),
    "inline_coupling_3": PhysicsTerm("inline_coupling_3", "Inline equation: coupling", _compute_inline_coupling_3),
    "inline_M_bh_3": PhysicsTerm("inline_M_bh_3", "Inline equation: M_bh", _compute_inline_M_bh_3),
    "inline_M_sun": PhysicsTerm("inline_M_sun", "Inline equation: M_sun", _compute_inline_M_sun),
    "inline_r_31": PhysicsTerm("inline_r_31", "Inline equation: r", _compute_inline_r_31),
    "inline_lambda_Q": PhysicsTerm("inline_lambda_Q", "Inline equation: lambda_Q", _compute_inline_lambda_Q),
    "inline_mass_ratio": PhysicsTerm("inline_mass_ratio", "Inline equation: mass_ratio", _compute_inline_mass_ratio),
    "inline_M_0": PhysicsTerm("inline_M_0", "Inline equation: M_0", _compute_inline_M_0),
    "inline_sigma": PhysicsTerm("inline_sigma", "Inline equation: sigma", _compute_inline_sigma),
    "inline_sigma_0": PhysicsTerm("inline_sigma_0", "Inline equation: sigma_0", _compute_inline_sigma_0),
    "inline_alpha_2": PhysicsTerm("inline_alpha_2", "Inline equation: alpha", _compute_inline_alpha_2),
    "inline_G_1": PhysicsTerm("inline_G_1", "Inline equation: G", _compute_inline_G_1),
    "inline_M_bulge": PhysicsTerm("inline_M_bulge", "Inline equation: M_bulge", _compute_inline_M_bulge),
    "inline_R_bulge": PhysicsTerm("inline_R_bulge", "Inline equation: R_bulge", _compute_inline_R_bulge),
    "inline_M_bh_4": PhysicsTerm("inline_M_bh_4", "Inline equation: M_bh", _compute_inline_M_bh_4),
    "inline_r_32": PhysicsTerm("inline_r_32", "Inline equation: r", _compute_inline_r_32),
    "inline_c_1": PhysicsTerm("inline_c_1", "Inline equation: c", _compute_inline_c_1),
    "inline_k_8": PhysicsTerm("inline_k_8", "Inline equation: k", _compute_inline_k_8),
    "inline_E_0_3": PhysicsTerm("inline_E_0_3", "Inline equation: E_0", _compute_inline_E_0_3),
    "inline_tau": PhysicsTerm("inline_tau", "Inline equation: tau", _compute_inline_tau),
    "inline_state_n": PhysicsTerm("inline_state_n", "Inline equation: state_n", _compute_inline_state_n),
    "inline_shift": PhysicsTerm("inline_shift", "Inline equation: shift", _compute_inline_shift),
    "inline_M_bh_5": PhysicsTerm("inline_M_bh_5", "Inline equation: M_bh", _compute_inline_M_bh_5),
    "inline_z": PhysicsTerm("inline_z", "Inline equation: z", _compute_inline_z),
    "inline_k_B": PhysicsTerm("inline_k_B", "Inline equation: k_B", _compute_inline_k_B),
    "inline_T": PhysicsTerm("inline_T", "Inline equation: T", _compute_inline_T),
    "inline_m_p": PhysicsTerm("inline_m_p", "Inline equation: m_p", _compute_inline_m_p),
    "inline_B_7": PhysicsTerm("inline_B_7", "Inline equation: B", _compute_inline_B_7),
    "inline_mu_0": PhysicsTerm("inline_mu_0", "Inline equation: mu_0", _compute_inline_mu_0),
    "inline_R_2": PhysicsTerm("inline_R_2", "Inline equation: R", _compute_inline_R_2),
    "inline_V_bulge": PhysicsTerm("inline_V_bulge", "Inline equation: V_bulge", _compute_inline_V_bulge),
    "inline_v_circ": PhysicsTerm("inline_v_circ", "Inline equation: v_circ", _compute_inline_v_circ),
    "inline_t_0": PhysicsTerm("inline_t_0", "Inline equation: t_0", _compute_inline_t_0),
    "inline_mass_46": PhysicsTerm("inline_mass_46", "Inline equation: mass", _compute_inline_mass_46),
    "inline_M_enclosed": PhysicsTerm("inline_M_enclosed", "Inline equation: M_enclosed", _compute_inline_M_enclosed),
    "inline_temp_1": PhysicsTerm("inline_temp_1", "Inline equation: temp", _compute_inline_temp_1),
    "inline_P_ICM": PhysicsTerm("inline_P_ICM", "Inline equation: P_ICM", _compute_inline_P_ICM),
    "inline_mass_47": PhysicsTerm("inline_mass_47", "Inline equation: mass", _compute_inline_mass_47),
    "inline_f_c": PhysicsTerm("inline_f_c", "Inline equation: f_c", _compute_inline_f_c),
    "inline_Phi": PhysicsTerm("inline_Phi", "Inline equation: Phi", _compute_inline_Phi),
    "inline_scale_density": PhysicsTerm("inline_scale_density", "Inline equation: scale_density", _compute_inline_scale_density),
    "inline_rho_DM": PhysicsTerm("inline_rho_DM", "Inline equation: rho_DM", _compute_inline_rho_DM),
    "inline_luminosity": PhysicsTerm("inline_luminosity", "Inline equation: luminosity", _compute_inline_luminosity),
    "inline_u_jet": PhysicsTerm("inline_u_jet", "Inline equation: u_jet", _compute_inline_u_jet),
    "inline_gamma_5": PhysicsTerm("inline_gamma_5", "Inline equation: gamma", _compute_inline_gamma_5),
    "inline_mass_48": PhysicsTerm("inline_mass_48", "Inline equation: mass", _compute_inline_mass_48),
    "inline_r_tidal": PhysicsTerm("inline_r_tidal", "Inline equation: r_tidal", _compute_inline_r_tidal),
    "inline_a_tidal": PhysicsTerm("inline_a_tidal", "Inline equation: a_tidal", _compute_inline_a_tidal),
    "inline_mass_49": PhysicsTerm("inline_mass_49", "Inline equation: mass", _compute_inline_mass_49),
    "inline_sigma_virial": PhysicsTerm("inline_sigma_virial", "Inline equation: sigma_virial", _compute_inline_sigma_virial),
    "inline_virial_ratio": PhysicsTerm("inline_virial_ratio", "Inline equation: virial_ratio", _compute_inline_virial_ratio),
    "inline_ne_central": PhysicsTerm("inline_ne_central", "Inline equation: ne_central", _compute_inline_ne_central),
    "inline_Lambda_T": PhysicsTerm("inline_Lambda_T", "Inline equation: Lambda_T", _compute_inline_Lambda_T),
    "inline_epsilon_X": PhysicsTerm("inline_epsilon_X", "Inline equation: epsilon_X", _compute_inline_epsilon_X),
    "inline_sigma_central": PhysicsTerm("inline_sigma_central", "Inline equation: sigma_central", _compute_inline_sigma_central),
    "inline_A_res_2": PhysicsTerm("inline_A_res_2", "Inline equation: A_res", _compute_inline_A_res_2),
    "inline_F_res": PhysicsTerm("inline_F_res", "Inline equation: F_res", _compute_inline_F_res),
    "inline_M_28": PhysicsTerm("inline_M_28", "Inline equation: M", _compute_inline_M_28),
    "inline_r_33": PhysicsTerm("inline_r_33", "Inline equation: r", _compute_inline_r_33),
    "inline_M_29": PhysicsTerm("inline_M_29", "Inline equation: M", _compute_inline_M_29),
    "inline_r_34": PhysicsTerm("inline_r_34", "Inline equation: r", _compute_inline_r_34),
    "inline_cnb": PhysicsTerm("inline_cnb", "Inline equation: cnb", _compute_inline_cnb),
    "inline_G_2": PhysicsTerm("inline_G_2", "Inline equation: G", _compute_inline_G_2),
    "inline_triadic": PhysicsTerm("inline_triadic", "Inline equation: triadic", _compute_inline_triadic),
    "inline_comp": PhysicsTerm("inline_comp", "Inline equation: comp", _compute_inline_comp),
    "inline_k_9": PhysicsTerm("inline_k_9", "Inline equation: k", _compute_inline_k_9),
    "inline_rho_ua_3": PhysicsTerm("inline_rho_ua_3", "Inline equation: rho_ua", _compute_inline_rho_ua_3),
    "inline_ratio": PhysicsTerm("inline_ratio", "Inline equation: ratio", _compute_inline_ratio),
    "inline_dpm_res": PhysicsTerm("inline_dpm_res", "Inline equation: dpm_res", _compute_inline_dpm_res),
    "inline_cos_theta": PhysicsTerm("inline_cos_theta", "Inline equation: cos_theta", _compute_inline_cos_theta),
    "inline_ratio_1": PhysicsTerm("inline_ratio_1", "Inline equation: ratio", _compute_inline_ratio_1),
    "inline_momentum_term": PhysicsTerm("inline_momentum_term", "Inline equation: momentum_term", _compute_inline_momentum_term),
    "inline_gravity_term": PhysicsTerm("inline_gravity_term", "Inline equation: gravity_term", _compute_inline_gravity_term),
    "inline_vac_term": PhysicsTerm("inline_vac_term", "Inline equation: vac_term", _compute_inline_vac_term),
    "inline_f_lenr": PhysicsTerm("inline_f_lenr", "Inline equation: f_lenr", _compute_inline_f_lenr),
    "inline_f_act": PhysicsTerm("inline_f_act", "Inline equation: f_act", _compute_inline_f_act),
    "inline_f_de": PhysicsTerm("inline_f_de", "Inline equation: f_de", _compute_inline_f_de),
    "inline_f_res_2": PhysicsTerm("inline_f_res_2", "Inline equation: f_res", _compute_inline_f_res_2),
    "inline_f_neutron": PhysicsTerm("inline_f_neutron", "Inline equation: f_neutron", _compute_inline_f_neutron),
    "inline_integrand_12": PhysicsTerm("inline_integrand_12", "Inline equation: integrand", _compute_inline_integrand_12),
    "inline_a_5": PhysicsTerm("inline_a_5", "Inline equation: a", _compute_inline_a_5),
    "inline_b_3": PhysicsTerm("inline_b_3", "Inline equation: b", _compute_inline_b_3),
    "inline_c_2": PhysicsTerm("inline_c_2", "Inline equation: c", _compute_inline_c_2),
    "inline_f_bi_i": PhysicsTerm("inline_f_bi_i", "Inline equation: f_bi_i", _compute_inline_f_bi_i),
    "inline_omega_7": PhysicsTerm("inline_omega_7", "Inline equation: omega", _compute_inline_omega_7),
    "inline_mod_2": PhysicsTerm("inline_mod_2", "Inline equation: mod", _compute_inline_mod_2),
    "inline_k1_2": PhysicsTerm("inline_k1_2", "Inline equation: k1", _compute_inline_k1_2),
    "inline_geom": PhysicsTerm("inline_geom", "Inline equation: geom", _compute_inline_geom),
    "inline_mod_3": PhysicsTerm("inline_mod_3", "Inline equation: mod", _compute_inline_mod_3),
    "inline_t_2": PhysicsTerm("inline_t_2", "Inline equation: t", _compute_inline_t_2),
    "inline_k1_3": PhysicsTerm("inline_k1_3", "Inline equation: k1", _compute_inline_k1_3),
    "inline_t_global": PhysicsTerm("inline_t_global", "Inline equation: t_global", _compute_inline_t_global),
    "inline_defaultValue_2": PhysicsTerm("inline_defaultValue_2", "Inline equation: defaultValue", _compute_inline_defaultValue_2),
    "inline_t_global_1": PhysicsTerm("inline_t_global_1", "Inline equation: t_global", _compute_inline_t_global_1),
    "inline_k1_4": PhysicsTerm("inline_k1_4", "Inline equation: k1", _compute_inline_k1_4),
    "inline_e_dpm_i": PhysicsTerm("inline_e_dpm_i", "Inline equation: e_dpm_i", _compute_inline_e_dpm_i),
    "inline_r_i_sq": PhysicsTerm("inline_r_i_sq", "Inline equation: r_i_sq", _compute_inline_r_i_sq),
    "inline_mod_i": PhysicsTerm("inline_mod_i", "Inline equation: mod_i", _compute_inline_mod_i),
    "inline_g_base_2": PhysicsTerm("inline_g_base_2", "Inline equation: g_base", _compute_inline_g_base_2),
    "inline_omega_i_val": PhysicsTerm("inline_omega_i_val", "Inline equation: omega_i_val", _compute_inline_omega_i_val),
    "inline_g_i": PhysicsTerm("inline_g_i", "Inline equation: g_i", _compute_inline_g_i),
    "inline_cos_term_2": PhysicsTerm("inline_cos_term_2", "Inline equation: cos_term", _compute_inline_cos_term_2),
    "inline_r_ug_i": PhysicsTerm("inline_r_ug_i", "Inline equation: r_ug_i", _compute_inline_r_ug_i),
    "inline_g_3": PhysicsTerm("inline_g_3", "Inline equation: g", _compute_inline_g_3),
    "inline_r_35": PhysicsTerm("inline_r_35", "Inline equation: r", _compute_inline_r_35),
    "inline_t_3": PhysicsTerm("inline_t_3", "Inline equation: t", _compute_inline_t_3),
    "inline_t_4": PhysicsTerm("inline_t_4", "Inline equation: t", _compute_inline_t_4),
    "inline_vacuum_density_1": PhysicsTerm("inline_vacuum_density_1", "Inline equation: vacuum_density", _compute_inline_vacuum_density_1),
    "inline_defaultValue_3": PhysicsTerm("inline_defaultValue_3", "Inline equation: defaultValue", _compute_inline_defaultValue_3),
    "inline_seed_orbit": PhysicsTerm("inline_seed_orbit", "Inline equation: seed_orbit", _compute_inline_seed_orbit),
    "inline_poly": PhysicsTerm("inline_poly", "Inline equation: poly", _compute_inline_poly),
    "inline_defaultValue_4": PhysicsTerm("inline_defaultValue_4", "Inline equation: defaultValue", _compute_inline_defaultValue_4),
    "inline_I_UNIT": PhysicsTerm("inline_I_UNIT", "Inline equation: I_UNIT", _compute_inline_I_UNIT),
    "inline_a_6": PhysicsTerm("inline_a_6", "Inline equation: a", _compute_inline_a_6),
    "inline_b_4": PhysicsTerm("inline_b_4", "Inline equation: b", _compute_inline_b_4),
    "inline_val_1": PhysicsTerm("inline_val_1", "Inline equation: val", _compute_inline_val_1),
    "inline_random_scale": PhysicsTerm("inline_random_scale", "Inline equation: random_scale", _compute_inline_random_scale),
    "inline_term2_5": PhysicsTerm("inline_term2_5", "Inline equation: term2", _compute_inline_term2_5),
    "inline_term3_1": PhysicsTerm("inline_term3_1", "Inline equation: term3", _compute_inline_term3_1),
    "inline_term4_1": PhysicsTerm("inline_term4_1", "Inline equation: term4", _compute_inline_term4_1),
    "inline_result_2": PhysicsTerm("inline_result_2", "Inline equation: result", _compute_inline_result_2),
    "inline_quantum_term": PhysicsTerm("inline_quantum_term", "Inline equation: quantum_term", _compute_inline_quantum_term),
    "inline_result_3": PhysicsTerm("inline_result_3", "Inline equation: result", _compute_inline_result_3),
    "inline_h_omega0": PhysicsTerm("inline_h_omega0", "Inline equation: h_omega0", _compute_inline_h_omega0),
    "inline_amp_3": PhysicsTerm("inline_amp_3", "Inline equation: amp", _compute_inline_amp_3),
    "inline_strength_2": PhysicsTerm("inline_strength_2", "Inline equation: strength", _compute_inline_strength_2),
    "inline_amp_4": PhysicsTerm("inline_amp_4", "Inline equation: amp", _compute_inline_amp_4),
    "inline_strength_3": PhysicsTerm("inline_strength_3", "Inline equation: strength", _compute_inline_strength_3),
    "inline_amp_5": PhysicsTerm("inline_amp_5", "Inline equation: amp", _compute_inline_amp_5),
    "inline_strength_4": PhysicsTerm("inline_strength_4", "Inline equation: strength", _compute_inline_strength_4),
    "inline_t_day_3": PhysicsTerm("inline_t_day_3", "Inline equation: t_day", _compute_inline_t_day_3),
    "inline_num": PhysicsTerm("inline_num", "Inline equation: num", _compute_inline_num),
    "inline_value_1": PhysicsTerm("inline_value_1", "Inline equation: value", _compute_inline_value_1),
    "inline_converted": PhysicsTerm("inline_converted", "Inline equation: converted", _compute_inline_converted),
    "inline_guess": PhysicsTerm("inline_guess", "Inline equation: guess", _compute_inline_guess),
    "inline_f_val": PhysicsTerm("inline_f_val", "Inline equation: f_val", _compute_inline_f_val),
    "inline_x_4": PhysicsTerm("inline_x_4", "Inline equation: x", _compute_inline_x_4),
    "inline_x0": PhysicsTerm("inline_x0", "Inline equation: x0", _compute_inline_x0),
    "inline_dt": PhysicsTerm("inline_dt", "Inline equation: dt", _compute_inline_dt),
    "inline_procVal": PhysicsTerm("inline_procVal", "Inline equation: procVal", _compute_inline_procVal),
    "inline_out": PhysicsTerm("inline_out", "Inline equation: out", _compute_inline_out),
    "inline_t_5": PhysicsTerm("inline_t_5", "Inline equation: t", _compute_inline_t_5),
    "inline_term3_2": PhysicsTerm("inline_term3_2", "Inline equation: term3", _compute_inline_term3_2),
    "inline_term5": PhysicsTerm("inline_term5", "Inline equation: term5", _compute_inline_term5),
    "inline_term_q": PhysicsTerm("inline_term_q", "Inline equation: term_q", _compute_inline_term_q),
    "inline_term_fluid": PhysicsTerm("inline_term_fluid", "Inline equation: term_fluid", _compute_inline_term_fluid),
    "inline_term_osc": PhysicsTerm("inline_term_osc", "Inline equation: term_osc", _compute_inline_term_osc),
    "inline_term_DM": PhysicsTerm("inline_term_DM", "Inline equation: term_DM", _compute_inline_term_DM),
    "inline_cum_D_1": PhysicsTerm("inline_cum_D_1", "Inline equation: cum_D", _compute_inline_cum_D_1),
    "inline_t_example": PhysicsTerm("inline_t_example", "Inline equation: t_example", _compute_inline_t_example),
    "inline_amp_6": PhysicsTerm("inline_amp_6", "Inline equation: amp", _compute_inline_amp_6),
    "inline_strength_5": PhysicsTerm("inline_strength_5", "Inline equation: strength", _compute_inline_strength_5),
    "inline_term4_2": PhysicsTerm("inline_term4_2", "Inline equation: term4", _compute_inline_term4_2),
    "inline_em_base_4": PhysicsTerm("inline_em_base_4", "Inline equation: em_base", _compute_inline_em_base_4),
    "inline_term_fluid_1": PhysicsTerm("inline_term_fluid_1", "Inline equation: term_fluid", _compute_inline_term_fluid_1),
    "inline_term_DM_1": PhysicsTerm("inline_term_DM_1", "Inline equation: term_DM", _compute_inline_term_DM_1),
    "inline_amp_7": PhysicsTerm("inline_amp_7", "Inline equation: amp", _compute_inline_amp_7),
    "inline_strength_6": PhysicsTerm("inline_strength_6", "Inline equation: strength", _compute_inline_strength_6),
    "inline_M_dot_2": PhysicsTerm("inline_M_dot_2", "Inline equation: M_dot", _compute_inline_M_dot_2),
    "inline_wind_pressure_1": PhysicsTerm("inline_wind_pressure_1", "Inline equation: wind_pressure", _compute_inline_wind_pressure_1),
    "inline_term_wind": PhysicsTerm("inline_term_wind", "Inline equation: term_wind", _compute_inline_term_wind),
    "inline_k1_5": PhysicsTerm("inline_k1_5", "Inline equation: k1", _compute_inline_k1_5),
    "inline_geom_factor_1": PhysicsTerm("inline_geom_factor_1", "Inline equation: geom_factor", _compute_inline_geom_factor_1),
    "inline_exp_barrier_1": PhysicsTerm("inline_exp_barrier_1", "Inline equation: exp_barrier", _compute_inline_exp_barrier_1),
    "inline_term_1": PhysicsTerm("inline_term_1", "Inline equation: term", _compute_inline_term_1),
    "inline_term1_3": PhysicsTerm("inline_term1_3", "Inline equation: term1", _compute_inline_term1_3),
    "inline_term2_6": PhysicsTerm("inline_term2_6", "Inline equation: term2", _compute_inline_term2_6),
    "inline_term3_3": PhysicsTerm("inline_term3_3", "Inline equation: term3", _compute_inline_term3_3),
    "inline_combined": PhysicsTerm("inline_combined", "Inline equation: combined", _compute_inline_combined),
    "inline_geom_factor_2": PhysicsTerm("inline_geom_factor_2", "Inline equation: geom_factor", _compute_inline_geom_factor_2),
    "inline_rho_vac_SCm_2": PhysicsTerm("inline_rho_vac_SCm_2", "Inline equation: rho_vac_SCm", _compute_inline_rho_vac_SCm_2),
    "inline_heaviside_term": PhysicsTerm("inline_heaviside_term", "Inline equation: heaviside_term", _compute_inline_heaviside_term),
    "inline_rho_vac_UA_3": PhysicsTerm("inline_rho_vac_UA_3", "Inline equation: rho_vac_UA", _compute_inline_rho_vac_UA_3),
    "inline_rho_vac_UA_4": PhysicsTerm("inline_rho_vac_UA_4", "Inline equation: rho_vac_UA", _compute_inline_rho_vac_UA_4),
    "inline_field_term": PhysicsTerm("inline_field_term", "Inline equation: field_term", _compute_inline_field_term),
    "inline_M_30": PhysicsTerm("inline_M_30", "Inline equation: M", _compute_inline_M_30),
    "inline_F_ug1": PhysicsTerm("inline_F_ug1", "Inline equation: F_ug1", _compute_inline_F_ug1),
    "inline_F_ug3": PhysicsTerm("inline_F_ug3", "Inline equation: F_ug3", _compute_inline_F_ug3),
    "inline_k1_6": PhysicsTerm("inline_k1_6", "Inline equation: k1", _compute_inline_k1_6),
    "inline_k1_7": PhysicsTerm("inline_k1_7", "Inline equation: k1", _compute_inline_k1_7),
    "inline_t_global_2": PhysicsTerm("inline_t_global_2", "Inline equation: t_global", _compute_inline_t_global_2),
    "inline_defaultValue_5": PhysicsTerm("inline_defaultValue_5", "Inline equation: defaultValue", _compute_inline_defaultValue_5),
    "inline_t_global_3": PhysicsTerm("inline_t_global_3", "Inline equation: t_global", _compute_inline_t_global_3),
    "inline_t_6": PhysicsTerm("inline_t_6", "Inline equation: t", _compute_inline_t_6),
    "inline_vacuum_density_2": PhysicsTerm("inline_vacuum_density_2", "Inline equation: vacuum_density", _compute_inline_vacuum_density_2),
    "inline_defaultValue_6": PhysicsTerm("inline_defaultValue_6", "Inline equation: defaultValue", _compute_inline_defaultValue_6),
    "inline_defaultValue_7": PhysicsTerm("inline_defaultValue_7", "Inline equation: defaultValue", _compute_inline_defaultValue_7),
    "inline_term1_4": PhysicsTerm("inline_term1_4", "Inline equation: term1", _compute_inline_term1_4),
    "inline_Hz_kms": PhysicsTerm("inline_Hz_kms", "Inline equation: Hz_kms", _compute_inline_Hz_kms),
    "inline_term1_5": PhysicsTerm("inline_term1_5", "Inline equation: term1", _compute_inline_term1_5),
    "inline_Hz_kms_1": PhysicsTerm("inline_Hz_kms_1", "Inline equation: Hz_kms", _compute_inline_Hz_kms_1),
    "inline_term_SN": PhysicsTerm("inline_term_SN", "Inline equation: term_SN", _compute_inline_term_SN),
    "inline_term1_6": PhysicsTerm("inline_term1_6", "Inline equation: term1", _compute_inline_term1_6),
    "inline_M_dot_3": PhysicsTerm("inline_M_dot_3", "Inline equation: M_dot", _compute_inline_M_dot_3),
    "inline_term1_7": PhysicsTerm("inline_term1_7", "Inline equation: term1", _compute_inline_term1_7),
    "inline_term_feedback": PhysicsTerm("inline_term_feedback", "Inline equation: term_feedback", _compute_inline_term_feedback),
    "inline_term1_8": PhysicsTerm("inline_term1_8", "Inline equation: term1", _compute_inline_term1_8),
    "inline_cool_pressure": PhysicsTerm("inline_cool_pressure", "Inline equation: cool_pressure", _compute_inline_cool_pressure),
    "inline_term_cool": PhysicsTerm("inline_term_cool", "Inline equation: term_cool", _compute_inline_term_cool),
    "inline_Hz_kms_2": PhysicsTerm("inline_Hz_kms_2", "Inline equation: Hz_kms", _compute_inline_Hz_kms_2),
    "inline_amp_8": PhysicsTerm("inline_amp_8", "Inline equation: amp", _compute_inline_amp_8),
    "inline_strength_7": PhysicsTerm("inline_strength_7", "Inline equation: strength", _compute_inline_strength_7),
    "inline_M_31": PhysicsTerm("inline_M_31", "Inline equation: M", _compute_inline_M_31),
    "inline_r_36": PhysicsTerm("inline_r_36", "Inline equation: r", _compute_inline_r_36),
    "inline_tolerance": PhysicsTerm("inline_tolerance", "Inline equation: tolerance", _compute_inline_tolerance),
    "inline_currentValue": PhysicsTerm("inline_currentValue", "Inline equation: currentValue", _compute_inline_currentValue),
    "inline_error_2": PhysicsTerm("inline_error_2", "Inline equation: error", _compute_inline_error_2),
    "inline_gradient": PhysicsTerm("inline_gradient", "Inline equation: gradient", _compute_inline_gradient),
    "inline_adjustment_1": PhysicsTerm("inline_adjustment_1", "Inline equation: adjustment", _compute_inline_adjustment_1),
    "inline_originalObservable": PhysicsTerm("inline_originalObservable", "Inline equation: originalObservable", _compute_inline_originalObservable),
    "inline_perturbedObservable": PhysicsTerm("inline_perturbedObservable", "Inline equation: perturbedObservable", _compute_inline_perturbedObservable),
    "inline_feedbackParameter": PhysicsTerm("inline_feedbackParameter", "Inline equation: feedbackParameter", _compute_inline_feedbackParameter),
    "inline_evolution_factor": PhysicsTerm("inline_evolution_factor", "Inline equation: evolution_factor", _compute_inline_evolution_factor),
    "inline_scaleFactor": PhysicsTerm("inline_scaleFactor", "Inline equation: scaleFactor", _compute_inline_scaleFactor),
    "inline_uqff_g_2": PhysicsTerm("inline_uqff_g_2", "Inline equation: uqff_g", _compute_inline_uqff_g_2),
    "inline_mag": PhysicsTerm("inline_mag", "Inline equation: mag", _compute_inline_mag),
    "inline_rho_0_5": PhysicsTerm("inline_rho_0_5", "Inline equation: rho_0", _compute_inline_rho_0_5),
    "inline_default_val": PhysicsTerm("inline_default_val", "Inline equation: default_val", _compute_inline_default_val),
    "inline_original": PhysicsTerm("inline_original", "Inline equation: original", _compute_inline_original),
    "inline_dynamic": PhysicsTerm("inline_dynamic", "Inline equation: dynamic", _compute_inline_dynamic),
    "inline_original_1": PhysicsTerm("inline_original_1", "Inline equation: original", _compute_inline_original_1),
    "inline_original_2": PhysicsTerm("inline_original_2", "Inline equation: original", _compute_inline_original_2),
    "inline_original_3": PhysicsTerm("inline_original_3", "Inline equation: original", _compute_inline_original_3),
    "inline_df_val": PhysicsTerm("inline_df_val", "Inline equation: df_val", _compute_inline_df_val),
    "inline_val_2": PhysicsTerm("inline_val_2", "Inline equation: val", _compute_inline_val_2),
    "inline_sqrt_ratio": PhysicsTerm("inline_sqrt_ratio", "Inline equation: sqrt_ratio", _compute_inline_sqrt_ratio),
    "inline_ratio2": PhysicsTerm("inline_ratio2", "Inline equation: ratio2", _compute_inline_ratio2),
    "inline_E_DPM_i_1": PhysicsTerm("inline_E_DPM_i_1", "Inline equation: E_DPM_i", _compute_inline_E_DPM_i_1),
    "inline_Ug1_i_1": PhysicsTerm("inline_Ug1_i_1", "Inline equation: Ug1_i", _compute_inline_Ug1_i_1),
    "inline_Ug2_i_1": PhysicsTerm("inline_Ug2_i_1", "Inline equation: Ug2_i", _compute_inline_Ug2_i_1),
    "inline_f_i_1": PhysicsTerm("inline_f_i_1", "Inline equation: f_i", _compute_inline_f_i_1),
    "inline_cos_term_3": PhysicsTerm("inline_cos_term_3", "Inline equation: cos_term", _compute_inline_cos_term_3),
    "inline_Ug3_i_1": PhysicsTerm("inline_Ug3_i_1", "Inline equation: Ug3_i", _compute_inline_Ug3_i_1),
    "inline_Ug4i_i": PhysicsTerm("inline_Ug4i_i", "Inline equation: Ug4i_i", _compute_inline_Ug4i_i),
    "inline_layer_g": PhysicsTerm("inline_layer_g", "Inline equation: layer_g", _compute_inline_layer_g),
    "inline_piPhase": PhysicsTerm("inline_piPhase", "Inline equation: piPhase", _compute_inline_piPhase),
    "inline_scaleFactor_1": PhysicsTerm("inline_scaleFactor_1", "Inline equation: scaleFactor", _compute_inline_scaleFactor_1),
    "inline_orbitSpeed": PhysicsTerm("inline_orbitSpeed", "Inline equation: orbitSpeed", _compute_inline_orbitSpeed),
    "inline_negativeTimeEffect": PhysicsTerm("inline_negativeTimeEffect", "Inline equation: negativeTimeEffect", _compute_inline_negativeTimeEffect),
    "inline_freq_2": PhysicsTerm("inline_freq_2", "Inline equation: freq", _compute_inline_freq_2),
    "inline_num_plasmoids": PhysicsTerm("inline_num_plasmoids", "Inline equation: num_plasmoids", _compute_inline_num_plasmoids),
    "inline_brightness": PhysicsTerm("inline_brightness", "Inline equation: brightness", _compute_inline_brightness),
    "inline_M_s_2": PhysicsTerm("inline_M_s_2", "Inline equation: M_s", _compute_inline_M_s_2),
    "inline_Ug": PhysicsTerm("inline_Ug", "Inline equation: Ug", _compute_inline_Ug),
    "inline_Um_2": PhysicsTerm("inline_Um_2", "Inline equation: Um", _compute_inline_Um_2),
    "inline_Ui": PhysicsTerm("inline_Ui", "Inline equation: Ui", _compute_inline_Ui),
    "inline_Ua": PhysicsTerm("inline_Ua", "Inline equation: Ua", _compute_inline_Ua),
    "inline_num_plasmoids_1": PhysicsTerm("inline_num_plasmoids_1", "Inline equation: num_plasmoids", _compute_inline_num_plasmoids_1),
    "inline_mu_over_rj_1": PhysicsTerm("inline_mu_over_rj_1", "Inline equation: mu_over_rj", _compute_inline_mu_over_rj_1),
    "inline_exp_arg_3": PhysicsTerm("inline_exp_arg_3", "Inline equation: exp_arg", _compute_inline_exp_arg_3),
    "inline_phi_hat_2": PhysicsTerm("inline_phi_hat_2", "Inline equation: phi_hat", _compute_inline_phi_hat_2),
    "inline_base_7": PhysicsTerm("inline_base_7", "Inline equation: base", _compute_inline_base_7),
    "inline_heaviside_f": PhysicsTerm("inline_heaviside_f", "Inline equation: heaviside_f", _compute_inline_heaviside_f),
    "inline_quasi_f": PhysicsTerm("inline_quasi_f", "Inline equation: quasi_f", _compute_inline_quasi_f),
    "inline_orig_f": PhysicsTerm("inline_orig_f", "Inline equation: orig_f", _compute_inline_orig_f),
    "inline_result_4": PhysicsTerm("inline_result_4", "Inline equation: result", _compute_inline_result_4),
    "inline_um_with": PhysicsTerm("inline_um_with", "Inline equation: um_with", _compute_inline_um_with),
    "inline_um_without": PhysicsTerm("inline_um_without", "Inline equation: um_without", _compute_inline_um_without),
    "inline_amplification": PhysicsTerm("inline_amplification", "Inline equation: amplification", _compute_inline_amplification),
    "inline_heav_factor": PhysicsTerm("inline_heav_factor", "Inline equation: heav_factor", _compute_inline_heav_factor),
    "inline_u_g2": PhysicsTerm("inline_u_g2", "Inline equation: u_g2", _compute_inline_u_g2),
    "inline_sum_1": PhysicsTerm("inline_sum_1", "Inline equation: sum", _compute_inline_sum_1),
    "inline_e_react_1": PhysicsTerm("inline_e_react_1", "Inline equation: e_react", _compute_inline_e_react_1),
    "inline_sum_2": PhysicsTerm("inline_sum_2", "Inline equation: sum", _compute_inline_sum_2),
    "inline_sin_term": PhysicsTerm("inline_sin_term", "Inline equation: sin_term", _compute_inline_sin_term),
    "inline_b_j": PhysicsTerm("inline_b_j", "Inline equation: b_j", _compute_inline_b_j),
    "inline_heaviside_f_1": PhysicsTerm("inline_heaviside_f_1", "Inline equation: heaviside_f", _compute_inline_heaviside_f_1),
    "inline_cos_term_4": PhysicsTerm("inline_cos_term_4", "Inline equation: cos_term", _compute_inline_cos_term_4),
    "inline_e_react_2": PhysicsTerm("inline_e_react_2", "Inline equation: e_react", _compute_inline_e_react_2),
    "inline_um": PhysicsTerm("inline_um", "Inline equation: um", _compute_inline_um),
    "inline_ug3": PhysicsTerm("inline_ug3", "Inline equation: ug3", _compute_inline_ug3),
    "inline_beta_1_1": PhysicsTerm("inline_beta_1_1", "Inline equation: beta_1", _compute_inline_beta_1_1),
    "inline_Omega_g_1": PhysicsTerm("inline_Omega_g_1", "Inline equation: Omega_g", _compute_inline_Omega_g_1),
    "inline_swirl_factor_1": PhysicsTerm("inline_swirl_factor_1", "Inline equation: swirl_factor", _compute_inline_swirl_factor_1),
    "inline_rho_vac_SCm_3": PhysicsTerm("inline_rho_vac_SCm_3", "Inline equation: rho_vac_SCm", _compute_inline_rho_vac_SCm_3),
    "inline_exp_term_1": PhysicsTerm("inline_exp_term_1", "Inline equation: exp_term", _compute_inline_exp_term_1),
    "inline_feedback_factor": PhysicsTerm("inline_feedback_factor", "Inline equation: feedback_factor", _compute_inline_feedback_factor),
    "inline_m_sun": PhysicsTerm("inline_m_sun", "Inline equation: m_sun", _compute_inline_m_sun),
    "inline_arg_2": PhysicsTerm("inline_arg_2", "Inline equation: arg", _compute_inline_arg_2),
    "inline_one_minus_exp_1": PhysicsTerm("inline_one_minus_exp_1", "Inline equation: one_minus_exp", _compute_inline_one_minus_exp_1),
    "inline_heaviside_f_2": PhysicsTerm("inline_heaviside_f_2", "Inline equation: heaviside_f", _compute_inline_heaviside_f_2),
    "inline_quasi_f_1": PhysicsTerm("inline_quasi_f_1", "Inline equation: quasi_f", _compute_inline_quasi_f_1),
    "inline_exp_pos": PhysicsTerm("inline_exp_pos", "Inline equation: exp_pos", _compute_inline_exp_pos),
    "inline_one_minus_pos": PhysicsTerm("inline_one_minus_pos", "Inline equation: one_minus_pos", _compute_inline_one_minus_pos),
    "inline_um_pos": PhysicsTerm("inline_um_pos", "Inline equation: um_pos", _compute_inline_um_pos),
    "inline_exp_neg": PhysicsTerm("inline_exp_neg", "Inline equation: exp_neg", _compute_inline_exp_neg),
    "inline_one_minus_neg": PhysicsTerm("inline_one_minus_neg", "Inline equation: one_minus_neg", _compute_inline_one_minus_neg),
    "inline_um_neg": PhysicsTerm("inline_um_neg", "Inline equation: um_neg", _compute_inline_um_neg),
    "inline_sin_omega": PhysicsTerm("inline_sin_omega", "Inline equation: sin_omega", _compute_inline_sin_omega),
    "inline_b_j_1": PhysicsTerm("inline_b_j_1", "Inline equation: b_j", _compute_inline_b_j_1),
    "inline_mu": PhysicsTerm("inline_mu", "Inline equation: mu", _compute_inline_mu),
    "inline_cos_term_5": PhysicsTerm("inline_cos_term_5", "Inline equation: cos_term", _compute_inline_cos_term_5),
    "inline_orig_p": PhysicsTerm("inline_orig_p", "Inline equation: orig_p", _compute_inline_orig_p),
    "inline_p": PhysicsTerm("inline_p", "Inline equation: p", _compute_inline_p),
    "inline_u_g3_sun": PhysicsTerm("inline_u_g3_sun", "Inline equation: u_g3_sun", _compute_inline_u_g3_sun),
    "inline_quasi_f_2": PhysicsTerm("inline_quasi_f_2", "Inline equation: quasi_f", _compute_inline_quasi_f_2),
    "inline_heaviside_f_3": PhysicsTerm("inline_heaviside_f_3", "Inline equation: heaviside_f", _compute_inline_heaviside_f_3),
    "inline_orig_f_1": PhysicsTerm("inline_orig_f_1", "Inline equation: orig_f", _compute_inline_orig_f_1),
    "inline_um_without_1": PhysicsTerm("inline_um_without_1", "Inline equation: um_without", _compute_inline_um_without_1),
    "inline_percent_increase": PhysicsTerm("inline_percent_increase", "Inline equation: percent_increase", _compute_inline_percent_increase),
    "inline_quasi_f_3": PhysicsTerm("inline_quasi_f_3", "Inline equation: quasi_f", _compute_inline_quasi_f_3),
    "inline_SCm_contrib_3": PhysicsTerm("inline_SCm_contrib_3", "Inline equation: SCm_contrib", _compute_inline_SCm_contrib_3),
    "inline_SCm_contrib_4": PhysicsTerm("inline_SCm_contrib_4", "Inline equation: SCm_contrib", _compute_inline_SCm_contrib_4),
    "inline_SCm_contrib_5": PhysicsTerm("inline_SCm_contrib_5", "Inline equation: SCm_contrib", _compute_inline_SCm_contrib_5),
    "inline_mu_s_3": PhysicsTerm("inline_mu_s_3", "Inline equation: mu_s", _compute_inline_mu_s_3),
    "inline_grad_Ms_r_3": PhysicsTerm("inline_grad_Ms_r_3", "Inline equation: grad_Ms_r", _compute_inline_grad_Ms_r_3),
    "inline_Ereact_6": PhysicsTerm("inline_Ereact_6", "Inline equation: Ereact", _compute_inline_Ereact_6),
    "inline_S_4": PhysicsTerm("inline_S_4", "Inline equation: S", _compute_inline_S_4),
    "inline_omega_s_t_5": PhysicsTerm("inline_omega_s_t_5", "Inline equation: omega_s_t", _compute_inline_omega_s_t_5),
    "inline_Bj_4": PhysicsTerm("inline_Bj_4", "Inline equation: Bj", _compute_inline_Bj_4),
    "inline_mu_j_6": PhysicsTerm("inline_mu_j_6", "Inline equation: mu_j", _compute_inline_mu_j_6),
    "inline_decay_5": PhysicsTerm("inline_decay_5", "Inline equation: decay", _compute_inline_decay_5),
    "inline_mod_4": PhysicsTerm("inline_mod_4", "Inline equation: mod", _compute_inline_mod_4),
    "inline_Ug1_6": PhysicsTerm("inline_Ug1_6", "Inline equation: Ug1", _compute_inline_Ug1_6),
    "inline_Ug2_3": PhysicsTerm("inline_Ug2_3", "Inline equation: Ug2", _compute_inline_Ug2_3),
    "inline_Ug3_2": PhysicsTerm("inline_Ug3_2", "Inline equation: Ug3", _compute_inline_Ug3_2),
    "inline_Ug4_3": PhysicsTerm("inline_Ug4_3", "Inline equation: Ug4", _compute_inline_Ug4_3),
    "inline_sum_Ugi_1": PhysicsTerm("inline_sum_Ugi_1", "Inline equation: sum_Ugi", _compute_inline_sum_Ugi_1),
    "inline_Ubi1": PhysicsTerm("inline_Ubi1", "Inline equation: Ubi1", _compute_inline_Ubi1),
    "inline_Ubi2": PhysicsTerm("inline_Ubi2", "Inline equation: Ubi2", _compute_inline_Ubi2),
    "inline_Ubi3": PhysicsTerm("inline_Ubi3", "Inline equation: Ubi3", _compute_inline_Ubi3),
    "inline_Ubi4": PhysicsTerm("inline_Ubi4", "Inline equation: Ubi4", _compute_inline_Ubi4),
    "inline_sum_Ubi_1": PhysicsTerm("inline_sum_Ubi_1", "Inline equation: sum_Ubi", _compute_inline_sum_Ubi_1),
    "inline_Um_3": PhysicsTerm("inline_Um_3", "Inline equation: Um", _compute_inline_Um_3),
    "inline_A_scalar_2": PhysicsTerm("inline_A_scalar_2", "Inline equation: A_scalar", _compute_inline_A_scalar_2),
    "inline_expected": PhysicsTerm("inline_expected", "Inline equation: expected", _compute_inline_expected),
    "inline_result_5": PhysicsTerm("inline_result_5", "Inline equation: result", _compute_inline_result_5),
    "inline_u_g2_1": PhysicsTerm("inline_u_g2_1", "Inline equation: u_g2", _compute_inline_u_g2_1),
    "inline_cos_pi_tn": PhysicsTerm("inline_cos_pi_tn", "Inline equation: cos_pi_tn", _compute_inline_cos_pi_tn),
    "inline_arg_3": PhysicsTerm("inline_arg_3", "Inline equation: arg", _compute_inline_arg_3),
    "inline_one_minus_exp_2": PhysicsTerm("inline_one_minus_exp_2", "Inline equation: one_minus_exp", _compute_inline_one_minus_exp_2),
    "inline_cos_pi": PhysicsTerm("inline_cos_pi", "Inline equation: cos_pi", _compute_inline_cos_pi),
    "inline_exp_val": PhysicsTerm("inline_exp_val", "Inline equation: exp_val", _compute_inline_exp_val),
    "inline_one_minus": PhysicsTerm("inline_one_minus", "Inline equation: one_minus", _compute_inline_one_minus),
    "inline_um_ex": PhysicsTerm("inline_um_ex", "Inline equation: um_ex", _compute_inline_um_ex),
    "inline_gamma_6": PhysicsTerm("inline_gamma_6", "Inline equation: gamma", _compute_inline_gamma_6),
    "inline_result_6": PhysicsTerm("inline_result_6", "Inline equation: result", _compute_inline_result_6),
    "inline_um_sun": PhysicsTerm("inline_um_sun", "Inline equation: um_sun", _compute_inline_um_sun),
    "inline_arg_4": PhysicsTerm("inline_arg_4", "Inline equation: arg", _compute_inline_arg_4),
    "inline_e_react_3": PhysicsTerm("inline_e_react_3", "Inline equation: e_react", _compute_inline_e_react_3),
    "inline_one_minus_exp_3": PhysicsTerm("inline_one_minus_exp_3", "Inline equation: one_minus_exp", _compute_inline_one_minus_exp_3),
    "inline_um_ex_1": PhysicsTerm("inline_um_ex_1", "Inline equation: um_ex", _compute_inline_um_ex_1),
    "inline_fraction": PhysicsTerm("inline_fraction", "Inline equation: fraction", _compute_inline_fraction),
    "inline_kappa_1": PhysicsTerm("inline_kappa_1", "Inline equation: kappa", _compute_inline_kappa_1),
    "inline_omega_8": PhysicsTerm("inline_omega_8", "Inline equation: omega", _compute_inline_omega_8),
    "inline_s_step": PhysicsTerm("inline_s_step", "Inline equation: s_step", _compute_inline_s_step),
    "inline_mod_factor": PhysicsTerm("inline_mod_factor", "Inline equation: mod_factor", _compute_inline_mod_factor),
    "inline_orig_delta": PhysicsTerm("inline_orig_delta", "Inline equation: orig_delta", _compute_inline_orig_delta),
    "inline_mod_f": PhysicsTerm("inline_mod_f", "Inline equation: mod_f", _compute_inline_mod_f),
    "inline_u_g2_2": PhysicsTerm("inline_u_g2_2", "Inline equation: u_g2", _compute_inline_u_g2_2),
    "inline_m_over_r2": PhysicsTerm("inline_m_over_r2", "Inline equation: m_over_r2", _compute_inline_m_over_r2),
    "inline_m_sun_1": PhysicsTerm("inline_m_sun_1", "Inline equation: m_sun", _compute_inline_m_sun_1),
    "inline_period_s_1": PhysicsTerm("inline_period_s_1", "Inline equation: period_s", _compute_inline_period_s_1),
    "inline_p_core": PhysicsTerm("inline_p_core", "Inline equation: p_core", _compute_inline_p_core),
    "inline_lambda_i_1": PhysicsTerm("inline_lambda_i_1", "Inline equation: lambda_i", _compute_inline_lambda_i_1),
    "inline_omega_s_t_6": PhysicsTerm("inline_omega_s_t_6", "Inline equation: omega_s_t", _compute_inline_omega_s_t_6),
    "inline_cos_pi_tn_1": PhysicsTerm("inline_cos_pi_tn_1", "Inline equation: cos_pi_tn", _compute_inline_cos_pi_tn_1),
    "inline_omega_9": PhysicsTerm("inline_omega_9", "Inline equation: omega", _compute_inline_omega_9),
    "inline_u_g3": PhysicsTerm("inline_u_g3", "Inline equation: u_g3", _compute_inline_u_g3),
    "inline_s": PhysicsTerm("inline_s", "Inline equation: s", _compute_inline_s),
    "inline_u_g2_3": PhysicsTerm("inline_u_g2_3", "Inline equation: u_g2", _compute_inline_u_g2_3),
    "inline_pert": PhysicsTerm("inline_pert", "Inline equation: pert", _compute_inline_pert),
    "inline_base_b": PhysicsTerm("inline_base_b", "Inline equation: base_b", _compute_inline_base_b),
    "inline_b_min": PhysicsTerm("inline_b_min", "Inline equation: b_min", _compute_inline_b_min),
    "inline_u_g3_1": PhysicsTerm("inline_u_g3_1", "Inline equation: u_g3", _compute_inline_u_g3_1),
    "inline_b_j_2": PhysicsTerm("inline_b_j_2", "Inline equation: b_j", _compute_inline_b_j_2),
    "inline_u_g3_2": PhysicsTerm("inline_u_g3_2", "Inline equation: u_g3", _compute_inline_u_g3_2),
    "inline_rho_product": PhysicsTerm("inline_rho_product", "Inline equation: rho_product", _compute_inline_rho_product),
    "inline_base_8": PhysicsTerm("inline_base_8", "Inline equation: base", _compute_inline_base_8),
    "inline_u_i_without": PhysicsTerm("inline_u_i_without", "Inline equation: u_i_without", _compute_inline_u_i_without),
    "inline_percent_increase_1": PhysicsTerm("inline_percent_increase_1", "Inline equation: percent_increase", _compute_inline_percent_increase_1),
    "inline_grad_ms_r": PhysicsTerm("inline_grad_ms_r", "Inline equation: grad_ms_r", _compute_inline_grad_ms_r),
    "inline_exp_term_2": PhysicsTerm("inline_exp_term_2", "Inline equation: exp_term", _compute_inline_exp_term_2),
    "inline_cos_tn": PhysicsTerm("inline_cos_tn", "Inline equation: cos_tn", _compute_inline_cos_tn),
    "inline_defect_factor": PhysicsTerm("inline_defect_factor", "Inline equation: defect_factor", _compute_inline_defect_factor),
    "inline_delta": PhysicsTerm("inline_delta", "Inline equation: delta", _compute_inline_delta),
    "inline_u_g1": PhysicsTerm("inline_u_g1", "Inline equation: u_g1", _compute_inline_u_g1),
    "inline_theta_2": PhysicsTerm("inline_theta_2", "Inline equation: theta", _compute_inline_theta_2),
    "inline_phi_mag": PhysicsTerm("inline_phi_mag", "Inline equation: phi_mag", _compute_inline_phi_mag),
    "inline_mag_1": PhysicsTerm("inline_mag_1", "Inline equation: mag", _compute_inline_mag_1),
    "inline_um_1": PhysicsTerm("inline_um_1", "Inline equation: um", _compute_inline_um_1),
    "inline_rho_5": PhysicsTerm("inline_rho_5", "Inline equation: rho", _compute_inline_rho_5),
    "inline_rho_6": PhysicsTerm("inline_rho_6", "Inline equation: rho", _compute_inline_rho_6),
    "inline_u_i": PhysicsTerm("inline_u_i", "Inline equation: u_i", _compute_inline_u_i),
    "inline_rho_sc_2": PhysicsTerm("inline_rho_sc_2", "Inline equation: rho_sc", _compute_inline_rho_sc_2),
    "inline_rho_7": PhysicsTerm("inline_rho_7", "Inline equation: rho", _compute_inline_rho_7),
    "inline_u_g2_4": PhysicsTerm("inline_u_g2_4", "Inline equation: u_g2", _compute_inline_u_g2_4),
    "inline_rho_ua_4": PhysicsTerm("inline_rho_ua_4", "Inline equation: rho_ua", _compute_inline_rho_ua_4),
    "inline_rho_8": PhysicsTerm("inline_rho_8", "Inline equation: rho", _compute_inline_rho_8),
    "inline_arg_5": PhysicsTerm("inline_arg_5", "Inline equation: arg", _compute_inline_arg_5),
    "inline_m_e_c2": PhysicsTerm("inline_m_e_c2", "Inline equation: m_e_c2", _compute_inline_m_e_c2),
    "inline_ratio_2": PhysicsTerm("inline_ratio_2", "Inline equation: ratio", _compute_inline_ratio_2),
    "inline_q_v_B": PhysicsTerm("inline_q_v_B", "Inline equation: q_v_B", _compute_inline_q_v_B),
    "inline_g_mu_B": PhysicsTerm("inline_g_mu_B", "Inline equation: g_mu_B", _compute_inline_g_mu_B),
    "inline_ratio_3": PhysicsTerm("inline_ratio_3", "Inline equation: ratio", _compute_inline_ratio_3),
    "inline_dx_10": PhysicsTerm("inline_dx_10", "Inline equation: dx", _compute_inline_dx_10),
    "inline_weight": PhysicsTerm("inline_weight", "Inline equation: weight", _compute_inline_weight),
    "inline_force": PhysicsTerm("inline_force", "Inline equation: force", _compute_inline_force),
    "inline_g_4": PhysicsTerm("inline_g_4", "Inline equation: g", _compute_inline_g_4),
    "inline_omega0_1": PhysicsTerm("inline_omega0_1", "Inline equation: omega0", _compute_inline_omega0_1),
    "inline_k_10": PhysicsTerm("inline_k_10", "Inline equation: k", _compute_inline_k_10),
    "inline_omegaL": PhysicsTerm("inline_omegaL", "Inline equation: omegaL", _compute_inline_omegaL),
    "inline_cos_theta_1": PhysicsTerm("inline_cos_theta_1", "Inline equation: cos_theta", _compute_inline_cos_theta_1),
    "inline_sin_theta": PhysicsTerm("inline_sin_theta", "Inline equation: sin_theta", _compute_inline_sin_theta),
    "inline_cos_act": PhysicsTerm("inline_cos_act", "Inline equation: cos_act", _compute_inline_cos_act),
    "inline_term_mom_2": PhysicsTerm("inline_term_mom_2", "Inline equation: term_mom", _compute_inline_term_mom_2),
    "inline_term_grav_2": PhysicsTerm("inline_term_grav_2", "Inline equation: term_grav", _compute_inline_term_grav_2),
    "inline_term_vac_1": PhysicsTerm("inline_term_vac_1", "Inline equation: term_vac", _compute_inline_term_vac_1),
    "inline_term_act": PhysicsTerm("inline_term_act", "Inline equation: term_act", _compute_inline_term_act),
    "inline_term_DE": PhysicsTerm("inline_term_DE", "Inline equation: term_DE", _compute_inline_term_DE),
    "inline_term_res_2": PhysicsTerm("inline_term_res_2", "Inline equation: term_res", _compute_inline_term_res_2),
    "inline_term_neut_1": PhysicsTerm("inline_term_neut_1", "Inline equation: term_neut", _compute_inline_term_neut_1),
    "inline_term_rel_2": PhysicsTerm("inline_term_rel_2", "Inline equation: term_rel", _compute_inline_term_rel_2),
    "inline_term_neutrino": PhysicsTerm("inline_term_neutrino", "Inline equation: term_neutrino", _compute_inline_term_neutrino),
    "inline_disc": PhysicsTerm("inline_disc", "Inline equation: disc", _compute_inline_disc),
    "inline_integ_1": PhysicsTerm("inline_integ_1", "Inline equation: integ", _compute_inline_integ_1),
    "inline_beta_2": PhysicsTerm("inline_beta_2", "Inline equation: beta", _compute_inline_beta_2),
    "inline_V_5": PhysicsTerm("inline_V_5", "Inline equation: V", _compute_inline_V_5),
    "inline_rho_9": PhysicsTerm("inline_rho_9", "Inline equation: rho", _compute_inline_rho_9),
    "inline_a_7": PhysicsTerm("inline_a_7", "Inline equation: a", _compute_inline_a_7),
    "inline_tn_3": PhysicsTerm("inline_tn_3", "Inline equation: tn", _compute_inline_tn_3),
    "inline_lambda_4": PhysicsTerm("inline_lambda_4", "Inline equation: lambda", _compute_inline_lambda_4),
    "inline_omega_s_4": PhysicsTerm("inline_omega_s_4", "Inline equation: omega_s", _compute_inline_omega_s_4),
    "inline_G_val": PhysicsTerm("inline_G_val", "Inline equation: G_val", _compute_inline_G_val),
    "inline_M_val": PhysicsTerm("inline_M_val", "Inline equation: M_val", _compute_inline_M_val),
    "inline_rho_10": PhysicsTerm("inline_rho_10", "Inline equation: rho", _compute_inline_rho_10),
    "inline_r_val": PhysicsTerm("inline_r_val", "Inline equation: r_val", _compute_inline_r_val),
    "inline_kB": PhysicsTerm("inline_kB", "Inline equation: kB", _compute_inline_kB),
    "inline_m_e_val": PhysicsTerm("inline_m_e_val", "Inline equation: m_e_val", _compute_inline_m_e_val),
    "inline_c_val_1": PhysicsTerm("inline_c_val_1", "Inline equation: c_val", _compute_inline_c_val_1),
    "inline_term1_9": PhysicsTerm("inline_term1_9", "Inline equation: term1", _compute_inline_term1_9),
    "inline_term2_7": PhysicsTerm("inline_term2_7", "Inline equation: term2", _compute_inline_term2_7),
    "inline_term3_4": PhysicsTerm("inline_term3_4", "Inline equation: term3", _compute_inline_term3_4),
    "inline_mu0_val": PhysicsTerm("inline_mu0_val", "Inline equation: mu0_val", _compute_inline_mu0_val),
    "inline_B_val_1": PhysicsTerm("inline_B_val_1", "Inline equation: B_val", _compute_inline_B_val_1),
    "inline_dpm_res_1": PhysicsTerm("inline_dpm_res_1", "Inline equation: dpm_res", _compute_inline_dpm_res_1),
    "inline_term1_10": PhysicsTerm("inline_term1_10", "Inline equation: term1", _compute_inline_term1_10),
    "inline_term2_8": PhysicsTerm("inline_term2_8", "Inline equation: term2", _compute_inline_term2_8),
    "inline_F": PhysicsTerm("inline_F", "Inline equation: F", _compute_inline_F),
    "inline_term_res_3": PhysicsTerm("inline_term_res_3", "Inline equation: term_res", _compute_inline_term_res_3),
    "inline_term1_11": PhysicsTerm("inline_term1_11", "Inline equation: term1", _compute_inline_term1_11),
    "inline_term2_9": PhysicsTerm("inline_term2_9", "Inline equation: term2", _compute_inline_term2_9),
    "inline_disc_1": PhysicsTerm("inline_disc_1", "Inline equation: disc", _compute_inline_disc_1),
    "inline_cos_term_6": PhysicsTerm("inline_cos_term_6", "Inline equation: cos_term", _compute_inline_cos_term_6),
    "inline_delta_1": PhysicsTerm("inline_delta_1", "Inline equation: delta", _compute_inline_delta_1),
    "inline_sensitivity": PhysicsTerm("inline_sensitivity", "Inline equation: sensitivity", _compute_inline_sensitivity),
    "inline_t_current": PhysicsTerm("inline_t_current", "Inline equation: t_current", _compute_inline_t_current),
    "inline_F_total": PhysicsTerm("inline_F_total", "Inline equation: F_total", _compute_inline_F_total),
    "inline_F_compressed": PhysicsTerm("inline_F_compressed", "Inline equation: F_compressed", _compute_inline_F_compressed),
    "inline_DPM_res_1": PhysicsTerm("inline_DPM_res_1", "Inline equation: DPM_res", _compute_inline_DPM_res_1),
    "inline_Ub1": PhysicsTerm("inline_Ub1", "Inline equation: Ub1", _compute_inline_Ub1),
    "inline_Ui_1": PhysicsTerm("inline_Ui_1", "Inline equation: Ui", _compute_inline_Ui_1),
    "inline_g_comp": PhysicsTerm("inline_g_comp", "Inline equation: g_comp", _compute_inline_g_comp),
    "inline_Q_wave": PhysicsTerm("inline_Q_wave", "Inline equation: Q_wave", _compute_inline_Q_wave),
    "inline_M_val_1": PhysicsTerm("inline_M_val_1", "Inline equation: M_val", _compute_inline_M_val_1),
    "inline_r_val_1": PhysicsTerm("inline_r_val_1", "Inline equation: r_val", _compute_inline_r_val_1),
    "inline_L_X_val": PhysicsTerm("inline_L_X_val", "Inline equation: L_X_val", _compute_inline_L_X_val),
    "inline_L_X_val_1": PhysicsTerm("inline_L_X_val_1", "Inline equation: L_X_val", _compute_inline_L_X_val_1),
    "inline_F_approach": PhysicsTerm("inline_F_approach", "Inline equation: F_approach", _compute_inline_F_approach),
    "inline_F_starburst": PhysicsTerm("inline_F_starburst", "Inline equation: F_starburst", _compute_inline_F_starburst),
    "inline_F_tidal_2": PhysicsTerm("inline_F_tidal_2", "Inline equation: F_tidal", _compute_inline_F_tidal_2),
    "inline_F_quiet": PhysicsTerm("inline_F_quiet", "Inline equation: F_quiet", _compute_inline_F_quiet),
    "inline_M_var": PhysicsTerm("inline_M_var", "Inline equation: M_var", _compute_inline_M_var),
    "inline_V_var": PhysicsTerm("inline_V_var", "Inline equation: V_var", _compute_inline_V_var),
    "inline_F_1": PhysicsTerm("inline_F_1", "Inline equation: F", _compute_inline_F_1),
    "inline_F_before_evolution": PhysicsTerm("inline_F_before_evolution", "Inline equation: F_before_evolution", _compute_inline_F_before_evolution),
    "inline_F_after_evolution": PhysicsTerm("inline_F_after_evolution", "Inline equation: F_after_evolution", _compute_inline_F_after_evolution),
    "inline_rho_vac_4": PhysicsTerm("inline_rho_vac_4", "Inline equation: rho_vac", _compute_inline_rho_vac_4),
    "inline_rho_A_2": PhysicsTerm("inline_rho_A_2", "Inline equation: rho_A", _compute_inline_rho_A_2),
    "inline_base_buoyancy": PhysicsTerm("inline_base_buoyancy", "Inline equation: base_buoyancy", _compute_inline_base_buoyancy),
    "inline_rho_ICM": PhysicsTerm("inline_rho_ICM", "Inline equation: rho_ICM", _compute_inline_rho_ICM),
    "inline_pressure_term": PhysicsTerm("inline_pressure_term", "Inline equation: pressure_term", _compute_inline_pressure_term),
    "inline_radio_enhancement": PhysicsTerm("inline_radio_enhancement", "Inline equation: radio_enhancement", _compute_inline_radio_enhancement),
    "inline_merger_factor": PhysicsTerm("inline_merger_factor", "Inline equation: merger_factor", _compute_inline_merger_factor),
    "inline_dm_factor_3": PhysicsTerm("inline_dm_factor_3", "Inline equation: dm_factor", _compute_inline_dm_factor_3),
    "inline_relativistic_pressure": PhysicsTerm("inline_relativistic_pressure", "Inline equation: relativistic_pressure", _compute_inline_relativistic_pressure),
    "inline_pi_val": PhysicsTerm("inline_pi_val", "Inline equation: pi_val", _compute_inline_pi_val),
    "inline_magnetic_energy": PhysicsTerm("inline_magnetic_energy", "Inline equation: magnetic_energy", _compute_inline_magnetic_energy),
    "inline_turbulence_factor": PhysicsTerm("inline_turbulence_factor", "Inline equation: turbulence_factor", _compute_inline_turbulence_factor),
    "inline_time_evolution": PhysicsTerm("inline_time_evolution", "Inline equation: time_evolution", _compute_inline_time_evolution),
    "inline_synchrotron_scaling": PhysicsTerm("inline_synchrotron_scaling", "Inline equation: synchrotron_scaling", _compute_inline_synchrotron_scaling),
    "inline_shock_acceleration": PhysicsTerm("inline_shock_acceleration", "Inline equation: shock_acceleration", _compute_inline_shock_acceleration),
    "inline_dm_coupling": PhysicsTerm("inline_dm_coupling", "Inline equation: dm_coupling", _compute_inline_dm_coupling),
    "inline_base_ui": PhysicsTerm("inline_base_ui", "Inline equation: base_ui", _compute_inline_base_ui),
    "inline_cluster_enhancement": PhysicsTerm("inline_cluster_enhancement", "Inline equation: cluster_enhancement", _compute_inline_cluster_enhancement),
    "inline_radio_coupling": PhysicsTerm("inline_radio_coupling", "Inline equation: radio_coupling", _compute_inline_radio_coupling),
    "inline_current_value": PhysicsTerm("inline_current_value", "Inline equation: current_value", _compute_inline_current_value),
    "inline_error_3": PhysicsTerm("inline_error_3", "Inline equation: error", _compute_inline_error_3),
    "inline_adjustment_2": PhysicsTerm("inline_adjustment_2", "Inline equation: adjustment", _compute_inline_adjustment_2),
    "inline_evolution_factor_1": PhysicsTerm("inline_evolution_factor_1", "Inline equation: evolution_factor", _compute_inline_evolution_factor_1),
    "inline_T_ICM_factor": PhysicsTerm("inline_T_ICM_factor", "Inline equation: T_ICM_factor", _compute_inline_T_ICM_factor),
    "inline_scaling": PhysicsTerm("inline_scaling", "Inline equation: scaling", _compute_inline_scaling),
    "inline_real_part": PhysicsTerm("inline_real_part", "Inline equation: real_part", _compute_inline_real_part),
    "inline_imag_part": PhysicsTerm("inline_imag_part", "Inline equation: imag_part", _compute_inline_imag_part),
    "inline_delta_2": PhysicsTerm("inline_delta_2", "Inline equation: delta", _compute_inline_delta_2),
    "inline_new_target": PhysicsTerm("inline_new_target", "Inline equation: new_target", _compute_inline_new_target),
    "inline_delta_pair_1": PhysicsTerm("inline_delta_pair_1", "Inline equation: delta_pair", _compute_inline_delta_pair_1),
    "inline_S_scale_1": PhysicsTerm("inline_S_scale_1", "Inline equation: S_scale", _compute_inline_S_scale_1),
    "inline_f_res_3": PhysicsTerm("inline_f_res_3", "Inline equation: f_res", _compute_inline_f_res_3),
    "inline_U_dp_2": PhysicsTerm("inline_U_dp_2", "Inline equation: U_dp", _compute_inline_U_dp_2),
    "inline_k_nuc_2": PhysicsTerm("inline_k_nuc_2", "Inline equation: k_nuc", _compute_inline_k_nuc_2),
    "inline_S_shell_2": PhysicsTerm("inline_S_shell_2", "Inline equation: S_shell", _compute_inline_S_shell_2),
    "inline_sin_term_1": PhysicsTerm("inline_sin_term_1", "Inline equation: sin_term", _compute_inline_sin_term_1),
    "inline_term2_10": PhysicsTerm("inline_term2_10", "Inline equation: term2", _compute_inline_term2_10),
    "inline_integ_2": PhysicsTerm("inline_integ_2", "Inline equation: integ", _compute_inline_integ_2),
    "inline_M_val_2": PhysicsTerm("inline_M_val_2", "Inline equation: M_val", _compute_inline_M_val_2),
    "inline_r_val_2": PhysicsTerm("inline_r_val_2", "Inline equation: r_val", _compute_inline_r_val_2),
    "inline_kB_val": PhysicsTerm("inline_kB_val", "Inline equation: kB_val", _compute_inline_kB_val),
    "inline_term2_11": PhysicsTerm("inline_term2_11", "Inline equation: term2", _compute_inline_term2_11),
    "inline_r_nucleus": PhysicsTerm("inline_r_nucleus", "Inline equation: r_nucleus", _compute_inline_r_nucleus),
    "inline_volume_nuclear": PhysicsTerm("inline_volume_nuclear", "Inline equation: volume_nuclear", _compute_inline_volume_nuclear),
    "inline_binding_contribution": PhysicsTerm("inline_binding_contribution", "Inline equation: binding_contribution", _compute_inline_binding_contribution),
    "inline_pairing_term": PhysicsTerm("inline_pairing_term", "Inline equation: pairing_term", _compute_inline_pairing_term),
    "inline_range_factor_1": PhysicsTerm("inline_range_factor_1", "Inline equation: range_factor", _compute_inline_range_factor_1),
    "inline_phase_3": PhysicsTerm("inline_phase_3", "Inline equation: phase", _compute_inline_phase_3),
    "inline_quantum_coherence": PhysicsTerm("inline_quantum_coherence", "Inline equation: quantum_coherence", _compute_inline_quantum_coherence),
    "inline_barrier_height": PhysicsTerm("inline_barrier_height", "Inline equation: barrier_height", _compute_inline_barrier_height),
    "inline_tunneling_factor": PhysicsTerm("inline_tunneling_factor", "Inline equation: tunneling_factor", _compute_inline_tunneling_factor),
    "inline_spin_coupling": PhysicsTerm("inline_spin_coupling", "Inline equation: spin_coupling", _compute_inline_spin_coupling),
    "inline_U_dp_3": PhysicsTerm("inline_U_dp_3", "Inline equation: U_dp", _compute_inline_U_dp_3),
    "inline_evolution_factor_2": PhysicsTerm("inline_evolution_factor_2", "Inline equation: evolution_factor", _compute_inline_evolution_factor_2),
    "inline_shell_evolution": PhysicsTerm("inline_shell_evolution", "Inline equation: shell_evolution", _compute_inline_shell_evolution),
    "inline_scaling_1": PhysicsTerm("inline_scaling_1", "Inline equation: scaling", _compute_inline_scaling_1),
    "inline_Z_mag": PhysicsTerm("inline_Z_mag", "Inline equation: Z_mag", _compute_inline_Z_mag),
    "inline_N_mag": PhysicsTerm("inline_N_mag", "Inline equation: N_mag", _compute_inline_N_mag),
    "inline_pair_effect": PhysicsTerm("inline_pair_effect", "Inline equation: pair_effect", _compute_inline_pair_effect),
    "inline_delta_3": PhysicsTerm("inline_delta_3", "Inline equation: delta", _compute_inline_delta_3),
    "inline_new_target_1": PhysicsTerm("inline_new_target_1", "Inline equation: new_target", _compute_inline_new_target_1),
    "inline_current_value_1": PhysicsTerm("inline_current_value_1", "Inline equation: current_value", _compute_inline_current_value_1),
    "inline_cycle_phase_1": PhysicsTerm("inline_cycle_phase_1", "Inline equation: cycle_phase", _compute_inline_cycle_phase_1),
    "inline_cycle_factor_1": PhysicsTerm("inline_cycle_factor_1", "Inline equation: cycle_factor", _compute_inline_cycle_factor_1),
    "inline_diffusion_decay": PhysicsTerm("inline_diffusion_decay", "Inline equation: diffusion_decay", _compute_inline_diffusion_decay),
    "inline_convection_enhancement": PhysicsTerm("inline_convection_enhancement", "Inline equation: convection_enhancement", _compute_inline_convection_enhancement),
    "inline_scaling_2": PhysicsTerm("inline_scaling_2", "Inline equation: scaling", _compute_inline_scaling_2),
    "inline_term_LENR": PhysicsTerm("inline_term_LENR", "Inline equation: term_LENR", _compute_inline_term_LENR),
    "inline_term_res_4": PhysicsTerm("inline_term_res_4", "Inline equation: term_res", _compute_inline_term_res_4),
    "inline_term_sweet": PhysicsTerm("inline_term_sweet", "Inline equation: term_sweet", _compute_inline_term_sweet),
    "inline_term_kozima": PhysicsTerm("inline_term_kozima", "Inline equation: term_kozima", _compute_inline_term_kozima),
    "inline_integ_3": PhysicsTerm("inline_integ_3", "Inline equation: integ", _compute_inline_integ_3),
    "inline_dpm_res_2": PhysicsTerm("inline_dpm_res_2", "Inline equation: dpm_res", _compute_inline_dpm_res_2),
    "inline_vacuum_term": PhysicsTerm("inline_vacuum_term", "Inline equation: vacuum_term", _compute_inline_vacuum_term),
    "inline_obs_scaling": PhysicsTerm("inline_obs_scaling", "Inline equation: obs_scaling", _compute_inline_obs_scaling),
    "inline_mass_scale": PhysicsTerm("inline_mass_scale", "Inline equation: mass_scale", _compute_inline_mass_scale),
    "inline_luminosity_factor": PhysicsTerm("inline_luminosity_factor", "Inline equation: luminosity_factor", _compute_inline_luminosity_factor),
    "inline_enhancement": PhysicsTerm("inline_enhancement", "Inline equation: enhancement", _compute_inline_enhancement),
    "inline_evolution_timescale": PhysicsTerm("inline_evolution_timescale", "Inline equation: evolution_timescale", _compute_inline_evolution_timescale),
    "inline_omega_10": PhysicsTerm("inline_omega_10", "Inline equation: omega", _compute_inline_omega_10),
    "inline_new_target_2": PhysicsTerm("inline_new_target_2", "Inline equation: new_target", _compute_inline_new_target_2),
    "inline_integrand_13": PhysicsTerm("inline_integrand_13", "Inline equation: integrand", _compute_inline_integrand_13),
    "inline_momentum_term_1": PhysicsTerm("inline_momentum_term_1", "Inline equation: momentum_term", _compute_inline_momentum_term_1),
    "inline_gravity_term_1": PhysicsTerm("inline_gravity_term_1", "Inline equation: gravity_term", _compute_inline_gravity_term_1),
    "inline_f_bi": PhysicsTerm("inline_f_bi", "Inline equation: f_bi", _compute_inline_f_bi),
    "inline_f_lenr_1": PhysicsTerm("inline_f_lenr_1", "Inline equation: f_lenr", _compute_inline_f_lenr_1),
    "inline_f_act_1": PhysicsTerm("inline_f_act_1", "Inline equation: f_act", _compute_inline_f_act_1),
    "inline_f_de_1": PhysicsTerm("inline_f_de_1", "Inline equation: f_de", _compute_inline_f_de_1),
    "inline_f_neutron_1": PhysicsTerm("inline_f_neutron_1", "Inline equation: f_neutron", _compute_inline_f_neutron_1),
    "inline_f_rel": PhysicsTerm("inline_f_rel", "Inline equation: f_rel", _compute_inline_f_rel),
    "inline_f_neutrino": PhysicsTerm("inline_f_neutrino", "Inline equation: f_neutrino", _compute_inline_f_neutrino),
    "inline_f_res_4": PhysicsTerm("inline_f_res_4", "Inline equation: f_res", _compute_inline_f_res_4),
    "inline_f_relativ": PhysicsTerm("inline_f_relativ", "Inline equation: f_relativ", _compute_inline_f_relativ),
    "inline_f_sweet": PhysicsTerm("inline_f_sweet", "Inline equation: f_sweet", _compute_inline_f_sweet),
    "inline_f_kozima": PhysicsTerm("inline_f_kozima", "Inline equation: f_kozima", _compute_inline_f_kozima),
    "inline_vac_term_1": PhysicsTerm("inline_vac_term_1", "Inline equation: vac_term", _compute_inline_vac_term_1),
    "inline_integrand_14": PhysicsTerm("inline_integrand_14", "Inline equation: integrand", _compute_inline_integrand_14),
    "inline_g_lande": PhysicsTerm("inline_g_lande", "Inline equation: g_lande", _compute_inline_g_lande),
    "inline_mu_b": PhysicsTerm("inline_mu_b", "Inline equation: mu_b", _compute_inline_mu_b),
    "inline_b0": PhysicsTerm("inline_b0", "Inline equation: b0", _compute_inline_b0),
    "inline_hbar_omega0": PhysicsTerm("inline_hbar_omega0", "Inline equation: hbar_omega0", _compute_inline_hbar_omega0),
    "inline_r_real": PhysicsTerm("inline_r_real", "Inline equation: r_real", _compute_inline_r_real),
    "inline_t_val": PhysicsTerm("inline_t_val", "Inline equation: t_val", _compute_inline_t_val),
    "inline_m": PhysicsTerm("inline_m", "Inline equation: m", _compute_inline_m),
    "inline_term1_num": PhysicsTerm("inline_term1_num", "Inline equation: term1_num", _compute_inline_term1_num),
    "inline_term1_denom": PhysicsTerm("inline_term1_denom", "Inline equation: term1_denom", _compute_inline_term1_denom),
    "inline_term1_12": PhysicsTerm("inline_term1_12", "Inline equation: term1", _compute_inline_term1_12),
    "inline_term2_12": PhysicsTerm("inline_term2_12", "Inline equation: term2", _compute_inline_term2_12),
    "inline_term3_5": PhysicsTerm("inline_term3_5", "Inline equation: term3", _compute_inline_term3_5),
    "inline_a_8": PhysicsTerm("inline_a_8", "Inline equation: a", _compute_inline_a_8),
    "inline_term_b1": PhysicsTerm("inline_term_b1", "Inline equation: term_b1", _compute_inline_term_b1),
    "inline_term_b3": PhysicsTerm("inline_term_b3", "Inline equation: term_b3", _compute_inline_term_b3),
    "inline_b_5": PhysicsTerm("inline_b_5", "Inline equation: b", _compute_inline_b_5),
    "inline_term_c1": PhysicsTerm("inline_term_c1", "Inline equation: term_c1", _compute_inline_term_c1),
    "inline_term_c2": PhysicsTerm("inline_term_c2", "Inline equation: term_c2", _compute_inline_term_c2),
    "inline_term_c3": PhysicsTerm("inline_term_c3", "Inline equation: term_c3", _compute_inline_term_c3),
    "inline_c_3": PhysicsTerm("inline_c_3", "Inline equation: c", _compute_inline_c_3),
    "inline_disc_2": PhysicsTerm("inline_disc_2", "Inline equation: disc", _compute_inline_disc_2),
    "inline_sqrt_disc": PhysicsTerm("inline_sqrt_disc", "Inline equation: sqrt_disc", _compute_inline_sqrt_disc),
    "inline_omega0_real": PhysicsTerm("inline_omega0_real", "Inline equation: omega0_real", _compute_inline_omega0_real),
    "inline_sigma_CNB": PhysicsTerm("inline_sigma_CNB", "Inline equation: sigma_CNB", _compute_inline_sigma_CNB),
    "inline_CNB_coupling": PhysicsTerm("inline_CNB_coupling", "Inline equation: CNB_coupling", _compute_inline_CNB_coupling),
    "inline_base_buoyancy_1": PhysicsTerm("inline_base_buoyancy_1", "Inline equation: base_buoyancy", _compute_inline_base_buoyancy_1),
    "inline_enhancement_1": PhysicsTerm("inline_enhancement_1", "Inline equation: enhancement", _compute_inline_enhancement_1),
    "inline_scaling_3": PhysicsTerm("inline_scaling_3", "Inline equation: scaling", _compute_inline_scaling_3),
    "inline_k_neutrino": PhysicsTerm("inline_k_neutrino", "Inline equation: k_neutrino", _compute_inline_k_neutrino),
    "inline_coupling_4": PhysicsTerm("inline_coupling_4", "Inline equation: coupling", _compute_inline_coupling_4),
    "inline_omega_11": PhysicsTerm("inline_omega_11", "Inline equation: omega", _compute_inline_omega_11),
    "inline_coherence": PhysicsTerm("inline_coherence", "Inline equation: coherence", _compute_inline_coherence),
    "inline_mass_scale_1": PhysicsTerm("inline_mass_scale_1", "Inline equation: mass_scale", _compute_inline_mass_scale_1),
    "inline_cnb_oscillation": PhysicsTerm("inline_cnb_oscillation", "Inline equation: cnb_oscillation", _compute_inline_cnb_oscillation),
    "inline_thermal_factor": PhysicsTerm("inline_thermal_factor", "Inline equation: thermal_factor", _compute_inline_thermal_factor),
    "inline_base_b_1": PhysicsTerm("inline_base_b_1", "Inline equation: base_b", _compute_inline_base_b_1),
    "inline_coupling_strength": PhysicsTerm("inline_coupling_strength", "Inline equation: coupling_strength", _compute_inline_coupling_strength),
    "inline_density_factor": PhysicsTerm("inline_density_factor", "Inline equation: density_factor", _compute_inline_density_factor),
    "inline_temperature_factor": PhysicsTerm("inline_temperature_factor", "Inline equation: temperature_factor", _compute_inline_temperature_factor),
    "inline_cnb_enhancement": PhysicsTerm("inline_cnb_enhancement", "Inline equation: cnb_enhancement", _compute_inline_cnb_enhancement),
    "inline_evolution_factor_3": PhysicsTerm("inline_evolution_factor_3", "Inline equation: evolution_factor", _compute_inline_evolution_factor_3),
    "inline_cnb_factor": PhysicsTerm("inline_cnb_factor", "Inline equation: cnb_factor", _compute_inline_cnb_factor),
    "inline_diffusion_decay_1": PhysicsTerm("inline_diffusion_decay_1", "Inline equation: diffusion_decay", _compute_inline_diffusion_decay_1),
    "inline_convection_enhancement_1": PhysicsTerm("inline_convection_enhancement_1", "Inline equation: convection_enhancement", _compute_inline_convection_enhancement_1),
    "inline_scaling_4": PhysicsTerm("inline_scaling_4", "Inline equation: scaling", _compute_inline_scaling_4),
    "inline_new_target_3": PhysicsTerm("inline_new_target_3", "Inline equation: new_target", _compute_inline_new_target_3),
    "inline_base_Ub1": PhysicsTerm("inline_base_Ub1", "Inline equation: base_Ub1", _compute_inline_base_Ub1),
    "inline_time_modulation": PhysicsTerm("inline_time_modulation", "Inline equation: time_modulation", _compute_inline_time_modulation),
    "inline_distance_factor": PhysicsTerm("inline_distance_factor", "Inline equation: distance_factor", _compute_inline_distance_factor),
    "inline_cluster_factor": PhysicsTerm("inline_cluster_factor", "Inline equation: cluster_factor", _compute_inline_cluster_factor),
    "inline_sf_enhancement": PhysicsTerm("inline_sf_enhancement", "Inline equation: sf_enhancement", _compute_inline_sf_enhancement),
    "inline_spiral_factor": PhysicsTerm("inline_spiral_factor", "Inline equation: spiral_factor", _compute_inline_spiral_factor),
    "inline_base_Ui": PhysicsTerm("inline_base_Ui", "Inline equation: base_Ui", _compute_inline_base_Ui),
    "inline_coupling_strength_1": PhysicsTerm("inline_coupling_strength_1", "Inline equation: coupling_strength", _compute_inline_coupling_strength_1),
    "inline_base_frequency": PhysicsTerm("inline_base_frequency", "Inline equation: base_frequency", _compute_inline_base_frequency),
    "inline_base_X2": PhysicsTerm("inline_base_X2", "Inline equation: base_X2", _compute_inline_base_X2),
    "inline_base_Q": PhysicsTerm("inline_base_Q", "Inline equation: base_Q", _compute_inline_base_Q),
    "inline_distance_modulation": PhysicsTerm("inline_distance_modulation", "Inline equation: distance_modulation", _compute_inline_distance_modulation),
    "inline_cluster_wave": PhysicsTerm("inline_cluster_wave", "Inline equation: cluster_wave", _compute_inline_cluster_wave),
    "inline_obs_oscillation": PhysicsTerm("inline_obs_oscillation", "Inline equation: obs_oscillation", _compute_inline_obs_oscillation),
    "inline_thermal_factor_1": PhysicsTerm("inline_thermal_factor_1", "Inline equation: thermal_factor", _compute_inline_thermal_factor_1),
    "inline_base_b_2": PhysicsTerm("inline_base_b_2", "Inline equation: base_b", _compute_inline_base_b_2),
    "inline_coupling_strength_2": PhysicsTerm("inline_coupling_strength_2", "Inline equation: coupling_strength", _compute_inline_coupling_strength_2),
    "inline_spectral_factor": PhysicsTerm("inline_spectral_factor", "Inline equation: spectral_factor", _compute_inline_spectral_factor),
    "inline_flux_factor": PhysicsTerm("inline_flux_factor", "Inline equation: flux_factor", _compute_inline_flux_factor),
    "inline_obs_enhancement": PhysicsTerm("inline_obs_enhancement", "Inline equation: obs_enhancement", _compute_inline_obs_enhancement),
    "inline_obs_factor": PhysicsTerm("inline_obs_factor", "Inline equation: obs_factor", _compute_inline_obs_factor),
    "inline_frequency_enhancement": PhysicsTerm("inline_frequency_enhancement", "Inline equation: frequency_enhancement", _compute_inline_frequency_enhancement),
    "inline_scaling_5": PhysicsTerm("inline_scaling_5", "Inline equation: scaling", _compute_inline_scaling_5),
    "inline_freq_factor": PhysicsTerm("inline_freq_factor", "Inline equation: freq_factor", _compute_inline_freq_factor),
    "inline_freq_factor_1": PhysicsTerm("inline_freq_factor_1", "Inline equation: freq_factor", _compute_inline_freq_factor_1),
    "inline_stellar_oscillation": PhysicsTerm("inline_stellar_oscillation", "Inline equation: stellar_oscillation", _compute_inline_stellar_oscillation),
    "inline_rotation_factor": PhysicsTerm("inline_rotation_factor", "Inline equation: rotation_factor", _compute_inline_rotation_factor),
    "inline_nuclear_factor": PhysicsTerm("inline_nuclear_factor", "Inline equation: nuclear_factor", _compute_inline_nuclear_factor),
    "inline_flare_factor": PhysicsTerm("inline_flare_factor", "Inline equation: flare_factor", _compute_inline_flare_factor),
    "inline_thermal_factor_2": PhysicsTerm("inline_thermal_factor_2", "Inline equation: thermal_factor", _compute_inline_thermal_factor_2),
    "inline_wind_factor": PhysicsTerm("inline_wind_factor", "Inline equation: wind_factor", _compute_inline_wind_factor),
    "inline_base_b_3": PhysicsTerm("inline_base_b_3", "Inline equation: base_b", _compute_inline_base_b_3),
    "inline_rotation_factor_1": PhysicsTerm("inline_rotation_factor_1", "Inline equation: rotation_factor", _compute_inline_rotation_factor_1),
    "inline_nuclear_factor_1": PhysicsTerm("inline_nuclear_factor_1", "Inline equation: nuclear_factor", _compute_inline_nuclear_factor_1),
    "inline_wind_factor_1": PhysicsTerm("inline_wind_factor_1", "Inline equation: wind_factor", _compute_inline_wind_factor_1),
    "inline_thermal_gradient": PhysicsTerm("inline_thermal_gradient", "Inline equation: thermal_gradient", _compute_inline_thermal_gradient),
    "inline_stellar_enhancement": PhysicsTerm("inline_stellar_enhancement", "Inline equation: stellar_enhancement", _compute_inline_stellar_enhancement),
    "inline_flare_factor_1": PhysicsTerm("inline_flare_factor_1", "Inline equation: flare_factor", _compute_inline_flare_factor_1),
    "inline_cme_factor": PhysicsTerm("inline_cme_factor", "Inline equation: cme_factor", _compute_inline_cme_factor),
    "inline_stellar_factor": PhysicsTerm("inline_stellar_factor", "Inline equation: stellar_factor", _compute_inline_stellar_factor),
    "inline_rotation_decay": PhysicsTerm("inline_rotation_decay", "Inline equation: rotation_decay", _compute_inline_rotation_decay),
    "inline_scaling_6": PhysicsTerm("inline_scaling_6", "Inline equation: scaling", _compute_inline_scaling_6),
    "inline_f_neutrino_1": PhysicsTerm("inline_f_neutrino_1", "Inline equation: f_neutrino", _compute_inline_f_neutrino_1),
    "inline_integrand_15": PhysicsTerm("inline_integrand_15", "Inline equation: integrand", _compute_inline_integrand_15),
    "inline_result_7": PhysicsTerm("inline_result_7", "Inline equation: result", _compute_inline_result_7),
    "inline_term_res_5": PhysicsTerm("inline_term_res_5", "Inline equation: term_res", _compute_inline_term_res_5),
    "inline_term_rel_3": PhysicsTerm("inline_term_rel_3", "Inline equation: term_rel", _compute_inline_term_rel_3),
    "inline_disc_3": PhysicsTerm("inline_disc_3", "Inline equation: disc", _compute_inline_disc_3),
    "inline_gravity_compressed_1": PhysicsTerm("inline_gravity_compressed_1", "Inline equation: gravity_compressed", _compute_inline_gravity_compressed_1),
    "inline_resonance_1": PhysicsTerm("inline_resonance_1", "Inline equation: resonance", _compute_inline_resonance_1),
    "inline_buoyancy_3": PhysicsTerm("inline_buoyancy_3", "Inline equation: buoyancy", _compute_inline_buoyancy_3),
    "inline_result_8": PhysicsTerm("inline_result_8", "Inline equation: result", _compute_inline_result_8),
    "inline_T_val": PhysicsTerm("inline_T_val", "Inline equation: T_val", _compute_inline_T_val),
    "inline_term3_6": PhysicsTerm("inline_term3_6", "Inline equation: term3", _compute_inline_term3_6),
    "inline_sum_real": PhysicsTerm("inline_sum_real", "Inline equation: sum_real", _compute_inline_sum_real),
    "inline_min_r": PhysicsTerm("inline_min_r", "Inline equation: min_r", _compute_inline_min_r),
    "inline_max_r": PhysicsTerm("inline_max_r", "Inline equation: max_r", _compute_inline_max_r),
    "inline_var_real": PhysicsTerm("inline_var_real", "Inline equation: var_real", _compute_inline_var_real),
    "inline_F1": PhysicsTerm("inline_F1", "Inline equation: F1", _compute_inline_F1),
    "inline_F2": PhysicsTerm("inline_F2", "Inline equation: F2", _compute_inline_F2),
    "inline_dpm_mom_update": PhysicsTerm("inline_dpm_mom_update", "Inline equation: dpm_mom_update", _compute_inline_dpm_mom_update),
    "inline_M_dot_4": PhysicsTerm("inline_M_dot_4", "Inline equation: M_dot", _compute_inline_M_dot_4),
    "inline_L_Edd": PhysicsTerm("inline_L_Edd", "Inline equation: L_Edd", _compute_inline_L_Edd),
    "inline_L_acc": PhysicsTerm("inline_L_acc", "Inline equation: L_acc", _compute_inline_L_acc),
    "inline_dt_norm": PhysicsTerm("inline_dt_norm", "Inline equation: dt_norm", _compute_inline_dt_norm),
    "inline_L_peak": PhysicsTerm("inline_L_peak", "Inline equation: L_peak", _compute_inline_L_peak),
    "inline_L_tde": PhysicsTerm("inline_L_tde", "Inline equation: L_tde", _compute_inline_L_tde),
    "inline_rho_gas": PhysicsTerm("inline_rho_gas", "Inline equation: rho_gas", _compute_inline_rho_gas),
    "inline_T_1": PhysicsTerm("inline_T_1", "Inline equation: T", _compute_inline_T_1),
    "inline_r_37": PhysicsTerm("inline_r_37", "Inline equation: r", _compute_inline_r_37),
    "inline_k_B_1": PhysicsTerm("inline_k_B_1", "Inline equation: k_B", _compute_inline_k_B_1),
    "inline_m_p_1": PhysicsTerm("inline_m_p_1", "Inline equation: m_p", _compute_inline_m_p_1),
    "inline_ionization_1": PhysicsTerm("inline_ionization_1", "Inline equation: ionization", _compute_inline_ionization_1),
    "inline_v_exp_2": PhysicsTerm("inline_v_exp_2", "Inline equation: v_exp", _compute_inline_v_exp_2),
    "inline_P_thermal_1": PhysicsTerm("inline_P_thermal_1", "Inline equation: P_thermal", _compute_inline_P_thermal_1),
    "inline_L_X": PhysicsTerm("inline_L_X", "Inline equation: L_X", _compute_inline_L_X),
    "inline_P_rad_1": PhysicsTerm("inline_P_rad_1", "Inline equation: P_rad", _compute_inline_P_rad_1),
    "inline_F_expansion_1": PhysicsTerm("inline_F_expansion_1", "Inline equation: F_expansion", _compute_inline_F_expansion_1),
    "inline_F_ionization_1": PhysicsTerm("inline_F_ionization_1", "Inline equation: F_ionization", _compute_inline_F_ionization_1),
    "inline_gas_nebula_2": PhysicsTerm("inline_gas_nebula_2", "Inline equation: gas_nebula", _compute_inline_gas_nebula_2),
    "inline_r0": PhysicsTerm("inline_r0", "Inline equation: r0", _compute_inline_r0),
    "inline_t0_1": PhysicsTerm("inline_t0_1", "Inline equation: t0", _compute_inline_t0_1),
    "inline_r_t_1": PhysicsTerm("inline_r_t_1", "Inline equation: r_t", _compute_inline_r_t_1),
    "inline_rho_t": PhysicsTerm("inline_rho_t", "Inline equation: rho_t", _compute_inline_rho_t),
    "inline_q_val_1": PhysicsTerm("inline_q_val_1", "Inline equation: q_val", _compute_inline_q_val_1),
    "inline_ionization_rate_1": PhysicsTerm("inline_ionization_rate_1", "Inline equation: ionization_rate", _compute_inline_ionization_rate_1),
    "inline_ion_frac_1": PhysicsTerm("inline_ion_frac_1", "Inline equation: ion_frac", _compute_inline_ion_frac_1),
    "inline_term_res_6": PhysicsTerm("inline_term_res_6", "Inline equation: term_res", _compute_inline_term_res_6),
    "inline_disc_4": PhysicsTerm("inline_disc_4", "Inline equation: disc", _compute_inline_disc_4),
    "inline_F_2": PhysicsTerm("inline_F_2", "Inline equation: F", _compute_inline_F_2),
    "inline_U_bi": PhysicsTerm("inline_U_bi", "Inline equation: U_bi", _compute_inline_U_bi),
    "inline_U_sc": PhysicsTerm("inline_U_sc", "Inline equation: U_sc", _compute_inline_U_sc),
    "inline_g_comp_1": PhysicsTerm("inline_g_comp_1", "Inline equation: g_comp", _compute_inline_g_comp_1),
    "inline_DPM_res_2": PhysicsTerm("inline_DPM_res_2", "Inline equation: DPM_res", _compute_inline_DPM_res_2),
    "inline_integrand_16": PhysicsTerm("inline_integrand_16", "Inline equation: integrand", _compute_inline_integrand_16),
    "inline_rho_11": PhysicsTerm("inline_rho_11", "Inline equation: rho", _compute_inline_rho_11),
    "inline_gas_nebula_3": PhysicsTerm("inline_gas_nebula_3", "Inline equation: gas_nebula", _compute_inline_gas_nebula_3),
    "inline_qs": PhysicsTerm("inline_qs", "Inline equation: qs", _compute_inline_qs),
    "inline_F_3": PhysicsTerm("inline_F_3", "Inline equation: F", _compute_inline_F_3),
    "inline_q_sum": PhysicsTerm("inline_q_sum", "Inline equation: q_sum", _compute_inline_q_sum),
    "inline_qs_1": PhysicsTerm("inline_qs_1", "Inline equation: qs", _compute_inline_qs_1),
    "inline_dipole": PhysicsTerm("inline_dipole", "Inline equation: dipole", _compute_inline_dipole),
    "inline_U_bi_1": PhysicsTerm("inline_U_bi_1", "Inline equation: U_bi", _compute_inline_U_bi_1),
    "inline_gas_contrib": PhysicsTerm("inline_gas_contrib", "Inline equation: gas_contrib", _compute_inline_gas_contrib),
    "inline_F_cassini": PhysicsTerm("inline_F_cassini", "Inline equation: F_cassini", _compute_inline_F_cassini),
    "inline_geom_factor_3": PhysicsTerm("inline_geom_factor_3", "Inline equation: geom_factor", _compute_inline_geom_factor_3),
    "inline_rho_vac_SCm_4": PhysicsTerm("inline_rho_vac_SCm_4", "Inline equation: rho_vac_SCm", _compute_inline_rho_vac_SCm_4),
    "inline_rho_vac_UA_5": PhysicsTerm("inline_rho_vac_UA_5", "Inline equation: rho_vac_UA", _compute_inline_rho_vac_UA_5),
    "inline_rho_vac_UA_6": PhysicsTerm("inline_rho_vac_UA_6", "Inline equation: rho_vac_UA", _compute_inline_rho_vac_UA_6),
    "inline_u_m": PhysicsTerm("inline_u_m", "Inline equation: u_m", _compute_inline_u_m),
    "inline_e_field": PhysicsTerm("inline_e_field", "Inline equation: e_field", _compute_inline_e_field),
    "inline_eta_1": PhysicsTerm("inline_eta_1", "Inline equation: eta", _compute_inline_eta_1),
    "inline_u_g1_1": PhysicsTerm("inline_u_g1_1", "Inline equation: u_g1", _compute_inline_u_g1_1),
    "inline_force_m82": PhysicsTerm("inline_force_m82", "Inline equation: force_m82", _compute_inline_force_m82),
    "inline_force_ic418": PhysicsTerm("inline_force_ic418", "Inline equation: force_ic418", _compute_inline_force_ic418),
    "inline_force_canis": PhysicsTerm("inline_force_canis", "Inline equation: force_canis", _compute_inline_force_canis),
    "inline_force_ngc6302": PhysicsTerm("inline_force_ngc6302", "Inline equation: force_ngc6302", _compute_inline_force_ngc6302),
    "inline_force_ngc7027": PhysicsTerm("inline_force_ngc7027", "Inline equation: force_ngc7027", _compute_inline_force_ngc7027),
    "inline_u_g1_spherical": PhysicsTerm("inline_u_g1_spherical", "Inline equation: u_g1_spherical", _compute_inline_u_g1_spherical),
    "inline_u_g1_toroidal": PhysicsTerm("inline_u_g1_toroidal", "Inline equation: u_g1_toroidal", _compute_inline_u_g1_toroidal),
    "inline_ratio_4": PhysicsTerm("inline_ratio_4", "Inline equation: ratio", _compute_inline_ratio_4),
    "inline_ratio_5": PhysicsTerm("inline_ratio_5", "Inline equation: ratio", _compute_inline_ratio_5),
    "inline_momentum_term_2": PhysicsTerm("inline_momentum_term_2", "Inline equation: momentum_term", _compute_inline_momentum_term_2),
    "inline_gravity_term_2": PhysicsTerm("inline_gravity_term_2", "Inline equation: gravity_term", _compute_inline_gravity_term_2),
    "inline_vac_term_2": PhysicsTerm("inline_vac_term_2", "Inline equation: vac_term", _compute_inline_vac_term_2),
    "inline_f_lenr_2": PhysicsTerm("inline_f_lenr_2", "Inline equation: f_lenr", _compute_inline_f_lenr_2),
    "inline_f_act_2": PhysicsTerm("inline_f_act_2", "Inline equation: f_act", _compute_inline_f_act_2),
    "inline_f_de_2": PhysicsTerm("inline_f_de_2", "Inline equation: f_de", _compute_inline_f_de_2),
    "inline_f_res_5": PhysicsTerm("inline_f_res_5", "Inline equation: f_res", _compute_inline_f_res_5),
    "inline_base_9": PhysicsTerm("inline_base_9", "Inline equation: base", _compute_inline_base_9),
    "inline_f_bi_i_1": PhysicsTerm("inline_f_bi_i_1", "Inline equation: f_bi_i", _compute_inline_f_bi_i_1),
    "inline_integrand_17": PhysicsTerm("inline_integrand_17", "Inline equation: integrand", _compute_inline_integrand_17),
    "inline_a_9": PhysicsTerm("inline_a_9", "Inline equation: a", _compute_inline_a_9),
    "inline_b_6": PhysicsTerm("inline_b_6", "Inline equation: b", _compute_inline_b_6),
    "inline_c_4": PhysicsTerm("inline_c_4", "Inline equation: c", _compute_inline_c_4),
    "inline_buoyancy_sn": PhysicsTerm("inline_buoyancy_sn", "Inline equation: buoyancy_sn", _compute_inline_buoyancy_sn),
    "inline_f_lenr_3": PhysicsTerm("inline_f_lenr_3", "Inline equation: f_lenr", _compute_inline_f_lenr_3),
    "inline_f_rel_1": PhysicsTerm("inline_f_rel_1", "Inline equation: f_rel", _compute_inline_f_rel_1),
    "inline_buoyancy_eta": PhysicsTerm("inline_buoyancy_eta", "Inline equation: buoyancy_eta", _compute_inline_buoyancy_eta),
    "inline_omega_12": PhysicsTerm("inline_omega_12", "Inline equation: omega", _compute_inline_omega_12),
    "inline_t_global_4": PhysicsTerm("inline_t_global_4", "Inline equation: t_global", _compute_inline_t_global_4),
    "inline_defaultValue_8": PhysicsTerm("inline_defaultValue_8", "Inline equation: defaultValue", _compute_inline_defaultValue_8),
    "inline_t_global_5": PhysicsTerm("inline_t_global_5", "Inline equation: t_global", _compute_inline_t_global_5),
    "inline_ua_sc": PhysicsTerm("inline_ua_sc", "Inline equation: ua_sc", _compute_inline_ua_sc),
    "inline_sin_theta_1": PhysicsTerm("inline_sin_theta_1", "Inline equation: sin_theta", _compute_inline_sin_theta_1),
    "inline_omega_i_val_1": PhysicsTerm("inline_omega_i_val_1", "Inline equation: omega_i_val", _compute_inline_omega_i_val_1),
    "inline_g_i_1": PhysicsTerm("inline_g_i_1", "Inline equation: g_i", _compute_inline_g_i_1),
    "inline_r_ug_i_1": PhysicsTerm("inline_r_ug_i_1", "Inline equation: r_ug_i", _compute_inline_r_ug_i_1),
    "inline_t_7": PhysicsTerm("inline_t_7", "Inline equation: t", _compute_inline_t_7),
    "inline_vacuum_density_3": PhysicsTerm("inline_vacuum_density_3", "Inline equation: vacuum_density", _compute_inline_vacuum_density_3),
    "inline_defaultValue_9": PhysicsTerm("inline_defaultValue_9", "Inline equation: defaultValue", _compute_inline_defaultValue_9),
    "inline_seed_orbit_1": PhysicsTerm("inline_seed_orbit_1", "Inline equation: seed_orbit", _compute_inline_seed_orbit_1),
    "inline_poly_1": PhysicsTerm("inline_poly_1", "Inline equation: poly", _compute_inline_poly_1),
    "inline_defaultValue_10": PhysicsTerm("inline_defaultValue_10", "Inline equation: defaultValue", _compute_inline_defaultValue_10),
    "inline_Hz_kms_3": PhysicsTerm("inline_Hz_kms_3", "Inline equation: Hz_kms", _compute_inline_Hz_kms_3),
    "inline_Ug1_7": PhysicsTerm("inline_Ug1_7", "Inline equation: Ug1", _compute_inline_Ug1_7),
    "inline_unc_2": PhysicsTerm("inline_unc_2", "Inline equation: unc", _compute_inline_unc_2),
    "inline_integral_val": PhysicsTerm("inline_integral_val", "Inline equation: integral_val", _compute_inline_integral_val),
    "inline_cos_term_7": PhysicsTerm("inline_cos_term_7", "Inline equation: cos_term", _compute_inline_cos_term_7),
    "inline_exp_factor": PhysicsTerm("inline_exp_factor", "Inline equation: exp_factor", _compute_inline_exp_factor),
    "inline_pert_1": PhysicsTerm("inline_pert_1", "Inline equation: pert", _compute_inline_pert_1),
    "inline_curv": PhysicsTerm("inline_curv", "Inline equation: curv", _compute_inline_curv),
    "inline_tr_factor": PhysicsTerm("inline_tr_factor", "Inline equation: tr_factor", _compute_inline_tr_factor),
    "inline_g_base_3": PhysicsTerm("inline_g_base_3", "Inline equation: g_base", _compute_inline_g_base_3),
    "inline_lambda_term": PhysicsTerm("inline_lambda_term", "Inline equation: lambda_term", _compute_inline_lambda_term),
    "inline_quantum_term_1": PhysicsTerm("inline_quantum_term_1", "Inline equation: quantum_term", _compute_inline_quantum_term_1),
    "inline_em_term": PhysicsTerm("inline_em_term", "Inline equation: em_term", _compute_inline_em_term),
    "inline_fluid_term": PhysicsTerm("inline_fluid_term", "Inline equation: fluid_term", _compute_inline_fluid_term),
    "inline_resonant_term": PhysicsTerm("inline_resonant_term", "Inline equation: resonant_term", _compute_inline_resonant_term),
    "inline_force_dust": PhysicsTerm("inline_force_dust", "Inline equation: force_dust", _compute_inline_force_dust),
    "inline_a_dust": PhysicsTerm("inline_a_dust", "Inline equation: a_dust", _compute_inline_a_dust),
    "inline_sc_correction": PhysicsTerm("inline_sc_correction", "Inline equation: sc_correction", _compute_inline_sc_correction),
    "inline_g_base_4": PhysicsTerm("inline_g_base_4", "Inline equation: g_base", _compute_inline_g_base_4),
    "inline_g_BH": PhysicsTerm("inline_g_BH", "Inline equation: g_BH", _compute_inline_g_BH),
    "inline_em_base_5": PhysicsTerm("inline_em_base_5", "Inline equation: em_base", _compute_inline_em_base_5),
    "inline_em_term_1": PhysicsTerm("inline_em_term_1", "Inline equation: em_term", _compute_inline_em_term_1),
    "inline_g_sun_1": PhysicsTerm("inline_g_sun_1", "Inline equation: g_sun", _compute_inline_g_sun_1),
    "inline_g_saturn_base": PhysicsTerm("inline_g_saturn_base", "Inline equation: g_saturn_base", _compute_inline_g_saturn_base),
    "inline_g_saturn": PhysicsTerm("inline_g_saturn", "Inline equation: g_saturn", _compute_inline_g_saturn),
    "inline_T_ring": PhysicsTerm("inline_T_ring", "Inline equation: T_ring", _compute_inline_T_ring),
    "inline_em_base_6": PhysicsTerm("inline_em_base_6", "Inline equation: em_base", _compute_inline_em_base_6),
    "inline_fluid_term_1": PhysicsTerm("inline_fluid_term_1", "Inline equation: fluid_term", _compute_inline_fluid_term_1),
    "inline_t_yr": PhysicsTerm("inline_t_yr", "Inline equation: t_yr", _compute_inline_t_yr),
    "inline_tau_s": PhysicsTerm("inline_tau_s", "Inline equation: tau_s", _compute_inline_tau_s),
    "inline_msf_factor": PhysicsTerm("inline_msf_factor", "Inline equation: msf_factor", _compute_inline_msf_factor),
    "inline_m_factor_1": PhysicsTerm("inline_m_factor_1", "Inline equation: m_factor", _compute_inline_m_factor_1),
    "inline_g_base_5": PhysicsTerm("inline_g_base_5", "Inline equation: g_base", _compute_inline_g_base_5),
    "inline_em_base_7": PhysicsTerm("inline_em_base_7", "Inline equation: em_base", _compute_inline_em_base_7),
    "inline_r_38": PhysicsTerm("inline_r_38", "Inline equation: r", _compute_inline_r_38),
    "inline_Ug1_8": PhysicsTerm("inline_Ug1_8", "Inline equation: Ug1", _compute_inline_Ug1_8),
    "inline_curv_1": PhysicsTerm("inline_curv_1", "Inline equation: curv", _compute_inline_curv_1),
    "inline_pressure": PhysicsTerm("inline_pressure", "Inline equation: pressure", _compute_inline_pressure),
    "inline_force_1": PhysicsTerm("inline_force_1", "Inline equation: force", _compute_inline_force_1),
    "inline_r_39": PhysicsTerm("inline_r_39", "Inline equation: r", _compute_inline_r_39),
    "inline_g_base_6": PhysicsTerm("inline_g_base_6", "Inline equation: g_base", _compute_inline_g_base_6),
    "inline_em_base_8": PhysicsTerm("inline_em_base_8", "Inline equation: em_base", _compute_inline_em_base_8),
    "inline_g_base_7": PhysicsTerm("inline_g_base_7", "Inline equation: g_base", _compute_inline_g_base_7),
    "inline_em_base_9": PhysicsTerm("inline_em_base_9", "Inline equation: em_base", _compute_inline_em_base_9),
    "inline_F_DPM": PhysicsTerm("inline_F_DPM", "Inline equation: F_DPM", _compute_inline_F_DPM),
    "inline_a_vac_diff": PhysicsTerm("inline_a_vac_diff", "Inline equation: a_vac_diff", _compute_inline_a_vac_diff),
    "inline_a_super": PhysicsTerm("inline_a_super", "Inline equation: a_super", _compute_inline_a_super),
    "inline_a_aether_res": PhysicsTerm("inline_a_aether_res", "Inline equation: a_aether_res", _compute_inline_a_aether_res),
    "inline_a_quantum": PhysicsTerm("inline_a_quantum", "Inline equation: a_quantum", _compute_inline_a_quantum),
    "inline_a_aether_freq": PhysicsTerm("inline_a_aether_freq", "Inline equation: a_aether_freq", _compute_inline_a_aether_freq),
    "inline_a_fluid": PhysicsTerm("inline_a_fluid", "Inline equation: a_fluid", _compute_inline_a_fluid),
    "inline_a_exp": PhysicsTerm("inline_a_exp", "Inline equation: a_exp", _compute_inline_a_exp),
    "inline_g_sum": PhysicsTerm("inline_g_sum", "Inline equation: g_sum", _compute_inline_g_sum),
    "inline_Ug1_9": PhysicsTerm("inline_Ug1_9", "Inline equation: Ug1", _compute_inline_Ug1_9),
    "inline_F_DPM_1": PhysicsTerm("inline_F_DPM_1", "Inline equation: F_DPM", _compute_inline_F_DPM_1),
    "inline_a_DPM_res": PhysicsTerm("inline_a_DPM_res", "Inline equation: a_DPM_res", _compute_inline_a_DPM_res),
    "inline_E_vac_ISM": PhysicsTerm("inline_E_vac_ISM", "Inline equation: E_vac_ISM", _compute_inline_E_vac_ISM),
    "inline_cos_term_8": PhysicsTerm("inline_cos_term_8", "Inline equation: cos_term", _compute_inline_cos_term_8),
    "inline_a_THz_res": PhysicsTerm("inline_a_THz_res", "Inline equation: a_THz_res", _compute_inline_a_THz_res),
    "inline_a_u_g4i_res": PhysicsTerm("inline_a_u_g4i_res", "Inline equation: a_u_g4i_res", _compute_inline_a_u_g4i_res),
    "inline_a_osc_res": PhysicsTerm("inline_a_osc_res", "Inline equation: a_osc_res", _compute_inline_a_osc_res),
    "inline_a_sc_freq": PhysicsTerm("inline_a_sc_freq", "Inline equation: a_sc_freq", _compute_inline_a_sc_freq),
    "inline_res_term": PhysicsTerm("inline_res_term", "Inline equation: res_term", _compute_inline_res_term),
    "inline_sc_corr": PhysicsTerm("inline_sc_corr", "Inline equation: sc_corr", _compute_inline_sc_corr),
    "inline_g_res_sc": PhysicsTerm("inline_g_res_sc", "Inline equation: g_res_sc", _compute_inline_g_res_sc),
    "inline_a_DPM": PhysicsTerm("inline_a_DPM", "Inline equation: a_DPM", _compute_inline_a_DPM),
    "inline_a_THz": PhysicsTerm("inline_a_THz", "Inline equation: a_THz", _compute_inline_a_THz),
    "inline_a_vac_diff_1": PhysicsTerm("inline_a_vac_diff_1", "Inline equation: a_vac_diff", _compute_inline_a_vac_diff_1),
    "inline_a_super_1": PhysicsTerm("inline_a_super_1", "Inline equation: a_super", _compute_inline_a_super_1),
    "inline_a_DPM_1": PhysicsTerm("inline_a_DPM_1", "Inline equation: a_DPM", _compute_inline_a_DPM_1),
    "inline_a_aether": PhysicsTerm("inline_a_aether", "Inline equation: a_aether", _compute_inline_a_aether),
    "inline_a_u_g4i": PhysicsTerm("inline_a_u_g4i", "Inline equation: a_u_g4i", _compute_inline_a_u_g4i),
    "inline_a_osc": PhysicsTerm("inline_a_osc", "Inline equation: a_osc", _compute_inline_a_osc),
    "inline_a_quantum_1": PhysicsTerm("inline_a_quantum_1", "Inline equation: a_quantum", _compute_inline_a_quantum_1),
    "inline_a_fluid_1": PhysicsTerm("inline_a_fluid_1", "Inline equation: a_fluid", _compute_inline_a_fluid_1),
    "inline_a_exp_1": PhysicsTerm("inline_a_exp_1", "Inline equation: a_exp", _compute_inline_a_exp_1),
    "inline_comp_1": PhysicsTerm("inline_comp_1", "Inline equation: comp", _compute_inline_comp_1),
    "inline_res": PhysicsTerm("inline_res", "Inline equation: res", _compute_inline_res),
    "inline_sc_int": PhysicsTerm("inline_sc_int", "Inline equation: sc_int", _compute_inline_sc_int),
    "inline_g_comp_res": PhysicsTerm("inline_g_comp_res", "Inline equation: g_comp_res", _compute_inline_g_comp_res),
    "inline_r_t_2": PhysicsTerm("inline_r_t_2", "Inline equation: r_t", _compute_inline_r_t_2),
    "inline_V_sys_t": PhysicsTerm("inline_V_sys_t", "Inline equation: V_sys_t", _compute_inline_V_sys_t),
    "inline_a_quantum_res": PhysicsTerm("inline_a_quantum_res", "Inline equation: a_quantum_res", _compute_inline_a_quantum_res),
    "inline_a_fluid_res": PhysicsTerm("inline_a_fluid_res", "Inline equation: a_fluid_res", _compute_inline_a_fluid_res),
    "inline_a_exp_res": PhysicsTerm("inline_a_exp_res", "Inline equation: a_exp_res", _compute_inline_a_exp_res),
    "inline_sc_int_1": PhysicsTerm("inline_sc_int_1", "Inline equation: sc_int", _compute_inline_sc_int_1),
    "inline_res_sum": PhysicsTerm("inline_res_sum", "Inline equation: res_sum", _compute_inline_res_sum),
    "inline_SCm_contrib_6": PhysicsTerm("inline_SCm_contrib_6", "Inline equation: SCm_contrib", _compute_inline_SCm_contrib_6),
    "inline_SCm_contrib_7": PhysicsTerm("inline_SCm_contrib_7", "Inline equation: SCm_contrib", _compute_inline_SCm_contrib_7),
    "inline_SCm_contrib_8": PhysicsTerm("inline_SCm_contrib_8", "Inline equation: SCm_contrib", _compute_inline_SCm_contrib_8),
    "inline_phi_hat_3": PhysicsTerm("inline_phi_hat_3", "Inline equation: phi_hat", _compute_inline_phi_hat_3),
    "inline_tolerance_1": PhysicsTerm("inline_tolerance_1", "Inline equation: tolerance", _compute_inline_tolerance_1),
    "inline_feedbackParameter_1": PhysicsTerm("inline_feedbackParameter_1", "Inline equation: feedbackParameter", _compute_inline_feedbackParameter_1),
    "inline_uqff_g_3": PhysicsTerm("inline_uqff_g_3", "Inline equation: uqff_g", _compute_inline_uqff_g_3),
    "inline_H0_4": PhysicsTerm("inline_H0_4", "Inline equation: H0", _compute_inline_H0_4),
    "inline_a_fluid_2": PhysicsTerm("inline_a_fluid_2", "Inline equation: a_fluid", _compute_inline_a_fluid_2),
    "inline_em_base_10": PhysicsTerm("inline_em_base_10", "Inline equation: em_base", _compute_inline_em_base_10),
    "inline_em_base_11": PhysicsTerm("inline_em_base_11", "Inline equation: em_base", _compute_inline_em_base_11),
    "inline_a_quantum_orbital_res": PhysicsTerm("inline_a_quantum_orbital_res", "Inline equation: a_quantum_orbital_res", _compute_inline_a_quantum_orbital_res),
    "inline_res_sum_1": PhysicsTerm("inline_res_sum_1", "Inline equation: res_sum", _compute_inline_res_sum_1),
    "inline_g_res": PhysicsTerm("inline_g_res", "Inline equation: g_res", _compute_inline_g_res),
    "inline_flux": PhysicsTerm("inline_flux", "Inline equation: flux", _compute_inline_flux),
    "inline_Hz_kms_4": PhysicsTerm("inline_Hz_kms_4", "Inline equation: Hz_kms", _compute_inline_Hz_kms_4),
    "inline_torque_base": PhysicsTerm("inline_torque_base", "Inline equation: torque_base", _compute_inline_torque_base),
    "inline_flux_1": PhysicsTerm("inline_flux_1", "Inline equation: flux", _compute_inline_flux_1),
    "inline_g_base_8": PhysicsTerm("inline_g_base_8", "Inline equation: g_base", _compute_inline_g_base_8),
    "inline_lambda_term_1": PhysicsTerm("inline_lambda_term_1", "Inline equation: lambda_term", _compute_inline_lambda_term_1),
    "inline_em_base_12": PhysicsTerm("inline_em_base_12", "Inline equation: em_base", _compute_inline_em_base_12),
    "inline_Ug1_10": PhysicsTerm("inline_Ug1_10", "Inline equation: Ug1", _compute_inline_Ug1_10),
    "inline_Ug2_4": PhysicsTerm("inline_Ug2_4", "Inline equation: Ug2", _compute_inline_Ug2_4),
    "inline_Ug4_4": PhysicsTerm("inline_Ug4_4", "Inline equation: Ug4", _compute_inline_Ug4_4),
    "inline_M_vis": PhysicsTerm("inline_M_vis", "Inline equation: M_vis", _compute_inline_M_vis),
    "inline_pert_mass": PhysicsTerm("inline_pert_mass", "Inline equation: pert_mass", _compute_inline_pert_mass),
    "inline_w_stellar": PhysicsTerm("inline_w_stellar", "Inline equation: w_stellar", _compute_inline_w_stellar),
    "inline_em_base_13": PhysicsTerm("inline_em_base_13", "Inline equation: em_base", _compute_inline_em_base_13),
    "inline_vac_ratio_1": PhysicsTerm("inline_vac_ratio_1", "Inline equation: vac_ratio", _compute_inline_vac_ratio_1),
    "inline_em_term_2": PhysicsTerm("inline_em_term_2", "Inline equation: em_term", _compute_inline_em_term_2),
    "inline_a_THz_1": PhysicsTerm("inline_a_THz_1", "Inline equation: a_THz", _compute_inline_a_THz_1),
    "inline_a_super_2": PhysicsTerm("inline_a_super_2", "Inline equation: a_super", _compute_inline_a_super_2),
    "inline_a_quantum_2": PhysicsTerm("inline_a_quantum_2", "Inline equation: a_quantum", _compute_inline_a_quantum_2),
    "inline_a_fluid_3": PhysicsTerm("inline_a_fluid_3", "Inline equation: a_fluid", _compute_inline_a_fluid_3),
    "inline_a_exp_2": PhysicsTerm("inline_a_exp_2", "Inline equation: a_exp", _compute_inline_a_exp_2),
    "inline_g_comp_res_1": PhysicsTerm("inline_g_comp_res_1", "Inline equation: g_comp_res", _compute_inline_g_comp_res_1),
    "inline_default_val_1": PhysicsTerm("inline_default_val_1", "Inline equation: default_val", _compute_inline_default_val_1),
    "inline_base_10": PhysicsTerm("inline_base_10", "Inline equation: base", _compute_inline_base_10),
    "inline_expansion_2": PhysicsTerm("inline_expansion_2", "Inline equation: expansion", _compute_inline_expansion_2),
    "inline_super_adj_1": PhysicsTerm("inline_super_adj_1", "Inline equation: super_adj", _compute_inline_super_adj_1),
    "inline_env_1": PhysicsTerm("inline_env_1", "Inline equation: env", _compute_inline_env_1),
    "inline_Ug_sum_1": PhysicsTerm("inline_Ug_sum_1", "Inline equation: Ug_sum", _compute_inline_Ug_sum_1),
    "inline_cosm_1": PhysicsTerm("inline_cosm_1", "Inline equation: cosm", _compute_inline_cosm_1),
    "inline_quantum_1": PhysicsTerm("inline_quantum_1", "Inline equation: quantum", _compute_inline_quantum_1),
    "inline_fluid_1": PhysicsTerm("inline_fluid_1", "Inline equation: fluid", _compute_inline_fluid_1),
    "inline_perturbation_1": PhysicsTerm("inline_perturbation_1", "Inline equation: perturbation", _compute_inline_perturbation_1),
    "inline_fexp_2": PhysicsTerm("inline_fexp_2", "Inline equation: fexp", _compute_inline_fexp_2),
    "inline_V_gal": PhysicsTerm("inline_V_gal", "Inline equation: V_gal", _compute_inline_V_gal),
    "inline_V_stellar": PhysicsTerm("inline_V_stellar", "Inline equation: V_stellar", _compute_inline_V_stellar),
    "inline_V_sombrero": PhysicsTerm("inline_V_sombrero", "Inline equation: V_sombrero", _compute_inline_V_sombrero),
    "inline_V_saturn": PhysicsTerm("inline_V_saturn", "Inline equation: V_saturn", _compute_inline_V_saturn),
    "inline_V_crab": PhysicsTerm("inline_V_crab", "Inline equation: V_crab", _compute_inline_V_crab),
    "inline_V_guide": PhysicsTerm("inline_V_guide", "Inline equation: V_guide", _compute_inline_V_guide),
    "inline_V_lagoon": PhysicsTerm("inline_V_lagoon", "Inline equation: V_lagoon", _compute_inline_V_lagoon),
    "inline_V_spirals": PhysicsTerm("inline_V_spirals", "Inline equation: V_spirals", _compute_inline_V_spirals),
    "inline_V_ngc": PhysicsTerm("inline_V_ngc", "Inline equation: V_ngc", _compute_inline_V_ngc),
    "inline_V_orion": PhysicsTerm("inline_V_orion", "Inline equation: V_orion", _compute_inline_V_orion),
    "inline_grav_base": PhysicsTerm("inline_grav_base", "Inline equation: grav_base", _compute_inline_grav_base),
    "inline_orb_grav": PhysicsTerm("inline_orb_grav", "Inline equation: orb_grav", _compute_inline_orb_grav),
    "inline_muge": PhysicsTerm("inline_muge", "Inline equation: muge", _compute_inline_muge),
    "inline_delta_omega": PhysicsTerm("inline_delta_omega", "Inline equation: delta_omega", _compute_inline_delta_omega),
    "inline_F_DPM_2": PhysicsTerm("inline_F_DPM_2", "Inline equation: F_DPM", _compute_inline_F_DPM_2),
    "inline_a_DPM_2": PhysicsTerm("inline_a_DPM_2", "Inline equation: a_DPM", _compute_inline_a_DPM_2),
    "inline_a_THz_2": PhysicsTerm("inline_a_THz_2", "Inline equation: a_THz", _compute_inline_a_THz_2),
    "inline_a_vac_diff_2": PhysicsTerm("inline_a_vac_diff_2", "Inline equation: a_vac_diff", _compute_inline_a_vac_diff_2),
    "inline_a_super_freq": PhysicsTerm("inline_a_super_freq", "Inline equation: a_super_freq", _compute_inline_a_super_freq),
    "inline_a_aether_res_1": PhysicsTerm("inline_a_aether_res_1", "Inline equation: a_aether_res", _compute_inline_a_aether_res_1),
    "inline_a_quantum_freq": PhysicsTerm("inline_a_quantum_freq", "Inline equation: a_quantum_freq", _compute_inline_a_quantum_freq),
    "inline_a_Aether_freq": PhysicsTerm("inline_a_Aether_freq", "Inline equation: a_Aether_freq", _compute_inline_a_Aether_freq),
    "inline_f_fluid_1": PhysicsTerm("inline_f_fluid_1", "Inline equation: f_fluid", _compute_inline_f_fluid_1),
    "inline_a_fluid_freq": PhysicsTerm("inline_a_fluid_freq", "Inline equation: a_fluid_freq", _compute_inline_a_fluid_freq),
    "inline_f_exp": PhysicsTerm("inline_f_exp", "Inline equation: f_exp", _compute_inline_f_exp),
    "inline_a_exp_freq": PhysicsTerm("inline_a_exp_freq", "Inline equation: a_exp_freq", _compute_inline_a_exp_freq),
    "inline_muge_1": PhysicsTerm("inline_muge_1", "Inline equation: muge", _compute_inline_muge_1),
    "inline_sqrt_unc_1": PhysicsTerm("inline_sqrt_unc_1", "Inline equation: sqrt_unc", _compute_inline_sqrt_unc_1),
    "inline_V_6": PhysicsTerm("inline_V_6", "Inline equation: V", _compute_inline_V_6),
    "inline_pert_2": PhysicsTerm("inline_pert_2", "Inline equation: pert", _compute_inline_pert_2),
    "inline_env_factor": PhysicsTerm("inline_env_factor", "Inline equation: env_factor", _compute_inline_env_factor),
    "inline_g_base_9": PhysicsTerm("inline_g_base_9", "Inline equation: g_base", _compute_inline_g_base_9),
    "inline_dm_pert_term": PhysicsTerm("inline_dm_pert_term", "Inline equation: dm_pert_term", _compute_inline_dm_pert_term),
    "inline_t_8": PhysicsTerm("inline_t_8", "Inline equation: t", _compute_inline_t_8),
    "inline_Ug2_5": PhysicsTerm("inline_Ug2_5", "Inline equation: Ug2", _compute_inline_Ug2_5),
    "inline_p_outflow": PhysicsTerm("inline_p_outflow", "Inline equation: p_outflow", _compute_inline_p_outflow),
    "inline_em_base_14": PhysicsTerm("inline_em_base_14", "Inline equation: em_base", _compute_inline_em_base_14),
    "inline_Hz_kms_5": PhysicsTerm("inline_Hz_kms_5", "Inline equation: Hz_kms", _compute_inline_Hz_kms_5),
    "inline_M_t_1": PhysicsTerm("inline_M_t_1", "Inline equation: M_t", _compute_inline_M_t_1),
    "inline_phase_4": PhysicsTerm("inline_phase_4", "Inline equation: phase", _compute_inline_phase_4),
    "inline_g_base_10": PhysicsTerm("inline_g_base_10", "Inline equation: g_base", _compute_inline_g_base_10),
    "inline_dm_pert_term_1": PhysicsTerm("inline_dm_pert_term_1", "Inline equation: dm_pert_term", _compute_inline_dm_pert_term_1),
    "inline_dm_term": PhysicsTerm("inline_dm_term", "Inline equation: dm_term", _compute_inline_dm_term),
    "inline_gw_term": PhysicsTerm("inline_gw_term", "Inline equation: gw_term", _compute_inline_gw_term),
    "inline_t_9": PhysicsTerm("inline_t_9", "Inline equation: t", _compute_inline_t_9),
    "inline_D_t": PhysicsTerm("inline_D_t", "Inline equation: D_t", _compute_inline_D_t),
    "inline_E_t": PhysicsTerm("inline_E_t", "Inline equation: E_t", _compute_inline_E_t),
    "inline_L_t_1": PhysicsTerm("inline_L_t_1", "Inline equation: L_t", _compute_inline_L_t_1),
    "inline_integral_val_1": PhysicsTerm("inline_integral_val_1", "Inline equation: integral_val", _compute_inline_integral_val_1),
    "inline_Ug3_prime": PhysicsTerm("inline_Ug3_prime", "Inline equation: Ug3_prime", _compute_inline_Ug3_prime),
    "inline_pert_3": PhysicsTerm("inline_pert_3", "Inline equation: pert", _compute_inline_pert_3),
    "inline_g_base_11": PhysicsTerm("inline_g_base_11", "Inline equation: g_base", _compute_inline_g_base_11),
    "inline_dm_pert_term_2": PhysicsTerm("inline_dm_pert_term_2", "Inline equation: dm_pert_term", _compute_inline_dm_pert_term_2),
    "inline_uqff_g_4": PhysicsTerm("inline_uqff_g_4", "Inline equation: uqff_g", _compute_inline_uqff_g_4),
    "inline_min": PhysicsTerm("inline_min", "Inline equation: min", _compute_inline_min),
    "inline_max": PhysicsTerm("inline_max", "Inline equation: max", _compute_inline_max),
    "inline_mean": PhysicsTerm("inline_mean", "Inline equation: mean", _compute_inline_mean),
    "inline_FU_1": PhysicsTerm("inline_FU_1", "Inline equation: FU", _compute_inline_FU_1),
    "inline_compressed_g": PhysicsTerm("inline_compressed_g", "Inline equation: compressed_g", _compute_inline_compressed_g),
    "inline_resonance_g": PhysicsTerm("inline_resonance_g", "Inline equation: resonance_g", _compute_inline_resonance_g),
    "inline_min_1": PhysicsTerm("inline_min_1", "Inline equation: min", _compute_inline_min_1),
    "inline_D_t_1": PhysicsTerm("inline_D_t_1", "Inline equation: D_t", _compute_inline_D_t_1),
    "inline_BH_term": PhysicsTerm("inline_BH_term", "Inline equation: BH_term", _compute_inline_BH_term),
    "inline_E_t_1": PhysicsTerm("inline_E_t_1", "Inline equation: E_t", _compute_inline_E_t_1),
    "inline_M_SN_t": PhysicsTerm("inline_M_SN_t", "Inline equation: M_SN_t", _compute_inline_M_SN_t),
    "inline_P_t": PhysicsTerm("inline_P_t", "Inline equation: P_t", _compute_inline_P_t),
    "inline_E_t_2": PhysicsTerm("inline_E_t_2", "Inline equation: E_t", _compute_inline_E_t_2),
    "inline_M_merge_t": PhysicsTerm("inline_M_merge_t", "Inline equation: M_merge_t", _compute_inline_M_merge_t),
    "inline_E_t_3": PhysicsTerm("inline_E_t_3", "Inline equation: E_t", _compute_inline_E_t_3),
    "inline_F_fil": PhysicsTerm("inline_F_fil", "Inline equation: F_fil", _compute_inline_F_fil),
    "inline_F_BH": PhysicsTerm("inline_F_BH", "Inline equation: F_BH", _compute_inline_F_BH),
    "inline_F_sn": PhysicsTerm("inline_F_sn", "Inline equation: F_sn", _compute_inline_F_sn),
    "inline_M_evo_t": PhysicsTerm("inline_M_evo_t", "Inline equation: M_evo_t", _compute_inline_M_evo_t),
    "inline_frame_rate_inv": PhysicsTerm("inline_frame_rate_inv", "Inline equation: frame_rate_inv", _compute_inline_frame_rate_inv),
    "inline_t_minus": PhysicsTerm("inline_t_minus", "Inline equation: t_minus", _compute_inline_t_minus),
    "inline_Ug1_11": PhysicsTerm("inline_Ug1_11", "Inline equation: Ug1", _compute_inline_Ug1_11),
    "inline_beta_term": PhysicsTerm("inline_beta_term", "Inline equation: beta_term", _compute_inline_beta_term),
    "inline_exp_cos": PhysicsTerm("inline_exp_cos", "Inline equation: exp_cos", _compute_inline_exp_cos),
    "inline_Um1": PhysicsTerm("inline_Um1", "Inline equation: Um1", _compute_inline_Um1),
    "inline_metric": PhysicsTerm("inline_metric", "Inline equation: metric", _compute_inline_metric),
    "inline_ub": PhysicsTerm("inline_ub", "Inline equation: ub", _compute_inline_ub),
    "inline_vac_sc": PhysicsTerm("inline_vac_sc", "Inline equation: vac_sc", _compute_inline_vac_sc),
    "inline_vac_ua": PhysicsTerm("inline_vac_ua", "Inline equation: vac_ua", _compute_inline_vac_ua),
    "inline_spin_factor": PhysicsTerm("inline_spin_factor", "Inline equation: spin_factor", _compute_inline_spin_factor),
    "inline_sc_factor": PhysicsTerm("inline_sc_factor", "Inline equation: sc_factor", _compute_inline_sc_factor),
    "inline_fu_ext": PhysicsTerm("inline_fu_ext", "Inline equation: fu_ext", _compute_inline_fu_ext),
    "inline_up_total": PhysicsTerm("inline_up_total", "Inline equation: up_total", _compute_inline_up_total),
    "inline_plasmoids": PhysicsTerm("inline_plasmoids", "Inline equation: plasmoids", _compute_inline_plasmoids),
    "inline_energy_frame": PhysicsTerm("inline_energy_frame", "Inline equation: energy_frame", _compute_inline_energy_frame),
    "inline_ug3_1": PhysicsTerm("inline_ug3_1", "Inline equation: ug3", _compute_inline_ug3_1),
    "inline_non_local": PhysicsTerm("inline_non_local", "Inline equation: non_local", _compute_inline_non_local),
    "inline_dx_11": PhysicsTerm("inline_dx_11", "Inline equation: dx", _compute_inline_dx_11),
    "inline_dy": PhysicsTerm("inline_dy", "Inline equation: dy", _compute_inline_dy),
    "inline_e_field_1": PhysicsTerm("inline_e_field_1", "Inline equation: e_field", _compute_inline_e_field_1),
    "inline_eta_2": PhysicsTerm("inline_eta_2", "Inline equation: eta", _compute_inline_eta_2),
    "inline_m_H": PhysicsTerm("inline_m_H", "Inline equation: m_H", _compute_inline_m_H),
    "inline_ug3_2": PhysicsTerm("inline_ug3_2", "Inline equation: ug3", _compute_inline_ug3_2),
    "inline_T_2": PhysicsTerm("inline_T_2", "Inline equation: T", _compute_inline_T_2),
    "inline_e_field_2": PhysicsTerm("inline_e_field_2", "Inline equation: e_field", _compute_inline_e_field_2),
    "inline_eta_3": PhysicsTerm("inline_eta_3", "Inline equation: eta", _compute_inline_eta_3),
    "inline_trans_E": PhysicsTerm("inline_trans_E", "Inline equation: trans_E", _compute_inline_trans_E),
    "inline_T_star": PhysicsTerm("inline_T_star", "Inline equation: T_star", _compute_inline_T_star),
    "inline_v_rad": PhysicsTerm("inline_v_rad", "Inline equation: v_rad", _compute_inline_v_rad),
    "inline_E_neut": PhysicsTerm("inline_E_neut", "Inline equation: E_neut", _compute_inline_E_neut),
    "inline_decay_6": PhysicsTerm("inline_decay_6", "Inline equation: decay", _compute_inline_decay_6),
    "inline_E_DNA": PhysicsTerm("inline_E_DNA", "Inline equation: E_DNA", _compute_inline_E_DNA),
    "inline_buoy": PhysicsTerm("inline_buoy", "Inline equation: buoy", _compute_inline_buoy),
    "inline_ug3_3": PhysicsTerm("inline_ug3_3", "Inline equation: ug3", _compute_inline_ug3_3),
    "inline_T_3": PhysicsTerm("inline_T_3", "Inline equation: T", _compute_inline_T_3),
    "inline_acc_lenr": PhysicsTerm("inline_acc_lenr", "Inline equation: acc_lenr", _compute_inline_acc_lenr),
    "inline_acc_higgs": PhysicsTerm("inline_acc_higgs", "Inline equation: acc_higgs", _compute_inline_acc_higgs),
    "inline_geo_angle": PhysicsTerm("inline_geo_angle", "Inline equation: geo_angle", _compute_inline_geo_angle),
    "inline_non_local_1": PhysicsTerm("inline_non_local_1", "Inline equation: non_local", _compute_inline_non_local_1),
    "inline_rho_UA_SCm": PhysicsTerm("inline_rho_UA_SCm", "Inline equation: rho_UA_SCm", _compute_inline_rho_UA_SCm),
    "inline_exp_cos_1": PhysicsTerm("inline_exp_cos_1", "Inline equation: exp_cos", _compute_inline_exp_cos_1),
    "inline_E_react_t": PhysicsTerm("inline_E_react_t", "Inline equation: E_react_t", _compute_inline_E_react_t),
    "inline_factor_1": PhysicsTerm("inline_factor_1", "Inline equation: factor", _compute_inline_factor_1),
    "inline_rho_UA_SCm_1": PhysicsTerm("inline_rho_UA_SCm_1", "Inline equation: rho_UA_SCm", _compute_inline_rho_UA_SCm_1),
    "inline_omega_H_t": PhysicsTerm("inline_omega_H_t", "Inline equation: omega_H_t", _compute_inline_omega_H_t),
    "inline_E_react_t_1": PhysicsTerm("inline_E_react_t_1", "Inline equation: E_react_t", _compute_inline_E_react_t_1),
    "inline_Um_val": PhysicsTerm("inline_Um_val", "Inline equation: Um_val", _compute_inline_Um_val),
    "inline_ug3_4": PhysicsTerm("inline_ug3_4", "Inline equation: ug3", _compute_inline_ug3_4),
    "inline_E": PhysicsTerm("inline_E", "Inline equation: E", _compute_inline_E),
    "inline_eta_4": PhysicsTerm("inline_eta_4", "Inline equation: eta", _compute_inline_eta_4),
    "inline_S2": PhysicsTerm("inline_S2", "Inline equation: S2", _compute_inline_S2),
    "inline_buoy_sum": PhysicsTerm("inline_buoy_sum", "Inline equation: buoy_sum", _compute_inline_buoy_sum),
    "inline_Q": PhysicsTerm("inline_Q", "Inline equation: Q", _compute_inline_Q),
    "inline_ug3_5": PhysicsTerm("inline_ug3_5", "Inline equation: ug3", _compute_inline_ug3_5),
    "inline_br_ww": PhysicsTerm("inline_br_ww", "Inline equation: br_ww", _compute_inline_br_ww),
    "inline_vac_ratio_2": PhysicsTerm("inline_vac_ratio_2", "Inline equation: vac_ratio", _compute_inline_vac_ratio_2),
    "inline_q_scale": PhysicsTerm("inline_q_scale", "Inline equation: q_scale", _compute_inline_q_scale),
    "inline_sin_term_2": PhysicsTerm("inline_sin_term_2", "Inline equation: sin_term", _compute_inline_sin_term_2),
    "inline_exp_non": PhysicsTerm("inline_exp_non", "Inline equation: exp_non", _compute_inline_exp_non),
    "inline_partial_t": PhysicsTerm("inline_partial_t", "Inline equation: partial_t", _compute_inline_partial_t),
    "inline_grad_term": PhysicsTerm("inline_grad_term", "Inline equation: grad_term", _compute_inline_grad_term),
    "inline_cos_term_9": PhysicsTerm("inline_cos_term_9", "Inline equation: cos_term", _compute_inline_cos_term_9),
    "inline_ratio_6": PhysicsTerm("inline_ratio_6", "Inline equation: ratio", _compute_inline_ratio_6),
    "inline_omega_i_t": PhysicsTerm("inline_omega_i_t", "Inline equation: omega_i_t", _compute_inline_omega_i_t),
    "inline_pot": PhysicsTerm("inline_pot", "Inline equation: pot", _compute_inline_pot),
    "inline_quant": PhysicsTerm("inline_quant", "Inline equation: quant", _compute_inline_quant),
    "inline_E0": PhysicsTerm("inline_E0", "Inline equation: E0", _compute_inline_E0),
    "inline_q_factor": PhysicsTerm("inline_q_factor", "Inline equation: q_factor", _compute_inline_q_factor),
    "inline_rad_factor": PhysicsTerm("inline_rad_factor", "Inline equation: rad_factor", _compute_inline_rad_factor),
    "inline_wave_factor": PhysicsTerm("inline_wave_factor", "Inline equation: wave_factor", _compute_inline_wave_factor),
    "inline_higgs_f": PhysicsTerm("inline_higgs_f", "Inline equation: higgs_f", _compute_inline_higgs_f),
    "inline_prec_f": PhysicsTerm("inline_prec_f", "Inline equation: prec_f", _compute_inline_prec_f),
    "inline_scale_f": PhysicsTerm("inline_scale_f", "Inline equation: scale_f", _compute_inline_scale_f),
    "inline_non_local_2": PhysicsTerm("inline_non_local_2", "Inline equation: non_local", _compute_inline_non_local_2),
    "inline_exp_cos_2": PhysicsTerm("inline_exp_cos_2", "Inline equation: exp_cos", _compute_inline_exp_cos_2),
    "inline_phi_tw": PhysicsTerm("inline_phi_tw", "Inline equation: phi_tw", _compute_inline_phi_tw),
    "inline_B_p": PhysicsTerm("inline_B_p", "Inline equation: B_p", _compute_inline_B_p),
    "inline_Ui_2": PhysicsTerm("inline_Ui_2", "Inline equation: Ui", _compute_inline_Ui_2),
    "inline_E_b": PhysicsTerm("inline_E_b", "Inline equation: E_b", _compute_inline_E_b),
    "inline_H_m": PhysicsTerm("inline_H_m", "Inline equation: H_m", _compute_inline_H_m),
    "inline_Um_v": PhysicsTerm("inline_Um_v", "Inline equation: Um_v", _compute_inline_Um_v),
    "inline_Ug3_v": PhysicsTerm("inline_Ug3_v", "Inline equation: Ug3_v", _compute_inline_Ug3_v),
    "inline_qsf": PhysicsTerm("inline_qsf", "Inline equation: qsf", _compute_inline_qsf),
    "inline_rdf": PhysicsTerm("inline_rdf", "Inline equation: rdf", _compute_inline_rdf),
    "inline_wtff": PhysicsTerm("inline_wtff", "Inline equation: wtff", _compute_inline_wtff),
    "inline_hff": PhysicsTerm("inline_hff", "Inline equation: hff", _compute_inline_hff),
    "inline_ptf": PhysicsTerm("inline_ptf", "Inline equation: ptf", _compute_inline_ptf),
    "inline_qsff": PhysicsTerm("inline_qsff", "Inline equation: qsff", _compute_inline_qsff),
    "inline_E_w": PhysicsTerm("inline_E_w", "Inline equation: E_w", _compute_inline_E_w),
    "inline_proofset": PhysicsTerm("inline_proofset", "Inline equation: proofset", _compute_inline_proofset),
    "inline_vac_r": PhysicsTerm("inline_vac_r", "Inline equation: vac_r", _compute_inline_vac_r),
    "inline_q_s": PhysicsTerm("inline_q_s", "Inline equation: q_s", _compute_inline_q_s),
    "inline_Ug3_v_1": PhysicsTerm("inline_Ug3_v_1", "Inline equation: Ug3_v", _compute_inline_Ug3_v_1),
    "inline_spatial_f": PhysicsTerm("inline_spatial_f", "Inline equation: spatial_f", _compute_inline_spatial_f),
    "inline_comp_f": PhysicsTerm("inline_comp_f", "Inline equation: comp_f", _compute_inline_comp_f),
    "inline_higgs_f_1": PhysicsTerm("inline_higgs_f_1", "Inline equation: higgs_f", _compute_inline_higgs_f_1),
    "inline_prec_f_1": PhysicsTerm("inline_prec_f_1", "Inline equation: prec_f", _compute_inline_prec_f_1),
    "inline_q_scale_1": PhysicsTerm("inline_q_scale_1", "Inline equation: q_scale", _compute_inline_q_scale_1),
    "inline_cons_leg": PhysicsTerm("inline_cons_leg", "Inline equation: cons_leg", _compute_inline_cons_leg),
    "inline_vac_leg": PhysicsTerm("inline_vac_leg", "Inline equation: vac_leg", _compute_inline_vac_leg),
    "inline_q_leg": PhysicsTerm("inline_q_leg", "Inline equation: q_leg", _compute_inline_q_leg),
    "inline_non_local_3": PhysicsTerm("inline_non_local_3", "Inline equation: non_local", _compute_inline_non_local_3),
    "inline_cos_term_10": PhysicsTerm("inline_cos_term_10", "Inline equation: cos_term", _compute_inline_cos_term_10),
    "inline_E_sp": PhysicsTerm("inline_E_sp", "Inline equation: E_sp", _compute_inline_E_sp),
    "inline_proofset_1": PhysicsTerm("inline_proofset_1", "Inline equation: proofset", _compute_inline_proofset_1),
    "inline_E_sp_1": PhysicsTerm("inline_E_sp_1", "Inline equation: E_sp", _compute_inline_E_sp_1),
    "inline_cons_leg_1": PhysicsTerm("inline_cons_leg_1", "Inline equation: cons_leg", _compute_inline_cons_leg_1),
    "inline_proofset_2": PhysicsTerm("inline_proofset_2", "Inline equation: proofset", _compute_inline_proofset_2),
    "inline_Hz_kms_6": PhysicsTerm("inline_Hz_kms_6", "Inline equation: Hz_kms", _compute_inline_Hz_kms_6),
    "inline_f_wind": PhysicsTerm("inline_f_wind", "Inline equation: f_wind", _compute_inline_f_wind),
    "inline_f_erode": PhysicsTerm("inline_f_erode", "Inline equation: f_erode", _compute_inline_f_erode),
    "inline_f_lensing": PhysicsTerm("inline_f_lensing", "Inline equation: f_lensing", _compute_inline_f_lensing),
    "inline_f_decay": PhysicsTerm("inline_f_decay", "Inline equation: f_decay", _compute_inline_f_decay),
    "inline_f_coll": PhysicsTerm("inline_f_coll", "Inline equation: f_coll", _compute_inline_f_coll),
    "inline_f_merge": PhysicsTerm("inline_f_merge", "Inline equation: f_merge", _compute_inline_f_merge),
    "inline_mag_term": PhysicsTerm("inline_mag_term", "Inline equation: mag_term", _compute_inline_mag_term),
    "inline_standing_1": PhysicsTerm("inline_standing_1", "Inline equation: standing", _compute_inline_standing_1),
    "inline_quantum_wave": PhysicsTerm("inline_quantum_wave", "Inline equation: quantum_wave", _compute_inline_quantum_wave),
    "inline_psi_tot": PhysicsTerm("inline_psi_tot", "Inline equation: psi_tot", _compute_inline_psi_tot),
    "inline_Htz": PhysicsTerm("inline_Htz", "Inline equation: Htz", _compute_inline_Htz),
    "inline_g_base_12": PhysicsTerm("inline_g_base_12", "Inline equation: g_base", _compute_inline_g_base_12),
    "inline_expected_1": PhysicsTerm("inline_expected_1", "Inline equation: expected", _compute_inline_expected_1),
    "inline_result_9": PhysicsTerm("inline_result_9", "Inline equation: result", _compute_inline_result_9),
    "inline_result_10": PhysicsTerm("inline_result_10", "Inline equation: result", _compute_inline_result_10),
    "inline_result_11": PhysicsTerm("inline_result_11", "Inline equation: result", _compute_inline_result_11),
    "inline_result_12": PhysicsTerm("inline_result_12", "Inline equation: result", _compute_inline_result_12),
    "inline_result_13": PhysicsTerm("inline_result_13", "Inline equation: result", _compute_inline_result_13),
    "inline_result_14": PhysicsTerm("inline_result_14", "Inline equation: result", _compute_inline_result_14),
    "inline_expected_2": PhysicsTerm("inline_expected_2", "Inline equation: expected", _compute_inline_expected_2),
    "inline_result_15": PhysicsTerm("inline_result_15", "Inline equation: result", _compute_inline_result_15),
    "inline_expected_3": PhysicsTerm("inline_expected_3", "Inline equation: expected", _compute_inline_expected_3),
    "inline_result_16": PhysicsTerm("inline_result_16", "Inline equation: result", _compute_inline_result_16),
    "inline_expected_4": PhysicsTerm("inline_expected_4", "Inline equation: expected", _compute_inline_expected_4),
    "inline_result_17": PhysicsTerm("inline_result_17", "Inline equation: result", _compute_inline_result_17),
    "inline_FDPM_2": PhysicsTerm("inline_FDPM_2", "Inline equation: FDPM", _compute_inline_FDPM_2),
    "inline_expected_5": PhysicsTerm("inline_expected_5", "Inline equation: expected", _compute_inline_expected_5),
    "inline_result_18": PhysicsTerm("inline_result_18", "Inline equation: result", _compute_inline_result_18),
    "inline_result_19": PhysicsTerm("inline_result_19", "Inline equation: result", _compute_inline_result_19),
    "inline_result_20": PhysicsTerm("inline_result_20", "Inline equation: result", _compute_inline_result_20),
    "inline_result_21": PhysicsTerm("inline_result_21", "Inline equation: result", _compute_inline_result_21),
    "inline_result_22": PhysicsTerm("inline_result_22", "Inline equation: result", _compute_inline_result_22),
    "inline_result_23": PhysicsTerm("inline_result_23", "Inline equation: result", _compute_inline_result_23),
    "inline_result_24": PhysicsTerm("inline_result_24", "Inline equation: result", _compute_inline_result_24),
    "inline_result_25": PhysicsTerm("inline_result_25", "Inline equation: result", _compute_inline_result_25),
    "inline_result_26": PhysicsTerm("inline_result_26", "Inline equation: result", _compute_inline_result_26),
    "inline_F_tidal_3": PhysicsTerm("inline_F_tidal_3", "Inline equation: F_tidal", _compute_inline_F_tidal_3),
    "inline_F_SF_1": PhysicsTerm("inline_F_SF_1", "Inline equation: F_SF", _compute_inline_F_SF_1),
    "inline_mu_dipole": PhysicsTerm("inline_mu_dipole", "Inline equation: mu_dipole", _compute_inline_mu_dipole),
    "inline_B_super": PhysicsTerm("inline_B_super", "Inline equation: B_super", _compute_inline_B_super),
    "inline_E_react_5": PhysicsTerm("inline_E_react_5", "Inline equation: E_react", _compute_inline_E_react_5),
    "inline_psi_int_1": PhysicsTerm("inline_psi_int_1", "Inline equation: psi_int", _compute_inline_psi_int_1),
    "inline_pert_4": PhysicsTerm("inline_pert_4", "Inline equation: pert", _compute_inline_pert_4),
    "inline_Ug_base": PhysicsTerm("inline_Ug_base", "Inline equation: Ug_base", _compute_inline_Ug_base),
    "inline_Hz_2": PhysicsTerm("inline_Hz_2", "Inline equation: Hz", _compute_inline_Hz_2),
    "inline_g_base_13": PhysicsTerm("inline_g_base_13", "Inline equation: g_base", _compute_inline_g_base_13),
    "inline_ug_sum": PhysicsTerm("inline_ug_sum", "Inline equation: ug_sum", _compute_inline_ug_sum),
    "inline_quantum_term_2": PhysicsTerm("inline_quantum_term_2", "Inline equation: quantum_term", _compute_inline_quantum_term_2),
    "inline_F_tidal_4": PhysicsTerm("inline_F_tidal_4", "Inline equation: F_tidal", _compute_inline_F_tidal_4),
    "inline_F_cluster_1": PhysicsTerm("inline_F_cluster_1", "Inline equation: F_cluster", _compute_inline_F_cluster_1),
    "inline_m_factor_2": PhysicsTerm("inline_m_factor_2", "Inline equation: m_factor", _compute_inline_m_factor_2),
    "inline_grad_term_1": PhysicsTerm("inline_grad_term_1", "Inline equation: grad_term", _compute_inline_grad_term_1),
    "inline_exp_decay": PhysicsTerm("inline_exp_decay", "Inline equation: exp_decay", _compute_inline_exp_decay),
    "inline_cos_phase": PhysicsTerm("inline_cos_phase", "Inline equation: cos_phase", _compute_inline_cos_phase),
    "inline_delta_4": PhysicsTerm("inline_delta_4", "Inline equation: delta", _compute_inline_delta_4),
    "inline_rho_dust_1": PhysicsTerm("inline_rho_dust_1", "Inline equation: rho_dust", _compute_inline_rho_dust_1),
    "inline_i_base": PhysicsTerm("inline_i_base", "Inline equation: i_base", _compute_inline_i_base),
    "inline_trz": PhysicsTerm("inline_trz", "Inline equation: trz", _compute_inline_trz),
    "inline_ua_sc_1": PhysicsTerm("inline_ua_sc_1", "Inline equation: ua_sc", _compute_inline_ua_sc_1),
    "inline_r_40": PhysicsTerm("inline_r_40", "Inline equation: r", _compute_inline_r_40),
    "inline_I_2": PhysicsTerm("inline_I_2", "Inline equation: I", _compute_inline_I_2),
    "inline_F_bar": PhysicsTerm("inline_F_bar", "Inline equation: F_bar", _compute_inline_F_bar),
    "inline_F_wave": PhysicsTerm("inline_F_wave", "Inline equation: F_wave", _compute_inline_F_wave),
    "inline_M_bar": PhysicsTerm("inline_M_bar", "Inline equation: M_bar", _compute_inline_M_bar),
    "inline_r_bar": PhysicsTerm("inline_r_bar", "Inline equation: r_bar", _compute_inline_r_bar),
    "inline_yr_s": PhysicsTerm("inline_yr_s", "Inline equation: yr_s", _compute_inline_yr_s),
    "inline_Hz_kms_7": PhysicsTerm("inline_Hz_kms_7", "Inline equation: Hz_kms", _compute_inline_Hz_kms_7),
    "inline_psi_1": PhysicsTerm("inline_psi_1", "Inline equation: psi", _compute_inline_psi_1),
    "inline_curv_2": PhysicsTerm("inline_curv_2", "Inline equation: curv", _compute_inline_curv_2),
    "inline_rho_d": PhysicsTerm("inline_rho_d", "Inline equation: rho_d", _compute_inline_rho_d),
    "inline_sc": PhysicsTerm("inline_sc", "Inline equation: sc", _compute_inline_sc),
    "inline_msf": PhysicsTerm("inline_msf", "Inline equation: msf", _compute_inline_msf),
    "inline_g_base_14": PhysicsTerm("inline_g_base_14", "Inline equation: g_base", _compute_inline_g_base_14),
    "inline_lambda_t": PhysicsTerm("inline_lambda_t", "Inline equation: lambda_t", _compute_inline_lambda_t),
    "inline_qterm": PhysicsTerm("inline_qterm", "Inline equation: qterm", _compute_inline_qterm),
    "inline_fterm": PhysicsTerm("inline_fterm", "Inline equation: fterm", _compute_inline_fterm),
    "inline_g_5": PhysicsTerm("inline_g_5", "Inline equation: g", _compute_inline_g_5),
    "inline_F_wind": PhysicsTerm("inline_F_wind", "Inline equation: F_wind", _compute_inline_F_wind),
    "inline_F_erode": PhysicsTerm("inline_F_erode", "Inline equation: F_erode", _compute_inline_F_erode),
    "inline_E_react_6": PhysicsTerm("inline_E_react_6", "Inline equation: E_react", _compute_inline_E_react_6),
    "inline_F_tidal_5": PhysicsTerm("inline_F_tidal_5", "Inline equation: F_tidal", _compute_inline_F_tidal_5),
    "inline_F_tail": PhysicsTerm("inline_F_tail", "Inline equation: F_tail", _compute_inline_F_tail),
    "inline_F_tidal_6": PhysicsTerm("inline_F_tidal_6", "Inline equation: F_tidal", _compute_inline_F_tidal_6),
    "inline_F_bridge": PhysicsTerm("inline_F_bridge", "Inline equation: F_bridge", _compute_inline_F_bridge),
    "inline_h_eff": PhysicsTerm("inline_h_eff", "Inline equation: h_eff", _compute_inline_h_eff),
    "inline_ug2_thz": PhysicsTerm("inline_ug2_thz", "Inline equation: ug2_thz", _compute_inline_ug2_thz),
    "inline_psi_2": PhysicsTerm("inline_psi_2", "Inline equation: psi", _compute_inline_psi_2),
    "inline_f_super_1": PhysicsTerm("inline_f_super_1", "Inline equation: f_super", _compute_inline_f_super_1),
    "inline_f_react_1": PhysicsTerm("inline_f_react_1", "Inline equation: f_react", _compute_inline_f_react_1),
    "inline_rho_12": PhysicsTerm("inline_rho_12", "Inline equation: rho", _compute_inline_rho_12),
    "inline_f_fluid_2": PhysicsTerm("inline_f_fluid_2", "Inline equation: f_fluid", _compute_inline_f_fluid_2),
    "inline_f_quantum_1": PhysicsTerm("inline_f_quantum_1", "Inline equation: f_quantum", _compute_inline_f_quantum_1),
    "inline_f_aether": PhysicsTerm("inline_f_aether", "Inline equation: f_aether", _compute_inline_f_aether),
    "inline_f_res_6": PhysicsTerm("inline_f_res_6", "Inline equation: f_res", _compute_inline_f_res_6),
    "inline_f_thz_1": PhysicsTerm("inline_f_thz_1", "Inline equation: f_thz", _compute_inline_f_thz_1),
    "inline_f_total": PhysicsTerm("inline_f_total", "Inline equation: f_total", _compute_inline_f_total),
    "inline_psi_3": PhysicsTerm("inline_psi_3", "Inline equation: psi", _compute_inline_psi_3),
    "inline_f_fluid_3": PhysicsTerm("inline_f_fluid_3", "Inline equation: f_fluid", _compute_inline_f_fluid_3),
    "inline_F_collapse": PhysicsTerm("inline_F_collapse", "Inline equation: F_collapse", _compute_inline_F_collapse),
    "inline_ug3_6": PhysicsTerm("inline_ug3_6", "Inline equation: ug3", _compute_inline_ug3_6),
    "inline_ui": PhysicsTerm("inline_ui", "Inline equation: ui", _compute_inline_ui),
    "inline_um_2": PhysicsTerm("inline_um_2", "Inline equation: um", _compute_inline_um_2),
    "inline_H0_5": PhysicsTerm("inline_H0_5", "Inline equation: H0", _compute_inline_H0_5),
    "inline_rho_vac_ua_prime": PhysicsTerm("inline_rho_vac_ua_prime", "Inline equation: rho_vac_ua_prime", _compute_inline_rho_vac_ua_prime),
    "inline_rho_vac_scm": PhysicsTerm("inline_rho_vac_scm", "Inline equation: rho_vac_scm", _compute_inline_rho_vac_scm),
    "inline_rho_vac_ua": PhysicsTerm("inline_rho_vac_ua", "Inline equation: rho_vac_ua", _compute_inline_rho_vac_ua),
    "inline_term2_13": PhysicsTerm("inline_term2_13", "Inline equation: term2", _compute_inline_term2_13),
    "inline_factor_2": PhysicsTerm("inline_factor_2", "Inline equation: factor", _compute_inline_factor_2),
    "inline_r_41": PhysicsTerm("inline_r_41", "Inline equation: r", _compute_inline_r_41),
    "inline_ug1_1": PhysicsTerm("inline_ug1_1", "Inline equation: ug1", _compute_inline_ug1_1),
    "inline_omega_s_5": PhysicsTerm("inline_omega_s_5", "Inline equation: omega_s", _compute_inline_omega_s_5),
    "inline_g_total": PhysicsTerm("inline_g_total", "Inline equation: g_total", _compute_inline_g_total),
    "inline_g_6": PhysicsTerm("inline_g_6", "Inline equation: g", _compute_inline_g_6),
    "inline_G_F_scaled": PhysicsTerm("inline_G_F_scaled", "Inline equation: G_F_scaled", _compute_inline_G_F_scaled),
    "inline_m_tilde": PhysicsTerm("inline_m_tilde", "Inline equation: m_tilde", _compute_inline_m_tilde),
    "inline_theta_3": PhysicsTerm("inline_theta_3", "Inline equation: theta", _compute_inline_theta_3),
    "inline_mu_1": PhysicsTerm("inline_mu_1", "Inline equation: mu", _compute_inline_mu_1),
    "inline_term2_14": PhysicsTerm("inline_term2_14", "Inline equation: term2", _compute_inline_term2_14),
    "inline_delta_n": PhysicsTerm("inline_delta_n", "Inline equation: delta_n", _compute_inline_delta_n),
    "inline_W": PhysicsTerm("inline_W", "Inline equation: W", _compute_inline_W),
    "inline_eta_5": PhysicsTerm("inline_eta_5", "Inline equation: eta", _compute_inline_eta_5),
    "inline_term2_15": PhysicsTerm("inline_term2_15", "Inline equation: term2", _compute_inline_term2_15),
    "inline_non_local_4": PhysicsTerm("inline_non_local_4", "Inline equation: non_local", _compute_inline_non_local_4),
    "inline_exp_inner": PhysicsTerm("inline_exp_inner", "Inline equation: exp_inner", _compute_inline_exp_inner),
    "inline_base_11": PhysicsTerm("inline_base_11", "Inline equation: base", _compute_inline_base_11),
    "inline_eta_6": PhysicsTerm("inline_eta_6", "Inline equation: eta", _compute_inline_eta_6),
    "inline_Hz_3": PhysicsTerm("inline_Hz_3", "Inline equation: Hz", _compute_inline_Hz_3),
    "inline_cos_term_11": PhysicsTerm("inline_cos_term_11", "Inline equation: cos_term", _compute_inline_cos_term_11),
    "inline_v_3": PhysicsTerm("inline_v_3", "Inline equation: v", _compute_inline_v_3),
    "inline_Hz_t": PhysicsTerm("inline_Hz_t", "Inline equation: Hz_t", _compute_inline_Hz_t),
    "inline_lambda_term_2": PhysicsTerm("inline_lambda_term_2", "Inline equation: lambda_term", _compute_inline_lambda_term_2),
    "inline_quantum_term_3": PhysicsTerm("inline_quantum_term_3", "Inline equation: quantum_term", _compute_inline_quantum_term_3),
    "inline_sys_term": PhysicsTerm("inline_sys_term", "Inline equation: sys_term", _compute_inline_sys_term),
    "inline_omega_13": PhysicsTerm("inline_omega_13", "Inline equation: omega", _compute_inline_omega_13),
    "inline_aSuperFreq": PhysicsTerm("inline_aSuperFreq", "Inline equation: aSuperFreq", _compute_inline_aSuperFreq),
    "inline_aAetherRes": PhysicsTerm("inline_aAetherRes", "Inline equation: aAetherRes", _compute_inline_aAetherRes),
    "inline_aQuantumFreq": PhysicsTerm("inline_aQuantumFreq", "Inline equation: aQuantumFreq", _compute_inline_aQuantumFreq),
    "inline_aAetherFreq": PhysicsTerm("inline_aAetherFreq", "Inline equation: aAetherFreq", _compute_inline_aAetherFreq),
    "inline_aFluidFreq": PhysicsTerm("inline_aFluidFreq", "Inline equation: aFluidFreq", _compute_inline_aFluidFreq),
    "inline_g_comp_2": PhysicsTerm("inline_g_comp_2", "Inline equation: g_comp", _compute_inline_g_comp_2),
    "inline_g_res_1": PhysicsTerm("inline_g_res_1", "Inline equation: g_res", _compute_inline_g_res_1),
    "inline_Hz_val": PhysicsTerm("inline_Hz_val", "Inline equation: Hz_val", _compute_inline_Hz_val),
    "inline_evac_neb": PhysicsTerm("inline_evac_neb", "Inline equation: evac_neb", _compute_inline_evac_neb),
    "inline_evac_ism": PhysicsTerm("inline_evac_ism", "Inline equation: evac_ism", _compute_inline_evac_ism),
    "inline_delta_evac": PhysicsTerm("inline_delta_evac", "Inline equation: delta_evac", _compute_inline_delta_evac),
    "inline_f_super_2": PhysicsTerm("inline_f_super_2", "Inline equation: f_super", _compute_inline_f_super_2),
    "inline_ua_scm_1": PhysicsTerm("inline_ua_scm_1", "Inline equation: ua_scm", _compute_inline_ua_scm_1),
    "inline_omega_i_1": PhysicsTerm("inline_omega_i_1", "Inline equation: omega_i", _compute_inline_omega_i_1),
    "inline_f_react_2": PhysicsTerm("inline_f_react_2", "Inline equation: f_react", _compute_inline_f_react_2),
    "inline_f_quantum_2": PhysicsTerm("inline_f_quantum_2", "Inline equation: f_quantum", _compute_inline_f_quantum_2),
    "inline_f_aether_1": PhysicsTerm("inline_f_aether_1", "Inline equation: f_aether", _compute_inline_f_aether_1),
    "inline_f_fluid_4": PhysicsTerm("inline_f_fluid_4", "Inline equation: f_fluid", _compute_inline_f_fluid_4),
    "inline_a_super_freq_1": PhysicsTerm("inline_a_super_freq_1", "Inline equation: a_super_freq", _compute_inline_a_super_freq_1),
    "inline_a_aether_res_2": PhysicsTerm("inline_a_aether_res_2", "Inline equation: a_aether_res", _compute_inline_a_aether_res_2),
    "inline_a_quantum_freq_1": PhysicsTerm("inline_a_quantum_freq_1", "Inline equation: a_quantum_freq", _compute_inline_a_quantum_freq_1),
    "inline_a_aether_freq_1": PhysicsTerm("inline_a_aether_freq_1", "Inline equation: a_aether_freq", _compute_inline_a_aether_freq_1),
    "inline_a_fluid_freq_1": PhysicsTerm("inline_a_fluid_freq_1", "Inline equation: a_fluid_freq", _compute_inline_a_fluid_freq_1),
    "inline_g_7": PhysicsTerm("inline_g_7", "Inline equation: g", _compute_inline_g_7),
    "inline_one_plus_z": PhysicsTerm("inline_one_plus_z", "Inline equation: one_plus_z", _compute_inline_one_plus_z),
    "inline_Hz_kms_8": PhysicsTerm("inline_Hz_kms_8", "Inline equation: Hz_kms", _compute_inline_Hz_kms_8),
    "inline_force_per_area": PhysicsTerm("inline_force_per_area", "Inline equation: force_per_area", _compute_inline_force_per_area),
    "inline_a_dust_base": PhysicsTerm("inline_a_dust_base", "Inline equation: a_dust_base", _compute_inline_a_dust_base),
    "inline_mag_vB": PhysicsTerm("inline_mag_vB", "Inline equation: mag_vB", _compute_inline_mag_vB),
    "inline_force_2": PhysicsTerm("inline_force_2", "Inline equation: force", _compute_inline_force_2),
    "inline_vac_ratio_3": PhysicsTerm("inline_vac_ratio_3", "Inline equation: vac_ratio", _compute_inline_vac_ratio_3),
    "inline_g_grav": PhysicsTerm("inline_g_grav", "Inline equation: g_grav", _compute_inline_g_grav),
    "inline_g_BH_1": PhysicsTerm("inline_g_BH_1", "Inline equation: g_BH", _compute_inline_g_BH_1),
    "inline_t_10": PhysicsTerm("inline_t_10", "Inline equation: t", _compute_inline_t_10),
    "inline_t_11": PhysicsTerm("inline_t_11", "Inline equation: t", _compute_inline_t_11),
    "inline_pert_5": PhysicsTerm("inline_pert_5", "Inline equation: pert", _compute_inline_pert_5),
    "inline_SCm_contrib_9": PhysicsTerm("inline_SCm_contrib_9", "Inline equation: SCm_contrib", _compute_inline_SCm_contrib_9),
    "inline_SCm_contrib_10": PhysicsTerm("inline_SCm_contrib_10", "Inline equation: SCm_contrib", _compute_inline_SCm_contrib_10),
    "inline_SCm_contrib_11": PhysicsTerm("inline_SCm_contrib_11", "Inline equation: SCm_contrib", _compute_inline_SCm_contrib_11),
    "inline_FU_2": PhysicsTerm("inline_FU_2", "Inline equation: FU", _compute_inline_FU_2),
    "inline_compressed_g_1": PhysicsTerm("inline_compressed_g_1", "Inline equation: compressed_g", _compute_inline_compressed_g_1),
    "inline_resonance_g_1": PhysicsTerm("inline_resonance_g_1", "Inline equation: resonance_g", _compute_inline_resonance_g_1),
    "inline_ua_base": PhysicsTerm("inline_ua_base", "Inline equation: ua_base", _compute_inline_ua_base),
    "inline_breakdown": PhysicsTerm("inline_breakdown", "Inline equation: breakdown", _compute_inline_breakdown),
    "inline_attraction": PhysicsTerm("inline_attraction", "Inline equation: attraction", _compute_inline_attraction),
    "inline_M_bh_over_d_g": PhysicsTerm("inline_M_bh_over_d_g", "Inline equation: M_bh_over_d_g", _compute_inline_M_bh_over_d_g),
    "inline_mod_factor_1": PhysicsTerm("inline_mod_factor_1", "Inline equation: mod_factor", _compute_inline_mod_factor_1),
    "inline_sum_3": PhysicsTerm("inline_sum_3", "Inline equation: sum", _compute_inline_sum_3),
    "inline_mu_j_7": PhysicsTerm("inline_mu_j_7", "Inline equation: mu_j", _compute_inline_mu_j_7),
    "inline_exp_term_3": PhysicsTerm("inline_exp_term_3", "Inline equation: exp_term", _compute_inline_exp_term_3),
    "inline_phi_hat_4": PhysicsTerm("inline_phi_hat_4", "Inline equation: phi_hat", _compute_inline_phi_hat_4),
    "inline_heaviside_factor_1": PhysicsTerm("inline_heaviside_factor_1", "Inline equation: heaviside_factor", _compute_inline_heaviside_factor_1),
    "inline_quasi_factor": PhysicsTerm("inline_quasi_factor", "Inline equation: quasi_factor", _compute_inline_quasi_factor),
    "inline_cos_term_12": PhysicsTerm("inline_cos_term_12", "Inline equation: cos_term", _compute_inline_cos_term_12),
    "inline_rho_sum_1": PhysicsTerm("inline_rho_sum_1", "Inline equation: rho_sum", _compute_inline_rho_sum_1),
    "inline_M_s_over_d_g": PhysicsTerm("inline_M_s_over_d_g", "Inline equation: M_s_over_d_g", _compute_inline_M_s_over_d_g),
    "inline_um_contrib": PhysicsTerm("inline_um_contrib", "Inline equation: um_contrib", _compute_inline_um_contrib),
    "inline_ug3_7": PhysicsTerm("inline_ug3_7", "Inline equation: ug3", _compute_inline_ug3_7),
    "inline_dg_ly": PhysicsTerm("inline_dg_ly", "Inline equation: dg_ly", _compute_inline_dg_ly),
    "inline_factor_3": PhysicsTerm("inline_factor_3", "Inline equation: factor", _compute_inline_factor_3),
    "inline_initial": PhysicsTerm("inline_initial", "Inline equation: initial", _compute_inline_initial),
    "inline_M_bh_6": PhysicsTerm("inline_M_bh_6", "Inline equation: M_bh", _compute_inline_M_bh_6),
    "inline_f_feedback_1": PhysicsTerm("inline_f_feedback_1", "Inline equation: f_feedback", _compute_inline_f_feedback_1),
    "inline_exp_term_4": PhysicsTerm("inline_exp_term_4", "Inline equation: exp_term", _compute_inline_exp_term_4),
    "inline_cos_term_13": PhysicsTerm("inline_cos_term_13", "Inline equation: cos_term", _compute_inline_cos_term_13),
    "inline_orig_f_2": PhysicsTerm("inline_orig_f_2", "Inline equation: orig_f", _compute_inline_orig_f_2),
    "inline_result_27": PhysicsTerm("inline_result_27", "Inline equation: result", _compute_inline_result_27),
    "inline_u_with": PhysicsTerm("inline_u_with", "Inline equation: u_with", _compute_inline_u_with),
    "inline_u_without": PhysicsTerm("inline_u_without", "Inline equation: u_without", _compute_inline_u_without),
    "inline_delta_percent": PhysicsTerm("inline_delta_percent", "Inline equation: delta_percent", _compute_inline_delta_percent),
    "inline_m_final": PhysicsTerm("inline_m_final", "Inline equation: m_final", _compute_inline_m_final),
    "inline_norm_factor": PhysicsTerm("inline_norm_factor", "Inline equation: norm_factor", _compute_inline_norm_factor),
    "inline_norm": PhysicsTerm("inline_norm", "Inline equation: norm", _compute_inline_norm),
}


def compute_equation(name: str, params: dict) -> float:
    """Compute a named equation with given parameters."""
    if name in ALL_EQUATIONS:
        return ALL_EQUATIONS[name].compute(params)
    return 0.0


def list_equations() -> list:
    """List all available equation names."""
    return list(ALL_EQUATIONS.keys())


def get_equation_info(name: str) -> dict:
    """Get information about a specific equation."""
    if name in ALL_EQUATIONS:
        eq = ALL_EQUATIONS[name]
        return {
            'name': eq.name,
            'description': eq.description
        }
    return {}


EQUATION_COUNT = 3565
CPP_EQUATIONS_AVAILABLE = True
