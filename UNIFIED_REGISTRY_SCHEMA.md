# UNIFIED_REGISTRY_SCHEMA.md — Master Registry column definitions + input provenance

**Generated by:** `registry_generator.py` (idempotent; read-only inputs; reserved-name outputs only)
**First generation:** 2026-07-22 (R0 session 1) — 2,537 rows, SHA-256 `3ba77c9f…`
**Session-2 regeneration:** 2026-07-22 — **2,543 rows**, SHA-256 `a9d8d691567f567c567d06698b8bbe8b222ec38e673225f96f0655051e0f1d7d`
**Session-2 additions:** corpus-citation scan (all 2,228 whitepapers → `UNIFIED_REGISTRY_CORPUS_CITATIONS.csv`, 2,149 distinct papers) merged into `corpus_citations` (2,186 row hits); inventory parser upgraded to full `**N. name**` capture (12/12 itemized entries)
**Session-3 (R0 COMPLETE):** 2026-07-22 — **2,544 rows** (PAPER_1072 wiring self-registered — pipeline live), SHA-256 `471ea383a6e77501cee44759a7027bbe4535a4c0dc88b0b7302b992b2d19633f`. Cross-language columns populated: **cpp_sites 496 matches** (from 545 C++ function candidates in uqff_exact_closures.cpp + MAIN_1_CoAnQi.cpp), **lean_sites 3 matches** (6 formal/*.lean files — low match expected, Lean naming differs; R3 improves). Residual machinery verified: all 348 value+reference odmap pairs already carry June residuals (0 blank); computation path in place for R1-assigned references. Determinism fix: sorted token iteration (set-order nondeterminism caught and eliminated). **Registry is ready for R1 adjudication.**

---

## Input provenance (all READ-ONLY, hash-pinned in fidelity gate)

| Input | Rows contributed | Origin tag |
|---|:-:|:-:|
| `PRIMITIVES_RECONCILIATION.csv` | 16 | `primitives` |
| `uqff_pure_calculator.PARADOX_TO_CLOSURE` (1,321 keys → 1,123 unique closures, **all 1,123 live-called, 0 errors**) | 1,123 | `dispatch` |
| `OVERDETERMINATION_MAP.csv` | 1,392 | `odmap` |
| `CLOSED_CONSTANTS_INVENTORY.md` (parser first-pass: 6/52 — see Known Limitations) | 6 | `inventory` |

Companion outputs: `UNIFIED_REGISTRY_GAPS.csv` (1,044 rows — odmap GAP-status observables), `UNIFIED_REGISTRY_DUPLICATES.csv` (169 rows — 167 dispatch alias-sets + 2 cross-origin matches).

## Columns

| Column | Meaning | Populated in |
|---|---|:-:|
| `quantity` | canonical lowercase key (dispatch primary key / observable / primitive name) | R0 |
| `origin` | {primitives, dispatch, odmap, inventory} — source namespace; unification happens in R1 | R0 |
| `kind` | {primitive, closure, observable, kernel_constant, lattice_node} | R0 (partial) |
| `canonical_route` | R1 verdict: YES / confirmation-N | **R1** |
| `formula` | closed form in primitives (odmap geometry field first-pass) | R0 partial → R1 |
| `value` | live-computed `primary_result` (dispatch) or recorded value (odmap/primitives) | R0 |
| `reference` | SI/CODATA/PDG/observed comparison value (odmap target field) | R0 partial |
| `residual_pct` | FULL-precision residual (PAPER_2129 rule: never vs rounded leads) | R0 partial → R0 s2 |
| `sector` / `phi_variant` | PAPER_2129 sector-selection assignments | **R1** |
| `paper_source` | seminal PAPER_N (regex-extracted from key/closure name; odmap primary_source) | R0 |
| `confirmations` | alias count (dispatch) → independent-derivation count | R0 → R1 |
| `py_sites` / `cpp_sites` / `lean_sites` | cross-language implementation sites | py: R0; cpp/lean: **R0 s2/R3** |
| `corpus_citations` | whitepaper citation count | **R0 s2** (corpus scan) |
| `status` | {locked, derived, verified_80_80, EXACT, GAP, TENSION, ERROR:*} | R0 |

## Known limitations (updated after R0 session 2)

1. ~~Inventory parser 6/52~~ → **RESOLVED session 2**: 12/12 itemized entries captured. NOTE: the inventory's "52" includes ~40 constants described only in prose ranges ("Library phased synthesis Ranges 1155-1180 → …") — not itemized, hence not machine-extractable; they enter the registry through their dispatch/odmap rows instead. Inventory rows carry the document's own 2026-05-28 fitted-vs-derived verification flag in their status.
2. `cpp_sites`/`lean_sites` blank — C++/Lean scan queued (session 3 / R3).
3. ~~`corpus_citations` blank~~ → **RESOLVED session 2**: populated from the full-corpus scan (2,186 hits). G-literal files: 168 (exactly cross-validates July recon); c-literal files: 57.
4. Cross-origin unification not attempted (by design — that is R1 adjudication).
5. 1,044 odmap GAP rows imported as-is; June-era symbolic/geometry placeholders needing R1 triage, not new gaps.
6. Corpus-scan finding worth flagging for R1: **PAPER_1072 (U_m Heaviside amplifier, cited by 799 files) is the 6th most-cited paper in the corpus and remains UNWIRED per CLAUDE.md** — highest-leverage wiring candidate identified by citation count.

## Idempotency contract

Two consecutive runs produce byte-identical output (verified 2026-07-22). No timestamps in CSV. Run metadata lives here and in SESSION_LOG only.
