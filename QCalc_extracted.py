from dpm_helpers import dpm_emergent_ug1, dpm_emergent_ug2

# ==============================================================================
# AUTO-GENERATED PHYSICS CALCULATORS
# ==============================================================================
# Source: MAIN_1_CoAnQi.cpp (C++ PhysicsTerm classes)
# Generator: cpp_to_qcalc_converter.py
# Date: 2026-02-15
# 
# NAMING CONVENTION: ALL NAMES PRESERVED EXACTLY FROM C++ SOURCE
# - Class names unchanged
# - Constant names unchanged  
# - Method signatures preserved
# ==============================================================================

# ========== AUTO-EXTRACTED CONSTANTS FROM C++ ==========
# Source: MAIN_1_CoAnQi.cpp
# Naming: Preserved exactly from C++ source
EXTRACTED_CONSTANTS = {
    'M_PI': 3.141592653589793,
    'PI': 3.141592653589793,
    'G': 6.6743e-11,  # Gravitational constant
    'c_light': 3e8,  # Speed of light
    'hbar': 1.0546e-34,  # Reduced Planck constant
    'M_sun': 1.989e30,  # Solar mass
    'epsilon_0': 8.854187817e-12,  # Vacuum permittivity
    'mu_0': 4 * math.pi * 1e-7,  # Vacuum permeability
    'G': 6.6743e-11,
    'c': 3e8,
    'm_e': 9.11e-31,
    'q': 1.6e-19,
    'hbar': 1.0546e-34,
    'mu_B': 9.274e-24,
    'g_Lande': 2.0,
    'F0': 1.83e71,
    'k_LENR': 1e-10,
    'omega_LENR': 2 * math.pi * 1.25e12,
    'k_neutron': 1e10,
    'sigma_n': 1e-4,
    'k_rel': 1e-10,
    'E_cm_astro': 1.24e24,
    'E_cm': 3.0264e-8,
    'x2': -1.35e172,
    'G': 6.6743e-11,
    'c': 3e8,
    'm_e': 9.11e-31,
    'q': 1.6e-19,
    'hbar': 1.0546e-34,
    'mu_B': 9.274e-24,
    'g_Lande': 2.0,
    'F0': 1.83e71,
    'k_LENR': 1e-10,
    'omega_LENR': 2 * math.pi * 1.25e12,
    'x2': -1.35e172,
    'k1': getDynamicParameter("k1", 1.0),
    'ki': getDynamicParameter("ki", 1.0),
    'k_eta': 2.75e8,
    'SSQ': 1.0,
    'N_QUANTUM': 26.0,
    'PI_SOURCE4': 3.141592653589793,
    'c_SOURCE4': 3.0e8,  # Speed of light (m/s)
    'G_SOURCE4': 6.67430e-11,  # Gravitational constant (m^3 kg^-1 s^-2)
    'H0_SOURCE4': 2.269e-18,  # Hubble constant (s^-1)
    'Lambda_SOURCE4': 1.1e-52,  # Cosmological constant (m^-2)
    'hbar_SOURCE4': 1.0546e-34,  # Reduced Planck constant (J�s)
    'num_strings_SOURCE4': 1e9,  # Number of magnetic strings (billions)
    'M_SUN': 1.989e30,  # Solar mass in kg
    'MPC_TO_M': 3.086e22,  # Megaparsec to meters
    'KPC_TO_M': 3.086e19,  # Kiloparsec to meters
    'G_CONST': 6.6743e-11,  # Gravitational constant m�/kg/s�
    'K_BOLTZ': 1.381e-23,  # Boltzmann constant J/K
    'C_LIGHT': 2.998e8,  # Speed of light m/s
    'PI_S114': 3.141592653589793,
    'K_R_S114': 1.0,  # Electrostatic barrier constant
    'Z_MAX_S114': 1000.0,  # Max Z for f_UA' and f_SCm
    'NU_THz_S114': 1e12,  # THz frequency (Hz)
    'RHO_VAC_UA_S114': 7.09e-36,  # Vacuum energy density [UA] (J/m^3)
    'H_Z_BASE_S114': 2.268e-18,  # Hubble constant base (s^-1)
    'E_RAD_S114': 0.1554,  # Radiation energy fraction
    'T_SF_S114': 3.156e13,  # Star formation timescale (s)
    'M_SF_S114': 1.5,  # SFR adjustment
    'PI_S115': 3.141592653589793,
    'K_R_S115': 1.0,
    'Z_MAX_S115': 1000.0,
    'NU_THz_S115': 1e12,
    'RHO_VAC_UA_S115': 7.09e-36,
    'H_Z_BASE_S115': 2.268e-18,
    'E_RAD_S115': 0.1554,
    'T_SF_S115': 3.156e13,
    'M_SF_S115': 1.5,
    'NUM_STATES_S115': 26,
    'QUANTUM_STATES_S116': 26,  # Sacred number (consistent with SOURCE115)
    'MAX_NODES_S116': 1000000,  # For 2025 hardware
    'MAX_DEPTH_S116': 8,  # Irreducible depth we actually run
    'PI_UNITY_S116': 3.14159265358979323846264338327950288419716939937510,
    'MAYAN_BAKTUN': 144000.0,
    'MAYAN_KATUN': 7200.0,
    'MAYAN_TUN': 360.0,
    'BIBLE_GENERATION': 33.333333333333333,  # Christ + Enoch resonance
    'GOLDEN_CYCLE': 25920.0,  # Precession
    'CONSCIOUSNESS_FREQ': 7.83,  # Schumann base
    'INFINITY_RATIO': 1.000000001,  # Infinity curve seed
    'NUM_STATES_S116': 26,
    'PI_MATH_S116': 3.14159265358979323846,
    'ALPHA_EM_INTEGRATED': 1.0 / 137.0,  # Fine structure constant
    'BIO_QUANTUM_FREQ_HZ': 400.0,  # Biological quantum frequency Hz
    'DELTA_E_PHASE_J': 5.52e-17,  # Phase energy delta J
    'DELTA_M_J': 0.78e6 * 1.602e-19,  # Mass delta J
    'ELECTRON_RADIUS_SCALE_FACTOR': 10.0,  # Electron radius scale
    'E_JET_J': 5.52e-18,  # Jet energy J
    'E_RAD_INTEGRATED': 0.1554,  # Radiation energy
    'E_ATOMIC_J': 1e-18,  # Atomic energy scale J
    'ECM_J': 189e9 * 1.602e-19,  # Center-of-mass energy J
    'ECM_ASTRO_J': 1.24e24,  # Astronomical energy J
    'E_FERMI_J': 10.0 * 1.602e-19,  # Fermi energy J
    'E_VAC_ISM_J_M3': 7.09e-37,  # ISM vacuum energy J/m³
    'E_VAC_NEB_J_M3': 7.09e-36,  # Nebula vacuum energy J/m³
    'DELTA_E_VAC_J': 6.381e-36,  # Vacuum energy fluctuation J
    'B_GRADIENT_T_M': 1e-7,  # Magnetic field gradient T/m
    'F_SUPER_N': 6.287e-19,  # Superconductive force N
    'DPM_GRAVITY_COUPLING': 1.0,  # DPM gravity coupling
    'DPM_LIGHT_COUPLING': 0.01,  # DPM light coupling
    'DPM_MOMENTUM_COUPLING': 0.93,  # DPM momentum coupling
    'DPM_STABILITY_PARAM': 0.01,  # DPM stability parameter
    'K_ACTION_COUPLING': 1e-6,  # Action coupling
    'K_DARK_ENERGY_COUPLING': 1e-30,  # Dark energy coupling
    'K_LENR_COUPLING': 1e-10,  # LENR coupling
    'K_NEUTRON_COUPLING': 1e10,  # Neutron coupling
    'K_RELATIVISTIC_COUPLING': 1e-10,  # Relativistic coupling
    'K_QUANTUM_COUPLING': 1.0,  # Quantum coupling
    'K_RESONANCE_COUPLING': 1.0,  # Resonance coupling
    'K_ETA_MESON_BASE': 2.75e8,  # Eta meson base coupling
    'SPACETIME_CURVATURE_INV_M2': 1e-22,  # Spacetime curvature 1/m²
    'GAMMA_DECAY_FACTOR': 1.0,  # Decay gamma factor
    'HUBBLE_Z_BASE_INV_S': 2.268e-18,  # Hubble constant variant 1/s
    'F_RELATIVISTIC_BASE_N': 4.30e33,  # Relativistic force base N
    'DELTA_X_DELTA_P': 1e-68,  # Position-momentum uncertainty
    'GAUSS_TO_TESLA': 1e-4,  # Gauss to Tesla conversion
    'ERG_PER_S_TO_WATT': 1e-7,  # erg/s to Watt conversion
    'KEV_TO_KELVIN': 1.16045e7,  # keV to Kelvin conversion
    'PARSEC_M': 3.086e16,  # Parsec in meters
    'KILOPARSEC_M': 3.086e19,  # Kiloparsec in meters
    'MEGAPARSEC_M': 3.086e22,  # Megaparsec in meters
    'LIGHT_YEAR_M': 9.461e15,  # Light year in meters
    'M_SOLAR_KG': 1.989e30,  # Solar mass kg
    'R_SOLAR_M': 6.96e8,  # Solar radius m
    'FRAME_TIME_S': 100.0,  # Frame time seconds
    'END_TIME_S': 30.78,  # Simulation end time seconds
    'MAX_DEPTH_HYPERGRAPH_INTEGRATED': 8,  # Max hypergraph depth
    'MAX_NODES_HYPERGRAPH_INTEGRATED': 1000000,  # Max hypergraph nodes
    'MAX_QUERY_LENGTH': 6000,
    'MAX_WINDOWS': 21,  # Increased for ALMA Cycle 12
    'NASA_API_KEY_1': "PNJaNeFWqMb2g0CEQGqJePkndqYfKvBzq6XJqAwg",
    'NASA_API_KEY_2': "FJnBo64nLFqExHwDchrcaf101D8wmGSm0cF27clz",
    'MAST_API_KEY': "emXvt90Htf0U4RogKTB5lqSxClUeg2pvMQxvZciM",
    'OPENAI_API_KEY': "your_openai_api_key_here",
    'COGNITO_CLIENT_ID': "your_cognito_client_id",
    'COGNITO_REGION': "us-east-1",
    'E_F': 10 * 1.602e-19,
    'EFSC_PI': 3.604e-16,
    'W_RES': 1.424e14,
    'DELTA_E_PHASE': 5.52e-17,
    'E_JET': 5.52e-18,
    'DELTA_M': 0.78e6 * 1.602e-19,
    'ALPHA_EM': 1.0 / 137.0,
    'PI_FREQ': 3.14,
    'NEGATIVE_TIME': -2512,
    'VACUUM_ENERGY': 1e-12,
    'BIO_QUANTUM_FREQ': 400,
    'REACTOR_EFFICIENCY': 555,
    'PROTON_RADIUS': 20,
    'ELECTRON_RADIUS': 10,
    'ORBIT_RADIUS': 150,
    'NUM_ELECTRONS': 2,
    'WIDTH': 350,
    'HEIGHT': 1000,
    'START_TIME': 15.03,
    'END_TIME': 30.78,
    'FRAME_TIME': 100,
    'SPINDLE_ORB_X': WIDTH / 2,
    'SPINDLE_ORB_Y': HEIGHT / 2,
    'M_PI': 3.14159265358979323846,
    'M_PI': 3.14159265358979323846,
    'M_PI': 3.14159265358979323846,
    'PI_VAL': 3.141592653589793,
    'M_PI': 3.14159265358979323846,
    'M_PI': 3.14159265358979323846,
}

# Merge into main CONSTANTS
CONSTANTS.update(EXTRACTED_CONSTANTS)

# ========== EXTRACTED CALCULATOR CLASSES ==========

class DynamicVacuumTerm:
    """
    Time-varying vacuum energy contribution (rho_vac * sin(freq*t))
    
    Source: MAIN_1_CoAnQi.cpp (lines 1120-1146)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "DynamicVacuum"
        self.description = "Time-varying vacuum energy contribution (rho_vac * sin(freq*t))"
        self.metadata = {}
        self.metadata['version'] = '2.0'
        self.metadata['source'] = 'Source134.cpp, Source162.cpp'
        self.amplitude = 0.0
        self.frequency = 0.0
    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return coupling * amplitude * rho_vac * math.sin(frequency * t)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{DynamicVacuumTerm}",
            description=self.description
        )


class QuantumCouplingTerm:
    """
    Non-local quantum entanglement coupling (hbar^2 / (M*r^2) * cos(t))
    
    Source: MAIN_1_CoAnQi.cpp (lines 1152-1177)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "QuantumCoupling"
        self.description = "Non-local quantum entanglement coupling (hbar^2 / (M*r^2) * cos(t))"
        self.metadata = {}
        self.metadata['version'] = '2.0'
        self.metadata['source'] = 'Source134.cpp, Source162.cpp'
        self.coupling_strength = 0.0
    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return alpha * coupling_strength * (hbar * hbar) / (M * r * r) * math.cos(t / 1e6)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{QuantumCouplingTerm}",
            description=self.description
        )


class DarkMatterHaloTerm:
    """
    NFW dark matter halo contribution (G*M*ln(1+x)/(r*x))
    
    Source: MAIN_1_CoAnQi.cpp (lines 1183-1214)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "DarkMatterHalo"
        self.description = "NFW dark matter halo contribution (G*M*ln(1+x)/(r*x))"
        self.metadata = {}
        self.metadata['version'] = '1.0'
        self.metadata['source'] = 'Source5.cpp'
        self.metadata['profile'] = 'NFW'
        self.M_halo = 0.0
        self.r_scale = 0.0
    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        if r == 0.0  or  r_scale == 0.0: return 0.0
        x = r / r_scale
        # double rho_0 = M_halo / (4.0 * M_PI * r_scale * r_scale * r_scale * (log(2.0) - 0.5));
        return G * M_halo * math.log(1 + x) / (r * x)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{DarkMatterHaloTerm}",
            description=self.description
        )


class VacuumEnergyTerm:
    """
    Vacuum energy fluctuation (lambda*E_vac*(1 + 0.1*sin(t)))
    
    Source: MAIN_1_CoAnQi.cpp (lines 1220-1244)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "VacuumEnergy"
        self.description = "Vacuum energy fluctuation (lambda*E_vac*(1 + 0.1*sin(t)))"
        self.metadata = {}
        self.metadata['version'] = '1.0'
        self.metadata['source'] = 'Source5.cpp, Source13_Enhanced.cpp'
        self.E_vac_scale = 0.0
        self.lambda_ = 0.0
    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return lambda_ * E_vac_scale * (1.0 + 0.1 * math.sin(1e-10 * t))
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{VacuumEnergyTerm}",
            description=self.description
        )


class QuantumEntanglementTerm:
    """
    Non-local quantum entanglement (hbar^2/(M*r^2)*cos(t/1e6))
    
    Source: MAIN_1_CoAnQi.cpp (lines 1250-1274)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "QuantumEntanglement"
        self.description = "Non-local quantum entanglement (hbar^2/(M*r^2)*cos(t/1e6))"
        self.metadata = {}
        self.metadata['version'] = '1.0'
        self.metadata['source'] = 'Source13_Enhanced.cpp'
        self.coupling_strength = 0.0
    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return coupling_strength * (hbar * hbar) / (M * r * r) * math.cos(t / 1e6)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{QuantumEntanglementTerm}",
            description=self.description
        )


class CosmicNeutrinoTerm:
    """
    CNB energy density contribution (n_nu * k_B * T_cnb / r^2)
    
    Source: MAIN_1_CoAnQi.cpp (lines 1280-1308)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "CosmicNeutrino"
        self.description = "CNB energy density contribution (n_nu * k_B * T_cnb / r^2)"
        self.metadata = {}
        self.metadata['version'] = '1.0'
        self.metadata['source'] = 'Source162.cpp'
        self.metadata['description'] = 'Cosmic Neutrino Background'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        k_B = 1.380649e-23
        energy_density = n_nu * k_B * T_cnb
        return energy_density / (r * r)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{CosmicNeutrinoTerm}",
            description=self.description
        )


class MultiSystemUQFFTerm:
    """
    Multi-system UQFF (NGC685/3507/3511/AT2024tvd) with DPM resonance, LENR, gravity compressed
    
    Source: MAIN_1_CoAnQi.cpp (lines 1314-1429)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "MultiSystemUQFF"
        self.description = "Multi-system UQFF (NGC685/3507/3511/AT2024tvd) with DPM resonance, LENR, gravity compressed"
        self.metadata = {}
        self.metadata['version'] = '1.0'
        self.metadata['source'] = 'Source163.cpp'
        self.metadata['systems'] = 'NGC685, NGC3507, NGC3511, AT2024tvd'
        self.system_M = 0.0
        self.system_r = 0.0
        self.system_L_X = 0.0
        self.system_B0 = 0.0
        self.system_omega0 = 0.0
    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        G = 6.6743e-11
        c = 3e8
        m_e = 9.11e-31
        q = 1.6e-19
        V = 1e-3
        theta = math.pi / 4.0
        # DPM coefficients
        # LENR term (dominant)
        k_LENR = 1e-10
        omega_LENR = 2 * math.pi * 1.25e12
        # Core terms
        term_vac = 7.09e-36 * DPM_stability
        # Resonance term
        g_Lande = 2.0
        mu_B = 9.274e-24
        hbar = 1.0546e-34
        # Gravity compressed
        # Buoyancy
        buoyancy = beta_i * V_infl * rho_vac_A * a_universal
        # Resonance U_r
        resonance = 2.0 * F_rel
        # Scale by x2 (quadratic root approximation)
        return (term_mom + term_grav + term_vac + LENR_term + term_res) * x2 + gravity_compressed + resonance + buoyancy
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{MultiSystemUQFFTerm}",
            description=self.description
        )


class DPMResonanceTerm:
    """
    DPM quantum magnetic resonance (Lande factor based)
    
    Source: MAIN_1_CoAnQi.cpp (lines 1435-1465)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "DPMResonance"
        self.description = "DPM quantum magnetic resonance (Lande factor based)"
        self.metadata = {}
        self.metadata['version'] = '1.0'
        self.metadata['source'] = 'Source163.cpp'
        self.metadata['equation'] = 'DPM_resonance = (g * mu_B * B0) / (hbar * omega0)'
        self.g_Lande = 0.0
        self.mu_B = 0.0
        self.B0 = 0.0
        self.omega0 = 0.0
    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        hbar = 1.0546e-34
        return (g_Lande * mu_B * B) / (hbar * omega)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{DPMResonanceTerm}",
            description=self.description
        )


class LENRExtendedTerm:
    """
    Extended LENR with frequency ratio squared scaling
    
    Source: MAIN_1_CoAnQi.cpp (lines 1471-1500)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "LENRExtended"
        self.description = "Extended LENR with frequency ratio squared scaling"
        self.metadata = {}
        self.metadata['version'] = '1.0'
        self.metadata['source'] = 'Source163.cpp'
        self.metadata['equation'] = 'F_LENR = k * (omega_LENR / omega0)^2'
        self.k_LENR = 0.0
        self.omega_LENR = 0.0
        self.omega0 = 0.0
    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return k * (omega_L / omega_0) ** (2.0)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{LENRExtendedTerm}",
            description=self.description
        )


class SMBHAccretionTerm:
    """
    SMBH accretion disk luminosity (radiative efficiency model)
    
    Source: MAIN_1_CoAnQi.cpp (lines 1506-1535)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "SMBHAccretion"
        self.description = "SMBH accretion disk luminosity (radiative efficiency model)"
        self.metadata = {}
        self.metadata['version'] = '1.0'
        self.metadata['source'] = 'Source163.cpp'
        self.metadata['equation'] = 'L_acc = eta * M_dot * c^2'
        self.M_bh = 0.0
        self.eta = 0.0
    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        c = 3e8
        M_dot = 0.1 * M / t_scale
        return eta * M_dot * c * c
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{SMBHAccretionTerm}",
            description=self.description
        )


class TDETerm:
    """
    Tidal Disruption Event with exponential + power-law decay
    
    Source: MAIN_1_CoAnQi.cpp (lines 1541-1569)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "TDE"
        self.description = "Tidal Disruption Event with exponential + power-law decay"
        self.metadata = {}
        self.metadata['version'] = '1.0'
        self.metadata['source'] = 'Source163.cpp - AT2024tvd'
        self.metadata['equation'] = 'L_TDE = L_peak * exp(-|dt|/0.3) * (1 + |dt|)^(-5/3)'
        self.L_peak = 0.0
        self.t_peak = 0.0
    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return L * math.exp(-fabs(dt_norm) / 0.3) * (1.0 + fabs(dt_norm)) ** (-5.0 / 3.0)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{TDETerm}",
            description=self.description
        )


class NebulaUQFFTerm:
    """
    Nebula UQFF with gas expansion, ionization, and radiation pressure
    
    Source: MAIN_1_CoAnQi.cpp (lines 1575-1678)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "NebulaUQFF"
        self.description = "Nebula UQFF with gas expansion, ionization, and radiation pressure"
        self.metadata = {}
        self.metadata['version'] = '1.0'
        self.metadata['source'] = 'Source164.cpp'
        self.metadata['systems'] = 'NGC3596, NGC1961, NGC5335, NGC2014, NGC2020'
        self.system_M = 0.0
        self.system_r = 0.0
        self.system_L_X = 0.0
        self.system_T = 0.0
        self.rho_gas = 0.0
    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        G = 6.6743e-11
        # Gravity compressed
        # Gas nebula integration (from Source164)
        k_B = 1.380649e-23
        m_p = 1.6726e-27
        c = 3e8
        # Thermal pressure
        P_thermal = rho_gas * k_B * system_T / m_p
        # Radiation pressure
        # Expansion force
        F_expansion = 4.0 * math.pi * system_r * system_r * rho_gas * v_exp * v_exp
        # Ionization contribution
        F_ionization = ionization * P_thermal * 4.0 * math.pi * system_r * system_r
        # Combined nebula force
        gas_nebula = F_expansion + F_ionization + P_rad * system_r * system_r
        return gravity + gas_nebula
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{NebulaUQFFTerm}",
            description=self.description
        )


class GasIonizationTerm:
    """
    Gas ionization fraction (Stromgren sphere model)
    
    Source: MAIN_1_CoAnQi.cpp (lines 1684-1728)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "GasIonization"
        self.description = "Gas ionization fraction (Stromgren sphere model)"
        self.metadata = {}
        self.metadata['version'] = '1.0'
        self.metadata['source'] = 'Source164.cpp'
        self.metadata['equation'] = 'ion_frac = ionization_rate / (n_gas * V)'
        self.L_X = 0.0
        self.r_nebula = 0.0
        self.rho_gas = 0.0
    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        q_val = 1.6e-19
        m_p = 1.6726e-27
        # Ionization energy (Hydrogen)
        photon_energy = 13.6 * q_val
        # Ionization rate (photons/s)
        # Number density
        # Volume
        # Ionization fraction
        return ion_frac * 1e40
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{GasIonizationTerm}",
            description=self.description
        )


class NebulaExpansionTerm:
    """
    Nebula expansion with time-dependent radius and density
    
    Source: MAIN_1_CoAnQi.cpp (lines 1734-1770)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "NebulaExpansion"
        self.description = "Nebula expansion with time-dependent radius and density"
        self.metadata = {}
        self.metadata['version'] = '1.0'
        self.metadata['source'] = 'Source164.cpp'
        self.metadata['equation'] = 'r(t) = r0 * (1 + v_exp * t / r0)'
        self.r0 = 0.0
        self.v_expansion = 0.0
    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        # Hubble-like expansion
        # Expansion energy
        E_expansion = 0.5 * M * v_exp * v_exp
        return E_expansion / r_t
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{NebulaExpansionTerm}",
            description=self.description
        )


class BuoyancyUQFFTerm:
    """
    Buoyancy UQFF for 
    
    Source: MAIN_1_CoAnQi.cpp (lines 1777-1885)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "BuoyancyUQFF_"
        self.description = "Buoyancy UQFF for "
        self.metadata = {}
        self.metadata['version'] = '1.0'
        self.metadata['source'] = 'Source165.cpp'
        self.metadata['equation'] = 'F_U_Bi_i = integrand * x2 (11-term force)'
        self.system_M = 0.0
        self.system_r = 0.0
        self.system_L_X = 0.0
        self.system_B0 = 0.0
        self.system_omega0 = 0.0
    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        G = 6.6743e-11
        c = 3e8
        m_e = 9.11e-31
        q = 1.6e-19
        hbar = 1.0546e-34
        mu_B = 9.274e-24
        g_Lande = 2.0
        F0 = 1.83e71
        k_LENR = 1e-10
        omega_LENR = 2 * math.pi * 1.25e12
        k_neutron = 1e10
        sigma_n = 1e-4
        k_rel = 1e-10
        E_cm_astro = 1.24e24
        E_cm = 3.0264e-8
        x2 = -1.35e172
        # DPM resonance (Zeeman)
        # LENR term
        # 11-term integrand
        term_base = -F0
        term_vac = 7.09e-36 * 0.01
        term_LENR = LENR
        term_res = 2.0 * q * system_B0 * 1e-3 * 0.707 * DPM_res
        term_neut = k_neutron * sigma_n
        term_neutrino = 9.07e-43
        integrand = term_base + term_mom + term_grav + term_vac + term_LENR + term_res + term_neut + term_rel + term_neutrino
        return integrand * x2
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{BuoyancyUQFFTerm}",
            description=self.description
        )


class InflationBuoyancyTerm:
    """
    Inflation-driven buoyancy force
    
    Source: MAIN_1_CoAnQi.cpp (lines 1891-1916)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "InflationBuoyancy"
        self.description = "Inflation-driven buoyancy force"
        self.metadata = {}
        self.metadata['version'] = '1.0'
        self.metadata['source'] = 'Source165.cpp'
        self.metadata['equation'] = 'U_bi = beta_i * V_infl * rho_vac * a_universal'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return beta_i * V_infl * rho_vac * a_univ
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{InflationBuoyancyTerm}",
            description=self.description
        )


class SuperconductiveTerm:
    """
    Time-dependent superconductivity with oscillating vacuum density
    
    Source: MAIN_1_CoAnQi.cpp (lines 1922-1952)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "Superconductivity"
        self.description = "Time-dependent superconductivity with oscillating vacuum density"
        self.metadata = {}
        self.metadata['version'] = '1.0'
        self.metadata['source'] = 'Source165.cpp'
        self.metadata['equation'] = 'U_i = lambda * (rho_SC/rho_UA) * omega_s * cos(pi*t_n) * (1 + f_TRZ)'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        t_n = t / t_scale
        return lambda_i * (rho_sc / rho_ua) * omega_s * cos_term * (1.0 + f_TRZ)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{SuperconductiveTerm}",
            description=self.description
        )


class NeutronScatteringTerm:
    """
    Neutron scattering cross-section contribution
    
    Source: MAIN_1_CoAnQi.cpp (lines 1958-1981)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "NeutronScattering"
        self.description = "Neutron scattering cross-section contribution"
        self.metadata = {}
        self.metadata['version'] = '1.0'
        self.metadata['source'] = 'Source165.cpp'
        self.metadata['equation'] = 'F_neutron = k_neutron * sigma_n'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return k_neutron * sigma_n
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{NeutronScatteringTerm}",
            description=self.description
        )


class AstroSystemUQFFTerm:
    """
    Astro System UQFF for 
    
    Source: MAIN_1_CoAnQi.cpp (lines 1988-2132)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "AstroSystemUQFF_"
        self.description = "Astro System UQFF for "
        self.metadata = {}
        self.metadata['version'] = '1.0'
        self.metadata['source'] = 'Source166.cpp'
        self.metadata['equation'] = 'F_U_Bi_i = integrand * x2 + gravity + resonance + buoyancy (12-term)'
        self.system_M = 0.0
        self.system_r = 0.0
        self.system_L_X = 0.0
        self.system_B0 = 0.0
        self.system_omega0 = 0.0
    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        G = 6.6743e-11
        c = 3e8
        m_e = 9.11e-31
        q = 1.6e-19
        hbar = 1.0546e-34
        mu_B = 9.274e-24
        g_Lande = 2.0
        F0 = 1.83e71
        k_LENR = 1e-10
        omega_LENR = 2 * math.pi * 1.25e12
        x2 = -1.35e172
        # DPM resonance (Zeeman)
        # LENR term
        # Gas nebula contribution
        gas_nebula = 1e-23 * system_r * system_r * 1e-10
        # 12-term integrand
        term_base = -F0
        term_vac = 7.09e-36 * 0.01
        term_LENR = LENR
        term_res = 2.0 * q * system_B0 * 1e-3 * 0.707 * DPM_res
        term_neut = 1e10 * 1e-4
        term_neutrino = 9.07e-42
        term_gas = gas_nebula
        integrand = term_base + term_mom + term_grav + term_vac + term_LENR + term_res + term_neut + term_rel + term_neutrino + term_gas
        # Add gravity compressed + resonance + buoyancy
        resonance = 2.0 * 4.30e33
        buoyancy = 1.0 * 1e-6 * 1e-30 * 1e12
        return integrand * x2 + gravity + resonance + buoyancy
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{AstroSystemUQFFTerm}",
            description=self.description
        )


class DipoleVortexTerm:
    """
    Dipole vortex species determination with golden ratio cycle
    
    Source: MAIN_1_CoAnQi.cpp (lines 2138-2162)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "DipoleVortex"
        self.description = "Dipole vortex species determination with golden ratio cycle"
        self.metadata = {}
        self.metadata['version'] = '1.0'
        self.metadata['source'] = 'Source166.cpp'
        self.metadata['equation'] = 'dipole * sin(2π * φ * 1.0) where φ = 0.618'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        golden_ratio = 0.618033988749895
        phase = 2.0 * math.pi * golden_ratio * 1.0
        return dipole_base * math.sin(phase)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{DipoleVortexTerm}",
            description=self.description
        )


class QuantumState26Term:
    """
    26-state quantum system with alphabet-like scaling
    
    Source: MAIN_1_CoAnQi.cpp (lines 2168-2195)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "QuantumState26"
        self.description = "26-state quantum system with alphabet-like scaling"
        self.metadata = {}
        self.metadata['version'] = '1.0'
        self.metadata['source'] = 'Source166.cpp'
        self.metadata['equation'] = 'Sum of quantum states n=1 to 26'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        # Sum of 26 quantum states (real part)
        # Each state contributes n (state number)
        sum = 0.0
        # TODO: Convert C++ for loop
        sum += static_cast<double>(n)
        return sum
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{QuantumState26Term}",
            description=self.description
        )


class TriadicScaleTerm:
    """
    Triadic UQFF scaling with enhanced beta_i = 1.0
    
    Source: MAIN_1_CoAnQi.cpp (lines 2201-2226)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "TriadicScale"
        self.description = "Triadic UQFF scaling with enhanced beta_i = 1.0"
        self.metadata = {}
        self.metadata['version'] = '1.0'
        self.metadata['source'] = 'Source166.cpp'
        self.metadata['equation'] = 'beta_i * V_infl * rho_vac * a_universal (triadic)'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return beta_i * V_infl * rho_vac * a_univ
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{TriadicScaleTerm}",
            description=self.description
        )


class UQFFMasterTerm:
    """
    UQFF Master Force for 
    
    Source: MAIN_1_CoAnQi.cpp (lines 2233-2290)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "UQFFMaster_"
        self.description = "UQFF Master Force for "
        self.metadata = {}
        self.metadata['version'] = '1.0'
        self.metadata['source'] = 'Source167.cpp'
        self.metadata['equation'] = 'F_master = U_g1 + U_g3 * f_Ub (June 2025 UQFF framework)'
        self.sfr = 0.0
        self.wind_vel = 0.0
        self.mag_field = 0.0
        self.f_Ub_scale = 0.0
        self.M = 0.0
        self.r = 0.0
    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        # const double Z_MAX = 1000.0;
        # U_g1: DPM with electrostatic barrier
        f_UA_prime = 0.999
        f_SCm = 0.001
        R_EB = 1.0
        nu_THz = 1e12
        theta = math.pi / 2.0
        phi = 0.0
        # U_g3: Combined force (simplified)
        # Master force
        return U_g1 + U_g3 * f_Ub_scale
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{UQFFMasterTerm}",
            description=self.description
        )


class ElectrostaticBarrierTerm:
    """
    Exponential barrier penetration term for electrostatic force
    
    Source: MAIN_1_CoAnQi.cpp (lines 2296-2319)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "ElectrostaticBarrier"
        self.description = "Exponential barrier penetration term for electrostatic force"
        self.metadata = {}
        self.metadata['version'] = '1.0'
        self.metadata['source'] = 'Source167.cpp'
        self.metadata['equation'] = 'exp(-R_EB/r) with R_EB = k_R * Z'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        result = 0.0  # TODO: Implement computation
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{ElectrostaticBarrierTerm}",
            description=self.description
        )


class ElectricFieldTerm:
    """
    Electric field derived from Universal Magnetism
    
    Source: MAIN_1_CoAnQi.cpp (lines 2325-2349)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "ElectricField"
        self.description = "Electric field derived from Universal Magnetism"
        self.metadata = {}
        self.metadata['version'] = '1.0'
        self.metadata['source'] = 'Source167.cpp'
        self.metadata['equation'] = 'E = (U_m / rho_vac_UA) * (1/r)'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return (U_m / rho_vac_UA) * (1.0 / r)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{ElectricFieldTerm}",
            description=self.description
        )


class NeutronProductionTerm:
    """
    Neutron production rate with 26 quantum states
    
    Source: MAIN_1_CoAnQi.cpp (lines 2355-2386)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "NeutronProduction"
        self.description = "Neutron production rate with 26 quantum states"
        self.metadata = {}
        self.metadata['version'] = '1.0'
        self.metadata['source'] = 'Source167.cpp'
        self.metadata['equation'] = 'eta = k_eta * exp(-SSQ*n/26) * exp(-(pi-t)) * (U_m/rho_vac)'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        k_eta = 2.75e8
        SSQ = 1.0
        N_QUANTUM = 26.0
        field_term = U_m / rho_vac_UA
        return k_eta * exp_ssq * exp_pi_t * field_term
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{NeutronProductionTerm}",
            description=self.description
        )


class CelestialBodyTerm:
    """
    Source4 celestial body simulation for 
    
    Source: MAIN_1_CoAnQi.cpp (lines 2392-2425)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "CelestialBody_"
        self.description = "Source4 celestial body simulation for "
        self.metadata = {}
        self.metadata['source'] = 'source4.cpp'
        self.metadata['type'] = 'celestial_simulation'
        self.body_mass = 0.0
        self.body_radius = 0.0
    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        G_const = 6.67430e-11
        # Simplified unified field contribution
        return gravitational * time_decay * oscillation
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{CelestialBodyTerm}",
            description=self.description
        )


class MUGETerm:
    """
    Source4 MUGE compressed gravity calculation
    
    Source: MAIN_1_CoAnQi.cpp (lines 2430-2462)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "MUGE_Compressed"
        self.description = "Source4 MUGE compressed gravity calculation"
        self.metadata = {}
        self.metadata['source'] = 'source4.cpp'
        self.metadata['type'] = 'muge_compressed'
        self.system_mass = 0.0
        self.compression_factor = 0.0
    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        G_const = 6.67430e-11
        # Multi-layer gravity with compression
        compressed = base_gravity * compression_factor
        return compressed * time_mod
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{MUGETerm}",
            description=self.description
        )


class QuasarJetTerm:
    """
    Source4 quasar jet Navier-Stokes simulation
    
    Source: MAIN_1_CoAnQi.cpp (lines 2467-2496)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "QuasarJet"
        self.description = "Source4 quasar jet Navier-Stokes simulation"
        self.metadata = {}
        self.metadata['source'] = 'source4.cpp'
        self.metadata['type'] = 'quasar_jet_simulation'
        self.jet_velocity = 0.0
        self.jet_power = 0.0
    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        # Relativistic jet dynamics
        c = 3e8
        return jet_power * gamma * time_evolution
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{QuasarJetTerm}",
            description=self.description
        )


class ReactorEnergyTerm:
    """
    Source4: SCm reactor efficiency with exponential time decay
    
    Source: MAIN_1_CoAnQi.cpp (lines 2502-2535)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "ReactorEnergy"
        self.description = "Source4: SCm reactor efficiency with exponential time decay"
        self.metadata = {}
        self.metadata['source'] = 'source4.cpp'
        self.metadata['type'] = 'reactor_energy'
        self.metadata['equation'] = 'Ereact = (rho_SCm * v_SCm^2 / rho_A) * exp(-kappa*t)'
        self.rho_SCm = 0.0
        self.v_SCm = 0.0
        self.rho_A = 0.0
        self.kappa = 0.0
    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return (scm_d * v * v / rho) * math.exp(-k * t)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{ReactorEnergyTerm}",
            description=self.description
        )


class MagneticDipoleTerm:
    """
    Source4: Time-varying magnetic dipole moment with SCm enhancement
    
    Source: MAIN_1_CoAnQi.cpp (lines 2541-2575)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "MagneticDipole"
        self.description = "Source4: Time-varying magnetic dipole moment with SCm enhancement"
        self.metadata = {}
        self.metadata['source'] = 'source4.cpp'
        self.metadata['type'] = 'magnetic_dipole'
        self.metadata['equation'] = 'mu_s = (Bs + 0.4*sin(omega_c*t) + SCm) * Rs^3'
        self.Bs_avg = 0.0
        self.omega_c = 0.0
        self.Rs = 0.0
        self.SCm_contrib = 0.0
    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return Bs_t * (R) ** (3)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{MagneticDipoleTerm}",
            description=self.description
        )


class MagneticJetFieldTerm:
    """
    Source4: Oscillating magnetic field in stellar jets
    
    Source: MAIN_1_CoAnQi.cpp (lines 2581-2612)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "MagneticJetField"
        self.description = "Source4: Oscillating magnetic field in stellar jets"
        self.metadata = {}
        self.metadata['source'] = 'source4.cpp'
        self.metadata['type'] = 'magnetic_jet_field'
        self.metadata['equation'] = 'Bj = B_base + 0.4*sin(omega_c*t) + SCm'
        self.B_base = 0.0
        self.omega_c = 0.0
        self.SCm_contrib = 0.0
    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return B0 + 0.4 * math.sin(omega * t) + scm
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{MagneticJetFieldTerm}",
            description=self.description
        )


class UnifiedBuoyancyTerm:
    """
    Source4: Galactic buoyancy force from Ug components
    
    Source: MAIN_1_CoAnQi.cpp (lines 2618-2662)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "UnifiedBuoyancy"
        self.description = "Source4: Galactic buoyancy force from Ug components"
        self.metadata = {}
        self.metadata['source'] = 'source4.cpp'
        self.metadata['type'] = 'unified_buoyancy'
        self.metadata['equation'] = 'Ubi = -beta_i * Ugi * Omega_g * Mbh/dg * wind_mod * UUA * cos(pi*tn)'
        self.beta_i = 0.0
        self.Omega_g = 0.0
        self.Mbh = 0.0
        self.dg = 0.0
        self.epsilon_sw = 0.0
        self.rho_sw = 0.0
        self.UUA = 0.0
    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        wind_mod = 1.0 + eps * rho
        return -beta * Ugi * omega * M / d * wind_mod * ua * math.cos(math.pi * tn)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{UnifiedBuoyancyTerm}",
            description=self.description
        )


class CompressedMUGEBaseTerm:
    """
    Source4: Compressed MUGE base gravitational term
    
    Source: MAIN_1_CoAnQi.cpp (lines 2668-2693)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "CompressedMUGE_Base"
        self.description = "Source4: Compressed MUGE base gravitational term"
        self.metadata = {}
        self.metadata['source'] = 'source4.cpp'
        self.metadata['type'] = 'compressed_muge_base'
        self.metadata['equation'] = 'g_base = G * M / r^2'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        if r == 0.0: return 0.0
        return dpm_emergent_ug1(M, r)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{CompressedMUGEBaseTerm}",
            description=self.description
        )


class CompressedMUGEExpansionTerm:
    """
    Source4: Hubble expansion correction for compressed MUGE
    
    Source: MAIN_1_CoAnQi.cpp (lines 2699-2724)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "CompressedMUGE_Expansion"
        self.description = "Source4: Hubble expansion correction for compressed MUGE"
        self.metadata = {}
        self.metadata['source'] = 'source4.cpp'
        self.metadata['type'] = 'compressed_muge_expansion'
        self.metadata['equation'] = 'expansion = 1 + H0 * t'
        self.H0 = 0.0
    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return 1.0 + H * t
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{CompressedMUGEExpansionTerm}",
            description=self.description
        )


class SuperconductiveAdjustmentTerm:
    """
    Source4: Magnetic superconductivity adjustment factor
    
    Source: MAIN_1_CoAnQi.cpp (lines 2730-2755)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "SuperconductiveAdj"
        self.description = "Source4: Magnetic superconductivity adjustment factor"
        self.metadata = {}
        self.metadata['source'] = 'source4.cpp'
        self.metadata['type'] = 'superconductive_adjustment'
        self.metadata['equation'] = 'adj = 1 - B/B_crit'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        if Bcrit == 0.0: return 0.0
        return 1.0 - B / Bcrit
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{SuperconductiveAdjustmentTerm}",
            description=self.description
        )


class CosmologicalConstantTerm:
    """
    Source4: Cosmological constant dark energy term
    
    Source: MAIN_1_CoAnQi.cpp (lines 2761-2786)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "CosmologicalConstant"
        self.description = "Source4: Cosmological constant dark energy term"
        self.metadata = {}
        self.metadata['source'] = 'source4.cpp'
        self.metadata['type'] = 'cosmological_constant'
        self.metadata['equation'] = 'term = Lambda * c^2 / 3'
        self.Lambda = 0.0
    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return L * c_light * c_light / 3.0
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{CosmologicalConstantTerm}",
            description=self.description
        )


class QuantumUncertaintyTerm:
    """
    Source4: Heisenberg uncertainty principle quantum term
    
    Source: MAIN_1_CoAnQi.cpp (lines 2792-2825)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "QuantumUncertainty"
        self.description = "Source4: Heisenberg uncertainty principle quantum term"
        self.metadata = {}
        self.metadata['source'] = 'source4.cpp'
        self.metadata['type'] = 'quantum_uncertainty'
        self.metadata['equation'] = '(hbar/sqrt(dx*dp)) * psi_int * (2*pi/t_Hubble)'
        self.delta_x_p = 0.0
        self.integral_psi = 0.0
        self.t_Hubble = 0.0
    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        if dx_dp == 0.0: return 0.0
        return (hbar / sqrt_unc) * psi * (2 * math.pi / t_hub)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{QuantumUncertaintyTerm}",
            description=self.description
        )


class FluidDynamicsTerm:
    """
    Source4: Fluid dynamics contribution to gravity
    
    Source: MAIN_1_CoAnQi.cpp (lines 2831-2855)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "FluidDynamics"
        self.description = "Source4: Fluid dynamics contribution to gravity"
        self.metadata = {}
        self.metadata['source'] = 'source4.cpp'
        self.metadata['type'] = 'fluid_dynamics'
        self.metadata['equation'] = 'term = rho_fluid * V * g_local'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return rho * V * g
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{FluidDynamicsTerm}",
            description=self.description
        )


class DensityPerturbationTerm:
    """
    Source4: Visible + dark matter density perturbation
    
    Source: MAIN_1_CoAnQi.cpp (lines 2861-2890)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "DensityPerturbation"
        self.description = "Source4: Visible + dark matter density perturbation"
        self.metadata = {}
        self.metadata['source'] = 'source4.cpp'
        self.metadata['type'] = 'density_perturbation'
        self.metadata['equation'] = '(M + M_DM) * (delta_rho/rho + 3*G*M/r^3)'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        if r == 0.0: return 0.0
        pert1 = delta_rho_rho
        return (M + M_DM) * (pert1 + pert2)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{DensityPerturbationTerm}",
            description=self.description
        )


class UQFFModule5Term:
    """
    Source5 self-expanding UQFF with dark matter halo and vacuum energy
    
    Source: MAIN_1_CoAnQi.cpp (lines 2896-2933)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "UQFFModule5_Enhanced"
        self.description = "Source5 self-expanding UQFF with dark matter halo and vacuum energy"
        self.metadata = {}
        self.metadata['source'] = 'source5.cpp'
        self.metadata['type'] = 'uqff_module5_enhanced'
        self.metadata['framework'] = 'self_expanding'
        self.dm_halo_mass = 0.0
        self.dm_scale_radius = 0.0
        self.vacuum_energy_scale = 0.0
    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        # Dark matter NFW profile contribution
        x = r / dm_scale_radius
        # Vacuum energy fluctuation
        # Combined contribution
        return dm_contrib + vac_contrib
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{UQFFModule5Term}",
            description=self.description
        )


class ResonanceMUGETerm:
    """
    Source5 resonance-based MUGE with superconductive enhancement
    
    Source: MAIN_1_CoAnQi.cpp (lines 2938-2969)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "ResonanceMUGE"
        self.description = "Source5 resonance-based MUGE with superconductive enhancement"
        self.metadata = {}
        self.metadata['source'] = 'source5.cpp'
        self.metadata['type'] = 'resonance_muge'
        self.resonance_freq = 0.0
        self.system_intensity = 0.0
    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        # Resonance-based multi-layer gravity
        superconductive_factor = 1.1
        return base_gravity * resonance_mod * superconductive_factor
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{ResonanceMUGETerm}",
            description=self.description
        )


class StateExportTerm:
    """
    Source5 cross-module state export and communication
    
    Source: MAIN_1_CoAnQi.cpp (lines 2974-3012)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "StateExport"
        self.description = "Source5 cross-module state export and communication"
        self.metadata = {}
        self.metadata['source'] = 'source5.cpp'
        self.metadata['type'] = 'state_export'
        self.metadata['capability'] = 'cross_module_communication'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        computation_count++
        # State tracking computation
        last_computed_value = state_evolution
        return state_evolution
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{StateExportTerm}",
            description=self.description
        )


class TimeVaryingRotationTerm:
    """
    Source5 time-varying rotation rate with cyclic modulation
    
    Source: MAIN_1_CoAnQi.cpp (lines 3018-3046)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "TimeVaryingRotation"
        self.description = "Source5 time-varying rotation rate with cyclic modulation"
        self.metadata = {}
        self.metadata['source'] = 'source5.cpp'
        self.metadata['type'] = 'time_varying_rotation'
        self.metadata['equation'] = 'omega_s(t) = omega_s - 0.4e-6 * sin(omega_c * t)'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return omega_s_base - amplitude * math.sin(omega_c * t)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{TimeVaryingRotationTerm}",
            description=self.description
        )


class NavierStokesFluidTerm:
    """
    Source5 Navier-Stokes fluid dynamics with diffusion, advection, projection
    
    Source: MAIN_1_CoAnQi.cpp (lines 3052-3093)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "NavierStokesFluid"
        self.description = "Source5 Navier-Stokes fluid dynamics with diffusion, advection, projection"
        self.metadata = {}
        self.metadata['source'] = 'source5.cpp'
        self.metadata['type'] = 'navier_stokes_fluid'
        self.metadata['physics'] = 'incompressible_flow'
        self.metadata['methods'] = 'diffuse, advect, project'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        # Simplified fluid dynamics contribution
        # Full implementation in FluidSolver class: diffuse, advect, project steps
        diffusion_term = viscosity * dt * grid_size * grid_size
        advection_term = dt * grid_size
        # Velocity field magnitude (simplified)
        last_velocity_magnitude = velocity
        return velocity
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{NavierStokesFluidTerm}",
            description=self.description
        )


class SpacetimeMetricModulationTerm:
    """
    Source5 spacetime metric modulation: A_mu_nu = g_mu_nu + eta * Ts00 * cos(pi*tn)
    
    Source: MAIN_1_CoAnQi.cpp (lines 3099-3131)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "SpacetimeMetricModulation"
        self.description = "Source5 spacetime metric modulation: A_mu_nu = g_mu_nu + eta * Ts00 * cos(pi*tn)"
        self.metadata = {}
        self.metadata['source'] = 'source5.cpp'
        self.metadata['type'] = 'spacetime_metric_modulation'
        self.metadata['equation'] = 'A_mu_nu = g_mu_nu + eta * Ts00 * cos(pi * tn)'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        # Modulation amplitude
        # Return scalar contribution (trace of metric perturbation)
        # Full A_mu_nu is 4x4 matrix, this returns scalar modulation
        return 4.0 * modulation
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{SpacetimeMetricModulationTerm}",
            description=self.description
        )


class FullUnifiedFieldTerm:
    """
    Source5 complete unified field: FU = sum(Ugi) + sum(Ubi) + Um + A_mu_nu
    
    Source: MAIN_1_CoAnQi.cpp (lines 3138-3179)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "FullUnifiedField"
        self.description = "Source5 complete unified field: FU = sum(Ugi) + sum(Ubi) + Um + A_mu_nu"
        self.metadata = {}
        self.metadata['source'] = 'source5.cpp'
        self.metadata['type'] = 'full_unified_field'
        self.metadata['equation'] = 'FU = sum(Ugi) + sum(Ubi) + Um + A_scalar'
        self.metadata['components'] = 'Ug1, Ug2, Ug3, Ug4, Ubi1-4, Um, A_mu_nu'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        # Simplified unified field calculation
        # In full implementation: compute all Ug1-4, Ubi1-4, Um, A_mu_nu and sum
        Ug_sum = k1 + k2 + k3 + k4
        Um = 1e-20
        return Ug_sum + Ubi_sum + Um + A_scalar
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{FullUnifiedFieldTerm}",
            description=self.description
        )


class ResonanceMUGE_DPMTerm:
    """
    Source5 resonance MUGE DPM term: fundamental dipolar moment resonance
    
    Source: MAIN_1_CoAnQi.cpp (lines 3186-3219)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "ResonanceMUGE_DPM"
        self.description = "Source5 resonance MUGE DPM term: fundamental dipolar moment resonance"
        self.metadata = {}
        self.metadata['source'] = 'source5.cpp'
        self.metadata['type'] = 'resonance_muge_dpm'
        self.metadata['equation'] = 'aDPM = FDPM * fDPM * Evac_neb * c * Vsys'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return FDPM * fDPM * Evac_neb * c_res * Vsys
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{ResonanceMUGE_DPMTerm}",
            description=self.description
        )


class ResonanceMUGE_THzTerm:
    """
    Source5 resonance MUGE THz frequency term
    
    Source: MAIN_1_CoAnQi.cpp (lines 3225-3256)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "ResonanceMUGE_THz"
        self.description = "Source5 resonance MUGE THz frequency term"
        self.metadata = {}
        self.metadata['source'] = 'source5.cpp'
        self.metadata['type'] = 'resonance_muge_thz'
        self.metadata['equation'] = 'aTHz = fTHz * Evac_neb * vexp * aDPM / (Evac_ISM * c)'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return fTHz * Evac_neb * vexp * aDPM / (Evac_ISM * c_res)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{ResonanceMUGE_THzTerm}",
            description=self.description
        )


class VacuumEnergyDifferentialTerm:
    """
    Source5 vacuum energy differential between nebula and ISM
    
    Source: MAIN_1_CoAnQi.cpp (lines 3262-3291)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "VacuumEnergyDifferential"
        self.description = "Source5 vacuum energy differential between nebula and ISM"
        self.metadata = {}
        self.metadata['source'] = 'source5.cpp'
        self.metadata['type'] = 'vacuum_differential'
        self.metadata['equation'] = 'avac_diff = Delta_Evac * vexp^2 * aDPM / (Evac_neb * c^2)'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return Delta_Evac * vexp * vexp * aDPM / (Evac_neb * c_res * c_res)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{VacuumEnergyDifferentialTerm}",
            description=self.description
        )


class SuperconductiveFrequencyTerm:
    """
    Source5 superconductive frequency enhancement term
    
    Source: MAIN_1_CoAnQi.cpp (lines 3297-3327)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "SuperconductiveFrequency"
        self.description = "Source5 superconductive frequency enhancement term"
        self.metadata = {}
        self.metadata['source'] = 'source5.cpp'
        self.metadata['type'] = 'superconductive_frequency'
        self.metadata['equation'] = 'asuper_freq = Fsuper * fTHz * aDPM / (Evac_neb * c)'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return Fsuper * fTHz * aDPM / (Evac_neb * c_res)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{SuperconductiveFrequencyTerm}",
            description=self.description
        )


class AetherResonanceTerm:
    """
    Source5 Universal Aether resonance with TRZ modulation
    
    Source: MAIN_1_CoAnQi.cpp (lines 3333-3363)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "AetherResonance"
        self.description = "Source5 Universal Aether resonance with TRZ modulation"
        self.metadata = {}
        self.metadata['source'] = 'source5.cpp'
        self.metadata['type'] = 'aether_resonance'
        self.metadata['equation'] = 'aaether_res = UA_SCM * omega_i * fTHz * aDPM * (1 + fTRZ)'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return UA_SCM * omega_i * fTHz * aDPM * (1 + fTRZ)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{AetherResonanceTerm}",
            description=self.description
        )


class QuantumFrequencyTerm:
    """
    Source5 quantum frequency coupling term
    
    Source: MAIN_1_CoAnQi.cpp (lines 3369-3399)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "QuantumFrequency"
        self.description = "Source5 quantum frequency coupling term"
        self.metadata = {}
        self.metadata['source'] = 'source5.cpp'
        self.metadata['type'] = 'quantum_frequency'
        self.metadata['equation'] = 'aquantum_freq = fquantum * Evac_neb * aDPM / (Evac_ISM * c)'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return fquantum * Evac_neb * aDPM / (Evac_ISM * c_res)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{QuantumFrequencyTerm}",
            description=self.description
        )


class AetherFrequencyTerm:
    """
    Source5 Aether frequency term
    
    Source: MAIN_1_CoAnQi.cpp (lines 3405-3435)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "AetherFrequency"
        self.description = "Source5 Aether frequency term"
        self.metadata = {}
        self.metadata['source'] = 'source5.cpp'
        self.metadata['type'] = 'aether_frequency'
        self.metadata['equation'] = 'aAether_freq = fAether * Evac_neb * aDPM / (Evac_ISM * c)'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return fAether * Evac_neb * aDPM / (Evac_ISM * c_res)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{AetherFrequencyTerm}",
            description=self.description
        )


class WormholeContributionTerm:
    """
    Source5 wormhole spacetime contribution term
    
    Source: MAIN_1_CoAnQi.cpp (lines 3441-3469)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "WormholeContribution"
        self.description = "Source5 wormhole spacetime contribution term"
        self.metadata = {}
        self.metadata['source'] = 'source5.cpp'
        self.metadata['type'] = 'wormhole_contribution'
        self.metadata['equation'] = 'a_wormhole = f_worm * Evac_neb / (b^2 + r^2)'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return f_worm * Evac_neb / (b * b + r * r)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{WormholeContributionTerm}",
            description=self.description
        )


class UnifiedFieldUg1Term:
    """
    Source6 unified field Ug1: magnetic dipole and mass gradient with decay
    
    Source: MAIN_1_CoAnQi.cpp (lines 3475-3522)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "UnifiedField_Ug1"
        self.description = "Source6 unified field Ug1: magnetic dipole and mass gradient with decay"
        self.metadata = {}
        self.metadata['source'] = 'source6.cpp'
        self.metadata['type'] = 'unified_field_ug1'
        self.metadata['physics'] = 'magnetic_dipole_mass_gradient'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        # Magnetic dipole moment (time-varying)
        SCm_contrib = 1e3
        # Mass gradient
        # Defect modulation
        # Combined Ug1
        return k1 * mu_s * grad_Ms_r * math.exp(-alpha * t) * math.cos(math.pi * tn) * defect
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{UnifiedFieldUg1Term}",
            description=self.description
        )


class UnifiedFieldUg2Term:
    """
    Source6 unified field Ug2: UA charge and SCm interaction with stellar wind
    
    Source: MAIN_1_CoAnQi.cpp (lines 3528-3579)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "UnifiedField_Ug2"
        self.description = "Source6 unified field Ug2: UA charge and SCm interaction with stellar wind"
        self.metadata = {}
        self.metadata['source'] = 'source6.cpp'
        self.metadata['type'] = 'unified_field_ug2'
        self.metadata['physics'] = 'ua_charge_scm_interaction'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        # Reactive energy (SCm)
        v_SCm = 0.99 * c_light
        # Step function (outside bubble)
        # Wind modulation
        wind_mod = 1.0 + delta_sw * v_sw
        # Combined Ug2
        return k2 * (QA + QUA) * Ms / (r * r) * S * wind_mod * HSCm * Ereact
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{UnifiedFieldUg2Term}",
            description=self.description
        )


class UnifiedFieldUg3Term:
    """
    Source6 unified field Ug3: magnetic jet and core penetration with rotation
    
    Source: MAIN_1_CoAnQi.cpp (lines 3585-3634)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "UnifiedField_Ug3"
        self.description = "Source6 unified field Ug3: magnetic jet and core penetration with rotation"
        self.metadata = {}
        self.metadata['source'] = 'source6.cpp'
        self.metadata['type'] = 'unified_field_ug3'
        self.metadata['physics'] = 'magnetic_jet_core_penetration'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        # Reactive energy
        v_SCm = 0.99 * c_light
        # Time-varying rotation
        # Magnetic jet field
        SCm_contrib = 1e3
        # Combined Ug3
        return k3 * Bj * math.cos(omega_s_t * t * math.pi) * Pcore * Ereact
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{UnifiedFieldUg3Term}",
            description=self.description
        )


class UnifiedFieldUg4Term:
    """
    Source6 unified field Ug4: galactic vacuum concentration with feedback
    
    Source: MAIN_1_CoAnQi.cpp (lines 3640-3679)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "UnifiedField_Ug4"
        self.description = "Source6 unified field Ug4: galactic vacuum concentration with feedback"
        self.metadata = {}
        self.metadata['source'] = 'source6.cpp'
        self.metadata['type'] = 'unified_field_ug4'
        self.metadata['physics'] = 'galactic_vacuum_concentration'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        # Decay and cycle
        # Combined Ug4
        return k4 * rho_v * C_concentration * Mbh / dg * decay * cycle * (1 + f_feedback)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{UnifiedFieldUg4Term}",
            description=self.description
        )


class UnifiedFieldUmTerm:
    """
    Source6 unified field Um: magnetic strings with multi-layer contribution
    
    Source: MAIN_1_CoAnQi.cpp (lines 3685-3741)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "UnifiedField_Um"
        self.description = "Source6 unified field Um: magnetic strings with multi-layer contribution"
        self.metadata = {}
        self.metadata['source'] = 'source6.cpp'
        self.metadata['type'] = 'unified_field_um'
        self.metadata['physics'] = 'magnetic_strings_multilayer'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        rj = Rb
        # Reactive energy
        v_SCm = 0.99 * c_light
        # Magnetic moment
        SCm_contrib = 1e3
        # Decay
        phi_hat = 1.0
        # Single string contribution
        single = mu_j / rj * decay * phi_hat
        # Combined Um (all strings)
        return single * num_strings * PSCm * Ereact
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{UnifiedFieldUmTerm}",
            description=self.description
        )


class SpacetimeMetricTerm:
    """
    Source6 spacetime metric A_mu_nu perturbation with stress-energy coupling
    
    Source: MAIN_1_CoAnQi.cpp (lines 3747-3781)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "SpacetimeMetric"
        self.description = "Source6 spacetime metric A_mu_nu perturbation with stress-energy coupling"
        self.metadata = {}
        self.metadata['source'] = 'source6.cpp'
        self.metadata['type'] = 'spacetime_metric'
        self.metadata['physics'] = 'metric_tensor_perturbation'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        # Metric perturbation (trace of A_mu_nu)
        # g_mu_nu = diag(1, -1, -1, -1) + perturbation
        # Trace: sum of diagonal elements with perturbation
        return trace
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{SpacetimeMetricTerm}",
            description=self.description
        )


class CompressedMUGETerm:
    """
    Source6 compressed MUGE: multi-layer universal gravity with compression
    
    Source: MAIN_1_CoAnQi.cpp (lines 3787-3837)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "CompressedMUGE"
        self.description = "Source6 compressed MUGE: multi-layer universal gravity with compression"
        self.metadata = {}
        self.metadata['source'] = 'source6.cpp'
        self.metadata['type'] = 'compressed_muge'
        self.metadata['physics'] = 'multilayer_gravity_compression'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        # Compressed MUGE calculation
        omega_eff = omega1 + omega2
        L = I * omega_eff
        # Compression factor
        expansion = vexp * t_age
        # Fluid contribution
        fluid_mod = 1.0 + ffluid
        # Effective gravitational acceleration
        return g_compressed
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{CompressedMUGETerm}",
            description=self.description
        )


class ResonanceMUGE_VacuumDiffTerm:
    """
    Source7 resonance MUGE vacuum: nebula-ISM vacuum energy differential
    
    Source: MAIN_1_CoAnQi.cpp (lines 3842-3886)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "ResonanceMUGE_VacuumDiff"
        self.description = "Source7 resonance MUGE vacuum: nebula-ISM vacuum energy differential"
        self.metadata = {}
        self.metadata['source'] = 'source7.cpp'
        self.metadata['type'] = 'resonance_muge_vacuum_diff'
        self.metadata['physics'] = 'vacuum_energy_differential'
        self.I = 0.0
        self.omega1 = 0.0
        self.omega2 = 0.0
        self.fDPM = 0.0
        self.k4_res = 0.0
    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        # Base DPM
        omega_eff = omega1 + omega2
        L = I * omega_eff
        aDPM = k4_res * L / I
        # Vacuum differential contribution
        vac_factor = Delta_Evac / Evac_neb
        avac_diff = aDPM * vac_factor
        return avac_diff
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{ResonanceMUGE_VacuumDiffTerm}",
            description=self.description
        )


class ResonanceMUGE_SuperFreqTerm:
    """
    Source7 resonance MUGE superconductivity: frequency-based resonance
    
    Source: MAIN_1_CoAnQi.cpp (lines 3891-3930)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "ResonanceMUGE_SuperFreq"
        self.description = "Source7 resonance MUGE superconductivity: frequency-based resonance"
        self.metadata = {}
        self.metadata['source'] = 'source7.cpp'
        self.metadata['type'] = 'resonance_muge_super_freq'
        self.metadata['physics'] = 'superconductive_frequency_resonance'
        self.I = 0.0
        self.omega1 = 0.0
        self.omega2 = 0.0
        self.fDPM = 0.0
        self.k4_res = 0.0
    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        # Base DPM
        omega_eff = omega1 + omega2
        L = I * omega_eff
        aDPM = k4_res * L / I
        # Superconductive frequency coupling
        return asuper_freq
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{ResonanceMUGE_SuperFreqTerm}",
            description=self.description
        )


class ResonanceMUGE_AetherResTerm:
    """
    Source7 resonance MUGE aether: universal aether resonance coupling
    
    Source: MAIN_1_CoAnQi.cpp (lines 3935-3976)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "ResonanceMUGE_AetherRes"
        self.description = "Source7 resonance MUGE aether: universal aether resonance coupling"
        self.metadata = {}
        self.metadata['source'] = 'source7.cpp'
        self.metadata['type'] = 'resonance_muge_aether'
        self.metadata['physics'] = 'universal_aether_resonance'
        self.I = 0.0
        self.omega1 = 0.0
        self.omega2 = 0.0
        self.fDPM = 0.0
        self.k4_res = 0.0
    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        # Base DPM
        omega_eff = omega1 + omega2
        L = I * omega_eff
        aDPM = k4_res * L / I
        # Aether resonance
        aaether_res = aDPM * UA_SCM * fAether / fDPM
        return aaether_res
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{ResonanceMUGE_AetherResTerm}",
            description=self.description
        )


class ResonanceMUGE_QuantumFreqTerm:
    """
    Source7 resonance MUGE quantum: quantum frequency modulation
    
    Source: MAIN_1_CoAnQi.cpp (lines 3981-4020)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "ResonanceMUGE_QuantumFreq"
        self.description = "Source7 resonance MUGE quantum: quantum frequency modulation"
        self.metadata = {}
        self.metadata['source'] = 'source7.cpp'
        self.metadata['type'] = 'resonance_muge_quantum_freq'
        self.metadata['physics'] = 'quantum_frequency_modulation'
        self.I = 0.0
        self.omega1 = 0.0
        self.omega2 = 0.0
        self.fDPM = 0.0
        self.k4_res = 0.0
    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        # Base DPM
        omega_eff = omega1 + omega2
        L = I * omega_eff
        aDPM = k4_res * L / I
        # Quantum frequency modulation
        return aquantum_freq
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{ResonanceMUGE_QuantumFreqTerm}",
            description=self.description
        )


class YAMLConfigTerm:
    """
    Source7 YAML configuration: enhanced data loading with YAML support
    
    Source: MAIN_1_CoAnQi.cpp (lines 4026-4057)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "YAMLConfig"
        self.description = "Source7 YAML configuration: enhanced data loading with YAML support"
        self.metadata = {}
        self.metadata['source'] = 'source7.cpp'
        self.metadata['type'] = 'yaml_configuration'
        self.metadata['capability'] = 'enhanced_data_loading'
        self.scaling_factor = 0.0
    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        # Demonstrates YAML-loaded parameter support
        # Time-varying configuration influence
        return config_influence
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{YAMLConfigTerm}",
            description=self.description
        )


class UQFFCoreBuoyancyTerm:
    """
    Source10 UQFF core: buoyancy force with LENR, activation, DE, resonance
    
    Source: MAIN_1_CoAnQi.cpp (lines 4063-4107)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "UQFFCoreBuoyancy"
        self.description = "Source10 UQFF core: buoyancy force with LENR, activation, DE, resonance"
        self.metadata = {}
        self.metadata['source'] = 'source10.cpp'
        self.metadata['type'] = 'uqff_core_buoyancy'
        self.metadata['physics'] = 'lenr_activation_de_resonance'
        self.integrand = 0.0
        self.x_2 = 0.0
        self.k_LENR = 0.0
        self.activation_energy = 0.0
        self.k_DE = 0.0
        self.resonance_coupling = 0.0
        self.rel_factor = 0.0
    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        # Buoyancy components
        term1 = integrand * x_2
        term3 = k_DE + resonance_coupling * neutron_factor
        F_U_Bi_i = term1 + term2 + term3 + term4
        return F_U_Bi_i
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{UQFFCoreBuoyancyTerm}",
            description=self.description
        )


class VacuumRepulsionTerm:
    """
    Source10 vacuum repulsion: surface tension spike/drop analogy
    
    Source: MAIN_1_CoAnQi.cpp (lines 4113-4144)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "VacuumRepulsion"
        self.description = "Source10 vacuum repulsion: surface tension spike/drop analogy"
        self.metadata = {}
        self.metadata['source'] = 'source10.cpp'
        self.metadata['type'] = 'vacuum_repulsion'
        self.metadata['physics'] = 'surface_tension_analogy'
        self.k_vac = 0.0
        self.delta_rho_vac = 0.0
        self.M_vac = 0.0
        self.v_vac = 0.0
    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        # Vacuum repulsion force
        F_vac_rep = k_vac * delta_rho_vac * M_vac * v_vac
        return F_vac_rep
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{VacuumRepulsionTerm}",
            description=self.description
        )


class THzShockCommunicationTerm:
    """
    Source10 THz shock: 26-layer tail star formation with terahertz communication
    
    Source: MAIN_1_CoAnQi.cpp (lines 4150-4184)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "THzShockCommunication"
        self.description = "Source10 THz shock: 26-layer tail star formation with terahertz communication"
        self.metadata = {}
        self.metadata['source'] = 'source10.cpp'
        self.metadata['type'] = 'thz_shock_communication'
        self.metadata['physics'] = 'tail_star_formation_26_layers'
        self.k_thz = 0.0
        self.omega_thz = 0.0
        self.omega_0 = 0.0
        self.conduit_scale = 0.0
    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        # THz shock force
        freq_ratio = omega_thz / omega_0
        F_thz_shock = k_thz * freq_ratio * freq_ratio * neutron_factor * conduit_scale
        return F_thz_shock
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{THzShockCommunicationTerm}",
            description=self.description
        )


class ConduitFormationTerm:
    """
    Source10 conduit: H + H2O abundance leading to COx formation
    
    Source: MAIN_1_CoAnQi.cpp (lines 4190-4221)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "ConduitFormation"
        self.description = "Source10 conduit: H + H2O abundance leading to COx formation"
        self.metadata = {}
        self.metadata['source'] = 'source10.cpp'
        self.metadata['type'] = 'conduit_formation'
        self.metadata['physics'] = 'h_water_cox_abundance'
        self.k_conduit = 0.0
        self.H_abundance = 0.0
        self.water_state = 0.0
    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        # Conduit formation force
        return F_conduit
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{ConduitFormationTerm}",
            description=self.description
        )


class SpookyActionTerm:
    """
    Source10 spooky action: quantum string/wave entanglement at distance
    
    Source: MAIN_1_CoAnQi.cpp (lines 4227-4256)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "SpookyAction"
        self.description = "Source10 spooky action: quantum string/wave entanglement at distance"
        self.metadata = {}
        self.metadata['source'] = 'source10.cpp'
        self.metadata['type'] = 'spooky_action'
        self.metadata['physics'] = 'quantum_string_wave_entanglement'
        self.k_spooky = 0.0
        self.string_wave = 0.0
        self.omega_0 = 0.0
    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        # Spooky action force
        return F_spooky
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{SpookyActionTerm}",
            description=self.description
        )


class DPMResonanceEnergyTerm:
    """
    Source10 DPM resonance: energy density from hydrogen g-factor and Bohr magneton
    
    Source: MAIN_1_CoAnQi.cpp (lines 4262-4298)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "DPMResonanceEnergy"
        self.description = "Source10 DPM resonance: energy density from hydrogen g-factor and Bohr magneton"
        self.metadata = {}
        self.metadata['source'] = 'source10.cpp'
        self.metadata['type'] = 'dpm_resonance_energy'
        self.metadata['physics'] = 'hydrogen_gfactor_bohr_magneton'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        # DPM resonance energy density
        muB_B0 = mu_B * B0
        g_muB_B0 = g_H * muB_B0
        h_omega0 = h_planck * omega_0
        base = g_muB_B0 / h_omega0
        E_DPM = base * 2.82e-56
        return E_DPM
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{DPMResonanceEnergyTerm}",
            description=self.description
        )


class Triadic26LayerTerm:
    """
    Source10 triadic: compressed UQFF with 26 gravitational layers (Ug1-4)
    
    Source: MAIN_1_CoAnQi.cpp (lines 4304-4351)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "Triadic26Layer"
        self.description = "Source10 triadic: compressed UQFF with 26 gravitational layers (Ug1-4)"
        self.metadata = {}
        self.metadata['source'] = 'source10.cpp'
        self.metadata['type'] = 'triadic_26_layer'
        self.metadata['physics'] = 'compressed_uqff_gravitational_layers'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        # Sum all 26 layers
        sum_Ug = 0.0
        # TODO: Convert C++ for loop
        sum_Ug += Ug1_vec[i] + Ug2_vec[i] + Ug3_vec[i] + Ug4_vec[i]
        # Add cosmological and quantum terms
        Lambda = 1.1e-52
        result = sum_Ug + Lambda_term
        return result
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{Triadic26LayerTerm}",
            description=self.description
        )


class MagnetarCoreTerm:
    """
    Source13 magnetar: base gravity + H(z) + B corrections + BH (M_BH=
    
    Source: MAIN_1_CoAnQi.cpp (lines 4361-4413)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "MagnetarCore_SGR1745"
        self.description = "Source13 magnetar: base gravity + H(z) + B corrections + BH (M_BH="
        self.metadata = {}

        self.M_BH = 0.0
        self.r_BH = 0.0
    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        # Base gravitational term
        # H(z) correction
        corr_H = 1 + Hz * t
        # B correction
        corr_B = f_sc
        # Term 1: Base + corrections
        term1 = ug1_base * corr_H * corr_B
        # BH term
        # UQFF Ug (simplified - includes vacuum contributions)
        Ug2 = 0.0
        Ug3 = 0.0
        Ug4 = ug1_base * f_sc
        term2 = ug1_base + Ug2 + Ug3 + Ug4
        return term1 + term_BH + term2
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{MagnetarCoreTerm}",
            description=self.description
        )


class MagnetarLambdaTerm:
    """
    Source13 magnetar: cosmological constant Lambda/3 term
    
    Source: MAIN_1_CoAnQi.cpp (lines 4419-4435)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "MagnetarLambda"
        self.description = "Source13 magnetar: cosmological constant Lambda/3 term"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return (Lambda * c_light * c_light) / 3.0
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{MagnetarLambdaTerm}",
            description=self.description
        )


class MagnetarEMTerm:
    """
    Source13 magnetar: electromagnetic v×B coupling (Lorentz force)
    
    Source: MAIN_1_CoAnQi.cpp (lines 4441-4463)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "MagnetarEM"
        self.description = "Source13 magnetar: electromagnetic v×B coupling (Lorentz force)"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        cross_vB = v_surf * B0
        return em_base * scale_EM
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{MagnetarEMTerm}",
            description=self.description
        )


class MagnetarGWTerm:
    """
    Source13 magnetar: gravitational wave emission (spindown dΩ/dt)^2
    
    Source: MAIN_1_CoAnQi.cpp (lines 4469-4501)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "MagnetarGW"
        self.description = "Source13 magnetar: gravitational wave emission (spindown dΩ/dt)^2"
        self.metadata = {}

        self.P_init = 0.0
        self.tau_Omega = 0.0
    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        # dOmega/dt
        omega0 = 2 * math.pi / P_init
        # GW prefactor
        return gw_prefactor * (dOdt * dOdt)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{MagnetarGWTerm}",
            description=self.description
        )


class MagnetarQuantumTerm:
    """
    Source13 magnetar: quantum uncertainty (ℏ/√(Δx·Δp)) contribution
    
    Source: MAIN_1_CoAnQi.cpp (lines 4507-4527)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "MagnetarQuantum"
        self.description = "Source13 magnetar: quantum uncertainty (ℏ/√(Δx·Δp)) contribution"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return (hbar / sqrt_unc) * integral_psi * (2 * math.pi / t_Hubble)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{MagnetarQuantumTerm}",
            description=self.description
        )


class MagnetarFluidTerm:
    """
    Source13 magnetar: magnetospheric fluid dynamics (ρ_fluid × V)
    
    Source: MAIN_1_CoAnQi.cpp (lines 4533-4557)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "MagnetarFluid"
        self.description = "Source13 magnetar: magnetospheric fluid dynamics (ρ_fluid × V)"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        # Base gravitational term
        # Volume
        return (rho_fluid * V * ug1_base) / M
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{MagnetarFluidTerm}",
            description=self.description
        )


class MagnetarOscillatoryTerm:
    """
    Source13 magnetar: oscillatory perturbations (standing + traveling waves)
    
    Source: MAIN_1_CoAnQi.cpp (lines 4563-4594)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "MagnetarOscillatory"
        self.description = "Source13 magnetar: oscillatory perturbations (standing + traveling waves)"
        self.metadata = {}

        self.A_osc = 0.0
        self.k_osc = 0.0
        self.omega_osc = 0.0
        self.x_pos = 0.0
    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        # Standing wave component
        # Traveling wave component
        arg = k_osc * x_pos - omega_osc * t
        return term_osc1 + term_osc2
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{MagnetarOscillatoryTerm}",
            description=self.description
        )


class MagnetarDarkMatterTerm:
    """
    Source13 magnetar: dark matter halo + density perturbations (M_DM=
    
    Source: MAIN_1_CoAnQi.cpp (lines 4600-4632)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "MagnetarDarkMatter"
        self.description = "Source13 magnetar: dark matter halo + density perturbations (M_DM="
        self.metadata = {}

        self.M_DM_factor = 0.0
        self.delta_rho_over_rho = 0.0
    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        # Dark matter mass
        M_dm = M * M_DM_factor
        # Density perturbations
        pert1 = delta_rho_over_rho
        return term_dm_force_like / M
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{MagnetarDarkMatterTerm}",
            description=self.description
        )


class MagnetarMagneticEnergyTerm:
    """
    Source13 magnetar: magnetic field energy (B²/2μ₀ × V)
    
    Source: MAIN_1_CoAnQi.cpp (lines 4638-4662)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "MagnetarMagneticEnergy"
        self.description = "Source13 magnetar: magnetic field energy (B²/2μ₀ × V)"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        # Volume
        # Magnetic energy
        return M_mag / (M * r)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{MagnetarMagneticEnergyTerm}",
            description=self.description
        )


class MagnetarDecayTerm:
    """
    Source13 magnetar: cumulative decay energy (L₀ × τ_decay × [1-e^(-t/τ)])
    
    Source: MAIN_1_CoAnQi.cpp (lines 4668-4695)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "MagnetarDecay"
        self.description = "Source13 magnetar: cumulative decay energy (L₀ × τ_decay × [1-e^(-t/τ)])"
        self.metadata = {}

        self.L0_W = 0.0
        self.tau_decay = 0.0
    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        # Cumulative decay energy
        return cum_D / (M * r)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{MagnetarDecayTerm}",
            description=self.description
        )


class Magnetar0501CoreTerm:
    """
    Source14 SGR 0501+4516: base gravity + H(z) + B corrections + f_TRZ time-reversal factor
    
    Source: MAIN_1_CoAnQi.cpp (lines 4705-4749)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "Magnetar0501Core_SGR0501"
        self.description = "Source14 SGR 0501+4516: base gravity + H(z) + B corrections + f_TRZ time-reversal factor"
        self.metadata = {}

        self.f_TRZ_factor = 0.0
    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        # Base gravitational term
        # B(t) decay
        # Term 1: Base + H(z) correction + B correction
        corr_H = 1 + H0 * t
        corr_B = 1 - Bt / B_crit
        term1 = ug1_base * corr_H * corr_B
        # Term 2: UQFF Ug with f_TRZ (time-reversal zone factor)
        Ug1 = ug1_base
        Ug2 = 0.0
        Ug3 = 0.0
        return term1 + term2
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{Magnetar0501CoreTerm}",
            description=self.description
        )


class Magnetar0501LambdaTerm:
    """
    Source14 SGR 0501+4516: cosmological constant Lambda/3
    
    Source: MAIN_1_CoAnQi.cpp (lines 4755-4771)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "Magnetar0501Lambda"
        self.description = "Source14 SGR 0501+4516: cosmological constant Lambda/3"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return (Lambda * c_light * c_light) / 3.0
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{Magnetar0501LambdaTerm}",
            description=self.description
        )


class Magnetar0501EMTerm:
    """
    Source14 SGR 0501+4516: scaled EM v×B with UA/SCm vacuum correction
    
    Source: MAIN_1_CoAnQi.cpp (lines 4777-4809)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "Magnetar0501EM"
        self.description = "Source14 SGR 0501+4516: scaled EM v×B with UA/SCm vacuum correction"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        # B(t) decay
        # v ? B term
        cross_vB = v_surf * Bt
        # Vacuum correction factor
        return (em_base * corr_UA) * scale_EM
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{Magnetar0501EMTerm}",
            description=self.description
        )


class Magnetar0501GWTerm:
    """
    Source14 SGR 0501+4516: gravitational wave (dΩ/dt)^2, P_init=
    
    Source: MAIN_1_CoAnQi.cpp (lines 4815-4847)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "Magnetar0501GW"
        self.description = "Source14 SGR 0501+4516: gravitational wave (dΩ/dt)^2, P_init="
        self.metadata = {}

        self.P_init = 0.0
        self.tau_Omega = 0.0
    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        # dOmega/dt
        omega0 = 2 * math.pi / P_init
        # GW prefactor
        return gw_prefactor * (dOdt * dOdt)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{Magnetar0501GWTerm}",
            description=self.description
        )


class Magnetar0501QuantumTerm:
    """
    Source14 SGR 0501+4516: quantum uncertainty (ℏ/√(Δx·Δp))
    
    Source: MAIN_1_CoAnQi.cpp (lines 4853-4873)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "Magnetar0501Quantum"
        self.description = "Source14 SGR 0501+4516: quantum uncertainty (ℏ/√(Δx·Δp))"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return (hbar / sqrt_unc) * integral_psi * (2 * math.pi / t_Hubble)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{Magnetar0501QuantumTerm}",
            description=self.description
        )


class Magnetar0501FluidTerm:
    """
    Source14 SGR 0501+4516: magnetospheric fluid (ρ_fluid × V × g / M)
    
    Source: MAIN_1_CoAnQi.cpp (lines 4879-4903)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "Magnetar0501Fluid"
        self.description = "Source14 SGR 0501+4516: magnetospheric fluid (ρ_fluid × V × g / M)"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        # Base gravitational term
        # Volume
        return (rho_fluid * V * ug1_base) / M
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{Magnetar0501FluidTerm}",
            description=self.description
        )


class Magnetar0501OscillatoryTerm:
    """
    Source14 SGR 0501+4516: oscillatory waves (standing + traveling), A=
    
    Source: MAIN_1_CoAnQi.cpp (lines 4909-4939)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "Magnetar0501Oscillatory"
        self.description = "Source14 SGR 0501+4516: oscillatory waves (standing + traveling), A="
        self.metadata = {}

        self.A_osc = 0.0
        self.k_osc = 0.0
        self.omega_osc = 0.0
        self.x_pos = 0.0
    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        # Standing wave component
        # Traveling wave component
        return term_osc1 + term_osc2
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{Magnetar0501OscillatoryTerm}",
            description=self.description
        )


class Magnetar0501DarkMatterTerm:
    """
    Source14 SGR 0501+4516: dark matter + density perturbations (M_DM=
    
    Source: MAIN_1_CoAnQi.cpp (lines 4945-4977)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "Magnetar0501DarkMatter"
        self.description = "Source14 SGR 0501+4516: dark matter + density perturbations (M_DM="
        self.metadata = {}

        self.M_DM_factor = 0.0
        self.delta_rho_over_rho = 0.0
    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        # Dark matter mass
        M_dm = M * M_DM_factor
        # Density perturbations
        pert1 = delta_rho_over_rho
        return term_dm_force_like / M
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{Magnetar0501DarkMatterTerm}",
            description=self.description
        )


class SgrAStarCoreTerm:
    """
    Source15 Sgr A*: SMBH with M(t) growth + H(z) + B + f_TRZ (M_dot_0=
    
    Source: MAIN_1_CoAnQi.cpp (lines 4987-5037)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "SgrAStar_Core"
        self.description = "Source15 Sgr A*: SMBH with M(t) growth + H(z) + B + f_TRZ (M_dot_0="
        self.metadata = {}

        self.f_TRZ_factor = 0.0
        self.M_dot_0 = 0.0
        self.tau_acc = 0.0
    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        # M(t) with mass accretion
        # B(t) decay (convert G to T)
        Bt = B_G * 1e-4
        # Term 1: Base gravity with M(t) + H(z) + B corrections
        corr_H = 1 + H0 * t
        corr_B = 1 - Bt / B_crit
        term1 = ug1_t * corr_H * corr_B
        # Term 2: UQFF Ug with f_TRZ
        Ug2 = 0.0
        Ug3 = 0.0
        Ug4 = Ug1 * corr_B
        return term1 + term2
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{SgrAStarCoreTerm}",
            description=self.description
        )


class SgrAStarLambdaTerm:
    """
    Source15 Sgr A*: cosmological constant Lambda/3
    
    Source: MAIN_1_CoAnQi.cpp (lines 5043-5059)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "SgrAStar_Lambda"
        self.description = "Source15 Sgr A*: cosmological constant Lambda/3"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return (Lambda * c_light * c_light) / 3.0
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{SgrAStarLambdaTerm}",
            description=self.description
        )


class SgrAStarEMTerm:
    """
    Source15 Sgr A*: EM v×B acceleration (no UA/SCm correction)
    
    Source: MAIN_1_CoAnQi.cpp (lines 5065-5090)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "SgrAStar_EM"
        self.description = "Source15 Sgr A*: EM v×B acceleration (no UA/SCm correction)"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        # B(t) in Tesla
        Bt = B_G * 1e-4
        # v ? B electromagnetic acceleration
        cross_vB = v_surf * Bt
        return (q_charge * cross_vB) / proton_mass
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{SgrAStarEMTerm}",
            description=self.description
        )


class SgrAStarGWTerm:
    """
    Source15 Sgr A*: GW from spin decay (spin=
    
    Source: MAIN_1_CoAnQi.cpp (lines 5096-5134)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "SgrAStar_GW"
        self.description = "Source15 Sgr A*: GW from spin decay (spin="
        self.metadata = {}

        self.spin_factor = 0.0
        self.tau_Omega = 0.0
    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        # M(t) with accretion
        # dOmega/dt
        omega0 = spin_factor * c_light / r
        # GW prefactor
        return gw_prefactor * (dOdt * dOdt)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{SgrAStarGWTerm}",
            description=self.description
        )


class SgrAStarQuantumTerm:
    """
    Source15 Sgr A*: quantum uncertainty (ℏ/√(Δx·Δp))
    
    Source: MAIN_1_CoAnQi.cpp (lines 5140-5160)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "SgrAStar_Quantum"
        self.description = "Source15 Sgr A*: quantum uncertainty (ℏ/√(Δx·Δp))"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return (hbar / sqrt_unc) * integral_psi * (2 * math.pi / t_Hubble)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{SgrAStarQuantumTerm}",
            description=self.description
        )


class SgrAStarFluidTerm:
    """
    Source15 Sgr A*: accretion disk fluid dynamics (ρ_fluid × V)
    
    Source: MAIN_1_CoAnQi.cpp (lines 5166-5196)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "SgrAStar_Fluid"
        self.description = "Source15 Sgr A*: accretion disk fluid dynamics (ρ_fluid × V)"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        # M(t) with accretion
        # Base gravity with M(t)
        # Volume
        return (rho_fluid * V * ug1_t) / Mt
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{SgrAStarFluidTerm}",
            description=self.description
        )


class SgrAStarOscillatoryTerm:
    """
    Source15 Sgr A*: oscillatory waves (A=
    
    Source: MAIN_1_CoAnQi.cpp (lines 5202-5233)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "SgrAStar_Oscillatory"
        self.description = "Source15 Sgr A*: oscillatory waves (A="
        self.metadata = {}

        self.A_osc = 0.0
        self.k_osc = 0.0
        self.omega_osc = 0.0
        self.x_pos = 0.0
    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        # Standing wave
        # Traveling wave
        arg = k_osc * x_pos - omega_osc * t
        return term_osc1 + term_osc2
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{SgrAStarOscillatoryTerm}",
            description=self.description
        )


class SgrAStarDarkMatterTerm:
    """
    Source15 Sgr A*: DM + precession (angle=
    
    Source: MAIN_1_CoAnQi.cpp (lines 5239-5281)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "SgrAStar_DarkMatter"
        self.description = "Source15 Sgr A*: DM + precession (angle="
        self.metadata = {}

        self.M_DM_factor = 0.0
        self.delta_rho_over_rho = 0.0
        self.precession_angle_deg = 0.0
    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        # M(t) with accretion
        # Dark matter mass
        M_dm = Mt * M_DM_factor
        # Precession factor
        # Density perturbations
        pert1 = delta_rho_over_rho
        return term_dm_force_like / Mt
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{SgrAStarDarkMatterTerm}",
            description=self.description
        )


class StarbirthCoreTerm:
    """
    Source16 Starbirth: Base gravity with M(t) star formation, H(z), B corrections
    
    Source: MAIN_1_CoAnQi.cpp (lines 5288-5331)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "Starbirth_Core"
        self.description = "Source16 Starbirth: Base gravity with M(t) star formation, H(z), B corrections"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        # M(t) with star formation
        # Ug1 base
        # Corrections
        corr_H = 1 + hubble * t
        corr_B = 1 - b_field / b_crit_val
        return ug1_t * corr_H * corr_B
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{StarbirthCoreTerm}",
            description=self.description
        )


class StarbirthLambdaTerm:
    """
    Source16 Starbirth: Lambda c^2 / 3
    
    Source: MAIN_1_CoAnQi.cpp (lines 5333-5349)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "Starbirth_Lambda"
        self.description = "Source16 Starbirth: Lambda c^2 / 3"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return (lambda_val * c_val * c_val) / 3.0
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{StarbirthLambdaTerm}",
            description=self.description
        )


class StarbirthUQFFTerm:
    """
    Source16 Starbirth: UQFF Ug with f_TRZ
    
    Source: MAIN_1_CoAnQi.cpp (lines 5351-5391)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "Starbirth_UQFF"
        self.description = "Source16 Starbirth: UQFF Ug with f_TRZ"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        # M(t)
        # Ug terms
        Ug2 = 0.0
        Ug3 = 0.0
        corr_B = 1 - b_field / b_crit_val
        Ug4 = Ug1 * corr_B
        return (Ug1 + Ug2 + Ug3 + Ug4) * (1 + f_trz)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{StarbirthUQFFTerm}",
            description=self.description
        )


class StarbirthEMTerm:
    """
    Source16 Starbirth: Scaled EM with UA correction
    
    Source: MAIN_1_CoAnQi.cpp (lines 5393-5423)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "Starbirth_EM"
        self.description = "Source16 Starbirth: Scaled EM with UA correction"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        cross_vB = v_gas * b_field
        return (em_base * corr_UA) * em_scale
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{StarbirthEMTerm}",
            description=self.description
        )


class StarbirthQuantumTerm:
    """
    Source16 Starbirth: Quantum uncertainty hbar/sqrt(dx*dp)
    
    Source: MAIN_1_CoAnQi.cpp (lines 5425-5449)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "Starbirth_Quantum"
        self.description = "Source16 Starbirth: Quantum uncertainty hbar/sqrt(dx*dp)"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return (h_val / sqrt_unc) * psi_int * (2 * math.pi / t_hub)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{StarbirthQuantumTerm}",
            description=self.description
        )


class StarbirthFluidTerm:
    """
    Source16 Starbirth: Fluid dynamics term
    
    Source: MAIN_1_CoAnQi.cpp (lines 5451-5486)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "Starbirth_Fluid"
        self.description = "Source16 Starbirth: Fluid dynamics term"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        # M(t)
        # Base gravity
        # Volume
        return (rho_f * V * ug1_t) / Mt
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{StarbirthFluidTerm}",
            description=self.description
        )


class StarbirthOscillatoryTerm:
    """
    Source16 Starbirth: Standing + traveling waves
    
    Source: MAIN_1_CoAnQi.cpp (lines 5488-5517)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "Starbirth_Oscillatory"
        self.description = "Source16 Starbirth: Standing + traveling waves"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        # Standing wave
        # Traveling wave
        arg = k_val * x_val - omega_val * t
        return term1 + term2
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{StarbirthOscillatoryTerm}",
            description=self.description
        )


class StarbirthDarkMatterTerm:
    """
    Source16 Starbirth: DM + density perturbations (M_DM=
    
    Source: MAIN_1_CoAnQi.cpp (lines 5519-5561)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "Starbirth_DarkMatter"
        self.description = "Source16 Starbirth: DM + density perturbations (M_DM="
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        # M(t)
        # Dark matter mass
        M_dm = Mt * m_dm_fac
        # Density perturbations
        pert1 = delta_rho
        return term_dm_force_like / Mt
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{StarbirthDarkMatterTerm}",
            description=self.description
        )


class StarbirthWindTerm:
    """
    Source16 Starbirth: Stellar wind feedback (pressure/density)
    
    Source: MAIN_1_CoAnQi.cpp (lines 5563-5583)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "Starbirth_Wind"
        self.description = "Source16 Starbirth: Stellar wind feedback (pressure/density)"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        wind_pressure = rho_w * v_w * v_w
        return wind_pressure / rho_f
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{StarbirthWindTerm}",
            description=self.description
        )


class Westerlund2CoreTerm:
    """
    Source17 Westerlund2: Base gravity with M(t) star formation, H(z), B corrections
    
    Source: MAIN_1_CoAnQi.cpp (lines 5590-5633)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "Westerlund2_Core"
        self.description = "Source17 Westerlund2: Base gravity with M(t) star formation, H(z), B corrections"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        # M(t) with star formation
        # Ug1 base
        # Corrections
        corr_H = 1 + hubble * t
        corr_B = 1 - b_field / b_crit_val
        return ug1_t * corr_H * corr_B
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{Westerlund2CoreTerm}",
            description=self.description
        )


class Westerlund2LambdaTerm:
    """
    Source17 Westerlund2: Lambda c^2 / 3
    
    Source: MAIN_1_CoAnQi.cpp (lines 5635-5651)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "Westerlund2_Lambda"
        self.description = "Source17 Westerlund2: Lambda c^2 / 3"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return (lambda_val * c_val * c_val) / 3.0
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{Westerlund2LambdaTerm}",
            description=self.description
        )


class Westerlund2UQFFTerm:
    """
    Source17 Westerlund2: UQFF Ug with f_TRZ
    
    Source: MAIN_1_CoAnQi.cpp (lines 5653-5693)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "Westerlund2_UQFF"
        self.description = "Source17 Westerlund2: UQFF Ug with f_TRZ"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        # M(t)
        # Ug terms
        Ug2 = 0.0
        Ug3 = 0.0
        corr_B = 1 - b_field / b_crit_val
        Ug4 = Ug1 * corr_B
        return (Ug1 + Ug2 + Ug3 + Ug4) * (1 + f_trz)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{Westerlund2UQFFTerm}",
            description=self.description
        )


class Westerlund2EMTerm:
    """
    Source17 Westerlund2: Scaled EM with UA correction
    
    Source: MAIN_1_CoAnQi.cpp (lines 5695-5725)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "Westerlund2_EM"
        self.description = "Source17 Westerlund2: Scaled EM with UA correction"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        cross_vB = v_gas * b_field
        return (em_base * corr_UA) * em_scale
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{Westerlund2EMTerm}",
            description=self.description
        )


class Westerlund2QuantumTerm:
    """
    Source17 Westerlund2: Quantum uncertainty hbar/sqrt(dx*dp)
    
    Source: MAIN_1_CoAnQi.cpp (lines 5727-5751)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "Westerlund2_Quantum"
        self.description = "Source17 Westerlund2: Quantum uncertainty hbar/sqrt(dx*dp)"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return (h_val / sqrt_unc) * psi_int * (2 * math.pi / t_hub)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{Westerlund2QuantumTerm}",
            description=self.description
        )


class Westerlund2FluidTerm:
    """
    Source17 Westerlund2: Fluid dynamics term
    
    Source: MAIN_1_CoAnQi.cpp (lines 5753-5788)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "Westerlund2_Fluid"
        self.description = "Source17 Westerlund2: Fluid dynamics term"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        # M(t)
        # Base gravity
        # Volume
        return (rho_f * V * ug1_t) / Mt
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{Westerlund2FluidTerm}",
            description=self.description
        )


class Westerlund2OscillatoryTerm:
    """
    Source17 Westerlund2: Standing + traveling waves
    
    Source: MAIN_1_CoAnQi.cpp (lines 5790-5819)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "Westerlund2_Oscillatory"
        self.description = "Source17 Westerlund2: Standing + traveling waves"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        # Standing wave
        # Traveling wave
        arg = k_val * x_val - omega_val * t
        return term1 + term2
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{Westerlund2OscillatoryTerm}",
            description=self.description
        )


class Westerlund2DarkMatterTerm:
    """
    Source17 Westerlund2: DM + density perturbations (M_DM=
    
    Source: MAIN_1_CoAnQi.cpp (lines 5821-5863)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "Westerlund2_DarkMatter"
        self.description = "Source17 Westerlund2: DM + density perturbations (M_DM="
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        # M(t)
        # Dark matter mass
        M_dm = Mt * m_dm_fac
        # Density perturbations
        pert1 = delta_rho
        return term_dm_force_like / Mt
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{Westerlund2DarkMatterTerm}",
            description=self.description
        )


class Westerlund2WindTerm:
    """
    Source17 Westerlund2: Stellar wind feedback (pressure/density)
    
    Source: MAIN_1_CoAnQi.cpp (lines 5865-5885)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "Westerlund2_Wind"
        self.description = "Source17 Westerlund2: Stellar wind feedback (pressure/density)"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        wind_pressure = rho_w * v_w * v_w
        return wind_pressure / rho_f
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{Westerlund2WindTerm}",
            description=self.description
        )


class PillarsCoreTerm:
    """
    Source18 Pillars: Base gravity with M(t), H(z), B, erosion E(t) corrections
    
    Source: MAIN_1_CoAnQi.cpp (lines 5892-5943)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "Pillars_Core"
        self.description = "Source18 Pillars: Base gravity with M(t), H(z), B, erosion E(t) corrections"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        # M(t) with star formation
        # E(t) erosion factor
        # Ug1 base
        # Corrections
        corr_H = 1 + hubble * t
        corr_B = 1 - b_field / b_crit_val
        corr_E = 1 - Et
        return ug1_t * corr_H * corr_B * corr_E
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{PillarsCoreTerm}",
            description=self.description
        )


class PillarsLambdaTerm:
    """
    Source18 Pillars: Lambda c^2 / 3
    
    Source: MAIN_1_CoAnQi.cpp (lines 5945-5961)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "Pillars_Lambda"
        self.description = "Source18 Pillars: Lambda c^2 / 3"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return (lambda_val * c_val * c_val) / 3.0
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{PillarsLambdaTerm}",
            description=self.description
        )


class PillarsUQFFTerm:
    """
    Source18 Pillars: UQFF Ug with f_TRZ
    
    Source: MAIN_1_CoAnQi.cpp (lines 5963-6003)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "Pillars_UQFF"
        self.description = "Source18 Pillars: UQFF Ug with f_TRZ"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        # M(t)
        # Ug terms
        Ug2 = 0.0
        Ug3 = 0.0
        corr_B = 1 - b_field / b_crit_val
        Ug4 = Ug1 * corr_B
        return (Ug1 + Ug2 + Ug3 + Ug4) * (1 + f_trz)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{PillarsUQFFTerm}",
            description=self.description
        )


class PillarsEMTerm:
    """
    Source18 Pillars: Scaled EM with UA correction
    
    Source: MAIN_1_CoAnQi.cpp (lines 6005-6035)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "Pillars_EM"
        self.description = "Source18 Pillars: Scaled EM with UA correction"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        cross_vB = v_gas * b_field
        return (em_base * corr_UA) * em_scale
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{PillarsEMTerm}",
            description=self.description
        )


class PillarsQuantumTerm:
    """
    Source18 Pillars: Quantum uncertainty hbar/sqrt(dx*dp)
    
    Source: MAIN_1_CoAnQi.cpp (lines 6037-6061)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "Pillars_Quantum"
        self.description = "Source18 Pillars: Quantum uncertainty hbar/sqrt(dx*dp)"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return (h_val / sqrt_unc) * psi_int * (2 * math.pi / t_hub)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{PillarsQuantumTerm}",
            description=self.description
        )


class PillarsFluidTerm:
    """
    Source18 Pillars: Fluid dynamics term
    
    Source: MAIN_1_CoAnQi.cpp (lines 6063-6098)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "Pillars_Fluid"
        self.description = "Source18 Pillars: Fluid dynamics term"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        # M(t)
        # Base gravity
        # Volume
        return (rho_f * V * ug1_t) / Mt
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{PillarsFluidTerm}",
            description=self.description
        )


class PillarsOscillatoryTerm:
    """
    Source18 Pillars: Standing + traveling waves
    
    Source: MAIN_1_CoAnQi.cpp (lines 6100-6129)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "Pillars_Oscillatory"
        self.description = "Source18 Pillars: Standing + traveling waves"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        # Standing wave
        # Traveling wave
        arg = k_val * x_val - omega_val * t
        return term1 + term2
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{PillarsOscillatoryTerm}",
            description=self.description
        )


class PillarsDarkMatterTerm:
    """
    Source18 Pillars: DM + density perturbations (M_DM=
    
    Source: MAIN_1_CoAnQi.cpp (lines 6131-6173)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "Pillars_DarkMatter"
        self.description = "Source18 Pillars: DM + density perturbations (M_DM="
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        # M(t)
        # Dark matter mass
        M_dm = Mt * m_dm_fac
        # Density perturbations
        pert1 = delta_rho
        return term_dm_force_like / Mt
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{PillarsDarkMatterTerm}",
            description=self.description
        )


class PillarsWindTerm:
    """
    Source18 Pillars: Stellar wind feedback (pressure/density)
    
    Source: MAIN_1_CoAnQi.cpp (lines 6175-6195)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "Pillars_Wind"
        self.description = "Source18 Pillars: Stellar wind feedback (pressure/density)"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        wind_pressure = rho_w * v_w * v_w
        return wind_pressure / rho_f
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{PillarsWindTerm}",
            description=self.description
        )


class PillarsErosionTerm:
    """
    Source18 Pillars: Photoevaporation erosion E(t) = E_0 * e^(-t/tau)
    
    Source: MAIN_1_CoAnQi.cpp (lines 6197-6221)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "Pillars_Erosion"
        self.description = "Source18 Pillars: Photoevaporation erosion E(t) = E_0 * e^(-t/tau)"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        # Erosion decay
        # Return as acceleration contribution (negative for loss)
        return -Et * 1e-10
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{PillarsErosionTerm}",
            description=self.description
        )


class EinsteinRingCoreTerm:
    """
    Source19 EinsteinRing: Base gravity with H(z), B, lensing amplification L(t)
    
    Source: MAIN_1_CoAnQi.cpp (lines 6228-6271)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "EinsteinRing_Core"
        self.description = "Source19 EinsteinRing: Base gravity with H(z), B, lensing amplification L(t)"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        # Base gravity
        # Lensing amplification
        # Corrections
        corr_H = 1 + hz_val * t
        corr_B = 1 - b_field / b_crit_val
        corr_L = 1 + L_t
        return ug1 * corr_H * corr_B * corr_L
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{EinsteinRingCoreTerm}",
            description=self.description
        )


class EinsteinRingLambdaTerm:
    """
    Source19 EinsteinRing: Lambda c^2 / 3
    
    Source: MAIN_1_CoAnQi.cpp (lines 6273-6289)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "EinsteinRing_Lambda"
        self.description = "Source19 EinsteinRing: Lambda c^2 / 3"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return (lambda_val * c_val * c_val) / 3.0
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{EinsteinRingLambdaTerm}",
            description=self.description
        )


class EinsteinRingUQFFTerm:
    """
    Source19 EinsteinRing: UQFF Ug with f_TRZ
    
    Source: MAIN_1_CoAnQi.cpp (lines 6291-6323)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "EinsteinRing_UQFF"
        self.description = "Source19 EinsteinRing: UQFF Ug with f_TRZ"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        # Ug terms
        Ug2 = 0.0
        Ug3 = 0.0
        corr_B = 1 - b_field / b_crit_val
        Ug4 = Ug1 * corr_B
        return (Ug1 + Ug2 + Ug3 + Ug4) * (1 + f_trz)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{EinsteinRingUQFFTerm}",
            description=self.description
        )


class EinsteinRingEMTerm:
    """
    Source19 EinsteinRing: Scaled EM with UA correction
    
    Source: MAIN_1_CoAnQi.cpp (lines 6325-6355)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "EinsteinRing_EM"
        self.description = "Source19 EinsteinRing: Scaled EM with UA correction"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        cross_vB = v_gas * b_field
        return (em_base * corr_UA) * em_scale
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{EinsteinRingEMTerm}",
            description=self.description
        )


class EinsteinRingQuantumTerm:
    """
    Source19 EinsteinRing: Quantum uncertainty hbar/sqrt(dx*dp)
    
    Source: MAIN_1_CoAnQi.cpp (lines 6357-6381)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "EinsteinRing_Quantum"
        self.description = "Source19 EinsteinRing: Quantum uncertainty hbar/sqrt(dx*dp)"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return (h_val / sqrt_unc) * psi_int * (2 * math.pi / t_hub)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{EinsteinRingQuantumTerm}",
            description=self.description
        )


class EinsteinRingFluidTerm:
    """
    Source19 EinsteinRing: Fluid dynamics term
    
    Source: MAIN_1_CoAnQi.cpp (lines 6383-6410)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "EinsteinRing_Fluid"
        self.description = "Source19 EinsteinRing: Fluid dynamics term"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        # Base gravity
        # Volume
        return (rho_f * V * ug1) / mass
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{EinsteinRingFluidTerm}",
            description=self.description
        )


class EinsteinRingOscillatoryTerm:
    """
    Source19 EinsteinRing: Standing + traveling waves
    
    Source: MAIN_1_CoAnQi.cpp (lines 6412-6441)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "EinsteinRing_Oscillatory"
        self.description = "Source19 EinsteinRing: Standing + traveling waves"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        # Standing wave
        # Traveling wave
        arg = k_val * x_val - omega_val * t
        return term1 + term2
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{EinsteinRingOscillatoryTerm}",
            description=self.description
        )


class EinsteinRingDarkMatterTerm:
    """
    Source19 EinsteinRing: DM + density perturbations (M_DM=
    
    Source: MAIN_1_CoAnQi.cpp (lines 6443-6477)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "EinsteinRing_DarkMatter"
        self.description = "Source19 EinsteinRing: DM + density perturbations (M_DM="
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        # Dark matter mass
        M_dm = mass * m_dm_fac
        # Density perturbations
        pert1 = delta_rho
        return term_dm_force_like / mass
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{EinsteinRingDarkMatterTerm}",
            description=self.description
        )


class EinsteinRingWindTerm:
    """
    Source19 EinsteinRing: Intergalactic wind feedback
    
    Source: MAIN_1_CoAnQi.cpp (lines 6479-6499)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "EinsteinRing_Wind"
        self.description = "Source19 EinsteinRing: Intergalactic wind feedback"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        wind_pressure = rho_w * v_w * v_w
        return wind_pressure / rho_f
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{EinsteinRingWindTerm}",
            description=self.description
        )


class NGC2525CoreTerm:
    """
    Source20 NGC2525: Base gravity with H(z), B corrections
    
    Source: MAIN_1_CoAnQi.cpp (lines 6506-6541)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "NGC2525_Core"
        self.description = "Source20 NGC2525: Base gravity with H(z), B corrections"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        # Base gravity
        # Corrections
        corr_H = 1 + hz_val * t
        corr_B = 1 - b_field / b_crit_val
        return ug1 * corr_H * corr_B
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{NGC2525CoreTerm}",
            description=self.description
        )


class NGC2525BlackHoleTerm:
    """
    Source20 NGC2525: Supermassive black hole acceleration (M_BH=
    
    Source: MAIN_1_CoAnQi.cpp (lines 6543-6565)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "NGC2525_BlackHole"
        self.description = "Source20 NGC2525: Supermassive black hole acceleration (M_BH="
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return (g_val * m_bh) / (r_bh_val * r_bh_val)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{NGC2525BlackHoleTerm}",
            description=self.description
        )


class NGC2525LambdaTerm:
    """
    Source20 NGC2525: Lambda c^2 / 3
    
    Source: MAIN_1_CoAnQi.cpp (lines 6567-6583)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "NGC2525_Lambda"
        self.description = "Source20 NGC2525: Lambda c^2 / 3"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return (lambda_val * c_val * c_val) / 3.0
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{NGC2525LambdaTerm}",
            description=self.description
        )


class NGC2525UQFFTerm:
    """
    Source20 NGC2525: UQFF Ug with f_TRZ
    
    Source: MAIN_1_CoAnQi.cpp (lines 6585-6617)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "NGC2525_UQFF"
        self.description = "Source20 NGC2525: UQFF Ug with f_TRZ"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        # Ug terms
        Ug2 = 0.0
        Ug3 = 0.0
        corr_B = 1 - b_field / b_crit_val
        Ug4 = Ug1 * corr_B
        return (Ug1 + Ug2 + Ug3 + Ug4) * (1 + f_trz)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{NGC2525UQFFTerm}",
            description=self.description
        )


class NGC2525EMTerm:
    """
    Source20 NGC2525: Scaled EM with UA correction
    
    Source: MAIN_1_CoAnQi.cpp (lines 6619-6649)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "NGC2525_EM"
        self.description = "Source20 NGC2525: Scaled EM with UA correction"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        cross_vB = v_gas * b_field
        return (em_base * corr_UA) * em_scale
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{NGC2525EMTerm}",
            description=self.description
        )


class NGC2525QuantumTerm:
    """
    Source20 NGC2525: Quantum uncertainty hbar/sqrt(dx*dp)
    
    Source: MAIN_1_CoAnQi.cpp (lines 6651-6675)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "NGC2525_Quantum"
        self.description = "Source20 NGC2525: Quantum uncertainty hbar/sqrt(dx*dp)"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return (h_val / sqrt_unc) * psi_int * (2 * math.pi / t_hub)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{NGC2525QuantumTerm}",
            description=self.description
        )


class NGC2525FluidTerm:
    """
    Source20 NGC2525: Fluid dynamics term
    
    Source: MAIN_1_CoAnQi.cpp (lines 6677-6704)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "NGC2525_Fluid"
        self.description = "Source20 NGC2525: Fluid dynamics term"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        # Base gravity
        # Volume
        return (rho_f * V * ug1) / mass
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{NGC2525FluidTerm}",
            description=self.description
        )


class NGC2525OscillatoryTerm:
    """
    Source20 NGC2525: Standing + traveling waves
    
    Source: MAIN_1_CoAnQi.cpp (lines 6706-6735)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "NGC2525_Oscillatory"
        self.description = "Source20 NGC2525: Standing + traveling waves"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        # Standing wave
        # Traveling wave
        arg = k_val * x_val - omega_val * t
        return term1 + term2
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{NGC2525OscillatoryTerm}",
            description=self.description
        )


class NGC2525DarkMatterTerm:
    """
    Source20 NGC2525: DM + density perturbations (M_DM=
    
    Source: MAIN_1_CoAnQi.cpp (lines 6737-6771)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "NGC2525_DarkMatter"
        self.description = "Source20 NGC2525: DM + density perturbations (M_DM="
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        # Dark matter mass
        M_dm = mass * m_dm_fac
        # Density perturbations
        pert1 = delta_rho
        return term_dm_force_like / mass
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{NGC2525DarkMatterTerm}",
            description=self.description
        )


class NGC2525SupernovaTerm:
    """
    Source20 NGC2525: Supernova mass loss M_SN(t) = M_SN0 * e^(-t/tau)
    
    Source: MAIN_1_CoAnQi.cpp (lines 6773-6801)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "NGC2525_Supernova"
        self.description = "Source20 NGC2525: Supernova mass loss M_SN(t) = M_SN0 * e^(-t/tau)"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        # M_SN(t) decay
        # Negative acceleration (mass loss)
        return -(g_val * M_SNt) / (radius * radius)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{NGC2525SupernovaTerm}",
            description=self.description
        )


class NGC3603CoreTerm:
    """
    Source21 NGC3603: Base gravity with M(t) star formation, H(z), B corrections
    
    Source: MAIN_1_CoAnQi.cpp (lines 6808-6851)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "NGC3603_Core"
        self.description = "Source21 NGC3603: Base gravity with M(t) star formation, H(z), B corrections"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        # M(t) with star formation
        # Ug1 base
        # Corrections
        corr_H = 1 + hubble * t
        corr_B = 1 - b_field / b_crit_val
        return ug1_t * corr_H * corr_B
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{NGC3603CoreTerm}",
            description=self.description
        )


class NGC3603LambdaTerm:
    """
    Source21 NGC3603: Lambda c^2 / 3
    
    Source: MAIN_1_CoAnQi.cpp (lines 6853-6869)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "NGC3603_Lambda"
        self.description = "Source21 NGC3603: Lambda c^2 / 3"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return (lambda_val * c_val * c_val) / 3.0
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{NGC3603LambdaTerm}",
            description=self.description
        )


class NGC3603UQFFTerm:
    """
    Source21 NGC3603: UQFF Ug with f_TRZ
    
    Source: MAIN_1_CoAnQi.cpp (lines 6871-6911)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "NGC3603_UQFF"
        self.description = "Source21 NGC3603: UQFF Ug with f_TRZ"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        # M(t)
        # Ug terms
        Ug2 = 0.0
        Ug3 = 0.0
        corr_B = 1 - b_field / b_crit_val
        Ug4 = Ug1 * corr_B
        return (Ug1 + Ug2 + Ug3 + Ug4) * (1 + f_trz)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{NGC3603UQFFTerm}",
            description=self.description
        )


class NGC3603EMTerm:
    """
    Source21 NGC3603: Scaled EM with UA correction
    
    Source: MAIN_1_CoAnQi.cpp (lines 6913-6943)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "NGC3603_EM"
        self.description = "Source21 NGC3603: Scaled EM with UA correction"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        cross_vB = v_gas * b_field
        return (em_base * corr_UA) * em_scale
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{NGC3603EMTerm}",
            description=self.description
        )


class NGC3603QuantumTerm:
    """
    Source21 NGC3603: Quantum uncertainty hbar/sqrt(dx*dp)
    
    Source: MAIN_1_CoAnQi.cpp (lines 6945-6969)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "NGC3603_Quantum"
        self.description = "Source21 NGC3603: Quantum uncertainty hbar/sqrt(dx*dp)"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return (h_val / sqrt_unc) * psi_int * (2 * math.pi / t_hub)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{NGC3603QuantumTerm}",
            description=self.description
        )


class NGC3603FluidTerm:
    """
    Source21 NGC3603: Fluid dynamics term
    
    Source: MAIN_1_CoAnQi.cpp (lines 6971-7006)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "NGC3603_Fluid"
        self.description = "Source21 NGC3603: Fluid dynamics term"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        # M(t)
        # Base gravity
        # Volume
        return (rho_f * V * ug1_t) / Mt
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{NGC3603FluidTerm}",
            description=self.description
        )


class NGC3603OscillatoryTerm:
    """
    Source21 NGC3603: Standing + traveling waves
    
    Source: MAIN_1_CoAnQi.cpp (lines 7008-7037)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "NGC3603_Oscillatory"
        self.description = "Source21 NGC3603: Standing + traveling waves"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        # Standing wave
        # Traveling wave
        arg = k_val * x_val - omega_val * t
        return term1 + term2
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{NGC3603OscillatoryTerm}",
            description=self.description
        )


class NGC3603DarkMatterTerm:
    """
    Source21 NGC3603: DM + density perturbations (M_DM=
    
    Source: MAIN_1_CoAnQi.cpp (lines 7039-7081)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "NGC3603_DarkMatter"
        self.description = "Source21 NGC3603: DM + density perturbations (M_DM="
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        # M(t)
        # Dark matter mass
        M_dm = Mt * m_dm_fac
        # Density perturbations
        pert1 = delta_rho
        return term_dm_force_like / Mt
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{NGC3603DarkMatterTerm}",
            description=self.description
        )


class NGC3603WindTerm:
    """
    Source21 NGC3603: Stellar wind feedback (pressure/density)
    
    Source: MAIN_1_CoAnQi.cpp (lines 7083-7103)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "NGC3603_Wind"
        self.description = "Source21 NGC3603: Stellar wind feedback (pressure/density)"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        wind_pressure = rho_w * v_w * v_w
        return wind_pressure / rho_f
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{NGC3603WindTerm}",
            description=self.description
        )


class NGC3603CavityPressureTerm:
    """
    Source21 NGC3603: Cavity pressure P(t) = P0 * e^(-t/tau)
    
    Source: MAIN_1_CoAnQi.cpp (lines 7105-7131)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "NGC3603_CavityPressure"
        self.description = "Source21 NGC3603: Cavity pressure P(t) = P0 * e^(-t/tau)"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        # P(t) decay
        # Acceleration from cavity pressure
        return Pt / rho_f
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{NGC3603CavityPressureTerm}",
            description=self.description
        )


class BubbleNebulaCoreTerm:
    """
    Source22 BubbleNebula: Core gravity with H(z), B, and E(t) expansion corrections
    
    Source: MAIN_1_CoAnQi.cpp (lines 7137-7164)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "BubbleNebula_Core"
        self.description = "Source22 BubbleNebula: Core gravity with H(z), B, and E(t) expansion corrections"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        corr_H = 1 + H0 * t
        corr_B = 1 - B / B_crit
        corr_E = 1 - Et
        return g_base * corr_H * corr_B * corr_E
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{BubbleNebulaCoreTerm}",
            description=self.description
        )


class BubbleNebulaLambdaTerm:
    """
    Source22 BubbleNebula: Cosmological constant Lambda * c^2 / 3
    
    Source: MAIN_1_CoAnQi.cpp (lines 7166-7185)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "BubbleNebula_Lambda"
        self.description = "Source22 BubbleNebula: Cosmological constant Lambda * c^2 / 3"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return (Lambda * c_light * c_light) / 3.0
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{BubbleNebulaLambdaTerm}",
            description=self.description
        )


class BubbleNebulaUQFFTerm:
    """
    Source22 BubbleNebula: UQFF Ug with f_TRZ and E(t) correction
    
    Source: MAIN_1_CoAnQi.cpp (lines 7187-7213)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "BubbleNebula_UQFF"
        self.description = "Source22 BubbleNebula: UQFF Ug with f_TRZ and E(t) correction"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        corr_B = 1 - B / B_crit
        Ug4 = Ug1 * corr_B
        return (Ug1 + Ug4) * (1 + f_TRZ) * (1 - Et)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{BubbleNebulaUQFFTerm}",
            description=self.description
        )


class BubbleNebulaEMTerm:
    """
    Source22 BubbleNebula: Scaled EM with UA vacuum correction
    
    Source: MAIN_1_CoAnQi.cpp (lines 7215-7241)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "BubbleNebula_EM"
        self.description = "Source22 BubbleNebula: Scaled EM with UA vacuum correction"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        cross_vB = gas_v * B
        return (em_base * corr_UA) * scale_EM
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{BubbleNebulaEMTerm}",
            description=self.description
        )


class BubbleNebulaQuantumTerm:
    """
    Source22 BubbleNebula: Quantum uncertainty hbar / sqrt(dx*dp)
    
    Source: MAIN_1_CoAnQi.cpp (lines 7243-7265)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "BubbleNebula_Quantum"
        self.description = "Source22 BubbleNebula: Quantum uncertainty hbar / sqrt(dx*dp)"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return (hbar / sqrt_unc) * integral_psi * (2 * math.pi / t_Hubble)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{BubbleNebulaQuantumTerm}",
            description=self.description
        )


class BubbleNebulaFluidTerm:
    """
    Source22 BubbleNebula: Fluid dynamics (rho * V * g) / M
    
    Source: MAIN_1_CoAnQi.cpp (lines 7267-7289)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "BubbleNebula_Fluid"
        self.description = "Source22 BubbleNebula: Fluid dynamics (rho * V * g) / M"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return (rho_fluid * V * g_base) / M
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{BubbleNebulaFluidTerm}",
            description=self.description
        )


class BubbleNebulaOscillatoryTerm:
    """
    Source22 BubbleNebula: Standing + traveling wave perturbations
    
    Source: MAIN_1_CoAnQi.cpp (lines 7291-7314)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "BubbleNebula_Oscillatory"
        self.description = "Source22 BubbleNebula: Standing + traveling wave perturbations"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        arg = k_osc * x_pos - omega_osc * t
        return term1 + term2
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{BubbleNebulaOscillatoryTerm}",
            description=self.description
        )


class BubbleNebulaDarkMatterTerm:
    """
    Source22 BubbleNebula: DM (85%) + density perturbations
    
    Source: MAIN_1_CoAnQi.cpp (lines 7316-7341)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "BubbleNebula_DarkMatter"
        self.description = "Source22 BubbleNebula: DM (85%) + density perturbations"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        M_dm = M * M_DM_factor
        pert1 = delta_rho_over_rho
        return term_dm_force / M
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{BubbleNebulaDarkMatterTerm}",
            description=self.description
        )


class BubbleNebulaWindTerm:
    """
    Source22 BubbleNebula: Stellar wind feedback (rho * v^2) / rho_fluid
    
    Source: MAIN_1_CoAnQi.cpp (lines 7343-7363)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "BubbleNebula_Wind"
        self.description = "Source22 BubbleNebula: Stellar wind feedback (rho * v^2) / rho_fluid"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        wind_pressure = rho_wind * v_wind * v_wind
        return wind_pressure / rho_fluid
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{BubbleNebulaWindTerm}",
            description=self.description
        )


class BubbleNebulaExpansionTerm:
    """
    Source22 BubbleNebula: Expansion correction E(t) = E_0 * (1 - e^(-t/tau))
    
    Source: MAIN_1_CoAnQi.cpp (lines 7365-7387)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "BubbleNebula_Expansion"
        self.description = "Source22 BubbleNebula: Expansion correction E(t) = E_0 * (1 - e^(-t/tau))"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return -g_base * Et
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{BubbleNebulaExpansionTerm}",
            description=self.description
        )


class AntennaeGalaxiesCoreTerm:
    """
    Source23 AntennaeGalaxies: Core gravity with M(t), H(z), B, and I(t) merger interaction
    
    Source: MAIN_1_CoAnQi.cpp (lines 7393-7423)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "AntennaeGalaxies_Core"
        self.description = "Source23 AntennaeGalaxies: Core gravity with M(t), H(z), B, and I(t) merger interaction"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        corr_H = 1 + Hz * t
        corr_B = 1 - B / B_crit
        corr_I = 1 + It
        return g_base * corr_H * corr_B * corr_I
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{AntennaeGalaxiesCoreTerm}",
            description=self.description
        )


class AntennaeGalaxiesLambdaTerm:
    """
    Source23 AntennaeGalaxies: Cosmological constant Lambda * c^2 / 3
    
    Source: MAIN_1_CoAnQi.cpp (lines 7425-7444)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "AntennaeGalaxies_Lambda"
        self.description = "Source23 AntennaeGalaxies: Cosmological constant Lambda * c^2 / 3"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return (Lambda * c_light * c_light) / 3.0
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{AntennaeGalaxiesLambdaTerm}",
            description=self.description
        )


class AntennaeGalaxiesUQFFTerm:
    """
    Source23 AntennaeGalaxies: UQFF Ug with f_TRZ and I(t) merger interaction
    
    Source: MAIN_1_CoAnQi.cpp (lines 7446-7475)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "AntennaeGalaxies_UQFF"
        self.description = "Source23 AntennaeGalaxies: UQFF Ug with f_TRZ and I(t) merger interaction"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        corr_B = 1 - B / B_crit
        Ug4 = Ug1 * corr_B
        return (Ug1 + Ug4) * (1 + f_TRZ) * (1 + It)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{AntennaeGalaxiesUQFFTerm}",
            description=self.description
        )


class AntennaeGalaxiesEMTerm:
    """
    Source23 AntennaeGalaxies: Scaled EM with UA vacuum correction
    
    Source: MAIN_1_CoAnQi.cpp (lines 7477-7503)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "AntennaeGalaxies_EM"
        self.description = "Source23 AntennaeGalaxies: Scaled EM with UA vacuum correction"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        cross_vB = gas_v * B
        return (em_base * corr_UA) * scale_EM
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{AntennaeGalaxiesEMTerm}",
            description=self.description
        )


class AntennaeGalaxiesQuantumTerm:
    """
    Source23 AntennaeGalaxies: Quantum uncertainty hbar / sqrt(dx*dp)
    
    Source: MAIN_1_CoAnQi.cpp (lines 7505-7527)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "AntennaeGalaxies_Quantum"
        self.description = "Source23 AntennaeGalaxies: Quantum uncertainty hbar / sqrt(dx*dp)"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return (hbar / sqrt_unc) * integral_psi * (2 * math.pi / t_Hubble)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{AntennaeGalaxiesQuantumTerm}",
            description=self.description
        )


class AntennaeGalaxiesFluidTerm:
    """
    Source23 AntennaeGalaxies: Fluid dynamics (rho * V * g) / M(t)
    
    Source: MAIN_1_CoAnQi.cpp (lines 7529-7555)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "AntennaeGalaxies_Fluid"
        self.description = "Source23 AntennaeGalaxies: Fluid dynamics (rho * V * g) / M(t)"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return (rho_fluid * V * g_base) / Mt
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{AntennaeGalaxiesFluidTerm}",
            description=self.description
        )


class AntennaeGalaxiesOscillatoryTerm:
    """
    Source23 AntennaeGalaxies: Standing + traveling wave perturbations
    
    Source: MAIN_1_CoAnQi.cpp (lines 7557-7580)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "AntennaeGalaxies_Oscillatory"
        self.description = "Source23 AntennaeGalaxies: Standing + traveling wave perturbations"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        arg = k_osc * x_pos - omega_osc * t
        return term1 + term2
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{AntennaeGalaxiesOscillatoryTerm}",
            description=self.description
        )


class AntennaeGalaxiesDarkMatterTerm:
    """
    Source23 AntennaeGalaxies: DM (85%) + density perturbations
    
    Source: MAIN_1_CoAnQi.cpp (lines 7582-7610)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "AntennaeGalaxies_DarkMatter"
        self.description = "Source23 AntennaeGalaxies: DM (85%) + density perturbations"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        M_dm = Mt * M_DM_factor
        pert1 = delta_rho_over_rho
        return term_dm_force / Mt
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{AntennaeGalaxiesDarkMatterTerm}",
            description=self.description
        )


class AntennaeGalaxiesWindTerm:
    """
    Source23 AntennaeGalaxies: Stellar wind feedback (rho * v^2) / rho_fluid
    
    Source: MAIN_1_CoAnQi.cpp (lines 7612-7632)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "AntennaeGalaxies_Wind"
        self.description = "Source23 AntennaeGalaxies: Stellar wind feedback (rho * v^2) / rho_fluid"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        wind_pressure = rho_wind * v_wind * v_wind
        return wind_pressure / rho_fluid
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{AntennaeGalaxiesWindTerm}",
            description=self.description
        )


class AntennaeGalaxiesMergerInteractionTerm:
    """
    Source23 AntennaeGalaxies: Merger interaction I(t) = I0 * e^(-t/tau_merger)
    
    Source: MAIN_1_CoAnQi.cpp (lines 7634-7660)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "AntennaeGalaxies_MergerInteraction"
        self.description = "Source23 AntennaeGalaxies: Merger interaction I(t) = I0 * e^(-t/tau_merger)"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return g_base * It
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{AntennaeGalaxiesMergerInteractionTerm}",
            description=self.description
        )


class HorseheadNebulaCoreTerm:
    """
    Source24 HorseheadNebula: Core gravity with H(z), B, and E(t) erosion corrections
    
    Source: MAIN_1_CoAnQi.cpp (lines 7666-7693)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "HorseheadNebula_Core"
        self.description = "Source24 HorseheadNebula: Core gravity with H(z), B, and E(t) erosion corrections"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        corr_H = 1 + H0 * t
        corr_B = 1 - B / B_crit
        corr_E = 1 - Et
        return g_base * corr_H * corr_B * corr_E
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{HorseheadNebulaCoreTerm}",
            description=self.description
        )


class HorseheadNebulaLambdaTerm:
    """
    Source24 HorseheadNebula: Cosmological constant Lambda * c^2 / 3
    
    Source: MAIN_1_CoAnQi.cpp (lines 7695-7714)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "HorseheadNebula_Lambda"
        self.description = "Source24 HorseheadNebula: Cosmological constant Lambda * c^2 / 3"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return (Lambda * c_light * c_light) / 3.0
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{HorseheadNebulaLambdaTerm}",
            description=self.description
        )


class HorseheadNebulaUQFFTerm:
    """
    Source24 HorseheadNebula: UQFF Ug with f_TRZ and E(t) erosion correction
    
    Source: MAIN_1_CoAnQi.cpp (lines 7716-7742)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "HorseheadNebula_UQFF"
        self.description = "Source24 HorseheadNebula: UQFF Ug with f_TRZ and E(t) erosion correction"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        corr_B = 1 - B / B_crit
        Ug4 = Ug1 * corr_B
        return (Ug1 + Ug4) * (1 + f_TRZ) * (1 - Et)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{HorseheadNebulaUQFFTerm}",
            description=self.description
        )


class HorseheadNebulaEMTerm:
    """
    Source24 HorseheadNebula: Scaled EM with UA vacuum correction
    
    Source: MAIN_1_CoAnQi.cpp (lines 7744-7770)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "HorseheadNebula_EM"
        self.description = "Source24 HorseheadNebula: Scaled EM with UA vacuum correction"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        cross_vB = gas_v * B
        return (em_base * corr_UA) * scale_EM
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{HorseheadNebulaEMTerm}",
            description=self.description
        )


class HorseheadNebulaQuantumTerm:
    """
    Source24 HorseheadNebula: Quantum uncertainty hbar / sqrt(dx*dp)
    
    Source: MAIN_1_CoAnQi.cpp (lines 7772-7794)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "HorseheadNebula_Quantum"
        self.description = "Source24 HorseheadNebula: Quantum uncertainty hbar / sqrt(dx*dp)"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return (hbar / sqrt_unc) * integral_psi * (2 * math.pi / t_Hubble)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{HorseheadNebulaQuantumTerm}",
            description=self.description
        )


class HorseheadNebulaFluidTerm:
    """
    Source24 HorseheadNebula: Fluid dynamics (rho * V * g) / M
    
    Source: MAIN_1_CoAnQi.cpp (lines 7796-7818)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "HorseheadNebula_Fluid"
        self.description = "Source24 HorseheadNebula: Fluid dynamics (rho * V * g) / M"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return (rho_fluid * V * g_base) / M
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{HorseheadNebulaFluidTerm}",
            description=self.description
        )


class HorseheadNebulaOscillatoryTerm:
    """
    Source24 HorseheadNebula: Standing + traveling wave perturbations
    
    Source: MAIN_1_CoAnQi.cpp (lines 7820-7843)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "HorseheadNebula_Oscillatory"
        self.description = "Source24 HorseheadNebula: Standing + traveling wave perturbations"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        arg = k_osc * x_pos - omega_osc * t
        return term1 + term2
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{HorseheadNebulaOscillatoryTerm}",
            description=self.description
        )


class HorseheadNebulaDarkMatterTerm:
    """
    Source24 HorseheadNebula: DM (85%) + density perturbations
    
    Source: MAIN_1_CoAnQi.cpp (lines 7845-7870)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "HorseheadNebula_DarkMatter"
        self.description = "Source24 HorseheadNebula: DM (85%) + density perturbations"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        M_dm = M * M_DM_factor
        pert1 = delta_rho_over_rho
        return term_dm_force / M
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{HorseheadNebulaDarkMatterTerm}",
            description=self.description
        )


class HorseheadNebulaWindTerm:
    """
    Source24 HorseheadNebula: Stellar wind feedback (rho * v^2) / rho_fluid
    
    Source: MAIN_1_CoAnQi.cpp (lines 7872-7892)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "HorseheadNebula_Wind"
        self.description = "Source24 HorseheadNebula: Stellar wind feedback (rho * v^2) / rho_fluid"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        wind_pressure = rho_wind * v_wind * v_wind
        return wind_pressure / rho_fluid
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{HorseheadNebulaWindTerm}",
            description=self.description
        )


class HorseheadNebulaErosionTerm:
    """
    Source24 HorseheadNebula: Erosion correction E(t) = E_0 * (1 - e^(-t/tau))
    
    Source: MAIN_1_CoAnQi.cpp (lines 7894-7916)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "HorseheadNebula_Erosion"
        self.description = "Source24 HorseheadNebula: Erosion correction E(t) = E_0 * (1 - e^(-t/tau))"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return -g_base * Et
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{HorseheadNebulaErosionTerm}",
            description=self.description
        )


class NGC1275CoreTerm:
    """
    Source25 NGC1275: Core gravity with H(z), B(t), and F(t) filament support
    
    Source: MAIN_1_CoAnQi.cpp (lines 7922-7950)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "NGC1275_Core"
        self.description = "Source25 NGC1275: Core gravity with H(z), B(t), and F(t) filament support"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        corr_H = 1 + Hz * t
        corr_B = 1 - Bt / B_crit
        corr_F = 1 + Ft
        return g_base * corr_H * corr_B * corr_F
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{NGC1275CoreTerm}",
            description=self.description
        )


class NGC1275BlackHoleTerm:
    """
    Source25 NGC1275: SMBH gravity term (800M Msun)
    
    Source: MAIN_1_CoAnQi.cpp (lines 7952-7971)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "NGC1275_BlackHole"
        self.description = "Source25 NGC1275: SMBH gravity term (800M Msun)"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return dpm_emergent_ug1(M_BH, r_BH)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{NGC1275BlackHoleTerm}",
            description=self.description
        )


class NGC1275LambdaTerm:
    """
    Source25 NGC1275: Cosmological constant Lambda * c^2 / 3
    
    Source: MAIN_1_CoAnQi.cpp (lines 7973-7992)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "NGC1275_Lambda"
        self.description = "Source25 NGC1275: Cosmological constant Lambda * c^2 / 3"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return (Lambda * c_light * c_light) / 3.0
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{NGC1275LambdaTerm}",
            description=self.description
        )


class NGC1275UQFFTerm:
    """
    Source25 NGC1275: UQFF Ug with f_TRZ, B(t), and F(t) filament support
    
    Source: MAIN_1_CoAnQi.cpp (lines 7994-8021)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "NGC1275_UQFF"
        self.description = "Source25 NGC1275: UQFF Ug with f_TRZ, B(t), and F(t) filament support"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        corr_B = 1 - Bt / B_crit
        Ug4 = Ug1 * corr_B
        return (Ug1 + Ug4) * (1 + f_TRZ) * (1 + Ft)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{NGC1275UQFFTerm}",
            description=self.description
        )


class NGC1275EMTerm:
    """
    Source25 NGC1275: Scaled EM with UA vacuum correction and B(t)
    
    Source: MAIN_1_CoAnQi.cpp (lines 8023-8050)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "NGC1275_EM"
        self.description = "Source25 NGC1275: Scaled EM with UA vacuum correction and B(t)"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        cross_vB = gas_v * Bt
        return (em_base * corr_UA) * scale_EM
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{NGC1275EMTerm}",
            description=self.description
        )


class NGC1275QuantumTerm:
    """
    Source25 NGC1275: Quantum uncertainty hbar / sqrt(dx*dp)
    
    Source: MAIN_1_CoAnQi.cpp (lines 8052-8074)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "NGC1275_Quantum"
        self.description = "Source25 NGC1275: Quantum uncertainty hbar / sqrt(dx*dp)"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return (hbar / sqrt_unc) * integral_psi * (2 * math.pi / t_Hubble)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{NGC1275QuantumTerm}",
            description=self.description
        )


class NGC1275FluidTerm:
    """
    Source25 NGC1275: Fluid dynamics (rho * V * g) / M
    
    Source: MAIN_1_CoAnQi.cpp (lines 8076-8098)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "NGC1275_Fluid"
        self.description = "Source25 NGC1275: Fluid dynamics (rho * V * g) / M"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return (rho_fluid * V * g_base) / M
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{NGC1275FluidTerm}",
            description=self.description
        )


class NGC1275OscillatoryTerm:
    """
    Source25 NGC1275: Standing + traveling wave perturbations
    
    Source: MAIN_1_CoAnQi.cpp (lines 8100-8123)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "NGC1275_Oscillatory"
        self.description = "Source25 NGC1275: Standing + traveling wave perturbations"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        arg = k_osc * x_pos - omega_osc * t
        return term1 + term2
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{NGC1275OscillatoryTerm}",
            description=self.description
        )


class NGC1275DarkMatterTerm:
    """
    Source25 NGC1275: DM (85%) + density perturbations
    
    Source: MAIN_1_CoAnQi.cpp (lines 8125-8150)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "NGC1275_DarkMatter"
        self.description = "Source25 NGC1275: DM (85%) + density perturbations"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        M_dm = M * M_DM_factor
        pert1 = delta_rho_over_rho
        return term_dm_force / M
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{NGC1275DarkMatterTerm}",
            description=self.description
        )


class NGC1275CoolingFlowTerm:
    """
    Source25 NGC1275: Cooling flow (rho_cool * v_cool^2) / rho_fluid
    
    Source: MAIN_1_CoAnQi.cpp (lines 8152-8172)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "NGC1275_CoolingFlow"
        self.description = "Source25 NGC1275: Cooling flow (rho_cool * v_cool^2) / rho_fluid"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        cool_pressure = rho_cool * v_cool * v_cool
        return cool_pressure / rho_fluid
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{NGC1275CoolingFlowTerm}",
            description=self.description
        )


class NGC1275FilamentSupportTerm:
    """
    Source25 NGC1275: Filament support F(t) = F0 * e^(-t/tau_fil)
    
    Source: MAIN_1_CoAnQi.cpp (lines 8174-8196)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "NGC1275_FilamentSupport"
        self.description = "Source25 NGC1275: Filament support F(t) = F0 * e^(-t/tau_fil)"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return g_base * Ft
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{NGC1275FilamentSupportTerm}",
            description=self.description
        )


class HUDFGalaxiesCoreTerm:
    """
    Source26 HUDFGalaxies: Core gravity with M(t), H(z), B, and I(t) interactions
    
    Source: MAIN_1_CoAnQi.cpp (lines 8202-8232)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "HUDFGalaxies_Core"
        self.description = "Source26 HUDFGalaxies: Core gravity with M(t), H(z), B, and I(t) interactions"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        corr_H = 1 + Hz * t
        corr_B = 1 - B / B_crit
        corr_I = 1 + It
        return g_base * corr_H * corr_B * corr_I
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{HUDFGalaxiesCoreTerm}",
            description=self.description
        )


class HUDFGalaxiesLambdaTerm:
    """
    Source26 HUDFGalaxies: Cosmological constant Lambda * c^2 / 3
    
    Source: MAIN_1_CoAnQi.cpp (lines 8234-8253)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "HUDFGalaxies_Lambda"
        self.description = "Source26 HUDFGalaxies: Cosmological constant Lambda * c^2 / 3"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return (Lambda * c_light * c_light) / 3.0
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{HUDFGalaxiesLambdaTerm}",
            description=self.description
        )


class HUDFGalaxiesUQFFTerm:
    """
    Source26 HUDFGalaxies: UQFF Ug with f_TRZ and I(t) interactions
    
    Source: MAIN_1_CoAnQi.cpp (lines 8255-8284)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "HUDFGalaxies_UQFF"
        self.description = "Source26 HUDFGalaxies: UQFF Ug with f_TRZ and I(t) interactions"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        corr_B = 1 - B / B_crit
        Ug4 = Ug1 * corr_B
        return (Ug1 + Ug4) * (1 + f_TRZ) * (1 + It)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{HUDFGalaxiesUQFFTerm}",
            description=self.description
        )


class HUDFGalaxiesEMTerm:
    """
    Source26 HUDFGalaxies: Scaled EM with UA vacuum correction
    
    Source: MAIN_1_CoAnQi.cpp (lines 8286-8312)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "HUDFGalaxies_EM"
        self.description = "Source26 HUDFGalaxies: Scaled EM with UA vacuum correction"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        cross_vB = gas_v * B
        return (em_base * corr_UA) * scale_EM
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{HUDFGalaxiesEMTerm}",
            description=self.description
        )


class HUDFGalaxiesQuantumTerm:
    """
    Source26 HUDFGalaxies: Quantum uncertainty hbar / sqrt(dx*dp)
    
    Source: MAIN_1_CoAnQi.cpp (lines 8314-8336)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "HUDFGalaxies_Quantum"
        self.description = "Source26 HUDFGalaxies: Quantum uncertainty hbar / sqrt(dx*dp)"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return (hbar / sqrt_unc) * integral_psi * (2 * math.pi / t_Hubble)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{HUDFGalaxiesQuantumTerm}",
            description=self.description
        )


class HUDFGalaxiesFluidTerm:
    """
    Source26 HUDFGalaxies: Fluid dynamics (rho * V * g) / M(t)
    
    Source: MAIN_1_CoAnQi.cpp (lines 8338-8364)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "HUDFGalaxies_Fluid"
        self.description = "Source26 HUDFGalaxies: Fluid dynamics (rho * V * g) / M(t)"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return (rho_fluid * V * g_base) / Mt
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{HUDFGalaxiesFluidTerm}",
            description=self.description
        )


class HUDFGalaxiesOscillatoryTerm:
    """
    Source26 HUDFGalaxies: Standing + traveling wave perturbations
    
    Source: MAIN_1_CoAnQi.cpp (lines 8366-8389)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "HUDFGalaxies_Oscillatory"
        self.description = "Source26 HUDFGalaxies: Standing + traveling wave perturbations"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        arg = k_osc * x_pos - omega_osc * t
        return term1 + term2
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{HUDFGalaxiesOscillatoryTerm}",
            description=self.description
        )


class HUDFGalaxiesDarkMatterTerm:
    """
    Source26 HUDFGalaxies: DM (85%) + density perturbations
    
    Source: MAIN_1_CoAnQi.cpp (lines 8391-8419)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "HUDFGalaxies_DarkMatter"
        self.description = "Source26 HUDFGalaxies: DM (85%) + density perturbations"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        M_dm = Mt * M_DM_factor
        pert1 = delta_rho_over_rho
        return term_dm_force / Mt
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{HUDFGalaxiesDarkMatterTerm}",
            description=self.description
        )


class HUDFGalaxiesWindTerm:
    """
    Source26 HUDFGalaxies: Stellar wind feedback (rho * v^2) / rho_fluid
    
    Source: MAIN_1_CoAnQi.cpp (lines 8421-8441)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "HUDFGalaxies_Wind"
        self.description = "Source26 HUDFGalaxies: Stellar wind feedback (rho * v^2) / rho_fluid"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        wind_pressure = rho_wind * v_wind * v_wind
        return wind_pressure / rho_fluid
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{HUDFGalaxiesWindTerm}",
            description=self.description
        )


class HUDFGalaxiesInteractionTerm:
    """
    Source26 HUDFGalaxies: Galaxy interaction I(t) = I0 * e^(-t/tau_inter)
    
    Source: MAIN_1_CoAnQi.cpp (lines 8443-8469)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "HUDFGalaxies_Interaction"
        self.description = "Source26 HUDFGalaxies: Galaxy interaction I(t) = I0 * e^(-t/tau_inter)"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return g_base * It
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{HUDFGalaxiesInteractionTerm}",
            description=self.description
        )


class NGC1792CoreTerm:
    """
    Source27 NGC1792: Core gravity with M(t) starburst, H(z), and B
    
    Source: MAIN_1_CoAnQi.cpp (lines 8475-8502)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "NGC1792_Core"
        self.description = "Source27 NGC1792: Core gravity with M(t) starburst, H(z), and B"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        corr_H = 1 + Hz * t
        corr_B = 1 - B / B_crit
        return g_base * corr_H * corr_B
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{NGC1792CoreTerm}",
            description=self.description
        )


class NGC1792LambdaTerm:
    """
    Source27 NGC1792: Cosmological constant Lambda * c^2 / 3
    
    Source: MAIN_1_CoAnQi.cpp (lines 8504-8523)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "NGC1792_Lambda"
        self.description = "Source27 NGC1792: Cosmological constant Lambda * c^2 / 3"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return (Lambda * c_light * c_light) / 3.0
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{NGC1792LambdaTerm}",
            description=self.description
        )


class NGC1792UQFFTerm:
    """
    Source27 NGC1792: UQFF Ug with f_TRZ and M(t) starburst
    
    Source: MAIN_1_CoAnQi.cpp (lines 8525-8552)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "NGC1792_UQFF"
        self.description = "Source27 NGC1792: UQFF Ug with f_TRZ and M(t) starburst"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        corr_B = 1 - B / B_crit
        Ug4 = Ug1 * corr_B
        return (Ug1 + Ug4) * (1 + f_TRZ)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{NGC1792UQFFTerm}",
            description=self.description
        )


class NGC1792EMTerm:
    """
    Source27 NGC1792: Scaled EM with UA vacuum correction
    
    Source: MAIN_1_CoAnQi.cpp (lines 8554-8580)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "NGC1792_EM"
        self.description = "Source27 NGC1792: Scaled EM with UA vacuum correction"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        cross_vB = gas_v * B
        return (em_base * corr_UA) * scale_EM
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{NGC1792EMTerm}",
            description=self.description
        )


class NGC1792QuantumTerm:
    """
    Source27 NGC1792: Quantum uncertainty hbar / sqrt(dx*dp)
    
    Source: MAIN_1_CoAnQi.cpp (lines 8582-8604)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "NGC1792_Quantum"
        self.description = "Source27 NGC1792: Quantum uncertainty hbar / sqrt(dx*dp)"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return (hbar / sqrt_unc) * integral_psi * (2 * math.pi / t_Hubble)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{NGC1792QuantumTerm}",
            description=self.description
        )


class NGC1792FluidTerm:
    """
    Source27 NGC1792: Fluid dynamics (rho * V * g) / M(t)
    
    Source: MAIN_1_CoAnQi.cpp (lines 8606-8632)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "NGC1792_Fluid"
        self.description = "Source27 NGC1792: Fluid dynamics (rho * V * g) / M(t)"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return (rho_fluid * V * g_base) / Mt
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{NGC1792FluidTerm}",
            description=self.description
        )


class NGC1792OscillatoryTerm:
    """
    Source27 NGC1792: Standing + traveling wave perturbations
    
    Source: MAIN_1_CoAnQi.cpp (lines 8634-8657)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "NGC1792_Oscillatory"
        self.description = "Source27 NGC1792: Standing + traveling wave perturbations"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        arg = k_osc * x_pos - omega_osc * t
        return term1 + term2
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{NGC1792OscillatoryTerm}",
            description=self.description
        )


class NGC1792DarkMatterTerm:
    """
    Source27 NGC1792: DM (85%) + density perturbations
    
    Source: MAIN_1_CoAnQi.cpp (lines 8659-8687)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "NGC1792_DarkMatter"
        self.description = "Source27 NGC1792: DM (85%) + density perturbations"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        M_dm = Mt * M_DM_factor
        pert1 = delta_rho_over_rho
        return term_dm_force / Mt
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{NGC1792DarkMatterTerm}",
            description=self.description
        )


class NGC1792SupernovaFeedbackTerm:
    """
    Source27 NGC1792: Supernova feedback (rho * v^2) / rho_fluid
    
    Source: MAIN_1_CoAnQi.cpp (lines 8689-8709)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "NGC1792_SupernovaFeedback"
        self.description = "Source27 NGC1792: Supernova feedback (rho * v^2) / rho_fluid"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        wind_pressure = rho_wind * v_wind * v_wind
        return wind_pressure / rho_fluid
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{NGC1792SupernovaFeedbackTerm}",
            description=self.description
        )


class AndromedaCoreTerm:
    """
    Source28 Andromeda: Core gravity with H(z) blueshift and B
    
    Source: MAIN_1_CoAnQi.cpp (lines 8715-8738)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "Andromeda_Core"
        self.description = "Source28 Andromeda: Core gravity with H(z) blueshift and B"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        corr_H = 1 + Hz * t
        corr_B = 1 - B / B_crit
        return g_base * corr_H * corr_B
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{AndromedaCoreTerm}",
            description=self.description
        )


class AndromedaBlackHoleTerm:
    """
    Source28 Andromeda: SMBH gravity term (140M Msun)
    
    Source: MAIN_1_CoAnQi.cpp (lines 8740-8759)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "Andromeda_BlackHole"
        self.description = "Source28 Andromeda: SMBH gravity term (140M Msun)"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return dpm_emergent_ug1(M_BH, r_BH)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{AndromedaBlackHoleTerm}",
            description=self.description
        )


class AndromedaLambdaTerm:
    """
    Source28 Andromeda: Cosmological constant Lambda * c^2 / 3
    
    Source: MAIN_1_CoAnQi.cpp (lines 8761-8780)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "Andromeda_Lambda"
        self.description = "Source28 Andromeda: Cosmological constant Lambda * c^2 / 3"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return (Lambda * c_light * c_light) / 3.0
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{AndromedaLambdaTerm}",
            description=self.description
        )


class AndromedaUQFFTerm:
    """
    Source28 Andromeda: UQFF Ug with f_TRZ
    
    Source: MAIN_1_CoAnQi.cpp (lines 8782-8805)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "Andromeda_UQFF"
        self.description = "Source28 Andromeda: UQFF Ug with f_TRZ"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        corr_B = 1 - B / B_crit
        Ug4 = Ug1 * corr_B
        return (Ug1 + Ug4) * (1 + f_TRZ)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{AndromedaUQFFTerm}",
            description=self.description
        )


class AndromedaEMTerm:
    """
    Source28 Andromeda: Scaled EM with UA vacuum correction
    
    Source: MAIN_1_CoAnQi.cpp (lines 8807-8833)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "Andromeda_EM"
        self.description = "Source28 Andromeda: Scaled EM with UA vacuum correction"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        cross_vB = v_orbit * B
        return (em_base * corr_UA) * scale_EM
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{AndromedaEMTerm}",
            description=self.description
        )


class AndromedaQuantumTerm:
    """
    Source28 Andromeda: Quantum uncertainty hbar / sqrt(dx*dp)
    
    Source: MAIN_1_CoAnQi.cpp (lines 8835-8857)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "Andromeda_Quantum"
        self.description = "Source28 Andromeda: Quantum uncertainty hbar / sqrt(dx*dp)"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return (hbar / sqrt_unc) * integral_psi * (2 * math.pi / t_Hubble)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{AndromedaQuantumTerm}",
            description=self.description
        )


class AndromedaFluidTerm:
    """
    Source28 Andromeda: Fluid dynamics (rho * V * g) / M
    
    Source: MAIN_1_CoAnQi.cpp (lines 8859-8881)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "Andromeda_Fluid"
        self.description = "Source28 Andromeda: Fluid dynamics (rho * V * g) / M"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return (rho_fluid * V * g_base) / M
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{AndromedaFluidTerm}",
            description=self.description
        )


class AndromedaOscillatoryTerm:
    """
    Source28 Andromeda: Standing + traveling wave perturbations
    
    Source: MAIN_1_CoAnQi.cpp (lines 8883-8906)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "Andromeda_Oscillatory"
        self.description = "Source28 Andromeda: Standing + traveling wave perturbations"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        arg = k_osc * x_pos - omega_osc * t
        return term1 + term2
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{AndromedaOscillatoryTerm}",
            description=self.description
        )


class AndromedaDarkMatterTerm:
    """
    Source28 Andromeda: DM (80%) + density perturbations
    
    Source: MAIN_1_CoAnQi.cpp (lines 8908-8933)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "Andromeda_DarkMatter"
        self.description = "Source28 Andromeda: DM (80%) + density perturbations"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        M_dm = M * M_DM_factor
        pert1 = delta_rho_over_rho
        return term_dm_force / M
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{AndromedaDarkMatterTerm}",
            description=self.description
        )


class SombreroCoreTerm:
    """
    Source29 Sombrero: Core gravity with H(z=0.0063) and B superconductivity
    
    Source: MAIN_1_CoAnQi.cpp (lines 8939-8962)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "Sombrero_Core"
        self.description = "Source29 Sombrero: Core gravity with H(z=0.0063) and B superconductivity"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        corr_H = 1 + Hz * t
        corr_B = 1 - B / B_crit
        return g_base * corr_H * corr_B
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{SombreroCoreTerm}",
            description=self.description
        )


class SombreroBlackHoleTerm:
    """
    Source29 Sombrero: SMBH gravity term (1 billion Msun)
    
    Source: MAIN_1_CoAnQi.cpp (lines 8964-8983)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "Sombrero_BlackHole"
        self.description = "Source29 Sombrero: SMBH gravity term (1 billion Msun)"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return dpm_emergent_ug1(M_BH, r_BH)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{SombreroBlackHoleTerm}",
            description=self.description
        )


class SombreroLambdaTerm:
    """
    Source29 Sombrero: Cosmological constant Lambda * c^2 / 3
    
    Source: MAIN_1_CoAnQi.cpp (lines 8985-9004)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "Sombrero_Lambda"
        self.description = "Source29 Sombrero: Cosmological constant Lambda * c^2 / 3"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return (Lambda * c_light * c_light) / 3.0
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{SombreroLambdaTerm}",
            description=self.description
        )


class SombreroUQFFTerm:
    """
    Source29 Sombrero: UQFF Ug with f_TRZ and superconductivity
    
    Source: MAIN_1_CoAnQi.cpp (lines 9006-9029)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "Sombrero_UQFF"
        self.description = "Source29 Sombrero: UQFF Ug with f_TRZ and superconductivity"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        corr_B = 1 - B / B_crit
        Ug4 = Ug1 * corr_B
        return (Ug1 + Ug4) * (1 + f_TRZ)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{SombreroUQFFTerm}",
            description=self.description
        )


class SombreroEMTerm:
    """
    Source29 Sombrero: Scaled EM with UA vacuum correction
    
    Source: MAIN_1_CoAnQi.cpp (lines 9031-9057)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "Sombrero_EM"
        self.description = "Source29 Sombrero: Scaled EM with UA vacuum correction"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        cross_vB = v_orbit * B
        return (em_base * corr_UA) * scale_EM
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{SombreroEMTerm}",
            description=self.description
        )


class SombreroQuantumTerm:
    """
    Source29 Sombrero: Quantum uncertainty hbar / sqrt(dx*dp)
    
    Source: MAIN_1_CoAnQi.cpp (lines 9059-9081)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "Sombrero_Quantum"
        self.description = "Source29 Sombrero: Quantum uncertainty hbar / sqrt(dx*dp)"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return (hbar / sqrt_unc) * integral_psi * (2 * math.pi / t_Hubble)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{SombreroQuantumTerm}",
            description=self.description
        )


class SombreroFluidTerm:
    """
    Source29 Sombrero: Fluid dynamics (rho * V * g) / M
    
    Source: MAIN_1_CoAnQi.cpp (lines 9083-9105)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "Sombrero_Fluid"
        self.description = "Source29 Sombrero: Fluid dynamics (rho * V * g) / M"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return (rho_fluid * V * g_base) / M
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{SombreroFluidTerm}",
            description=self.description
        )


class SombreroOscillatoryTerm:
    """
    Source29 Sombrero: Standing + traveling wave perturbations
    
    Source: MAIN_1_CoAnQi.cpp (lines 9107-9130)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "Sombrero_Oscillatory"
        self.description = "Source29 Sombrero: Standing + traveling wave perturbations"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        arg = k_osc * x_pos - omega_osc * t
        return term1 + term2
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{SombreroOscillatoryTerm}",
            description=self.description
        )


class SombreroDarkMatterTerm:
    """
    Source29 Sombrero: DM (20%) + density perturbations
    
    Source: MAIN_1_CoAnQi.cpp (lines 9132-9157)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "Sombrero_DarkMatter"
        self.description = "Source29 Sombrero: DM (20%) + density perturbations"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        M_dm = M * M_DM_factor
        pert1 = delta_rho_over_rho
        return term_dm_force / M
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{SombreroDarkMatterTerm}",
            description=self.description
        )


class SombreroDustTerm:
    """
    Source29 Sombrero: Dust drag D_dust (prominent dust lane)
    
    Source: MAIN_1_CoAnQi.cpp (lines 9159-9177)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "Sombrero_Dust"
        self.description = "Source29 Sombrero: Dust drag D_dust (prominent dust lane)"
        self.metadata = {}

        self.D_dust = 0.0
    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return D_dust
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{SombreroDustTerm}",
            description=self.description
        )


class SaturnCoreTerm:
    """
    Source30 Saturn: Core gravity (Saturn + Sun) with B superconductivity
    
    Source: MAIN_1_CoAnQi.cpp (lines 9183-9208)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "Saturn_Core"
        self.description = "Source30 Saturn: Core gravity (Saturn + Sun) with B superconductivity"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        corr_B = 1 - B / B_crit
        return (g_saturn + g_sun) * corr_B
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{SaturnCoreTerm}",
            description=self.description
        )


class SaturnLambdaTerm:
    """
    Source30 Saturn: Cosmological constant Lambda * c^2 / 3
    
    Source: MAIN_1_CoAnQi.cpp (lines 9210-9229)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "Saturn_Lambda"
        self.description = "Source30 Saturn: Cosmological constant Lambda * c^2 / 3"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return (Lambda * c_light * c_light) / 3.0
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{SaturnLambdaTerm}",
            description=self.description
        )


class SaturnUQFFTerm:
    """
    Source30 Saturn: UQFF Ug with f_TRZ
    
    Source: MAIN_1_CoAnQi.cpp (lines 9231-9254)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "Saturn_UQFF"
        self.description = "Source30 Saturn: UQFF Ug with f_TRZ"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        corr_B = 1 - B / B_crit
        Ug4 = Ug1 * corr_B
        return (Ug1 + Ug4) * (1 + f_TRZ)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{SaturnUQFFTerm}",
            description=self.description
        )


class SaturnEMTerm:
    """
    Source30 Saturn: Scaled EM with UA vacuum correction
    
    Source: MAIN_1_CoAnQi.cpp (lines 9256-9282)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "Saturn_EM"
        self.description = "Source30 Saturn: Scaled EM with UA vacuum correction"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        cross_vB = v_orbit * B
        return (em_base * corr_UA) * scale_EM
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{SaturnEMTerm}",
            description=self.description
        )


class SaturnQuantumTerm:
    """
    Source30 Saturn: Quantum uncertainty hbar / sqrt(dx*dp)
    
    Source: MAIN_1_CoAnQi.cpp (lines 9284-9306)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "Saturn_Quantum"
        self.description = "Source30 Saturn: Quantum uncertainty hbar / sqrt(dx*dp)"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return (hbar / sqrt_unc) * integral_psi * (2 * math.pi / t_Hubble)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{SaturnQuantumTerm}",
            description=self.description
        )


class SaturnFluidTerm:
    """
    Source30 Saturn: Fluid dynamics (rho * V * g) / M
    
    Source: MAIN_1_CoAnQi.cpp (lines 9308-9330)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "Saturn_Fluid"
        self.description = "Source30 Saturn: Fluid dynamics (rho * V * g) / M"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return (rho_fluid * V * g_base) / M
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{SaturnFluidTerm}",
            description=self.description
        )


class SaturnOscillatoryTerm:
    """
    Source30 Saturn: Standing + traveling wave perturbations
    
    Source: MAIN_1_CoAnQi.cpp (lines 9332-9355)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "Saturn_Oscillatory"
        self.description = "Source30 Saturn: Standing + traveling wave perturbations"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        arg = k_osc * x_pos - omega_osc * t
        return term1 + term2
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{SaturnOscillatoryTerm}",
            description=self.description
        )


class SaturnDarkMatterTerm:
    """
    Source30 Saturn: Visible mass with density perturbations (M_DM=0)
    
    Source: MAIN_1_CoAnQi.cpp (lines 9357-9382)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "Saturn_DarkMatter"
        self.description = "Source30 Saturn: Visible mass with density perturbations (M_DM=0)"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        M_dm = M * M_DM_factor
        pert1 = delta_rho_over_rho
        return term_dm_force / M
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{SaturnDarkMatterTerm}",
            description=self.description
        )


class SaturnRingTidalTerm:
    """
    Source30 Saturn: Ring tidal force G * M_ring / r_ring^2
    
    Source: MAIN_1_CoAnQi.cpp (lines 9384-9403)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "Saturn_RingTidal"
        self.description = "Source30 Saturn: Ring tidal force G * M_ring / r_ring^2"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return dpm_emergent_ug1(M_ring, r_ring)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{SaturnRingTidalTerm}",
            description=self.description
        )


class SaturnWindTerm:
    """
    Source30 Saturn: Atmospheric wind v_wind^2 * 1e-12
    
    Source: MAIN_1_CoAnQi.cpp (lines 9405-9424)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "Saturn_Wind"
        self.description = "Source30 Saturn: Atmospheric wind v_wind^2 * 1e-12"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return v_wind * v_wind * wind_scale
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{SaturnWindTerm}",
            description=self.description
        )


class M16CoreTerm:
    """
    Source31 M16: Core gravity with M(t) star formation and erosion
    
    Source: MAIN_1_CoAnQi.cpp (lines 9430-9456)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "M16_Core"
        self.description = "Source31 M16: Core gravity with M(t) star formation and erosion"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        corr_H = 1 + Hz * t
        corr_B = 1 - B / B_crit
        return g_base * corr_H * corr_B
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{M16CoreTerm}",
            description=self.description
        )


class M16LambdaTerm:
    """
    Source31 M16: Cosmological constant Lambda * c^2 / 3
    
    Source: MAIN_1_CoAnQi.cpp (lines 9458-9477)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "M16_Lambda"
        self.description = "Source31 M16: Cosmological constant Lambda * c^2 / 3"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return (Lambda * c_light * c_light) / 3.0
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{M16LambdaTerm}",
            description=self.description
        )


class M16UQFFTerm:
    """
    Source31 M16: UQFF Ug with f_TRZ and M(t)
    
    Source: MAIN_1_CoAnQi.cpp (lines 9479-9505)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "M16_UQFF"
        self.description = "Source31 M16: UQFF Ug with f_TRZ and M(t)"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        corr_B = 1 - B / B_crit
        Ug4 = Ug1 * corr_B
        return (Ug1 + Ug4) * (1 + f_TRZ)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{M16UQFFTerm}",
            description=self.description
        )


class M16EMTerm:
    """
    Source31 M16: Scaled EM with UA vacuum correction (v_gas=100 km/s)
    
    Source: MAIN_1_CoAnQi.cpp (lines 9507-9533)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "M16_EM"
        self.description = "Source31 M16: Scaled EM with UA vacuum correction (v_gas=100 km/s)"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        cross_vB = v_gas * B
        return (em_base * corr_UA) * scale_EM
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{M16EMTerm}",
            description=self.description
        )


class M16QuantumTerm:
    """
    Source31 M16: Quantum uncertainty hbar / sqrt(dx*dp)
    
    Source: MAIN_1_CoAnQi.cpp (lines 9535-9557)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "M16_Quantum"
        self.description = "Source31 M16: Quantum uncertainty hbar / sqrt(dx*dp)"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return (hbar / sqrt_unc) * integral_psi * (2 * math.pi / t_Hubble)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{M16QuantumTerm}",
            description=self.description
        )


class M16FluidTerm:
    """
    Source31 M16: Fluid dynamics with M(t)
    
    Source: MAIN_1_CoAnQi.cpp (lines 9559-9583)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "M16_Fluid"
        self.description = "Source31 M16: Fluid dynamics with M(t)"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return (rho_fluid * V * g_base) / M_t
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{M16FluidTerm}",
            description=self.description
        )


class M16OscillatoryTerm:
    """
    Source31 M16: Standing + traveling wave perturbations
    
    Source: MAIN_1_CoAnQi.cpp (lines 9585-9608)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "M16_Oscillatory"
        self.description = "Source31 M16: Standing + traveling wave perturbations"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        arg = k_osc * x_pos - omega_osc * t
        return term1 + term2
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{M16OscillatoryTerm}",
            description=self.description
        )


class M16DarkMatterTerm:
    """
    Source31 M16: Visible mass with density perturbations (M_DM=0)
    
    Source: MAIN_1_CoAnQi.cpp (lines 9610-9637)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "M16_DarkMatter"
        self.description = "Source31 M16: Visible mass with density perturbations (M_DM=0)"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        M_dm = M_t * M_DM_factor
        pert1 = delta_rho_over_rho
        return term_dm_force / M_t
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{M16DarkMatterTerm}",
            description=self.description
        )


class M16StarFormationTerm:
    """
    Source31 M16: Star formation M_sf(t) = (SFR * t) / M0
    
    Source: MAIN_1_CoAnQi.cpp (lines 9639-9660)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "M16_StarFormation"
        self.description = "Source31 M16: Star formation M_sf(t) = (SFR * t) / M0"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return M_sf
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{M16StarFormationTerm}",
            description=self.description
        )


class M16RadiationErosionTerm:
    """
    Source31 M16: Radiation erosion E_rad(t) = E0 * (1 - exp(-t/tau))
    
    Source: MAIN_1_CoAnQi.cpp (lines 9662-9681)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "M16_RadiationErosion"
        self.description = "Source31 M16: Radiation erosion E_rad(t) = E0 * (1 - exp(-t/tau))"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return E0 * (1 - math.exp(-t / tau_erode))
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{M16RadiationErosionTerm}",
            description=self.description
        )


class CrabCoreTerm:
    """
    Source32 Crab: Core gravity with r(t) expansion
    
    Source: MAIN_1_CoAnQi.cpp (lines 9687-9713)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "Crab_Core"
        self.description = "Source32 Crab: Core gravity with r(t) expansion"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        r_t = r0 + v_exp * t
        corr_H = 1 + Hz * t
        corr_B = 1 - B / B_crit
        return g_base * corr_H * corr_B
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{CrabCoreTerm}",
            description=self.description
        )


class CrabLambdaTerm:
    """
    Source32 Crab: Cosmological constant Lambda * c^2 / 3
    
    Source: MAIN_1_CoAnQi.cpp (lines 9715-9734)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "Crab_Lambda"
        self.description = "Source32 Crab: Cosmological constant Lambda * c^2 / 3"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return (Lambda * c_light * c_light) / 3.0
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{CrabLambdaTerm}",
            description=self.description
        )


class CrabUQFFTerm:
    """
    Source32 Crab: UQFF Ug with f_TRZ and r(t)
    
    Source: MAIN_1_CoAnQi.cpp (lines 9736-9762)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "Crab_UQFF"
        self.description = "Source32 Crab: UQFF Ug with f_TRZ and r(t)"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        r_t = r0 + v_exp * t
        corr_B = 1 - B / B_crit
        Ug4 = Ug1 * corr_B
        return (Ug1 + Ug4) * (1 + f_TRZ)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{CrabUQFFTerm}",
            description=self.description
        )


class CrabEMTerm:
    """
    Source32 Crab: Scaled EM with UA correction (v_shock=1500 km/s)
    
    Source: MAIN_1_CoAnQi.cpp (lines 9764-9790)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "Crab_EM"
        self.description = "Source32 Crab: Scaled EM with UA correction (v_shock=1500 km/s)"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        cross_vB = v_shock * B
        return (em_base * corr_UA) * scale_EM
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{CrabEMTerm}",
            description=self.description
        )


class CrabQuantumTerm:
    """
    Source32 Crab: Quantum uncertainty hbar / sqrt(dx*dp)
    
    Source: MAIN_1_CoAnQi.cpp (lines 9792-9814)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "Crab_Quantum"
        self.description = "Source32 Crab: Quantum uncertainty hbar / sqrt(dx*dp)"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return (hbar / sqrt_unc) * integral_psi * (2 * math.pi / t_Hubble)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{CrabQuantumTerm}",
            description=self.description
        )


class CrabFluidTerm:
    """
    Source32 Crab: Fluid dynamics with r(t)
    
    Source: MAIN_1_CoAnQi.cpp (lines 9816-9839)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "Crab_Fluid"
        self.description = "Source32 Crab: Fluid dynamics with r(t)"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        r_t = r0 + v_exp * t
        return (rho_fluid * V * g_base) / M
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{CrabFluidTerm}",
            description=self.description
        )


class CrabOscillatoryTerm:
    """
    Source32 Crab: Standing + traveling wave perturbations
    
    Source: MAIN_1_CoAnQi.cpp (lines 9841-9864)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "Crab_Oscillatory"
        self.description = "Source32 Crab: Standing + traveling wave perturbations"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        arg = k_osc * x_pos - omega_osc * t
        return term1 + term2
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{CrabOscillatoryTerm}",
            description=self.description
        )


class CrabDarkMatterTerm:
    """
    Source32 Crab: Visible mass with density perturbations (M_DM=0)
    
    Source: MAIN_1_CoAnQi.cpp (lines 9866-9893)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "Crab_DarkMatter"
        self.description = "Source32 Crab: Visible mass with density perturbations (M_DM=0)"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        r_t = r0 + v_exp * t
        M_dm = M * M_DM_factor
        pert1 = delta_rho_over_rho
        return term_dm_force / M
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{CrabDarkMatterTerm}",
            description=self.description
        )


class CrabPulsarWindTerm:
    """
    Source32 Crab: Pulsar wind a_wind = [P/(4πr²) * (1+v/c)] / ρ
    
    Source: MAIN_1_CoAnQi.cpp (lines 9895-9921)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "Crab_PulsarWind"
        self.description = "Source32 Crab: Pulsar wind a_wind = [P/(4πr²) * (1+v/c)] / ρ"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        r_t = r0 + v_exp * t
        rel_corr = 1 + v_shock / c_light
        return a_wind * scale_wind
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{CrabPulsarWindTerm}",
            description=self.description
        )


class CrabMagneticTerm:
    """
    Source32 Crab: Magnetic M_mag = (q * v_shock * B) / m_e
    
    Source: MAIN_1_CoAnQi.cpp (lines 9923-9944)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "Crab_Magnetic"
        self.description = "Source32 Crab: Magnetic M_mag = (q * v_shock * B) / m_e"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return (q * v_shock * B / m_e) * scale_mag
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{CrabMagneticTerm}",
            description=self.description
        )


class SGR1745CoreTerm:
    """
    Source33 SGR1745: Core gravity with extreme B superconductivity
    
    Source: MAIN_1_CoAnQi.cpp (lines 9950-9973)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "SGR1745_Core"
        self.description = "Source33 SGR1745: Core gravity with extreme B superconductivity"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        corr_H = 1 + Hz * t
        corr_B = 1 - B / B_crit
        return g_base * corr_H * corr_B
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{SGR1745CoreTerm}",
            description=self.description
        )


class SGR1745LambdaTerm:
    """
    Source33 SGR1745: Cosmological constant Lambda * c^2 / 3
    
    Source: MAIN_1_CoAnQi.cpp (lines 9975-9994)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "SGR1745_Lambda"
        self.description = "Source33 SGR1745: Cosmological constant Lambda * c^2 / 3"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return (Lambda * c_light * c_light) / 3.0
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{SGR1745LambdaTerm}",
            description=self.description
        )


class SGR1745UQFFTerm:
    """
    Source33 SGR1745: UQFF Ug with f_TRZ and extreme B
    
    Source: MAIN_1_CoAnQi.cpp (lines 9996-10019)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "SGR1745_UQFF"
        self.description = "Source33 SGR1745: UQFF Ug with f_TRZ and extreme B"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        corr_B = 1 - B / B_crit
        Ug4 = Ug1 * corr_B
        return (Ug1 + Ug4) * (1 + f_TRZ)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{SGR1745UQFFTerm}",
            description=self.description
        )


class SGR1745EMTerm:
    """
    Source33 SGR1745: Scaled EM with extreme B=2e10 T
    
    Source: MAIN_1_CoAnQi.cpp (lines 10021-10047)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "SGR1745_EM"
        self.description = "Source33 SGR1745: Scaled EM with extreme B=2e10 T"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        cross_vB = v_spin * B
        return (em_base * corr_UA) * scale_EM
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{SGR1745EMTerm}",
            description=self.description
        )


class SGR1745QuantumTerm:
    """
    Source33 SGR1745: Quantum uncertainty hbar / sqrt(dx*dp)
    
    Source: MAIN_1_CoAnQi.cpp (lines 10049-10071)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "SGR1745_Quantum"
        self.description = "Source33 SGR1745: Quantum uncertainty hbar / sqrt(dx*dp)"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return (hbar / sqrt_unc) * integral_psi * (2 * math.pi / t_Hubble)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{SGR1745QuantumTerm}",
            description=self.description
        )


class SGR1745FluidTerm:
    """
    Source33 SGR1745: Crust dynamics rho_crust = 1e17 kg/m^3
    
    Source: MAIN_1_CoAnQi.cpp (lines 10073-10095)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "SGR1745_Fluid"
        self.description = "Source33 SGR1745: Crust dynamics rho_crust = 1e17 kg/m^3"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return (rho_crust * V * g_base) / M
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{SGR1745FluidTerm}",
            description=self.description
        )


class SGR1745OscillatoryTerm:
    """
    Source33 SGR1745: Pulsations omega=1.67 rad/s (P=3.76s)
    
    Source: MAIN_1_CoAnQi.cpp (lines 10097-10120)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "SGR1745_Oscillatory"
        self.description = "Source33 SGR1745: Pulsations omega=1.67 rad/s (P=3.76s)"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        arg = k_osc * x_pos - omega_osc * t
        return term1 + term2
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{SGR1745OscillatoryTerm}",
            description=self.description
        )


class SGR1745DarkMatterTerm:
    """
    Source33 SGR1745: Visible mass with density perturbations (M_DM=0)
    
    Source: MAIN_1_CoAnQi.cpp (lines 10122-10147)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "SGR1745_DarkMatter"
        self.description = "Source33 SGR1745: Visible mass with density perturbations (M_DM=0)"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        M_dm = M * M_DM_factor
        pert1 = delta_rho_over_rho
        return term_dm_force / M
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{SGR1745DarkMatterTerm}",
            description=self.description
        )


class SGR1745_DPMTerm:
    """
    Source34 SGR1745-UQFF: DPM resonance a_DPM = (I/A) * (ω₁-ω₂)
    
    Source: MAIN_1_CoAnQi.cpp (lines 10153-10173)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "SGR1745_DPM"
        self.description = "Source34 SGR1745-UQFF: DPM resonance a_DPM = (I/A) * (ω₁-ω₂)"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return (I / A) * (omega_1 - omega_2)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{SGR1745_DPMTerm}",
            description=self.description
        )


class SGR1745_THzTerm:
    """
    Source34 SGR1745-UQFF: THz pipeline a_THz = (f·E_vac·v·a_DPM)/(E_ISM·c)
    
    Source: MAIN_1_CoAnQi.cpp (lines 10175-10195)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "SGR1745_THz"
        self.description = "Source34 SGR1745-UQFF: THz pipeline a_THz = (f·E_vac·v·a_DPM)/(E_ISM·c)"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return (f_THz * E_vac_neb * v_exp * a_DPM) / (E_vac_ISM * c_light)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{SGR1745_THzTerm}",
            description=self.description
        )


class SGR1745_VacDiffTerm:
    """
    Source34 SGR1745-UQFF: Vacuum differential a_vac_diff
    
    Source: MAIN_1_CoAnQi.cpp (lines 10197-10217)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "SGR1745_VacDiff"
        self.description = "Source34 SGR1745-UQFF: Vacuum differential a_vac_diff"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return (E0 * f_vac_diff * V_sys) / (h * f_vac_diff) * a_DPM
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{SGR1745_VacDiffTerm}",
            description=self.description
        )


class SGR1745_SuperFreqTerm:
    """
    Source34 SGR1745-UQFF: Superconductor frequency a_super_freq
    
    Source: MAIN_1_CoAnQi.cpp (lines 10219-10239)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "SGR1745_SuperFreq"
        self.description = "Source34 SGR1745-UQFF: Superconductor frequency a_super_freq"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return (h * f_super * f_DPM * a_DPM) / (E_vac_ISM * c_light)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{SGR1745_SuperFreqTerm}",
            description=self.description
        )


class SGR1745_AetherResTerm:
    """
    Source34 SGR1745-UQFF: Aether resonance a_aether_res
    
    Source: MAIN_1_CoAnQi.cpp (lines 10241-10261)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "SGR1745_AetherRes"
        self.description = "Source34 SGR1745-UQFF: Aether resonance a_aether_res"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return f_aether * 1e-8 * f_DPM * (1 + f_TRZ) * a_DPM
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{SGR1745_AetherResTerm}",
            description=self.description
        )


class SGR1745_Ug4iTerm:
    """
    Source34 SGR1745-UQFF: Reactive U_g4i term
    
    Source: MAIN_1_CoAnQi.cpp (lines 10263-10283)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "SGR1745_Ug4i"
        self.description = "Source34 SGR1745-UQFF: Reactive U_g4i term"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return (f_sc * Ug1 * f_react * a_DPM) / (E_vac_ISM * c_light)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{SGR1745_Ug4iTerm}",
            description=self.description
        )


class SGR1745_QuantumFreqTerm:
    """
    Source34 SGR1745-UQFF: Quantum frequency a_quantum_freq
    
    Source: MAIN_1_CoAnQi.cpp (lines 10285-10305)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "SGR1745_QuantumFreq"
        self.description = "Source34 SGR1745-UQFF: Quantum frequency a_quantum_freq"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return (f_quantum * E_vac_neb * a_DPM) / (E_vac_ISM * c_light)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{SGR1745_QuantumFreqTerm}",
            description=self.description
        )


class SGR1745_AetherFreqTerm:
    """
    Source34 SGR1745-UQFF: Aether frequency a_Aether_freq
    
    Source: MAIN_1_CoAnQi.cpp (lines 10307-10327)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "SGR1745_AetherFreq"
        self.description = "Source34 SGR1745-UQFF: Aether frequency a_Aether_freq"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return (f_Aether * E_vac_neb * a_DPM) / (E_vac_ISM * c_light)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{SGR1745_AetherFreqTerm}",
            description=self.description
        )


class SGR1745_FluidFreqTerm:
    """
    Source34 SGR1745-UQFF: Fluid frequency a_fluid_freq
    
    Source: MAIN_1_CoAnQi.cpp (lines 10329-10349)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "SGR1745_FluidFreq"
        self.description = "Source34 SGR1745-UQFF: Fluid frequency a_fluid_freq"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return (f_fluid * E_vac_neb * V_sys) / (E_vac_ISM * c_light)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{SGR1745_FluidFreqTerm}",
            description=self.description
        )


class SGR1745_ExpFreqTerm:
    """
    Source34 SGR1745-UQFF: Expansion frequency a_exp_freq
    
    Source: MAIN_1_CoAnQi.cpp (lines 10351-10371)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "SGR1745_ExpFreq"
        self.description = "Source34 SGR1745-UQFF: Expansion frequency a_exp_freq"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return (f_exp * E_vac_neb * a_DPM) / (E_vac_ISM * c_light)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{SGR1745_ExpFreqTerm}",
            description=self.description
        )


class SgrA_DPMTerm:
    """
    Source35 SgrA*-UQFF: DPM resonance (scaled for SMBH)
    
    Source: MAIN_1_CoAnQi.cpp (lines 10377-10397)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "SgrA_DPM"
        self.description = "Source35 SgrA*-UQFF: DPM resonance (scaled for SMBH)"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return (I / A) * (omega_1 - omega_2)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{SgrA_DPMTerm}",
            description=self.description
        )


class SgrA_THzTerm:
    """
    Source35 SgrA*-UQFF: THz pipeline (f=1e9 Hz for SMBH)
    
    Source: MAIN_1_CoAnQi.cpp (lines 10399-10419)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "SgrA_THz"
        self.description = "Source35 SgrA*-UQFF: THz pipeline (f=1e9 Hz for SMBH)"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return (f_THz * E_vac_neb * v_exp * a_DPM) / (E_vac_ISM * c_light)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{SgrA_THzTerm}",
            description=self.description
        )


class SgrA_VacDiffTerm:
    """
    Source35 SgrA*-UQFF: Vacuum differential
    
    Source: MAIN_1_CoAnQi.cpp (lines 10421-10441)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "SgrA_VacDiff"
        self.description = "Source35 SgrA*-UQFF: Vacuum differential"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return (E0 * f_vac_diff * V_sys) / (h * f_vac_diff) * a_DPM
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{SgrA_VacDiffTerm}",
            description=self.description
        )


class SgrA_SuperFreqTerm:
    """
    Source35 SgrA*-UQFF: Superconductor frequency
    
    Source: MAIN_1_CoAnQi.cpp (lines 10443-10463)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "SgrA_SuperFreq"
        self.description = "Source35 SgrA*-UQFF: Superconductor frequency"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return (h * f_super * f_DPM * a_DPM) / (E_vac_ISM * c_light)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{SgrA_SuperFreqTerm}",
            description=self.description
        )


class SgrA_AetherResTerm:
    """
    Source35 SgrA*-UQFF: Aether resonance
    
    Source: MAIN_1_CoAnQi.cpp (lines 10465-10485)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "SgrA_AetherRes"
        self.description = "Source35 SgrA*-UQFF: Aether resonance"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return f_aether * 1e-8 * f_DPM * (1 + f_TRZ) * a_DPM
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{SgrA_AetherResTerm}",
            description=self.description
        )


class SgrA_Ug4iTerm:
    """
    Source35 SgrA*-UQFF: Reactive U_g4i
    
    Source: MAIN_1_CoAnQi.cpp (lines 10487-10507)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "SgrA_Ug4i"
        self.description = "Source35 SgrA*-UQFF: Reactive U_g4i"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return (f_sc * Ug1 * f_react * a_DPM) / (E_vac_ISM * c_light)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{SgrA_Ug4iTerm}",
            description=self.description
        )


class SgrA_QuantumFreqTerm:
    """
    Source35 SgrA*-UQFF: Quantum frequency
    
    Source: MAIN_1_CoAnQi.cpp (lines 10509-10529)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "SgrA_QuantumFreq"
        self.description = "Source35 SgrA*-UQFF: Quantum frequency"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return (f_quantum * E_vac_neb * a_DPM) / (E_vac_ISM * c_light)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{SgrA_QuantumFreqTerm}",
            description=self.description
        )


class SgrA_AetherFreqTerm:
    """
    Source35 SgrA*-UQFF: Aether frequency
    
    Source: MAIN_1_CoAnQi.cpp (lines 10531-10551)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "SgrA_AetherFreq"
        self.description = "Source35 SgrA*-UQFF: Aether frequency"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return (f_Aether * E_vac_neb * a_DPM) / (E_vac_ISM * c_light)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{SgrA_AetherFreqTerm}",
            description=self.description
        )


class SgrA_FluidFreqTerm:
    """
    Source35 SgrA*-UQFF: Fluid frequency
    
    Source: MAIN_1_CoAnQi.cpp (lines 10553-10573)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "SgrA_FluidFreq"
        self.description = "Source35 SgrA*-UQFF: Fluid frequency"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return (f_fluid * E_vac_neb * V_sys) / (E_vac_ISM * c_light)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{SgrA_FluidFreqTerm}",
            description=self.description
        )


class SgrA_ExpFreqTerm:
    """
    Source35 SgrA*-UQFF: Expansion frequency
    
    Source: MAIN_1_CoAnQi.cpp (lines 10575-10595)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "SgrA_ExpFreq"
        self.description = "Source35 SgrA*-UQFF: Expansion frequency"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return (f_exp * E_vac_neb * a_DPM) / (E_vac_ISM * c_light)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{SgrA_ExpFreqTerm}",
            description=self.description
        )


class YoungStarsCore:
    """
    Source36 YoungStars: Core gravity with star formation mass factor M_SF(t)
    
    Source: MAIN_1_CoAnQi.cpp (lines 10603-10630)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "YoungStars_Core"
        self.description = "Source36 YoungStars: Core gravity with star formation mass factor M_SF(t)"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        G = 6.67430e-11
        M_sun = 1.98847e30
        year_s = 3.15576e7
        t_yr = t / year_s
        H0 = 2.3e-18
        z = 0.0
        B_crit = 1e-6
        return (dpm_emergent_ug1(M, r)) * (1.0 + M_SF) * expansion * sc_correction
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{YoungStarsCore}",
            description=self.description
        )


class YoungStarsLambda:
    """
    Source36 YoungStars: Cosmological constant term
    
    Source: MAIN_1_CoAnQi.cpp (lines 10632-10647)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "YoungStars_Lambda"
        self.description = "Source36 YoungStars: Cosmological constant term"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        Lambda = 1.1056e-52
        c = 2.99792458e8
        return Lambda * c * c / 3.0
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{YoungStarsLambda}",
            description=self.description
        )


class YoungStarsUQFF:
    """
    Source36 YoungStars: UQFF sum (Ug1-4) with outflow velocity SC term
    
    Source: MAIN_1_CoAnQi.cpp (lines 10649-10674)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "YoungStars_UQFF"
        self.description = "Source36 YoungStars: UQFF sum (Ug1-4) with outflow velocity SC term"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        G = 6.67430e-11
        M_sun = 1.98847e30
        rho_UA = 7.09e-36
        rho_SCm = 7.09e-37
        Ug3 = 0.0
        f_sc = 0.01
        Ug4 = Ug1 * f_sc
        return Ug1 + Ug2 + Ug3 + Ug4
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{YoungStarsUQFF}",
            description=self.description
        )


class YoungStarsEM:
    """
    Source36 YoungStars: EM Lorentz with vacuum ratio correction
    
    Source: MAIN_1_CoAnQi.cpp (lines 10676-10696)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "YoungStars_EM"
        self.description = "Source36 YoungStars: EM Lorentz with vacuum ratio correction"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        q = 1.60217663e-19
        m_p = 1.67262192e-27
        rho_UA = 7.09e-36
        rho_SCm = 7.09e-37
        return (q * v_out * B / m_p) * vac_ratio
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{YoungStarsEM}",
            description=self.description
        )


class YoungStarsQuantum:
    """
    Source36 YoungStars: Quantum uncertainty term
    
    Source: MAIN_1_CoAnQi.cpp (lines 10698-10718)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "YoungStars_Quantum"
        self.description = "Source36 YoungStars: Quantum uncertainty term"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        hbar = 1.054571817e-34
        Delta_x = 1e-34
        Delta_p = hbar / Delta_x
        integral_psi = 1.0
        year_s = 3.15576e7
        t_Hubble = 13.8e9 * year_s
        return (hbar / unc) * integral_psi * (2.0 * 3.141592653589793 / t_Hubble)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{YoungStarsQuantum}",
            description=self.description
        )


class YoungStarsFluid:
    """
    Source36 YoungStars: Fluid dynamics term
    
    Source: MAIN_1_CoAnQi.cpp (lines 10720-10740)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "YoungStars_Fluid"
        self.description = "Source36 YoungStars: Fluid dynamics term"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        V = 1.0 / rho_fluid
        G = 6.67430e-11
        M_sun = 1.98847e30
        return rho_fluid * V * g_base
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{YoungStarsFluid}",
            description=self.description
        )


class YoungStarsOscillatory:
    """
    Source36 YoungStars: Resonant oscillatory term (standing + traveling)
    
    Source: MAIN_1_CoAnQi.cpp (lines 10742-10763)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "YoungStars_Oscillatory"
        self.description = "Source36 YoungStars: Resonant oscillatory term (standing + traveling)"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        A = 1e-10
        k = 1e20
        omega = 2.0 * 3.141592653589793 * 1.411e16
        year_s = 3.15576e7
        return standing + traveling_real
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{YoungStarsOscillatory}",
            description=self.description
        )


class YoungStarsDarkMatter:
    """
    Source36 YoungStars: Dark matter perturbation term
    
    Source: MAIN_1_CoAnQi.cpp (lines 10765-10786)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "YoungStars_DarkMatter"
        self.description = "Source36 YoungStars: Dark matter perturbation term"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        G = 6.67430e-11
        M_sun = 1.98847e30
        M_visible = M
        M_DM = 5.67 * M_visible
        delta_rho = 1e-5 * 1e-24
        rho = 1e-24
        return (M_visible + M_DM) * (delta_rho / rho + 3.0 * G * M / (r * r * r))
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{YoungStarsDarkMatter}",
            description=self.description
        )


class YoungStarsOutflowPressure:
    """
    Source36 YoungStars: Outflow pressure P=rho*v^2*(1+t/tau) UNIQUE
    
    Source: MAIN_1_CoAnQi.cpp (lines 10788-10806)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "YoungStars_OutflowPressure"
        self.description = "Source36 YoungStars: Outflow pressure P=rho*v^2*(1+t/tau) UNIQUE"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        year_s = 3.15576e7
        # P_outflow = rho * v^2 * (1 + t/tau) - time-evolving repulsive pressure
        return rho_fluid * v_out * v_out * (1.0 + t / t_evolve)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{YoungStarsOutflowPressure}",
            description=self.description
        )


class YoungStarsStarFormation:
    """
    Source36 YoungStars: Star formation mass factor M_SF=SFR*t/M UNIQUE
    
    Source: MAIN_1_CoAnQi.cpp (lines 10808-10826)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "YoungStars_StarFormationFactor"
        self.description = "Source36 YoungStars: Star formation mass factor M_SF=SFR*t/M UNIQUE"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        M_sun = 1.98847e30
        year_s = 3.15576e7
        t_yr = t / year_s
        return (SFR * t_yr) / M
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{YoungStarsStarFormation}",
            description=self.description
        )


class BigBangCore:
    """
    Source37 BigBang: Core gravity with M(t)=M_total*(t/t_H), r(t)=c*t
    
    Source: MAIN_1_CoAnQi.cpp (lines 10834-10865)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "BigBang_Core"
        self.description = "Source37 BigBang: Core gravity with M(t)=M_total*(t/t_H), r(t)=c*t"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        G = 6.67430e-11
        year_s = 3.15576e7
        t_Hubble = 13.8e9 * year_s
        c = 2.99792458e8
        r_t = c * t
        if r_t < 1e-30:
        r_t = 1e-30
        H0 = 70.0 * 1e3 / 3.0857e22
        Omega_m = 0.3
        Omega_Lambda = 0.7
        expansion = 1.0 + Hz * t
        B_crit = 1e-6
        return (dpm_emergent_ug1(M_t, r_t)) * expansion * sc_correction
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{BigBangCore}",
            description=self.description
        )


class BigBangLambda:
    """
    Source37 BigBang: Cosmological constant term
    
    Source: MAIN_1_CoAnQi.cpp (lines 10867-10882)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "BigBang_Lambda"
        self.description = "Source37 BigBang: Cosmological constant term"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        Lambda = 1.1056e-52
        c = 2.99792458e8
        return Lambda * c * c / 3.0
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{BigBangLambda}",
            description=self.description
        )


class BigBangUg1:
    """
    Source37 BigBang: Ug1 = G*M(t)/r(t)^2
    
    Source: MAIN_1_CoAnQi.cpp (lines 10884-10906)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "BigBang_Ug1"
        self.description = "Source37 BigBang: Ug1 = G*M(t)/r(t)^2"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        G = 6.67430e-11
        year_s = 3.15576e7
        t_Hubble = 13.8e9 * year_s
        c = 2.99792458e8
        r_t = c * t
        if r_t < 1e-30:
        r_t = 1e-30
        return (G * M_t) / (r_t * r_t)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{BigBangUg1}",
            description=self.description
        )


class BigBangUg4:
    """
    Source37 BigBang: Ug4 reaction = Ug1 * f_sc
    
    Source: MAIN_1_CoAnQi.cpp (lines 10908-10932)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "BigBang_Ug4"
        self.description = "Source37 BigBang: Ug4 reaction = Ug1 * f_sc"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        G = 6.67430e-11
        year_s = 3.15576e7
        t_Hubble = 13.8e9 * year_s
        c = 2.99792458e8
        r_t = c * t
        if r_t < 1e-30:
        r_t = 1e-30
        f_sc = 0.01
        return Ug1 * f_sc
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{BigBangUg4}",
            description=self.description
        )


class BigBangQuantum:
    """
    Source37 BigBang: Quantum uncertainty term
    
    Source: MAIN_1_CoAnQi.cpp (lines 10934-10954)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "BigBang_Quantum"
        self.description = "Source37 BigBang: Quantum uncertainty term"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        hbar = 1.054571817e-34
        Delta_x = 1e-34
        Delta_p = hbar / Delta_x
        integral_psi = 1.0
        year_s = 3.15576e7
        t_Hubble = 13.8e9 * year_s
        return (hbar / unc) * integral_psi * (2.0 * 3.141592653589793 / t_Hubble)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{BigBangQuantum}",
            description=self.description
        )


class BigBangFluid:
    """
    Source37 BigBang: Fluid dynamics term
    
    Source: MAIN_1_CoAnQi.cpp (lines 10956-10981)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "BigBang_Fluid"
        self.description = "Source37 BigBang: Fluid dynamics term"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        V = 1.0 / rho_fluid
        G = 6.67430e-11
        year_s = 3.15576e7
        t_Hubble = 13.8e9 * year_s
        c = 2.99792458e8
        r_t = c * t
        if r_t < 1e-30:
        r_t = 1e-30
        return rho_fluid * V * g_base
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{BigBangFluid}",
            description=self.description
        )


class BigBangOscillatory:
    """
    Source37 BigBang: Oscillatory resonance term
    
    Source: MAIN_1_CoAnQi.cpp (lines 10983-11003)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "BigBang_Oscillatory"
        self.description = "Source37 BigBang: Oscillatory resonance term"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        A = 1e-10
        k = 1e20
        omega = 2.0 * 3.141592653589793 * 1.411e16
        return standing + traveling_real
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{BigBangOscillatory}",
            description=self.description
        )


class BigBangDarkMatter:
    """
    Source37 BigBang: Dark matter fractional term
    
    Source: MAIN_1_CoAnQi.cpp (lines 11005-11029)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "BigBang_DarkMatter"
        self.description = "Source37 BigBang: Dark matter fractional term"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        DM_fraction = 0.268
        G = 6.67430e-11
        year_s = 3.15576e7
        t_Hubble = 13.8e9 * year_s
        c = 2.99792458e8
        r_t = c * t
        if r_t < 1e-30:
        r_t = 1e-30
        return DM_fraction * g_base
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{BigBangDarkMatter}",
            description=self.description
        )


class BigBangQuantumGravity:
    """
    Source37 BigBang: Quantum gravity QG=(hbar*c/l_p^2)*(t/t_p) UNIQUE
    
    Source: MAIN_1_CoAnQi.cpp (lines 11031-11049)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "BigBang_QuantumGravity"
        self.description = "Source37 BigBang: Quantum gravity QG=(hbar*c/l_p^2)*(t/t_p) UNIQUE"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        hbar = 1.054571817e-34
        c = 2.99792458e8
        t_p = l_p / c
        # QG = (hbar * c / l_p^2) * (t / t_p) - Planck scale effects
        return (hbar * c / (l_p * l_p)) * (t / t_p)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{BigBangQuantumGravity}",
            description=self.description
        )


class BigBangGravitationalWave:
    """
    Source37 BigBang: Gravitational wave GW=h*c^2/lambda*sin(...) UNIQUE
    
    Source: MAIN_1_CoAnQi.cpp (lines 11051-11071)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "BigBang_GravitationalWave"
        self.description = "Source37 BigBang: Gravitational wave GW=h*c^2/lambda*sin(...) UNIQUE"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        c = 2.99792458e8
        year_s = 3.15576e7
        r_t = c * t
        # GW = h * c^2 / lambda * sin(2pi/lambda*r - 2pi/year*t)
        result = 0.0  # Assign result
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{BigBangGravitationalWave}",
            description=self.description
        )


class BigBangCosmicEvolution:
    """
    Source37 BigBang: Cosmic evolution z(t)=t_H/t-1 (redshift proxy) UNIQUE
    
    Source: MAIN_1_CoAnQi.cpp (lines 11073-11094)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "BigBang_CosmicEvolution"
        self.description = "Source37 BigBang: Cosmic evolution z(t)=t_H/t-1 (redshift proxy) UNIQUE"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        year_s = 3.15576e7
        t_Hubble = 13.8e9 * year_s
        c = 2.99792458e8
        r_t = c * t
        # Returns redshift as proxy for cosmic evolution (high z early, 0 now)
        return z_t
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{BigBangCosmicEvolution}",
            description=self.description
        )


class M51Core:
    """
    Source38 M51: Core gravity with M_SF + expansion + SC + F_env(tidal+SF)
    
    Source: MAIN_1_CoAnQi.cpp (lines 11102-11135)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "M51_Core"
        self.description = "Source38 M51: Core gravity with M_SF + expansion + SC + F_env(tidal+SF)"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        G = 6.67430e-11
        M_sun = 1.98847e30
        year_s = 3.15576e7
        t_yr = t / year_s
        z = 0.0026
        H0 = 2.3e-18
        B_crit = 1e-6
        # Environmental forces: tidal + star formation
        F_env = F_tidal + F_SF
        return (dpm_emergent_ug1(M, r)) * (1.0 + M_SF) * expansion * sc_correction * (1.0 + F_env)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{M51Core}",
            description=self.description
        )


class M51Ug1Dipole:
    """
    Source38 M51: Ug1 = I*A*omega*B (BH magnetic dipole) UNIQUE
    
    Source: MAIN_1_CoAnQi.cpp (lines 11137-11158)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "M51_Ug1Dipole"
        self.description = "Source38 M51: Ug1 = I*A*omega*B (BH magnetic dipole) UNIQUE"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        M_sun = 1.98847e30
        I_dipole = 0.4 * M_BH * r_BH * r_BH
        A_dipole = 3.141592653589793 * r_BH * r_BH
        omega_spin = 2.0 * 3.141592653589793 / 1e4
        # Ug1 = I * A * omega * B (magnetic dipole interaction)
        return I_dipole * A_dipole * omega_spin * B
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{M51Ug1Dipole}",
            description=self.description
        )


class M51Ug2Superconductor:
    """
    Source38 M51: Ug2 = B^2/(2*mu_0) (superconductor term) UNIQUE
    
    Source: MAIN_1_CoAnQi.cpp (lines 11160-11176)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "M51_Ug2Superconductor"
        self.description = "Source38 M51: Ug2 = B^2/(2*mu_0) (superconductor term) UNIQUE"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        mu_0 = 1.25663706212e-6
        # Ug2 = B^2 / (2*mu_0) - superconductor magnetic energy density
        return (B * B) / (2.0 * mu_0)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{M51Ug2Superconductor}",
            description=self.description
        )


class M51Ug3Tidal:
    """
    Source38 M51: Ug3' = G*M_NGC5195/d^2 (external tidal) UNIQUE
    
    Source: MAIN_1_CoAnQi.cpp (lines 11178-11196)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "M51_Ug3Tidal"
        self.description = "Source38 M51: Ug3' = G*M_NGC5195/d^2 (external tidal) UNIQUE"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        G = 6.67430e-11
        M_sun = 1.98847e30
        # Ug3' = G * M_NGC5195 / d^2 - tidal force from companion galaxy
        return (G * M_NGC5195) / (d * d)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{M51Ug3Tidal}",
            description=self.description
        )


class M51Ug4Reaction:
    """
    Source38 M51: Ug4 = k_4*E_react*exp(-0.0005*t) (reaction decay) UNIQUE
    
    Source: MAIN_1_CoAnQi.cpp (lines 11198-11214)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "M51_Ug4Reaction"
        self.description = "Source38 M51: Ug4 = k_4*E_react*exp(-0.0005*t) (reaction decay) UNIQUE"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        k_4 = 1e-10
        # Ug4 = k_4 * E_react(t) - reactive term with decay
        return k_4 * E_react
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{M51Ug4Reaction}",
            description=self.description
        )


class M51UiVacuum:
    """
    Source38 M51: Ui vacuum interaction = lambda*(rho_SCm/rho_UA)*omega*cos(pi*t) UNIQUE
    
    Source: MAIN_1_CoAnQi.cpp (lines 11216-11236)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "M51_UiVacuum"
        self.description = "Source38 M51: Ui vacuum interaction = lambda*(rho_SCm/rho_UA)*omega*cos(pi*t) UNIQUE"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        lambda_I = 1.0
        rho_SCm = 7.09e-37
        rho_UA = 7.09e-36
        omega_i = 1e10
        t_n = t
        F_RZ = 0.1
        # Ui = lambda_I * (rho_SCm/rho_UA) * omega_i * cos(pi*t_n) * (1 + F_RZ)
        result = 0.0  # Assign result
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{M51UiVacuum}",
            description=self.description
        )


class M51Lambda:
    """
    Source38 M51: Cosmological constant term
    
    Source: MAIN_1_CoAnQi.cpp (lines 11238-11253)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "M51_Lambda"
        self.description = "Source38 M51: Cosmological constant term"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        Lambda = 1.1056e-52
        c = 2.99792458e8
        return Lambda * c * c / 3.0
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{M51Lambda}",
            description=self.description
        )


class M51Quantum:
    """
    Source38 M51: Quantum term with psi_spiral wave function UNIQUE
    
    Source: MAIN_1_CoAnQi.cpp (lines 11255-11283)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "M51_Quantum"
        self.description = "Source38 M51: Quantum term with psi_spiral wave function UNIQUE"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        hbar = 1.054571817e-34
        Delta_x = 1e-34
        Delta_p = hbar / Delta_x
        year_s = 3.15576e7
        t_Hubble = 13.8e9 * year_s
        # Spiral arm wave function contribution
        sigma = 1e20
        m = 2.0
        theta = 0.0
        A = 1.0
        return (hbar / unc) * integral_psi * (2.0 * 3.141592653589793 / t_Hubble)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{M51Quantum}",
            description=self.description
        )


class M51Fluid:
    """
    Source38 M51: Fluid dynamics term
    
    Source: MAIN_1_CoAnQi.cpp (lines 11285-11305)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "M51_Fluid"
        self.description = "Source38 M51: Fluid dynamics term"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        V = 1.0 / rho_fluid
        G = 6.67430e-11
        M_sun = 1.98847e30
        return rho_fluid * V * g_base
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{M51Fluid}",
            description=self.description
        )


class M51DarkMatter:
    """
    Source38 M51: Dark matter with curvature term
    
    Source: MAIN_1_CoAnQi.cpp (lines 11307-11329)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "M51_DarkMatter"
        self.description = "Source38 M51: Dark matter with curvature term"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        G = 6.67430e-11
        M_sun = 1.98847e30
        M_visible = M
        M_DM = 5.67 * M_visible
        delta_rho = 1e-5 * 1e-24
        rho = 1e-24
        return (M_visible + M_DM) * (delta_rho / rho + curvature)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{M51DarkMatter}",
            description=self.description
        )


class M51EnvironmentalForces:
    """
    Source38 M51: F_env = F_tidal + F_SF (environmental forces) UNIQUE
    
    Source: MAIN_1_CoAnQi.cpp (lines 11331-11354)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "M51_EnvironmentalForces"
        self.description = "Source38 M51: F_env = F_tidal + F_SF (environmental forces) UNIQUE"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        G = 6.67430e-11
        M_sun = 1.98847e30
        year_s = 3.15576e7
        # F_env = F_tidal + F_SF (tidal from NGC5195 + star formation pressure)
        return F_tidal + F_SF
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{M51EnvironmentalForces}",
            description=self.description
        )


class NGC1316Core:
    """
    Source39 NGC1316: Core with M_merge(t)=1e10*exp(-t/tau) merger mass UNIQUE
    
    Source: MAIN_1_CoAnQi.cpp (lines 11362-11389)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "NGC1316_Core"
        self.description = "Source39 NGC1316: Core with M_merge(t)=1e10*exp(-t/tau) merger mass UNIQUE"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        G = 6.67430e-11
        M_sun = 1.98847e30
        year_s = 3.15576e7
        z = 0.0059
        H0 = 2.3e-18
        B_crit = 1e-6
        return (dpm_emergent_ug1(M, r)) * m_factor * expansion * sc_correction
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{NGC1316Core}",
            description=self.description
        )


class NGC1316Ug1Dipole:
    """
    Source39 NGC1316: Ug1 BH magnetic dipole (M_BH=1.5e9 Msun)
    
    Source: MAIN_1_CoAnQi.cpp (lines 11391-11411)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "NGC1316_Ug1Dipole"
        self.description = "Source39 NGC1316: Ug1 BH magnetic dipole (M_BH=1.5e9 Msun)"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        M_sun = 1.98847e30
        I_dipole = 0.4 * M_BH * r_BH * r_BH
        A_dipole = 3.141592653589793 * r_BH * r_BH
        omega_spin = 2.0 * 3.141592653589793 / 1e5
        return I_dipole * A_dipole * omega_spin * B
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{NGC1316Ug1Dipole}",
            description=self.description
        )


class NGC1316Ug2Superconductor:
    """
    Source39 NGC1316: Ug2 superconductor magnetic energy
    
    Source: MAIN_1_CoAnQi.cpp (lines 11413-11428)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "NGC1316_Ug2Superconductor"
        self.description = "Source39 NGC1316: Ug2 superconductor magnetic energy"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        mu_0 = 1.25663706212e-6
        return (B * B) / (2.0 * mu_0)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{NGC1316Ug2Superconductor}",
            description=self.description
        )


class NGC1316Ug3External:
    """
    Source39 NGC1316: Ug3 external tidal from spiral companion
    
    Source: MAIN_1_CoAnQi.cpp (lines 11430-11447)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "NGC1316_Ug3External"
        self.description = "Source39 NGC1316: Ug3 external tidal from spiral companion"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        G = 6.67430e-11
        M_sun = 1.98847e30
        return (G * M_spiral) / (d * d)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{NGC1316Ug3External}",
            description=self.description
        )


class NGC1316Ug4Reaction:
    """
    Source39 NGC1316: Ug4 reaction term
    
    Source: MAIN_1_CoAnQi.cpp (lines 11449-11464)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "NGC1316_Ug4Reaction"
        self.description = "Source39 NGC1316: Ug4 reaction term"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        k_4 = 1e-10
        return k_4 * E_react
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{NGC1316Ug4Reaction}",
            description=self.description
        )


class NGC1316UiVacuum:
    """
    Source39 NGC1316: Ui vacuum interaction term
    
    Source: MAIN_1_CoAnQi.cpp (lines 11466-11484)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "NGC1316_UiVacuum"
        self.description = "Source39 NGC1316: Ui vacuum interaction term"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        lambda_I = 1.0
        rho_SCm = 7.09e-37
        rho_UA = 7.09e-36
        omega_i = 1e10
        F_RZ = 0.1
        result = 0.0  # Assign result
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{NGC1316UiVacuum}",
            description=self.description
        )


class NGC1316Lambda:
    """
    Source39 NGC1316: Cosmological constant
    
    Source: MAIN_1_CoAnQi.cpp (lines 11486-11501)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "NGC1316_Lambda"
        self.description = "Source39 NGC1316: Cosmological constant"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        Lambda = 1.1056e-52
        c = 2.99792458e8
        return Lambda * c * c / 3.0
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{NGC1316Lambda}",
            description=self.description
        )


class NGC1316Quantum:
    """
    Source39 NGC1316: Quantum with psi_dust wave function UNIQUE
    
    Source: MAIN_1_CoAnQi.cpp (lines 11503-11527)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "NGC1316_Quantum"
        self.description = "Source39 NGC1316: Quantum with psi_dust wave function UNIQUE"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        hbar = 1.054571817e-34
        Delta_x = 1e-34
        Delta_p = hbar / Delta_x
        year_s = 3.15576e7
        t_Hubble = 13.8e9 * year_s
        # Dust wave function psi_dust
        sigma_dust = 5e19
        A = 1.0
        return (hbar / unc) * psi_dust_sq * (2.0 * 3.141592653589793 / t_Hubble)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{NGC1316Quantum}",
            description=self.description
        )


class NGC1316FluidDust:
    """
    Source39 NGC1316: Fluid with rho_dust density UNIQUE
    
    Source: MAIN_1_CoAnQi.cpp (lines 11529-11550)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "NGC1316_FluidDust"
        self.description = "Source39 NGC1316: Fluid with rho_dust density UNIQUE"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        V = 1.0 / rho_dust
        G = 6.67430e-11
        M_sun = 1.98847e30
        # Fluid term uses rho_dust instead of rho_fluid
        return rho_dust * V * g_base
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{NGC1316FluidDust}",
            description=self.description
        )


class NGC1316DarkMatter:
    """
    Source39 NGC1316: Dark matter term
    
    Source: MAIN_1_CoAnQi.cpp (lines 11552-11573)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "NGC1316_DarkMatter"
        self.description = "Source39 NGC1316: Dark matter term"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        G = 6.67430e-11
        M_sun = 1.98847e30
        M_visible = M
        M_DM = 5.67 * M_visible
        delta_rho = 1e-5 * 1e-24
        rho = 1e-24
        return (M_visible + M_DM) * (delta_rho / rho + 3.0 * G * M / (r * r * r))
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{NGC1316DarkMatter}",
            description=self.description
        )


class NGC1316MergerForces:
    """
    Source39 NGC1316: Merger forces = F_tidal + F_cluster UNIQUE
    
    Source: MAIN_1_CoAnQi.cpp (lines 11575-11597)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "NGC1316_MergerForces"
        self.description = "Source39 NGC1316: Merger forces = F_tidal + F_cluster UNIQUE"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        G = 6.67430e-11
        M_sun = 1.98847e30
        M_cluster = 1e6 * M_sun
        k_cluster = 1e-15
        F_cluster = k_cluster * M_cluster
        # F_env = F_tidal + F_cluster (tidal from spiral + disrupted clusters)
        return F_tidal + F_cluster
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{NGC1316MergerForces}",
            description=self.description
        )


class SMBHBinaryDPMResonance:
    """
    Source40 SMBHBinary: DPM core resonance f_DPM*rho_vac/c UNIQUE
    
    Source: MAIN_1_CoAnQi.cpp (lines 11605-11622)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "SMBHBinary_DPMResonance"
        self.description = "Source40 SMBHBinary: DPM core resonance f_DPM*rho_vac/c UNIQUE"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        c = 2.99792458e8
        # f_DPM * rho_vac / c - Di-pseudo-monopole resonance frequency
        return f_DPM * rho_vac_plasm / c
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{SMBHBinaryDPMResonance}",
            description=self.description
        )


class SMBHBinaryTHzResonance:
    """
    Source40 SMBHBinary: THz pipeline resonance f_THz*sin(omega*t) UNIQUE
    
    Source: MAIN_1_CoAnQi.cpp (lines 11624-11640)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "SMBHBinary_THzResonance"
        self.description = "Source40 SMBHBinary: THz pipeline resonance f_THz*sin(omega*t) UNIQUE"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        omega = 2.0 * 3.141592653589793 * 1.411e16
        # f_THz * sin(omega*t) - THz hole pipeline resonance
        result = 0.0  # Assign result
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{SMBHBinaryTHzResonance}",
            description=self.description
        )


class SMBHBinaryUg4iResonance:
    """
    Source40 SMBHBinary: U_g4i reactive resonance UNIQUE
    
    Source: MAIN_1_CoAnQi.cpp (lines 11642-11660)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "SMBHBinary_Ug4iResonance"
        self.description = "Source40 SMBHBinary: U_g4i reactive resonance UNIQUE"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        lambda_I = 1.0
        f_TRZ = 0.1
        omega = 2.0 * 3.141592653589793 * 1.411e16
        # f_react * lambda_I * (1 + f_TRZ) * cos(omega*t) - Reactive U_g4i
        result = 0.0  # Assign result
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{SMBHBinaryUg4iResonance}",
            description=self.description
        )


class SMBHBinaryPlasmoticVacuum:
    """
    Source40 SMBHBinary: Plasmotic vacuum energy UNIQUE
    
    Source: MAIN_1_CoAnQi.cpp (lines 11662-11678)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "SMBHBinary_PlasmoticVacuum"
        self.description = "Source40 SMBHBinary: Plasmotic vacuum energy UNIQUE"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        lambda_I = 1.0
        # lambda_I * rho_vac_plasm - Plasmotic vacuum energy density
        return lambda_I * rho_vac_plasm
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{SMBHBinaryPlasmoticVacuum}",
            description=self.description
        )


class SMBHBinaryQuantumResonance:
    """
    Source40 SMBHBinary: Quantum resonance f_quantum/unc UNIQUE
    
    Source: MAIN_1_CoAnQi.cpp (lines 11680-11698)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "SMBHBinary_QuantumResonance"
        self.description = "Source40 SMBHBinary: Quantum resonance f_quantum/unc UNIQUE"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        hbar = 1.054571817e-34
        Delta_x = 1e-34
        # f_quantum / unc - Quantum resonance frequency
        return f_quantum / unc
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{SMBHBinaryQuantumResonance}",
            description=self.description
        )


class SMBHBinaryFluidResonance:
    """
    Source40 SMBHBinary: Fluid resonance f_fluid*(rho/rho_ref) UNIQUE
    
    Source: MAIN_1_CoAnQi.cpp (lines 11700-11717)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "SMBHBinary_FluidResonance"
        self.description = "Source40 SMBHBinary: Fluid resonance f_fluid*(rho/rho_ref) UNIQUE"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        rho_ref = 1e-24
        # f_fluid * (rho / rho_ref) - Density-modulated fluid resonance
        return f_fluid * (rho / rho_ref)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{SMBHBinaryFluidResonance}",
            description=self.description
        )


class SMBHBinaryOscillatoryResonance:
    """
    Source40 SMBHBinary: Oscillatory resonance f*exp(-t/tau)*|psi|^2 UNIQUE
    
    Source: MAIN_1_CoAnQi.cpp (lines 11719-11741)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "SMBHBinary_OscillatoryResonance"
        self.description = "Source40 SMBHBinary: Oscillatory resonance f*exp(-t/tau)*|psi|^2 UNIQUE"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        A = 1e-10
        k = 1e20
        omega = 2.0 * 3.141592653589793 * f_super
        # f_super * exp(-t/t_coal) * |psi|^2 - Exponentially decaying resonance
        result = 0.0  # Assign result
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{SMBHBinaryOscillatoryResonance}",
            description=self.description
        )


class SMBHBinaryExpansionResonance:
    """
    Source40 SMBHBinary: Cosmic expansion resonance (Aether freq) UNIQUE
    
    Source: MAIN_1_CoAnQi.cpp (lines 11743-11758)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "SMBHBinary_ExpansionResonance"
        self.description = "Source40 SMBHBinary: Cosmic expansion resonance (Aether freq) UNIQUE"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        # f_Aether (constant) - Cosmic expansion resonance frequency
        return f_Aether
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{SMBHBinaryExpansionResonance}",
            description=self.description
        )


class SMBHBinaryCoalescence:
    """
    Source40 SMBHBinary: Binary coalescence dynamics (2PN waveform) UNIQUE
    
    Source: MAIN_1_CoAnQi.cpp (lines 11760-11785)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "SMBHBinary_Coalescence"
        self.description = "Source40 SMBHBinary: Binary coalescence dynamics (2PN waveform) UNIQUE"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        M_sun = 1.98847e30
        M_total = M1 + M2
        G = 6.67430e-11
        c = 2.99792458e8
        # Chirp mass influence: G*M_chirp/c^2 * (1 - t/t_coal) - Binary dynamics
        if tau_factor < 0.0:
        tau_factor = 0.0
        return (dpm_emergent_ug1(M_chirp, c)) * tau_factor
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{SMBHBinaryCoalescence}",
            description=self.description
        )


class BackgroundAetherMinkowski:
    """
    Source41 BackgroundAether: Minkowski metric g_μν=[1,-1,-1,-1] UNIQUE
    
    Source: MAIN_1_CoAnQi.cpp (lines 11793-11807)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "BackgroundAether_Minkowski"
        self.description = "Source41 BackgroundAether: Minkowski metric g_μν=[1,-1,-1,-1] UNIQUE"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        # g_00 = 1 (time component of Minkowski metric, fixed)
        return 1.0
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{BackgroundAetherMinkowski}",
            description=self.description
        )


class BackgroundAetherPerturbedMetric:
    """
    Source41 BackgroundAether: Perturbed metric A=g+eta*T_s UNIQUE
    
    Source: MAIN_1_CoAnQi.cpp (lines 11809-11828)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "BackgroundAether_PerturbedMetric"
        self.description = "Source41 BackgroundAether: Perturbed metric A=g+eta*T_s UNIQUE"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        T_s_base = 1.27e3
        rho_vac_A = 1.11e7
        T_s = T_s_base + rho_vac_A
        # A_?? = g_?? + eta * T_s (perturbed metric)
        # Return perturbation term eta * T_s
        return eta * T_s
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{BackgroundAetherPerturbedMetric}",
            description=self.description
        )


class BackgroundAetherStressEnergy:
    """
    Source41 BackgroundAether: Stress-energy tensor T_s=1.123e7 J/m³ UNIQUE
    
    Source: MAIN_1_CoAnQi.cpp (lines 11830-11847)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "BackgroundAether_StressEnergy"
        self.description = "Source41 BackgroundAether: Stress-energy tensor T_s=1.123e7 J/m³ UNIQUE"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        T_s_base = 1.27e3
        rho_vac_A = 1.11e7
        T_s = T_s_base + rho_vac_A
        # T_s^?? (diagonal stress-energy tensor, return scalar approximation)
        return T_s
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{BackgroundAetherStressEnergy}",
            description=self.description
        )


class BackgroundAetherCoupling:
    """
    Source41 BackgroundAether: Coupling parameter eta~1e-22 UNIQUE
    
    Source: MAIN_1_CoAnQi.cpp (lines 11849-11864)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "BackgroundAether_Coupling"
        self.description = "Source41 BackgroundAether: Coupling parameter eta~1e-22 UNIQUE"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        # eta parameter for metric perturbation strength
        return eta
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{BackgroundAetherCoupling}",
            description=self.description
        )


class HeavisideFraction:
    """
    Source42 Heaviside: f_Heaviside = 0.01 (unitless threshold fraction)
    
    Source: MAIN_1_CoAnQi.cpp (lines 11873-11887)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "Heaviside_Fraction"
        self.description = "Source42 Heaviside: f_Heaviside = 0.01 (unitless threshold fraction)"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return f_Heaviside
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{HeavisideFraction}",
            description=self.description
        )


class HeavisideFactor:
    """
    Source42 Heaviside: (1 + 10^13 × f_H) = 1 + 1e11 amplification UNIQUE
    
    Source: MAIN_1_CoAnQi.cpp (lines 11889-11904)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "Heaviside_Factor"
        self.description = "Source42 Heaviside: (1 + 10^13 × f_H) = 1 + 1e11 amplification UNIQUE"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return 1.0 + scale * f_Heaviside
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{HeavisideFactor}",
            description=self.description
        )


class HeavisideUmBase:
    """
    Source42 Heaviside: U_m base = (μ/r)(1-e^(-γt cos(πt_n)))φ̂·P_SCm·E_react
    
    Source: MAIN_1_CoAnQi.cpp (lines 11906-11931)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "Heaviside_UmBase"
        self.description = "Source42 Heaviside: U_m base = (μ/r)(1-e^(-γt cos(πt_n)))φ̂·P_SCm·E_react"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        pi = 3.141592653589793
        return (mu_j / r_j) * one_minus_exp * phi_hat_j * P_SCm * E_react
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{HeavisideUmBase}",
            description=self.description
        )


class HeavisideUmWithHeaviside:
    """
    Source42 Heaviside: U_m × (1+10^13·f_H) × (1+f_quasi) UNIQUE amplification
    
    Source: MAIN_1_CoAnQi.cpp (lines 11933-11969)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "Heaviside_UmAmplified"
        self.description = "Source42 Heaviside: U_m × (1+10^13·f_H) × (1+f_quasi) UNIQUE amplification"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        # Compute base
        pi = 3.141592653589793
        # Apply Heaviside amplification
        heaviside_factor = 1.0 + scale * f_Heaviside
        # Apply quasi factor
        quasi_factor = 1.0 + f_quasi
        return base * heaviside_factor * quasi_factor
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{HeavisideUmWithHeaviside}",
            description=self.description
        )


class HeavisideUmWithoutHeaviside:
    """
    Source42 Heaviside: U_m without Heaviside (baseline comparison)
    
    Source: MAIN_1_CoAnQi.cpp (lines 11971-12002)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "Heaviside_UmUnscaled"
        self.description = "Source42 Heaviside: U_m without Heaviside (baseline comparison)"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        # Compute base without Heaviside (f_Heaviside = 0)
        pi = 3.141592653589793
        # Apply only quasi factor (no Heaviside)
        quasi_factor = 1.0 + f_quasi
        return base * quasi_factor
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{HeavisideUmWithoutHeaviside}",
            description=self.description
        )


class HeavisideQuasiFactor:
    """
    Source42 Heaviside: (1 + f_quasi) quasi-scaling factor
    
    Source: MAIN_1_CoAnQi.cpp (lines 12004-12018)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "Heaviside_QuasiFactor"
        self.description = "Source42 Heaviside: (1 + f_quasi) quasi-scaling factor"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return 1.0 + f_quasi
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{HeavisideQuasiFactor}",
            description=self.description
        )


class HeavisideExponentialDecay:
    """
    Source42 Heaviside: (1 - e^(-γt cos(πt_n))) time-modulated decay
    
    Source: MAIN_1_CoAnQi.cpp (lines 12020-12037)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "Heaviside_ExpDecay"
        self.description = "Source42 Heaviside: (1 - e^(-γt cos(πt_n))) time-modulated decay"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        pi = 3.141592653589793
        result = 0.0  # Assign result
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{HeavisideExponentialDecay}",
            description=self.description
        )


class HeavisideMagneticMoment:
    """
    Source42 Heaviside: μ_j/r_j magnetic moment density
    
    Source: MAIN_1_CoAnQi.cpp (lines 12039-12054)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "Heaviside_MagneticMoment"
        self.description = "Source42 Heaviside: μ_j/r_j magnetic moment density"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return mu_j / r_j
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{HeavisideMagneticMoment}",
            description=self.description
        )


class HeavisideAmplificationRatio:
    """
    Source42 Heaviside: Amplification ratio ~1e11 UNIQUE threshold effect
    
    Source: MAIN_1_CoAnQi.cpp (lines 12056-12073)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "Heaviside_AmplificationRatio"
        self.description = "Source42 Heaviside: Amplification ratio ~1e11 UNIQUE threshold effect"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        heaviside_factor = 1.0 + scale * f_Heaviside
        # Returns amplification ratio (typically ~1e11 for default values)
        return heaviside_factor
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{HeavisideAmplificationRatio}",
            description=self.description
        )


class HydrogenResonance_Amplitude:
    """
    Source43 HydrogenRes: Nuclear resonance amplitude A_res for PToE UNIQUE
    
    Source: MAIN_1_CoAnQi.cpp (lines 12083-12104)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "HRes_Amplitude"
        self.description = "Source43 HydrogenRes: Nuclear resonance amplitude A_res for PToE UNIQUE"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        # A_res = k_A ? Z ? (A/A_H) ? (1 + ?_pair)
        return A_res
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{HydrogenResonance_Amplitude}",
            description=self.description
        )


class HydrogenResonance_Frequency:
    """
    Source43 HydrogenRes: Resonance frequency f_res from binding energy UNIQUE
    
    Source: MAIN_1_CoAnQi.cpp (lines 12107-12130)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "HRes_Frequency"
        self.description = "Source43 HydrogenRes: Resonance frequency f_res from binding energy UNIQUE"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        # Convert E_bind to Joules
        E_J = E_bind * 1.602e-19
        # f_res = (E_bind/h) ? (A_H/A)
        return f_res
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{HydrogenResonance_Frequency}",
            description=self.description
        )


class HydrogenResonance_DeepPairing:
    """
    Source43 HydrogenRes: Deep pairing U_dp between nuclear components UNIQUE
    
    Source: MAIN_1_CoAnQi.cpp (lines 12133-12154)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "HRes_DeepPairing"
        self.description = "Source43 HydrogenRes: Deep pairing U_dp between nuclear components UNIQUE"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        # U_dp = k ? (A1 ? A2 / f_dp?) ? cos(?_dp)
        return U_dp
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{HydrogenResonance_DeepPairing}",
            description=self.description
        )


class HydrogenResonance_NuclearCoupling:
    """
    Source43 HydrogenRes: Nuclear coupling k_nuc with N/Z ratio UNIQUE
    
    Source: MAIN_1_CoAnQi.cpp (lines 12157-12177)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "HRes_NuclearCoupling"
        self.description = "Source43 HydrogenRes: Nuclear coupling k_nuc with N/Z ratio UNIQUE"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        # k_nuc = k_0 ? (N/Z) ? (1 + ?_pair)
        return k_nuc
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{HydrogenResonance_NuclearCoupling}",
            description=self.description
        )


class HydrogenResonance_ShellCorrection:
    """
    Source43 HydrogenRes: Shell correction S_shell from magic numbers UNIQUE
    
    Source: MAIN_1_CoAnQi.cpp (lines 12180-12211)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "HRes_ShellCorrection"
        self.description = "Source43 HydrogenRes: Shell correction S_shell from magic numbers UNIQUE"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        # Magic numbers: 2, 8, 20, 28, 50, 82, 126
        Z_magic = 0.0, N_magic = 0.0
        # TODO: Convert C++ for loop
        if abs(Z - m:
            < 1.0)
        Z_magic = m
        if abs(N - m:
            < 1.0)
        N_magic = m
        # S_shell = S_scale ? (Z_magic + N_magic)
        return S_shell
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{HydrogenResonance_ShellCorrection}",
            description=self.description
        )


class HydrogenResonance_Buoyancy:
    """
    Source43 HydrogenRes: Enhanced buoyancy with nuclear pairing+magic UNIQUE
    
    Source: MAIN_1_CoAnQi.cpp (lines 12214-12266)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "HRes_Buoyancy"
        self.description = "Source43 HydrogenRes: Enhanced buoyancy with nuclear pairing+magic UNIQUE"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        # Nuclear radius and volume
        rho_nuc = 2.3e17
        # Binding energy contribution
        E_J = E_bind * 1.602e-19
        # Pairing effects
        A_int = (int)A, Z_int = (int)Z
        pairing = 0.0
        if A_int % 2 == 0  and  Z_int % 2 == 0:
        pairing = 0.5
        elif A_int % 2 == 1:
        pairing = 0.0
        else:
        pairing = -0.5
        # Magic number enhancement
        magic_enhance = 1.0
        # TODO: Convert C++ for loop
        if abs(Z - m:
            < 1.0  or  abs(N - m) < 1.0)
        magic_enhance = 1.5
        # Strong force range
        # Combined buoyancy
        return buoyancy
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{HydrogenResonance_Buoyancy}",
            description=self.description
        )


class HydrogenResonance_Superconductive:
    """
    Source43 HydrogenRes: SC with quantum coherence+tunneling UNIQUE
    
    Source: MAIN_1_CoAnQi.cpp (lines 12269-12312)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "HRes_Superconductive"
        self.description = "Source43 HydrogenRes: SC with quantum coherence+tunneling UNIQUE"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        # Nuclear frequency
        # Quantum coherence (real part only for simplicity)
        phase = 2.0 * math.pi * nu_nuc * t
        # Coulomb barrier and tunneling
        V_barrier = 1e6 * Z
        # Isotope effects
        isotope_factor = 1.0
        if A > 2.0 * Z:
        isotope_factor = 1.2
        elif A < 2.0 * Z:
        isotope_factor = 0.8
        # Nuclear spin coupling (simplified)
        A_int = (int)A
        spin_coupling = 1.0 + 0.1 * spin
        # Combined superconductive term
        SC_enhanced = SC_m * coherence * tunneling * isotope_factor * spin_coupling + U_dp
        return SC_enhanced
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{HydrogenResonance_Superconductive}",
            description=self.description
        )


class HydrogenResonance_PairingEnergy:
    """
    Source43 HydrogenRes: Pairing energy δ_pair for even-odd nuclei UNIQUE
    
    Source: MAIN_1_CoAnQi.cpp (lines 12315-12343)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "HRes_PairingEnergy"
        self.description = "Source43 HydrogenRes: Pairing energy δ_pair for even-odd nuclei UNIQUE"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        # Pairing energy based on even-odd structure
        A_int = (int)A, Z_int = (int)Z
        pairing_term = delta_pair
        if A_int % 2 == 0  and  Z_int % 2 == 0:
        pairing_term += 0.5
        elif A_int % 2 == 1:
        pairing_term += 0.0
        else:
        pairing_term -= 0.5
        return pairing_term
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{HydrogenResonance_PairingEnergy}",
            description=self.description
        )


class HydrogenResonance_MagicStability:
    """
    Source43 HydrogenRes: Magic number stability enhancement UNIQUE
    
    Source: MAIN_1_CoAnQi.cpp (lines 12346-12375)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "HRes_MagicStability"
        self.description = "Source43 HydrogenRes: Magic number stability enhancement UNIQUE"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        # Magic numbers from nuclear shell model
        stability_factor = 1.0
        # TODO: Convert C++ for loop
        if abs(Z - m:
            < 1.0  or  abs(N - m) < 1.0)
        stability_factor = 1.5
        break
        return stability_factor
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{HydrogenResonance_MagicStability}",
            description=self.description
        )


class HydrogenResonance_TunnelingProb:
    """
    Source43 HydrogenRes: Coulomb barrier tunneling probability UNIQUE
    
    Source: MAIN_1_CoAnQi.cpp (lines 12378-12400)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "HRes_TunnelingProb"
        self.description = "Source43 HydrogenRes: Coulomb barrier tunneling probability UNIQUE"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        # Coulomb barrier height (eV)
        V_barrier = 1e6 * Z
        # Tunneling probability (WKB approximation)
        return tunneling_prob
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{HydrogenResonance_TunnelingProb}",
            description=self.description
        )


class HydrogenResonance_Integrand:
    """
    Source43 HydrogenRes: Full H_res integrand formula UNIQUE
    
    Source: MAIN_1_CoAnQi.cpp (lines 12403-12428)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "HRes_Integrand"
        self.description = "Source43 HydrogenRes: Full H_res integrand formula UNIQUE"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        # Get component terms
        # Integrand = A_res ? sin(2?f_res ? t) + U_dp ? SC_m ? k_nuc + S_shell
        return integrand
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{HydrogenResonance_Integrand}",
            description=self.description
        )


class HydrogenResonance_Full:
    """
    Source43 HydrogenRes: Complete H_res = integrand × x2 UNIQUE
    
    Source: MAIN_1_CoAnQi.cpp (lines 12431-12451)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "HRes_Full"
        self.description = "Source43 HydrogenRes: Complete H_res = integrand × x2 UNIQUE"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        # Get integrand and quadratic root
        # H_res = integrand ? x2
        H_res = integrand * x2
        return H_res
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{HydrogenResonance_Full}",
            description=self.description
        )


class SurfaceMagnetic_Oscillation:
    """
    Source43 SurfaceMag: Solar magnetic field oscillation B_j UNIQUE
    
    Source: MAIN_1_CoAnQi.cpp (lines 12454-12475)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "SurfMag_Oscillation"
        self.description = "Source43 SurfaceMag: Solar magnetic field oscillation B_j UNIQUE"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        # B_j = B_s ? (B_s/B_ref)^k_3 ? cos(omega_s ? t)
        return B_j
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{SurfaceMagnetic_Oscillation}",
            description=self.description
        )


class SurfaceMagnetic_MinField:
    """
    Source43 SurfaceMag: Minimum magnetic field B_s_min (quiet Sun) UNIQUE
    
    Source: MAIN_1_CoAnQi.cpp (lines 12478-12492)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "SurfMag_MinField"
        self.description = "Source43 SurfaceMag: Minimum magnetic field B_s_min (quiet Sun) UNIQUE"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return B_s_min
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{SurfaceMagnetic_MinField}",
            description=self.description
        )


class SurfaceMagnetic_MaxField:
    """
    Source43 SurfaceMag: Maximum magnetic field B_s_max (sunspot) UNIQUE
    
    Source: MAIN_1_CoAnQi.cpp (lines 12495-12509)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "SurfMag_MaxField"
        self.description = "Source43 SurfaceMag: Maximum magnetic field B_s_max (sunspot) UNIQUE"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return B_s_max
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{SurfaceMagnetic_MaxField}",
            description=self.description
        )


class SurfaceMagnetic_CycleReference:
    """
    Source43 SurfaceMag: Solar cycle reference field B_ref UNIQUE
    
    Source: MAIN_1_CoAnQi.cpp (lines 12512-12535)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "SurfMag_CycleRef"
        self.description = "Source43 SurfaceMag: Solar cycle reference field B_ref UNIQUE"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        # Solar cycle phase
        # B_ref varies from 0.1?B_max to 1.0?B_max over 11-year cycle
        return B_ref
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{SurfaceMagnetic_CycleReference}",
            description=self.description
        )


class Source10_F_U_Bi_iTerm:
    """
    UQFF Core buoyancy force from source10.cpp
    
    Source: MAIN_1_CoAnQi.cpp (lines 14208-14237)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "Source10_F_U_Bi_i"
        self.description = "UQFF Core buoyancy force from source10.cpp"
        self.metadata = {}
        self.metadata['version'] = '1.0'
        self.metadata['source'] = 'source10.cpp'
        self.metadata['equation'] = 'F_U_Bi_i = integrand * x_2'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        # Core UQFF buoyancy calculation
        # Compute components
        return integrand * x_2
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{Source10_F_U_Bi_iTerm}",
            description=self.description
        )


class Source10_g_UQFFTerm:
    """
    Compressed UQFF gravity field
    
    Source: MAIN_1_CoAnQi.cpp (lines 14242-14267)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "Source10_g_UQFF"
        self.description = "Compressed UQFF gravity field"
        self.metadata = {}
        self.metadata['version'] = '1.0'
        self.metadata['source'] = 'source10.cpp'
        self.metadata['equation'] = 'g_UQFF(r,t) compressed gravity'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        G_const = 6.674e-11
        # Compressed gravity formulation
        result = 0.0  # Assign result
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{Source10_g_UQFFTerm}",
            description=self.description
        )


class Source10_DPM_ResonanceTerm:
    """
    Dark matter resonance term
    
    Source: MAIN_1_CoAnQi.cpp (lines 14272-14295)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "Source10_DPM_Resonance"
        self.description = "Dark matter resonance term"
        self.metadata = {}
        self.metadata['version'] = '1.0'
        self.metadata['source'] = 'source10.cpp'
        self.metadata['equation'] = 'DPM resonance coupling'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        result = 0.0  # TODO: Implement computation
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{Source10_DPM_ResonanceTerm}",
            description=self.description
        )


class Source10_E_cmTerm:
    """
    Center of mass energy calculation
    
    Source: MAIN_1_CoAnQi.cpp (lines 14300-14326)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "Source10_E_cm"
        self.description = "Center of mass energy calculation"
        self.metadata = {}
        self.metadata['version'] = '1.0'
        self.metadata['source'] = 'source10.cpp'
        self.metadata['equation'] = 'E_cm = center of mass energy'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        c = 3e8
        # Relativistic energy
        return M * c * c * (gamma - 1.0)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{Source10_E_cmTerm}",
            description=self.description
        )


class Source11_Ug1Term:
    """
    U_g1 gravity term from celestial body dynamics
    
    Source: MAIN_1_CoAnQi.cpp (lines 14332-14372)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "Source11_Ug1"
        self.description = "U_g1 gravity term from celestial body dynamics"
        self.metadata = {}
        self.metadata['version'] = '1.0'
        self.metadata['source'] = 'source11.cpp'
        self.metadata['equation'] = 'U_g1 = k1 * mu_s * grad_Ms_r * defect'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        # Compute mu_s
        # Compute gradient
        G_const = 6.674e-11
        # Defect factor
        result = 0.0  # Assign result
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{Source11_Ug1Term}",
            description=self.description
        )


class Source11_Ug2Term:
    """
    U_g2 gravity term with solar wind modulation
    
    Source: MAIN_1_CoAnQi.cpp (lines 14377-14420)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "Source11_Ug2"
        self.description = "U_g2 gravity term with solar wind modulation"
        self.metadata = {}
        self.metadata['version'] = '1.0'
        self.metadata['source'] = 'source11.cpp'
        self.metadata['equation'] = 'U_g2 = k2 * (QA + QUA) * Ms/r^2 * S * wind_mod * HSCm * Ereact'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        # Step function
        # Wind modulation
        wind_mod = 1.0 + delta_sw * v_sw
        # Reactive energy
        return k2 * (QA + QUA) * Ms / (r * r) * S * wind_mod * HSCm * Ereact
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{Source11_Ug2Term}",
            description=self.description
        )


class Source11_Ug3Term:
    """
    U_g3 magnetic coupling term
    
    Source: MAIN_1_CoAnQi.cpp (lines 14425-14463)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "Source11_Ug3"
        self.description = "U_g3 magnetic coupling term"
        self.metadata = {}
        self.metadata['version'] = '1.0'
        self.metadata['source'] = 'source11.cpp'
        self.metadata['equation'] = 'U_g3 = k3 * Bj * cos(omega_s_t) * Pcore * Ereact'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        # Compute omega_s(t)
        # Compute B_j
        # Reactive energy
        result = 0.0  # Assign result
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{Source11_Ug3Term}",
            description=self.description
        )


class Source11_Ug4Term:
    """
    U_g4 black hole contribution term
    
    Source: MAIN_1_CoAnQi.cpp (lines 14468-14500)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "Source11_Ug4"
        self.description = "U_g4 black hole contribution term"
        self.metadata = {}
        self.metadata['version'] = '1.0'
        self.metadata['source'] = 'source11.cpp'
        self.metadata['equation'] = 'U_g4 = k4 * rho_v * C * Mbh/dg * decay * cycle'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return k4 * rho_v * C_concentration * Mbh / dg * decay * cycle * (1 + f_feedback)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{Source11_Ug4Term}",
            description=self.description
        )


class Source11_UmTerm:
    """
    Universal magnetism term with string contributions
    
    Source: MAIN_1_CoAnQi.cpp (lines 14505-14549)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "Source11_Um"
        self.description = "Universal magnetism term with string contributions"
        self.metadata = {}
        self.metadata['version'] = '1.0'
        self.metadata['source'] = 'source11.cpp'
        self.metadata['equation'] = 'U_m = mu_j/rj * decay * phi_hat * num_strings * PSCm * Ereact'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        # Compute mu_j
        # Decay factor
        # Reactive energy
        single = mu_j / rj * decay * phi_hat
        return single * num_strings * PSCm * Ereact
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{Source11_UmTerm}",
            description=self.description
        )


class UQFFModule5_DarkMatterHaloTerm:
    """
    NFW dark matter halo contribution
    
    Source: MAIN_1_CoAnQi.cpp (lines 14555-14586)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "UQFFModule5_DarkMatterHalo"
        self.description = "NFW dark matter halo contribution"
        self.metadata = {}
        self.metadata['version'] = '2.0-Enhanced'
        self.metadata['source'] = 'Source5.cpp'
        self.metadata['equation'] = 'NFW dark matter halo profile'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        G_const = 6.674e-11
        if r == 0.0  or  r_scale == 0.0: return 0.0
        x = r / r_scale
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{UQFFModule5_DarkMatterHaloTerm}",
            description=self.description
        )


class UQFFModule5_VacuumEnergyTerm:
    """
    Vacuum energy fluctuation term
    
    Source: MAIN_1_CoAnQi.cpp (lines 14591-14614)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "UQFFModule5_VacuumEnergy"
        self.description = "Vacuum energy fluctuation term"
        self.metadata = {}
        self.metadata['version'] = '2.0-Enhanced'
        self.metadata['source'] = 'Source5.cpp'
        self.metadata['equation'] = 'Time-varying vacuum energy'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        result = 0.0  # TODO: Implement computation
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{UQFFModule5_VacuumEnergyTerm}",
            description=self.description
        )


class Source5_CompressedBaseTerm:
    """
    MUGE compressed base gravity calculation
    
    Source: MAIN_1_CoAnQi.cpp (lines 14619-14644)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "Source5_CompressedBase"
        self.description = "MUGE compressed base gravity calculation"
        self.metadata = {}
        self.metadata['version'] = '2.0-Enhanced'
        self.metadata['source'] = 'Source5.cpp'
        self.metadata['equation'] = 'MUGE compressed base calculation'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        G_const = 6.674e-11
        # Compressed gravity base
        return G_const * M / r
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{Source5_CompressedBaseTerm}",
            description=self.description
        )


class Source5_CompressedExpansionTerm:
    """
    MUGE compressed expansion (Hubble flow)
    
    Source: MAIN_1_CoAnQi.cpp (lines 14649-14673)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "Source5_CompressedExpansion"
        self.description = "MUGE compressed expansion (Hubble flow)"
        self.metadata = {}
        self.metadata['version'] = '2.0-Enhanced'
        self.metadata['source'] = 'Source5.cpp'
        self.metadata['equation'] = 'MUGE compressed expansion with Hubble'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        # Hubble expansion contribution
        result = 0.0  # Assign result
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{Source5_CompressedExpansionTerm}",
            description=self.description
        )


class Source5_CompressedQuantumTerm:
    """
    Compressed quantum uncertainty term
    
    Source: MAIN_1_CoAnQi.cpp (lines 14678-14710)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "Source5_CompressedQuantum"
        self.description = "Compressed quantum uncertainty term"
        self.metadata = {}
        self.metadata['version'] = '2.0-Enhanced'
        self.metadata['source'] = 'Source5.cpp'
        self.metadata['equation'] = 'Compressed quantum uncertainty contribution'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        if Delta_x_p == 0.0: return 0.0
        # Quantum uncertainty contribution
        quantum_factor = hbar / Delta_x_p
        time_factor = integral_psi / tHubble
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{Source5_CompressedQuantumTerm}",
            description=self.description
        )


class Source5_CompressedCosmTerm:
    """
    Compressed cosmological constant (Lambda)
    
    Source: MAIN_1_CoAnQi.cpp (lines 14715-14739)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "Source5_CompressedCosm"
        self.description = "Compressed cosmological constant (Lambda)"
        self.metadata = {}
        self.metadata['version'] = '2.0-Enhanced'
        self.metadata['source'] = 'Source5.cpp'
        self.metadata['equation'] = 'Compressed cosmological constant contribution'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        # Cosmological constant contribution (repulsive)
        return -(Lambda * r * r * r) / 3.0
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{Source5_CompressedCosmTerm}",
            description=self.description
        )


class Source5_CompressedFluidTerm:
    """
    Compressed fluid dynamics contribution
    
    Source: MAIN_1_CoAnQi.cpp (lines 14744-14771)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "Source5_CompressedFluid"
        self.description = "Compressed fluid dynamics contribution"
        self.metadata = {}
        self.metadata['version'] = '2.0-Enhanced'
        self.metadata['source'] = 'Source5.cpp'
        self.metadata['equation'] = 'MUGE compressed fluid dynamics'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        # Fluid contribution to compressed gravity
        return 4.0 * math.pi * 6.674e-11 * fluid_factor / (3.0 * M)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{Source5_CompressedFluidTerm}",
            description=self.description
        )


class Source5_CompressedPerturbationTerm:
    """
    Compressed metric perturbation term
    
    Source: MAIN_1_CoAnQi.cpp (lines 14776-14800)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "Source5_CompressedPerturbation"
        self.description = "Compressed metric perturbation term"
        self.metadata = {}
        self.metadata['version'] = '2.0-Enhanced'
        self.metadata['source'] = 'Source5.cpp'
        self.metadata['equation'] = 'MUGE compressed metric perturbation'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        # Time-varying metric perturbation
        result = 0.0  # Assign result
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{Source5_CompressedPerturbationTerm}",
            description=self.description
        )


class Source5_ResonanceMUGETerm:
    """
    MUGE resonance term with quality factor
    
    Source: MAIN_1_CoAnQi.cpp (lines 14805-14832)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "Source5_ResonanceMUGE"
        self.description = "MUGE resonance term with quality factor"
        self.metadata = {}
        self.metadata['version'] = '2.0-Enhanced'
        self.metadata['source'] = 'Source5.cpp'
        self.metadata['equation'] = 'MUGE resonance calculation'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        # Resonant contribution with quality factor
        return resonance
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{Source5_ResonanceMUGETerm}",
            description=self.description
        )


class Source5_CompressedSuperAdjTerm:
    """
    Compressed supercritical magnetic adjustment
    
    Source: MAIN_1_CoAnQi.cpp (lines 14837-14866)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "Source5_CompressedSuperAdj"
        self.description = "Compressed supercritical magnetic adjustment"
        self.metadata = {}
        self.metadata['version'] = '2.0-Enhanced'
        self.metadata['source'] = 'Source5.cpp'
        self.metadata['equation'] = 'Compressed supercritical adjustment'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        # Supercritical field adjustment
        if B_field > B_crit:
        return 0.0
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{Source5_CompressedSuperAdjTerm}",
            description=self.description
        )


class Source5_CompressedEnvTerm:
    """
    Compressed environmental thermal term
    
    Source: MAIN_1_CoAnQi.cpp (lines 14871-14896)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "Source5_CompressedEnv"
        self.description = "Compressed environmental thermal term"
        self.metadata = {}
        self.metadata['version'] = '2.0-Enhanced'
        self.metadata['source'] = 'Source5.cpp'
        self.metadata['equation'] = 'Compressed environmental contribution'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        k_B = 1.38e-23
        # Environmental thermal contribution
        result = 0.0  # Assign result
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{Source5_CompressedEnvTerm}",
            description=self.description
        )


class Source5_CompressedUgSumTerm:
    """
    Total compressed Ug gravity sum
    
    Source: MAIN_1_CoAnQi.cpp (lines 14901-14927)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "Source5_CompressedUgSum"
        self.description = "Total compressed Ug gravity sum"
        self.metadata = {}
        self.metadata['version'] = '2.0-Enhanced'
        self.metadata['source'] = 'Source5.cpp'
        self.metadata['equation'] = 'Sum of Ug1+Ug2+Ug3+Ug4 compressed'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        # Total unified gravity
        return Ug1 + Ug2 + Ug3 + Ug4
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{Source5_CompressedUgSumTerm}",
            description=self.description
        )


class ESO137_BuoyancyTerm:
    """
    ESO137-001 buoyancy force
    
    Source: MAIN_1_CoAnQi.cpp (lines 14941-14966)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "ESO137_Buoyancy"
        self.description = "ESO137-001 buoyancy force"
        self.metadata = {}
        self.metadata['version'] = '1.0'
        self.metadata['source'] = 'Source155.cpp'
        self.metadata['system'] = 'ESO137-001'
        self.metadata['M'] = '2e41 kg'
        self.metadata['r'] = '6.17e21 m'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        M = 2e41
        r = 6.17e21
        B0 = 2e-9
        rho_gas = 1e-23
        # Buoyancy force calculation
        return M * B0 * rho_gas / (r * r)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{ESO137_BuoyancyTerm}",
            description=self.description
        )


class ESO137_CompressedTerm:
    """
    ESO137-001 compressed integrand
    
    Source: MAIN_1_CoAnQi.cpp (lines 14968-14990)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "ESO137_Compressed"
        self.description = "ESO137-001 compressed integrand"
        self.metadata = {}
        self.metadata['version'] = '1.0'
        self.metadata['source'] = 'Source155.cpp'
        self.metadata['system'] = 'ESO137-001'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        M = 2e41
        r = 6.17e21
        G_const = 6.674e-11
        # Compressed gravity integrand
        result = 0.0  # Assign result
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{ESO137_CompressedTerm}",
            description=self.description
        )


class ESO137_ResonantTerm:
    """
    ESO137-001 DPM resonance
    
    Source: MAIN_1_CoAnQi.cpp (lines 14992-15013)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "ESO137_Resonant"
        self.description = "ESO137-001 DPM resonance"
        self.metadata = {}
        self.metadata['version'] = '1.0'
        self.metadata['source'] = 'Source155.cpp'
        self.metadata['system'] = 'ESO137-001'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        omega0 = 1e-15
        DPM = 0.93
        # DPM resonance
        result = 0.0  # Assign result
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{ESO137_ResonantTerm}",
            description=self.description
        )


class ESO137_SuperconductiveTerm:
    """
    ESO137-001 superconductive term
    
    Source: MAIN_1_CoAnQi.cpp (lines 15015-15036)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "ESO137_Superconductive"
        self.description = "ESO137-001 superconductive term"
        self.metadata = {}
        self.metadata['version'] = '1.0'
        self.metadata['source'] = 'Source155.cpp'
        self.metadata['system'] = 'ESO137-001'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        rho_vac_SCm = 7.09e-37
        omega_s = 2.5e-6
        # Superconductive vacuum term
        result = 0.0  # Assign result
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{ESO137_SuperconductiveTerm}",
            description=self.description
        )


class ESO137_LENRTerm:
    """
    ESO137-001 LENR resonance term
    
    Source: MAIN_1_CoAnQi.cpp (lines 15038-15059)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "ESO137_LENR"
        self.description = "ESO137-001 LENR resonance term"
        self.metadata = {}
        self.metadata['version'] = '1.0'
        self.metadata['source'] = 'Source155.cpp'
        self.metadata['system'] = 'ESO137-001'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        k_LENR = 1e-10
        omega_LENR = 2 * math.pi * 1.25e12
        # LENR resonance (dominant due to low omega0)
        result = 0.0  # Assign result
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{ESO137_LENRTerm}",
            description=self.description
        )


class ESO137_CompressedGTerm:
    """
    ESO137-001 compressed g(r,t)
    
    Source: MAIN_1_CoAnQi.cpp (lines 15061-15083)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "ESO137_CompressedG"
        self.description = "ESO137-001 compressed g(r,t)"
        self.metadata = {}
        self.metadata['version'] = '1.0'
        self.metadata['source'] = 'Source155.cpp'
        self.metadata['system'] = 'ESO137-001'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        M = 2e41
        r = 6.17e21
        G_const = 6.674e-11
        # Compressed g(r,t)
        result = 0.0  # Assign result
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{ESO137_CompressedGTerm}",
            description=self.description
        )


class ESO137_Q_waveTerm:
    """
    ESO137-001 quantum wave
    
    Source: MAIN_1_CoAnQi.cpp (lines 15085-15107)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "ESO137_Q_wave"
        self.description = "ESO137-001 quantum wave"
        self.metadata = {}
        self.metadata['version'] = '1.0'
        self.metadata['source'] = 'Source155.cpp'
        self.metadata['system'] = 'ESO137-001'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        hbar = 1.0546e-34
        c = 3e8
        lambda_ = 1.0
        # Quantum wave contribution
        result = 0.0  # Assign result
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{ESO137_Q_waveTerm}",
            description=self.description
        )


class ESO137_Ub1Term:
    """
    ESO137-001 Ub1 component
    
    Source: MAIN_1_CoAnQi.cpp (lines 15109-15130)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "ESO137_Ub1"
        self.description = "ESO137-001 Ub1 component"
        self.metadata = {}
        self.metadata['version'] = '1.0'
        self.metadata['source'] = 'Source155.cpp'
        self.metadata['system'] = 'ESO137-001'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        beta_i = 0.6
        # Buoyancy component Ub1
        return beta_i * Omega_g
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{ESO137_Ub1Term}",
            description=self.description
        )


class ESO137_UiTerm:
    """
    ESO137-001 Ui unified field
    
    Source: MAIN_1_CoAnQi.cpp (lines 15132-15153)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "ESO137_Ui"
        self.description = "ESO137-001 Ui unified field"
        self.metadata = {}
        self.metadata['version'] = '1.0'
        self.metadata['source'] = 'Source155.cpp'
        self.metadata['system'] = 'ESO137-001'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        lambda_i = 1.0
        rho_vac_UA = 7.09e-36
        # Unified field component Ui
        result = 0.0  # Assign result
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{ESO137_UiTerm}",
            description=self.description
        )


class NGC1365_BuoyancyTerm:
    """
    NGC1365 barred spiral buoyancy
    
    Source: MAIN_1_CoAnQi.cpp (lines 15156-15180)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "NGC1365_Buoyancy"
        self.description = "NGC1365 barred spiral buoyancy"
        self.metadata = {}
        self.metadata['version'] = '1.0'
        self.metadata['source'] = 'Source155.cpp'
        self.metadata['system'] = 'NGC1365'
        self.metadata['M'] = '7.17e41 kg'
        self.metadata['r'] = '9.46e20 m'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        M = 7.17e41
        r = 9.46e20
        B0 = 1e-9
        rho_gas = 1e-23
        return M * B0 * rho_gas / (r * r)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{NGC1365_BuoyancyTerm}",
            description=self.description
        )


class NGC1365_CompressedTerm:
    """
    NGC1365 compressed integrand
    
    Source: MAIN_1_CoAnQi.cpp (lines 15182-15203)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "NGC1365_Compressed"
        self.description = "NGC1365 compressed integrand"
        self.metadata = {}
        self.metadata['version'] = '1.0'
        self.metadata['source'] = 'Source155.cpp'
        self.metadata['system'] = 'NGC1365'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        M = 7.17e41
        r = 9.46e20
        G_const = 6.674e-11
        result = 0.0  # Assign result
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{NGC1365_CompressedTerm}",
            description=self.description
        )


class NGC1365_LENRTerm:
    """
    NGC1365 LENR resonance
    
    Source: MAIN_1_CoAnQi.cpp (lines 15205-15225)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "NGC1365_LENR"
        self.description = "NGC1365 LENR resonance"
        self.metadata = {}
        self.metadata['version'] = '1.0'
        self.metadata['source'] = 'Source155.cpp'
        self.metadata['system'] = 'NGC1365'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        k_LENR = 1e-10
        omega_LENR = 2 * math.pi * 1.25e12
        result = 0.0  # Assign result
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{NGC1365_LENRTerm}",
            description=self.description
        )


class Vela_BuoyancyTerm:
    """
    Vela Pulsar buoyancy force
    
    Source: MAIN_1_CoAnQi.cpp (lines 15228-15252)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "Vela_Buoyancy"
        self.description = "Vela Pulsar buoyancy force"
        self.metadata = {}
        self.metadata['version'] = '1.0'
        self.metadata['source'] = 'Source155.cpp'
        self.metadata['system'] = 'Vela Pulsar'
        self.metadata['M'] = '2.8e30 kg'
        self.metadata['r'] = '1.7e17 m'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        M = 2.8e30
        r = 1.7e17
        B0 = 3e-8
        rho_gas = 1e-23
        return M * B0 * rho_gas / (r * r)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{Vela_BuoyancyTerm}",
            description=self.description
        )


class Vela_CompressedTerm:
    """
    Vela compressed integrand
    
    Source: MAIN_1_CoAnQi.cpp (lines 15254-15275)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "Vela_Compressed"
        self.description = "Vela compressed integrand"
        self.metadata = {}
        self.metadata['version'] = '1.0'
        self.metadata['source'] = 'Source155.cpp'
        self.metadata['system'] = 'Vela Pulsar'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        M = 2.8e30
        r = 1.7e17
        G_const = 6.674e-11
        result = 0.0  # Assign result
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{Vela_CompressedTerm}",
            description=self.description
        )


class Vela_LENRTerm:
    """
    Vela LENR resonance
    
    Source: MAIN_1_CoAnQi.cpp (lines 15277-15297)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "Vela_LENR"
        self.description = "Vela LENR resonance"
        self.metadata = {}
        self.metadata['version'] = '1.0'
        self.metadata['source'] = 'Source155.cpp'
        self.metadata['system'] = 'Vela Pulsar'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        k_LENR = 1e-10
        omega_LENR = 2 * math.pi * 1.25e12
        result = 0.0  # Assign result
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{Vela_LENRTerm}",
            description=self.description
        )


class ASASSN14li_BuoyancyTerm:
    """
    ASASSN-14li TDE buoyancy
    
    Source: MAIN_1_CoAnQi.cpp (lines 15300-15324)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "ASASSN14li_Buoyancy"
        self.description = "ASASSN-14li TDE buoyancy"
        self.metadata = {}
        self.metadata['version'] = '1.0'
        self.metadata['source'] = 'Source155.cpp'
        self.metadata['system'] = 'ASASSN-14li'
        self.metadata['M'] = '1.989e37 kg'
        self.metadata['r'] = '3.09e18 m'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        M = 1.989e37
        r = 3.09e18
        B0 = 1e-9
        rho_gas = 1e-20
        return M * B0 * rho_gas / (r * r)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{ASASSN14li_BuoyancyTerm}",
            description=self.description
        )


class ASASSN14li_CompressedTerm:
    """
    ASASSN-14li compressed integrand
    
    Source: MAIN_1_CoAnQi.cpp (lines 15326-15347)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "ASASSN14li_Compressed"
        self.description = "ASASSN-14li compressed integrand"
        self.metadata = {}
        self.metadata['version'] = '1.0'
        self.metadata['source'] = 'Source155.cpp'
        self.metadata['system'] = 'ASASSN-14li'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        M = 1.989e37
        r = 3.09e18
        G_const = 6.674e-11
        result = 0.0  # Assign result
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{ASASSN14li_CompressedTerm}",
            description=self.description
        )


class ASASSN14li_LENRTerm:
    """
    ASASSN-14li LENR resonance
    
    Source: MAIN_1_CoAnQi.cpp (lines 15349-15369)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "ASASSN14li_LENR"
        self.description = "ASASSN-14li LENR resonance"
        self.metadata = {}
        self.metadata['version'] = '1.0'
        self.metadata['source'] = 'Source155.cpp'
        self.metadata['system'] = 'ASASSN-14li'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        k_LENR = 1e-10
        omega_LENR = 2 * math.pi * 1.25e12
        result = 0.0  # Assign result
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{ASASSN14li_LENRTerm}",
            description=self.description
        )


class ElGordo_BuoyancyTerm:
    """
    El Gordo cluster buoyancy
    
    Source: MAIN_1_CoAnQi.cpp (lines 15372-15396)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "ElGordo_Buoyancy"
        self.description = "El Gordo cluster buoyancy"
        self.metadata = {}
        self.metadata['version'] = '1.0'
        self.metadata['source'] = 'Source155.cpp'
        self.metadata['system'] = 'El Gordo'
        self.metadata['M'] = '4.97e45 kg'
        self.metadata['r'] = '3.09e22 m'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        M = 4.97e45
        r = 3.09e22
        B0 = 1e-10
        rho_gas = 1e-24
        return M * B0 * rho_gas / (r * r)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{ElGordo_BuoyancyTerm}",
            description=self.description
        )


class ElGordo_CompressedTerm:
    """
    El Gordo compressed integrand
    
    Source: MAIN_1_CoAnQi.cpp (lines 15398-15419)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "ElGordo_Compressed"
        self.description = "El Gordo compressed integrand"
        self.metadata = {}
        self.metadata['version'] = '1.0'
        self.metadata['source'] = 'Source155.cpp'
        self.metadata['system'] = 'El Gordo'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        M = 4.97e45
        r = 3.09e22
        G_const = 6.674e-11
        result = 0.0  # Assign result
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{ElGordo_CompressedTerm}",
            description=self.description
        )


class ElGordo_LENRTerm:
    """
    El Gordo LENR resonance
    
    Source: MAIN_1_CoAnQi.cpp (lines 15421-15441)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "ElGordo_LENR"
        self.description = "El Gordo LENR resonance"
        self.metadata = {}
        self.metadata['version'] = '1.0'
        self.metadata['source'] = 'Source155.cpp'
        self.metadata['system'] = 'El Gordo'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        k_LENR = 1e-10
        omega_LENR = 2 * math.pi * 1.25e12
        result = 0.0  # Assign result
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{ElGordo_LENRTerm}",
            description=self.description
        )


class Heaviside_FactorTerm:
    """
    Heaviside step function
    
    Source: MAIN_1_CoAnQi.cpp (lines 15450-15470)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "Heaviside_Factor"
        self.description = "Heaviside step function"
        self.metadata = {}
        self.metadata['version'] = '1.0'
        self.metadata['source'] = 'source100.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        # Step function: 1 if r > Rb, else 0
        return (r > Rb) ? 1.0 : 0.0
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{Heaviside_FactorTerm}",
            description=self.description
        )


class Heaviside_UmBaseTerm:
    """
    Um base calculation
    
    Source: MAIN_1_CoAnQi.cpp (lines 15472-15493)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "Heaviside_UmBase"
        self.description = "Um base calculation"
        self.metadata = {}
        self.metadata['version'] = '1.0'
        self.metadata['source'] = 'source100.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        if rj == 0.0: return 0.0
        return mu_j / rj
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{Heaviside_UmBaseTerm}",
            description=self.description
        )


class Heliosphere_H_SCmTerm:
    """
    Heliosphere thickness H_SCm
    
    Source: MAIN_1_CoAnQi.cpp (lines 15498-15523)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "Heliosphere_H_SCm"
        self.description = "Heliosphere thickness H_SCm"
        self.metadata = {}
        self.metadata['version'] = '1.0'
        self.metadata['source'] = 'source101.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        if rho_sw * v_sw * v_sw == 0.0: return 0.0
        # Heliosphere thickness
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{Heliosphere_H_SCmTerm}",
            description=self.description
        )


class UgIndex_U_giTerm:
    """
    Indexed U_gi component
    
    Source: MAIN_1_CoAnQi.cpp (lines 15528-15548)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "UgIndex_U_gi"
        self.description = "Indexed U_gi component"
        self.metadata = {}
        self.metadata['version'] = '1.0'
        self.metadata['source'] = 'source102.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        i = (int)self.get_param("index_i", 1)
        # Indexed gravity component
        result = 0.0  # Assign result
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{UgIndex_U_giTerm}",
            description=self.description
        )


class UgIndex_K_iTerm:
    """
    Coupling constant K_i
    
    Source: MAIN_1_CoAnQi.cpp (lines 15550-15570)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "UgIndex_K_i"
        self.description = "Coupling constant K_i"
        self.metadata = {}
        self.metadata['version'] = '1.0'
        self.metadata['source'] = 'source102.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        i = (int)self.get_param("index_i", 1)
        # Coupling constant K_i
        return k_base * (1.0 + 0.1 * i)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{UgIndex_K_iTerm}",
            description=self.description
        )


class Inertia_Lambda_iTerm:
    """
    Inertia coupling λ_i
    
    Source: MAIN_1_CoAnQi.cpp (lines 15575-15595)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "Inertia_Lambda_i"
        self.description = "Inertia coupling λ_i"
        self.metadata = {}
        self.metadata['version'] = '1.0'
        self.metadata['source'] = 'Source103.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        i = (int)self.get_param("index_i", 1)
        # Inertia coupling lambda_i
        result = 0.0  # Assign result
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{Inertia_Lambda_iTerm}",
            description=self.description
        )


class Inertia_U_iTerm:
    """
    Inertial field U_i
    
    Source: MAIN_1_CoAnQi.cpp (lines 15597-15617)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "Inertia_U_i"
        self.description = "Inertial field U_i"
        self.metadata = {}
        self.metadata['version'] = '1.0'
        self.metadata['source'] = 'Source103.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        # Inertial field U_i
        return rho_vac_UA * V_infl
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{Inertia_U_iTerm}",
            description=self.description
        )


class MagMoment_Mu_jTerm:
    """
    Magnetic moment μ_j
    
    Source: MAIN_1_CoAnQi.cpp (lines 15622-15642)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "MagMoment_Mu_j"
        self.description = "Magnetic moment μ_j"
        self.metadata = {}
        self.metadata['version'] = '1.0'
        self.metadata['source'] = 'source104.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        # Magnetic moment ?_j = B_j * R_s^3
        result = 0.0  # Assign result
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{MagMoment_Mu_jTerm}",
            description=self.description
        )


class MagMoment_B_jTerm:
    """
    Time-varying B_j field
    
    Source: MAIN_1_CoAnQi.cpp (lines 15644-15665)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "MagMoment_B_j"
        self.description = "Time-varying B_j field"
        self.metadata = {}
        self.metadata['version'] = '1.0'
        self.metadata['source'] = 'source104.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        B_base = 1e-3
        SCm_contrib = 1e3
        # Time-varying magnetic field
        result = 0.0  # Assign result
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{MagMoment_B_jTerm}",
            description=self.description
        )


class BlackHole_M_bhTerm:
    """
    Black hole mass
    
    Source: MAIN_1_CoAnQi.cpp (lines 15670-15690)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "BlackHole_M_bh"
        self.description = "Black hole mass"
        self.metadata = {}
        self.metadata['version'] = '1.0'
        self.metadata['source'] = 'source105.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        M_sun = 1.989e30
        # Black hole mass in kg
        return Mbh_solar * M_sun
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{BlackHole_M_bhTerm}",
            description=self.description
        )


class BlackHole_U_b1Term:
    """
    Black hole U_b1 term
    
    Source: MAIN_1_CoAnQi.cpp (lines 15692-15717)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "BlackHole_U_b1"
        self.description = "Black hole U_b1 term"
        self.metadata = {}
        self.metadata['version'] = '1.0'
        self.metadata['source'] = 'source105.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        if dg == 0.0: return 0.0
        # Black hole buoyancy contribution
        return beta_i * Omega_g * Mbh / dg
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{BlackHole_U_b1Term}",
            description=self.description
        )


class NegativeTime_CosPiTnTerm:
    """
    cos(π t_n) negative time
    
    Source: MAIN_1_CoAnQi.cpp (lines 15722-15741)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "NegativeTime_CosPiTn"
        self.description = "cos(π t_n) negative time"
        self.metadata = {}
        self.metadata['version'] = '1.0'
        self.metadata['source'] = 'source106.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        # Negative time cosine factor
        result = 0.0  # Assign result
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{NegativeTime_CosPiTnTerm}",
            description=self.description
        )


class NegativeTime_ExpTermTerm:
    """
    exp(-γt cos(πt_n))
    
    Source: MAIN_1_CoAnQi.cpp (lines 15743-15763)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "NegativeTime_ExpTerm"
        self.description = "exp(-γt cos(πt_n))"
        self.metadata = {}
        self.metadata['version'] = '1.0'
        self.metadata['source'] = 'source106.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        # Exponential decay with negative time modulation
        result = 0.0  # Assign result
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{NegativeTime_ExpTermTerm}",
            description=self.description
        )


class PiConstant_MuJExampleTerm:
    """
    μ_j with π modulation
    
    Source: MAIN_1_CoAnQi.cpp (lines 15768-15789)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "PiConstant_MuJExample"
        self.description = "μ_j with π modulation"
        self.metadata = {}
        self.metadata['version'] = '1.0'
        self.metadata['source'] = 'source107.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        # Magnetic moment with ? constant
        result = 0.0  # Assign result
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{PiConstant_MuJExampleTerm}",
            description=self.description
        )


class CorePenetration_P_coreTerm:
    """
    Core penetration probability
    
    Source: MAIN_1_CoAnQi.cpp (lines 15794-15813)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "CorePenetration_P_core"
        self.description = "Core penetration probability"
        self.metadata = {}
        self.metadata['version'] = '1.0'
        self.metadata['source'] = 'source108.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        # Core penetration probability
        result = 0.0  # Assign result
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{CorePenetration_P_coreTerm}",
            description=self.description
        )


class QuasiLong_FactorTerm:
    """
    Quasi-longitudinal geometric factor
    
    Source: MAIN_1_CoAnQi.cpp (lines 15818-15838)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "QuasiLong_Factor"
        self.description = "Quasi-longitudinal geometric factor"
        self.metadata = {}
        self.metadata['version'] = '1.0'
        self.metadata['source'] = 'source109.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        # Quasi-longitudinal factor
        result = 0.0  # Assign result
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{QuasiLong_FactorTerm}",
            description=self.description
        )


class OuterBubble_S_r_RbTerm:
    """
    Outer field bubble step function S(r-R_b)
    
    Source: MAIN_1_CoAnQi.cpp (lines 15846-15867)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "OuterBubble_S_r_Rb"
        self.description = "Outer field bubble step function S(r-R_b)"
        self.metadata = {}
        self.metadata['version'] = '1.0'
        self.metadata['source'] = 'source110.cpp'
        self.metadata['equation'] = 'S(r - R_b) step function'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        # Step function: S = 1 if r >= R_b, else 0
        return (r >= R_b) ? 1.0 : 0.0
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{OuterBubble_S_r_RbTerm}",
            description=self.description
        )


class OuterBubble_U_g2Term:
    """
    U_g2 with outer field bubble step function
    
    Source: MAIN_1_CoAnQi.cpp (lines 15869-15893)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "OuterBubble_U_g2"
        self.description = "U_g2 with outer field bubble step function"
        self.metadata = {}
        self.metadata['version'] = '1.0'
        self.metadata['source'] = 'source110.cpp'
        self.metadata['R_b'] = '1.496e13 m (100 AU)'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        # U_g2 with step function
        return k2 * S * M_s / (r * r)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{OuterBubble_U_g2Term}",
            description=self.description
        )


class OuterBubble_R_bTerm:
    """
    Radius of outer field bubble (100 AU)
    
    Source: MAIN_1_CoAnQi.cpp (lines 15895-15913)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "OuterBubble_R_b"
        self.description = "Radius of outer field bubble (100 AU)"
        self.metadata = {}
        self.metadata['version'] = '1.0'
        self.metadata['source'] = 'source110.cpp'
        self.metadata['value'] = '1.496e13 m'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        # R_b = 100 AU in meters
        return 1.496e13
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{OuterBubble_R_bTerm}",
            description=self.description
        )


class OuterBubble_R_bInAUTerm:
    """
    Outer field bubble radius in AU
    
    Source: MAIN_1_CoAnQi.cpp (lines 15915-15932)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "OuterBubble_R_bInAU"
        self.description = "Outer field bubble radius in AU"
        self.metadata = {}
        self.metadata['version'] = '1.0'
        self.metadata['source'] = 'source110.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        # R_b in AU
        return 100.0
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{OuterBubble_R_bInAUTerm}",
            description=self.description
        )


class Reciprocation_Gamma_sTerm:
    """
    Reciprocation decay rate γ in s⁻¹
    
    Source: MAIN_1_CoAnQi.cpp (lines 15937-15958)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "Reciprocation_Gamma_s"
        self.description = "Reciprocation decay rate γ in s⁻¹"
        self.metadata = {}
        self.metadata['version'] = '1.0'
        self.metadata['source'] = 'source111.cpp'
        self.metadata['gamma_day'] = '0.00005 day⁻¹'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        day_to_s = 86400.0
        # ? in s??
        return gamma_day / day_to_s
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{Reciprocation_Gamma_sTerm}",
            description=self.description
        )


class Reciprocation_CosPiTnTerm:
    """
    Negative time oscillation cos(π t_n)
    
    Source: MAIN_1_CoAnQi.cpp (lines 15960-15979)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "Reciprocation_CosPiTn"
        self.description = "Negative time oscillation cos(π t_n)"
        self.metadata = {}
        self.metadata['version'] = '1.0'
        self.metadata['source'] = 'source111.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        # cos(? t_n)
        result = 0.0  # Assign result
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{Reciprocation_CosPiTnTerm}",
            description=self.description
        )


class Reciprocation_ExpTermTerm:
    """
    Reciprocation exponential decay term
    
    Source: MAIN_1_CoAnQi.cpp (lines 15981-16002)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "Reciprocation_ExpTerm"
        self.description = "Reciprocation exponential decay term"
        self.metadata = {}
        self.metadata['version'] = '1.0'
        self.metadata['source'] = 'source111.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        # exp(-? t cos(? t_n))
        result = 0.0  # Assign result
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{Reciprocation_ExpTermTerm}",
            description=self.description
        )


class Reciprocation_OneMinusExpTerm:
    """
    U_m reciprocation factor 1-exp(-γt cos(πt_n))
    
    Source: MAIN_1_CoAnQi.cpp (lines 16004-16025)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "Reciprocation_OneMinusExp"
        self.description = "U_m reciprocation factor 1-exp(-γt cos(πt_n))"
        self.metadata = {}
        self.metadata['version'] = '1.0'
        self.metadata['source'] = 'source111.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        # 1 - exp(-? t cos(? t_n))
        result = 0.0  # Assign result
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{Reciprocation_OneMinusExpTerm}",
            description=self.description
        )


class Reciprocation_Gamma_dayTerm:
    """
    Reciprocation decay rate in day⁻¹
    
    Source: MAIN_1_CoAnQi.cpp (lines 16027-16045)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "Reciprocation_Gamma_day"
        self.description = "Reciprocation decay rate in day⁻¹"
        self.metadata = {}
        self.metadata['version'] = '1.0'
        self.metadata['source'] = 'source111.cpp'
        self.metadata['value'] = '0.00005 day⁻¹'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        # ? in day??
        return 0.00005
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{Reciprocation_Gamma_dayTerm}",
            description=self.description
        )


class Reciprocation_UmExampleTerm:
    """
    U_m with reciprocation decay
    
    Source: MAIN_1_CoAnQi.cpp (lines 16047-16070)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "Reciprocation_UmExample"
        self.description = "U_m with reciprocation decay"
        self.metadata = {}
        self.metadata['version'] = '1.0'
        self.metadata['source'] = 'source111.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        # U_m contribution: (?_j/r_j) * [1-exp(-?t cos(?t_n))]
        return mu_over_rj * factor
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{Reciprocation_UmExampleTerm}",
            description=self.description
        )


class ScmPenetration_UmBaseTerm:
    """
    Base U_m term before SCm penetration
    
    Source: MAIN_1_CoAnQi.cpp (lines 16075-16094)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "ScmPenetration_UmBase"
        self.description = "Base U_m term before SCm penetration"
        self.metadata = {}
        self.metadata['version'] = '1.0'
        self.metadata['source'] = 'source112.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        # Base U_m without P_SCm
        return mu_over_rj
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{ScmPenetration_UmBaseTerm}",
            description=self.description
        )


class ScmPenetration_UmContributionTerm:
    """
    U_m with [SCm] penetration factor
    
    Source: MAIN_1_CoAnQi.cpp (lines 16096-16116)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "ScmPenetration_UmContribution"
        self.description = "U_m with [SCm] penetration factor"
        self.metadata = {}
        self.metadata['version'] = '1.0'
        self.metadata['source'] = 'source112.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        # U_m with P_SCm penetration
        return mu_over_rj * P_SCm
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{ScmPenetration_UmContributionTerm}",
            description=self.description
        )


class ScmPenetration_P_SCmTerm:
    """
    [SCm] superconducting penetration factor
    
    Source: MAIN_1_CoAnQi.cpp (lines 16118-16137)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "ScmPenetration_P_SCm"
        self.description = "[SCm] superconducting penetration factor"
        self.metadata = {}
        self.metadata['version'] = '1.0'
        self.metadata['source'] = 'source112.cpp'
        self.metadata['sun'] = '~1'
        self.metadata['planet'] = '~1e-3'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        # P_SCm for Sun (plasma core has full penetration)
        return 1.0
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{ScmPenetration_P_SCmTerm}",
            description=self.description
        )


class ScmPenetration_UmPlanetTerm:
    """
    U_m for planets with reduced P_SCm
    
    Source: MAIN_1_CoAnQi.cpp (lines 16139-16160)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "ScmPenetration_UmPlanet"
        self.description = "U_m for planets with reduced P_SCm"
        self.metadata = {}
        self.metadata['version'] = '1.0'
        self.metadata['source'] = 'source112.cpp'
        self.metadata['P_SCm_planet'] = '1e-3'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        # U_m for planet with reduced penetration
        return mu_over_rj * P_SCm_planet
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{ScmPenetration_UmPlanetTerm}",
            description=self.description
        )


class ScmReactivity_Kappa_sTerm:
    """
    [SCm] reactivity decay rate κ in s⁻¹
    
    Source: MAIN_1_CoAnQi.cpp (lines 16165-16186)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "ScmReactivity_Kappa_s"
        self.description = "[SCm] reactivity decay rate κ in s⁻¹"
        self.metadata = {}
        self.metadata['version'] = '1.0'
        self.metadata['source'] = 'source113.cpp'
        self.metadata['kappa_day'] = '0.0005 day⁻¹'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        day_to_s = 86400.0
        # ? in s??
        return kappa_day / day_to_s
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{ScmReactivity_Kappa_sTerm}",
            description=self.description
        )


class ScmReactivity_E_reactTerm:
    """
    [SCm] reactivity energy E_react with decay
    
    Source: MAIN_1_CoAnQi.cpp (lines 16188-16210)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "ScmReactivity_E_react"
        self.description = "[SCm] reactivity energy E_react with decay"
        self.metadata = {}
        self.metadata['version'] = '1.0'
        self.metadata['source'] = 'source113.cpp'
        self.metadata['E_react_0'] = '1e46 J'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        # E_react = 10^46 * exp(-? t)
        result = 0.0  # Assign result
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{ScmReactivity_E_reactTerm}",
            description=self.description
        )


class ScmReactivity_UmExampleTerm:
    """
    U_m with [SCm] reactivity decay
    
    Source: MAIN_1_CoAnQi.cpp (lines 16212-16234)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "ScmReactivity_UmExample"
        self.description = "U_m with [SCm] reactivity decay"
        self.metadata = {}
        self.metadata['version'] = '1.0'
        self.metadata['source'] = 'source113.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        # U_m with E_react decay
        return E_react * 1e-10
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{ScmReactivity_UmExampleTerm}",
            description=self.description
        )


class ScmReactivity_Kappa_dayTerm:
    """
    [SCm] reactivity decay rate in day⁻¹
    
    Source: MAIN_1_CoAnQi.cpp (lines 16236-16254)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "ScmReactivity_Kappa_day"
        self.description = "[SCm] reactivity decay rate in day⁻¹"
        self.metadata = {}
        self.metadata['version'] = '1.0'
        self.metadata['source'] = 'source113.cpp'
        self.metadata['value'] = '0.0005 day⁻¹'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        # ? in day??
        return 0.0005
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{ScmReactivity_Kappa_dayTerm}",
            description=self.description
        )


class SolarCycle_Omega_cTerm:
    """
    Solar cycle frequency ω_c
    
    Source: MAIN_1_CoAnQi.cpp (lines 16259-16279)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "SolarCycle_Omega_c"
        self.description = "Solar cycle frequency ω_c"
        self.metadata = {}
        self.metadata['version'] = '1.0'
        self.metadata['source'] = 'source114.cpp'
        self.metadata['period'] = '3.96e8 s (~12.55 years)'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        # ?_c = 2? / period
        return 2.0 * math.pi / period
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{SolarCycle_Omega_cTerm}",
            description=self.description
        )


class SolarCycle_SinOmegaCTTerm:
    """
    Solar cycle oscillation sin(ω_c t)
    
    Source: MAIN_1_CoAnQi.cpp (lines 16281-16301)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "SolarCycle_SinOmegaCT"
        self.description = "Solar cycle oscillation sin(ω_c t)"
        self.metadata = {}
        self.metadata['version'] = '1.0'
        self.metadata['source'] = 'source114.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        omega_c = 2.0 * math.pi / period
        # sin(?_c t)
        result = 0.0  # Assign result
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{SolarCycle_SinOmegaCTTerm}",
            description=self.description
        )


class SolarCycle_MuJExampleTerm:
    """
    Magnetic moment μ_j with solar cycle modulation
    
    Source: MAIN_1_CoAnQi.cpp (lines 16303-16326)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "SolarCycle_MuJExample"
        self.description = "Magnetic moment μ_j with solar cycle modulation"
        self.metadata = {}
        self.metadata['version'] = '1.0'
        self.metadata['source'] = 'source114.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        omega_c = 2.0 * math.pi / period
        # ?_j = (B_j_base + 0.4 sin(?_c t)) * R_s?
        result = 0.0  # Assign result
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{SolarCycle_MuJExampleTerm}",
            description=self.description
        )


class SolarCycle_PeriodYearsTerm:
    """
    Solar cycle period in years
    
    Source: MAIN_1_CoAnQi.cpp (lines 16328-16349)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "SolarCycle_PeriodYears"
        self.description = "Solar cycle period in years"
        self.metadata = {}
        self.metadata['version'] = '1.0'
        self.metadata['source'] = 'source114.cpp'
        self.metadata['value'] = '12.55 years'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        year_to_s = 3.154e7
        # Period in years
        return period_s / year_to_s
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{SolarCycle_PeriodYearsTerm}",
            description=self.description
        )


class SolarCycle_B_j_ModulatedTerm:
    """
    Surface magnetic field B_j with solar cycle
    
    Source: MAIN_1_CoAnQi.cpp (lines 16351-16373)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "SolarCycle_B_j_Modulated"
        self.description = "Surface magnetic field B_j with solar cycle"
        self.metadata = {}
        self.metadata['version'] = '1.0'
        self.metadata['source'] = 'source114.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        omega_c = 2.0 * math.pi / period
        # B_j = B_j_base + amplitude * sin(?_c t)
        result = 0.0  # Assign result
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{SolarCycle_B_j_ModulatedTerm}",
            description=self.description
        )


class SolarWindMod_ModulationFactorTerm:
    """
    Solar wind modulation (1 + δ_sw v_sw)
    
    Source: MAIN_1_CoAnQi.cpp (lines 16378-16398)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "SolarWindMod_ModulationFactor"
        self.description = "Solar wind modulation (1 + δ_sw v_sw)"
        self.metadata = {}
        self.metadata['version'] = '1.0'
        self.metadata['source'] = 'Source115.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        # Modulation factor: (1 + ?_sw v_sw)
        return 1.0 + delta_sw * v_sw
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{SolarWindMod_ModulationFactorTerm}",
            description=self.description
        )


class SolarWindMod_U_g2Term:
    """
    U_g2 with solar wind modulation
    
    Source: MAIN_1_CoAnQi.cpp (lines 16400-16423)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "SolarWindMod_U_g2"
        self.description = "U_g2 with solar wind modulation"
        self.metadata = {}
        self.metadata['version'] = '1.0'
        self.metadata['source'] = 'Source115.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        # U_g2 with solar wind modulation
        modulation = 1.0 + delta_sw * v_sw
        return modulation * M_s / (r * r)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{SolarWindMod_U_g2Term}",
            description=self.description
        )


class SolarWindMod_Delta_swTerm:
    """
    Solar wind modulation parameter δ_sw
    
    Source: MAIN_1_CoAnQi.cpp (lines 16425-16443)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "SolarWindMod_Delta_sw"
        self.description = "Solar wind modulation parameter δ_sw"
        self.metadata = {}
        self.metadata['version'] = '1.0'
        self.metadata['source'] = 'Source115.cpp'
        self.metadata['value'] = '0.01'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        # ?_sw unitless modulation parameter
        return 0.01
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{SolarWindMod_Delta_swTerm}",
            description=self.description
        )


class SolarWindMod_U_g2_no_modTerm:
    """
    U_g2 without solar wind modulation
    
    Source: MAIN_1_CoAnQi.cpp (lines 16445-16465)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "SolarWindMod_U_g2_no_mod"
        self.description = "U_g2 without solar wind modulation"
        self.metadata = {}
        self.metadata['version'] = '1.0'
        self.metadata['source'] = 'Source115.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        # U_g2 without modulation
        return M_s / (r * r)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{SolarWindMod_U_g2_no_modTerm}",
            description=self.description
        )


class SolarWindVel_ModulationFactorTerm:
    """
    Velocity-based modulation factor
    
    Source: MAIN_1_CoAnQi.cpp (lines 16470-16490)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "SolarWindVel_ModulationFactor"
        self.description = "Velocity-based modulation factor"
        self.metadata = {}
        self.metadata['version'] = '1.0'
        self.metadata['source'] = 'source116.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        # Modulation: (1 + ?_sw v_sw)
        return 1.0 + delta_sw * v_sw
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{SolarWindVel_ModulationFactorTerm}",
            description=self.description
        )


class SolarWindVel_U_g2Term:
    """
    U_g2 with velocity modulation
    
    Source: MAIN_1_CoAnQi.cpp (lines 16492-16514)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "SolarWindVel_U_g2"
        self.description = "U_g2 with velocity modulation"
        self.metadata = {}
        self.metadata['version'] = '1.0'
        self.metadata['source'] = 'source116.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        modulation = 1.0 + delta_sw * v_sw
        return modulation * M_s / (r * r)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{SolarWindVel_U_g2Term}",
            description=self.description
        )


class SolarWindVel_V_swTerm:
    """
    Solar wind velocity v_sw
    
    Source: MAIN_1_CoAnQi.cpp (lines 16516-16534)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "SolarWindVel_V_sw"
        self.description = "Solar wind velocity v_sw"
        self.metadata = {}
        self.metadata['version'] = '1.0'
        self.metadata['source'] = 'source116.cpp'
        self.metadata['value'] = '5e5 m/s'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        # v_sw = 500 km/s in m/s
        return 5e5
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{SolarWindVel_V_swTerm}",
            description=self.description
        )


class SolarWindVel_V_swKmSTerm:
    """
    Solar wind velocity in km/s
    
    Source: MAIN_1_CoAnQi.cpp (lines 16536-16554)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "SolarWindVel_V_swKmS"
        self.description = "Solar wind velocity in km/s"
        self.metadata = {}
        self.metadata['version'] = '1.0'
        self.metadata['source'] = 'source116.cpp'
        self.metadata['value'] = '500 km/s'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        # v_sw in km/s
        return 500.0
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{SolarWindVel_V_swKmSTerm}",
            description=self.description
        )


class SolarWindVel_U_g2_no_swTerm:
    """
    U_g2 without solar wind velocity
    
    Source: MAIN_1_CoAnQi.cpp (lines 16556-16576)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "SolarWindVel_U_g2_no_sw"
        self.description = "U_g2 without solar wind velocity"
        self.metadata = {}
        self.metadata['version'] = '1.0'
        self.metadata['source'] = 'source116.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        # U_g2 with v_sw = 0
        return M_s / (r * r)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{SolarWindVel_U_g2_no_swTerm}",
            description=self.description
        )


class StellarMass_M_sOverR2Term:
    """
    Stellar mass per area M_s/r²
    
    Source: MAIN_1_CoAnQi.cpp (lines 16581-16601)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "StellarMass_M_sOverR2"
        self.description = "Stellar mass per area M_s/r²"
        self.metadata = {}
        self.metadata['version'] = '1.0'
        self.metadata['source'] = 'source117.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        # M_s / r?
        return M_s / (r * r)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{StellarMass_M_sOverR2Term}",
            description=self.description
        )


class StellarMass_U_g1Term:
    """
    U_g1 with stellar mass
    
    Source: MAIN_1_CoAnQi.cpp (lines 16603-16624)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "StellarMass_U_g1"
        self.description = "U_g1 with stellar mass"
        self.metadata = {}
        self.metadata['version'] = '1.0'
        self.metadata['source'] = 'source117.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        # U_g1 = k1 * M_s / r?
        return k1 * M_s / (r * r)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{StellarMass_U_g1Term}",
            description=self.description
        )


class StellarMass_U_g2Term:
    """
    U_g2 with stellar mass
    
    Source: MAIN_1_CoAnQi.cpp (lines 16626-16647)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "StellarMass_U_g2"
        self.description = "U_g2 with stellar mass"
        self.metadata = {}
        self.metadata['version'] = '1.0'
        self.metadata['source'] = 'source117.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        # U_g2 = k2 * M_s / r?
        return k2 * M_s / (r * r)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{StellarMass_U_g2Term}",
            description=self.description
        )


class StellarMass_M_sTerm:
    """
    Stellar mass M_s (1 M_☉)
    
    Source: MAIN_1_CoAnQi.cpp (lines 16649-16667)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "StellarMass_M_s"
        self.description = "Stellar mass M_s (1 M_☉)"
        self.metadata = {}
        self.metadata['version'] = '1.0'
        self.metadata['source'] = 'source117.cpp'
        self.metadata['value'] = '1.989e30 kg'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        # M_s = 1 solar mass
        return 1.989e30
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{StellarMass_M_sTerm}",
            description=self.description
        )


class StellarMass_M_sInMsunTerm:
    """
    Stellar mass in solar masses
    
    Source: MAIN_1_CoAnQi.cpp (lines 16669-16687)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "StellarMass_M_sInMsun"
        self.description = "Stellar mass in solar masses"
        self.metadata = {}
        self.metadata['version'] = '1.0'
        self.metadata['source'] = 'source117.cpp'
        self.metadata['value'] = '1 M_☉'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        # M_s in solar masses
        return 1.0
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{StellarMass_M_sInMsunTerm}",
            description=self.description
        )


class StellarRot_Omega_s_tTerm:
    """
    Time-dependent rotation rate ω_s(t)
    
    Source: MAIN_1_CoAnQi.cpp (lines 16692-16711)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "StellarRot_Omega_s_t"
        self.description = "Time-dependent rotation rate ω_s(t)"
        self.metadata = {}
        self.metadata['version'] = '1.0'
        self.metadata['source'] = 'source118.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        # ?_s(t) - simplified as constant
        return omega_s
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{StellarRot_Omega_s_tTerm}",
            description=self.description
        )


class StellarRot_U_g3Term:
    """
    U_g3 with rotation modulation
    
    Source: MAIN_1_CoAnQi.cpp (lines 16713-16735)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "StellarRot_U_g3"
        self.description = "U_g3 with rotation modulation"
        self.metadata = {}
        self.metadata['version'] = '1.0'
        self.metadata['source'] = 'source118.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        # U_g3 = B_j * cos(?_s t) * P_core * E_react
        result = 0.0  # Assign result
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{StellarRot_U_g3Term}",
            description=self.description
        )


class StellarRot_U_iTerm:
    """
    U_i with rotation and negative time
    
    Source: MAIN_1_CoAnQi.cpp (lines 16737-16758)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "StellarRot_U_i"
        self.description = "U_i with rotation and negative time"
        self.metadata = {}
        self.metadata['version'] = '1.0'
        self.metadata['source'] = 'source118.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        # U_i = ?_s * cos(? t_n) * ?_i
        result = 0.0  # Assign result
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{StellarRot_U_iTerm}",
            description=self.description
        )


class StellarRot_Omega_sTerm:
    """
    Stellar rotation rate ω_s
    
    Source: MAIN_1_CoAnQi.cpp (lines 16760-16778)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "StellarRot_Omega_s"
        self.description = "Stellar rotation rate ω_s"
        self.metadata = {}
        self.metadata['version'] = '1.0'
        self.metadata['source'] = 'source118.cpp'
        self.metadata['value'] = '2.5e-6 rad/s'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        # ?_s = 2? / (29 days)
        return 2.5e-6
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{StellarRot_Omega_sTerm}",
            description=self.description
        )


class StellarRot_Period_daysTerm:
    """
    Rotation period in days
    
    Source: MAIN_1_CoAnQi.cpp (lines 16780-16801)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "StellarRot_Period_days"
        self.description = "Rotation period in days"
        self.metadata = {}
        self.metadata['version'] = '1.0'
        self.metadata['source'] = 'source118.cpp'
        self.metadata['value'] = '~29 days'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        day_to_s = 86400.0
        # Period = 2? / ?_s in days
        return (2.0 * math.pi / omega_s) / day_to_s
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{StellarRot_Period_daysTerm}",
            description=self.description
        )


class StepFunc_S_r_RbTerm:
    """
    Step function for field activation
    
    Source: MAIN_1_CoAnQi.cpp (lines 16806-16826)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "StepFunc_S_r_Rb"
        self.description = "Step function for field activation"
        self.metadata = {}
        self.metadata['version'] = '1.0'
        self.metadata['source'] = 'Source119.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        # Step function S(r - R_b)
        return (r >= R_b) ? 1.0 : 0.0
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{StepFunc_S_r_RbTerm}",
            description=self.description
        )


class StepFunc_U_g2Term:
    """
    U_g2 activated by step function
    
    Source: MAIN_1_CoAnQi.cpp (lines 16828-16850)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "StepFunc_U_g2"
        self.description = "U_g2 activated by step function"
        self.metadata = {}
        self.metadata['version'] = '1.0'
        self.metadata['source'] = 'Source119.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        # U_g2 with step function
        return S * M_s / (r * r)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{StepFunc_U_g2Term}",
            description=self.description
        )


class StressEnergy_T_sTerm:
    """
    Stress-energy tensor scalar T_s
    
    Source: MAIN_1_CoAnQi.cpp (lines 16855-16875)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "StressEnergy_T_s"
        self.description = "Stress-energy tensor scalar T_s"
        self.metadata = {}
        self.metadata['version'] = '1.0'
        self.metadata['source'] = 'source120.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        # T_s = T_s_base + ?_vac_A
        return T_s_base + rho_vac_A
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{StressEnergy_T_sTerm}",
            description=self.description
        )


class StressEnergy_A_mu_nuTerm:
    """
    Perturbed metric A_μν
    
    Source: MAIN_1_CoAnQi.cpp (lines 16877-16899)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "StressEnergy_A_mu_nu"
        self.description = "Perturbed metric A_μν"
        self.metadata = {}
        self.metadata['version'] = '1.0'
        self.metadata['source'] = 'source120.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        # A_?? = g_?? + ? T_s (diagonal component)
        T_s = T_s_base + rho_vac_A
        return 1.0 + epsilon * T_s
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{StressEnergy_A_mu_nuTerm}",
            description=self.description
        )


class StressEnergy_PerturbationTerm:
    """
    Metric perturbation ε T_s
    
    Source: MAIN_1_CoAnQi.cpp (lines 16901-16923)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "StressEnergy_Perturbation"
        self.description = "Metric perturbation ε T_s"
        self.metadata = {}
        self.metadata['version'] = '1.0'
        self.metadata['source'] = 'source120.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        # Perturbation: ? * T_s
        T_s = T_s_base + rho_vac_A
        return epsilon * T_s
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{StressEnergy_PerturbationTerm}",
            description=self.description
        )


class SurfaceMagField_B_jTerm:
    """
    Magnetic field B_j scaled by surface field
    
    Source: MAIN_1_CoAnQi.cpp (lines 16928-16949)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "SurfaceMagField_B_j"
        self.description = "Magnetic field B_j scaled by surface field"
        self.metadata = {}
        self.metadata['version'] = '1.0'
        self.metadata['source'] = 'source121.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        # B_j scaled by surface field: B_j_base * (B_s / B_ref)
        return B_j_base * (B_s / B_ref)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{SurfaceMagField_B_jTerm}",
            description=self.description
        )


class SurfaceMagField_U_g3_exampleTerm:
    """
    U_g3 with surface field modulation
    
    Source: MAIN_1_CoAnQi.cpp (lines 16951-16976)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "SurfaceMagField_U_g3_example"
        self.description = "U_g3 with surface field modulation"
        self.metadata = {}
        self.metadata['version'] = '1.0'
        self.metadata['source'] = 'source121.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        # U_g3 with B_s scaling
        result = 0.0  # Assign result
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{SurfaceMagField_U_g3_exampleTerm}",
            description=self.description
        )


class SurfaceMagField_B_s_minTerm:
    """
    Minimum surface magnetic field B_s
    
    Source: MAIN_1_CoAnQi.cpp (lines 16978-16996)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "SurfaceMagField_B_s_min"
        self.description = "Minimum surface magnetic field B_s"
        self.metadata = {}
        self.metadata['version'] = '1.0'
        self.metadata['source'] = 'source121.cpp'
        self.metadata['value'] = '1e-4 T (quiet Sun)'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        # Minimum surface field (quiet Sun)
        return 1e-4
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{SurfaceMagField_B_s_minTerm}",
            description=self.description
        )


class SurfaceMagField_B_s_maxTerm:
    """
    Maximum surface magnetic field B_s
    
    Source: MAIN_1_CoAnQi.cpp (lines 16998-17016)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "SurfaceMagField_B_s_max"
        self.description = "Maximum surface magnetic field B_s"
        self.metadata = {}
        self.metadata['version'] = '1.0'
        self.metadata['source'] = 'source121.cpp'
        self.metadata['value'] = '0.4 T (sunspot)'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        # Maximum surface field (sunspot)
        return 0.4
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{SurfaceMagField_B_s_maxTerm}",
            description=self.description
        )


class SurfaceTemp_B_j_hypotheticalTerm:
    """
    Hypothetical B_j with temperature scaling
    
    Source: MAIN_1_CoAnQi.cpp (lines 17021-17042)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "SurfaceTemp_B_j_hypothetical"
        self.description = "Hypothetical B_j with temperature scaling"
        self.metadata = {}
        self.metadata['version'] = '1.0'
        self.metadata['source'] = 'Source122.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        # Hypothetical B_j scaled by temperature: B_j_base * (T_s / T_s_ref)
        return B_j_base * (T_s / T_s_ref)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{SurfaceTemp_B_j_hypotheticalTerm}",
            description=self.description
        )


class SurfaceTemp_U_g3_exampleTerm:
    """
    U_g3 with temperature scaling
    
    Source: MAIN_1_CoAnQi.cpp (lines 17044-17069)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "SurfaceTemp_U_g3_example"
        self.description = "U_g3 with temperature scaling"
        self.metadata = {}
        self.metadata['version'] = '1.0'
        self.metadata['source'] = 'Source122.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        # U_g3 with temperature scaling
        result = 0.0  # Assign result
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{SurfaceTemp_U_g3_exampleTerm}",
            description=self.description
        )


class SurfaceTemp_T_sTerm:
    """
    Surface temperature T_s
    
    Source: MAIN_1_CoAnQi.cpp (lines 17071-17089)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "SurfaceTemp_T_s"
        self.description = "Surface temperature T_s"
        self.metadata = {}
        self.metadata['version'] = '1.0'
        self.metadata['source'] = 'Source122.cpp'
        self.metadata['value'] = '5778 K (Sun)'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        # T_s = 5778 K (Sun effective temperature)
        return 5778.0
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{SurfaceTemp_T_sTerm}",
            description=self.description
        )


class TimeReversalZone_TRZFactorTerm:
    """
    Time-reversal zone factor f_TRZ
    
    Source: MAIN_1_CoAnQi.cpp (lines 17094-17112)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "TimeReversalZone_TRZFactor"
        self.description = "Time-reversal zone factor f_TRZ"
        self.metadata = {}
        self.metadata['version'] = '1.0'
        self.metadata['source'] = 'source123.cpp'
        self.metadata['value'] = '0.1'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        # f_TRZ = 0.1 (10% enhancement)
        return 0.1
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{TimeReversalZone_TRZFactorTerm}",
            description=self.description
        )


class TimeReversalZone_U_i_baseTerm:
    """
    Base U_i without TRZ enhancement
    
    Source: MAIN_1_CoAnQi.cpp (lines 17114-17135)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "TimeReversalZone_U_i_base"
        self.description = "Base U_i without TRZ enhancement"
        self.metadata = {}
        self.metadata['version'] = '1.0'
        self.metadata['source'] = 'source123.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        # U_i base without TRZ
        result = 0.0  # Assign result
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{TimeReversalZone_U_i_baseTerm}",
            description=self.description
        )


class TimeReversalZone_U_iTerm:
    """
    U_i with TRZ enhancement
    
    Source: MAIN_1_CoAnQi.cpp (lines 17137-17160)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "TimeReversalZone_U_i"
        self.description = "U_i with TRZ enhancement"
        self.metadata = {}
        self.metadata['version'] = '1.0'
        self.metadata['source'] = 'source123.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        # U_i with TRZ: (1 + f_TRZ) * base
        return (1.0 + f_TRZ) * base
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{TimeReversalZone_U_iTerm}",
            description=self.description
        )


class TimeReversalZone_F_TRZTerm:
    """
    TRZ enhancement factor (1 + f_TRZ)
    
    Source: MAIN_1_CoAnQi.cpp (lines 17162-17181)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "TimeReversalZone_F_TRZ"
        self.description = "TRZ enhancement factor (1 + f_TRZ)"
        self.metadata = {}
        self.metadata['version'] = '1.0'
        self.metadata['source'] = 'source123.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        # Enhancement factor: (1 + f_TRZ)
        return 1.0 + f_TRZ
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{TimeReversalZone_F_TRZTerm}",
            description=self.description
        )


class TimeReversalZone_U_i_no_TRZTerm:
    """
    U_i without TRZ
    
    Source: MAIN_1_CoAnQi.cpp (lines 17183-17204)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "TimeReversalZone_U_i_no_TRZ"
        self.description = "U_i without TRZ"
        self.metadata = {}
        self.metadata['version'] = '1.0'
        self.metadata['source'] = 'source123.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        # U_i without TRZ (f_TRZ = 0)
        result = 0.0  # Assign result
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{TimeReversalZone_U_i_no_TRZTerm}",
            description=self.description
        )


class Ug1Defect_Delta_defTerm:
    """
    Ug1 defect factor δ_def
    
    Source: MAIN_1_CoAnQi.cpp (lines 17209-17230)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "Ug1Defect_Delta_def"
        self.description = "Ug1 defect factor δ_def"
        self.metadata = {}
        self.metadata['version'] = '1.0'
        self.metadata['source'] = 'source124.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        # ?_def = amplitude * sin(?_def * t)
        result = 0.0  # Assign result
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{Ug1Defect_Delta_defTerm}",
            description=self.description
        )


class Ug1Defect_U_g1Term:
    """
    U_g1 with defect modulation
    
    Source: MAIN_1_CoAnQi.cpp (lines 17232-17257)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "Ug1Defect_U_g1"
        self.description = "U_g1 with defect modulation"
        self.metadata = {}
        self.metadata['version'] = '1.0'
        self.metadata['source'] = 'source124.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        # U_g1 with defect: (1 + ?_def) * k1 * M_s / r?
        return (1.0 + delta_def) * k1 * M_s / (r * r)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{Ug1Defect_U_g1Term}",
            description=self.description
        )


class Ug1Defect_Period_yearsTerm:
    """
    Defect oscillation period in years
    
    Source: MAIN_1_CoAnQi.cpp (lines 17259-17279)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "Ug1Defect_Period_years"
        self.description = "Defect oscillation period in years"
        self.metadata = {}
        self.metadata['version'] = '1.0'
        self.metadata['source'] = 'source124.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        year_to_day = 365.25
        # Period = 2? / ?_def in years
        return (2.0 * math.pi / omega_def) / year_to_day
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{Ug1Defect_Period_yearsTerm}",
            description=self.description
        )


class Ug3DiskVec_PhiHat_jTerm:
    """
    Disk plane unit vector φ̂_j
    
    Source: MAIN_1_CoAnQi.cpp (lines 17284-17306)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "Ug3DiskVec_PhiHat_j"
        self.description = "Disk plane unit vector φ̂_j"
        self.metadata = {}
        self.metadata['version'] = '1.0'
        self.metadata['source'] = 'source125.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        # Unit vector magnitude (always 1)
        # Components: [cos(?_j), sin(?_j), 0]
        result = 0.0  # Assign result
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{Ug3DiskVec_PhiHat_jTerm}",
            description=self.description
        )


class Ug3DiskVec_UmBaseTerm:
    """
    Base U_m term
    
    Source: MAIN_1_CoAnQi.cpp (lines 17308-17327)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "Ug3DiskVec_UmBase"
        self.description = "Base U_m term"
        self.metadata = {}
        self.metadata['version'] = '1.0'
        self.metadata['source'] = 'source125.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        # Base U_m without disk vector
        return mu_over_rj
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{Ug3DiskVec_UmBaseTerm}",
            description=self.description
        )


class Ug3DiskVec_UmContributionTerm:
    """
    U_m with disk vector contribution
    
    Source: MAIN_1_CoAnQi.cpp (lines 17329-17349)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "Ug3DiskVec_UmContribution"
        self.description = "U_m with disk vector contribution"
        self.metadata = {}
        self.metadata['version'] = '1.0'
        self.metadata['source'] = 'source125.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        # U_m with disk vector (magnitude = 1)
        return mu_over_rj * phi_hat_magnitude
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{Ug3DiskVec_UmContributionTerm}",
            description=self.description
        )


class Ug3DiskVec_PhiHatMagnitudeTerm:
    """
    Magnitude of φ̂_j (unit vector)
    
    Source: MAIN_1_CoAnQi.cpp (lines 17351-17369)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "Ug3DiskVec_PhiHatMagnitude"
        self.description = "Magnitude of φ̂_j (unit vector)"
        self.metadata = {}
        self.metadata['version'] = '1.0'
        self.metadata['source'] = 'source125.cpp'
        self.metadata['value'] = '1.0'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        # Unit vector magnitude
        return 1.0
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{Ug3DiskVec_PhiHatMagnitudeTerm}",
            description=self.description
        )


class AetherVacDensity_Rho_vac_ATerm:
    """
    Aether vacuum energy density ρ_vac,A
    
    Source: MAIN_1_CoAnQi.cpp (lines 17374-17392)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "AetherVacDensity_Rho_vac_A"
        self.description = "Aether vacuum energy density ρ_vac,A"
        self.metadata = {}
        self.metadata['version'] = '1.0'
        self.metadata['source'] = 'source126.cpp'
        self.metadata['value'] = '1e-23 J/m³'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        # ?_vac,A = 1e-23 J/m? (aether vacuum density)
        return 1e-23
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{AetherVacDensity_Rho_vac_ATerm}",
            description=self.description
        )


class AetherVacDensity_T_s_contributionTerm:
    """
    T_s with aether contribution
    
    Source: MAIN_1_CoAnQi.cpp (lines 17394-17414)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "AetherVacDensity_T_s_contribution"
        self.description = "T_s with aether contribution"
        self.metadata = {}
        self.metadata['version'] = '1.0'
        self.metadata['source'] = 'source126.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        # T_s = T_s_base + ?_vac,A ? 1.123e7 J/m?
        return T_s_base + rho_vac_A
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{AetherVacDensity_T_s_contributionTerm}",
            description=self.description
        )


class AetherVacDensity_A_mu_nuTerm:
    """
    Metric perturbation ε T_s from aether
    
    Source: MAIN_1_CoAnQi.cpp (lines 17416-17436)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "AetherVacDensity_A_mu_nu"
        self.description = "Metric perturbation ε T_s from aether"
        self.metadata = {}
        self.metadata['version'] = '1.0'
        self.metadata['source'] = 'source126.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        # A_?? perturbation: ? T_s ? 1.123e-15
        return epsilon * T_s
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{AetherVacDensity_A_mu_nuTerm}",
            description=self.description
        )


class AetherVacDensity_Epsilon_factorTerm:
    """
    Perturbation parameter ε
    
    Source: MAIN_1_CoAnQi.cpp (lines 17438-17456)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "AetherVacDensity_Epsilon_factor"
        self.description = "Perturbation parameter ε"
        self.metadata = {}
        self.metadata['version'] = '1.0'
        self.metadata['source'] = 'source126.cpp'
        self.metadata['value'] = '1e-22'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        # ? = 1e-22 (small perturbation parameter)
        return 1e-22
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{AetherVacDensity_Epsilon_factorTerm}",
            description=self.description
        )


class UnivInertiaVac_Rho_vac_UiTerm:
    """
    Universal Inertia vacuum density ρ_vac,Ui
    
    Source: MAIN_1_CoAnQi.cpp (lines 17461-17479)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "UnivInertiaVac_Rho_vac_Ui"
        self.description = "Universal Inertia vacuum density ρ_vac,Ui"
        self.metadata = {}
        self.metadata['version'] = '1.0'
        self.metadata['source'] = 'source127.cpp'
        self.metadata['value'] = '2.84e-36 J/m³'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        # ?_vac,Ui = 2.84e-36 J/m? (Sun, level 13)
        return 2.84e-36
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{UnivInertiaVac_Rho_vac_UiTerm}",
            description=self.description
        )


class UnivInertiaVac_U_i_baseTerm:
    """
    Base U_i inertial term
    
    Source: MAIN_1_CoAnQi.cpp (lines 17481-17502)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "UnivInertiaVac_U_i_base"
        self.description = "Base U_i inertial term"
        self.metadata = {}
        self.metadata['version'] = '1.0'
        self.metadata['source'] = 'source127.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        # U_i base = ?_s cos(?t_n) ?_i
        result = 0.0  # Assign result
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{UnivInertiaVac_U_i_baseTerm}",
            description=self.description
        )


class UnivInertiaVac_U_iTerm:
    """
    U_i with vacuum density scaling
    
    Source: MAIN_1_CoAnQi.cpp (lines 17504-17530)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "UnivInertiaVac_U_i"
        self.description = "U_i with vacuum density scaling"
        self.metadata = {}
        self.metadata['version'] = '1.0'
        self.metadata['source'] = 'source127.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        # U_i ? (1 + f_TRZ) ?_s cos(?t_n) ?_i ?[?_SCm ? ?_UA] ? 1.38e-47 J/m?
        return (1.0 + f_TRZ) * base * vac_product
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{UnivInertiaVac_U_iTerm}",
            description=self.description
        )


class ScmVacDensity_Rho_vac_SCmTerm:
    """
    [SCm] vacuum energy density ρ_vac,SCm
    
    Source: MAIN_1_CoAnQi.cpp (lines 17535-17553)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "ScmVacDensity_Rho_vac_SCm"
        self.description = "[SCm] vacuum energy density ρ_vac,SCm"
        self.metadata = {}
        self.metadata['version'] = '1.0'
        self.metadata['source'] = 'source128.cpp'
        self.metadata['value'] = '7.09e-37 J/m³'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        # ?_vac,[SCm] = 7.09e-37 J/m? (Sun, level 13)
        return 7.09e-37
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{ScmVacDensity_Rho_vac_SCmTerm}",
            description=self.description
        )


class ScmVacDensity_U_g2_exampleTerm:
    """
    U_g2 with SCm vacuum scaling
    
    Source: MAIN_1_CoAnQi.cpp (lines 17555-17579)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "ScmVacDensity_U_g2_example"
        self.description = "U_g2 with SCm vacuum scaling"
        self.metadata = {}
        self.metadata['version'] = '1.0'
        self.metadata['source'] = 'source128.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        # U_g2 ? S(r-R_b) (1+?_sw v_sw) M_s/r? ?_vac,SCm E_react ? 1.18e53 J/m?
        return S_factor * modulation * (M_s / (r * r)) * rho_vac_SCm * E_react
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{ScmVacDensity_U_g2_exampleTerm}",
            description=self.description
        )


class ScmVacDensity_U_i_exampleTerm:
    """
    U_i with SCm vacuum coupling
    
    Source: MAIN_1_CoAnQi.cpp (lines 17581-17607)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "ScmVacDensity_U_i_example"
        self.description = "U_i with SCm vacuum coupling"
        self.metadata = {}
        self.metadata['version'] = '1.0'
        self.metadata['source'] = 'source128.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        # U_i with SCm vacuum: ? 1.38e-47 J/m?
        return (1.0 + f_TRZ) * base * vac_product
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{ScmVacDensity_U_i_exampleTerm}",
            description=self.description
        )


class UaVacDensity_Rho_vac_UATerm:
    """
    [UA] vacuum energy density ρ_vac,UA
    
    Source: MAIN_1_CoAnQi.cpp (lines 17612-17630)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "UaVacDensity_Rho_vac_UA"
        self.description = "[UA] vacuum energy density ρ_vac,UA"
        self.metadata = {}
        self.metadata['version'] = '1.0'
        self.metadata['source'] = 'source129.cpp'
        self.metadata['value'] = '7.09e-36 J/m³'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        # ?_vac,[UA] = 7.09e-36 J/m? (Sun, level 13)
        return 7.09e-36
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{UaVacDensity_Rho_vac_UATerm}",
            description=self.description
        )


class UaVacDensity_U_g2_exampleTerm:
    """
    U_g2 with UA vacuum scaling
    
    Source: MAIN_1_CoAnQi.cpp (lines 17632-17656)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "UaVacDensity_U_g2_example"
        self.description = "U_g2 with UA vacuum scaling"
        self.metadata = {}
        self.metadata['version'] = '1.0'
        self.metadata['source'] = 'source129.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        # U_g2 ? 1.18e53 J/m? with UA vacuum
        return S_factor * modulation * (M_s / (r * r)) * rho_vac_UA * E_react
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{UaVacDensity_U_g2_exampleTerm}",
            description=self.description
        )


class UaVacDensity_U_i_exampleTerm:
    """
    U_i with UA vacuum coupling
    
    Source: MAIN_1_CoAnQi.cpp (lines 17658-17684)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "UaVacDensity_U_i_example"
        self.description = "U_i with UA vacuum coupling"
        self.metadata = {}
        self.metadata['version'] = '1.0'
        self.metadata['source'] = 'source129.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        # U_i ? 1.38e-47 J/m?
        return (1.0 + f_TRZ) * base * vac_product
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{UaVacDensity_U_i_exampleTerm}",
            description=self.description
        )


class UnivInertiaVac2_U_i_baseTerm:
    """
    U_i base (source130 variant)
    
    Source: MAIN_1_CoAnQi.cpp (lines 17689-17710)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "UnivInertiaVac2_U_i_base"
        self.description = "U_i base (source130 variant)"
        self.metadata = {}
        self.metadata['version'] = '1.0'
        self.metadata['source'] = 'source130.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        # U_i base without vacuum
        result = 0.0  # Assign result
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{UnivInertiaVac2_U_i_baseTerm}",
            description=self.description
        )


class UnivInertiaVac2_U_iTerm:
    """
    U_i full (source130 variant)
    
    Source: MAIN_1_CoAnQi.cpp (lines 17712-17738)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "UnivInertiaVac2_U_i"
        self.description = "U_i full (source130 variant)"
        self.metadata = {}
        self.metadata['version'] = '1.0'
        self.metadata['source'] = 'source130.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        # U_i with vacuum (source130 implementation)
        return (1.0 + f_TRZ) * base * vac_product
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{UnivInertiaVac2_U_iTerm}",
            description=self.description
        )


class ScmVelocity_V_scmTerm:
    """
    [SCm] velocity v_SCm
    
    Source: MAIN_1_CoAnQi.cpp (lines 17746-17761)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "ScmVelocity_V_scm"
        self.description = "[SCm] velocity v_SCm"
        self.metadata = {}
        self.metadata['version'] = '1.0'
        self.metadata['source'] = 'source131.cpp'
        self.metadata['value'] = '1e8 m/s'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return 1e8
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{ScmVelocity_V_scmTerm}",
            description=self.description
        )


class ScmVelocity_E_react_baseTerm:
    """
    Base E_react energy
    
    Source: MAIN_1_CoAnQi.cpp (lines 17763-17780)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "ScmVelocity_E_react_base"
        self.description = "Base E_react energy"
        self.metadata = {}
        self.metadata['version'] = '1.0'
        self.metadata['source'] = 'source131.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return (rho_SCm * v_scm * v_scm) / rho_A
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{ScmVelocity_E_react_baseTerm}",
            description=self.description
        )


class ScmVelocity_E_reactTerm:
    """
    E_react with decay
    
    Source: MAIN_1_CoAnQi.cpp (lines 17782-17799)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "ScmVelocity_E_react"
        self.description = "E_react with decay"
        self.metadata = {}
        self.metadata['version'] = '1.0'
        self.metadata['source'] = 'source131.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        t_day = t / 86400.0
        result = 0.0  # Assign result
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{ScmVelocity_E_reactTerm}",
            description=self.description
        )


class ScmVelocity_UmExampleTerm:
    """
    U_m with E_react
    
    Source: MAIN_1_CoAnQi.cpp (lines 17801-17817)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "ScmVelocity_UmExample"
        self.description = "U_m with E_react"
        self.metadata = {}
        self.metadata['version'] = '1.0'
        self.metadata['source'] = 'source131.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return mu_over_r * E_react
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{ScmVelocity_UmExampleTerm}",
            description=self.description
        )


class ButterflyNebula_DPM_momentumTerm:
    """
    DPM momentum term
    
    Source: MAIN_1_CoAnQi.cpp (lines 17820-17832)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "ButterflyNebula_DPM_momentum"
        self.description = "DPM momentum term"
        self.metadata = {}
        self.metadata['source'] = 'source132.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return M / (r * r)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{ButterflyNebula_DPM_momentumTerm}",
            description=self.description
        )


class ButterflyNebula_DPM_gravityTerm:
    """
    DPM gravity contribution
    
    Source: MAIN_1_CoAnQi.cpp (lines 17834-17847)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "ButterflyNebula_DPM_gravity"
        self.description = "DPM gravity contribution"
        self.metadata = {}
        self.metadata['source'] = 'source132.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        G = 6.674e-11
        return dpm_emergent_ug1(M, r)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{ButterflyNebula_DPM_gravityTerm}",
            description=self.description
        )


class ButterflyNebula_LENR_termTerm:
    """
    LENR resonance factor
    
    Source: MAIN_1_CoAnQi.cpp (lines 17849-17861)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "ButterflyNebula_LENR_term"
        self.description = "LENR resonance factor"
        self.metadata = {}
        self.metadata['source'] = 'source132.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return rho_LENR / rho_0
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{ButterflyNebula_LENR_termTerm}",
            description=self.description
        )


class ButterflyNebula_F_U_BiTerm:
    """
    Total UQFF force
    
    Source: MAIN_1_CoAnQi.cpp (lines 17863-17875)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "ButterflyNebula_F_U_Bi"
        self.description = "Total UQFF force"
        self.metadata = {}
        self.metadata['source'] = 'source132.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return integrand * x2
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{ButterflyNebula_F_U_BiTerm}",
            description=self.description
        )


class CentaurusA_DPM_momentumTerm:
    """
    DPM momentum NGC5128
    
    Source: MAIN_1_CoAnQi.cpp (lines 17878-17890)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "CentaurusA_DPM_momentum"
        self.description = "DPM momentum NGC5128"
        self.metadata = {}
        self.metadata['source'] = 'source133.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return M / (r * r)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{CentaurusA_DPM_momentumTerm}",
            description=self.description
        )


class CentaurusA_F_U_BiTerm:
    """
    NGC5128 UQFF force
    
    Source: MAIN_1_CoAnQi.cpp (lines 17892-17904)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "CentaurusA_F_U_Bi"
        self.description = "NGC5128 UQFF force"
        self.metadata = {}
        self.metadata['source'] = 'source133.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return integrand * x2
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{CentaurusA_F_U_BiTerm}",
            description=self.description
        )


class Abell2256_IntegrandTerm:
    """
    Abell 2256 integrand
    
    Source: MAIN_1_CoAnQi.cpp (lines 17907-17919)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "Abell2256_Integrand"
        self.description = "Abell 2256 integrand"
        self.metadata = {}
        self.metadata['source'] = 'source134.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return M / (r * r * r)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{Abell2256_IntegrandTerm}",
            description=self.description
        )


class Abell2256_DPM_resonanceTerm:
    """
    DPM resonance oscillation
    
    Source: MAIN_1_CoAnQi.cpp (lines 17921-17932)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "Abell2256_DPM_resonance"
        self.description = "DPM resonance oscillation"
        self.metadata = {}
        self.metadata['source'] = 'source134.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        result = 0.0  # TODO: Implement computation
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{Abell2256_DPM_resonanceTerm}",
            description=self.description
        )


class Abell2256_X2Term:
    """
    Quadratic root x2
    
    Source: MAIN_1_CoAnQi.cpp (lines 17934-17948)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "Abell2256_X2"
        self.description = "Quadratic root x2"
        self.metadata = {}
        self.metadata['source'] = 'source134.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        discriminant = b * b - 4 * a * c
        result = 0.0  # Assign result
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{Abell2256_X2Term}",
            description=self.description
        )


class Abell2256_LENR_termTerm:
    """
    LENR resonance
    
    Source: MAIN_1_CoAnQi.cpp (lines 17950-17961)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "Abell2256_LENR_term"
        self.description = "LENR resonance"
        self.metadata = {}
        self.metadata['source'] = 'source134.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return 1.0 / rho_0
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{Abell2256_LENR_termTerm}",
            description=self.description
        )


class Abell2256_Q_waveTerm:
    """
    Quantum wave term
    
    Source: MAIN_1_CoAnQi.cpp (lines 17963-17974)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "Abell2256_Q_wave"
        self.description = "Quantum wave term"
        self.metadata = {}
        self.metadata['source'] = 'source134.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        result = 0.0  # TODO: Implement computation
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{Abell2256_Q_waveTerm}",
            description=self.description
        )


class Abell2256_Ub1Term:
    """
    U_b1 buoyancy
    
    Source: MAIN_1_CoAnQi.cpp (lines 17976-17987)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "Abell2256_Ub1"
        self.description = "U_b1 buoyancy"
        self.metadata = {}
        self.metadata['source'] = 'source134.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return rho_vac * 1e50
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{Abell2256_Ub1Term}",
            description=self.description
        )


class Abell2256_UiTerm:
    """
    Universal Inertia
    
    Source: MAIN_1_CoAnQi.cpp (lines 17989-18000)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "Abell2256_Ui"
        self.description = "Universal Inertia"
        self.metadata = {}
        self.metadata['source'] = 'source134.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return omega_s * 1e-40
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{Abell2256_UiTerm}",
            description=self.description
        )


class Abell2256_FTerm:
    """
    Total force F
    
    Source: MAIN_1_CoAnQi.cpp (lines 18002-18014)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "Abell2256_F"
        self.description = "Total force F"
        self.metadata = {}
        self.metadata['source'] = 'source134.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return integrand * x2
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{Abell2256_FTerm}",
            description=self.description
        )


class Abell2256_CompressedTerm:
    """
    Compressed gravity g
    
    Source: MAIN_1_CoAnQi.cpp (lines 18016-18028)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "Abell2256_Compressed"
        self.description = "Compressed gravity g"
        self.metadata = {}
        self.metadata['source'] = 'source134.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return (M / r) * 1e-10
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{Abell2256_CompressedTerm}",
            description=self.description
        )


class Abell2256_ResonantTerm:
    """
    Resonant mode
    
    Source: MAIN_1_CoAnQi.cpp (lines 18030-18041)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "Abell2256_Resonant"
        self.description = "Resonant mode"
        self.metadata = {}
        self.metadata['source'] = 'source134.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        result = 0.0  # TODO: Implement computation
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{Abell2256_ResonantTerm}",
            description=self.description
        )


class Abell2256_BuoyancyTerm:
    """
    Buoyancy term
    
    Source: MAIN_1_CoAnQi.cpp (lines 18043-18054)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "Abell2256_Buoyancy"
        self.description = "Buoyancy term"
        self.metadata = {}
        self.metadata['source'] = 'source134.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return rho_vac * 1e55
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{Abell2256_BuoyancyTerm}",
            description=self.description
        )


class Abell2256_SuperconductiveTerm:
    """
    Superconductive term
    
    Source: MAIN_1_CoAnQi.cpp (lines 18056-18067)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "Abell2256_Superconductive"
        self.description = "Superconductive term"
        self.metadata = {}
        self.metadata['source'] = 'source134.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return E_react * 1e-30
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{Abell2256_SuperconductiveTerm}",
            description=self.description
        )


class Abell2256_CompressedGTerm:
    """
    Compressed G field
    
    Source: MAIN_1_CoAnQi.cpp (lines 18069-18082)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "Abell2256_CompressedG"
        self.description = "Compressed G field"
        self.metadata = {}
        self.metadata['source'] = 'source134.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        G = 6.674e-11
        return dpm_emergent_ug1(M, r)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{Abell2256_CompressedGTerm}",
            description=self.description
        )


class ASASSN14li_IntegrandTerm:
    """
    ASASSN-14li integrand
    
    Source: MAIN_1_CoAnQi.cpp (lines 18085-18097)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "ASASSN14li_Integrand"
        self.description = "ASASSN-14li integrand"
        self.metadata = {}
        self.metadata['source'] = 'source135.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return M / (r * r * r)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{ASASSN14li_IntegrandTerm}",
            description=self.description
        )


class ASASSN14li_DPM_resonanceTerm:
    """
    TDE resonance
    
    Source: MAIN_1_CoAnQi.cpp (lines 18099-18110)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "ASASSN14li_DPM_resonance"
        self.description = "TDE resonance"
        self.metadata = {}
        self.metadata['source'] = 'source135.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        result = 0.0  # TODO: Implement computation
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{ASASSN14li_DPM_resonanceTerm}",
            description=self.description
        )


class ASASSN14li_X2Term:
    """
    Quadratic root
    
    Source: MAIN_1_CoAnQi.cpp (lines 18112-18122)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "ASASSN14li_X2"
        self.description = "Quadratic root"
        self.metadata = {}
        self.metadata['source'] = 'source135.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return 1e12
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{ASASSN14li_X2Term}",
            description=self.description
        )


class ASASSN14li_LENR_termTerm:
    """
    LENR TDE
    
    Source: MAIN_1_CoAnQi.cpp (lines 18124-18135)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "ASASSN14li_LENR_term"
        self.description = "LENR TDE"
        self.metadata = {}
        self.metadata['source'] = 'source135.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return 1.0 / rho_0
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{ASASSN14li_LENR_termTerm}",
            description=self.description
        )


class ASASSN14li_Q_waveTerm:
    """
    Quantum wave
    
    Source: MAIN_1_CoAnQi.cpp (lines 18137-18148)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "ASASSN14li_Q_wave"
        self.description = "Quantum wave"
        self.metadata = {}
        self.metadata['source'] = 'source135.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        result = 0.0  # TODO: Implement computation
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{ASASSN14li_Q_waveTerm}",
            description=self.description
        )


class ASASSN14li_FTerm:
    """
    TDE force
    
    Source: MAIN_1_CoAnQi.cpp (lines 18150-18162)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "ASASSN14li_F"
        self.description = "TDE force"
        self.metadata = {}
        self.metadata['source'] = 'source135.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return integrand * x2
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{ASASSN14li_FTerm}",
            description=self.description
        )


class CentaurusA_X2Term:
    """
    CenA quadratic root
    
    Source: MAIN_1_CoAnQi.cpp (lines 18169-18179)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "CentaurusA_X2"
        self.description = "CenA quadratic root"
        self.metadata = {}
        self.metadata['source'] = 'source136.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return 1e12
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{CentaurusA_X2Term}",
            description=self.description
        )


class CentaurusA_LENR_termTerm:
    """
    CenA LENR
    
    Source: MAIN_1_CoAnQi.cpp (lines 18181-18192)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "CentaurusA_LENR_term"
        self.description = "CenA LENR"
        self.metadata = {}
        self.metadata['source'] = 'source136.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return 1.0 / rho_0
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{CentaurusA_LENR_termTerm}",
            description=self.description
        )


class CentaurusA_FTerm:
    """
    CenA total force
    
    Source: MAIN_1_CoAnQi.cpp (lines 18194-18206)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "CentaurusA_F"
        self.description = "CenA total force"
        self.metadata = {}
        self.metadata['source'] = 'source136.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return integrand * x2
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{CentaurusA_FTerm}",
            description=self.description
        )


class CentaurusA_CompressedTerm:
    """
    CenA compressed g
    
    Source: MAIN_1_CoAnQi.cpp (lines 18208-18220)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "CentaurusA_Compressed"
        self.description = "CenA compressed g"
        self.metadata = {}
        self.metadata['source'] = 'source136.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return (M / r) * 1e-10
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{CentaurusA_CompressedTerm}",
            description=self.description
        )


class CrabNebula_IntegrandTerm:
    """
    Crab integrand
    
    Source: MAIN_1_CoAnQi.cpp (lines 18222-18234)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "CrabNebula_Integrand"
        self.description = "Crab integrand"
        self.metadata = {}
        self.metadata['source'] = 'source137.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return M / (r * r * r)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{CrabNebula_IntegrandTerm}",
            description=self.description
        )


class CrabNebula_LENR_termTerm:
    """
    Crab LENR
    
    Source: MAIN_1_CoAnQi.cpp (lines 18236-18247)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "CrabNebula_LENR_term"
        self.description = "Crab LENR"
        self.metadata = {}
        self.metadata['source'] = 'source137.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return 1.0 / rho_0
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{CrabNebula_LENR_termTerm}",
            description=self.description
        )


class CrabNebula_FTerm:
    """
    Crab total force
    
    Source: MAIN_1_CoAnQi.cpp (lines 18249-18261)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "CrabNebula_F"
        self.description = "Crab total force"
        self.metadata = {}
        self.metadata['source'] = 'source137.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return integrand * x2
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{CrabNebula_FTerm}",
            description=self.description
        )


class CrabNebula_CompressedTerm:
    """
    Crab compressed g
    
    Source: MAIN_1_CoAnQi.cpp (lines 18263-18275)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "CrabNebula_Compressed"
        self.description = "Crab compressed g"
        self.metadata = {}
        self.metadata['source'] = 'source137.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return (M / r) * 1e-10
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{CrabNebula_CompressedTerm}",
            description=self.description
        )


class ElGordo_IntegrandTerm:
    """
    El Gordo integrand
    
    Source: MAIN_1_CoAnQi.cpp (lines 18277-18289)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "ElGordo_Integrand"
        self.description = "El Gordo integrand"
        self.metadata = {}
        self.metadata['source'] = 'source138.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return M / (r * r * r)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{ElGordo_IntegrandTerm}",
            description=self.description
        )


class ElGordo_LENR_termTerm:
    """
    El Gordo LENR
    
    Source: MAIN_1_CoAnQi.cpp (lines 18291-18302)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "ElGordo_LENR_term"
        self.description = "El Gordo LENR"
        self.metadata = {}
        self.metadata['source'] = 'source138.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return 1.0 / rho_0
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{ElGordo_LENR_termTerm}",
            description=self.description
        )


class ElGordo_FTerm:
    """
    El Gordo total force
    
    Source: MAIN_1_CoAnQi.cpp (lines 18304-18316)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "ElGordo_F"
        self.description = "El Gordo total force"
        self.metadata = {}
        self.metadata['source'] = 'source138.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return integrand * x2
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{ElGordo_FTerm}",
            description=self.description
        )


class ESO137_FTerm:
    """
    ESO 137-001 total force
    
    Source: MAIN_1_CoAnQi.cpp (lines 18320-18332)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "ESO137_F"
        self.description = "ESO 137-001 total force"
        self.metadata = {}
        self.metadata['source'] = 'Source139.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return integrand * x2
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{ESO137_FTerm}",
            description=self.description
        )


class IC2163_IntegrandTerm:
    """
    IC 2163 integrand
    
    Source: MAIN_1_CoAnQi.cpp (lines 18336-18348)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "IC2163_Integrand"
        self.description = "IC 2163 integrand"
        self.metadata = {}
        self.metadata['source'] = 'source140.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return M / (r * r * r)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{IC2163_IntegrandTerm}",
            description=self.description
        )


class IC2163_LENR_termTerm:
    """
    IC 2163 LENR
    
    Source: MAIN_1_CoAnQi.cpp (lines 18350-18361)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "IC2163_LENR_term"
        self.description = "IC 2163 LENR"
        self.metadata = {}
        self.metadata['source'] = 'source140.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return 1.0 / rho_0
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{IC2163_LENR_termTerm}",
            description=self.description
        )


class IC2163_FTerm:
    """
    IC 2163 total force
    
    Source: MAIN_1_CoAnQi.cpp (lines 18363-18375)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "IC2163_F"
        self.description = "IC 2163 total force"
        self.metadata = {}
        self.metadata['source'] = 'source140.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return integrand * x2
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{IC2163_FTerm}",
            description=self.description
        )


class IC2163_CompressedTerm:
    """
    IC 2163 compressed g
    
    Source: MAIN_1_CoAnQi.cpp (lines 18377-18389)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "IC2163_Compressed"
        self.description = "IC 2163 compressed g"
        self.metadata = {}
        self.metadata['source'] = 'source140.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return (M / r) * 1e-10
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{IC2163_CompressedTerm}",
            description=self.description
        )


class J1610_IntegrandTerm:
    """
    Integrand M/r³ for J1610+1811 high-z quasar (z=3.122)
    
    Source: MAIN_1_CoAnQi.cpp (lines 18394-18410)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "J1610_Integrand"
        self.description = "Integrand M/r³ for J1610+1811 high-z quasar (z=3.122)"
        self.metadata = {}
        self.metadata['source'] = 'source141.cpp'
        self.metadata['system'] = 'J1610+1811'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return M / (r * r * r)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{J1610_IntegrandTerm}",
            description=self.description
        )


class J1610_LENR_termTerm:
    """
    LENR resonance: 1/ρ₀ for J1610 (dominant term)
    
    Source: MAIN_1_CoAnQi.cpp (lines 18412-18422)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "J1610_LENR_term"
        self.description = "LENR resonance: 1/ρ₀ for J1610 (dominant term)"
        self.metadata = {}
        self.metadata['source'] = 'source141.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return 1e15
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{J1610_LENR_termTerm}",
            description=self.description
        )


class J1610_FTerm:
    """
    Total UQFF force for J1610+1811
    
    Source: MAIN_1_CoAnQi.cpp (lines 18424-18434)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "J1610_F"
        self.description = "Total UQFF force for J1610+1811"
        self.metadata = {}
        self.metadata['source'] = 'source141.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return 1.99e-2 * (-1.35e172)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{J1610_FTerm}",
            description=self.description
        )


class J1610_CompressedTerm:
    """
    Compressed gravity for high-z quasar
    
    Source: MAIN_1_CoAnQi.cpp (lines 18436-18446)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "J1610_Compressed"
        self.description = "Compressed gravity for high-z quasar"
        self.metadata = {}
        self.metadata['source'] = 'source141.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return (1.73e40 / 9.63e20) * 1e-10
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{J1610_CompressedTerm}",
            description=self.description
        )


class JupiterAurorae_IntegrandTerm:
    """
    Integrand for Jupiter auroral physics (Io plasma torus)
    
    Source: MAIN_1_CoAnQi.cpp (lines 18449-18463)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "JupiterAurorae_Integrand"
        self.description = "Integrand for Jupiter auroral physics (Io plasma torus)"
        self.metadata = {}
        self.metadata['source'] = 'source142.cpp'
        self.metadata['system'] = 'Jupiter'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return 1.898e27 / (7.1492e7 * 7.1492e7 * 7.1492e7)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{JupiterAurorae_IntegrandTerm}",
            description=self.description
        )


class JupiterAurorae_LENR_termTerm:
    """
    LENR resonance for Jupiter (magnetospheric coupling)
    
    Source: MAIN_1_CoAnQi.cpp (lines 18465-18472)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "JupiterAurorae_LENR_term"
        self.description = "LENR resonance for Jupiter (magnetospheric coupling)"
        self.metadata = {}
        self.metadata['source'] = 'source142.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return 1e12
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{JupiterAurorae_LENR_termTerm}",
            description=self.description
        )


class JupiterAurorae_FTerm:
    """
    Total UQFF force for Jupiter aurorae
    
    Source: MAIN_1_CoAnQi.cpp (lines 18474-18484)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "JupiterAurorae_F"
        self.description = "Total UQFF force for Jupiter aurorae"
        self.metadata = {}
        self.metadata['source'] = 'source142.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return 5.19e11 * (-3.40e172)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{JupiterAurorae_FTerm}",
            description=self.description
        )


class JupiterAurorae_CompressedTerm:
    """
    Compressed g for Jupiter
    
    Source: MAIN_1_CoAnQi.cpp (lines 18486-18496)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "JupiterAurorae_Compressed"
        self.description = "Compressed g for Jupiter"
        self.metadata = {}
        self.metadata['source'] = 'source142.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return (1.898e27 / 7.1492e7) * 1e-10
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{JupiterAurorae_CompressedTerm}",
            description=self.description
        )


class LagoonNebula_IntegrandTerm:
    """
    Integrand for Lagoon Nebula (star formation region)
    
    Source: MAIN_1_CoAnQi.cpp (lines 18499-18513)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "LagoonNebula_Integrand"
        self.description = "Integrand for Lagoon Nebula (star formation region)"
        self.metadata = {}
        self.metadata['source'] = 'source143.cpp'
        self.metadata['system'] = 'Lagoon M8'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return 1e36 / (2.36e17 * 2.36e17 * 2.36e17)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{LagoonNebula_IntegrandTerm}",
            description=self.description
        )


class LagoonNebula_LENR_termTerm:
    """
    LENR for Lagoon Nebula
    
    Source: MAIN_1_CoAnQi.cpp (lines 18515-18522)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "LagoonNebula_LENR_term"
        self.description = "LENR for Lagoon Nebula"
        self.metadata = {}
        self.metadata['source'] = 'source143.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return 1e12
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{LagoonNebula_LENR_termTerm}",
            description=self.description
        )


class LagoonNebula_FTerm:
    """
    Total UQFF force for Lagoon Nebula
    
    Source: MAIN_1_CoAnQi.cpp (lines 18524-18534)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "LagoonNebula_F"
        self.description = "Total UQFF force for Lagoon Nebula"
        self.metadata = {}
        self.metadata['source'] = 'source143.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return 7.61e-2 * (-3.40e172)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{LagoonNebula_FTerm}",
            description=self.description
        )


class LagoonNebula_CompressedTerm:
    """
    Compressed g for Lagoon Nebula
    
    Source: MAIN_1_CoAnQi.cpp (lines 18536-18546)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "LagoonNebula_Compressed"
        self.description = "Compressed g for Lagoon Nebula"
        self.metadata = {}
        self.metadata['source'] = 'source143.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return (1e36 / 2.36e17) * 1e-10
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{LagoonNebula_CompressedTerm}",
            description=self.description
        )


class LagoonNebulaVar_IntegrandTerm:
    """
    Lagoon Nebula variant integrand
    
    Source: MAIN_1_CoAnQi.cpp (lines 18549-18563)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "LagoonNebulaVar_Integrand"
        self.description = "Lagoon Nebula variant integrand"
        self.metadata = {}
        self.metadata['source'] = 'source144.cpp'
        self.metadata['variant'] = 'true'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return 1e36 / (2.36e17 * 2.36e17 * 2.36e17)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{LagoonNebulaVar_IntegrandTerm}",
            description=self.description
        )


class LagoonNebulaVar_LENR_termTerm:
    """
    Lagoon variant LENR
    
    Source: MAIN_1_CoAnQi.cpp (lines 18565-18572)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "LagoonNebulaVar_LENR_term"
        self.description = "Lagoon variant LENR"
        self.metadata = {}
        self.metadata['source'] = 'source144.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return 1e12
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{LagoonNebulaVar_LENR_termTerm}",
            description=self.description
        )


class LagoonNebulaVar_FTerm:
    """
    Lagoon variant total force
    
    Source: MAIN_1_CoAnQi.cpp (lines 18574-18584)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "LagoonNebulaVar_F"
        self.description = "Lagoon variant total force"
        self.metadata = {}
        self.metadata['source'] = 'source144.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return 7.61e-2 * (-3.40e172)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{LagoonNebulaVar_FTerm}",
            description=self.description
        )


class LagoonNebulaVar_CompressedTerm:
    """
    Lagoon variant compressed g
    
    Source: MAIN_1_CoAnQi.cpp (lines 18586-18596)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "LagoonNebulaVar_Compressed"
        self.description = "Lagoon variant compressed g"
        self.metadata = {}
        self.metadata['source'] = 'source144.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return (1e36 / 2.36e17) * 1e-10
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{LagoonNebulaVar_CompressedTerm}",
            description=self.description
        )


class M87Jet_IntegrandTerm:
    """
    Integrand for M87 relativistic jet (~0.5c, EHT SMBH M=6.5e9 M☉)
    
    Source: MAIN_1_CoAnQi.cpp (lines 18599-18613)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "M87Jet_Integrand"
        self.description = "Integrand for M87 relativistic jet (~0.5c, EHT SMBH M=6.5e9 M☉)"
        self.metadata = {}
        self.metadata['source'] = 'source145.cpp'
        self.metadata['system'] = 'M87 Virgo A'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return 1.29e40 / (4.63e19 * 4.63e19 * 4.63e19)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{M87Jet_IntegrandTerm}",
            description=self.description
        )


class M87Jet_LENR_termTerm:
    """
    LENR for M87 jet (superluminal knots)
    
    Source: MAIN_1_CoAnQi.cpp (lines 18615-18622)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "M87Jet_LENR_term"
        self.description = "LENR for M87 jet (superluminal knots)"
        self.metadata = {}
        self.metadata['source'] = 'source145.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return 1e15
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{M87Jet_LENR_termTerm}",
            description=self.description
        )


class M87Jet_FTerm:
    """
    Total UQFF force for M87 jet
    
    Source: MAIN_1_CoAnQi.cpp (lines 18624-18634)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "M87Jet_F"
        self.description = "Total UQFF force for M87 jet"
        self.metadata = {}
        self.metadata['source'] = 'source145.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return 1.30e1 * (-1.35e172)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{M87Jet_FTerm}",
            description=self.description
        )


class M87Jet_CompressedTerm:
    """
    Compressed g for M87 jet
    
    Source: MAIN_1_CoAnQi.cpp (lines 18636-18646)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "M87Jet_Compressed"
        self.description = "Compressed g for M87 jet"
        self.metadata = {}
        self.metadata['source'] = 'source145.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return (1.29e40 / 4.63e19) * 1e-10
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{M87Jet_CompressedTerm}",
            description=self.description
        )


class NGC1365_IntegrandTerm:
    """
    Integrand for NGC 1365 double-barred Seyfert (z=0.0055)
    
    Source: MAIN_1_CoAnQi.cpp (lines 18649-18663)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "NGC1365_Integrand"
        self.description = "Integrand for NGC 1365 double-barred Seyfert (z=0.0055)"
        self.metadata = {}
        self.metadata['source'] = 'source146.cpp'
        self.metadata['system'] = 'NGC 1365 Fornax'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return 7.17e41 / (9.46e20 * 9.46e20 * 9.46e20)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{NGC1365_IntegrandTerm}",
            description=self.description
        )


class NGC1365_LENR_termTerm:
    """
    LENR for NGC 1365 (AGN X-ray variability)
    
    Source: MAIN_1_CoAnQi.cpp (lines 18665-18672)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "NGC1365_LENR_term"
        self.description = "LENR for NGC 1365 (AGN X-ray variability)"
        self.metadata = {}
        self.metadata['source'] = 'source146.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return 1e15
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{NGC1365_LENR_termTerm}",
            description=self.description
        )


class NGC1365_FTerm:
    """
    Total UQFF force for NGC 1365
    
    Source: MAIN_1_CoAnQi.cpp (lines 18674-18684)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "NGC1365_F"
        self.description = "Total UQFF force for NGC 1365"
        self.metadata = {}
        self.metadata['source'] = 'source146.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return 8.47e-1 * (-1.35e172)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{NGC1365_FTerm}",
            description=self.description
        )


class NGC2207_IntegrandTerm:
    """
    Integrand for NGC 2207 colliding galaxies (tidal bridge)
    
    Source: MAIN_1_CoAnQi.cpp (lines 18689-18703)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "NGC2207_Integrand"
        self.description = "Integrand for NGC 2207 colliding galaxies (tidal bridge)"
        self.metadata = {}
        self.metadata['source'] = 'source147.cpp'
        self.metadata['system'] = 'NGC 2207/IC 2163'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return 3.978e40 / (4.40e20 * 4.40e20 * 4.40e20)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{NGC2207_IntegrandTerm}",
            description=self.description
        )


class NGC2207_LENR_termTerm:
    """
    LENR for NGC 2207 (interaction-driven)
    
    Source: MAIN_1_CoAnQi.cpp (lines 18705-18712)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "NGC2207_LENR_term"
        self.description = "LENR for NGC 2207 (interaction-driven)"
        self.metadata = {}
        self.metadata['source'] = 'source147.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return 1e12
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{NGC2207_LENR_termTerm}",
            description=self.description
        )


class NGC2207_FTerm:
    """
    Total UQFF force for NGC 2207
    
    Source: MAIN_1_CoAnQi.cpp (lines 18714-18724)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "NGC2207_F"
        self.description = "Total UQFF force for NGC 2207"
        self.metadata = {}
        self.metadata['source'] = 'source147.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return 4.67e-1 * (-3.40e172)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{NGC2207_FTerm}",
            description=self.description
        )


class NGC2207_CompressedTerm:
    """
    Compressed g for NGC 2207
    
    Source: MAIN_1_CoAnQi.cpp (lines 18726-18736)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "NGC2207_Compressed"
        self.description = "Compressed g for NGC 2207"
        self.metadata = {}
        self.metadata['source'] = 'source147.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return (3.978e40 / 4.40e20) * 1e-10
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{NGC2207_CompressedTerm}",
            description=self.description
        )


class RAquarii_IntegrandTerm:
    """
    Integrand for R Aquarii symbiotic binary (Mira variable + white dwarf)
    
    Source: MAIN_1_CoAnQi.cpp (lines 18739-18753)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "RAquarii_Integrand"
        self.description = "Integrand for R Aquarii symbiotic binary (Mira variable + white dwarf)"
        self.metadata = {}
        self.metadata['source'] = 'source148.cpp'
        self.metadata['system'] = 'R Aquarii'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return 3.978e30 / (2.18e15 * 2.18e15 * 2.18e15)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{RAquarii_IntegrandTerm}",
            description=self.description
        )


class RAquarii_LENR_termTerm:
    """
    LENR for R Aquarii (mass transfer jets)
    
    Source: MAIN_1_CoAnQi.cpp (lines 18755-18762)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "RAquarii_LENR_term"
        self.description = "LENR for R Aquarii (mass transfer jets)"
        self.metadata = {}
        self.metadata['source'] = 'source148.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return 1e12
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{RAquarii_LENR_termTerm}",
            description=self.description
        )


class RAquarii_FTerm:
    """
    Total UQFF force for R Aquarii
    
    Source: MAIN_1_CoAnQi.cpp (lines 18764-18774)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "RAquarii_F"
        self.description = "Total UQFF force for R Aquarii"
        self.metadata = {}
        self.metadata['source'] = 'source148.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return 3.84e0 * (-3.40e172)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{RAquarii_FTerm}",
            description=self.description
        )


class RAquarii_CompressedTerm:
    """
    Compressed g for R Aquarii
    
    Source: MAIN_1_CoAnQi.cpp (lines 18776-18786)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "RAquarii_Compressed"
        self.description = "Compressed g for R Aquarii"
        self.metadata = {}
        self.metadata['source'] = 'source148.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return (3.978e30 / 2.18e15) * 1e-10
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{RAquarii_CompressedTerm}",
            description=self.description
        )


class SgrAStar_IntegrandTerm:
    """
    Integrand for Sagittarius A* (Galactic Center, Nobel 2020)
    
    Source: MAIN_1_CoAnQi.cpp (lines 18789-18803)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "SgrAStar_Integrand"
        self.description = "Integrand for Sagittarius A* (Galactic Center, Nobel 2020)"
        self.metadata = {}
        self.metadata['source'] = 'source149.cpp'
        self.metadata['system'] = 'Sgr A* MW'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return 8.56e36 / (6.17e18 * 6.17e18 * 6.17e18)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{SgrAStar_IntegrandTerm}",
            description=self.description
        )


class SgrAStar_LENR_termTerm:
    """
    LENR for Sgr A* (flare activity)
    
    Source: MAIN_1_CoAnQi.cpp (lines 18805-18812)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "SgrAStar_LENR_term"
        self.description = "LENR for Sgr A* (flare activity)"
        self.metadata = {}
        self.metadata['source'] = 'source149.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return 1e15
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{SgrAStar_LENR_termTerm}",
            description=self.description
        )


class SgrAStar_FTerm:
    """
    Total UQFF force for Sgr A*
    
    Source: MAIN_1_CoAnQi.cpp (lines 18814-18824)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "SgrAStar_F"
        self.description = "Total UQFF force for Sgr A*"
        self.metadata = {}
        self.metadata['source'] = 'source149.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return 3.65e-2 * (-1.35e172)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{SgrAStar_FTerm}",
            description=self.description
        )


class SgrAStar_CompressedTerm:
    """
    Compressed g for Sgr A*
    
    Source: MAIN_1_CoAnQi.cpp (lines 18826-18836)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "SgrAStar_Compressed"
        self.description = "Compressed g for Sgr A*"
        self.metadata = {}
        self.metadata['source'] = 'source149.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return (8.56e36 / 6.17e18) * 1e-10
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{SgrAStar_CompressedTerm}",
            description=self.description
        )


class SPTCLJ2215_IntegrandTerm:
    """
    Integrand for SPT-CL J2215 galaxy cluster (z=0.7)
    
    Source: MAIN_1_CoAnQi.cpp (lines 18839-18853)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "SPTCLJ2215_Integrand"
        self.description = "Integrand for SPT-CL J2215 galaxy cluster (z=0.7)"
        self.metadata = {}
        self.metadata['source'] = 'source150.cpp'
        self.metadata['system'] = 'SPT-CL J2215-3537'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return 1.46e45 / (3.09e22 * 3.09e22 * 3.09e22)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{SPTCLJ2215_IntegrandTerm}",
            description=self.description
        )


class SPTCLJ2215_LENR_termTerm:
    """
    LENR for SPT-CL J2215
    
    Source: MAIN_1_CoAnQi.cpp (lines 18855-18862)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "SPTCLJ2215_LENR_term"
        self.description = "LENR for SPT-CL J2215"
        self.metadata = {}
        self.metadata['source'] = 'source150.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return 1e15
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{SPTCLJ2215_LENR_termTerm}",
            description=self.description
        )


class SPTCLJ2215_FTerm:
    """
    Total UQFF force for SPT-CL J2215
    
    Source: MAIN_1_CoAnQi.cpp (lines 18864-18874)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "SPTCLJ2215_F"
        self.description = "Total UQFF force for SPT-CL J2215"
        self.metadata = {}
        self.metadata['source'] = 'source150.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return 4.95e-1 * (-1.35e172)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{SPTCLJ2215_FTerm}",
            description=self.description
        )


class SPTCLJ2215_CompressedTerm:
    """
    Compressed g for SPT-CL J2215
    
    Source: MAIN_1_CoAnQi.cpp (lines 18876-18886)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "SPTCLJ2215_Compressed"
        self.description = "Compressed g for SPT-CL J2215"
        self.metadata = {}
        self.metadata['source'] = 'source150.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return (1.46e45 / 3.09e22) * 1e-10
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{SPTCLJ2215_CompressedTerm}",
            description=self.description
        )


class StephanQuintet_IntegrandTerm:
    """
    Integrand for Stephan's Quintet compact group (JWST NIRCam target)
    
    Source: MAIN_1_CoAnQi.cpp (lines 18889-18903)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "StephanQuintet_Integrand"
        self.description = "Integrand for Stephan's Quintet compact group (JWST NIRCam target)"
        self.metadata = {}
        self.metadata['source'] = 'source151.cpp'
        self.metadata['system'] = 'Stephan\'s Quintet'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return 2e39 / (3.09e22 * 3.09e22 * 3.09e22)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{StephanQuintet_IntegrandTerm}",
            description=self.description
        )


class StephanQuintet_LENR_termTerm:
    """
    LENR for Stephan's Quintet
    
    Source: MAIN_1_CoAnQi.cpp (lines 18905-18912)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "StephanQuintet_LENR_term"
        self.description = "LENR for Stephan's Quintet"
        self.metadata = {}
        self.metadata['source'] = 'source151.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return 1e15
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{StephanQuintet_LENR_termTerm}",
            description=self.description
        )


class StephanQuintet_FTerm:
    """
    Total UQFF force for Stephan's Quintet
    
    Source: MAIN_1_CoAnQi.cpp (lines 18914-18924)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "StephanQuintet_F"
        self.description = "Total UQFF force for Stephan's Quintet"
        self.metadata = {}
        self.metadata['source'] = 'source151.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return 6.79e-7 * (-1.35e172)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{StephanQuintet_FTerm}",
            description=self.description
        )


class StephanQuintet_CompressedTerm:
    """
    Compressed g for Stephan's Quintet
    
    Source: MAIN_1_CoAnQi.cpp (lines 18926-18936)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "StephanQuintet_Compressed"
        self.description = "Compressed g for Stephan's Quintet"
        self.metadata = {}
        self.metadata['source'] = 'source151.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return (2e39 / 3.09e22) * 1e-10
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{StephanQuintet_CompressedTerm}",
            description=self.description
        )


class VelaPulsar_IntegrandTerm:
    """
    Integrand for Vela Pulsar (89 ms period, Gum Nebula)
    
    Source: MAIN_1_CoAnQi.cpp (lines 18939-18953)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "VelaPulsar_Integrand"
        self.description = "Integrand for Vela Pulsar (89 ms period, Gum Nebula)"
        self.metadata = {}
        self.metadata['source'] = 'source152.cpp'
        self.metadata['system'] = 'Vela PSR J0835-4510'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return 2.8e30 / (1.7e17 * 1.7e17 * 1.7e17)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{VelaPulsar_IntegrandTerm}",
            description=self.description
        )


class VelaPulsar_LENR_termTerm:
    """
    LENR for Vela Pulsar
    
    Source: MAIN_1_CoAnQi.cpp (lines 18955-18962)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "VelaPulsar_LENR_term"
        self.description = "LENR for Vela Pulsar"
        self.metadata = {}
        self.metadata['source'] = 'source152.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return 1e12
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{VelaPulsar_LENR_termTerm}",
            description=self.description
        )


class VelaPulsar_FTerm:
    """
    Total UQFF force for Vela Pulsar
    
    Source: MAIN_1_CoAnQi.cpp (lines 18964-18974)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "VelaPulsar_F"
        self.description = "Total UQFF force for Vela Pulsar"
        self.metadata = {}
        self.metadata['source'] = 'source152.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return 5.71e-5 * (-3.40e172)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{VelaPulsar_FTerm}",
            description=self.description
        )


class VelaPulsar_CompressedTerm:
    """
    Compressed g for Vela Pulsar
    
    Source: MAIN_1_CoAnQi.cpp (lines 18976-18986)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "VelaPulsar_Compressed"
        self.description = "Compressed g for Vela Pulsar"
        self.metadata = {}
        self.metadata['source'] = 'source152.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return (2.8e30 / 1.7e17) * 1e-10
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{VelaPulsar_CompressedTerm}",
            description=self.description
        )


class NGC4993_IntegrandTerm:
    """
    NGC 4993 (neutron star merger host)
    
    Source: MAIN_1_CoAnQi.cpp (lines 18989-19003)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "NGC4993_Integrand"
        self.description = "NGC 4993 (neutron star merger host)"
        self.metadata = {}
        self.metadata['source'] = 'source153.cpp'
        self.metadata['system'] = 'NGC 4993 GW170817'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return 5e40 / (4e20 * 4e20 * 4e20)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{NGC4993_IntegrandTerm}",
            description=self.description
        )


class NGC4993_LENR_termTerm:
    """
    LENR for NGC 4993
    
    Source: MAIN_1_CoAnQi.cpp (lines 19005-19012)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "NGC4993_LENR_term"
        self.description = "LENR for NGC 4993"
        self.metadata = {}
        self.metadata['source'] = 'source153.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return 1e15
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{NGC4993_LENR_termTerm}",
            description=self.description
        )


class NGC4993_FTerm:
    """
    Total force NGC 4993
    
    Source: MAIN_1_CoAnQi.cpp (lines 19014-19024)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "NGC4993_F"
        self.description = "Total force NGC 4993"
        self.metadata = {}
        self.metadata['source'] = 'source153.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return 7.81e-1 * (-1.35e172)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{NGC4993_FTerm}",
            description=self.description
        )


class NGC4993_CompressedTerm:
    """
    Compressed g NGC 4993
    
    Source: MAIN_1_CoAnQi.cpp (lines 19026-19036)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "NGC4993_Compressed"
        self.description = "Compressed g NGC 4993"
        self.metadata = {}
        self.metadata['source'] = 'source153.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return (5e40 / 4e20) * 1e-10
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{NGC4993_CompressedTerm}",
            description=self.description
        )


class NGC6240_IntegrandTerm:
    """
    NGC 6240 (merging dual AGN)
    
    Source: MAIN_1_CoAnQi.cpp (lines 19038-19052)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "NGC6240_Integrand"
        self.description = "NGC 6240 (merging dual AGN)"
        self.metadata = {}
        self.metadata['source'] = 'source154.cpp'
        self.metadata['system'] = 'NGC 6240 dual AGN'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return 8e40 / (6e20 * 6e20 * 6e20)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{NGC6240_IntegrandTerm}",
            description=self.description
        )


class NGC6240_LENR_termTerm:
    """
    LENR for NGC 6240
    
    Source: MAIN_1_CoAnQi.cpp (lines 19054-19061)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "NGC6240_LENR_term"
        self.description = "LENR for NGC 6240"
        self.metadata = {}
        self.metadata['source'] = 'source154.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return 1e15
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{NGC6240_LENR_termTerm}",
            description=self.description
        )


class NGC6240_FTerm:
    """
    Total force NGC 6240
    
    Source: MAIN_1_CoAnQi.cpp (lines 19063-19073)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "NGC6240_F"
        self.description = "Total force NGC 6240"
        self.metadata = {}
        self.metadata['source'] = 'source154.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return 3.70e-1 * (-1.35e172)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{NGC6240_FTerm}",
            description=self.description
        )


class NGC6240_CompressedTerm:
    """
    Compressed g NGC 6240
    
    Source: MAIN_1_CoAnQi.cpp (lines 19075-19085)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "NGC6240_Compressed"
        self.description = "Compressed g NGC 6240"
        self.metadata = {}
        self.metadata['source'] = 'source154.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return (8e40 / 6e20) * 1e-10
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{NGC6240_CompressedTerm}",
            description=self.description
        )


class OmegaCentauri_IntegrandTerm:
    """
    Omega Centauri (largest MW globular)
    
    Source: MAIN_1_CoAnQi.cpp (lines 19087-19101)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "OmegaCentauri_Integrand"
        self.description = "Omega Centauri (largest MW globular)"
        self.metadata = {}
        self.metadata['source'] = 'source155.cpp'
        self.metadata['system'] = 'Omega Centauri'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return 8e36 / (4e17 * 4e17 * 4e17)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{OmegaCentauri_IntegrandTerm}",
            description=self.description
        )


class OmegaCentauri_LENR_termTerm:
    """
    LENR for Omega Centauri
    
    Source: MAIN_1_CoAnQi.cpp (lines 19103-19110)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "OmegaCentauri_LENR_term"
        self.description = "LENR for Omega Centauri"
        self.metadata = {}
        self.metadata['source'] = 'source155.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return 1e12
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{OmegaCentauri_LENR_termTerm}",
            description=self.description
        )


class OmegaCentauri_FTerm:
    """
    Total force Omega Centauri
    
    Source: MAIN_1_CoAnQi.cpp (lines 19112-19122)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "OmegaCentauri_F"
        self.description = "Total force Omega Centauri"
        self.metadata = {}
        self.metadata['source'] = 'source155.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return 1.25e-1 * (-3.40e172)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{OmegaCentauri_FTerm}",
            description=self.description
        )


class OmegaCentauri_CompressedTerm:
    """
    Compressed g Omega Centauri
    
    Source: MAIN_1_CoAnQi.cpp (lines 19124-19134)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "OmegaCentauri_Compressed"
        self.description = "Compressed g Omega Centauri"
        self.metadata = {}
        self.metadata['source'] = 'source155.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return (8e36 / 4e17) * 1e-10
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{OmegaCentauri_CompressedTerm}",
            description=self.description
        )


class OrionNebula_IntegrandTerm:
    """
    Orion Nebula (stellar nursery)
    
    Source: MAIN_1_CoAnQi.cpp (lines 19136-19150)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "OrionNebula_Integrand"
        self.description = "Orion Nebula (stellar nursery)"
        self.metadata = {}
        self.metadata['source'] = 'source156.cpp'
        self.metadata['system'] = 'Orion M42'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return 2e36 / (3e17 * 3e17 * 3e17)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{OrionNebula_IntegrandTerm}",
            description=self.description
        )


class OrionNebula_LENR_termTerm:
    """
    LENR for Orion Nebula
    
    Source: MAIN_1_CoAnQi.cpp (lines 19152-19159)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "OrionNebula_LENR_term"
        self.description = "LENR for Orion Nebula"
        self.metadata = {}
        self.metadata['source'] = 'source156.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return 1e12
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{OrionNebula_LENR_termTerm}",
            description=self.description
        )


class OrionNebula_FTerm:
    """
    Total force Orion Nebula
    
    Source: MAIN_1_CoAnQi.cpp (lines 19161-19171)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "OrionNebula_F"
        self.description = "Total force Orion Nebula"
        self.metadata = {}
        self.metadata['source'] = 'source156.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return 7.41e-2 * (-3.40e172)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{OrionNebula_FTerm}",
            description=self.description
        )


class OrionNebula_CompressedTerm:
    """
    Compressed g Orion Nebula
    
    Source: MAIN_1_CoAnQi.cpp (lines 19173-19183)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "OrionNebula_Compressed"
        self.description = "Compressed g Orion Nebula"
        self.metadata = {}
        self.metadata['source'] = 'source156.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return (2e36 / 3e17) * 1e-10
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{OrionNebula_CompressedTerm}",
            description=self.description
        )


class Perseus_IntegrandTerm:
    """
    Perseus Cluster (Chandra cavities)
    
    Source: MAIN_1_CoAnQi.cpp (lines 19185-19199)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "Perseus_Integrand"
        self.description = "Perseus Cluster (Chandra cavities)"
        self.metadata = {}
        self.metadata['source'] = 'source157.cpp'
        self.metadata['system'] = 'Perseus Abell 426'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return 2.5e45 / (5e22 * 5e22 * 5e22)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{Perseus_IntegrandTerm}",
            description=self.description
        )


class Perseus_LENR_termTerm:
    """
    LENR for Perseus
    
    Source: MAIN_1_CoAnQi.cpp (lines 19201-19208)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "Perseus_LENR_term"
        self.description = "LENR for Perseus"
        self.metadata = {}
        self.metadata['source'] = 'source157.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return 1e15
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{Perseus_LENR_termTerm}",
            description=self.description
        )


class Perseus_FTerm:
    """
    Total force Perseus
    
    Source: MAIN_1_CoAnQi.cpp (lines 19210-19220)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "Perseus_F"
        self.description = "Total force Perseus"
        self.metadata = {}
        self.metadata['source'] = 'source157.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return 2.00e-1 * (-1.35e172)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{Perseus_FTerm}",
            description=self.description
        )


class Perseus_CompressedTerm:
    """
    Compressed g Perseus
    
    Source: MAIN_1_CoAnQi.cpp (lines 19222-19232)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "Perseus_Compressed"
        self.description = "Compressed g Perseus"
        self.metadata = {}
        self.metadata['source'] = 'source157.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return (2.5e45 / 5e22) * 1e-10
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{Perseus_CompressedTerm}",
            description=self.description
        )


class PolarRing_IntegrandTerm:
    """
    Polar Ring Galaxy (NGC 4650A)
    
    Source: MAIN_1_CoAnQi.cpp (lines 19234-19248)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "PolarRing_Integrand"
        self.description = "Polar Ring Galaxy (NGC 4650A)"
        self.metadata = {}
        self.metadata['source'] = 'source158.cpp'
        self.metadata['system'] = 'Polar Ring NGC 4650A'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return 1e41 / (8e20 * 8e20 * 8e20)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{PolarRing_IntegrandTerm}",
            description=self.description
        )


class PolarRing_LENR_termTerm:
    """
    LENR for Polar Ring
    
    Source: MAIN_1_CoAnQi.cpp (lines 19250-19257)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "PolarRing_LENR_term"
        self.description = "LENR for Polar Ring"
        self.metadata = {}
        self.metadata['source'] = 'source158.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return 1e15
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{PolarRing_LENR_termTerm}",
            description=self.description
        )


class PolarRing_FTerm:
    """
    Total force Polar Ring
    
    Source: MAIN_1_CoAnQi.cpp (lines 19259-19269)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "PolarRing_F"
        self.description = "Total force Polar Ring"
        self.metadata = {}
        self.metadata['source'] = 'source158.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return 1.95e-1 * (-1.35e172)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{PolarRing_FTerm}",
            description=self.description
        )


class PolarRing_CompressedTerm:
    """
    Compressed g Polar Ring
    
    Source: MAIN_1_CoAnQi.cpp (lines 19271-19281)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "PolarRing_Compressed"
        self.description = "Compressed g Polar Ring"
        self.metadata = {}
        self.metadata['source'] = 'source158.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return (1e41 / 8e20) * 1e-10
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{PolarRing_CompressedTerm}",
            description=self.description
        )


class QuasarJ0313_IntegrandTerm:
    """
    Quasar J0313 (earliest known z=7.64)
    
    Source: MAIN_1_CoAnQi.cpp (lines 19283-19297)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "QuasarJ0313_Integrand"
        self.description = "Quasar J0313 (earliest known z=7.64)"
        self.metadata = {}
        self.metadata['source'] = 'source159.cpp'
        self.metadata['system'] = 'J0313-1806 z=7.64'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return 3e40 / (1e21 * 1e21 * 1e21)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{QuasarJ0313_IntegrandTerm}",
            description=self.description
        )


class QuasarJ0313_LENR_termTerm:
    """
    LENR for J0313
    
    Source: MAIN_1_CoAnQi.cpp (lines 19299-19306)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "QuasarJ0313_LENR_term"
        self.description = "LENR for J0313"
        self.metadata = {}
        self.metadata['source'] = 'source159.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return 1e15
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{QuasarJ0313_LENR_termTerm}",
            description=self.description
        )


class QuasarJ0313_FTerm:
    """
    Total force J0313
    
    Source: MAIN_1_CoAnQi.cpp (lines 19308-19318)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "QuasarJ0313_F"
        self.description = "Total force J0313"
        self.metadata = {}
        self.metadata['source'] = 'source159.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return 3.00e-2 * (-1.35e172)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{QuasarJ0313_FTerm}",
            description=self.description
        )


class QuasarJ0313_CompressedTerm:
    """
    Compressed g J0313
    
    Source: MAIN_1_CoAnQi.cpp (lines 19320-19330)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "QuasarJ0313_Compressed"
        self.description = "Compressed g J0313"
        self.metadata = {}
        self.metadata['source'] = 'source159.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return (3e40 / 1e21) * 1e-10
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{QuasarJ0313_CompressedTerm}",
            description=self.description
        )


class RXJ1713_IntegrandTerm:
    """
    RX J1713 SNR (TeV gamma-ray)
    
    Source: MAIN_1_CoAnQi.cpp (lines 19332-19346)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "RXJ1713_Integrand"
        self.description = "RX J1713 SNR (TeV gamma-ray)"
        self.metadata = {}
        self.metadata['source'] = 'source160.cpp'
        self.metadata['system'] = 'RXJ1713.7-3946 SNR'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return 5e30 / (2e17 * 2e17 * 2e17)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{RXJ1713_IntegrandTerm}",
            description=self.description
        )


class RXJ1713_LENR_termTerm:
    """
    LENR for RX J1713
    
    Source: MAIN_1_CoAnQi.cpp (lines 19348-19355)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "RXJ1713_LENR_term"
        self.description = "LENR for RX J1713"
        self.metadata = {}
        self.metadata['source'] = 'source160.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return 1e12
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{RXJ1713_LENR_termTerm}",
            description=self.description
        )


class RXJ1713_FTerm:
    """
    Total force RX J1713
    
    Source: MAIN_1_CoAnQi.cpp (lines 19357-19367)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "RXJ1713_F"
        self.description = "Total force RX J1713"
        self.metadata = {}
        self.metadata['source'] = 'source160.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return 6.25e-5 * (-3.40e172)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{RXJ1713_FTerm}",
            description=self.description
        )


class RXJ1713_CompressedTerm:
    """
    Compressed g RX J1713
    
    Source: MAIN_1_CoAnQi.cpp (lines 19369-19379)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "RXJ1713_Compressed"
        self.description = "Compressed g RX J1713"
        self.metadata = {}
        self.metadata['source'] = 'source160.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return (5e30 / 2e17) * 1e-10
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{RXJ1713_CompressedTerm}",
            description=self.description
        )


class BuoyancyAstro_IntegrandTerm:
    """
    Multi-system buoyancy integrand (5 systems)
    
    Source: MAIN_1_CoAnQi.cpp (lines 19384-19400)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "BuoyancyAstro_Integrand"
        self.description = "Multi-system buoyancy integrand (5 systems)"
        self.metadata = {}
        self.metadata['source'] = 'source161.cpp'
        self.metadata['system'] = 'J1610/PLCK/PSZ2/ASKAP/Sonif'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return M / (r * r * r)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{BuoyancyAstro_IntegrandTerm}",
            description=self.description
        )


class BuoyancyAstro_LENR_termTerm:
    """
    LENR for buoyancy systems
    
    Source: MAIN_1_CoAnQi.cpp (lines 19402-19409)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "BuoyancyAstro_LENR_term"
        self.description = "LENR for buoyancy systems"
        self.metadata = {}
        self.metadata['source'] = 'source161.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return 1e15
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{BuoyancyAstro_LENR_termTerm}",
            description=self.description
        )


class BuoyancyAstro_FTerm:
    """
    Total force buoyancy astro
    
    Source: MAIN_1_CoAnQi.cpp (lines 19411-19421)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "BuoyancyAstro_F"
        self.description = "Total force buoyancy astro"
        self.metadata = {}
        self.metadata['source'] = 'source161.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return 3.04e1 * (-1.35e172)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{BuoyancyAstro_FTerm}",
            description=self.description
        )


class BuoyancyAstro_CompressedTerm:
    """
    Compressed g buoyancy astro
    
    Source: MAIN_1_CoAnQi.cpp (lines 19423-19433)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "BuoyancyAstro_Compressed"
        self.description = "Compressed g buoyancy astro"
        self.metadata = {}
        self.metadata['source'] = 'source161.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return (2.785e30 / 3.09e15) * 1e-10
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{BuoyancyAstro_CompressedTerm}",
            description=self.description
        )


class BuoyancyCNB_IntegrandTerm:
    """
    CNB neutrino integrand Centaurus A
    
    Source: MAIN_1_CoAnQi.cpp (lines 19436-19450)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "BuoyancyCNB_Integrand"
        self.description = "CNB neutrino integrand Centaurus A"
        self.metadata = {}
        self.metadata['source'] = 'source162.cpp'
        self.metadata['system'] = 'Centaurus A CNB'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return 1.094e38 / (6.17e17 * 6.17e17 * 6.17e17)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{BuoyancyCNB_IntegrandTerm}",
            description=self.description
        )


class BuoyancyCNB_LENR_termTerm:
    """
    LENR for CNB
    
    Source: MAIN_1_CoAnQi.cpp (lines 19452-19459)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "BuoyancyCNB_LENR_term"
        self.description = "LENR for CNB"
        self.metadata = {}
        self.metadata['source'] = 'source162.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return 1e15
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{BuoyancyCNB_LENR_termTerm}",
            description=self.description
        )


class BuoyancyCNB_FTerm:
    """
    Total force CNB
    
    Source: MAIN_1_CoAnQi.cpp (lines 19461-19471)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "BuoyancyCNB_F"
        self.description = "Total force CNB"
        self.metadata = {}
        self.metadata['source'] = 'source162.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return 4.66e-1 * (-1.35e172)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{BuoyancyCNB_FTerm}",
            description=self.description
        )


class BuoyancyCNB_CompressedTerm:
    """
    Compressed g CNB
    
    Source: MAIN_1_CoAnQi.cpp (lines 19473-19483)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "BuoyancyCNB_Compressed"
        self.description = "Compressed g CNB"
        self.metadata = {}
        self.metadata['source'] = 'source162.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return (1.094e38 / 6.17e17) * 1e-10
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{BuoyancyCNB_CompressedTerm}",
            description=self.description
        )


class AstroSystems163_IntegrandTerm:
    """
    NGC/AT2024tvd integrand
    
    Source: MAIN_1_CoAnQi.cpp (lines 19486-19500)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "AstroSystems163_Integrand"
        self.description = "NGC/AT2024tvd integrand"
        self.metadata = {}
        self.metadata['source'] = 'source163.cpp'
        self.metadata['system'] = 'NGC multi'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return 5e40 / (5e20 * 5e20 * 5e20)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{AstroSystems163_IntegrandTerm}",
            description=self.description
        )


class AstroSystems163_LENR_termTerm:
    """
    LENR for NGC multi
    
    Source: MAIN_1_CoAnQi.cpp (lines 19502-19509)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "AstroSystems163_LENR_term"
        self.description = "LENR for NGC multi"
        self.metadata = {}
        self.metadata['source'] = 'source163.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return 1e15
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{AstroSystems163_LENR_termTerm}",
            description=self.description
        )


class AstroSystems163_FTerm:
    """
    Total force NGC multi
    
    Source: MAIN_1_CoAnQi.cpp (lines 19511-19521)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "AstroSystems163_F"
        self.description = "Total force NGC multi"
        self.metadata = {}
        self.metadata['source'] = 'source163.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return 4.00e-1 * (-1.35e172)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{AstroSystems163_FTerm}",
            description=self.description
        )


class AstroSystems163_CompressedTerm:
    """
    Compressed g NGC multi
    
    Source: MAIN_1_CoAnQi.cpp (lines 19523-19533)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "AstroSystems163_Compressed"
        self.description = "Compressed g NGC multi"
        self.metadata = {}
        self.metadata['source'] = 'source163.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return (5e40 / 5e20) * 1e-10
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{AstroSystems163_CompressedTerm}",
            description=self.description
        )


class NebulaTriadic_IntegrandTerm:
    """
    Nebula triadic integrand (gas ionization)
    
    Source: MAIN_1_CoAnQi.cpp (lines 19536-19550)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "NebulaTriadic_Integrand"
        self.description = "Nebula triadic integrand (gas ionization)"
        self.metadata = {}
        self.metadata['source'] = 'source164.cpp'
        self.metadata['system'] = 'NGC Nebulae'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return 2e36 / (4e17 * 4e17 * 4e17)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{NebulaTriadic_IntegrandTerm}",
            description=self.description
        )


class NebulaTriadic_LENR_termTerm:
    """
    LENR for nebulae
    
    Source: MAIN_1_CoAnQi.cpp (lines 19552-19559)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "NebulaTriadic_LENR_term"
        self.description = "LENR for nebulae"
        self.metadata = {}
        self.metadata['source'] = 'source164.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return 1e12
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{NebulaTriadic_LENR_termTerm}",
            description=self.description
        )


class NebulaTriadic_FTerm:
    """
    Total force nebula triadic
    
    Source: MAIN_1_CoAnQi.cpp (lines 19561-19571)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "NebulaTriadic_F"
        self.description = "Total force nebula triadic"
        self.metadata = {}
        self.metadata['source'] = 'source164.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return 3.13e-2 * (-3.40e172)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{NebulaTriadic_FTerm}",
            description=self.description
        )


class NebulaTriadic_CompressedTerm:
    """
    Compressed g nebula triadic
    
    Source: MAIN_1_CoAnQi.cpp (lines 19573-19583)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "NebulaTriadic_Compressed"
        self.description = "Compressed g nebula triadic"
        self.metadata = {}
        self.metadata['source'] = 'source164.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return (2e36 / 4e17) * 1e-10
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{NebulaTriadic_CompressedTerm}",
            description=self.description
        )


class BuoyancyModule_IntegrandTerm:
    """
    M74/Eagle/M84 integrand
    
    Source: MAIN_1_CoAnQi.cpp (lines 19586-19600)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "BuoyancyModule_Integrand"
        self.description = "M74/Eagle/M84 integrand"
        self.metadata = {}
        self.metadata['source'] = 'source165.cpp'
        self.metadata['system'] = 'M74/M16/M84'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return 7.17e41 / (9.46e20 * 9.46e20 * 9.46e20)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{BuoyancyModule_IntegrandTerm}",
            description=self.description
        )


class BuoyancyModule_LENR_termTerm:
    """
    LENR for M74/M16/M84
    
    Source: MAIN_1_CoAnQi.cpp (lines 19602-19609)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "BuoyancyModule_LENR_term"
        self.description = "LENR for M74/M16/M84"
        self.metadata = {}
        self.metadata['source'] = 'source165.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return 1e15
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{BuoyancyModule_LENR_termTerm}",
            description=self.description
        )


class BuoyancyModule_FTerm:
    """
    Total force M74/M16/M84
    
    Source: MAIN_1_CoAnQi.cpp (lines 19611-19621)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "BuoyancyModule_F"
        self.description = "Total force M74/M16/M84"
        self.metadata = {}
        self.metadata['source'] = 'source165.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return 8.47e-1 * (-1.35e172)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{BuoyancyModule_FTerm}",
            description=self.description
        )


class BuoyancyModule_CompressedTerm:
    """
    Compressed g M74/M16/M84
    
    Source: MAIN_1_CoAnQi.cpp (lines 19623-19633)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "BuoyancyModule_Compressed"
        self.description = "Compressed g M74/M16/M84"
        self.metadata = {}
        self.metadata['source'] = 'source165.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return (7.17e41 / 9.46e20) * 1e-10
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{BuoyancyModule_CompressedTerm}",
            description=self.description
        )


class UQFF8Astro_IntegrandTerm:
    """
    8+1 astro systems integrand (26 quantum states)
    
    Source: MAIN_1_CoAnQi.cpp (lines 19636-19650)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "UQFF8Astro_Integrand"
        self.description = "8+1 astro systems integrand (26 quantum states)"
        self.metadata = {}
        self.metadata['source'] = 'source166.cpp'
        self.metadata['system'] = '9 astro systems'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return 1e41 / (7e20 * 7e20 * 7e20)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{UQFF8Astro_IntegrandTerm}",
            description=self.description
        )


class UQFF8Astro_LENR_termTerm:
    """
    LENR for 8 astro
    
    Source: MAIN_1_CoAnQi.cpp (lines 19652-19659)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "UQFF8Astro_LENR_term"
        self.description = "LENR for 8 astro"
        self.metadata = {}
        self.metadata['source'] = 'source166.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return 1e15
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{UQFF8Astro_LENR_termTerm}",
            description=self.description
        )


class UQFF8Astro_FTerm:
    """
    Total force 8 astro
    
    Source: MAIN_1_CoAnQi.cpp (lines 19661-19671)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "UQFF8Astro_F"
        self.description = "Total force 8 astro"
        self.metadata = {}
        self.metadata['source'] = 'source166.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return 2.92e-1 * (-1.35e172)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{UQFF8Astro_FTerm}",
            description=self.description
        )


class UQFF8Astro_CompressedTerm:
    """
    Compressed g 8 astro
    
    Source: MAIN_1_CoAnQi.cpp (lines 19673-19683)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "UQFF8Astro_Compressed"
        self.description = "Compressed g 8 astro"
        self.metadata = {}
        self.metadata['source'] = 'source166.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return (1e41 / 7e20) * 1e-10
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{UQFF8Astro_CompressedTerm}",
            description=self.description
        )


class UQFFCore_IntegrandTerm:
    """
    UQFF Core M82/IC418/Canis
    
    Source: MAIN_1_CoAnQi.cpp (lines 19686-19700)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "UQFFCore_Integrand"
        self.description = "UQFF Core M82/IC418/Canis"
        self.metadata = {}
        self.metadata['source'] = 'source167.cpp'
        self.metadata['system'] = 'M82 starburst'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return 3e41 / (6e20 * 6e20 * 6e20)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{UQFFCore_IntegrandTerm}",
            description=self.description
        )


class UQFFCore_LENR_termTerm:
    """
    LENR for UQFF Core
    
    Source: MAIN_1_CoAnQi.cpp (lines 19702-19709)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "UQFFCore_LENR_term"
        self.description = "LENR for UQFF Core"
        self.metadata = {}
        self.metadata['source'] = 'source167.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return 1e15
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{UQFFCore_LENR_termTerm}",
            description=self.description
        )


class UQFFCore_FTerm:
    """
    Total force UQFF Core
    
    Source: MAIN_1_CoAnQi.cpp (lines 19711-19721)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "UQFFCore_F"
        self.description = "Total force UQFF Core"
        self.metadata = {}
        self.metadata['source'] = 'source167.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return 1.39e-1 * (-1.35e172)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{UQFFCore_FTerm}",
            description=self.description
        )


class UQFFCore_CompressedTerm:
    """
    Compressed g UQFF Core
    
    Source: MAIN_1_CoAnQi.cpp (lines 19723-19733)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "UQFFCore_Compressed"
        self.description = "Compressed g UQFF Core"
        self.metadata = {}
        self.metadata['source'] = 'source167.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return (3e41 / 6e20) * 1e-10
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{UQFFCore_CompressedTerm}",
            description=self.description
        )


class Buoyancy168_IntegrandTerm:
    """
    SN 1006 buoyancy integrand
    
    Source: MAIN_1_CoAnQi.cpp (lines 19736-19750)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "Buoyancy168_Integrand"
        self.description = "SN 1006 buoyancy integrand"
        self.metadata = {}
        self.metadata['source'] = 'source168.cpp'
        self.metadata['system'] = 'SN 1006 SNR'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return 1e31 / (1e18 * 1e18 * 1e18)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{Buoyancy168_IntegrandTerm}",
            description=self.description
        )


class Buoyancy168_LENR_termTerm:
    """
    LENR for SN 1006
    
    Source: MAIN_1_CoAnQi.cpp (lines 19752-19759)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "Buoyancy168_LENR_term"
        self.description = "LENR for SN 1006"
        self.metadata = {}
        self.metadata['source'] = 'source168.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return 1e12
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{Buoyancy168_LENR_termTerm}",
            description=self.description
        )


class Buoyancy168_FTerm:
    """
    Total force SN 1006
    
    Source: MAIN_1_CoAnQi.cpp (lines 19761-19771)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "Buoyancy168_F"
        self.description = "Total force SN 1006"
        self.metadata = {}
        self.metadata['source'] = 'source168.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return 1.00e-5 * (-3.40e172)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{Buoyancy168_FTerm}",
            description=self.description
        )


class Buoyancy168_CompressedTerm:
    """
    Compressed g SN 1006
    
    Source: MAIN_1_CoAnQi.cpp (lines 19773-19783)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "Buoyancy168_Compressed"
        self.description = "Compressed g SN 1006"
        self.metadata = {}
        self.metadata['source'] = 'source168.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return (1e31 / 1e18) * 1e-10
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{Buoyancy168_CompressedTerm}",
            description=self.description
        )


class CassiniBuoyancy_IntegrandTerm:
    """
    Cassini Saturn U_Bi/U_Ii/U_Mi
    
    Source: MAIN_1_CoAnQi.cpp (lines 19786-19800)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "CassiniBuoyancy_Integrand"
        self.description = "Cassini Saturn U_Bi/U_Ii/U_Mi"
        self.metadata = {}
        self.metadata['source'] = 'source169.cpp'
        self.metadata['system'] = 'Cassini Saturn'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return 5.68e26 / (5.82e7 * 5.82e7 * 5.82e7)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{CassiniBuoyancy_IntegrandTerm}",
            description=self.description
        )


class CassiniBuoyancy_LENR_termTerm:
    """
    LENR for Cassini
    
    Source: MAIN_1_CoAnQi.cpp (lines 19802-19809)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "CassiniBuoyancy_LENR_term"
        self.description = "LENR for Cassini"
        self.metadata = {}
        self.metadata['source'] = 'source169.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return 1e12
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{CassiniBuoyancy_LENR_termTerm}",
            description=self.description
        )


class CassiniBuoyancy_FTerm:
    """
    Total force Cassini
    
    Source: MAIN_1_CoAnQi.cpp (lines 19811-19821)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "CassiniBuoyancy_F"
        self.description = "Total force Cassini"
        self.metadata = {}
        self.metadata['source'] = 'source169.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return 2.88e12 * (-3.40e172)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{CassiniBuoyancy_FTerm}",
            description=self.description
        )


class CassiniBuoyancy_CompressedTerm:
    """
    Compressed g Cassini
    
    Source: MAIN_1_CoAnQi.cpp (lines 19823-19833)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "CassiniBuoyancy_Compressed"
        self.description = "Compressed g Cassini"
        self.metadata = {}
        self.metadata['source'] = 'source169.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return (5.68e26 / 5.82e7) * 1e-10
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{CassiniBuoyancy_CompressedTerm}",
            description=self.description
        )


class MultiAstro_IntegrandTerm:
    """
    11 multi-astro systems (DPM + ACP)
    
    Source: MAIN_1_CoAnQi.cpp (lines 19836-19850)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "MultiAstro_Integrand"
        self.description = "11 multi-astro systems (DPM + ACP)"
        self.metadata = {}
        self.metadata['source'] = 'source170.cpp'
        self.metadata['system'] = '11 multi-astro'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return 1e41 / (8e20 * 8e20 * 8e20)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{MultiAstro_IntegrandTerm}",
            description=self.description
        )


class MultiAstro_LENR_termTerm:
    """
    LENR for multi-astro
    
    Source: MAIN_1_CoAnQi.cpp (lines 19852-19859)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "MultiAstro_LENR_term"
        self.description = "LENR for multi-astro"
        self.metadata = {}
        self.metadata['source'] = 'source170.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return 1e15
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{MultiAstro_LENR_termTerm}",
            description=self.description
        )


class MultiAstro_FTerm:
    """
    Total force multi-astro
    
    Source: MAIN_1_CoAnQi.cpp (lines 19861-19871)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "MultiAstro_F"
        self.description = "Total force multi-astro"
        self.metadata = {}
        self.metadata['source'] = 'source170.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return 1.95e-1 * (-1.35e172)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{MultiAstro_FTerm}",
            description=self.description
        )


class MultiAstro_CompressedTerm:
    """
    Compressed g multi-astro
    
    Source: MAIN_1_CoAnQi.cpp (lines 19873-19883)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "MultiAstro_Compressed"
        self.description = "Compressed g multi-astro"
        self.metadata = {}
        self.metadata['source'] = 'source170.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return (1e41 / 8e20) * 1e-10
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{MultiAstro_CompressedTerm}",
            description=self.description
        )


class EightAstro_IntegrandTerm:
    """
    8 LMC/NGC systems (AFGL/NGC346)
    
    Source: MAIN_1_CoAnQi.cpp (lines 19886-19900)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "EightAstro_Integrand"
        self.description = "8 LMC/NGC systems (AFGL/NGC346)"
        self.metadata = {}
        self.metadata['source'] = 'source171.cpp'
        self.metadata['system'] = '8 LMC/NGC'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return 3e37 / (4.5e18 * 4.5e18 * 4.5e18)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{EightAstro_IntegrandTerm}",
            description=self.description
        )


class EightAstro_LENR_termTerm:
    """
    LENR for 8 astro
    
    Source: MAIN_1_CoAnQi.cpp (lines 19902-19909)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "EightAstro_LENR_term"
        self.description = "LENR for 8 astro"
        self.metadata = {}
        self.metadata['source'] = 'source171.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return 1e15
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{EightAstro_LENR_termTerm}",
            description=self.description
        )


class EightAstro_FTerm:
    """
    Total force 8 astro
    
    Source: MAIN_1_CoAnQi.cpp (lines 19911-19921)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "EightAstro_F"
        self.description = "Total force 8 astro"
        self.metadata = {}
        self.metadata['source'] = 'source171.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return 3.29e-2 * (-1.35e172)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{EightAstro_FTerm}",
            description=self.description
        )


class EightAstro_CompressedTerm:
    """
    Compressed g 8 astro
    
    Source: MAIN_1_CoAnQi.cpp (lines 19923-19933)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "EightAstro_Compressed"
        self.description = "Compressed g 8 astro"
        self.metadata = {}
        self.metadata['source'] = 'source171.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return (3e37 / 4.5e18) * 1e-10
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{EightAstro_CompressedTerm}",
            description=self.description
        )


class NineteenAstro_IntegrandTerm:
    """
    19 systems 26D polynomial (NGC2264/UGC10214/M42/Tarantula)
    
    Source: MAIN_1_CoAnQi.cpp (lines 19936-19950)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "NineteenAstro_Integrand"
        self.description = "19 systems 26D polynomial (NGC2264/UGC10214/M42/Tarantula)"
        self.metadata = {}
        self.metadata['source'] = 'source172.cpp'
        self.metadata['system'] = '19 systems 26D'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return 1e41 / (1e21 * 1e21 * 1e21)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{NineteenAstro_IntegrandTerm}",
            description=self.description
        )


class NineteenAstro_LENR_termTerm:
    """
    LENR for 19 astro (26 quantum states)
    
    Source: MAIN_1_CoAnQi.cpp (lines 19952-19959)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "NineteenAstro_LENR_term"
        self.description = "LENR for 19 astro (26 quantum states)"
        self.metadata = {}
        self.metadata['source'] = 'source172.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return 1e15
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{NineteenAstro_LENR_termTerm}",
            description=self.description
        )


class NineteenAstro_FTerm:
    """
    Total force 19 astro 26D
    
    Source: MAIN_1_CoAnQi.cpp (lines 19961-19971)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "NineteenAstro_F"
        self.description = "Total force 19 astro 26D"
        self.metadata = {}
        self.metadata['source'] = 'source172.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return 1.00e-2 * (-1.35e172)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{NineteenAstro_FTerm}",
            description=self.description
        )


class NineteenAstro_CompressedTerm:
    """
    Compressed g 19 astro 26D
    
    Source: MAIN_1_CoAnQi.cpp (lines 19973-19983)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "NineteenAstro_Compressed"
        self.description = "Compressed g 19 astro 26D"
        self.metadata = {}
        self.metadata['source'] = 'source172.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return (1e41 / 1e21) * 1e-10
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{NineteenAstro_CompressedTerm}",
            description=self.description
        )


class WolframFieldUnity_IntegrandTerm:
    """
    Wolfram hypergraph emergent spacetime (26 quantum states, PI infinity decoder)
    
    Source: MAIN_1_CoAnQi.cpp (lines 19986-20000)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "WolframFieldUnity_Integrand"
        self.description = "Wolfram hypergraph emergent spacetime (26 quantum states, PI infinity decoder)"
        self.metadata = {}
        self.metadata['source'] = 'source173.cpp'
        self.metadata['system'] = 'Wolfram Hypergraph'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return 1e40 / (1e20 * 1e20 * 1e20)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{WolframFieldUnity_IntegrandTerm}",
            description=self.description
        )


class WolframFieldUnity_LENR_termTerm:
    """
    LENR for Wolfram hypergraph (causal invariance)
    
    Source: MAIN_1_CoAnQi.cpp (lines 20002-20009)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "WolframFieldUnity_LENR_term"
        self.description = "LENR for Wolfram hypergraph (causal invariance)"
        self.metadata = {}
        self.metadata['source'] = 'source173.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return 1e15
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{WolframFieldUnity_LENR_termTerm}",
            description=self.description
        )


class WolframFieldUnity_FTerm:
    """
    Total force Wolfram (multiway quantum evolution, sacred time constants)
    
    Source: MAIN_1_CoAnQi.cpp (lines 20011-20021)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "WolframFieldUnity_F"
        self.description = "Total force Wolfram (multiway quantum evolution, sacred time constants)"
        self.metadata = {}
        self.metadata['source'] = 'source173.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return 1.00e0 * (-1.35e172)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{WolframFieldUnity_FTerm}",
            description=self.description
        )


class WolframFieldUnity_CompressedTerm:
    """
    Compressed g Wolfram (312-digit PI decoder, Mayan/Biblical time)
    
    Source: MAIN_1_CoAnQi.cpp (lines 20023-20033)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "WolframFieldUnity_Compressed"
        self.description = "Compressed g Wolfram (312-digit PI decoder, Mayan/Biblical time)"
        self.metadata = {}
        self.metadata['source'] = 'source173.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return (1e40 / 1e20) * 1e-10
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{WolframFieldUnity_CompressedTerm}",
            description=self.description
        )


class J1610_DPM_resonanceTerm:
    """
    J1610 DPM magnetic resonance g·μB·B₀/(ℏ·ω₀)
    
    Source: MAIN_1_CoAnQi.cpp (lines 20042-20057)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "J1610_DPM_resonance"
        self.description = "J1610 DPM magnetic resonance g·μB·B₀/(ℏ·ω₀)"
        self.metadata = {}
        self.metadata['source'] = 'source141.cpp'
        self.metadata['method'] = 'computeDPM_resonance'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        g = 2.0, muB = 9.274e-24, B0 = 1e-5, hbar = 1.0546e-34, omega0 = 1e-15
        return (g * muB * B0) / (hbar * omega0)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{J1610_DPM_resonanceTerm}",
            description=self.description
        )


class J1610_Q_waveTerm:
    """
    J1610 quantum wave function ψ(t)
    
    Source: MAIN_1_CoAnQi.cpp (lines 20059-20073)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "J1610_Q_wave"
        self.description = "J1610 quantum wave function ψ(t)"
        self.metadata = {}
        self.metadata['source'] = 'source141.cpp'
        self.metadata['method'] = 'computeQ_wave'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        result = 0.0  # TODO: Implement computation
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{J1610_Q_waveTerm}",
            description=self.description
        )


class J1610_Ub1Term:
    """
    J1610 universal buoyancy U_b1
    
    Source: MAIN_1_CoAnQi.cpp (lines 20075-20089)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "J1610_Ub1"
        self.description = "J1610 universal buoyancy U_b1"
        self.metadata = {}
        self.metadata['source'] = 'source141.cpp'
        self.metadata['method'] = 'computeUb1'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return 0.6 * 1e-6 * 7.09e-36
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{J1610_Ub1Term}",
            description=self.description
        )


class J1610_UiTerm:
    """
    J1610 universal inertia U_i(t)
    
    Source: MAIN_1_CoAnQi.cpp (lines 20091-20105)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "J1610_Ui"
        self.description = "J1610 universal inertia U_i(t)"
        self.metadata = {}
        self.metadata['source'] = 'source141.cpp'
        self.metadata['method'] = 'computeUi'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        result = 0.0  # TODO: Implement computation
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{J1610_UiTerm}",
            description=self.description
        )


class J1610_X2Term:
    """
    J1610 quadratic root x₂ for UQFF
    
    Source: MAIN_1_CoAnQi.cpp (lines 20107-20121)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "J1610_X2"
        self.description = "J1610 quadratic root x₂ for UQFF"
        self.metadata = {}
        self.metadata['source'] = 'source141.cpp'
        self.metadata['method'] = 'computeX2'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return -1.35e172
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{J1610_X2Term}",
            description=self.description
        )


class J1610_ActivationTerm:
    """
    J1610 activation force at 300 Hz
    
    Source: MAIN_1_CoAnQi.cpp (lines 20123-20137)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "J1610_Activation"
        self.description = "J1610 activation force at 300 Hz"
        self.metadata = {}
        self.metadata['source'] = 'source141.cpp'
        self.metadata['term'] = 'F_act'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        result = 0.0  # TODO: Implement computation
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{J1610_ActivationTerm}",
            description=self.description
        )


class J1610_DirectedEnergyTerm:
    """
    J1610 directed energy coupling to X-ray luminosity
    
    Source: MAIN_1_CoAnQi.cpp (lines 20139-20153)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "J1610_DirectedEnergy"
        self.description = "J1610 directed energy coupling to X-ray luminosity"
        self.metadata = {}
        self.metadata['source'] = 'source141.cpp'
        self.metadata['term'] = 'F_DE'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return 1e-30 * 1e39
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{J1610_DirectedEnergyTerm}",
            description=self.description
        )


class J1610_NeutronTerm:
    """
    J1610 neutron coupling k·σ
    
    Source: MAIN_1_CoAnQi.cpp (lines 20155-20169)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "J1610_Neutron"
        self.description = "J1610 neutron coupling k·σ"
        self.metadata = {}
        self.metadata['source'] = 'source141.cpp'
        self.metadata['term'] = 'F_neutron'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return 1e10 * 1e-4
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{J1610_NeutronTerm}",
            description=self.description
        )


class J1610_RelativisticTerm:
    """
    J1610 relativistic coherence (E_cm_astro/E_cm)²
    
    Source: MAIN_1_CoAnQi.cpp (lines 20171-20185)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "J1610_Relativistic"
        self.description = "J1610 relativistic coherence (E_cm_astro/E_cm)²"
        self.metadata = {}
        self.metadata['source'] = 'source141.cpp'
        self.metadata['term'] = 'F_rel'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        result = 0.0  # TODO: Implement computation
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{J1610_RelativisticTerm}",
            description=self.description
        )


class J1610_NeutrinoTerm:
    """
    J1610 neutrino background force
    
    Source: MAIN_1_CoAnQi.cpp (lines 20187-20201)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "J1610_Neutrino"
        self.description = "J1610 neutrino background force"
        self.metadata = {}
        self.metadata['source'] = 'source141.cpp'
        self.metadata['term'] = 'F_neutrino'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return 9.07e-42
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{J1610_NeutrinoTerm}",
            description=self.description
        )


class M87Jet_DPM_resonanceTerm:
    """
    M87 jet DPM resonance
    
    Source: MAIN_1_CoAnQi.cpp (lines 20204-20214)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "M87Jet_DPM_resonance"
        self.description = "M87 jet DPM resonance"
        self.metadata = {}
        self.metadata['source'] = 'source145.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return (2.0 * 9.274e-24 * 1e-5) / (1.0546e-34 * 1e-15)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{M87Jet_DPM_resonanceTerm}",
            description=self.description
        )


class M87Jet_Q_waveTerm:
    """
    M87 quantum wave
    
    Source: MAIN_1_CoAnQi.cpp (lines 20215-20225)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "M87Jet_Q_wave"
        self.description = "M87 quantum wave"
        self.metadata = {}
        self.metadata['source'] = 'source145.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        result = 0.0  # TODO: Implement computation
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{M87Jet_Q_waveTerm}",
            description=self.description
        )


class M87Jet_Ub1Term:
    """
    M87 U_b1
    
    Source: MAIN_1_CoAnQi.cpp (lines 20226-20236)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "M87Jet_Ub1"
        self.description = "M87 U_b1"
        self.metadata = {}
        self.metadata['source'] = 'source145.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return 0.6 * 1e-6 * 7.09e-36
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{M87Jet_Ub1Term}",
            description=self.description
        )


class M87Jet_UiTerm:
    """
    M87 inertia
    
    Source: MAIN_1_CoAnQi.cpp (lines 20237-20247)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "M87Jet_Ui"
        self.description = "M87 inertia"
        self.metadata = {}
        self.metadata['source'] = 'source145.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        result = 0.0  # TODO: Implement computation
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{M87Jet_UiTerm}",
            description=self.description
        )


class M87Jet_X2Term:
    """
    M87 x₂ root
    
    Source: MAIN_1_CoAnQi.cpp (lines 20248-20258)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "M87Jet_X2"
        self.description = "M87 x₂ root"
        self.metadata = {}
        self.metadata['source'] = 'source145.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return -1.35e172
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{M87Jet_X2Term}",
            description=self.description
        )


class M87Jet_ActivationTerm:
    """
    M87 activation
    
    Source: MAIN_1_CoAnQi.cpp (lines 20259-20269)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "M87Jet_Activation"
        self.description = "M87 activation"
        self.metadata = {}
        self.metadata['source'] = 'source145.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        result = 0.0  # TODO: Implement computation
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{M87Jet_ActivationTerm}",
            description=self.description
        )


class M87Jet_DirectedEnergyTerm:
    """
    M87 directed energy
    
    Source: MAIN_1_CoAnQi.cpp (lines 20270-20280)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "M87Jet_DirectedEnergy"
        self.description = "M87 directed energy"
        self.metadata = {}
        self.metadata['source'] = 'source145.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return 1e-30 * 1e34
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{M87Jet_DirectedEnergyTerm}",
            description=self.description
        )


class M87Jet_NeutronTerm:
    """
    M87 neutron
    
    Source: MAIN_1_CoAnQi.cpp (lines 20281-20291)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "M87Jet_Neutron"
        self.description = "M87 neutron"
        self.metadata = {}
        self.metadata['source'] = 'source145.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return 1e10 * 1e-4
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{M87Jet_NeutronTerm}",
            description=self.description
        )


class M87Jet_RelativisticTerm:
    """
    M87 relativistic
    
    Source: MAIN_1_CoAnQi.cpp (lines 20292-20302)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "M87Jet_Relativistic"
        self.description = "M87 relativistic"
        self.metadata = {}
        self.metadata['source'] = 'source145.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        result = 0.0  # TODO: Implement computation
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{M87Jet_RelativisticTerm}",
            description=self.description
        )


class M87Jet_NeutrinoTerm:
    """
    M87 neutrino
    
    Source: MAIN_1_CoAnQi.cpp (lines 20303-20313)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "M87Jet_Neutrino"
        self.description = "M87 neutrino"
        self.metadata = {}
        self.metadata['source'] = 'source145.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return 9.07e-42
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{M87Jet_NeutrinoTerm}",
            description=self.description
        )


class SgrAStar_DPM_resonanceTerm:
    """
    Sgr A* DPM magnetic resonance
    
    Source: MAIN_1_CoAnQi.cpp (lines 20316-20330)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "SgrAStar_DPM_resonance"
        self.description = "Sgr A* DPM magnetic resonance"
        self.metadata = {}
        self.metadata['source'] = 'source149.cpp'
        self.metadata['system'] = 'Galactic Center'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return (2.0 * 9.274e-24 * 1e-5) / (1.0546e-34 * 1e-15)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{SgrAStar_DPM_resonanceTerm}",
            description=self.description
        )


class SgrAStar_Q_waveTerm:
    """
    Sgr A* quantum wave
    
    Source: MAIN_1_CoAnQi.cpp (lines 20331-20341)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "SgrAStar_Q_wave"
        self.description = "Sgr A* quantum wave"
        self.metadata = {}
        self.metadata['source'] = 'source149.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        result = 0.0  # TODO: Implement computation
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{SgrAStar_Q_waveTerm}",
            description=self.description
        )


class SgrAStar_Ub1Term:
    """
    Sgr A* U_b1
    
    Source: MAIN_1_CoAnQi.cpp (lines 20342-20352)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "SgrAStar_Ub1"
        self.description = "Sgr A* U_b1"
        self.metadata = {}
        self.metadata['source'] = 'source149.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return 0.6 * 1e-6 * 7.09e-36
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{SgrAStar_Ub1Term}",
            description=self.description
        )


class SgrAStar_UiTerm:
    """
    Sgr A* inertia
    
    Source: MAIN_1_CoAnQi.cpp (lines 20353-20363)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "SgrAStar_Ui"
        self.description = "Sgr A* inertia"
        self.metadata = {}
        self.metadata['source'] = 'source149.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        result = 0.0  # TODO: Implement computation
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{SgrAStar_UiTerm}",
            description=self.description
        )


class SgrAStar_X2Term:
    """
    Sgr A* quadratic root
    
    Source: MAIN_1_CoAnQi.cpp (lines 20364-20374)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "SgrAStar_X2"
        self.description = "Sgr A* quadratic root"
        self.metadata = {}
        self.metadata['source'] = 'source149.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return -1.35e172
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{SgrAStar_X2Term}",
            description=self.description
        )


class SgrAStar_ActivationTerm:
    """
    Sgr A* activation
    
    Source: MAIN_1_CoAnQi.cpp (lines 20375-20385)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "SgrAStar_Activation"
        self.description = "Sgr A* activation"
        self.metadata = {}
        self.metadata['source'] = 'source149.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        result = 0.0  # TODO: Implement computation
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{SgrAStar_ActivationTerm}",
            description=self.description
        )


class SgrAStar_DirectedEnergyTerm:
    """
    Sgr A* directed energy
    
    Source: MAIN_1_CoAnQi.cpp (lines 20386-20396)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "SgrAStar_DirectedEnergy"
        self.description = "Sgr A* directed energy"
        self.metadata = {}
        self.metadata['source'] = 'source149.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return 1e-30 * 1e36
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{SgrAStar_DirectedEnergyTerm}",
            description=self.description
        )


class SgrAStar_NeutronTerm:
    """
    Sgr A* neutron
    
    Source: MAIN_1_CoAnQi.cpp (lines 20397-20407)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "SgrAStar_Neutron"
        self.description = "Sgr A* neutron"
        self.metadata = {}
        self.metadata['source'] = 'source149.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return 1e10 * 1e-4
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{SgrAStar_NeutronTerm}",
            description=self.description
        )


class SgrAStar_RelativisticTerm:
    """
    Sgr A* relativistic
    
    Source: MAIN_1_CoAnQi.cpp (lines 20408-20418)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "SgrAStar_Relativistic"
        self.description = "Sgr A* relativistic"
        self.metadata = {}
        self.metadata['source'] = 'source149.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        result = 0.0  # TODO: Implement computation
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{SgrAStar_RelativisticTerm}",
            description=self.description
        )


class SgrAStar_NeutrinoTerm:
    """
    Sgr A* neutrino
    
    Source: MAIN_1_CoAnQi.cpp (lines 20419-20429)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "SgrAStar_Neutrino"
        self.description = "Sgr A* neutrino"
        self.metadata = {}
        self.metadata['source'] = 'source149.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return 9.07e-42
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{SgrAStar_NeutrinoTerm}",
            description=self.description
        )


class NGC1365_DPM_resonanceTerm:
    """
    NGC1365 DPM resonance
    
    Source: MAIN_1_CoAnQi.cpp (lines 20432-20439)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "NGC1365_DPM_resonance"
        self.description = "NGC1365 DPM resonance"
        self.metadata = {}
        self.metadata['source'] = 'source146.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return 1.76e6
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{NGC1365_DPM_resonanceTerm}",
            description=self.description
        )


class NGC1365_Q_waveTerm:
    """
    NGC1365 quantum wave
    
    Source: MAIN_1_CoAnQi.cpp (lines 20440-20447)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "NGC1365_Q_wave"
        self.description = "NGC1365 quantum wave"
        self.metadata = {}
        self.metadata['source'] = 'source146.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        result = 0.0  # TODO: Implement computation
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{NGC1365_Q_waveTerm}",
            description=self.description
        )


class NGC1365_Ub1Term:
    """
    NGC1365 U_b1
    
    Source: MAIN_1_CoAnQi.cpp (lines 20448-20455)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "NGC1365_Ub1"
        self.description = "NGC1365 U_b1"
        self.metadata = {}
        self.metadata['source'] = 'source146.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return 4.25e-42
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{NGC1365_Ub1Term}",
            description=self.description
        )


class NGC1365_UiTerm:
    """
    NGC1365 inertia
    
    Source: MAIN_1_CoAnQi.cpp (lines 20456-20463)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "NGC1365_Ui"
        self.description = "NGC1365 inertia"
        self.metadata = {}
        self.metadata['source'] = 'source146.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        result = 0.0  # TODO: Implement computation
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{NGC1365_UiTerm}",
            description=self.description
        )


class NGC1365_X2Term:
    """
    NGC1365 x₂
    
    Source: MAIN_1_CoAnQi.cpp (lines 20464-20471)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "NGC1365_X2"
        self.description = "NGC1365 x₂"
        self.metadata = {}
        self.metadata['source'] = 'source146.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return -1.35e172
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{NGC1365_X2Term}",
            description=self.description
        )


class NGC1365_ActivationTerm:
    """
    NGC1365 activation
    
    Source: MAIN_1_CoAnQi.cpp (lines 20472-20479)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "NGC1365_Activation"
        self.description = "NGC1365 activation"
        self.metadata = {}
        self.metadata['source'] = 'source146.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        result = 0.0  # TODO: Implement computation
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{NGC1365_ActivationTerm}",
            description=self.description
        )


class NGC1365_DirectedEnergyTerm:
    """
    NGC1365 directed energy
    
    Source: MAIN_1_CoAnQi.cpp (lines 20480-20487)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "NGC1365_DirectedEnergy"
        self.description = "NGC1365 directed energy"
        self.metadata = {}
        self.metadata['source'] = 'source146.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return 1e-30 * 1e40
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{NGC1365_DirectedEnergyTerm}",
            description=self.description
        )


class NGC1365_NeutronTerm:
    """
    NGC1365 neutron
    
    Source: MAIN_1_CoAnQi.cpp (lines 20488-20495)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "NGC1365_Neutron"
        self.description = "NGC1365 neutron"
        self.metadata = {}
        self.metadata['source'] = 'source146.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return 1e6
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{NGC1365_NeutronTerm}",
            description=self.description
        )


class NGC1365_RelativisticTerm:
    """
    NGC1365 relativistic
    
    Source: MAIN_1_CoAnQi.cpp (lines 20496-20503)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "NGC1365_Relativistic"
        self.description = "NGC1365 relativistic"
        self.metadata = {}
        self.metadata['source'] = 'source146.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return 1.69e26
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{NGC1365_RelativisticTerm}",
            description=self.description
        )


class NGC1365_NeutrinoTerm:
    """
    NGC1365 neutrino
    
    Source: MAIN_1_CoAnQi.cpp (lines 20504-20511)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "NGC1365_Neutrino"
        self.description = "NGC1365 neutrino"
        self.metadata = {}
        self.metadata['source'] = 'source146.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return 9.07e-42
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{NGC1365_NeutrinoTerm}",
            description=self.description
        )


class VelaPulsar_DPM_resonanceTerm:
    """
    Vela Pulsar DPM resonance (89ms period)
    
    Source: MAIN_1_CoAnQi.cpp (lines 20514-20521)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "VelaPulsar_DPM_resonance"
        self.description = "Vela Pulsar DPM resonance (89ms period)"
        self.metadata = {}
        self.metadata['source'] = 'source152.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return 1.76e6
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{VelaPulsar_DPM_resonanceTerm}",
            description=self.description
        )


class VelaPulsar_Q_waveTerm:
    """
    Vela quantum wave (11.2 Hz)
    
    Source: MAIN_1_CoAnQi.cpp (lines 20522-20529)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "VelaPulsar_Q_wave"
        self.description = "Vela quantum wave (11.2 Hz)"
        self.metadata = {}
        self.metadata['source'] = 'source152.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        result = 0.0  # TODO: Implement computation
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{VelaPulsar_Q_waveTerm}",
            description=self.description
        )


class VelaPulsar_Ub1Term:
    """
    Vela U_b1
    
    Source: MAIN_1_CoAnQi.cpp (lines 20530-20537)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "VelaPulsar_Ub1"
        self.description = "Vela U_b1"
        self.metadata = {}
        self.metadata['source'] = 'source152.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return 4.25e-42
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{VelaPulsar_Ub1Term}",
            description=self.description
        )


class VelaPulsar_UiTerm:
    """
    Vela inertia
    
    Source: MAIN_1_CoAnQi.cpp (lines 20538-20545)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "VelaPulsar_Ui"
        self.description = "Vela inertia"
        self.metadata = {}
        self.metadata['source'] = 'source152.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        result = 0.0  # TODO: Implement computation
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{VelaPulsar_UiTerm}",
            description=self.description
        )


class VelaPulsar_X2Term:
    """
    Vela x₂ (pulsar variant)
    
    Source: MAIN_1_CoAnQi.cpp (lines 20546-20553)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "VelaPulsar_X2"
        self.description = "Vela x₂ (pulsar variant)"
        self.metadata = {}
        self.metadata['source'] = 'source152.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return -3.40e172
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{VelaPulsar_X2Term}",
            description=self.description
        )


class VelaPulsar_ActivationTerm:
    """
    Vela activation (pulse frequency)
    
    Source: MAIN_1_CoAnQi.cpp (lines 20554-20561)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "VelaPulsar_Activation"
        self.description = "Vela activation (pulse frequency)"
        self.metadata = {}
        self.metadata['source'] = 'source152.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        result = 0.0  # TODO: Implement computation
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{VelaPulsar_ActivationTerm}",
            description=self.description
        )


class VelaPulsar_DirectedEnergyTerm:
    """
    Vela directed energy
    
    Source: MAIN_1_CoAnQi.cpp (lines 20562-20569)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "VelaPulsar_DirectedEnergy"
        self.description = "Vela directed energy"
        self.metadata = {}
        self.metadata['source'] = 'source152.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return 1e-30 * 1e32
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{VelaPulsar_DirectedEnergyTerm}",
            description=self.description
        )


class VelaPulsar_NeutronTerm:
    """
    Vela neutron
    
    Source: MAIN_1_CoAnQi.cpp (lines 20570-20577)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "VelaPulsar_Neutron"
        self.description = "Vela neutron"
        self.metadata = {}
        self.metadata['source'] = 'source152.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return 1e10 * 1e-4
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{VelaPulsar_NeutronTerm}",
            description=self.description
        )


class VelaPulsar_RelativisticTerm:
    """
    Vela relativistic
    
    Source: MAIN_1_CoAnQi.cpp (lines 20578-20585)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "VelaPulsar_Relativistic"
        self.description = "Vela relativistic"
        self.metadata = {}
        self.metadata['source'] = 'source152.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return 1.69e26
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{VelaPulsar_RelativisticTerm}",
            description=self.description
        )


class VelaPulsar_NeutrinoTerm:
    """
    Vela neutrino
    
    Source: MAIN_1_CoAnQi.cpp (lines 20586-20593)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "VelaPulsar_Neutrino"
        self.description = "Vela neutrino"
        self.metadata = {}
        self.metadata['source'] = 'source152.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return 9.07e-42
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{VelaPulsar_NeutrinoTerm}",
            description=self.description
        )


class NGC2207_DPM_resonanceTerm:
    """
    NGC2207 collision DPM
    
    Source: MAIN_1_CoAnQi.cpp (lines 20596-20603)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "NGC2207_DPM_resonance"
        self.description = "NGC2207 collision DPM"
        self.metadata = {}
        self.metadata['source'] = 'source147.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return 1.76e6
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{NGC2207_DPM_resonanceTerm}",
            description=self.description
        )


class NGC2207_Q_waveTerm:
    """
    NGC2207 tidal quantum wave
    
    Source: MAIN_1_CoAnQi.cpp (lines 20604-20611)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "NGC2207_Q_wave"
        self.description = "NGC2207 tidal quantum wave"
        self.metadata = {}
        self.metadata['source'] = 'source147.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        result = 0.0  # TODO: Implement computation
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{NGC2207_Q_waveTerm}",
            description=self.description
        )


class NGC2207_Ub1Term:
    """
    NGC2207 U_b1
    
    Source: MAIN_1_CoAnQi.cpp (lines 20612-20619)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "NGC2207_Ub1"
        self.description = "NGC2207 U_b1"
        self.metadata = {}
        self.metadata['source'] = 'source147.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return 4.25e-42
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{NGC2207_Ub1Term}",
            description=self.description
        )


class NGC2207_UiTerm:
    """
    NGC2207 inertia
    
    Source: MAIN_1_CoAnQi.cpp (lines 20620-20627)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "NGC2207_Ui"
        self.description = "NGC2207 inertia"
        self.metadata = {}
        self.metadata['source'] = 'source147.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        result = 0.0  # TODO: Implement computation
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{NGC2207_UiTerm}",
            description=self.description
        )


class NGC2207_X2Term:
    """
    NGC2207 x₂
    
    Source: MAIN_1_CoAnQi.cpp (lines 20628-20635)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "NGC2207_X2"
        self.description = "NGC2207 x₂"
        self.metadata = {}
        self.metadata['source'] = 'source147.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return -3.40e172
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{NGC2207_X2Term}",
            description=self.description
        )


class NGC2207_ActivationTerm:
    """
    NGC2207 activation
    
    Source: MAIN_1_CoAnQi.cpp (lines 20636-20643)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "NGC2207_Activation"
        self.description = "NGC2207 activation"
        self.metadata = {}
        self.metadata['source'] = 'source147.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        result = 0.0  # TODO: Implement computation
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{NGC2207_ActivationTerm}",
            description=self.description
        )


class NGC2207_DirectedEnergyTerm:
    """
    NGC2207 directed energy
    
    Source: MAIN_1_CoAnQi.cpp (lines 20644-20651)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "NGC2207_DirectedEnergy"
        self.description = "NGC2207 directed energy"
        self.metadata = {}
        self.metadata['source'] = 'source147.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return 1e-30 * 1e38
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{NGC2207_DirectedEnergyTerm}",
            description=self.description
        )


class NGC2207_NeutronTerm:
    """
    NGC2207 neutron
    
    Source: MAIN_1_CoAnQi.cpp (lines 20652-20659)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "NGC2207_Neutron"
        self.description = "NGC2207 neutron"
        self.metadata = {}
        self.metadata['source'] = 'source147.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return 1e6
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{NGC2207_NeutronTerm}",
            description=self.description
        )


class NGC2207_RelativisticTerm:
    """
    NGC2207 relativistic
    
    Source: MAIN_1_CoAnQi.cpp (lines 20660-20667)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "NGC2207_Relativistic"
        self.description = "NGC2207 relativistic"
        self.metadata = {}
        self.metadata['source'] = 'source147.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return 1.69e26
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{NGC2207_RelativisticTerm}",
            description=self.description
        )


class NGC2207_NeutrinoTerm:
    """
    NGC2207 neutrino
    
    Source: MAIN_1_CoAnQi.cpp (lines 20668-20675)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "NGC2207_Neutrino"
        self.description = "NGC2207 neutrino"
        self.metadata = {}
        self.metadata['source'] = 'source147.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return 9.07e-42
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{NGC2207_NeutrinoTerm}",
            description=self.description
        )


class PerseusCluster_DPM_resonanceTerm:
    """
    Perseus Abell 426 DPM resonance
    
    Source: MAIN_1_CoAnQi.cpp (lines 20678-20685)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "PerseusCluster_DPM_resonance"
        self.description = "Perseus Abell 426 DPM resonance"
        self.metadata = {}
        self.metadata['source'] = 'source154.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return 1.76e6
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{PerseusCluster_DPM_resonanceTerm}",
            description=self.description
        )


class PerseusCluster_Q_waveTerm:
    """
    Perseus quantum wave
    
    Source: MAIN_1_CoAnQi.cpp (lines 20686-20693)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "PerseusCluster_Q_wave"
        self.description = "Perseus quantum wave"
        self.metadata = {}
        self.metadata['source'] = 'source154.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        result = 0.0  # TODO: Implement computation
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{PerseusCluster_Q_waveTerm}",
            description=self.description
        )


class PerseusCluster_Ub1Term:
    """
    Perseus U_b1
    
    Source: MAIN_1_CoAnQi.cpp (lines 20694-20701)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "PerseusCluster_Ub1"
        self.description = "Perseus U_b1"
        self.metadata = {}
        self.metadata['source'] = 'source154.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return 4.25e-42
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{PerseusCluster_Ub1Term}",
            description=self.description
        )


class PerseusCluster_UiTerm:
    """
    Perseus inertia
    
    Source: MAIN_1_CoAnQi.cpp (lines 20702-20709)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "PerseusCluster_Ui"
        self.description = "Perseus inertia"
        self.metadata = {}
        self.metadata['source'] = 'source154.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        result = 0.0  # TODO: Implement computation
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{PerseusCluster_UiTerm}",
            description=self.description
        )


class PerseusCluster_X2Term:
    """
    Perseus x₂
    
    Source: MAIN_1_CoAnQi.cpp (lines 20710-20717)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "PerseusCluster_X2"
        self.description = "Perseus x₂"
        self.metadata = {}
        self.metadata['source'] = 'source154.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return -3.40e172
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{PerseusCluster_X2Term}",
            description=self.description
        )


class PerseusCluster_ActivationTerm:
    """
    Perseus activation
    
    Source: MAIN_1_CoAnQi.cpp (lines 20718-20725)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "PerseusCluster_Activation"
        self.description = "Perseus activation"
        self.metadata = {}
        self.metadata['source'] = 'source154.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        result = 0.0  # TODO: Implement computation
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{PerseusCluster_ActivationTerm}",
            description=self.description
        )


class PerseusCluster_DirectedEnergyTerm:
    """
    Perseus directed energy
    
    Source: MAIN_1_CoAnQi.cpp (lines 20726-20733)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "PerseusCluster_DirectedEnergy"
        self.description = "Perseus directed energy"
        self.metadata = {}
        self.metadata['source'] = 'source154.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return 1e-30 * 1e36
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{PerseusCluster_DirectedEnergyTerm}",
            description=self.description
        )


class PerseusCluster_NeutronTerm:
    """
    Perseus neutron
    
    Source: MAIN_1_CoAnQi.cpp (lines 20734-20741)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "PerseusCluster_Neutron"
        self.description = "Perseus neutron"
        self.metadata = {}
        self.metadata['source'] = 'source154.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return 1e6
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{PerseusCluster_NeutronTerm}",
            description=self.description
        )


class PerseusCluster_RelativisticTerm:
    """
    Perseus relativistic
    
    Source: MAIN_1_CoAnQi.cpp (lines 20742-20749)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "PerseusCluster_Relativistic"
        self.description = "Perseus relativistic"
        self.metadata = {}
        self.metadata['source'] = 'source154.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return 1.69e26
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{PerseusCluster_RelativisticTerm}",
            description=self.description
        )


class PerseusCluster_NeutrinoTerm:
    """
    Perseus neutrino
    
    Source: MAIN_1_CoAnQi.cpp (lines 20750-20757)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "PerseusCluster_Neutrino"
        self.description = "Perseus neutrino"
        self.metadata = {}
        self.metadata['source'] = 'source154.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return 9.07e-42
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{PerseusCluster_NeutrinoTerm}",
            description=self.description
        )


class OrionM42_DPM_resonanceTerm:
    """
    Orion M42 DPM resonance
    
    Source: MAIN_1_CoAnQi.cpp (lines 20760-20767)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "OrionM42_DPM_resonance"
        self.description = "Orion M42 DPM resonance"
        self.metadata = {}
        self.metadata['source'] = 'source155.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return 1.76e6
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{OrionM42_DPM_resonanceTerm}",
            description=self.description
        )


class OrionM42_Q_waveTerm:
    """
    Orion quantum wave
    
    Source: MAIN_1_CoAnQi.cpp (lines 20768-20775)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "OrionM42_Q_wave"
        self.description = "Orion quantum wave"
        self.metadata = {}
        self.metadata['source'] = 'source155.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        result = 0.0  # TODO: Implement computation
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{OrionM42_Q_waveTerm}",
            description=self.description
        )


class OrionM42_Ub1Term:
    """
    Orion U_b1
    
    Source: MAIN_1_CoAnQi.cpp (lines 20776-20783)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "OrionM42_Ub1"
        self.description = "Orion U_b1"
        self.metadata = {}
        self.metadata['source'] = 'source155.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return 4.25e-42
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{OrionM42_Ub1Term}",
            description=self.description
        )


class OrionM42_UiTerm:
    """
    Orion inertia
    
    Source: MAIN_1_CoAnQi.cpp (lines 20784-20791)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "OrionM42_Ui"
        self.description = "Orion inertia"
        self.metadata = {}
        self.metadata['source'] = 'source155.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        result = 0.0  # TODO: Implement computation
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{OrionM42_UiTerm}",
            description=self.description
        )


class OrionM42_X2Term:
    """
    Orion x₂
    
    Source: MAIN_1_CoAnQi.cpp (lines 20792-20799)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "OrionM42_X2"
        self.description = "Orion x₂"
        self.metadata = {}
        self.metadata['source'] = 'source155.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return -3.40e172
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{OrionM42_X2Term}",
            description=self.description
        )


class OrionM42_ActivationTerm:
    """
    Orion activation
    
    Source: MAIN_1_CoAnQi.cpp (lines 20800-20807)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "OrionM42_Activation"
        self.description = "Orion activation"
        self.metadata = {}
        self.metadata['source'] = 'source155.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        result = 0.0  # TODO: Implement computation
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{OrionM42_ActivationTerm}",
            description=self.description
        )


class OrionM42_DirectedEnergyTerm:
    """
    Orion directed energy
    
    Source: MAIN_1_CoAnQi.cpp (lines 20808-20815)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "OrionM42_DirectedEnergy"
        self.description = "Orion directed energy"
        self.metadata = {}
        self.metadata['source'] = 'source155.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return 1e-30 * 1e33
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{OrionM42_DirectedEnergyTerm}",
            description=self.description
        )


class OrionM42_NeutronTerm:
    """
    Orion neutron
    
    Source: MAIN_1_CoAnQi.cpp (lines 20816-20823)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "OrionM42_Neutron"
        self.description = "Orion neutron"
        self.metadata = {}
        self.metadata['source'] = 'source155.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return 1e6
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{OrionM42_NeutronTerm}",
            description=self.description
        )


class OrionM42_RelativisticTerm:
    """
    Orion relativistic
    
    Source: MAIN_1_CoAnQi.cpp (lines 20824-20831)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "OrionM42_Relativistic"
        self.description = "Orion relativistic"
        self.metadata = {}
        self.metadata['source'] = 'source155.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return 1.69e26
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{OrionM42_RelativisticTerm}",
            description=self.description
        )


class OrionM42_NeutrinoTerm:
    """
    Orion neutrino
    
    Source: MAIN_1_CoAnQi.cpp (lines 20832-20839)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "OrionM42_Neutrino"
        self.description = "Orion neutrino"
        self.metadata = {}
        self.metadata['source'] = 'source155.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return 9.07e-42
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{OrionM42_NeutrinoTerm}",
            description=self.description
        )


class LagoonM8_DPM_resonanceTerm:
    """
    Lagoon M8 HII DPM resonance
    
    Source: MAIN_1_CoAnQi.cpp (lines 20842-20849)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "LagoonM8_DPM_resonance"
        self.description = "Lagoon M8 HII DPM resonance"
        self.metadata = {}
        self.metadata['source'] = 'source142.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return 1.76e6
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{LagoonM8_DPM_resonanceTerm}",
            description=self.description
        )


class LagoonM8_Q_waveTerm:
    """
    Lagoon quantum wave
    
    Source: MAIN_1_CoAnQi.cpp (lines 20850-20857)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "LagoonM8_Q_wave"
        self.description = "Lagoon quantum wave"
        self.metadata = {}
        self.metadata['source'] = 'source142.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        result = 0.0  # TODO: Implement computation
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{LagoonM8_Q_waveTerm}",
            description=self.description
        )


class LagoonM8_Ub1Term:
    """
    Lagoon U_b1
    
    Source: MAIN_1_CoAnQi.cpp (lines 20858-20865)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "LagoonM8_Ub1"
        self.description = "Lagoon U_b1"
        self.metadata = {}
        self.metadata['source'] = 'source142.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return 4.25e-42
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{LagoonM8_Ub1Term}",
            description=self.description
        )


class LagoonM8_UiTerm:
    """
    Lagoon inertia
    
    Source: MAIN_1_CoAnQi.cpp (lines 20866-20873)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "LagoonM8_Ui"
        self.description = "Lagoon inertia"
        self.metadata = {}
        self.metadata['source'] = 'source142.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        result = 0.0  # TODO: Implement computation
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{LagoonM8_UiTerm}",
            description=self.description
        )


class LagoonM8_X2Term:
    """
    Lagoon x₂
    
    Source: MAIN_1_CoAnQi.cpp (lines 20874-20881)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "LagoonM8_X2"
        self.description = "Lagoon x₂"
        self.metadata = {}
        self.metadata['source'] = 'source142.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return -3.40e172
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{LagoonM8_X2Term}",
            description=self.description
        )


class LagoonM8_ActivationTerm:
    """
    Lagoon activation
    
    Source: MAIN_1_CoAnQi.cpp (lines 20882-20889)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "LagoonM8_Activation"
        self.description = "Lagoon activation"
        self.metadata = {}
        self.metadata['source'] = 'source142.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        result = 0.0  # TODO: Implement computation
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{LagoonM8_ActivationTerm}",
            description=self.description
        )


class LagoonM8_DirectedEnergyTerm:
    """
    Lagoon directed energy
    
    Source: MAIN_1_CoAnQi.cpp (lines 20890-20897)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "LagoonM8_DirectedEnergy"
        self.description = "Lagoon directed energy"
        self.metadata = {}
        self.metadata['source'] = 'source142.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return 1e-30 * 1e33
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{LagoonM8_DirectedEnergyTerm}",
            description=self.description
        )


class LagoonM8_NeutronTerm:
    """
    Lagoon neutron
    
    Source: MAIN_1_CoAnQi.cpp (lines 20898-20905)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "LagoonM8_Neutron"
        self.description = "Lagoon neutron"
        self.metadata = {}
        self.metadata['source'] = 'source142.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return 1e6
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{LagoonM8_NeutronTerm}",
            description=self.description
        )


class LagoonM8_RelativisticTerm:
    """
    Lagoon relativistic
    
    Source: MAIN_1_CoAnQi.cpp (lines 20906-20913)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "LagoonM8_Relativistic"
        self.description = "Lagoon relativistic"
        self.metadata = {}
        self.metadata['source'] = 'source142.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return 1.69e26
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{LagoonM8_RelativisticTerm}",
            description=self.description
        )


class LagoonM8_NeutrinoTerm:
    """
    Lagoon neutrino
    
    Source: MAIN_1_CoAnQi.cpp (lines 20914-20921)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "LagoonM8_Neutrino"
        self.description = "Lagoon neutrino"
        self.metadata = {}
        self.metadata['source'] = 'source142.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return 9.07e-42
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{LagoonM8_NeutrinoTerm}",
            description=self.description
        )


class ESO137_G_timeTerm:
    """
    ESO137 time-dependent gravity g(r,t)
    
    Source: MAIN_1_CoAnQi.cpp (lines 20930-20945)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "ESO137_G_time"
        self.description = "ESO137 time-dependent gravity g(r,t)"
        self.metadata = {}
        self.metadata['source'] = 'source100.cpp'
        self.metadata['method'] = 'computeG'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        M = 1e38, r = 1e19, G = 6.674e-11
        result = 0.0  # Assign result
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{ESO137_G_timeTerm}",
            description=self.description
        )


class NGC1365A_G_timeTerm:
    """
    NGC1365 g(r,t) with AGN variability
    
    Source: MAIN_1_CoAnQi.cpp (lines 20946-20956)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "NGC1365A_G_time"
        self.description = "NGC1365 g(r,t) with AGN variability"
        self.metadata = {}
        self.metadata['source'] = 'source101.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        result = 0.0  # TODO: Implement computation
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{NGC1365A_G_timeTerm}",
            description=self.description
        )


class NGC5728_G_timeTerm:
    """
    NGC5728 g(r,t)
    
    Source: MAIN_1_CoAnQi.cpp (lines 20957-20967)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "NGC5728_G_time"
        self.description = "NGC5728 g(r,t)"
        self.metadata = {}
        self.metadata['source'] = 'source102.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        result = 0.0  # TODO: Implement computation
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{NGC5728_G_timeTerm}",
            description=self.description
        )


class Vela_G_timeTerm:
    """
    Vela Pulsar g(t) at 11.2 Hz
    
    Source: MAIN_1_CoAnQi.cpp (lines 20968-20978)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "Vela_G_time"
        self.description = "Vela Pulsar g(t) at 11.2 Hz"
        self.metadata = {}
        self.metadata['source'] = 'source103.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        result = 0.0  # TODO: Implement computation
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{Vela_G_timeTerm}",
            description=self.description
        )


class M82_G_timeTerm:
    """
    M82 Cigar Galaxy g(r,t)
    
    Source: MAIN_1_CoAnQi.cpp (lines 20979-20989)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "M82_G_time"
        self.description = "M82 Cigar Galaxy g(r,t)"
        self.metadata = {}
        self.metadata['source'] = 'source104.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        result = 0.0  # TODO: Implement computation
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{M82_G_timeTerm}",
            description=self.description
        )


class Carina_G_timeTerm:
    """
    Carina Nebula g(r,t)
    
    Source: MAIN_1_CoAnQi.cpp (lines 20990-21000)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "Carina_G_time"
        self.description = "Carina Nebula g(r,t)"
        self.metadata = {}
        self.metadata['source'] = 'source105.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        result = 0.0  # TODO: Implement computation
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{Carina_G_timeTerm}",
            description=self.description
        )


class TadpoleGalaxy_G_timeTerm:
    """
    Tadpole Galaxy g(r,t) with tidal tail
    
    Source: MAIN_1_CoAnQi.cpp (lines 21001-21011)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "TadpoleGalaxy_G_time"
        self.description = "Tadpole Galaxy g(r,t) with tidal tail"
        self.metadata = {}
        self.metadata['source'] = 'source106.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        result = 0.0  # TODO: Implement computation
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{TadpoleGalaxy_G_timeTerm}",
            description=self.description
        )


class MiceGalaxies_G_timeTerm:
    """
    Mice Galaxies g(r,t) interaction
    
    Source: MAIN_1_CoAnQi.cpp (lines 21012-21022)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "MiceGalaxies_G_time"
        self.description = "Mice Galaxies g(r,t) interaction"
        self.metadata = {}
        self.metadata['source'] = 'source107.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        result = 0.0  # TODO: Implement computation
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{MiceGalaxies_G_timeTerm}",
            description=self.description
        )


class NGC2264_G_timeTerm:
    """
    NGC2264 Christmas Tree g(r,t)
    
    Source: MAIN_1_CoAnQi.cpp (lines 21023-21033)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "NGC2264_G_time"
        self.description = "NGC2264 Christmas Tree g(r,t)"
        self.metadata = {}
        self.metadata['source'] = 'source108.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        result = 0.0  # TODO: Implement computation
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{NGC2264_G_timeTerm}",
            description=self.description
        )


class M42_G_timeTerm:
    """
    Orion M42 g(r,t)
    
    Source: MAIN_1_CoAnQi.cpp (lines 21034-21044)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "M42_G_time"
        self.description = "Orion M42 g(r,t)"
        self.metadata = {}
        self.metadata['source'] = 'source109.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        result = 0.0  # TODO: Implement computation
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{M42_G_timeTerm}",
            description=self.description
        )


class CrabNebula_G_timeTerm:
    """
    Crab Nebula g(t) 30 Hz pulsar
    
    Source: MAIN_1_CoAnQi.cpp (lines 21047-21057)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "CrabNebula_G_time"
        self.description = "Crab Nebula g(t) 30 Hz pulsar"
        self.metadata = {}
        self.metadata['source'] = 'source110.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        result = 0.0  # TODO: Implement computation
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{CrabNebula_G_timeTerm}",
            description=self.description
        )


class BlackWidowPulsar_G_timeTerm:
    """
    Black Widow Pulsar g(t) 622 Hz
    
    Source: MAIN_1_CoAnQi.cpp (lines 21058-21068)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "BlackWidowPulsar_G_time"
        self.description = "Black Widow Pulsar g(t) 622 Hz"
        self.metadata = {}
        self.metadata['source'] = 'source111.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        result = 0.0  # TODO: Implement computation
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{BlackWidowPulsar_G_timeTerm}",
            description=self.description
        )


class Redback_G_timeTerm:
    """
    Redback Pulsar g(t) 317 Hz
    
    Source: MAIN_1_CoAnQi.cpp (lines 21069-21079)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "Redback_G_time"
        self.description = "Redback Pulsar g(t) 317 Hz"
        self.metadata = {}
        self.metadata['source'] = 'source112.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        result = 0.0  # TODO: Implement computation
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{Redback_G_timeTerm}",
            description=self.description
        )


class Fermi_G_timeTerm:
    """
    Fermi Bubbles g(r,t)
    
    Source: MAIN_1_CoAnQi.cpp (lines 21080-21090)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "Fermi_G_time"
        self.description = "Fermi Bubbles g(r,t)"
        self.metadata = {}
        self.metadata['source'] = 'source113.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        result = 0.0  # TODO: Implement computation
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{Fermi_G_timeTerm}",
            description=self.description
        )


class Holmberg15A_G_timeTerm:
    """
    Holmberg 15A supermassive BH g(r,t)
    
    Source: MAIN_1_CoAnQi.cpp (lines 21091-21101)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "Holmberg15A_G_time"
        self.description = "Holmberg 15A supermassive BH g(r,t)"
        self.metadata = {}
        self.metadata['source'] = 'source114.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        result = 0.0  # TODO: Implement computation
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{Holmberg15A_G_timeTerm}",
            description=self.description
        )


class NineteenSystems_G_timeTerm:
    """
    19-system master g(r,t) 26D polynomial
    
    Source: MAIN_1_CoAnQi.cpp (lines 21102-21116)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "NineteenSystems_G_time"
        self.description = "19-system master g(r,t) 26D polynomial"
        self.metadata = {}
        self.metadata['source'] = 'source115.cpp'
        self.metadata['systems'] = '19'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        result = 0.0  # TODO: Implement computation
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{NineteenSystems_G_timeTerm}",
            description=self.description
        )


class WolframHypergraph_G_timeTerm:
    """
    Wolfram hypergraph g(t) with PI infinity decoder
    
    Source: MAIN_1_CoAnQi.cpp (lines 21117-21131)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "WolframHypergraph_G_time"
        self.description = "Wolfram hypergraph g(t) with PI infinity decoder"
        self.metadata = {}
        self.metadata['source'] = 'source116.cpp'
        self.metadata['PI_digits'] = '312'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        result = 0.0  # TODO: Implement computation
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{WolframHypergraph_G_timeTerm}",
            description=self.description
        )


class NGC253_G_timeTerm:
    """
    NGC253 Sculptor Galaxy g(r,t)
    
    Source: MAIN_1_CoAnQi.cpp (lines 21132-21142)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "NGC253_G_time"
        self.description = "NGC253 Sculptor Galaxy g(r,t)"
        self.metadata = {}
        self.metadata['source'] = 'source117.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        result = 0.0  # TODO: Implement computation
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{NGC253_G_timeTerm}",
            description=self.description
        )


class M87Jet_G_timeTerm:
    """
    M87 jet g(r,t) with precession
    
    Source: MAIN_1_CoAnQi.cpp (lines 21143-21153)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "M87Jet_G_time"
        self.description = "M87 jet g(r,t) with precession"
        self.metadata = {}
        self.metadata['source'] = 'source118.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        result = 0.0  # TODO: Implement computation
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{M87Jet_G_timeTerm}",
            description=self.description
        )


class Centaurus_G_timeTerm:
    """
    Centaurus A g(r,t)
    
    Source: MAIN_1_CoAnQi.cpp (lines 21154-21164)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "Centaurus_G_time"
        self.description = "Centaurus A g(r,t)"
        self.metadata = {}
        self.metadata['source'] = 'source119.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        result = 0.0  # TODO: Implement computation
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{Centaurus_G_timeTerm}",
            description=self.description
        )


class Antlia_G_timeTerm:
    """
    Antlia Dwarf g(r,t)
    
    Source: MAIN_1_CoAnQi.cpp (lines 21167-21174)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "Antlia_G_time"
        self.description = "Antlia Dwarf g(r,t)"
        self.metadata = {}
        self.metadata['source'] = 'source120.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        result = 0.0  # TODO: Implement computation
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{Antlia_G_timeTerm}",
            description=self.description
        )


class Fornax_G_timeTerm:
    """
    Fornax Cluster g(r,t)
    
    Source: MAIN_1_CoAnQi.cpp (lines 21175-21182)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "Fornax_G_time"
        self.description = "Fornax Cluster g(r,t)"
        self.metadata = {}
        self.metadata['source'] = 'source121.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        result = 0.0  # TODO: Implement computation
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{Fornax_G_timeTerm}",
            description=self.description
        )


class Sculptor_G_timeTerm:
    """
    Sculptor Dwarf g(r,t)
    
    Source: MAIN_1_CoAnQi.cpp (lines 21183-21190)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "Sculptor_G_time"
        self.description = "Sculptor Dwarf g(r,t)"
        self.metadata = {}
        self.metadata['source'] = 'source122.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        result = 0.0  # TODO: Implement computation
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{Sculptor_G_timeTerm}",
            description=self.description
        )


class LeoI_G_timeTerm:
    """
    Leo I Dwarf g(r,t)
    
    Source: MAIN_1_CoAnQi.cpp (lines 21191-21198)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "LeoI_G_time"
        self.description = "Leo I Dwarf g(r,t)"
        self.metadata = {}
        self.metadata['source'] = 'source123.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        result = 0.0  # TODO: Implement computation
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{LeoI_G_timeTerm}",
            description=self.description
        )


class LeoII_G_timeTerm:
    """
    Leo II g(r,t)
    
    Source: MAIN_1_CoAnQi.cpp (lines 21199-21206)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "LeoII_G_time"
        self.description = "Leo II g(r,t)"
        self.metadata = {}
        self.metadata['source'] = 'source124.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        result = 0.0  # TODO: Implement computation
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{LeoII_G_timeTerm}",
            description=self.description
        )


class UrsaMinor_G_timeTerm:
    """
    Ursa Minor g(r,t)
    
    Source: MAIN_1_CoAnQi.cpp (lines 21207-21214)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "UrsaMinor_G_time"
        self.description = "Ursa Minor g(r,t)"
        self.metadata = {}
        self.metadata['source'] = 'source125.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        result = 0.0  # TODO: Implement computation
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{UrsaMinor_G_timeTerm}",
            description=self.description
        )


class Draco_G_timeTerm:
    """
    Draco Dwarf g(r,t)
    
    Source: MAIN_1_CoAnQi.cpp (lines 21215-21222)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "Draco_G_time"
        self.description = "Draco Dwarf g(r,t)"
        self.metadata = {}
        self.metadata['source'] = 'source126.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        result = 0.0  # TODO: Implement computation
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{Draco_G_timeTerm}",
            description=self.description
        )


class Sextans_G_timeTerm:
    """
    Sextans Dwarf g(r,t)
    
    Source: MAIN_1_CoAnQi.cpp (lines 21223-21230)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "Sextans_G_time"
        self.description = "Sextans Dwarf g(r,t)"
        self.metadata = {}
        self.metadata['source'] = 'source127.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        result = 0.0  # TODO: Implement computation
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{Sextans_G_timeTerm}",
            description=self.description
        )


class Carina2_G_timeTerm:
    """
    Carina II g(r,t)
    
    Source: MAIN_1_CoAnQi.cpp (lines 21231-21238)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "Carina2_G_time"
        self.description = "Carina II g(r,t)"
        self.metadata = {}
        self.metadata['source'] = 'source128.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        result = 0.0  # TODO: Implement computation
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{Carina2_G_timeTerm}",
            description=self.description
        )


class Reticulum_G_timeTerm:
    """
    Reticulum II g(r,t)
    
    Source: MAIN_1_CoAnQi.cpp (lines 21239-21246)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "Reticulum_G_time"
        self.description = "Reticulum II g(r,t)"
        self.metadata = {}
        self.metadata['source'] = 'source129.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        result = 0.0  # TODO: Implement computation
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{Reticulum_G_timeTerm}",
            description=self.description
        )


class Tucana_G_timeTerm:
    """
    Tucana II g(r,t)
    
    Source: MAIN_1_CoAnQi.cpp (lines 21249-21256)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "Tucana_G_time"
        self.description = "Tucana II g(r,t)"
        self.metadata = {}
        self.metadata['source'] = 'source130.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        result = 0.0  # TODO: Implement computation
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{Tucana_G_timeTerm}",
            description=self.description
        )


class Horologium_G_timeTerm:
    """
    Horologium I g(r,t)
    
    Source: MAIN_1_CoAnQi.cpp (lines 21257-21264)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "Horologium_G_time"
        self.description = "Horologium I g(r,t)"
        self.metadata = {}
        self.metadata['source'] = 'source131.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        result = 0.0  # TODO: Implement computation
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{Horologium_G_timeTerm}",
            description=self.description
        )


class Grus_G_timeTerm:
    """
    Grus I g(r,t)
    
    Source: MAIN_1_CoAnQi.cpp (lines 21265-21272)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "Grus_G_time"
        self.description = "Grus I g(r,t)"
        self.metadata = {}
        self.metadata['source'] = 'source132.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        result = 0.0  # TODO: Implement computation
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{Grus_G_timeTerm}",
            description=self.description
        )


class Phoenix_G_timeTerm:
    """
    Phoenix Dwarf g(r,t)
    
    Source: MAIN_1_CoAnQi.cpp (lines 21273-21280)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "Phoenix_G_time"
        self.description = "Phoenix Dwarf g(r,t)"
        self.metadata = {}
        self.metadata['source'] = 'source133.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        result = 0.0  # TODO: Implement computation
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{Phoenix_G_timeTerm}",
            description=self.description
        )


class Eridanus_G_timeTerm:
    """
    Eridanus II g(r,t)
    
    Source: MAIN_1_CoAnQi.cpp (lines 21281-21288)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "Eridanus_G_time"
        self.description = "Eridanus II g(r,t)"
        self.metadata = {}
        self.metadata['source'] = 'source134.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        result = 0.0  # TODO: Implement computation
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{Eridanus_G_timeTerm}",
            description=self.description
        )


class Aquarius_G_timeTerm:
    """
    Aquarius II g(r,t)
    
    Source: MAIN_1_CoAnQi.cpp (lines 21289-21296)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "Aquarius_G_time"
        self.description = "Aquarius II g(r,t)"
        self.metadata = {}
        self.metadata['source'] = 'source135.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        result = 0.0  # TODO: Implement computation
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{Aquarius_G_timeTerm}",
            description=self.description
        )


class Pisces_G_timeTerm:
    """
    Pisces II g(r,t)
    
    Source: MAIN_1_CoAnQi.cpp (lines 21297-21304)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "Pisces_G_time"
        self.description = "Pisces II g(r,t)"
        self.metadata = {}
        self.metadata['source'] = 'source136.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        result = 0.0  # TODO: Implement computation
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{Pisces_G_timeTerm}",
            description=self.description
        )


class Pegasus_G_timeTerm:
    """
    Pegasus III g(r,t)
    
    Source: MAIN_1_CoAnQi.cpp (lines 21305-21312)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "Pegasus_G_time"
        self.description = "Pegasus III g(r,t)"
        self.metadata = {}
        self.metadata['source'] = 'source137.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        result = 0.0  # TODO: Implement computation
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{Pegasus_G_timeTerm}",
            description=self.description
        )


class Andromeda_G_timeTerm:
    """
    Andromeda M31 g(r,t)
    
    Source: MAIN_1_CoAnQi.cpp (lines 21313-21320)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "Andromeda_G_time"
        self.description = "Andromeda M31 g(r,t)"
        self.metadata = {}
        self.metadata['source'] = 'source138.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        result = 0.0  # TODO: Implement computation
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{Andromeda_G_timeTerm}",
            description=self.description
        )


class Triangulum_G_timeTerm:
    """
    Triangulum M33 g(r,t)
    
    Source: MAIN_1_CoAnQi.cpp (lines 21321-21328)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "Triangulum_G_time"
        self.description = "Triangulum M33 g(r,t)"
        self.metadata = {}
        self.metadata['source'] = 'source139.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        result = 0.0  # TODO: Implement computation
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{Triangulum_G_timeTerm}",
            description=self.description
        )


class LMC_G_timeTerm:
    """
    Large Magellanic Cloud g(r,t)
    
    Source: MAIN_1_CoAnQi.cpp (lines 21329-21336)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "LMC_G_time"
        self.description = "Large Magellanic Cloud g(r,t)"
        self.metadata = {}
        self.metadata['source'] = 'source140.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        result = 0.0  # TODO: Implement computation
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{LMC_G_timeTerm}",
            description=self.description
        )


class J1610_G_timeTerm:
    """
    J1610 quasar g(r,t) z=3.122
    
    Source: MAIN_1_CoAnQi.cpp (lines 21339-21346)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "J1610_G_time"
        self.description = "J1610 quasar g(r,t) z=3.122"
        self.metadata = {}
        self.metadata['source'] = 'source141.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        result = 0.0  # TODO: Implement computation
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{J1610_G_timeTerm}",
            description=self.description
        )


class JupiterAurorae_G_timeTerm:
    """
    Jupiter aurorae g(r,t) Io plasma
    
    Source: MAIN_1_CoAnQi.cpp (lines 21347-21354)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "JupiterAurorae_G_time"
        self.description = "Jupiter aurorae g(r,t) Io plasma"
        self.metadata = {}
        self.metadata['source'] = 'source141.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        result = 0.0  # TODO: Implement computation
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{JupiterAurorae_G_timeTerm}",
            description=self.description
        )


class LagoonM8HII_G_timeTerm:
    """
    Lagoon M8 HII region g(r,t)
    
    Source: MAIN_1_CoAnQi.cpp (lines 21355-21362)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "LagoonM8HII_G_time"
        self.description = "Lagoon M8 HII region g(r,t)"
        self.metadata = {}
        self.metadata['source'] = 'source142.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        result = 0.0  # TODO: Implement computation
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{LagoonM8HII_G_timeTerm}",
            description=self.description
        )


class M87JetEHT_G_timeTerm:
    """
    M87 EHT jet g(r,t) v~0.5c
    
    Source: MAIN_1_CoAnQi.cpp (lines 21363-21370)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "M87JetEHT_G_time"
        self.description = "M87 EHT jet g(r,t) v~0.5c"
        self.metadata = {}
        self.metadata['source'] = 'source145.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        result = 0.0  # TODO: Implement computation
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{M87JetEHT_G_timeTerm}",
            description=self.description
        )


class NGC1365Seyfert_G_timeTerm:
    """
    NGC1365 Seyfert g(r,t)
    
    Source: MAIN_1_CoAnQi.cpp (lines 21371-21378)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "NGC1365Seyfert_G_time"
        self.description = "NGC1365 Seyfert g(r,t)"
        self.metadata = {}
        self.metadata['source'] = 'source146.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        result = 0.0  # TODO: Implement computation
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{NGC1365Seyfert_G_timeTerm}",
            description=self.description
        )


class NGC2207IC2163_G_timeTerm:
    """
    NGC2207/IC2163 collision g(r,t)
    
    Source: MAIN_1_CoAnQi.cpp (lines 21379-21386)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "NGC2207IC2163_G_time"
        self.description = "NGC2207/IC2163 collision g(r,t)"
        self.metadata = {}
        self.metadata['source'] = 'source147.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        result = 0.0  # TODO: Implement computation
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{NGC2207IC2163_G_timeTerm}",
            description=self.description
        )


class RAquarii_G_timeTerm:
    """
    R Aquarii symbiotic g(r,t) 44yr period
    
    Source: MAIN_1_CoAnQi.cpp (lines 21387-21394)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "RAquarii_G_time"
        self.description = "R Aquarii symbiotic g(r,t) 44yr period"
        self.metadata = {}
        self.metadata['source'] = 'source148.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        result = 0.0  # TODO: Implement computation
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{RAquarii_G_timeTerm}",
            description=self.description
        )


class SgrAStarGC_G_timeTerm:
    """
    Sgr A* Galactic Center g(r,t) flares
    
    Source: MAIN_1_CoAnQi.cpp (lines 21395-21402)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "SgrAStarGC_G_time"
        self.description = "Sgr A* Galactic Center g(r,t) flares"
        self.metadata = {}
        self.metadata['source'] = 'source149.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        result = 0.0  # TODO: Implement computation
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{SgrAStarGC_G_timeTerm}",
            description=self.description
        )


class SPTCLJ2215_G_timeTerm:
    """
    SPT-CL J2215 cluster g(r,t) z=0.7
    
    Source: MAIN_1_CoAnQi.cpp (lines 21403-21410)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "SPTCLJ2215_G_time"
        self.description = "SPT-CL J2215 cluster g(r,t) z=0.7"
        self.metadata = {}
        self.metadata['source'] = 'source150.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        result = 0.0  # TODO: Implement computation
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{SPTCLJ2215_G_timeTerm}",
            description=self.description
        )


class StephanQuintet_G_timeTerm:
    """
    Stephan's Quintet g(r,t) JWST
    
    Source: MAIN_1_CoAnQi.cpp (lines 21411-21418)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "StephanQuintet_G_time"
        self.description = "Stephan's Quintet g(r,t) JWST"
        self.metadata = {}
        self.metadata['source'] = 'source151.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        result = 0.0  # TODO: Implement computation
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{StephanQuintet_G_timeTerm}",
            description=self.description
        )


class VelaPulsarSNR_G_timeTerm:
    """
    Vela SNR g(t) 89ms pulsar
    
    Source: MAIN_1_CoAnQi.cpp (lines 21421-21428)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "VelaPulsarSNR_G_time"
        self.description = "Vela SNR g(t) 89ms pulsar"
        self.metadata = {}
        self.metadata['source'] = 'source152.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        result = 0.0  # TODO: Implement computation
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{VelaPulsarSNR_G_timeTerm}",
            description=self.description
        )


class NGC4993GW170817_G_timeTerm:
    """
    NGC4993 GW170817 kilonova g(r,t) NS merger
    
    Source: MAIN_1_CoAnQi.cpp (lines 21429-21436)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "NGC4993GW170817_G_time"
        self.description = "NGC4993 GW170817 kilonova g(r,t) NS merger"
        self.metadata = {}
        self.metadata['source'] = 'source153.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        result = 0.0  # TODO: Implement computation
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{NGC4993GW170817_G_timeTerm}",
            description=self.description
        )


class NGC6240DualAGN_G_timeTerm:
    """
    NGC6240 dual AGN g(r,t)
    
    Source: MAIN_1_CoAnQi.cpp (lines 21437-21444)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "NGC6240DualAGN_G_time"
        self.description = "NGC6240 dual AGN g(r,t)"
        self.metadata = {}
        self.metadata['source'] = 'source154.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        result = 0.0  # TODO: Implement computation
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{NGC6240DualAGN_G_timeTerm}",
            description=self.description
        )


class OmegaCentauri_G_timeTerm:
    """
    Omega Centauri globular g(r,t)
    
    Source: MAIN_1_CoAnQi.cpp (lines 21445-21452)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "OmegaCentauri_G_time"
        self.description = "Omega Centauri globular g(r,t)"
        self.metadata = {}
        self.metadata['source'] = 'source155.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        result = 0.0  # TODO: Implement computation
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{OmegaCentauri_G_timeTerm}",
            description=self.description
        )


class OrionM42Trapezium_G_timeTerm:
    """
    Orion Trapezium g(r,t)
    
    Source: MAIN_1_CoAnQi.cpp (lines 21453-21460)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "OrionM42Trapezium_G_time"
        self.description = "Orion Trapezium g(r,t)"
        self.metadata = {}
        self.metadata['source'] = 'source156.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        result = 0.0  # TODO: Implement computation
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{OrionM42Trapezium_G_timeTerm}",
            description=self.description
        )


class PerseusAbell426_G_timeTerm:
    """
    Perseus Abell 426 cluster g(r,t)
    
    Source: MAIN_1_CoAnQi.cpp (lines 21461-21468)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "PerseusAbell426_G_time"
        self.description = "Perseus Abell 426 cluster g(r,t)"
        self.metadata = {}
        self.metadata['source'] = 'source157.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        result = 0.0  # TODO: Implement computation
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{PerseusAbell426_G_timeTerm}",
            description=self.description
        )


class PolarRingNGC4650A_G_timeTerm:
    """
    Polar Ring NGC4650A g(r,t)
    
    Source: MAIN_1_CoAnQi.cpp (lines 21469-21476)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "PolarRingNGC4650A_G_time"
        self.description = "Polar Ring NGC4650A g(r,t)"
        self.metadata = {}
        self.metadata['source'] = 'source158.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        result = 0.0  # TODO: Implement computation
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{PolarRingNGC4650A_G_timeTerm}",
            description=self.description
        )


class QuasarJ0313_G_timeTerm:
    """
    Quasar J0313 z=7.64 earliest g(r,t)
    
    Source: MAIN_1_CoAnQi.cpp (lines 21477-21484)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "QuasarJ0313_G_time"
        self.description = "Quasar J0313 z=7.64 earliest g(r,t)"
        self.metadata = {}
        self.metadata['source'] = 'source159.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        result = 0.0  # TODO: Implement computation
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{QuasarJ0313_G_timeTerm}",
            description=self.description
        )


class RXJ1713TeVSNR_G_timeTerm:
    """
    RX J1713 TeV SNR g(r,t)
    
    Source: MAIN_1_CoAnQi.cpp (lines 21485-21492)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "RXJ1713TeVSNR_G_time"
        self.description = "RX J1713 TeV SNR g(r,t)"
        self.metadata = {}
        self.metadata['source'] = 'source160.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        result = 0.0  # TODO: Implement computation
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{RXJ1713TeVSNR_G_timeTerm}",
            description=self.description
        )


class SMC_G_timeTerm:
    """
    Small Magellanic Cloud g(r,t)
    
    Source: MAIN_1_CoAnQi.cpp (lines 21493-21500)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "SMC_G_time"
        self.description = "Small Magellanic Cloud g(r,t)"
        self.metadata = {}
        self.metadata['source'] = 'source160.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        result = 0.0  # TODO: Implement computation
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{SMC_G_timeTerm}",
            description=self.description
        )


class BuoyancyAstro5_G_timeTerm:
    """
    5-system buoyancy g(r,t)
    
    Source: MAIN_1_CoAnQi.cpp (lines 21503-21510)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "BuoyancyAstro5_G_time"
        self.description = "5-system buoyancy g(r,t)"
        self.metadata = {}
        self.metadata['source'] = 'source161.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        result = 0.0  # TODO: Implement computation
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{BuoyancyAstro5_G_timeTerm}",
            description=self.description
        )


class BuoyancyCNB_G_timeTerm:
    """
    Centaurus A CNB g(r,t)
    
    Source: MAIN_1_CoAnQi.cpp (lines 21511-21518)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "BuoyancyCNB_G_time"
        self.description = "Centaurus A CNB g(r,t)"
        self.metadata = {}
        self.metadata['source'] = 'source162.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        result = 0.0  # TODO: Implement computation
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{BuoyancyCNB_G_timeTerm}",
            description=self.description
        )


class AstroSystems163_G_timeTerm:
    """
    NGC 685/3507/3511 + TDE g(r,t)
    
    Source: MAIN_1_CoAnQi.cpp (lines 21519-21526)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "AstroSystems163_G_time"
        self.description = "NGC 685/3507/3511 + TDE g(r,t)"
        self.metadata = {}
        self.metadata['source'] = 'source163.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        result = 0.0  # TODO: Implement computation
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{AstroSystems163_G_timeTerm}",
            description=self.description
        )


class NebulaTriadic_G_timeTerm:
    """
    Triadic nebula g(r,t) ionization
    
    Source: MAIN_1_CoAnQi.cpp (lines 21527-21534)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "NebulaTriadic_G_time"
        self.description = "Triadic nebula g(r,t) ionization"
        self.metadata = {}
        self.metadata['source'] = 'source164.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        result = 0.0  # TODO: Implement computation
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{NebulaTriadic_G_timeTerm}",
            description=self.description
        )


class BuoyancyModule_G_timeTerm:
    """
    M74/Eagle/M84 buoyancy g(r,t)
    
    Source: MAIN_1_CoAnQi.cpp (lines 21535-21542)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "BuoyancyModule_G_time"
        self.description = "M74/Eagle/M84 buoyancy g(r,t)"
        self.metadata = {}
        self.metadata['source'] = 'source165.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        result = 0.0  # TODO: Implement computation
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{BuoyancyModule_G_timeTerm}",
            description=self.description
        )


class UQFF8Astro_G_timeTerm:
    """
    8-system 26-state g(r,t)
    
    Source: MAIN_1_CoAnQi.cpp (lines 21543-21554)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "UQFF8Astro_G_time"
        self.description = "8-system 26-state g(r,t)"
        self.metadata = {}
        self.metadata['source'] = 'source166.cpp'
        self.metadata['quantum_states'] = '26'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        result = 0.0  # TODO: Implement computation
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{UQFF8Astro_G_timeTerm}",
            description=self.description
        )


class UQFFCore_G_timeTerm:
    """
    M82/IC418/Canis core g(r,t)
    
    Source: MAIN_1_CoAnQi.cpp (lines 21555-21562)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "UQFFCore_G_time"
        self.description = "M82/IC418/Canis core g(r,t)"
        self.metadata = {}
        self.metadata['source'] = 'source167.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        result = 0.0  # TODO: Implement computation
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{UQFFCore_G_timeTerm}",
            description=self.description
        )


class Buoyancy168_G_timeTerm:
    """
    SN1006/EtaCar/Chandra g(r,t)
    
    Source: MAIN_1_CoAnQi.cpp (lines 21563-21570)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "Buoyancy168_G_time"
        self.description = "SN1006/EtaCar/Chandra g(r,t)"
        self.metadata = {}
        self.metadata['source'] = 'source168.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        result = 0.0  # TODO: Implement computation
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{Buoyancy168_G_timeTerm}",
            description=self.description
        )


class CassiniBuoyancy_G_timeTerm:
    """
    Saturn Cassini g(r,t) 29.5yr orbit
    
    Source: MAIN_1_CoAnQi.cpp (lines 21571-21578)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "CassiniBuoyancy_G_time"
        self.description = "Saturn Cassini g(r,t) 29.5yr orbit"
        self.metadata = {}
        self.metadata['source'] = 'source169.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        result = 0.0  # TODO: Implement computation
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{CassiniBuoyancy_G_timeTerm}",
            description=self.description
        )


class MultiAstro_G_timeTerm:
    """
    11-system NGC/Cassini/ESO g(r,t)
    
    Source: MAIN_1_CoAnQi.cpp (lines 21579-21590)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "MultiAstro_G_time"
        self.description = "11-system NGC/Cassini/ESO g(r,t)"
        self.metadata = {}
        self.metadata['source'] = 'source170.cpp'
        self.metadata['systems'] = '11'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        result = 0.0  # TODO: Implement computation
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{MultiAstro_G_timeTerm}",
            description=self.description
        )


class EightAstro_G_timeTerm:
    """
    AFGL5180/NGC346/LMC g(r,t)
    
    Source: MAIN_1_CoAnQi.cpp (lines 21591-21598)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "EightAstro_G_time"
        self.description = "AFGL5180/NGC346/LMC g(r,t)"
        self.metadata = {}
        self.metadata['source'] = 'source171.cpp'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        result = 0.0  # TODO: Implement computation
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{EightAstro_G_timeTerm}",
            description=self.description
        )


class NineteenAstro26D_G_timeTerm:
    """
    19-system 26D master g(r,t)
    
    Source: MAIN_1_CoAnQi.cpp (lines 21599-21610)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "NineteenAstro26D_G_time"
        self.description = "19-system 26D master g(r,t)"
        self.metadata = {}
        self.metadata['source'] = 'source172.cpp'
        self.metadata['26D_polynomial'] = 'true'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        result = 0.0  # TODO: Implement computation
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{NineteenAstro26D_G_timeTerm}",
            description=self.description
        )


class WolframFieldUnity_G_timeTerm:
    """
    Wolfram hypergraph g(t) 312-digit PI infinity decoder
    
    Source: MAIN_1_CoAnQi.cpp (lines 21611-21625)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "WolframFieldUnity_G_time"
        self.description = "Wolfram hypergraph g(t) 312-digit PI infinity decoder"
        self.metadata = {}
        self.metadata['source'] = 'source173.cpp'
        self.metadata['hypergraph'] = 'emergent_spacetime'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        result = 0.0  # TODO: Implement computation
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{WolframFieldUnity_G_timeTerm}",
            description=self.description
        )


class ESO137_HeavisideFactor:
    """
    ESO137 Heaviside threshold amplification factor 1+10^13*f
    
    Source: MAIN_1_CoAnQi.cpp (lines 21637-21653)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "ESO137_HeavisideFactor"
        self.description = "ESO137 Heaviside threshold amplification factor 1+10^13*f"
        self.metadata = {}
        self.metadata['source'] = 'source100.cpp'
        self.metadata['physics'] = 'threshold_amplification'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        f_Heaviside = 0.01
        scale = 1e13
        return 1.0 + scale * f_Heaviside
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{ESO137_HeavisideFactor}",
            description=self.description
        )


class ESO137_UmBase:
    """
    ESO137 U_m magnetic energy base (no Heaviside/Quasi)
    
    Source: MAIN_1_CoAnQi.cpp (lines 21655-21674)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "ESO137_UmBase"
        self.description = "ESO137 U_m magnetic energy base (no Heaviside/Quasi)"
        self.metadata = {}
        self.metadata['source'] = 'source100.cpp'
        self.metadata['physics'] = 'magnetic_energy_base'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        mu_over_rj = 3.38e23 / 1.496e13
        gamma = 5e-5 / 86400.0
        E_react = 1e46
        return mu_over_rj * one_minus_exp * 1.0 * 1.0 * E_react
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{ESO137_UmBase}",
            description=self.description
        )


class ESO137_UmContribution:
    """
    ESO137 full U_m with Heaviside & Quasi amplification
    
    Source: MAIN_1_CoAnQi.cpp (lines 21676-21698)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "ESO137_UmContribution"
        self.description = "ESO137 full U_m with Heaviside & Quasi amplification"
        self.metadata = {}
        self.metadata['source'] = 'source100.cpp'
        self.metadata['physics'] = 'total_magnetic_contribution'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        mu_over_rj = 3.38e23 / 1.496e13
        gamma = 5e-5 / 86400.0
        E_react = 1e46
        base = mu_over_rj * one_minus_exp * 1.0 * 1.0 * E_react
        heaviside_f = 1.0 + 1e13 * 0.01
        quasi_f = 1.0 + 0.01
        return base * heaviside_f * quasi_f
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{ESO137_UmContribution}",
            description=self.description
        )


class NGC1365A_H_SCm:
    """
    NGC1365A heliosphere thickness factor H_SCm
    
    Source: MAIN_1_CoAnQi.cpp (lines 21701-21715)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "NGC1365A_H_SCm"
        self.description = "NGC1365A heliosphere thickness factor H_SCm"
        self.metadata = {}
        self.metadata['source'] = 'source101.cpp'
        self.metadata['physics'] = 'heliosphere_thickness'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return 1.0
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{NGC1365A_H_SCm}",
            description=self.description
        )


class NGC1365A_U_g2:
    """
    NGC1365A U_g2 with heliosphere thickness H_SCm
    
    Source: MAIN_1_CoAnQi.cpp (lines 21717-21739)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "NGC1365A_U_g2"
        self.description = "NGC1365A U_g2 with heliosphere thickness H_SCm"
        self.metadata = {}
        self.metadata['source'] = 'source101.cpp'
        self.metadata['physics'] = 'gravity_with_heliosphere'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        k_2 = 1.0
        rho_sum = 7.09e-36 + 1e-25
        M_s = 1.989e30
        r = 1.496e13
        S_r_Rb = 1.0
        swirl_factor = 1.0 + 0.02 * 400e3
        H_SCm = 1.0
        E_react = 1e46
        return k_2 * (rho_sum * M_s / (r * r)) * S_r_Rb * swirl_factor * H_SCm * E_react
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{NGC1365A_U_g2}",
            description=self.description
        )


class NGC1365A_SwirlFactor:
    """
    NGC1365A solar wind swirl factor (1 + delta*v)
    
    Source: MAIN_1_CoAnQi.cpp (lines 21741-21757)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "NGC1365A_SwirlFactor"
        self.description = "NGC1365A solar wind swirl factor (1 + delta*v)"
        self.metadata = {}
        self.metadata['source'] = 'source101.cpp'
        self.metadata['physics'] = 'solar_wind_modulation'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        delta_sw = 0.02
        v_sw = 400e3
        return 1.0 + delta_sw * v_sw
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{NGC1365A_SwirlFactor}",
            description=self.description
        )


class NGC5728_U_gi:
    """
    NGC5728 indexed gravitational field U_gi
    
    Source: MAIN_1_CoAnQi.cpp (lines 21762-21782)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "NGC5728_U_gi"
        self.description = "NGC5728 indexed gravitational field U_gi"
        self.metadata = {}
        self.metadata['source'] = 'source102.cpp'
        self.metadata['physics'] = 'gravity_field_index'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        i = 1
        k_i = 1.0
        rho_vac_UA = 7.09e-36
        M_s = 1.989e30
        r = 1.496e13
        E_react = 1e46
        return k_i * (rho_vac_UA * M_s / (r * r)) * E_react
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{NGC5728_U_gi}",
            description=self.description
        )


class NGC5728_K_i:
    """
    NGC5728 coupling constant k_i for U_gi summation
    
    Source: MAIN_1_CoAnQi.cpp (lines 21784-21798)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "NGC5728_K_i"
        self.description = "NGC5728 coupling constant k_i for U_gi summation"
        self.metadata = {}
        self.metadata['source'] = 'source102.cpp'
        self.metadata['physics'] = 'coupling_constant'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return 1.0
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{NGC5728_K_i}",
            description=self.description
        )


class NGC5728_SumKUgi:
    """
    NGC5728 total Σk_i·U_gi gravity sum (i=1 to 4)
    
    Source: MAIN_1_CoAnQi.cpp (lines 21800-21825)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "NGC5728_SumKUgi"
        self.description = "NGC5728 total Σk_i·U_gi gravity sum (i=1 to 4)"
        self.metadata = {}
        self.metadata['source'] = 'source102.cpp'
        self.metadata['physics'] = 'total_gravity_sum'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        sum = 0.0
        k_1 = 1.0, k_2 = 1.0, k_3 = 1.0, k_4 = 1.0
        rho_UA = 7.09e-36, rho_SCm = 1e-25
        M_s = 1.989e30, M_bh = 1e37
        r = 1.496e13
        E_react = 1e46
        # Sum U_g1 through U_g4
        sum += k_1 * (rho_UA * M_s / (r * r)) * E_react
        sum += k_2 * (rho_SCm * M_s / (r * r)) * E_react
        sum += k_3 * M_s * E_react
        sum += k_4 * (M_bh / (r * r)) * E_react
        return sum
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{NGC5728_SumKUgi}",
            description=self.description
        )


class MagneticMoment_Mu_j:
    """
    Time-dependent magnetic moment μ_j(t) with solar cycle
    
    Source: MAIN_1_CoAnQi.cpp (lines 21828-21844)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "MagneticMoment_Mu_j"
        self.description = "Time-dependent magnetic moment μ_j(t) with solar cycle"
        self.metadata = {}
        self.metadata['source'] = 'source104.cpp'
        self.metadata['physics'] = 'magnetic_dipole_moment'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        mu_0 = 3.38e23
        result = 0.0  # Assign result
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{MagneticMoment_Mu_j}",
            description=self.description
        )


class MagneticMoment_B_j:
    """
    Magnetic field strength B_j(t) = μ_j/r³
    
    Source: MAIN_1_CoAnQi.cpp (lines 21846-21864)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "MagneticMoment_B_j"
        self.description = "Magnetic field strength B_j(t) = μ_j/r³"
        self.metadata = {}
        self.metadata['source'] = 'source104.cpp'
        self.metadata['physics'] = 'magnetic_field_strength'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        mu_0 = 3.38e23
        r = 1.496e13
        return mu_j / (r * r * r)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{MagneticMoment_B_j}",
            description=self.description
        )


class MagneticMoment_UmContrib:
    """
    U_m magnetic energy with time-varying μ_j(t)
    
    Source: MAIN_1_CoAnQi.cpp (lines 21866-21888)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "MagneticMoment_UmContrib"
        self.description = "U_m magnetic energy with time-varying μ_j(t)"
        self.metadata = {}
        self.metadata['source'] = 'source104.cpp'
        self.metadata['physics'] = 'magnetic_energy_contribution'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        mu_0 = 3.38e23
        r_j = 1.496e13
        gamma = 5e-5 / 86400.0
        E_react = 1e46
        return (mu_j / r_j) * one_minus_exp * 1.0 * 1.0 * E_react
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{MagneticMoment_UmContrib}",
            description=self.description
        )


class BlackHole_M_bh:
    """
    Galactic black hole mass M_bh
    
    Source: MAIN_1_CoAnQi.cpp (lines 21891-21905)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "BlackHole_M_bh"
        self.description = "Galactic black hole mass M_bh"
        self.metadata = {}
        self.metadata['source'] = 'source105.cpp'
        self.metadata['physics'] = 'SMBH_mass'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return 1e37
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{BlackHole_M_bh}",
            description=self.description
        )


class BlackHole_M_bhInMsun:
    """
    Black hole mass in solar masses M_bh/M_sun
    
    Source: MAIN_1_CoAnQi.cpp (lines 21907-21923)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "BlackHole_M_bhInMsun"
        self.description = "Black hole mass in solar masses M_bh/M_sun"
        self.metadata = {}
        self.metadata['source'] = 'source105.cpp'
        self.metadata['physics'] = 'SMBH_solar_masses'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        M_bh = 1e37
        M_sun = 1.989e30
        return M_bh / M_sun
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{BlackHole_M_bhInMsun}",
            description=self.description
        )


class BlackHole_U_b1:
    """
    U_b1 buoyancy term with galactic SMBH
    
    Source: MAIN_1_CoAnQi.cpp (lines 21925-21942)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "BlackHole_U_b1"
        self.description = "U_b1 buoyancy term with galactic SMBH"
        self.metadata = {}
        self.metadata['source'] = 'source105.cpp'
        self.metadata['physics'] = 'buoyancy_SMBH'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        M_bh = 1e37
        D_g = 8.5e3 * 3.086e16
        E_react = 1e46
        return (M_bh / D_g) * E_react
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{BlackHole_U_b1}",
            description=self.description
        )


class NegativeTime_T_n:
    """
    Reciprocation time t_n = -t for time-reversal symmetry
    
    Source: MAIN_1_CoAnQi.cpp (lines 21945-21959)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "NegativeTime_T_n"
        self.description = "Reciprocation time t_n = -t for time-reversal symmetry"
        self.metadata = {}
        self.metadata['source'] = 'source106.cpp'
        self.metadata['physics'] = 'reciprocation_time'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return -t
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{NegativeTime_T_n}",
            description=self.description
        )


class NegativeTime_CosPiTn:
    """
    Cosine of reciprocation phase cos(π·t_n)
    
    Source: MAIN_1_CoAnQi.cpp (lines 21961-21976)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "NegativeTime_CosPiTn"
        self.description = "Cosine of reciprocation phase cos(π·t_n)"
        self.metadata = {}
        self.metadata['source'] = 'source106.cpp'
        self.metadata['physics'] = 'cosine_reciprocation'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        t_n = -t
        result = 0.0  # Assign result
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{NegativeTime_CosPiTn}",
            description=self.description
        )


class NegativeTime_OneMinusExp:
    """
    Time buildup factor (1 - exp) with t_n modulation
    
    Source: MAIN_1_CoAnQi.cpp (lines 21978-21995)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "NegativeTime_OneMinusExp"
        self.description = "Time buildup factor (1 - exp) with t_n modulation"
        self.metadata = {}
        self.metadata['source'] = 'source106.cpp'
        self.metadata['physics'] = 'decay_buildup'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        gamma = 5e-5 / 86400.0
        t_n = -t
        result = 0.0  # Assign result
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{NegativeTime_OneMinusExp}",
            description=self.description
        )


class PiConstant_Pi:
    """
    Pi constant π (3.14159..., Wolfram 312-digit precision)
    
    Source: MAIN_1_CoAnQi.cpp (lines 21998-22013)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "PiConstant_Pi"
        self.description = "Pi constant π (3.14159..., Wolfram 312-digit precision)"
        self.metadata = {}
        self.metadata['source'] = 'source107.cpp'
        self.metadata['physics'] = 'pi_constant'
        self.metadata['digits'] = '312'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return 3.14159265358979323846
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{PiConstant_Pi}",
            description=self.description
        )


class PiConstant_CosPiTn:
    """
    Pi-based cosine phase cos(π·t_n)
    
    Source: MAIN_1_CoAnQi.cpp (lines 22015-22030)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "PiConstant_CosPiTn"
        self.description = "Pi-based cosine phase cos(π·t_n)"
        self.metadata = {}
        self.metadata['source'] = 'source107.cpp'
        self.metadata['physics'] = 'pi_phase_modulation'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        t_n = 0.0
        result = 0.0  # Assign result
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{PiConstant_CosPiTn}",
            description=self.description
        )


class PiConstant_SinOmegaCT:
    """
    Solar cycle sine modulation sin(ω_c·t)
    
    Source: MAIN_1_CoAnQi.cpp (lines 22032-22047)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "PiConstant_SinOmegaCT"
        self.description = "Solar cycle sine modulation sin(ω_c·t)"
        self.metadata = {}
        self.metadata['source'] = 'source107.cpp'
        self.metadata['physics'] = 'solar_cycle_sine'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        result = 0.0  # TODO: Implement computation
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{PiConstant_SinOmegaCT}",
            description=self.description
        )


class Decay_Gamma_s:
    """
    Reciprocation decay rate γ in s^-1
    
    Source: MAIN_1_CoAnQi.cpp (lines 22050-22065)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "Decay_Gamma_s"
        self.description = "Reciprocation decay rate γ in s^-1"
        self.metadata = {}
        self.metadata['source'] = 'source111.cpp'
        self.metadata['physics'] = 'decay_rate'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        gamma_day = 5e-5
        return gamma_day / 86400.0
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{Decay_Gamma_s}",
            description=self.description
        )


class Decay_ExpTerm:
    """
    Exponential decay term exp(-γt·cos(πt_n))
    
    Source: MAIN_1_CoAnQi.cpp (lines 22067-22084)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "Decay_ExpTerm"
        self.description = "Exponential decay term exp(-γt·cos(πt_n))"
        self.metadata = {}
        self.metadata['source'] = 'source111.cpp'
        self.metadata['physics'] = 'exponential_decay'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        gamma = 5e-5 / 86400.0
        t_n = 0.0
        result = 0.0  # Assign result
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{Decay_ExpTerm}",
            description=self.description
        )


class Decay_OneMinusExp:
    """
    Time buildup (1 - exp(-γt·cos(πt_n)))
    
    Source: MAIN_1_CoAnQi.cpp (lines 22086-22103)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "Decay_OneMinusExp"
        self.description = "Time buildup (1 - exp(-γt·cos(πt_n)))"
        self.metadata = {}
        self.metadata['source'] = 'source111.cpp'
        self.metadata['physics'] = 'buildup_factor'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        gamma = 5e-5 / 86400.0
        t_n = 0.0
        result = 0.0  # Assign result
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{Decay_OneMinusExp}",
            description=self.description
        )


class ScmPenetration_P_SCm:
    """
    SCm penetration factor P_SCm
    
    Source: MAIN_1_CoAnQi.cpp (lines 22108-22122)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "ScmPenetration_P_SCm"
        self.description = "SCm penetration factor P_SCm"
        self.metadata = {}
        self.metadata['source'] = 'source112.cpp'
        self.metadata['physics'] = 'penetration_factor'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return 1.0
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{ScmPenetration_P_SCm}",
            description=self.description
        )


class ScmPenetration_UmPlanet:
    """
    Planetary U_m with reduced P_SCm
    
    Source: MAIN_1_CoAnQi.cpp (lines 22124-22140)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "ScmPenetration_UmPlanet"
        self.description = "Planetary U_m with reduced P_SCm"
        self.metadata = {}
        self.metadata['source'] = 'source112.cpp'
        self.metadata['physics'] = 'planetary_magnetic'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        U_m_base = 2.25e-4
        P_SCm_planet = 1e-3
        return U_m_base * P_SCm_planet
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{ScmPenetration_UmPlanet}",
            description=self.description
        )


class ScmReactivity_Kappa_day:
    """
    Reactivity decay rate (day^-1)
    
    Source: MAIN_1_CoAnQi.cpp (lines 22143-22157)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "ScmReactivity_Kappa_day"
        self.description = "Reactivity decay rate (day^-1)"
        self.metadata = {}
        self.metadata['source'] = 'source113.cpp'
        self.metadata['physics'] = 'decay_rate'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return 0.0005
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{ScmReactivity_Kappa_day}",
            description=self.description
        )


class ScmReactivity_E_reactDecay:
    """
    E_react exponential decay
    
    Source: MAIN_1_CoAnQi.cpp (lines 22159-22176)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "ScmReactivity_E_reactDecay"
        self.description = "E_react exponential decay"
        self.metadata = {}
        self.metadata['source'] = 'source113.cpp'
        self.metadata['physics'] = 'exponential_decay'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        E_0 = 1e46
        kappa_day = 0.0005
        t_day = t / 86400.0
        result = 0.0  # Assign result
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{ScmReactivity_E_reactDecay}",
            description=self.description
        )


class SolarCycle_Omega_c:
    """
    Solar cycle angular frequency ω_c
    
    Source: MAIN_1_CoAnQi.cpp (lines 22179-22194)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "SolarCycle_Omega_c"
        self.description = "Solar cycle angular frequency ω_c"
        self.metadata = {}
        self.metadata['source'] = 'source114.cpp'
        self.metadata['physics'] = 'solar_cycle'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        T_cycle = 11.0 * 365.25 * 86400.0
        return 2.0 * 3.14159 / T_cycle
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{SolarCycle_Omega_c}",
            description=self.description
        )


class SolarCycle_PeriodYears:
    """
    Solar cycle period (years)
    
    Source: MAIN_1_CoAnQi.cpp (lines 22196-22210)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "SolarCycle_PeriodYears"
        self.description = "Solar cycle period (years)"
        self.metadata = {}
        self.metadata['source'] = 'source114.cpp'
        self.metadata['physics'] = 'solar_cycle'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return 11.0
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{SolarCycle_PeriodYears}",
            description=self.description
        )


class SolarCycle_MuJ_Modulated:
    """
    Magnetic moment with 11-year modulation
    
    Source: MAIN_1_CoAnQi.cpp (lines 22212-22228)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "SolarCycle_MuJ_Modulated"
        self.description = "Magnetic moment with 11-year modulation"
        self.metadata = {}
        self.metadata['source'] = 'source114.cpp'
        self.metadata['physics'] = 'magnetic_moment'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        mu_0 = 3.38e20
        omega_c = 1.59e-8
        result = 0.0  # Assign result
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{SolarCycle_MuJ_Modulated}",
            description=self.description
        )


class SolarCycle_B_j_Modulated:
    """
    Surface magnetic field with solar cycle
    
    Source: MAIN_1_CoAnQi.cpp (lines 22230-22246)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "SolarCycle_B_j_Modulated"
        self.description = "Surface magnetic field with solar cycle"
        self.metadata = {}
        self.metadata['source'] = 'source114.cpp'
        self.metadata['physics'] = 'surface_field'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        B_0 = 1.0
        omega_c = 1.59e-8
        result = 0.0  # Assign result
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{SolarCycle_B_j_Modulated}",
            description=self.description
        )


class SolarWind_Delta_sw:
    """
    Solar wind modulation δ_sw
    
    Source: MAIN_1_CoAnQi.cpp (lines 22249-22263)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "SolarWind_Delta_sw"
        self.description = "Solar wind modulation δ_sw"
        self.metadata = {}
        self.metadata['source'] = 'source115.cpp'
        self.metadata['physics'] = 'solar_wind'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return 0.01
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{SolarWind_Delta_sw}",
            description=self.description
        )


class SolarWind_ModulationFactor:
    """
    Modulation factor (1 + δ_sw)
    
    Source: MAIN_1_CoAnQi.cpp (lines 22265-22280)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "SolarWind_ModulationFactor"
        self.description = "Modulation factor (1 + δ_sw)"
        self.metadata = {}
        self.metadata['source'] = 'source115.cpp'
        self.metadata['physics'] = 'modulation'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        delta_sw = 0.01
        return (1.0 + delta_sw)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{SolarWind_ModulationFactor}",
            description=self.description
        )


class SolarWind_U_g2_NoMod:
    """
    U_g2 without solar wind modulation
    
    Source: MAIN_1_CoAnQi.cpp (lines 22282-22299)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "SolarWind_U_g2_NoMod"
        self.description = "U_g2 without solar wind modulation"
        self.metadata = {}
        self.metadata['source'] = 'source115.cpp'
        self.metadata['physics'] = 'gravity_unmodulated'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        G = 6.674e-11
        M_s = 1.989e30
        r = 1.496e13
        return (G * M_s) / (r * r)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{SolarWind_U_g2_NoMod}",
            description=self.description
        )


class SolarWindVelocity_V_sw:
    """
    Solar wind velocity v_sw
    
    Source: MAIN_1_CoAnQi.cpp (lines 22302-22316)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "SolarWindVelocity_V_sw"
        self.description = "Solar wind velocity v_sw"
        self.metadata = {}
        self.metadata['source'] = 'source116.cpp'
        self.metadata['physics'] = 'solar_wind'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return 5e5
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{SolarWindVelocity_V_sw}",
            description=self.description
        )


class SolarWindVelocity_V_swKmS:
    """
    Solar wind velocity (km/s)
    
    Source: MAIN_1_CoAnQi.cpp (lines 22318-22332)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "SolarWindVelocity_V_swKmS"
        self.description = "Solar wind velocity (km/s)"
        self.metadata = {}
        self.metadata['source'] = 'source116.cpp'
        self.metadata['physics'] = 'solar_wind'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return 500.0
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{SolarWindVelocity_V_swKmS}",
            description=self.description
        )


class SolarWindVelocity_U_g2_NoSW:
    """
    U_g2 with v_sw=0
    
    Source: MAIN_1_CoAnQi.cpp (lines 22334-22351)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "SolarWindVelocity_U_g2_NoSW"
        self.description = "U_g2 with v_sw=0"
        self.metadata = {}
        self.metadata['source'] = 'source116.cpp'
        self.metadata['physics'] = 'gravity_no_wind'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        G = 6.674e-11
        M_s = 1.989e30
        r = 1.496e13
        return (G * M_s) / (r * r)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{SolarWindVelocity_U_g2_NoSW}",
            description=self.description
        )


class StellarMass_M_s:
    """
    Stellar mass M_s
    
    Source: MAIN_1_CoAnQi.cpp (lines 22354-22368)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "StellarMass_M_s"
        self.description = "Stellar mass M_s"
        self.metadata = {}
        self.metadata['source'] = 'source117.cpp'
        self.metadata['physics'] = 'stellar_mass'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return 1.989e30
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{StellarMass_M_s}",
            description=self.description
        )


class StellarMass_M_sInMsun:
    """
    Stellar mass in solar masses
    
    Source: MAIN_1_CoAnQi.cpp (lines 22370-22384)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "StellarMass_M_sInMsun"
        self.description = "Stellar mass in solar masses"
        self.metadata = {}
        self.metadata['source'] = 'source117.cpp'
        self.metadata['physics'] = 'stellar_mass'

    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return 1.0
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{StellarMass_M_sInMsun}",
            description=self.description
        )


class ReactorEfficiencyTerm:
    """
    Ereact: Core reactivity efficiency = (rho_SCm * v_SCm^2 / rho_A) * exp(-kappa*t)
    
    Source: MAIN_1_CoAnQi.cpp (lines 26838-26857)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "ReactorEfficiency"
        self.description = "Ereact: Core reactivity efficiency = (rho_SCm * v_SCm^2 / rho_A) * exp(-kappa*t)"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        auto it_rho_SCm = params.find("SCm_density")
        result = 0.0  # Assign result
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{ReactorEfficiencyTerm}",
            description=self.description
        )


class MuSTerm:
    """
    mu_s: Magnetic dipole moment = Bs(t) * Rs^3
    
    Source: MAIN_1_CoAnQi.cpp (lines 26859-26874)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "MagneticDipoleMoment"
        self.description = "mu_s: Magnetic dipole moment = Bs(t) * Rs^3"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        auto it_Bs = params.find("Bs_avg")
        auto it_Rs = params.find("Rs")
        return Bs * Rs * Rs * Rs
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{MuSTerm}",
            description=self.description
        )


class GradMsRTerm:
    """
    grad_Ms_r: Surface gravity gradient = G*Ms/Rs^2
    
    Source: MAIN_1_CoAnQi.cpp (lines 26876-26891)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "SurfaceGravityGradient"
        self.description = "grad_Ms_r: Surface gravity gradient = G*Ms/Rs^2"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        auto it_M = params.find("mass")
        auto it_R = params.find("radius")
        return G_SOURCE4 * M / (R * R)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{GradMsRTerm}",
            description=self.description
        )


class BjTerm:
    """
    Bj: Magnetic string field = 1e-3 + 0.4*sin(omega_c*t) + rho_SCm*1e-18
    
    Source: MAIN_1_CoAnQi.cpp (lines 26893-26909)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "MagneticStringField"
        self.description = "Bj: Magnetic string field = 1e-3 + 0.4*sin(omega_c*t) + rho_SCm*1e-18"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        auto it_SCm = params.find("SCm_density")
        result = 0.0  # Assign result
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{BjTerm}",
            description=self.description
        )


class OmegaSTTerm:
    """
    omega_s_t: String rotation frequency = omega_s - 0.4e-6*sin(omega_c*t)
    
    Source: MAIN_1_CoAnQi.cpp (lines 26911-26926)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "StringRotationFrequency"
        self.description = "omega_s_t: String rotation frequency = omega_s - 0.4e-6*sin(omega_c*t)"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        auto it_omega_s = params.find("omega_s")
        auto it_omega_c = params.find("omega_c")
        result = 0.0  # Assign result
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{OmegaSTTerm}",
            description=self.description
        )


class MuJTerm:
    """
    mu_j: String magnetic dipole moment = Bj(t) * Rs^3
    
    Source: MAIN_1_CoAnQi.cpp (lines 26928-26943)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "StringMagneticDipole"
        self.description = "mu_j: String magnetic dipole moment = Bj(t) * Rs^3"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        auto it_Bj = params.find("Bj")
        auto it_Rs = params.find("Rs")
        return Bj * Rs * Rs * Rs
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{MuJTerm}",
            description=self.description
        )


class UniversalGravity1Term:
    """
    Ug1: Magnetic dipole-gradient gravity with defect modulation
    
    Source: MAIN_1_CoAnQi.cpp (lines 26947-26972)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "UniversalGravity1"
        self.description = "Ug1: Magnetic dipole-gradient gravity with defect modulation"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        auto it_mu_s = params.find("mu_s")
        auto it_grad = params.find("grad_Ms_r")
        auto it_tn = params.find("tn")
        result = 0.0  # Assign result
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{UniversalGravity1Term}",
            description=self.description
        )


class UniversalGravity2Term:
    """
    Ug2: Charge-reactivity gravity with solar wind modulation
    
    Source: MAIN_1_CoAnQi.cpp (lines 26974-27005)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "UniversalGravity2"
        self.description = "Ug2: Charge-reactivity gravity with solar wind modulation"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        auto it_QUA = params.find("QUA")
        auto it_M = params.find("mass")
        auto it_r = params.find("radius")
        auto it_Ereact = params.find("Ereact")
        auto it_S = params.find("step_function")
        wind_mod = 1.0 + delta_sw * v_sw
        return k2 * (QA + QUA) * M / (r * r) * S * wind_mod * HSCm * Ereact
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{UniversalGravity2Term}",
            description=self.description
        )


class UniversalGravity3Term:
    """
    Ug3: Magnetic string rotation gravity
    
    Source: MAIN_1_CoAnQi.cpp (lines 27007-27031)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "UniversalGravity3"
        self.description = "Ug3: Magnetic string rotation gravity"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        auto it_Bj = params.find("Bj")
        auto it_omega_s_t = params.find("omega_s_t")
        auto it_Pcore = params.find("Pcore")
        auto it_Ereact = params.find("Ereact")
        result = 0.0  # Assign result
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{UniversalGravity3Term}",
            description=self.description
        )


class UniversalGravity4Term:
    """
    Ug4: Vacuum energy concentration gravity
    
    Source: MAIN_1_CoAnQi.cpp (lines 27033-27061)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "UniversalGravity4"
        self.description = "Ug4: Vacuum energy concentration gravity"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        auto it_Mbh = params.find("Mbh")
        auto it_dg = params.find("dg")
        auto it_tn = params.find("tn")
        return k4 * rho_v * C_concentration * Mbh / dg * decay * cycle * (1 + f_feedback)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{UniversalGravity4Term}",
            description=self.description
        )


class UniversalBuoyancyTerm:
    """
    Ubi: Universal buoyancy from galactic rotation
    
    Source: MAIN_1_CoAnQi.cpp (lines 27063-27093)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "UniversalBuoyancy"
        self.description = "Ubi: Universal buoyancy from galactic rotation"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        auto it_Ugi = params.find("Ugi")
        auto it_Mbh = params.find("Mbh")
        auto it_dg = params.find("dg")
        auto it_rho_sw = params.find("rho_sw")
        auto it_tn = params.find("tn")
        wind_mod = 1.0 + epsilon_sw * rho_sw
        result = 0.0  # Assign result
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{UniversalBuoyancyTerm}",
            description=self.description
        )


class UniversalMagnetismTerm:
    """
    Um: Billion magnetic strings
    
    Source: MAIN_1_CoAnQi.cpp (lines 27095-27124)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "UniversalMagnetism"
        self.description = "Um: Billion magnetic strings"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        auto it_mu_j = params.find("mu_j")
        auto it_rj = params.find("rj")
        auto it_PSCm = params.find("PSCm")
        auto it_Ereact = params.find("Ereact")
        auto it_tn = params.find("tn")
        single = mu_j / rj * decay
        return single * num_strings * PSCm * Ereact
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{UniversalMagnetismTerm}",
            description=self.description
        )


class UniversalAetherTerm:
    """
    A_mu_nu: Cosmic aether metric tensor trace
    
    Source: MAIN_1_CoAnQi.cpp (lines 27126-27145)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "UniversalAether"
        self.description = "A_mu_nu: Cosmic aether metric tensor trace"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        auto it_tn = params.find("tn")
        return -2.0 + 4.0 * mod
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{UniversalAetherTerm}",
            description=self.description
        )


class UnifiedFieldTerm:
    """
    FU: Complete unified field (sum_Ugi + sum_Ubi + Um + A_mu_nu_trace)
    
    Source: MAIN_1_CoAnQi.cpp (lines 27147-27168)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "UnifiedField"
        self.description = "FU: Complete unified field (sum_Ugi + sum_Ubi + Um + A_mu_nu_trace)"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        auto it_Ugi = params.find("sum_Ugi")
        auto it_Ubi = params.find("sum_Ubi")
        auto it_Um = params.find("Um")
        auto it_A = params.find("A_scalar")
        return sum_Ugi + sum_Ubi + Um + A_scalar
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{UnifiedFieldTerm}",
            description=self.description
        )


class MUGECompressedBaseTerm:
    """
    Compressed MUGE base term: G*M/r^2 (Newtonian gravitational acceleration)
    
    Source: MAIN_1_CoAnQi.cpp (lines 27172-27188)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "MUGE_CompressedBase"
        self.description = "Compressed MUGE base term: G*M/r^2 (Newtonian gravitational acceleration)"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        auto it_M = params.find("mass")
        auto it_r = params.find("radius")
        return G_SOURCE4 * M / (r * r)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{MUGECompressedBaseTerm}",
            description=self.description
        )


class MUGEExpansionTerm:
    """
    Hubble expansion factor: 1 + H0*t where H0 = 2.269e-18 s^-1
    
    Source: MAIN_1_CoAnQi.cpp (lines 27190-27203)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "MUGE_Expansion"
        self.description = "Hubble expansion factor: 1 + H0*t where H0 = 2.269e-18 s^-1"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        auto it_t_sys = params.find("t_sys")
        return 1.0 + H0_SOURCE4 * t_sys
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{MUGEExpansionTerm}",
            description=self.description
        )


class MUGESuperAdjustmentTerm:
    """
    Superconductive magnetic adjustment: 1 - B/Bcrit
    
    Source: MAIN_1_CoAnQi.cpp (lines 27205-27221)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "MUGE_SuperconductiveAdjustment"
        self.description = "Superconductive magnetic adjustment: 1 - B/Bcrit"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        auto it_B = params.find("B_field")
        auto it_Bcrit = params.find("Bcrit")
        return 1.0 - B / Bcrit
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{MUGESuperAdjustmentTerm}",
            description=self.description
        )


class MUGEEnvelopeTerm:
    """
    Envelope modulation factor (currently neutral = 1.0)
    
    Source: MAIN_1_CoAnQi.cpp (lines 27223-27234)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "MUGE_Envelope"
        self.description = "Envelope modulation factor (currently neutral = 1.0)"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return 1.0
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{MUGEEnvelopeTerm}",
            description=self.description
        )


class MUGEUgSumTerm:
    """
    Sum of Ug1-4 components (extensible)
    
    Source: MAIN_1_CoAnQi.cpp (lines 27236-27247)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "MUGE_UgSum"
        self.description = "Sum of Ug1-4 components (extensible)"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return 0.0
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{MUGEUgSumTerm}",
            description=self.description
        )


class MUGECosmologicalTerm:
    """
    Cosmological constant term: Lambda*c^2/3 (dark energy acceleration)
    
    Source: MAIN_1_CoAnQi.cpp (lines 27249-27260)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "MUGE_Cosmological"
        self.description = "Cosmological constant term: Lambda*c^2/3 (dark energy acceleration)"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return Lambda_SOURCE4 * c_SOURCE4 * c_SOURCE4 / 3.0
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{MUGECosmologicalTerm}",
            description=self.description
        )


class MUGEQuantumTerm:
    """
    Quantum uncertainty term: (hbar/Delta_xp)*integral_psi*(2*PI/tHubble)
    
    Source: MAIN_1_CoAnQi.cpp (lines 27262-27279)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "MUGE_Quantum"
        self.description = "Quantum uncertainty term: (hbar/Delta_xp)*integral_psi*(2*PI/tHubble)"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return (hbar_SOURCE4 / Delta_x_p) * integral_psi * (2.0 * PI_SOURCE4 / tHubble)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{MUGEQuantumTerm}",
            description=self.description
        )


class MUGEFluidTerm:
    """
    Fluid dynamics term: rho_fluid * Vsys * g_local (Navier-Stokes coupling)
    
    Source: MAIN_1_CoAnQi.cpp (lines 27281-27298)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "MUGE_Fluid"
        self.description = "Fluid dynamics term: rho_fluid * Vsys * g_local (Navier-Stokes coupling)"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        auto it_rho = params.find("rho_fluid")
        auto it_V = params.find("Vsys")
        auto it_g = params.find("g_local")
        return rho_fluid * Vsys * g_local
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{MUGEFluidTerm}",
            description=self.description
        )


class MUGEPerturbationTerm:
    """
    Dark matter perturbation: (M+M_DM)*(delta_rho/rho + 3*G*M/r^3)
    
    Source: MAIN_1_CoAnQi.cpp (lines 27300-27320)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "MUGE_Perturbation"
        self.description = "Dark matter perturbation: (M+M_DM)*(delta_rho/rho + 3*G*M/r^3)"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        auto it_M = params.find("mass")
        auto it_M_DM = params.find("M_DM")
        auto it_delta = params.find("delta_rho_rho")
        auto it_r = params.find("radius")
        return (M + M_DM) * (delta_rho_rho + 3.0 * G_SOURCE4 * M / (r * r * r))
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{MUGEPerturbationTerm}",
            description=self.description
        )


class MUGEResonanceADPMTerm:
    """
    Base DPM acceleration: aDPM = FDPM * fDPM * Evac_neb * c_res * Vsys
    
    Source: MAIN_1_CoAnQi.cpp (lines 27324-27344)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "MUGEResonanceADPM"
        self.description = "Base DPM acceleration: aDPM = FDPM * fDPM * Evac_neb * c_res * Vsys"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return FDPM * fDPM * Evac_neb * c_res * Vsys
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{MUGEResonanceADPMTerm}",
            description=self.description
        )


class MUGEResonanceATHzTerm:
    """
    THz frequency contribution: aTHz = fTHz * Evac_neb * vexp * aDPM / (Evac_ISM * c_res)
    
    Source: MAIN_1_CoAnQi.cpp (lines 27346-27363)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "MUGEResonanceATHz"
        self.description = "THz frequency contribution: aTHz = fTHz * Evac_neb * vexp * aDPM / (Evac_ISM * c_res)"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return (Evac_ISM * c_res != 0.0) ? (fTHz * Evac_neb * vexp * aDPM) / (Evac_ISM * c_res) : 0.0
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{MUGEResonanceATHzTerm}",
            description=self.description
        )


class MUGEResonanceAvacDiffTerm:
    """
    Vacuum energy differential: avac_diff = Delta_Evac * vexp^2 * aDPM / (Evac_neb * c_res^2)
    
    Source: MAIN_1_CoAnQi.cpp (lines 27365-27381)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "MUGEResonanceAvacDiff"
        self.description = "Vacuum energy differential: avac_diff = Delta_Evac * vexp^2 * aDPM / (Evac_neb * c_res^2)"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return (Evac_neb * c_res * c_res != 0.0) ? (Delta_Evac * vexp * vexp * aDPM) / (Evac_neb * c_res * c_res) : 0.0
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{MUGEResonanceAvacDiffTerm}",
            description=self.description
        )


class MUGEResonanceASuperFreqTerm:
    """
    Superconductive frequency resonance: asuper_freq = Fsuper * fTHz * aDPM / (Evac_neb * c_res)
    
    Source: MAIN_1_CoAnQi.cpp (lines 27383-27399)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "MUGEResonanceASuperFreq"
        self.description = "Superconductive frequency resonance: asuper_freq = Fsuper * fTHz * aDPM / (Evac_neb * c_res)"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return (Evac_neb * c_res != 0.0) ? (Fsuper * fTHz * aDPM) / (Evac_neb * c_res) : 0.0
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{MUGEResonanceASuperFreqTerm}",
            description=self.description
        )


class MUGEResonanceAAetherResTerm:
    """
    Aether resonance coupling: aaether_res = UA_SCM * omega_i * fTHz * aDPM * (1 + fTRZ)
    
    Source: MAIN_1_CoAnQi.cpp (lines 27401-27417)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "MUGEResonanceAAetherRes"
        self.description = "Aether resonance coupling: aaether_res = UA_SCM * omega_i * fTHz * aDPM * (1 + fTRZ)"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return UA_SCM * omega_i * fTHz * aDPM * (1.0 + fTRZ)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{MUGEResonanceAAetherResTerm}",
            description=self.description
        )


class MUGEResonanceUg4iTerm:
    """
    Reactor gravity component: Ug4i = k4_res * Ereact * freact * aDPM / (Evac_neb * c_res)
    
    Source: MAIN_1_CoAnQi.cpp (lines 27419-27436)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "MUGEResonanceUg4i"
        self.description = "Reactor gravity component: Ug4i = k4_res * Ereact * freact * aDPM / (Evac_neb * c_res)"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return (Evac_neb * c_res != 0.0) ? (k4_res * Ereact * freact * aDPM) / (Evac_neb * c_res) : 0.0
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{MUGEResonanceUg4iTerm}",
            description=self.description
        )


class MUGEResonanceAQuantumFreqTerm:
    """
    Quantum frequency contribution: aquantum_freq = fquantum * Evac_neb * aDPM / (Evac_ISM * c_res)
    
    Source: MAIN_1_CoAnQi.cpp (lines 27438-27454)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "MUGEResonanceAQuantumFreq"
        self.description = "Quantum frequency contribution: aquantum_freq = fquantum * Evac_neb * aDPM / (Evac_ISM * c_res)"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return (Evac_ISM * c_res != 0.0) ? (fquantum * Evac_neb * aDPM) / (Evac_ISM * c_res) : 0.0
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{MUGEResonanceAQuantumFreqTerm}",
            description=self.description
        )


class MUGEResonanceAAetherFreqTerm:
    """
    Aether frequency component: aAether_freq = fAether * Evac_neb * aDPM / (Evac_ISM * c_res)
    
    Source: MAIN_1_CoAnQi.cpp (lines 27456-27472)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "MUGEResonanceAAetherFreq"
        self.description = "Aether frequency component: aAether_freq = fAether * Evac_neb * aDPM / (Evac_ISM * c_res)"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return (Evac_ISM * c_res != 0.0) ? (fAether * Evac_neb * aDPM) / (Evac_ISM * c_res) : 0.0
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{MUGEResonanceAAetherFreqTerm}",
            description=self.description
        )


class MUGEResonanceAFluidFreqTerm:
    """
    Fluid dynamics frequency: afluid_freq = ffluid * Evac_neb * Vsys / (Evac_ISM * c_res)
    
    Source: MAIN_1_CoAnQi.cpp (lines 27474-27490)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "MUGEResonanceAFluidFreq"
        self.description = "Fluid dynamics frequency: afluid_freq = ffluid * Evac_neb * Vsys / (Evac_ISM * c_res)"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return (Evac_ISM * c_res != 0.0) ? (ffluid * Evac_neb * Vsys) / (Evac_ISM * c_res) : 0.0
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{MUGEResonanceAFluidFreqTerm}",
            description=self.description
        )


class MUGEResonanceOscTerm:
    """
    Oscillatory term (simplified to zero in current implementation)
    
    Source: MAIN_1_CoAnQi.cpp (lines 27492-27503)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "MUGEResonanceOsc"
        self.description = "Oscillatory term (simplified to zero in current implementation)"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return 0.0
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{MUGEResonanceOscTerm}",
            description=self.description
        )


class MUGEResonanceAExpFreqTerm:
    """
    Expansion frequency (Hubble): aexp_freq = fexp * Evac_neb * aDPM / (Evac_ISM * c_res), fexp = 2*PI*H_z*t
    
    Source: MAIN_1_CoAnQi.cpp (lines 27505-27522)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "MUGEResonanceAExpFreq"
        self.description = "Expansion frequency (Hubble): aexp_freq = fexp * Evac_neb * aDPM / (Evac_ISM * c_res), fexp = 2*PI*H_z*t"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        fexp = 2.0 * PI_SOURCE4 * H_z * t
        return (Evac_ISM * c_res != 0.0) ? (fexp * Evac_neb * aDPM) / (Evac_ISM * c_res) : 0.0
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{MUGEResonanceAExpFreqTerm}",
            description=self.description
        )


class MUGEResonanceFTRZTerm:
    """
    TRZ factor component (pass-through): returns fTRZ parameter directly
    
    Source: MAIN_1_CoAnQi.cpp (lines 27524-27535)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "MUGEResonanceFTRZ"
        self.description = "TRZ factor component (pass-through): returns fTRZ parameter directly"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return params.get("fTRZ", 0.1)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{MUGEResonanceFTRZTerm}",
            description=self.description
        )


class MUGEResonanceWormholeTerm:
    """
    Wormhole metric contribution: a_wormhole = f_worm * Evac_neb / (b^2 + r^2)
    
    Source: MAIN_1_CoAnQi.cpp (lines 27537-27553)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "MUGEResonanceWormhole"
        self.description = "Wormhole metric contribution: a_wormhole = f_worm * Evac_neb / (b^2 + r^2)"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        denom = b * b + r * r
        return (denom != 0.0) ? (f_worm * Evac_neb) / denom : 0.0
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{MUGEResonanceWormholeTerm}",
            description=self.description
        )


class TauLeptonDipoleTerm:
    """
    Tau anomalous magnetic moment: a_tau = a_tau_SM + a_tau_BSM (arXiv:2506.15245)
    
    Source: MAIN_1_CoAnQi.cpp (lines 27563-27594)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "TauLeptonDipole"
        self.description = "Tau anomalous magnetic moment: a_tau = a_tau_SM + a_tau_BSM (arXiv:2506.15245)"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        # Get BSM contribution if specified, otherwise return SM value
        total = a_tau_SM + a_tau_BSM
        # Clamp to experimental bounds if requested
        return total
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{TauLeptonDipoleTerm}",
            description=self.description
        )


class CKMVcbTerm:
    """
    CKM matrix element |V_cb| = (39.2+/-0.9)e-3 from Belle II B->D l nu (arXiv:2506.15256)
    
    Source: MAIN_1_CoAnQi.cpp (lines 27598-27626)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "CKMVcb"
        self.description = "CKM matrix element |V_cb| = (39.2+/-0.9)e-3 from Belle II B->D l nu (arXiv:2506.15256)"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        # Return |V_cb|^2 for rate calculations, or |V_cb| directly
        bool squared = params.count("squared")  and  params.get("squared", 0.0) > 0
        return squared ? (V_cb * V_cb) : V_cb
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{CKMVcbTerm}",
            description=self.description
        )


class LFVBranchingTerm:
    """
    LFV branching ratio B0->K*0 tau+/-e-/+: BR < 5.9e-6 at 90% CL (arXiv:2506.15347)
    
    Source: MAIN_1_CoAnQi.cpp (lines 27630-27658)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "LFVBranching"
        self.description = "LFV branching ratio B0->K*0 tau+/-e-/+: BR < 5.9e-6 at 90% CL (arXiv:2506.15347)"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        # Return theoretical BR if specified, otherwise return experimental limit
        return params.get("BR_theory", 0.0)
        # Return 90% or 95% CL limit based on parameter
        bool use_95CL = params.count("use_95CL")  and  params.get("use_95CL", 0.0) > 0
        return use_95CL ? BR_limit_95CL : BR_limit_90CL
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{LFVBranchingTerm}",
            description=self.description
        )


class VectorLikeQuarkTerm:
    """
    VLQ coupling kappa(m): singlet T [0.22,0.52], triplet TBY [0.14,0.46] (arXiv:2506.15515)
    
    Source: MAIN_1_CoAnQi.cpp (lines 27662-27699)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "VectorLikeQuark"
        self.description = "VLQ coupling kappa(m): singlet T [0.22,0.52], triplet TBY [0.14,0.46] (arXiv:2506.15515)"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        bool is_triplet = params.count("is_triplet")  and  params.get("is_triplet", 0.0) > 0
        # Interpolate kappa bounds based on mass
        kappa_min = is_triplet ? kappa_TBY_min : kappa_T_min
        kappa_max = is_triplet ? kappa_TBY_max : kappa_T_max
        # Return central value of allowed kappa range at given mass
        return kappa_min + mass_frac * (kappa_max - kappa_min)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{VectorLikeQuarkTerm}",
            description=self.description
        )


class UnifiedFieldTerm_SOURCE4:
    """
    FU: Complete unified field (sum_Ugi + sum_Ubi + Um + A_mu_nu_trace)
    
    Source: MAIN_1_CoAnQi.cpp (lines 27703-27726)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "UnifiedField_SOURCE4"
        self.description = "FU: Complete unified field (sum_Ugi + sum_Ubi + Um + A_mu_nu_trace)"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        auto it_Ugi = params.find("sum_Ugi")
        auto it_Ubi = params.find("sum_Ubi")
        auto it_Um = params.find("Um")
        auto it_A = params.find("A_scalar")
        return sum_Ugi + sum_Ubi + Um + A_scalar
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{UnifiedFieldTerm_SOURCE4}",
            description=self.description
        )


class MUGESuperAdjTerm:
    """
    Superconductive magnetic adjustment: 1 - B/Bcrit
    
    Source: MAIN_1_CoAnQi.cpp (lines 27730-27754)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "MUGE_SuperconductiveAdjustment"
        self.description = "Superconductive magnetic adjustment: 1 - B/Bcrit"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        auto it_B = params.find("B_field")
        auto it_Bcrit = params.find("Bcrit")
        if Bcrit_val == 0.0: return 1.0
        return 1.0 - B_val / Bcrit_val
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{MUGESuperAdjTerm}",
            description=self.description
        )


class MUGECosmTerm:
    """
    Cosmological constant term: Lambda*c^2/3
    
    Source: MAIN_1_CoAnQi.cpp (lines 27756-27769)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "MUGE_Cosmological"
        self.description = "Cosmological constant term: Lambda*c^2/3"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return Lambda_SOURCE4 * c_SOURCE4 * c_SOURCE4 / 3.0
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{MUGECosmTerm}",
            description=self.description
        )


class SGR1745MagnetarTerm:
    """
    SGR 1745-2900: Magnetar system (I=1e21, M=2.984e30 kg, B=1e10 T)
    
    Source: MAIN_1_CoAnQi.cpp (lines 27773-27786)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "SGR1745Magnetar"
        self.description = "SGR 1745-2900: Magnetar system (I=1e21, M=2.984e30 kg, B=1e10 T)"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return compute_resonance_MUGE_SOURCE4(sgr1745_SOURCE4, res_params_default_SOURCE4)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{SGR1745MagnetarTerm}",
            description=self.description
        )


class SagittariusAStarTerm:
    """
    Sagittarius A*: Supermassive black hole (M=8.155e36 kg, M_DM=1e37 kg)
    
    Source: MAIN_1_CoAnQi.cpp (lines 27788-27801)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "SagittariusAStar"
        self.description = "Sagittarius A*: Supermassive black hole (M=8.155e36 kg, M_DM=1e37 kg)"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return compute_resonance_MUGE_SOURCE4(sagA_SOURCE4, res_params_default_SOURCE4)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{SagittariusAStarTerm}",
            description=self.description
        )


class TapestryStarbirthTerm:
    """
    Tapestry of Blazing Starbirth: Nebula (M=1.989e35 kg, Vsys=1e53 m^3)
    
    Source: MAIN_1_CoAnQi.cpp (lines 27803-27816)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "TapestryStarbirth"
        self.description = "Tapestry of Blazing Starbirth: Nebula (M=1.989e35 kg, Vsys=1e53 m^3)"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return compute_resonance_MUGE_SOURCE4(tapestry_SOURCE4, res_params_default_SOURCE4)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{TapestryStarbirthTerm}",
            description=self.description
        )


class Westerlund2ClusterTerm:
    """
    Westerlund 2: Stellar cluster (similar to Tapestry parameters)
    
    Source: MAIN_1_CoAnQi.cpp (lines 27818-27831)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "Westerlund2Cluster"
        self.description = "Westerlund 2: Stellar cluster (similar to Tapestry parameters)"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return compute_resonance_MUGE_SOURCE4(westerlund_SOURCE4, res_params_default_SOURCE4)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{Westerlund2ClusterTerm}",
            description=self.description
        )


class PillarsCreationTerm:
    """
    Pillars of Creation: Molecular cloud (M=1.989e32 kg, r=1 ly)
    
    Source: MAIN_1_CoAnQi.cpp (lines 27833-27846)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "PillarsCreation"
        self.description = "Pillars of Creation: Molecular cloud (M=1.989e32 kg, r=1 ly)"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return compute_resonance_MUGE_SOURCE4(pillars_SOURCE4, res_params_default_SOURCE4)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{PillarsCreationTerm}",
            description=self.description
        )


class RingsRelativityTerm:
    """
    Rings of Relativity: Cosmological structure (M=1.989e36 kg, z=0.01)
    
    Source: MAIN_1_CoAnQi.cpp (lines 27848-27861)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "RingsRelativity"
        self.description = "Rings of Relativity: Cosmological structure (M=1.989e36 kg, z=0.01)"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return compute_resonance_MUGE_SOURCE4(rings_SOURCE4, res_params_default_SOURCE4)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{RingsRelativityTerm}",
            description=self.description
        )


class StudentGuideUniverseTerm:
    """
    Student's Guide to the Universe: Observable universe (M=1e53 kg, r=10 Gly)
    
    Source: MAIN_1_CoAnQi.cpp (lines 27863-27876)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "StudentGuideUniverse"
        self.description = "Student's Guide to the Universe: Observable universe (M=1e53 kg, r=10 Gly)"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        return compute_resonance_MUGE_SOURCE4(student_guide_SOURCE4, res_params_default_SOURCE4)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{StudentGuideUniverseTerm}",
            description=self.description
        )


class CompressedMUGETerm_SOURCE4:
    """
    Compressed MUGE: 9-term gravity equation wrapper
    
    Source: MAIN_1_CoAnQi.cpp (lines 27880-27900)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "CompressedMUGE_SOURCE4"
        self.description = "Compressed MUGE: 9-term gravity equation wrapper"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        MUGESystem_SOURCE4 system
        auto it_mass = params.find("mass")
        auto it_r = params.find("radius")
        if it_mass != params.end(:
            ) system.M = it_mass->second
        if it_r != params.end(:
            ) system.r = it_r->second
        return compute_compressed_MUGE_SOURCE4(system)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{CompressedMUGETerm_SOURCE4}",
            description=self.description
        )


class ResonanceMUGETerm_SOURCE4:
    """
    Resonance MUGE: 13-term + wormhole resonance equation wrapper
    
    Source: MAIN_1_CoAnQi.cpp (lines 27902-27922)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "ResonanceMUGE_SOURCE4"
        self.description = "Resonance MUGE: 13-term + wormhole resonance equation wrapper"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        MUGESystem_SOURCE4 system
        auto it_mass = params.find("mass")
        auto it_r = params.find("radius")
        if it_mass != params.end(:
            ) system.M = it_mass->second
        if it_r != params.end(:
            ) system.r = it_r->second
        return compute_resonance_MUGE_SOURCE4(system, res_params_default_SOURCE4)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{ResonanceMUGETerm_SOURCE4}",
            description=self.description
        )


class SMBHDynamicVacuumTerm_SOURCE82:
    """
    SMBH time-varying vacuum energy: E_vac = amp * rho_vac * sin(freq*t)
    
    Source: MAIN_1_CoAnQi.cpp (lines 28320-28341)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "SMBH_DynamicVacuum"
        self.description = "SMBH time-varying vacuum energy: E_vac = amp * rho_vac * sin(freq*t)"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        auto it_amp = params.find("amplitude")
        auto it_rho = params.find("rho_vac_UA")
        auto it_freq = params.find("frequency")
        result = 0.0  # Assign result
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{SMBHDynamicVacuumTerm_SOURCE82}",
            description=self.description
        )


class SMBHQuantumCouplingTerm_SOURCE82:
    """
    SMBH quantum coupling: F_q = g_coupling * (M_bh/M_sun)^(1/3) * exp(-r/lambda)
    
    Source: MAIN_1_CoAnQi.cpp (lines 28343-28369)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "SMBH_QuantumCoupling"
        self.description = "SMBH quantum coupling: F_q = g_coupling * (M_bh/M_sun)^(1/3) * exp(-r/lambda)"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        auto it_coupling = params.find("coupling_strength")
        auto it_M_bh = params.find("M_bh")
        auto it_M_sun = params.find("M_sun")
        auto it_r = params.find("r")
        auto it_lambda = params.find("lambda_Q")
        result = 0.0  # Assign result
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{SMBHQuantumCouplingTerm_SOURCE82}",
            description=self.description
        )


class SMBHMSigmaRelationTerm_SOURCE82:
    """
    M-s relation: M_bh = M_0 * (sigma/sigma_0)^4.24
    
    Source: MAIN_1_CoAnQi.cpp (lines 28371-28394)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "SMBH_MSigmaRelation"
        self.description = "M-s relation: M_bh = M_0 * (sigma/sigma_0)^4.24"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        auto it_M0 = params.find("M_0")
        auto it_sigma = params.find("sigma")
        auto it_sigma0 = params.find("sigma_0")
        auto it_alpha = params.find("alpha_M_sigma")
        result = 0.0  # Assign result
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{SMBHMSigmaRelationTerm_SOURCE82}",
            description=self.description
        )


class SMBHBulgeGravityTerm_SOURCE82:
    """
    Bulge gravitational potential: F = -G*M_bulge/R_bulge
    
    Source: MAIN_1_CoAnQi.cpp (lines 28396-28417)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "SMBH_BulgeGravity"
        self.description = "Bulge gravitational potential: F = -G*M_bulge/R_bulge"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        auto it_G = params.find("G")
        auto it_M_bulge = params.find("M_bulge")
        auto it_R_bulge = params.find("R_bulge")
        return -G * M_bulge / R_bulge
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{SMBHBulgeGravityTerm_SOURCE82}",
            description=self.description
        )


class SMBHUg1Term_SOURCE82:
    """
    SMBH Ug1 (Newtonian): G*M_bh/r^2
    
    Source: MAIN_1_CoAnQi.cpp (lines 28419-28437)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "SMBH_Ug1"
        self.description = "SMBH Ug1 (Newtonian): G*M_bh/r^2"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        auto it_G = params.find("G")
        auto it_M_bh = params.find("M_bh")
        auto it_r = params.find("r")
        return dpm_emergent_ug1(M_bh, r)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{SMBHUg1Term_SOURCE82}",
            description=self.description
        )


class SMBHUg2Term_SOURCE82:
    """
    SMBH Ug2 (GR correction): Ug1 * 3GM/(rc^2)
    
    Source: MAIN_1_CoAnQi.cpp (lines 28439-28460)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "SMBH_Ug2"
        self.description = "SMBH Ug2 (GR correction): Ug1 * 3GM/(rc^2)"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        auto it_G = params.find("G")
        auto it_M_bh = params.find("M_bh")
        auto it_r = params.find("r")
        auto it_c = params.find("c")
        return Ug1 * (3.0 * G * M_bh) / (r * c * c)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{SMBHUg2Term_SOURCE82}",
            description=self.description
        )


class SMBHUg3Term_SOURCE82:
    """
    SMBH Ug3 (quantum gravity): k_monopole * exp(-r/lambda_Q)
    
    Source: MAIN_1_CoAnQi.cpp (lines 28462-28480)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "SMBH_Ug3"
        self.description = "SMBH Ug3 (quantum gravity): k_monopole * exp(-r/lambda_Q)"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        auto it_k = params.find("k_monopole")
        auto it_r = params.find("r")
        auto it_lambda = params.find("lambda_Q")
        result = 0.0  # Assign result
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{SMBHUg3Term_SOURCE82}",
            description=self.description
        )


class SMBHUg4Term_SOURCE82:
    """
    SMBH Ug4 (vacuum energy): rho_vac*c^2*r^3/(3*M_bh)
    
    Source: MAIN_1_CoAnQi.cpp (lines 28482-28502)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "SMBH_Ug4"
        self.description = "SMBH Ug4 (vacuum energy): rho_vac*c^2*r^3/(3*M_bh)"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        auto it_rho = params.find("rho_vac_UA")
        auto it_c = params.find("c")
        auto it_r = params.find("r")
        auto it_M_bh = params.find("M_bh")
        return rho_vac * c * c * (r * r * r) / (3.0 * M_bh)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{SMBHUg4Term_SOURCE82}",
            description=self.description
        )


class SMBHReactorEfficiencyTerm_SOURCE82:
    """
    Reactor efficiency decay: E_react = E_0 * exp(-t/tau)
    
    Source: MAIN_1_CoAnQi.cpp (lines 28504-28520)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "SMBH_ReactorEfficiency"
        self.description = "Reactor efficiency decay: E_react = E_0 * exp(-t/tau)"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        auto it_E0 = params.find("E_react_0")
        auto it_tau = params.find("tau_decay")
        result = 0.0  # Assign result
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{SMBHReactorEfficiencyTerm_SOURCE82}",
            description=self.description
        )


class SMBHPseudoMonopoleTerm_SOURCE82:
    """
    Pseudo-monopole state shift: delta_n(state)
    
    Source: MAIN_1_CoAnQi.cpp (lines 28522-28538)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "SMBH_PseudoMonopole"
        self.description = "Pseudo-monopole state shift: delta_n(state)"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        auto it_state = params.find("state_n")
        auto it_shift = params.find("delta_shift")
        return shift * state_n
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{SMBHPseudoMonopoleTerm_SOURCE82}",
            description=self.description
        )


class SMBHRedshiftCorrectionTerm_SOURCE82:
    """
    Redshift mass correction: M_obs = M_intrinsic * (1+z)
    
    Source: MAIN_1_CoAnQi.cpp (lines 28540-28556)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "SMBH_RedshiftCorrection"
        self.description = "Redshift mass correction: M_obs = M_intrinsic * (1+z)"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        auto it_M = params.find("M_bh")
        auto it_z = params.find("redshift")
        return M_bh * (1.0 + z)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{SMBHRedshiftCorrectionTerm_SOURCE82}",
            description=self.description
        )


class SMBHUiTerm_SOURCE82:
    """
    SMBH internal energy: Ui = 3/2 * k_B * T * (M_bh/m_p)
    
    Source: MAIN_1_CoAnQi.cpp (lines 28558-28578)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "SMBH_Ui"
        self.description = "SMBH internal energy: Ui = 3/2 * k_B * T * (M_bh/m_p)"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        auto it_k = params.find("k_B")
        auto it_T = params.find("T_virial")
        auto it_M_bh = params.find("M_bh")
        auto it_mp = params.find("m_proton")
        return 1.5 * k_B * T * (M_bh / m_p)
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{SMBHUiTerm_SOURCE82}",
            description=self.description
        )


class SMBHUmTerm_SOURCE82:
    """
    SMBH magnetic energy: Um = B^2/(2*mu_0) * V_bulge
    
    Source: MAIN_1_CoAnQi.cpp (lines 28580-28599)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "SMBH_Um"
        self.description = "SMBH magnetic energy: Um = B^2/(2*mu_0) * V_bulge"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        auto it_B = params.find("B_field")
        auto it_mu0 = params.find("mu_0")
        auto it_R = params.find("R_bulge")
        return (B * B) / (2.0 * mu_0) * V_bulge
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{SMBHUmTerm_SOURCE82}",
            description=self.description
        )


class SMBHOmegaSGalacticTerm_SOURCE82:
    """
    Galactic angular velocity: omega_s = v_circ / R_bulge
    
    Source: MAIN_1_CoAnQi.cpp (lines 28601-28617)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "SMBH_OmegaS"
        self.description = "Galactic angular velocity: omega_s = v_circ / R_bulge"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        auto it_v = params.find("v_circ")
        auto it_R = params.find("R_bulge")
        return v_circ / R
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{SMBHOmegaSGalacticTerm_SOURCE82}",
            description=self.description
        )


class SMBHCosmicTimeTerm_SOURCE82:
    """
    Cosmic time: t_cosmic = t_0 / (1+z)^(3/2)
    
    Source: MAIN_1_CoAnQi.cpp (lines 28619-28635)
    Naming: Preserved exactly from C++ PhysicsTerm class
    """
    
    def __init__(self):
        self.name = "SMBH_CosmicTime"
        self.description = "Cosmic time: t_cosmic = t_0 / (1+z)^(3/2)"
        self.metadata = {}


    
    def get_param(self, key: str, default: float = 0.0) -> float:
        """Get dynamic parameter (compatibility layer)"""
        return default  # TODO: Implement dynamic parameter storage
    
    def compute(self, t: float = 0.0, params: dict = None) -> EquationResult:
        """
        Compute physics term value
        
        Args:
            t: Time parameter (seconds)
            params: Dictionary of input parameters
            
        Returns:
            EquationResult with computed value and LaTeX equation
        """
        if params is None:
            params = {}
        
        # Computation (auto-converted from C++)
        auto it_t0 = params.find("t_universe")
        auto it_z = params.find("redshift")
        result = 0.0  # Assign result
        
        return EquationResult(
            name=self.name,
            value=result if 'result' in dir() else 0.0,
            unit="SI",
            latex=r"\text{SMBHCosmicTimeTerm_SOURCE82}",
            description=self.description
        )


# ========== CALCULATOR REGISTRY ==========
EXTRACTED_CALCULATORS = {
    'DynamicVacuumTerm': DynamicVacuumTerm,
    'QuantumCouplingTerm': QuantumCouplingTerm,
    'DarkMatterHaloTerm': DarkMatterHaloTerm,
    'VacuumEnergyTerm': VacuumEnergyTerm,
    'QuantumEntanglementTerm': QuantumEntanglementTerm,
    'CosmicNeutrinoTerm': CosmicNeutrinoTerm,
    'MultiSystemUQFFTerm': MultiSystemUQFFTerm,
    'DPMResonanceTerm': DPMResonanceTerm,
    'LENRExtendedTerm': LENRExtendedTerm,
    'SMBHAccretionTerm': SMBHAccretionTerm,
    'TDETerm': TDETerm,
    'NebulaUQFFTerm': NebulaUQFFTerm,
    'GasIonizationTerm': GasIonizationTerm,
    'NebulaExpansionTerm': NebulaExpansionTerm,
    'BuoyancyUQFFTerm': BuoyancyUQFFTerm,
    'InflationBuoyancyTerm': InflationBuoyancyTerm,
    'SuperconductiveTerm': SuperconductiveTerm,
    'NeutronScatteringTerm': NeutronScatteringTerm,
    'AstroSystemUQFFTerm': AstroSystemUQFFTerm,
    'DipoleVortexTerm': DipoleVortexTerm,
    'QuantumState26Term': QuantumState26Term,
    'TriadicScaleTerm': TriadicScaleTerm,
    'UQFFMasterTerm': UQFFMasterTerm,
    'ElectrostaticBarrierTerm': ElectrostaticBarrierTerm,
    'ElectricFieldTerm': ElectricFieldTerm,
    'NeutronProductionTerm': NeutronProductionTerm,
    'CelestialBodyTerm': CelestialBodyTerm,
    'MUGETerm': MUGETerm,
    'QuasarJetTerm': QuasarJetTerm,
    'ReactorEnergyTerm': ReactorEnergyTerm,
    'MagneticDipoleTerm': MagneticDipoleTerm,
    'MagneticJetFieldTerm': MagneticJetFieldTerm,
    'UnifiedBuoyancyTerm': UnifiedBuoyancyTerm,
    'CompressedMUGEBaseTerm': CompressedMUGEBaseTerm,
    'CompressedMUGEExpansionTerm': CompressedMUGEExpansionTerm,
    'SuperconductiveAdjustmentTerm': SuperconductiveAdjustmentTerm,
    'CosmologicalConstantTerm': CosmologicalConstantTerm,
    'QuantumUncertaintyTerm': QuantumUncertaintyTerm,
    'FluidDynamicsTerm': FluidDynamicsTerm,
    'DensityPerturbationTerm': DensityPerturbationTerm,
    'UQFFModule5Term': UQFFModule5Term,
    'ResonanceMUGETerm': ResonanceMUGETerm,
    'StateExportTerm': StateExportTerm,
    'TimeVaryingRotationTerm': TimeVaryingRotationTerm,
    'NavierStokesFluidTerm': NavierStokesFluidTerm,
    'SpacetimeMetricModulationTerm': SpacetimeMetricModulationTerm,
    'FullUnifiedFieldTerm': FullUnifiedFieldTerm,
    'ResonanceMUGE_DPMTerm': ResonanceMUGE_DPMTerm,
    'ResonanceMUGE_THzTerm': ResonanceMUGE_THzTerm,
    'VacuumEnergyDifferentialTerm': VacuumEnergyDifferentialTerm,
    'SuperconductiveFrequencyTerm': SuperconductiveFrequencyTerm,
    'AetherResonanceTerm': AetherResonanceTerm,
    'QuantumFrequencyTerm': QuantumFrequencyTerm,
    'AetherFrequencyTerm': AetherFrequencyTerm,
    'WormholeContributionTerm': WormholeContributionTerm,
    'UnifiedFieldUg1Term': UnifiedFieldUg1Term,
    'UnifiedFieldUg2Term': UnifiedFieldUg2Term,
    'UnifiedFieldUg3Term': UnifiedFieldUg3Term,
    'UnifiedFieldUg4Term': UnifiedFieldUg4Term,
    'UnifiedFieldUmTerm': UnifiedFieldUmTerm,
    'SpacetimeMetricTerm': SpacetimeMetricTerm,
    'CompressedMUGETerm': CompressedMUGETerm,
    'ResonanceMUGE_VacuumDiffTerm': ResonanceMUGE_VacuumDiffTerm,
    'ResonanceMUGE_SuperFreqTerm': ResonanceMUGE_SuperFreqTerm,
    'ResonanceMUGE_AetherResTerm': ResonanceMUGE_AetherResTerm,
    'ResonanceMUGE_QuantumFreqTerm': ResonanceMUGE_QuantumFreqTerm,
    'YAMLConfigTerm': YAMLConfigTerm,
    'UQFFCoreBuoyancyTerm': UQFFCoreBuoyancyTerm,
    'VacuumRepulsionTerm': VacuumRepulsionTerm,
    'THzShockCommunicationTerm': THzShockCommunicationTerm,
    'ConduitFormationTerm': ConduitFormationTerm,
    'SpookyActionTerm': SpookyActionTerm,
    'DPMResonanceEnergyTerm': DPMResonanceEnergyTerm,
    'Triadic26LayerTerm': Triadic26LayerTerm,
    'MagnetarCoreTerm': MagnetarCoreTerm,
    'MagnetarLambdaTerm': MagnetarLambdaTerm,
    'MagnetarEMTerm': MagnetarEMTerm,
    'MagnetarGWTerm': MagnetarGWTerm,
    'MagnetarQuantumTerm': MagnetarQuantumTerm,
    'MagnetarFluidTerm': MagnetarFluidTerm,
    'MagnetarOscillatoryTerm': MagnetarOscillatoryTerm,
    'MagnetarDarkMatterTerm': MagnetarDarkMatterTerm,
    'MagnetarMagneticEnergyTerm': MagnetarMagneticEnergyTerm,
    'MagnetarDecayTerm': MagnetarDecayTerm,
    'Magnetar0501CoreTerm': Magnetar0501CoreTerm,
    'Magnetar0501LambdaTerm': Magnetar0501LambdaTerm,
    'Magnetar0501EMTerm': Magnetar0501EMTerm,
    'Magnetar0501GWTerm': Magnetar0501GWTerm,
    'Magnetar0501QuantumTerm': Magnetar0501QuantumTerm,
    'Magnetar0501FluidTerm': Magnetar0501FluidTerm,
    'Magnetar0501OscillatoryTerm': Magnetar0501OscillatoryTerm,
    'Magnetar0501DarkMatterTerm': Magnetar0501DarkMatterTerm,
    'SgrAStarCoreTerm': SgrAStarCoreTerm,
    'SgrAStarLambdaTerm': SgrAStarLambdaTerm,
    'SgrAStarEMTerm': SgrAStarEMTerm,
    'SgrAStarGWTerm': SgrAStarGWTerm,
    'SgrAStarQuantumTerm': SgrAStarQuantumTerm,
    'SgrAStarFluidTerm': SgrAStarFluidTerm,
    'SgrAStarOscillatoryTerm': SgrAStarOscillatoryTerm,
    'SgrAStarDarkMatterTerm': SgrAStarDarkMatterTerm,
    'StarbirthCoreTerm': StarbirthCoreTerm,
    'StarbirthLambdaTerm': StarbirthLambdaTerm,
    'StarbirthUQFFTerm': StarbirthUQFFTerm,
    'StarbirthEMTerm': StarbirthEMTerm,
    'StarbirthQuantumTerm': StarbirthQuantumTerm,
    'StarbirthFluidTerm': StarbirthFluidTerm,
    'StarbirthOscillatoryTerm': StarbirthOscillatoryTerm,
    'StarbirthDarkMatterTerm': StarbirthDarkMatterTerm,
    'StarbirthWindTerm': StarbirthWindTerm,
    'Westerlund2CoreTerm': Westerlund2CoreTerm,
    'Westerlund2LambdaTerm': Westerlund2LambdaTerm,
    'Westerlund2UQFFTerm': Westerlund2UQFFTerm,
    'Westerlund2EMTerm': Westerlund2EMTerm,
    'Westerlund2QuantumTerm': Westerlund2QuantumTerm,
    'Westerlund2FluidTerm': Westerlund2FluidTerm,
    'Westerlund2OscillatoryTerm': Westerlund2OscillatoryTerm,
    'Westerlund2DarkMatterTerm': Westerlund2DarkMatterTerm,
    'Westerlund2WindTerm': Westerlund2WindTerm,
    'PillarsCoreTerm': PillarsCoreTerm,
    'PillarsLambdaTerm': PillarsLambdaTerm,
    'PillarsUQFFTerm': PillarsUQFFTerm,
    'PillarsEMTerm': PillarsEMTerm,
    'PillarsQuantumTerm': PillarsQuantumTerm,
    'PillarsFluidTerm': PillarsFluidTerm,
    'PillarsOscillatoryTerm': PillarsOscillatoryTerm,
    'PillarsDarkMatterTerm': PillarsDarkMatterTerm,
    'PillarsWindTerm': PillarsWindTerm,
    'PillarsErosionTerm': PillarsErosionTerm,
    'EinsteinRingCoreTerm': EinsteinRingCoreTerm,
    'EinsteinRingLambdaTerm': EinsteinRingLambdaTerm,
    'EinsteinRingUQFFTerm': EinsteinRingUQFFTerm,
    'EinsteinRingEMTerm': EinsteinRingEMTerm,
    'EinsteinRingQuantumTerm': EinsteinRingQuantumTerm,
    'EinsteinRingFluidTerm': EinsteinRingFluidTerm,
    'EinsteinRingOscillatoryTerm': EinsteinRingOscillatoryTerm,
    'EinsteinRingDarkMatterTerm': EinsteinRingDarkMatterTerm,
    'EinsteinRingWindTerm': EinsteinRingWindTerm,
    'NGC2525CoreTerm': NGC2525CoreTerm,
    'NGC2525BlackHoleTerm': NGC2525BlackHoleTerm,
    'NGC2525LambdaTerm': NGC2525LambdaTerm,
    'NGC2525UQFFTerm': NGC2525UQFFTerm,
    'NGC2525EMTerm': NGC2525EMTerm,
    'NGC2525QuantumTerm': NGC2525QuantumTerm,
    'NGC2525FluidTerm': NGC2525FluidTerm,
    'NGC2525OscillatoryTerm': NGC2525OscillatoryTerm,
    'NGC2525DarkMatterTerm': NGC2525DarkMatterTerm,
    'NGC2525SupernovaTerm': NGC2525SupernovaTerm,
    'NGC3603CoreTerm': NGC3603CoreTerm,
    'NGC3603LambdaTerm': NGC3603LambdaTerm,
    'NGC3603UQFFTerm': NGC3603UQFFTerm,
    'NGC3603EMTerm': NGC3603EMTerm,
    'NGC3603QuantumTerm': NGC3603QuantumTerm,
    'NGC3603FluidTerm': NGC3603FluidTerm,
    'NGC3603OscillatoryTerm': NGC3603OscillatoryTerm,
    'NGC3603DarkMatterTerm': NGC3603DarkMatterTerm,
    'NGC3603WindTerm': NGC3603WindTerm,
    'NGC3603CavityPressureTerm': NGC3603CavityPressureTerm,
    'BubbleNebulaCoreTerm': BubbleNebulaCoreTerm,
    'BubbleNebulaLambdaTerm': BubbleNebulaLambdaTerm,
    'BubbleNebulaUQFFTerm': BubbleNebulaUQFFTerm,
    'BubbleNebulaEMTerm': BubbleNebulaEMTerm,
    'BubbleNebulaQuantumTerm': BubbleNebulaQuantumTerm,
    'BubbleNebulaFluidTerm': BubbleNebulaFluidTerm,
    'BubbleNebulaOscillatoryTerm': BubbleNebulaOscillatoryTerm,
    'BubbleNebulaDarkMatterTerm': BubbleNebulaDarkMatterTerm,
    'BubbleNebulaWindTerm': BubbleNebulaWindTerm,
    'BubbleNebulaExpansionTerm': BubbleNebulaExpansionTerm,
    'AntennaeGalaxiesCoreTerm': AntennaeGalaxiesCoreTerm,
    'AntennaeGalaxiesLambdaTerm': AntennaeGalaxiesLambdaTerm,
    'AntennaeGalaxiesUQFFTerm': AntennaeGalaxiesUQFFTerm,
    'AntennaeGalaxiesEMTerm': AntennaeGalaxiesEMTerm,
    'AntennaeGalaxiesQuantumTerm': AntennaeGalaxiesQuantumTerm,
    'AntennaeGalaxiesFluidTerm': AntennaeGalaxiesFluidTerm,
    'AntennaeGalaxiesOscillatoryTerm': AntennaeGalaxiesOscillatoryTerm,
    'AntennaeGalaxiesDarkMatterTerm': AntennaeGalaxiesDarkMatterTerm,
    'AntennaeGalaxiesWindTerm': AntennaeGalaxiesWindTerm,
    'AntennaeGalaxiesMergerInteractionTerm': AntennaeGalaxiesMergerInteractionTerm,
    'HorseheadNebulaCoreTerm': HorseheadNebulaCoreTerm,
    'HorseheadNebulaLambdaTerm': HorseheadNebulaLambdaTerm,
    'HorseheadNebulaUQFFTerm': HorseheadNebulaUQFFTerm,
    'HorseheadNebulaEMTerm': HorseheadNebulaEMTerm,
    'HorseheadNebulaQuantumTerm': HorseheadNebulaQuantumTerm,
    'HorseheadNebulaFluidTerm': HorseheadNebulaFluidTerm,
    'HorseheadNebulaOscillatoryTerm': HorseheadNebulaOscillatoryTerm,
    'HorseheadNebulaDarkMatterTerm': HorseheadNebulaDarkMatterTerm,
    'HorseheadNebulaWindTerm': HorseheadNebulaWindTerm,
    'HorseheadNebulaErosionTerm': HorseheadNebulaErosionTerm,
    'NGC1275CoreTerm': NGC1275CoreTerm,
    'NGC1275BlackHoleTerm': NGC1275BlackHoleTerm,
    'NGC1275LambdaTerm': NGC1275LambdaTerm,
    'NGC1275UQFFTerm': NGC1275UQFFTerm,
    'NGC1275EMTerm': NGC1275EMTerm,
    'NGC1275QuantumTerm': NGC1275QuantumTerm,
    'NGC1275FluidTerm': NGC1275FluidTerm,
    'NGC1275OscillatoryTerm': NGC1275OscillatoryTerm,
    'NGC1275DarkMatterTerm': NGC1275DarkMatterTerm,
    'NGC1275CoolingFlowTerm': NGC1275CoolingFlowTerm,
    'NGC1275FilamentSupportTerm': NGC1275FilamentSupportTerm,
    'HUDFGalaxiesCoreTerm': HUDFGalaxiesCoreTerm,
    'HUDFGalaxiesLambdaTerm': HUDFGalaxiesLambdaTerm,
    'HUDFGalaxiesUQFFTerm': HUDFGalaxiesUQFFTerm,
    'HUDFGalaxiesEMTerm': HUDFGalaxiesEMTerm,
    'HUDFGalaxiesQuantumTerm': HUDFGalaxiesQuantumTerm,
    'HUDFGalaxiesFluidTerm': HUDFGalaxiesFluidTerm,
    'HUDFGalaxiesOscillatoryTerm': HUDFGalaxiesOscillatoryTerm,
    'HUDFGalaxiesDarkMatterTerm': HUDFGalaxiesDarkMatterTerm,
    'HUDFGalaxiesWindTerm': HUDFGalaxiesWindTerm,
    'HUDFGalaxiesInteractionTerm': HUDFGalaxiesInteractionTerm,
    'NGC1792CoreTerm': NGC1792CoreTerm,
    'NGC1792LambdaTerm': NGC1792LambdaTerm,
    'NGC1792UQFFTerm': NGC1792UQFFTerm,
    'NGC1792EMTerm': NGC1792EMTerm,
    'NGC1792QuantumTerm': NGC1792QuantumTerm,
    'NGC1792FluidTerm': NGC1792FluidTerm,
    'NGC1792OscillatoryTerm': NGC1792OscillatoryTerm,
    'NGC1792DarkMatterTerm': NGC1792DarkMatterTerm,
    'NGC1792SupernovaFeedbackTerm': NGC1792SupernovaFeedbackTerm,
    'AndromedaCoreTerm': AndromedaCoreTerm,
    'AndromedaBlackHoleTerm': AndromedaBlackHoleTerm,
    'AndromedaLambdaTerm': AndromedaLambdaTerm,
    'AndromedaUQFFTerm': AndromedaUQFFTerm,
    'AndromedaEMTerm': AndromedaEMTerm,
    'AndromedaQuantumTerm': AndromedaQuantumTerm,
    'AndromedaFluidTerm': AndromedaFluidTerm,
    'AndromedaOscillatoryTerm': AndromedaOscillatoryTerm,
    'AndromedaDarkMatterTerm': AndromedaDarkMatterTerm,
    'SombreroCoreTerm': SombreroCoreTerm,
    'SombreroBlackHoleTerm': SombreroBlackHoleTerm,
    'SombreroLambdaTerm': SombreroLambdaTerm,
    'SombreroUQFFTerm': SombreroUQFFTerm,
    'SombreroEMTerm': SombreroEMTerm,
    'SombreroQuantumTerm': SombreroQuantumTerm,
    'SombreroFluidTerm': SombreroFluidTerm,
    'SombreroOscillatoryTerm': SombreroOscillatoryTerm,
    'SombreroDarkMatterTerm': SombreroDarkMatterTerm,
    'SombreroDustTerm': SombreroDustTerm,
    'SaturnCoreTerm': SaturnCoreTerm,
    'SaturnLambdaTerm': SaturnLambdaTerm,
    'SaturnUQFFTerm': SaturnUQFFTerm,
    'SaturnEMTerm': SaturnEMTerm,
    'SaturnQuantumTerm': SaturnQuantumTerm,
    'SaturnFluidTerm': SaturnFluidTerm,
    'SaturnOscillatoryTerm': SaturnOscillatoryTerm,
    'SaturnDarkMatterTerm': SaturnDarkMatterTerm,
    'SaturnRingTidalTerm': SaturnRingTidalTerm,
    'SaturnWindTerm': SaturnWindTerm,
    'M16CoreTerm': M16CoreTerm,
    'M16LambdaTerm': M16LambdaTerm,
    'M16UQFFTerm': M16UQFFTerm,
    'M16EMTerm': M16EMTerm,
    'M16QuantumTerm': M16QuantumTerm,
    'M16FluidTerm': M16FluidTerm,
    'M16OscillatoryTerm': M16OscillatoryTerm,
    'M16DarkMatterTerm': M16DarkMatterTerm,
    'M16StarFormationTerm': M16StarFormationTerm,
    'M16RadiationErosionTerm': M16RadiationErosionTerm,
    'CrabCoreTerm': CrabCoreTerm,
    'CrabLambdaTerm': CrabLambdaTerm,
    'CrabUQFFTerm': CrabUQFFTerm,
    'CrabEMTerm': CrabEMTerm,
    'CrabQuantumTerm': CrabQuantumTerm,
    'CrabFluidTerm': CrabFluidTerm,
    'CrabOscillatoryTerm': CrabOscillatoryTerm,
    'CrabDarkMatterTerm': CrabDarkMatterTerm,
    'CrabPulsarWindTerm': CrabPulsarWindTerm,
    'CrabMagneticTerm': CrabMagneticTerm,
    'SGR1745CoreTerm': SGR1745CoreTerm,
    'SGR1745LambdaTerm': SGR1745LambdaTerm,
    'SGR1745UQFFTerm': SGR1745UQFFTerm,
    'SGR1745EMTerm': SGR1745EMTerm,
    'SGR1745QuantumTerm': SGR1745QuantumTerm,
    'SGR1745FluidTerm': SGR1745FluidTerm,
    'SGR1745OscillatoryTerm': SGR1745OscillatoryTerm,
    'SGR1745DarkMatterTerm': SGR1745DarkMatterTerm,
    'SGR1745_DPMTerm': SGR1745_DPMTerm,
    'SGR1745_THzTerm': SGR1745_THzTerm,
    'SGR1745_VacDiffTerm': SGR1745_VacDiffTerm,
    'SGR1745_SuperFreqTerm': SGR1745_SuperFreqTerm,
    'SGR1745_AetherResTerm': SGR1745_AetherResTerm,
    'SGR1745_Ug4iTerm': SGR1745_Ug4iTerm,
    'SGR1745_QuantumFreqTerm': SGR1745_QuantumFreqTerm,
    'SGR1745_AetherFreqTerm': SGR1745_AetherFreqTerm,
    'SGR1745_FluidFreqTerm': SGR1745_FluidFreqTerm,
    'SGR1745_ExpFreqTerm': SGR1745_ExpFreqTerm,
    'SgrA_DPMTerm': SgrA_DPMTerm,
    'SgrA_THzTerm': SgrA_THzTerm,
    'SgrA_VacDiffTerm': SgrA_VacDiffTerm,
    'SgrA_SuperFreqTerm': SgrA_SuperFreqTerm,
    'SgrA_AetherResTerm': SgrA_AetherResTerm,
    'SgrA_Ug4iTerm': SgrA_Ug4iTerm,
    'SgrA_QuantumFreqTerm': SgrA_QuantumFreqTerm,
    'SgrA_AetherFreqTerm': SgrA_AetherFreqTerm,
    'SgrA_FluidFreqTerm': SgrA_FluidFreqTerm,
    'SgrA_ExpFreqTerm': SgrA_ExpFreqTerm,
    'YoungStarsCore': YoungStarsCore,
    'YoungStarsLambda': YoungStarsLambda,
    'YoungStarsUQFF': YoungStarsUQFF,
    'YoungStarsEM': YoungStarsEM,
    'YoungStarsQuantum': YoungStarsQuantum,
    'YoungStarsFluid': YoungStarsFluid,
    'YoungStarsOscillatory': YoungStarsOscillatory,
    'YoungStarsDarkMatter': YoungStarsDarkMatter,
    'YoungStarsOutflowPressure': YoungStarsOutflowPressure,
    'YoungStarsStarFormation': YoungStarsStarFormation,
    'BigBangCore': BigBangCore,
    'BigBangLambda': BigBangLambda,
    'BigBangUg1': BigBangUg1,
    'BigBangUg4': BigBangUg4,
    'BigBangQuantum': BigBangQuantum,
    'BigBangFluid': BigBangFluid,
    'BigBangOscillatory': BigBangOscillatory,
    'BigBangDarkMatter': BigBangDarkMatter,
    'BigBangQuantumGravity': BigBangQuantumGravity,
    'BigBangGravitationalWave': BigBangGravitationalWave,
    'BigBangCosmicEvolution': BigBangCosmicEvolution,
    'M51Core': M51Core,
    'M51Ug1Dipole': M51Ug1Dipole,
    'M51Ug2Superconductor': M51Ug2Superconductor,
    'M51Ug3Tidal': M51Ug3Tidal,
    'M51Ug4Reaction': M51Ug4Reaction,
    'M51UiVacuum': M51UiVacuum,
    'M51Lambda': M51Lambda,
    'M51Quantum': M51Quantum,
    'M51Fluid': M51Fluid,
    'M51DarkMatter': M51DarkMatter,
    'M51EnvironmentalForces': M51EnvironmentalForces,
    'NGC1316Core': NGC1316Core,
    'NGC1316Ug1Dipole': NGC1316Ug1Dipole,
    'NGC1316Ug2Superconductor': NGC1316Ug2Superconductor,
    'NGC1316Ug3External': NGC1316Ug3External,
    'NGC1316Ug4Reaction': NGC1316Ug4Reaction,
    'NGC1316UiVacuum': NGC1316UiVacuum,
    'NGC1316Lambda': NGC1316Lambda,
    'NGC1316Quantum': NGC1316Quantum,
    'NGC1316FluidDust': NGC1316FluidDust,
    'NGC1316DarkMatter': NGC1316DarkMatter,
    'NGC1316MergerForces': NGC1316MergerForces,
    'SMBHBinaryDPMResonance': SMBHBinaryDPMResonance,
    'SMBHBinaryTHzResonance': SMBHBinaryTHzResonance,
    'SMBHBinaryUg4iResonance': SMBHBinaryUg4iResonance,
    'SMBHBinaryPlasmoticVacuum': SMBHBinaryPlasmoticVacuum,
    'SMBHBinaryQuantumResonance': SMBHBinaryQuantumResonance,
    'SMBHBinaryFluidResonance': SMBHBinaryFluidResonance,
    'SMBHBinaryOscillatoryResonance': SMBHBinaryOscillatoryResonance,
    'SMBHBinaryExpansionResonance': SMBHBinaryExpansionResonance,
    'SMBHBinaryCoalescence': SMBHBinaryCoalescence,
    'BackgroundAetherMinkowski': BackgroundAetherMinkowski,
    'BackgroundAetherPerturbedMetric': BackgroundAetherPerturbedMetric,
    'BackgroundAetherStressEnergy': BackgroundAetherStressEnergy,
    'BackgroundAetherCoupling': BackgroundAetherCoupling,
    'HeavisideFraction': HeavisideFraction,
    'HeavisideFactor': HeavisideFactor,
    'HeavisideUmBase': HeavisideUmBase,
    'HeavisideUmWithHeaviside': HeavisideUmWithHeaviside,
    'HeavisideUmWithoutHeaviside': HeavisideUmWithoutHeaviside,
    'HeavisideQuasiFactor': HeavisideQuasiFactor,
    'HeavisideExponentialDecay': HeavisideExponentialDecay,
    'HeavisideMagneticMoment': HeavisideMagneticMoment,
    'HeavisideAmplificationRatio': HeavisideAmplificationRatio,
    'HydrogenResonance_Amplitude': HydrogenResonance_Amplitude,
    'HydrogenResonance_Frequency': HydrogenResonance_Frequency,
    'HydrogenResonance_DeepPairing': HydrogenResonance_DeepPairing,
    'HydrogenResonance_NuclearCoupling': HydrogenResonance_NuclearCoupling,
    'HydrogenResonance_ShellCorrection': HydrogenResonance_ShellCorrection,
    'HydrogenResonance_Buoyancy': HydrogenResonance_Buoyancy,
    'HydrogenResonance_Superconductive': HydrogenResonance_Superconductive,
    'HydrogenResonance_PairingEnergy': HydrogenResonance_PairingEnergy,
    'HydrogenResonance_MagicStability': HydrogenResonance_MagicStability,
    'HydrogenResonance_TunnelingProb': HydrogenResonance_TunnelingProb,
    'HydrogenResonance_Integrand': HydrogenResonance_Integrand,
    'HydrogenResonance_Full': HydrogenResonance_Full,
    'SurfaceMagnetic_Oscillation': SurfaceMagnetic_Oscillation,
    'SurfaceMagnetic_MinField': SurfaceMagnetic_MinField,
    'SurfaceMagnetic_MaxField': SurfaceMagnetic_MaxField,
    'SurfaceMagnetic_CycleReference': SurfaceMagnetic_CycleReference,
    'Source10_F_U_Bi_iTerm': Source10_F_U_Bi_iTerm,
    'Source10_g_UQFFTerm': Source10_g_UQFFTerm,
    'Source10_DPM_ResonanceTerm': Source10_DPM_ResonanceTerm,
    'Source10_E_cmTerm': Source10_E_cmTerm,
    'Source11_Ug1Term': Source11_Ug1Term,
    'Source11_Ug2Term': Source11_Ug2Term,
    'Source11_Ug3Term': Source11_Ug3Term,
    'Source11_Ug4Term': Source11_Ug4Term,
    'Source11_UmTerm': Source11_UmTerm,
    'UQFFModule5_DarkMatterHaloTerm': UQFFModule5_DarkMatterHaloTerm,
    'UQFFModule5_VacuumEnergyTerm': UQFFModule5_VacuumEnergyTerm,
    'Source5_CompressedBaseTerm': Source5_CompressedBaseTerm,
    'Source5_CompressedExpansionTerm': Source5_CompressedExpansionTerm,
    'Source5_CompressedQuantumTerm': Source5_CompressedQuantumTerm,
    'Source5_CompressedCosmTerm': Source5_CompressedCosmTerm,
    'Source5_CompressedFluidTerm': Source5_CompressedFluidTerm,
    'Source5_CompressedPerturbationTerm': Source5_CompressedPerturbationTerm,
    'Source5_ResonanceMUGETerm': Source5_ResonanceMUGETerm,
    'Source5_CompressedSuperAdjTerm': Source5_CompressedSuperAdjTerm,
    'Source5_CompressedEnvTerm': Source5_CompressedEnvTerm,
    'Source5_CompressedUgSumTerm': Source5_CompressedUgSumTerm,
    'ESO137_BuoyancyTerm': ESO137_BuoyancyTerm,
    'ESO137_CompressedTerm': ESO137_CompressedTerm,
    'ESO137_ResonantTerm': ESO137_ResonantTerm,
    'ESO137_SuperconductiveTerm': ESO137_SuperconductiveTerm,
    'ESO137_LENRTerm': ESO137_LENRTerm,
    'ESO137_CompressedGTerm': ESO137_CompressedGTerm,
    'ESO137_Q_waveTerm': ESO137_Q_waveTerm,
    'ESO137_Ub1Term': ESO137_Ub1Term,
    'ESO137_UiTerm': ESO137_UiTerm,
    'NGC1365_BuoyancyTerm': NGC1365_BuoyancyTerm,
    'NGC1365_CompressedTerm': NGC1365_CompressedTerm,
    'NGC1365_LENRTerm': NGC1365_LENRTerm,
    'Vela_BuoyancyTerm': Vela_BuoyancyTerm,
    'Vela_CompressedTerm': Vela_CompressedTerm,
    'Vela_LENRTerm': Vela_LENRTerm,
    'ASASSN14li_BuoyancyTerm': ASASSN14li_BuoyancyTerm,
    'ASASSN14li_CompressedTerm': ASASSN14li_CompressedTerm,
    'ASASSN14li_LENRTerm': ASASSN14li_LENRTerm,
    'ElGordo_BuoyancyTerm': ElGordo_BuoyancyTerm,
    'ElGordo_CompressedTerm': ElGordo_CompressedTerm,
    'ElGordo_LENRTerm': ElGordo_LENRTerm,
    'Heaviside_FactorTerm': Heaviside_FactorTerm,
    'Heaviside_UmBaseTerm': Heaviside_UmBaseTerm,
    'Heliosphere_H_SCmTerm': Heliosphere_H_SCmTerm,
    'UgIndex_U_giTerm': UgIndex_U_giTerm,
    'UgIndex_K_iTerm': UgIndex_K_iTerm,
    'Inertia_Lambda_iTerm': Inertia_Lambda_iTerm,
    'Inertia_U_iTerm': Inertia_U_iTerm,
    'MagMoment_Mu_jTerm': MagMoment_Mu_jTerm,
    'MagMoment_B_jTerm': MagMoment_B_jTerm,
    'BlackHole_M_bhTerm': BlackHole_M_bhTerm,
    'BlackHole_U_b1Term': BlackHole_U_b1Term,
    'NegativeTime_CosPiTnTerm': NegativeTime_CosPiTnTerm,
    'NegativeTime_ExpTermTerm': NegativeTime_ExpTermTerm,
    'PiConstant_MuJExampleTerm': PiConstant_MuJExampleTerm,
    'CorePenetration_P_coreTerm': CorePenetration_P_coreTerm,
    'QuasiLong_FactorTerm': QuasiLong_FactorTerm,
    'OuterBubble_S_r_RbTerm': OuterBubble_S_r_RbTerm,
    'OuterBubble_U_g2Term': OuterBubble_U_g2Term,
    'OuterBubble_R_bTerm': OuterBubble_R_bTerm,
    'OuterBubble_R_bInAUTerm': OuterBubble_R_bInAUTerm,
    'Reciprocation_Gamma_sTerm': Reciprocation_Gamma_sTerm,
    'Reciprocation_CosPiTnTerm': Reciprocation_CosPiTnTerm,
    'Reciprocation_ExpTermTerm': Reciprocation_ExpTermTerm,
    'Reciprocation_OneMinusExpTerm': Reciprocation_OneMinusExpTerm,
    'Reciprocation_Gamma_dayTerm': Reciprocation_Gamma_dayTerm,
    'Reciprocation_UmExampleTerm': Reciprocation_UmExampleTerm,
    'ScmPenetration_UmBaseTerm': ScmPenetration_UmBaseTerm,
    'ScmPenetration_UmContributionTerm': ScmPenetration_UmContributionTerm,
    'ScmPenetration_P_SCmTerm': ScmPenetration_P_SCmTerm,
    'ScmPenetration_UmPlanetTerm': ScmPenetration_UmPlanetTerm,
    'ScmReactivity_Kappa_sTerm': ScmReactivity_Kappa_sTerm,
    'ScmReactivity_E_reactTerm': ScmReactivity_E_reactTerm,
    'ScmReactivity_UmExampleTerm': ScmReactivity_UmExampleTerm,
    'ScmReactivity_Kappa_dayTerm': ScmReactivity_Kappa_dayTerm,
    'SolarCycle_Omega_cTerm': SolarCycle_Omega_cTerm,
    'SolarCycle_SinOmegaCTTerm': SolarCycle_SinOmegaCTTerm,
    'SolarCycle_MuJExampleTerm': SolarCycle_MuJExampleTerm,
    'SolarCycle_PeriodYearsTerm': SolarCycle_PeriodYearsTerm,
    'SolarCycle_B_j_ModulatedTerm': SolarCycle_B_j_ModulatedTerm,
    'SolarWindMod_ModulationFactorTerm': SolarWindMod_ModulationFactorTerm,
    'SolarWindMod_U_g2Term': SolarWindMod_U_g2Term,
    'SolarWindMod_Delta_swTerm': SolarWindMod_Delta_swTerm,
    'SolarWindMod_U_g2_no_modTerm': SolarWindMod_U_g2_no_modTerm,
    'SolarWindVel_ModulationFactorTerm': SolarWindVel_ModulationFactorTerm,
    'SolarWindVel_U_g2Term': SolarWindVel_U_g2Term,
    'SolarWindVel_V_swTerm': SolarWindVel_V_swTerm,
    'SolarWindVel_V_swKmSTerm': SolarWindVel_V_swKmSTerm,
    'SolarWindVel_U_g2_no_swTerm': SolarWindVel_U_g2_no_swTerm,
    'StellarMass_M_sOverR2Term': StellarMass_M_sOverR2Term,
    'StellarMass_U_g1Term': StellarMass_U_g1Term,
    'StellarMass_U_g2Term': StellarMass_U_g2Term,
    'StellarMass_M_sTerm': StellarMass_M_sTerm,
    'StellarMass_M_sInMsunTerm': StellarMass_M_sInMsunTerm,
    'StellarRot_Omega_s_tTerm': StellarRot_Omega_s_tTerm,
    'StellarRot_U_g3Term': StellarRot_U_g3Term,
    'StellarRot_U_iTerm': StellarRot_U_iTerm,
    'StellarRot_Omega_sTerm': StellarRot_Omega_sTerm,
    'StellarRot_Period_daysTerm': StellarRot_Period_daysTerm,
    'StepFunc_S_r_RbTerm': StepFunc_S_r_RbTerm,
    'StepFunc_U_g2Term': StepFunc_U_g2Term,
    'StressEnergy_T_sTerm': StressEnergy_T_sTerm,
    'StressEnergy_A_mu_nuTerm': StressEnergy_A_mu_nuTerm,
    'StressEnergy_PerturbationTerm': StressEnergy_PerturbationTerm,
    'SurfaceMagField_B_jTerm': SurfaceMagField_B_jTerm,
    'SurfaceMagField_U_g3_exampleTerm': SurfaceMagField_U_g3_exampleTerm,
    'SurfaceMagField_B_s_minTerm': SurfaceMagField_B_s_minTerm,
    'SurfaceMagField_B_s_maxTerm': SurfaceMagField_B_s_maxTerm,
    'SurfaceTemp_B_j_hypotheticalTerm': SurfaceTemp_B_j_hypotheticalTerm,
    'SurfaceTemp_U_g3_exampleTerm': SurfaceTemp_U_g3_exampleTerm,
    'SurfaceTemp_T_sTerm': SurfaceTemp_T_sTerm,
    'TimeReversalZone_TRZFactorTerm': TimeReversalZone_TRZFactorTerm,
    'TimeReversalZone_U_i_baseTerm': TimeReversalZone_U_i_baseTerm,
    'TimeReversalZone_U_iTerm': TimeReversalZone_U_iTerm,
    'TimeReversalZone_F_TRZTerm': TimeReversalZone_F_TRZTerm,
    'TimeReversalZone_U_i_no_TRZTerm': TimeReversalZone_U_i_no_TRZTerm,
    'Ug1Defect_Delta_defTerm': Ug1Defect_Delta_defTerm,
    'Ug1Defect_U_g1Term': Ug1Defect_U_g1Term,
    'Ug1Defect_Period_yearsTerm': Ug1Defect_Period_yearsTerm,
    'Ug3DiskVec_PhiHat_jTerm': Ug3DiskVec_PhiHat_jTerm,
    'Ug3DiskVec_UmBaseTerm': Ug3DiskVec_UmBaseTerm,
    'Ug3DiskVec_UmContributionTerm': Ug3DiskVec_UmContributionTerm,
    'Ug3DiskVec_PhiHatMagnitudeTerm': Ug3DiskVec_PhiHatMagnitudeTerm,
    'AetherVacDensity_Rho_vac_ATerm': AetherVacDensity_Rho_vac_ATerm,
    'AetherVacDensity_T_s_contributionTerm': AetherVacDensity_T_s_contributionTerm,
    'AetherVacDensity_A_mu_nuTerm': AetherVacDensity_A_mu_nuTerm,
    'AetherVacDensity_Epsilon_factorTerm': AetherVacDensity_Epsilon_factorTerm,
    'UnivInertiaVac_Rho_vac_UiTerm': UnivInertiaVac_Rho_vac_UiTerm,
    'UnivInertiaVac_U_i_baseTerm': UnivInertiaVac_U_i_baseTerm,
    'UnivInertiaVac_U_iTerm': UnivInertiaVac_U_iTerm,
    'ScmVacDensity_Rho_vac_SCmTerm': ScmVacDensity_Rho_vac_SCmTerm,
    'ScmVacDensity_U_g2_exampleTerm': ScmVacDensity_U_g2_exampleTerm,
    'ScmVacDensity_U_i_exampleTerm': ScmVacDensity_U_i_exampleTerm,
    'UaVacDensity_Rho_vac_UATerm': UaVacDensity_Rho_vac_UATerm,
    'UaVacDensity_U_g2_exampleTerm': UaVacDensity_U_g2_exampleTerm,
    'UaVacDensity_U_i_exampleTerm': UaVacDensity_U_i_exampleTerm,
    'UnivInertiaVac2_U_i_baseTerm': UnivInertiaVac2_U_i_baseTerm,
    'UnivInertiaVac2_U_iTerm': UnivInertiaVac2_U_iTerm,
    'ScmVelocity_V_scmTerm': ScmVelocity_V_scmTerm,
    'ScmVelocity_E_react_baseTerm': ScmVelocity_E_react_baseTerm,
    'ScmVelocity_E_reactTerm': ScmVelocity_E_reactTerm,
    'ScmVelocity_UmExampleTerm': ScmVelocity_UmExampleTerm,
    'ButterflyNebula_DPM_momentumTerm': ButterflyNebula_DPM_momentumTerm,
    'ButterflyNebula_DPM_gravityTerm': ButterflyNebula_DPM_gravityTerm,
    'ButterflyNebula_LENR_termTerm': ButterflyNebula_LENR_termTerm,
    'ButterflyNebula_F_U_BiTerm': ButterflyNebula_F_U_BiTerm,
    'CentaurusA_DPM_momentumTerm': CentaurusA_DPM_momentumTerm,
    'CentaurusA_F_U_BiTerm': CentaurusA_F_U_BiTerm,
    'Abell2256_IntegrandTerm': Abell2256_IntegrandTerm,
    'Abell2256_DPM_resonanceTerm': Abell2256_DPM_resonanceTerm,
    'Abell2256_X2Term': Abell2256_X2Term,
    'Abell2256_LENR_termTerm': Abell2256_LENR_termTerm,
    'Abell2256_Q_waveTerm': Abell2256_Q_waveTerm,
    'Abell2256_Ub1Term': Abell2256_Ub1Term,
    'Abell2256_UiTerm': Abell2256_UiTerm,
    'Abell2256_FTerm': Abell2256_FTerm,
    'Abell2256_CompressedTerm': Abell2256_CompressedTerm,
    'Abell2256_ResonantTerm': Abell2256_ResonantTerm,
    'Abell2256_BuoyancyTerm': Abell2256_BuoyancyTerm,
    'Abell2256_SuperconductiveTerm': Abell2256_SuperconductiveTerm,
    'Abell2256_CompressedGTerm': Abell2256_CompressedGTerm,
    'ASASSN14li_IntegrandTerm': ASASSN14li_IntegrandTerm,
    'ASASSN14li_DPM_resonanceTerm': ASASSN14li_DPM_resonanceTerm,
    'ASASSN14li_X2Term': ASASSN14li_X2Term,
    'ASASSN14li_LENR_termTerm': ASASSN14li_LENR_termTerm,
    'ASASSN14li_Q_waveTerm': ASASSN14li_Q_waveTerm,
    'ASASSN14li_FTerm': ASASSN14li_FTerm,
    'CentaurusA_X2Term': CentaurusA_X2Term,
    'CentaurusA_LENR_termTerm': CentaurusA_LENR_termTerm,
    'CentaurusA_FTerm': CentaurusA_FTerm,
    'CentaurusA_CompressedTerm': CentaurusA_CompressedTerm,
    'CrabNebula_IntegrandTerm': CrabNebula_IntegrandTerm,
    'CrabNebula_LENR_termTerm': CrabNebula_LENR_termTerm,
    'CrabNebula_FTerm': CrabNebula_FTerm,
    'CrabNebula_CompressedTerm': CrabNebula_CompressedTerm,
    'ElGordo_IntegrandTerm': ElGordo_IntegrandTerm,
    'ElGordo_LENR_termTerm': ElGordo_LENR_termTerm,
    'ElGordo_FTerm': ElGordo_FTerm,
    'ESO137_FTerm': ESO137_FTerm,
    'IC2163_IntegrandTerm': IC2163_IntegrandTerm,
    'IC2163_LENR_termTerm': IC2163_LENR_termTerm,
    'IC2163_FTerm': IC2163_FTerm,
    'IC2163_CompressedTerm': IC2163_CompressedTerm,
    'J1610_IntegrandTerm': J1610_IntegrandTerm,
    'J1610_LENR_termTerm': J1610_LENR_termTerm,
    'J1610_FTerm': J1610_FTerm,
    'J1610_CompressedTerm': J1610_CompressedTerm,
    'JupiterAurorae_IntegrandTerm': JupiterAurorae_IntegrandTerm,
    'JupiterAurorae_LENR_termTerm': JupiterAurorae_LENR_termTerm,
    'JupiterAurorae_FTerm': JupiterAurorae_FTerm,
    'JupiterAurorae_CompressedTerm': JupiterAurorae_CompressedTerm,
    'LagoonNebula_IntegrandTerm': LagoonNebula_IntegrandTerm,
    'LagoonNebula_LENR_termTerm': LagoonNebula_LENR_termTerm,
    'LagoonNebula_FTerm': LagoonNebula_FTerm,
    'LagoonNebula_CompressedTerm': LagoonNebula_CompressedTerm,
    'LagoonNebulaVar_IntegrandTerm': LagoonNebulaVar_IntegrandTerm,
    'LagoonNebulaVar_LENR_termTerm': LagoonNebulaVar_LENR_termTerm,
    'LagoonNebulaVar_FTerm': LagoonNebulaVar_FTerm,
    'LagoonNebulaVar_CompressedTerm': LagoonNebulaVar_CompressedTerm,
    'M87Jet_IntegrandTerm': M87Jet_IntegrandTerm,
    'M87Jet_LENR_termTerm': M87Jet_LENR_termTerm,
    'M87Jet_FTerm': M87Jet_FTerm,
    'M87Jet_CompressedTerm': M87Jet_CompressedTerm,
    'NGC1365_IntegrandTerm': NGC1365_IntegrandTerm,
    'NGC1365_LENR_termTerm': NGC1365_LENR_termTerm,
    'NGC1365_FTerm': NGC1365_FTerm,
    'NGC2207_IntegrandTerm': NGC2207_IntegrandTerm,
    'NGC2207_LENR_termTerm': NGC2207_LENR_termTerm,
    'NGC2207_FTerm': NGC2207_FTerm,
    'NGC2207_CompressedTerm': NGC2207_CompressedTerm,
    'RAquarii_IntegrandTerm': RAquarii_IntegrandTerm,
    'RAquarii_LENR_termTerm': RAquarii_LENR_termTerm,
    'RAquarii_FTerm': RAquarii_FTerm,
    'RAquarii_CompressedTerm': RAquarii_CompressedTerm,
    'SgrAStar_IntegrandTerm': SgrAStar_IntegrandTerm,
    'SgrAStar_LENR_termTerm': SgrAStar_LENR_termTerm,
    'SgrAStar_FTerm': SgrAStar_FTerm,
    'SgrAStar_CompressedTerm': SgrAStar_CompressedTerm,
    'SPTCLJ2215_IntegrandTerm': SPTCLJ2215_IntegrandTerm,
    'SPTCLJ2215_LENR_termTerm': SPTCLJ2215_LENR_termTerm,
    'SPTCLJ2215_FTerm': SPTCLJ2215_FTerm,
    'SPTCLJ2215_CompressedTerm': SPTCLJ2215_CompressedTerm,
    'StephanQuintet_IntegrandTerm': StephanQuintet_IntegrandTerm,
    'StephanQuintet_LENR_termTerm': StephanQuintet_LENR_termTerm,
    'StephanQuintet_FTerm': StephanQuintet_FTerm,
    'StephanQuintet_CompressedTerm': StephanQuintet_CompressedTerm,
    'VelaPulsar_IntegrandTerm': VelaPulsar_IntegrandTerm,
    'VelaPulsar_LENR_termTerm': VelaPulsar_LENR_termTerm,
    'VelaPulsar_FTerm': VelaPulsar_FTerm,
    'VelaPulsar_CompressedTerm': VelaPulsar_CompressedTerm,
    'NGC4993_IntegrandTerm': NGC4993_IntegrandTerm,
    'NGC4993_LENR_termTerm': NGC4993_LENR_termTerm,
    'NGC4993_FTerm': NGC4993_FTerm,
    'NGC4993_CompressedTerm': NGC4993_CompressedTerm,
    'NGC6240_IntegrandTerm': NGC6240_IntegrandTerm,
    'NGC6240_LENR_termTerm': NGC6240_LENR_termTerm,
    'NGC6240_FTerm': NGC6240_FTerm,
    'NGC6240_CompressedTerm': NGC6240_CompressedTerm,
    'OmegaCentauri_IntegrandTerm': OmegaCentauri_IntegrandTerm,
    'OmegaCentauri_LENR_termTerm': OmegaCentauri_LENR_termTerm,
    'OmegaCentauri_FTerm': OmegaCentauri_FTerm,
    'OmegaCentauri_CompressedTerm': OmegaCentauri_CompressedTerm,
    'OrionNebula_IntegrandTerm': OrionNebula_IntegrandTerm,
    'OrionNebula_LENR_termTerm': OrionNebula_LENR_termTerm,
    'OrionNebula_FTerm': OrionNebula_FTerm,
    'OrionNebula_CompressedTerm': OrionNebula_CompressedTerm,
    'Perseus_IntegrandTerm': Perseus_IntegrandTerm,
    'Perseus_LENR_termTerm': Perseus_LENR_termTerm,
    'Perseus_FTerm': Perseus_FTerm,
    'Perseus_CompressedTerm': Perseus_CompressedTerm,
    'PolarRing_IntegrandTerm': PolarRing_IntegrandTerm,
    'PolarRing_LENR_termTerm': PolarRing_LENR_termTerm,
    'PolarRing_FTerm': PolarRing_FTerm,
    'PolarRing_CompressedTerm': PolarRing_CompressedTerm,
    'QuasarJ0313_IntegrandTerm': QuasarJ0313_IntegrandTerm,
    'QuasarJ0313_LENR_termTerm': QuasarJ0313_LENR_termTerm,
    'QuasarJ0313_FTerm': QuasarJ0313_FTerm,
    'QuasarJ0313_CompressedTerm': QuasarJ0313_CompressedTerm,
    'RXJ1713_IntegrandTerm': RXJ1713_IntegrandTerm,
    'RXJ1713_LENR_termTerm': RXJ1713_LENR_termTerm,
    'RXJ1713_FTerm': RXJ1713_FTerm,
    'RXJ1713_CompressedTerm': RXJ1713_CompressedTerm,
    'BuoyancyAstro_IntegrandTerm': BuoyancyAstro_IntegrandTerm,
    'BuoyancyAstro_LENR_termTerm': BuoyancyAstro_LENR_termTerm,
    'BuoyancyAstro_FTerm': BuoyancyAstro_FTerm,
    'BuoyancyAstro_CompressedTerm': BuoyancyAstro_CompressedTerm,
    'BuoyancyCNB_IntegrandTerm': BuoyancyCNB_IntegrandTerm,
    'BuoyancyCNB_LENR_termTerm': BuoyancyCNB_LENR_termTerm,
    'BuoyancyCNB_FTerm': BuoyancyCNB_FTerm,
    'BuoyancyCNB_CompressedTerm': BuoyancyCNB_CompressedTerm,
    'AstroSystems163_IntegrandTerm': AstroSystems163_IntegrandTerm,
    'AstroSystems163_LENR_termTerm': AstroSystems163_LENR_termTerm,
    'AstroSystems163_FTerm': AstroSystems163_FTerm,
    'AstroSystems163_CompressedTerm': AstroSystems163_CompressedTerm,
    'NebulaTriadic_IntegrandTerm': NebulaTriadic_IntegrandTerm,
    'NebulaTriadic_LENR_termTerm': NebulaTriadic_LENR_termTerm,
    'NebulaTriadic_FTerm': NebulaTriadic_FTerm,
    'NebulaTriadic_CompressedTerm': NebulaTriadic_CompressedTerm,
    'BuoyancyModule_IntegrandTerm': BuoyancyModule_IntegrandTerm,
    'BuoyancyModule_LENR_termTerm': BuoyancyModule_LENR_termTerm,
    'BuoyancyModule_FTerm': BuoyancyModule_FTerm,
    'BuoyancyModule_CompressedTerm': BuoyancyModule_CompressedTerm,
    'UQFF8Astro_IntegrandTerm': UQFF8Astro_IntegrandTerm,
    'UQFF8Astro_LENR_termTerm': UQFF8Astro_LENR_termTerm,
    'UQFF8Astro_FTerm': UQFF8Astro_FTerm,
    'UQFF8Astro_CompressedTerm': UQFF8Astro_CompressedTerm,
    'UQFFCore_IntegrandTerm': UQFFCore_IntegrandTerm,
    'UQFFCore_LENR_termTerm': UQFFCore_LENR_termTerm,
    'UQFFCore_FTerm': UQFFCore_FTerm,
    'UQFFCore_CompressedTerm': UQFFCore_CompressedTerm,
    'Buoyancy168_IntegrandTerm': Buoyancy168_IntegrandTerm,
    'Buoyancy168_LENR_termTerm': Buoyancy168_LENR_termTerm,
    'Buoyancy168_FTerm': Buoyancy168_FTerm,
    'Buoyancy168_CompressedTerm': Buoyancy168_CompressedTerm,
    'CassiniBuoyancy_IntegrandTerm': CassiniBuoyancy_IntegrandTerm,
    'CassiniBuoyancy_LENR_termTerm': CassiniBuoyancy_LENR_termTerm,
    'CassiniBuoyancy_FTerm': CassiniBuoyancy_FTerm,
    'CassiniBuoyancy_CompressedTerm': CassiniBuoyancy_CompressedTerm,
    'MultiAstro_IntegrandTerm': MultiAstro_IntegrandTerm,
    'MultiAstro_LENR_termTerm': MultiAstro_LENR_termTerm,
    'MultiAstro_FTerm': MultiAstro_FTerm,
    'MultiAstro_CompressedTerm': MultiAstro_CompressedTerm,
    'EightAstro_IntegrandTerm': EightAstro_IntegrandTerm,
    'EightAstro_LENR_termTerm': EightAstro_LENR_termTerm,
    'EightAstro_FTerm': EightAstro_FTerm,
    'EightAstro_CompressedTerm': EightAstro_CompressedTerm,
    'NineteenAstro_IntegrandTerm': NineteenAstro_IntegrandTerm,
    'NineteenAstro_LENR_termTerm': NineteenAstro_LENR_termTerm,
    'NineteenAstro_FTerm': NineteenAstro_FTerm,
    'NineteenAstro_CompressedTerm': NineteenAstro_CompressedTerm,
    'WolframFieldUnity_IntegrandTerm': WolframFieldUnity_IntegrandTerm,
    'WolframFieldUnity_LENR_termTerm': WolframFieldUnity_LENR_termTerm,
    'WolframFieldUnity_FTerm': WolframFieldUnity_FTerm,
    'WolframFieldUnity_CompressedTerm': WolframFieldUnity_CompressedTerm,
    'J1610_DPM_resonanceTerm': J1610_DPM_resonanceTerm,
    'J1610_Q_waveTerm': J1610_Q_waveTerm,
    'J1610_Ub1Term': J1610_Ub1Term,
    'J1610_UiTerm': J1610_UiTerm,
    'J1610_X2Term': J1610_X2Term,
    'J1610_ActivationTerm': J1610_ActivationTerm,
    'J1610_DirectedEnergyTerm': J1610_DirectedEnergyTerm,
    'J1610_NeutronTerm': J1610_NeutronTerm,
    'J1610_RelativisticTerm': J1610_RelativisticTerm,
    'J1610_NeutrinoTerm': J1610_NeutrinoTerm,
    'M87Jet_DPM_resonanceTerm': M87Jet_DPM_resonanceTerm,
    'M87Jet_Q_waveTerm': M87Jet_Q_waveTerm,
    'M87Jet_Ub1Term': M87Jet_Ub1Term,
    'M87Jet_UiTerm': M87Jet_UiTerm,
    'M87Jet_X2Term': M87Jet_X2Term,
    'M87Jet_ActivationTerm': M87Jet_ActivationTerm,
    'M87Jet_DirectedEnergyTerm': M87Jet_DirectedEnergyTerm,
    'M87Jet_NeutronTerm': M87Jet_NeutronTerm,
    'M87Jet_RelativisticTerm': M87Jet_RelativisticTerm,
    'M87Jet_NeutrinoTerm': M87Jet_NeutrinoTerm,
    'SgrAStar_DPM_resonanceTerm': SgrAStar_DPM_resonanceTerm,
    'SgrAStar_Q_waveTerm': SgrAStar_Q_waveTerm,
    'SgrAStar_Ub1Term': SgrAStar_Ub1Term,
    'SgrAStar_UiTerm': SgrAStar_UiTerm,
    'SgrAStar_X2Term': SgrAStar_X2Term,
    'SgrAStar_ActivationTerm': SgrAStar_ActivationTerm,
    'SgrAStar_DirectedEnergyTerm': SgrAStar_DirectedEnergyTerm,
    'SgrAStar_NeutronTerm': SgrAStar_NeutronTerm,
    'SgrAStar_RelativisticTerm': SgrAStar_RelativisticTerm,
    'SgrAStar_NeutrinoTerm': SgrAStar_NeutrinoTerm,
    'NGC1365_DPM_resonanceTerm': NGC1365_DPM_resonanceTerm,
    'NGC1365_Q_waveTerm': NGC1365_Q_waveTerm,
    'NGC1365_Ub1Term': NGC1365_Ub1Term,
    'NGC1365_UiTerm': NGC1365_UiTerm,
    'NGC1365_X2Term': NGC1365_X2Term,
    'NGC1365_ActivationTerm': NGC1365_ActivationTerm,
    'NGC1365_DirectedEnergyTerm': NGC1365_DirectedEnergyTerm,
    'NGC1365_NeutronTerm': NGC1365_NeutronTerm,
    'NGC1365_RelativisticTerm': NGC1365_RelativisticTerm,
    'NGC1365_NeutrinoTerm': NGC1365_NeutrinoTerm,
    'VelaPulsar_DPM_resonanceTerm': VelaPulsar_DPM_resonanceTerm,
    'VelaPulsar_Q_waveTerm': VelaPulsar_Q_waveTerm,
    'VelaPulsar_Ub1Term': VelaPulsar_Ub1Term,
    'VelaPulsar_UiTerm': VelaPulsar_UiTerm,
    'VelaPulsar_X2Term': VelaPulsar_X2Term,
    'VelaPulsar_ActivationTerm': VelaPulsar_ActivationTerm,
    'VelaPulsar_DirectedEnergyTerm': VelaPulsar_DirectedEnergyTerm,
    'VelaPulsar_NeutronTerm': VelaPulsar_NeutronTerm,
    'VelaPulsar_RelativisticTerm': VelaPulsar_RelativisticTerm,
    'VelaPulsar_NeutrinoTerm': VelaPulsar_NeutrinoTerm,
    'NGC2207_DPM_resonanceTerm': NGC2207_DPM_resonanceTerm,
    'NGC2207_Q_waveTerm': NGC2207_Q_waveTerm,
    'NGC2207_Ub1Term': NGC2207_Ub1Term,
    'NGC2207_UiTerm': NGC2207_UiTerm,
    'NGC2207_X2Term': NGC2207_X2Term,
    'NGC2207_ActivationTerm': NGC2207_ActivationTerm,
    'NGC2207_DirectedEnergyTerm': NGC2207_DirectedEnergyTerm,
    'NGC2207_NeutronTerm': NGC2207_NeutronTerm,
    'NGC2207_RelativisticTerm': NGC2207_RelativisticTerm,
    'NGC2207_NeutrinoTerm': NGC2207_NeutrinoTerm,
    'PerseusCluster_DPM_resonanceTerm': PerseusCluster_DPM_resonanceTerm,
    'PerseusCluster_Q_waveTerm': PerseusCluster_Q_waveTerm,
    'PerseusCluster_Ub1Term': PerseusCluster_Ub1Term,
    'PerseusCluster_UiTerm': PerseusCluster_UiTerm,
    'PerseusCluster_X2Term': PerseusCluster_X2Term,
    'PerseusCluster_ActivationTerm': PerseusCluster_ActivationTerm,
    'PerseusCluster_DirectedEnergyTerm': PerseusCluster_DirectedEnergyTerm,
    'PerseusCluster_NeutronTerm': PerseusCluster_NeutronTerm,
    'PerseusCluster_RelativisticTerm': PerseusCluster_RelativisticTerm,
    'PerseusCluster_NeutrinoTerm': PerseusCluster_NeutrinoTerm,
    'OrionM42_DPM_resonanceTerm': OrionM42_DPM_resonanceTerm,
    'OrionM42_Q_waveTerm': OrionM42_Q_waveTerm,
    'OrionM42_Ub1Term': OrionM42_Ub1Term,
    'OrionM42_UiTerm': OrionM42_UiTerm,
    'OrionM42_X2Term': OrionM42_X2Term,
    'OrionM42_ActivationTerm': OrionM42_ActivationTerm,
    'OrionM42_DirectedEnergyTerm': OrionM42_DirectedEnergyTerm,
    'OrionM42_NeutronTerm': OrionM42_NeutronTerm,
    'OrionM42_RelativisticTerm': OrionM42_RelativisticTerm,
    'OrionM42_NeutrinoTerm': OrionM42_NeutrinoTerm,
    'LagoonM8_DPM_resonanceTerm': LagoonM8_DPM_resonanceTerm,
    'LagoonM8_Q_waveTerm': LagoonM8_Q_waveTerm,
    'LagoonM8_Ub1Term': LagoonM8_Ub1Term,
    'LagoonM8_UiTerm': LagoonM8_UiTerm,
    'LagoonM8_X2Term': LagoonM8_X2Term,
    'LagoonM8_ActivationTerm': LagoonM8_ActivationTerm,
    'LagoonM8_DirectedEnergyTerm': LagoonM8_DirectedEnergyTerm,
    'LagoonM8_NeutronTerm': LagoonM8_NeutronTerm,
    'LagoonM8_RelativisticTerm': LagoonM8_RelativisticTerm,
    'LagoonM8_NeutrinoTerm': LagoonM8_NeutrinoTerm,
    'ESO137_G_timeTerm': ESO137_G_timeTerm,
    'NGC1365A_G_timeTerm': NGC1365A_G_timeTerm,
    'NGC5728_G_timeTerm': NGC5728_G_timeTerm,
    'Vela_G_timeTerm': Vela_G_timeTerm,
    'M82_G_timeTerm': M82_G_timeTerm,
    'Carina_G_timeTerm': Carina_G_timeTerm,
    'TadpoleGalaxy_G_timeTerm': TadpoleGalaxy_G_timeTerm,
    'MiceGalaxies_G_timeTerm': MiceGalaxies_G_timeTerm,
    'NGC2264_G_timeTerm': NGC2264_G_timeTerm,
    'M42_G_timeTerm': M42_G_timeTerm,
    'CrabNebula_G_timeTerm': CrabNebula_G_timeTerm,
    'BlackWidowPulsar_G_timeTerm': BlackWidowPulsar_G_timeTerm,
    'Redback_G_timeTerm': Redback_G_timeTerm,
    'Fermi_G_timeTerm': Fermi_G_timeTerm,
    'Holmberg15A_G_timeTerm': Holmberg15A_G_timeTerm,
    'NineteenSystems_G_timeTerm': NineteenSystems_G_timeTerm,
    'WolframHypergraph_G_timeTerm': WolframHypergraph_G_timeTerm,
    'NGC253_G_timeTerm': NGC253_G_timeTerm,
    'M87Jet_G_timeTerm': M87Jet_G_timeTerm,
    'Centaurus_G_timeTerm': Centaurus_G_timeTerm,
    'Antlia_G_timeTerm': Antlia_G_timeTerm,
    'Fornax_G_timeTerm': Fornax_G_timeTerm,
    'Sculptor_G_timeTerm': Sculptor_G_timeTerm,
    'LeoI_G_timeTerm': LeoI_G_timeTerm,
    'LeoII_G_timeTerm': LeoII_G_timeTerm,
    'UrsaMinor_G_timeTerm': UrsaMinor_G_timeTerm,
    'Draco_G_timeTerm': Draco_G_timeTerm,
    'Sextans_G_timeTerm': Sextans_G_timeTerm,
    'Carina2_G_timeTerm': Carina2_G_timeTerm,
    'Reticulum_G_timeTerm': Reticulum_G_timeTerm,
    'Tucana_G_timeTerm': Tucana_G_timeTerm,
    'Horologium_G_timeTerm': Horologium_G_timeTerm,
    'Grus_G_timeTerm': Grus_G_timeTerm,
    'Phoenix_G_timeTerm': Phoenix_G_timeTerm,
    'Eridanus_G_timeTerm': Eridanus_G_timeTerm,
    'Aquarius_G_timeTerm': Aquarius_G_timeTerm,
    'Pisces_G_timeTerm': Pisces_G_timeTerm,
    'Pegasus_G_timeTerm': Pegasus_G_timeTerm,
    'Andromeda_G_timeTerm': Andromeda_G_timeTerm,
    'Triangulum_G_timeTerm': Triangulum_G_timeTerm,
    'LMC_G_timeTerm': LMC_G_timeTerm,
    'J1610_G_timeTerm': J1610_G_timeTerm,
    'JupiterAurorae_G_timeTerm': JupiterAurorae_G_timeTerm,
    'LagoonM8HII_G_timeTerm': LagoonM8HII_G_timeTerm,
    'M87JetEHT_G_timeTerm': M87JetEHT_G_timeTerm,
    'NGC1365Seyfert_G_timeTerm': NGC1365Seyfert_G_timeTerm,
    'NGC2207IC2163_G_timeTerm': NGC2207IC2163_G_timeTerm,
    'RAquarii_G_timeTerm': RAquarii_G_timeTerm,
    'SgrAStarGC_G_timeTerm': SgrAStarGC_G_timeTerm,
    'SPTCLJ2215_G_timeTerm': SPTCLJ2215_G_timeTerm,
    'StephanQuintet_G_timeTerm': StephanQuintet_G_timeTerm,
    'VelaPulsarSNR_G_timeTerm': VelaPulsarSNR_G_timeTerm,
    'NGC4993GW170817_G_timeTerm': NGC4993GW170817_G_timeTerm,
    'NGC6240DualAGN_G_timeTerm': NGC6240DualAGN_G_timeTerm,
    'OmegaCentauri_G_timeTerm': OmegaCentauri_G_timeTerm,
    'OrionM42Trapezium_G_timeTerm': OrionM42Trapezium_G_timeTerm,
    'PerseusAbell426_G_timeTerm': PerseusAbell426_G_timeTerm,
    'PolarRingNGC4650A_G_timeTerm': PolarRingNGC4650A_G_timeTerm,
    'QuasarJ0313_G_timeTerm': QuasarJ0313_G_timeTerm,
    'RXJ1713TeVSNR_G_timeTerm': RXJ1713TeVSNR_G_timeTerm,
    'SMC_G_timeTerm': SMC_G_timeTerm,
    'BuoyancyAstro5_G_timeTerm': BuoyancyAstro5_G_timeTerm,
    'BuoyancyCNB_G_timeTerm': BuoyancyCNB_G_timeTerm,
    'AstroSystems163_G_timeTerm': AstroSystems163_G_timeTerm,
    'NebulaTriadic_G_timeTerm': NebulaTriadic_G_timeTerm,
    'BuoyancyModule_G_timeTerm': BuoyancyModule_G_timeTerm,
    'UQFF8Astro_G_timeTerm': UQFF8Astro_G_timeTerm,
    'UQFFCore_G_timeTerm': UQFFCore_G_timeTerm,
    'Buoyancy168_G_timeTerm': Buoyancy168_G_timeTerm,
    'CassiniBuoyancy_G_timeTerm': CassiniBuoyancy_G_timeTerm,
    'MultiAstro_G_timeTerm': MultiAstro_G_timeTerm,
    'EightAstro_G_timeTerm': EightAstro_G_timeTerm,
    'NineteenAstro26D_G_timeTerm': NineteenAstro26D_G_timeTerm,
    'WolframFieldUnity_G_timeTerm': WolframFieldUnity_G_timeTerm,
    'ESO137_HeavisideFactor': ESO137_HeavisideFactor,
    'ESO137_UmBase': ESO137_UmBase,
    'ESO137_UmContribution': ESO137_UmContribution,
    'NGC1365A_H_SCm': NGC1365A_H_SCm,
    'NGC1365A_U_g2': NGC1365A_U_g2,
    'NGC1365A_SwirlFactor': NGC1365A_SwirlFactor,
    'NGC5728_U_gi': NGC5728_U_gi,
    'NGC5728_K_i': NGC5728_K_i,
    'NGC5728_SumKUgi': NGC5728_SumKUgi,
    'MagneticMoment_Mu_j': MagneticMoment_Mu_j,
    'MagneticMoment_B_j': MagneticMoment_B_j,
    'MagneticMoment_UmContrib': MagneticMoment_UmContrib,
    'BlackHole_M_bh': BlackHole_M_bh,
    'BlackHole_M_bhInMsun': BlackHole_M_bhInMsun,
    'BlackHole_U_b1': BlackHole_U_b1,
    'NegativeTime_T_n': NegativeTime_T_n,
    'NegativeTime_CosPiTn': NegativeTime_CosPiTn,
    'NegativeTime_OneMinusExp': NegativeTime_OneMinusExp,
    'PiConstant_Pi': PiConstant_Pi,
    'PiConstant_CosPiTn': PiConstant_CosPiTn,
    'PiConstant_SinOmegaCT': PiConstant_SinOmegaCT,
    'Decay_Gamma_s': Decay_Gamma_s,
    'Decay_ExpTerm': Decay_ExpTerm,
    'Decay_OneMinusExp': Decay_OneMinusExp,
    'ScmPenetration_P_SCm': ScmPenetration_P_SCm,
    'ScmPenetration_UmPlanet': ScmPenetration_UmPlanet,
    'ScmReactivity_Kappa_day': ScmReactivity_Kappa_day,
    'ScmReactivity_E_reactDecay': ScmReactivity_E_reactDecay,
    'SolarCycle_Omega_c': SolarCycle_Omega_c,
    'SolarCycle_PeriodYears': SolarCycle_PeriodYears,
    'SolarCycle_MuJ_Modulated': SolarCycle_MuJ_Modulated,
    'SolarCycle_B_j_Modulated': SolarCycle_B_j_Modulated,
    'SolarWind_Delta_sw': SolarWind_Delta_sw,
    'SolarWind_ModulationFactor': SolarWind_ModulationFactor,
    'SolarWind_U_g2_NoMod': SolarWind_U_g2_NoMod,
    'SolarWindVelocity_V_sw': SolarWindVelocity_V_sw,
    'SolarWindVelocity_V_swKmS': SolarWindVelocity_V_swKmS,
    'SolarWindVelocity_U_g2_NoSW': SolarWindVelocity_U_g2_NoSW,
    'StellarMass_M_s': StellarMass_M_s,
    'StellarMass_M_sInMsun': StellarMass_M_sInMsun,
    'ReactorEfficiencyTerm': ReactorEfficiencyTerm,
    'MuSTerm': MuSTerm,
    'GradMsRTerm': GradMsRTerm,
    'BjTerm': BjTerm,
    'OmegaSTTerm': OmegaSTTerm,
    'MuJTerm': MuJTerm,
    'UniversalGravity1Term': UniversalGravity1Term,
    'UniversalGravity2Term': UniversalGravity2Term,
    'UniversalGravity3Term': UniversalGravity3Term,
    'UniversalGravity4Term': UniversalGravity4Term,
    'UniversalBuoyancyTerm': UniversalBuoyancyTerm,
    'UniversalMagnetismTerm': UniversalMagnetismTerm,
    'UniversalAetherTerm': UniversalAetherTerm,
    'UnifiedFieldTerm': UnifiedFieldTerm,
    'MUGECompressedBaseTerm': MUGECompressedBaseTerm,
    'MUGEExpansionTerm': MUGEExpansionTerm,
    'MUGESuperAdjustmentTerm': MUGESuperAdjustmentTerm,
    'MUGEEnvelopeTerm': MUGEEnvelopeTerm,
    'MUGEUgSumTerm': MUGEUgSumTerm,
    'MUGECosmologicalTerm': MUGECosmologicalTerm,
    'MUGEQuantumTerm': MUGEQuantumTerm,
    'MUGEFluidTerm': MUGEFluidTerm,
    'MUGEPerturbationTerm': MUGEPerturbationTerm,
    'MUGEResonanceADPMTerm': MUGEResonanceADPMTerm,
    'MUGEResonanceATHzTerm': MUGEResonanceATHzTerm,
    'MUGEResonanceAvacDiffTerm': MUGEResonanceAvacDiffTerm,
    'MUGEResonanceASuperFreqTerm': MUGEResonanceASuperFreqTerm,
    'MUGEResonanceAAetherResTerm': MUGEResonanceAAetherResTerm,
    'MUGEResonanceUg4iTerm': MUGEResonanceUg4iTerm,
    'MUGEResonanceAQuantumFreqTerm': MUGEResonanceAQuantumFreqTerm,
    'MUGEResonanceAAetherFreqTerm': MUGEResonanceAAetherFreqTerm,
    'MUGEResonanceAFluidFreqTerm': MUGEResonanceAFluidFreqTerm,
    'MUGEResonanceOscTerm': MUGEResonanceOscTerm,
    'MUGEResonanceAExpFreqTerm': MUGEResonanceAExpFreqTerm,
    'MUGEResonanceFTRZTerm': MUGEResonanceFTRZTerm,
    'MUGEResonanceWormholeTerm': MUGEResonanceWormholeTerm,
    'TauLeptonDipoleTerm': TauLeptonDipoleTerm,
    'CKMVcbTerm': CKMVcbTerm,
    'LFVBranchingTerm': LFVBranchingTerm,
    'VectorLikeQuarkTerm': VectorLikeQuarkTerm,
    'UnifiedFieldTerm_SOURCE4': UnifiedFieldTerm_SOURCE4,
    'MUGESuperAdjTerm': MUGESuperAdjTerm,
    'MUGECosmTerm': MUGECosmTerm,
    'SGR1745MagnetarTerm': SGR1745MagnetarTerm,
    'SagittariusAStarTerm': SagittariusAStarTerm,
    'TapestryStarbirthTerm': TapestryStarbirthTerm,
    'Westerlund2ClusterTerm': Westerlund2ClusterTerm,
    'PillarsCreationTerm': PillarsCreationTerm,
    'RingsRelativityTerm': RingsRelativityTerm,
    'StudentGuideUniverseTerm': StudentGuideUniverseTerm,
    'CompressedMUGETerm_SOURCE4': CompressedMUGETerm_SOURCE4,
    'ResonanceMUGETerm_SOURCE4': ResonanceMUGETerm_SOURCE4,
    'SMBHDynamicVacuumTerm_SOURCE82': SMBHDynamicVacuumTerm_SOURCE82,
    'SMBHQuantumCouplingTerm_SOURCE82': SMBHQuantumCouplingTerm_SOURCE82,
    'SMBHMSigmaRelationTerm_SOURCE82': SMBHMSigmaRelationTerm_SOURCE82,
    'SMBHBulgeGravityTerm_SOURCE82': SMBHBulgeGravityTerm_SOURCE82,
    'SMBHUg1Term_SOURCE82': SMBHUg1Term_SOURCE82,
    'SMBHUg2Term_SOURCE82': SMBHUg2Term_SOURCE82,
    'SMBHUg3Term_SOURCE82': SMBHUg3Term_SOURCE82,
    'SMBHUg4Term_SOURCE82': SMBHUg4Term_SOURCE82,
    'SMBHReactorEfficiencyTerm_SOURCE82': SMBHReactorEfficiencyTerm_SOURCE82,
    'SMBHPseudoMonopoleTerm_SOURCE82': SMBHPseudoMonopoleTerm_SOURCE82,
    'SMBHRedshiftCorrectionTerm_SOURCE82': SMBHRedshiftCorrectionTerm_SOURCE82,
    'SMBHUiTerm_SOURCE82': SMBHUiTerm_SOURCE82,
    'SMBHUmTerm_SOURCE82': SMBHUmTerm_SOURCE82,
    'SMBHOmegaSGalacticTerm_SOURCE82': SMBHOmegaSGalacticTerm_SOURCE82,
    'SMBHCosmicTimeTerm_SOURCE82': SMBHCosmicTimeTerm_SOURCE82,
}
